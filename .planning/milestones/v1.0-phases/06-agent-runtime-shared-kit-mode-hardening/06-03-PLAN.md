---
phase: 06-agent-runtime-shared-kit-mode-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - cli/src/cursor/cursorLocalLauncher.ts
  - cli/src/cursor/cursorRemoteLauncher.ts
  - cli/src/agent/sessionBase.ts
  - cli/src/modules/common/launcher/BaseLocalLauncher.ts
  - cli/src/modules/common/remote/RemoteLauncherBase.ts
  - cli/src/agent/localLaunchPolicy.ts
autonomous: true
requirements:
  - REFA-02
must_haves:
  truths:
    - "cursorLocalLauncher and cursorRemoteLauncher both consume permissionModeToCursorArgs from cli/src/agent/modeConfig"
    - "Duplicate inline helper permissionModeToAgentArgs is deleted"
    - "Zero `permissionMode as string` casts remain in either launcher file"
    - "buildAgentArgs is exported from cursorRemoteLauncher.ts so Plan 04 tests can call it"
    - "Four base files carry @implements (Phase 6 SC#1) JSDoc anchors for grep-verifiable concept positions"
  artifacts:
    - path: "cli/src/cursor/cursorLocalLauncher.ts"
      provides: "Local launcher consuming shared modeConfig; no inline mode-mapping helper"
      not_contains: "function permissionModeToCursorArgs"
    - path: "cli/src/cursor/cursorRemoteLauncher.ts"
      provides: "Remote launcher consuming shared modeConfig; exported buildAgentArgs; no inline duplicate"
      not_contains: "permissionModeToAgentArgs"
    - path: "cli/src/agent/sessionBase.ts"
      provides: "AgentSessionBase with @implements SessionContext (Phase 6 SC#1) JSDoc"
      contains: "@implements SessionContext (Phase 6 SC#1)"
    - path: "cli/src/modules/common/launcher/BaseLocalLauncher.ts"
      provides: "BaseLocalLauncher with @implements LocalAdapter (Phase 6 SC#1) JSDoc"
      contains: "@implements LocalAdapter (Phase 6 SC#1)"
    - path: "cli/src/modules/common/remote/RemoteLauncherBase.ts"
      provides: "RemoteLauncherBase with @implements RemoteAdapter (Phase 6 SC#1) JSDoc"
      contains: "@implements RemoteAdapter (Phase 6 SC#1)"
    - path: "cli/src/agent/localLaunchPolicy.ts"
      provides: "localLaunchPolicy with @implements LaunchPolicy (Phase 6 SC#1) JSDoc"
      contains: "@implements LaunchPolicy (Phase 6 SC#1)"
  key_links:
    - from: "cli/src/cursor/cursorLocalLauncher.ts"
      to: "cli/src/agent/modeConfig.ts"
      via: "import permissionModeToCursorArgs"
      pattern: "from '@/agent/modeConfig'"
    - from: "cli/src/cursor/cursorRemoteLauncher.ts"
      to: "cli/src/agent/modeConfig.ts"
      via: "import permissionModeToCursorArgs"
      pattern: "from '@/agent/modeConfig'"
---

<objective>
Slice 3 of Phase 6: collapse the duplicated permission-mode-mapping helpers in both launchers onto the shared `cli/src/agent/modeConfig.ts`, drop the three `permissionMode as string` casts, narrow `applyDisplayMode`'s parameter type, export `buildAgentArgs` for Plan 04 tests, and stamp the four SC#1 base files with JSDoc concept-position anchors.

Purpose: REFA-02 / SC#1 / SC#5 — eliminate mapping duplication; SC#1 — anchor the five-concept shared-kit position via grep.
Output: Both launcher files import the single helper from `@/agent/modeConfig`; `permissionModeToAgentArgs` deleted; `as string` casts removed; `applyDisplayMode` accepts `PermissionMode | undefined`; `buildAgentArgs` exported; four `@implements ... (Phase 6 SC#1)` JSDoc tags landed.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-CONTEXT.md
@.planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-RESEARCH.md
@.planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-PATTERNS.md
@cli/src/cursor/cursorLocalLauncher.ts
@cli/src/cursor/cursorRemoteLauncher.ts
@cli/src/cursor/cursorLocal.ts
@cli/src/agent/sessionBase.ts
@cli/src/agent/localLaunchPolicy.ts
@cli/src/modules/common/launcher/BaseLocalLauncher.ts
@cli/src/modules/common/remote/RemoteLauncherBase.ts

<interfaces>
<!-- Existing identifiers being modified or consumed -->

From cli/src/cursor/cursorLocalLauncher.ts (current — to be removed):
- function permissionModeToCursorArgs(mode?: string): { mode?: 'plan' | 'ask'; yolo?: boolean }   (lines 6-17)
- session.getPermissionMode() as string                                                            (line 24)

From cli/src/cursor/cursorRemoteLauncher.ts (current — to be removed/narrowed):
- function permissionModeToAgentArgs(mode?: string): { mode?: string; yolo?: boolean }             (lines 43-48)
- mode.permissionMode as string                                                                    (lines 97-98)
- private applyDisplayMode(permissionMode: string | undefined): void                               (line ~217)
- function buildAgentArgs(...) — currently NOT exported; Plan 04 test needs export

From cli/src/agent/modeConfig.ts (created in Plan 02):
- export type CursorArgsFragment = { mode?: 'plan' | 'ask'; yolo?: boolean }
- export function permissionModeToCursorArgs(mode: PermissionMode | undefined): CursorArgsFragment   (throws on unknown)

From cli/src/api/types.ts:25 (alias chain — RESEARCH D-100 resolution):
- export type SessionPermissionMode = PermissionMode    // unifies AgentSessionBase.getPermissionMode() return type with cursor PermissionMode without generics or override
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Converge cursorLocalLauncher onto shared modeConfig</name>
  <files>cli/src/cursor/cursorLocalLauncher.ts</files>
  <read_first>
    - cli/src/cursor/cursorLocalLauncher.ts (full — 30-50 lines)
    - cli/src/agent/modeConfig.ts (created by Plan 02)
    - cli/src/cursor/modes.ts (created by Plan 01)
    - .planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-PATTERNS.md (§MODIFIED cursorLocalLauncher.ts — exact diff)
  </read_first>
  <action>
    Delete the inline `function permissionModeToCursorArgs(...)` defined at lines 6-17 entirely. Add `import { permissionModeToCursorArgs } from '@/agent/modeConfig'` near the top of the file. On the call site (currently `permissionModeToCursorArgs(session.getPermissionMode() as string)`), drop the `as string` cast — the imported helper accepts `PermissionMode | undefined` directly because `AgentSessionBase.getPermissionMode()` already returns `SessionPermissionMode | undefined` which is a type alias for `PermissionMode | undefined` (RESEARCH §D-100 resolution; verified `cli/src/api/types.ts:25`). No generic or override changes needed in `sessionBase.ts`. Re-emit the call as `const { mode, yolo } = permissionModeToCursorArgs(session.getPermissionMode())`.
  </action>
  <verify>
    <automated>bun typecheck && bun run --cwd cli test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "function permissionModeToCursorArgs" cli/src/cursor/cursorLocalLauncher.ts` returns NO match.
    - `rg "permissionMode as string" cli/src/cursor/cursorLocalLauncher.ts` returns NO match.
    - `rg "from '@/agent/modeConfig'" cli/src/cursor/cursorLocalLauncher.ts` matches.
    - `bun typecheck` exits 0.
    - `bun run --cwd cli test` exits 0.
  </acceptance_criteria>
  <done>cursorLocalLauncher.ts has zero inline mapping logic, zero `as string` casts on permissionMode, and consumes the shared helper.</done>
</task>

<task type="auto">
  <name>Task 2: Converge cursorRemoteLauncher onto shared modeConfig and export buildAgentArgs</name>
  <files>cli/src/cursor/cursorRemoteLauncher.ts</files>
  <read_first>
    - cli/src/cursor/cursorRemoteLauncher.ts (full — ~250 lines)
    - cli/src/agent/modeConfig.ts (created by Plan 02)
    - cli/src/cursor/modes.ts (created by Plan 01)
    - .planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-PATTERNS.md (§MODIFIED cursorRemoteLauncher.ts — exact diff)
    - .planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-RESEARCH.md (§Code Examples — Recommended cursorRemoteLauncher.ts change + Open Question 3)
  </read_first>
  <action>
    Delete the inline `function permissionModeToAgentArgs(...)` (currently lines 43-48) entirely. Add `import { permissionModeToCursorArgs } from '@/agent/modeConfig'` and `import type { PermissionMode } from './modes'` to the top of the file. At the previous call sites (currently lines 97-98), drop both `permissionMode as string` casts and replace the call with `const { mode: agentMode, yolo } = permissionModeToCursorArgs(mode.permissionMode); this.applyDisplayMode(mode.permissionMode);`. Narrow the `applyDisplayMode` private-method signature from `(permissionMode: string | undefined)` to `(permissionMode: PermissionMode | undefined)` (RESEARCH Open Question 3 — purely type narrowing, no runtime effect; the `displayPermissionMode: string | null` field stays as-is since it stores the last-rendered string label). Add `export` to the existing `function buildAgentArgs(...)` declaration so Plan 04's test can import it directly (RESEARCH §Code Examples Test 3, recommendation A). DO NOT change the `buildAgentArgs` signature beyond adding `export`.
  </action>
  <verify>
    <automated>bun typecheck && bun run --cwd cli test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "function permissionModeToAgentArgs" cli/src/cursor/cursorRemoteLauncher.ts` returns NO match.
    - `rg "permissionMode as string" cli/src/cursor/cursorRemoteLauncher.ts` returns NO match (BOTH casts gone).
    - `rg "from '@/agent/modeConfig'" cli/src/cursor/cursorRemoteLauncher.ts` matches.
    - `rg "from './modes'" cli/src/cursor/cursorRemoteLauncher.ts` matches (PermissionMode type import).
    - `rg "^export function buildAgentArgs|^export\\s.*buildAgentArgs" cli/src/cursor/cursorRemoteLauncher.ts` matches (buildAgentArgs is exported).
    - `rg "applyDisplayMode\\(permissionMode: string \\| undefined\\)" cli/src/cursor/cursorRemoteLauncher.ts` returns NO match (signature narrowed).
    - `bun typecheck` exits 0.
    - `bun run --cwd cli test` exits 0.
  </acceptance_criteria>
  <done>cursorRemoteLauncher.ts has zero inline mapping logic, zero `as string` casts on permissionMode, narrowed `applyDisplayMode` parameter, exported `buildAgentArgs`, and consumes the shared helper.</done>
</task>

<task type="auto">
  <name>Task 3: Stamp four SC#1 base files with @implements JSDoc anchors</name>
  <files>
    cli/src/agent/sessionBase.ts,
    cli/src/modules/common/launcher/BaseLocalLauncher.ts,
    cli/src/modules/common/remote/RemoteLauncherBase.ts,
    cli/src/agent/localLaunchPolicy.ts
  </files>
  <read_first>
    - cli/src/agent/sessionBase.ts (lines 1-40 — confirm class declaration position)
    - cli/src/modules/common/launcher/BaseLocalLauncher.ts (lines 1-40)
    - cli/src/modules/common/remote/RemoteLauncherBase.ts (lines 1-30)
    - cli/src/agent/localLaunchPolicy.ts (full — leaf module)
    - .planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-PATTERNS.md (Pattern E + §MODIFIED entries — exact JSDoc shape)
    - .planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-RESEARCH.md (§Recommended JSDoc tag form)
  </read_first>
  <action>
    Add a JSDoc block immediately above `export class AgentSessionBase` in `cli/src/agent/sessionBase.ts` containing the line `@implements SessionContext (Phase 6 SC#1)` plus a one-paragraph note explaining the concept-position role and that the file is intentionally NOT renamed (CONTEXT D-89). Repeat the pattern with `@implements LocalAdapter (Phase 6 SC#1)` above the class in `cli/src/modules/common/launcher/BaseLocalLauncher.ts`, `@implements RemoteAdapter (Phase 6 SC#1)` above the class in `cli/src/modules/common/remote/RemoteLauncherBase.ts`, and `@implements LaunchPolicy (Phase 6 SC#1)` at the very top of `cli/src/agent/localLaunchPolicy.ts` (D-93 — leaf file body unchanged). Do NOT add any tag to `loopBase.ts` (D-90 enumerates exactly 4 SC#1 concepts; loopBase carries none). Do NOT modify any class body, signature, or import. The grep anchor target is `@implements (SessionContext|LocalAdapter|RemoteAdapter|LaunchPolicy) \(Phase 6 SC#1\)` and must total ≥ 4 hits across `cli/src`.
  </action>
  <verify>
    <automated>bun typecheck && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "@implements SessionContext \\(Phase 6 SC#1\\)" cli/src/agent/sessionBase.ts` returns 1.
    - `rg -c "@implements LocalAdapter \\(Phase 6 SC#1\\)" cli/src/modules/common/launcher/BaseLocalLauncher.ts` returns 1.
    - `rg -c "@implements RemoteAdapter \\(Phase 6 SC#1\\)" cli/src/modules/common/remote/RemoteLauncherBase.ts` returns 1.
    - `rg -c "@implements LaunchPolicy \\(Phase 6 SC#1\\)" cli/src/agent/localLaunchPolicy.ts` returns 1.
    - Total: `rg -c "@implements (SessionContext|LocalAdapter|RemoteAdapter|LaunchPolicy) \\(Phase 6 SC#1\\)" cli/src | awk -F: '{s+=$2} END {print s+0}'` ≥ 4.
    - `bun typecheck` exits 0 (JSDoc-only changes — no compile impact).
    - `bun run test` exits 0 at slice boundary.
    - Slice 3 phase-gate: `rg "permissionModeToAgentArgs" cli/src shared/src hub/src web/src` returns 0 lines AND `rg "permissionMode as string" cli/src/cursor/cursorLocalLauncher.ts cli/src/cursor/cursorRemoteLauncher.ts` returns 0 lines.
  </acceptance_criteria>
  <done>Four SC#1 concept-position JSDoc anchors are in place; existing `bun typecheck && bun run test` green; ripgrep confirms duplicate-helper and as-string-cast residue is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Refactor preserves existing semantics — same launcher entry, same spawn args layout, same RPC surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-NA | — | cli/src/cursor launchers + base file JSDoc | accept | No new threats; refactor preserves existing semantics. Args-array shape unchanged (verified by Plan 04 tests). Type narrowing is compile-time only. JSDoc anchors are documentation. |
</threat_model>

<verification>
- `bun typecheck && bun run test` green at slice boundary (D-107 #3 gate).
- `rg "permissionModeToAgentArgs" cli/src shared/src hub/src web/src` → 0 hits.
- `rg "permissionMode as string" cli/src/cursor/cursorLocalLauncher.ts cli/src/cursor/cursorRemoteLauncher.ts` → 0 hits.
- `rg "@implements (SessionContext|LocalAdapter|RemoteAdapter|LaunchPolicy) \\(Phase 6 SC#1\\)" cli/src` → ≥ 4 hits.
- `npx madge --circular --extensions ts,tsx cli/src/cursor` exits 0 (still clean from Plan 01).
</verification>

<success_criteria>
- Both launcher files consume `permissionModeToCursorArgs` from `@/agent/modeConfig` exclusively; no inline duplicates.
- All `permissionMode as string` casts in launcher files are deleted.
- `applyDisplayMode` parameter narrows to `PermissionMode | undefined`.
- `buildAgentArgs` is exported from `cursorRemoteLauncher.ts` (single-line surface change for Plan 04 tests).
- Four SC#1 JSDoc anchors are grep-verifiable.
- Existing test suite stays green.
</success_criteria>

<output>
Create `.planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-03-SUMMARY.md` when done, recording:
- Diff stats per file.
- ripgrep counts for the three D-108 keywords (helper duplicate, agent-args duplicate, as-string casts) before/after.
- Confirmation that `bun typecheck && bun run test` green.
</output>
