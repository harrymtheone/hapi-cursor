---
phase: 06-agent-runtime-shared-kit-mode-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - cli/src/cursor/cursorRemoteLauncher.test.ts
  - cli/src/cursor/cursorLocalLauncher.test.ts
  - scripts/check-no-cut-agents.sh
  - package.json
autonomous: true
requirements:
  - REFA-02
  - REFA-05
must_haves:
  truths:
    - "Mid-session yolo switch on remote launcher emits --yolo and excludes --mode in turn-2 args"
    - "Mid-session plan ↔ default switch on local launcher emits --mode plan then drops it on revert"
    - "Mid-session plan turn on remote launcher emits --mode plan and excludes --yolo (cross-layer parity)"
    - "scripts/check-no-cut-agents.sh enforces all D-108 keywords and madge --circular cli/src/cursor exit 0 as a single guard"
    - "Existing 532-test suite stays green; no regressions"
  artifacts:
    - path: "cli/src/cursor/cursorRemoteLauncher.test.ts"
      provides: "Mid-session yolo + plan switch coverage via direct buildAgentArgs export"
      contains: "buildAgentArgs"
    - path: "cli/src/cursor/cursorLocalLauncher.test.ts"
      provides: "Mid-session plan switch on local via mocked spawnWithTerminalGuard"
      contains: "spawnWithTerminalGuard"
    - path: "scripts/check-no-cut-agents.sh"
      provides: "Phase 6 ripgrep + madge guard block"
      contains: "Phase-6"
  key_links:
    - from: "cli/src/cursor/cursorRemoteLauncher.test.ts"
      to: "cli/src/cursor/cursorRemoteLauncher.ts"
      via: "import { buildAgentArgs }"
      pattern: "buildAgentArgs"
    - from: "scripts/check-no-cut-agents.sh"
      to: "madge --circular --extensions ts,tsx cli/src/cursor"
      via: "npx invocation"
      pattern: "madge --circular"
---

<objective>
Slice 4 of Phase 6: land the three new tests (D-104), extend the existing source-guard script with the four Phase 6 ripgrep keyword sweeps + the madge-circular exit-code guard (D-108), optionally pin `madge` as a root devDependency for deterministic CI, and confirm the full phase gate (`bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh`) passes.

Purpose: REFA-02 + REFA-05 phase-level guard — make the prior three slices' invariants enforceable from CI; mitigate the threat of misclassified-mode-as-trusted-input being silently re-introduced by future contributors.
Output: 3 new test cases (modeConfig unit was Plan 02; this plan adds remote + local launcher mid-session tests); extended `scripts/check-no-cut-agents.sh` with Phase 6 block; `madge` devDep added if not already present; full phase gate green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-CONTEXT.md
@.planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-RESEARCH.md
@.planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-PATTERNS.md
@.planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-VALIDATION.md
@cli/src/cursor/cursorRemoteLauncher.ts
@cli/src/cursor/cursorLocal.ts
@cli/src/runner/buildCliArgs.test.ts
@cli/src/cursor/utils/cursorEventConverter.test.ts
@scripts/check-no-cut-agents.sh
@package.json

<interfaces>
<!-- Identifiers consumed by Plan 04 tests + guard -->

From cli/src/cursor/cursorRemoteLauncher.ts (after Plan 03):
- export function buildAgentArgs(opts: { message: string; cwd: string; sessionId?: string | null; mode?: 'plan' | 'ask'; yolo?: boolean }): string[]

From cli/src/agent/modeConfig.ts (after Plan 02):
- export function permissionModeToCursorArgs(mode: PermissionMode | undefined): CursorArgsFragment

From cli/src/cursor/cursorLocal.ts (Plan 04 test target):
- export async function cursorLocal(opts: { abort: AbortSignal; chatId: string | null; path: string; mode?: 'plan' | 'ask'; yolo?: boolean })   (signature anchor — verify exact shape during read_first)
- internally calls spawnWithTerminalGuard from '@/utils/spawnWithTerminalGuard'

From scripts/check-no-cut-agents.sh:
- existing Phase-3 / Phase-4 / Phase-5 inline blocks at the documented line ranges (Phase-5 at lines 124-134) — Phase-6 block follows the same `if rg pattern; then exit 1; fi; echo ✅` shape
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cursorRemoteLauncher.test.ts — mid-session yolo + plan switch</name>
  <files>cli/src/cursor/cursorRemoteLauncher.test.ts</files>
  <read_first>
    - cli/src/cursor/cursorRemoteLauncher.ts (confirm buildAgentArgs is exported from Plan 03)
    - cli/src/agent/modeConfig.ts (created by Plan 02)
    - cli/src/runner/buildCliArgs.test.ts (analog args-shape test pattern)
    - .planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-PATTERNS.md (§NEW cursorRemoteLauncher.test.ts)
    - .planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-RESEARCH.md (§Code Examples Test 3)
  </read_first>
  <behavior>
    - turn 1 with default + turn 2 with yolo: turn-2 args contain '--yolo' and do NOT contain '--mode'
    - turn 1 with default: args contain neither '--yolo' nor '--mode'
    - plan turn: args contain '--mode' followed by 'plan' and do NOT contain '--yolo'
  </behavior>
  <action>
    Create cli/src/cursor/cursorRemoteLauncher.test.ts using the `buildCliArgs.test.ts` idiom: `import { describe, it, expect } from 'vitest'`; `import { buildAgentArgs } from './cursorRemoteLauncher'`; `import { permissionModeToCursorArgs } from '@/agent/modeConfig'`. Two `it()` cases — (a) "args contain --yolo and not --mode after switching from default to yolo" computing `frag1 = permissionModeToCursorArgs('default')` then `args1 = buildAgentArgs({ message: 'hi', cwd: '/tmp', sessionId: 's1', ...frag1 })` asserting neither flag, then `frag2 = permissionModeToCursorArgs('yolo')`, `args2 = buildAgentArgs({ message: 'continue', cwd: '/tmp', sessionId: 's1', ...frag2 })` asserting `args2` contains '--yolo' and does not contain '--mode'; and (b) "args contain --mode plan and not --yolo for plan turn" computing `frag = permissionModeToCursorArgs('plan')`, `args = buildAgentArgs({ message: 'q', cwd: '/tmp', sessionId: null, ...frag })` asserting `expect(args).toEqual(expect.arrayContaining(['--mode', 'plan']))` and not '--yolo'. 4-space indent; vitest globals = false (per cli/vitest.config.ts) so import describe/it/expect explicitly. Do NOT mock `node:child_process.spawn`; the test asserts on the args array directly via the Plan-03 export.
  </action>
  <verify>
    <automated>bun run --cwd cli test -- --run src/cursor/cursorRemoteLauncher.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File cli/src/cursor/cursorRemoteLauncher.test.ts exists.
    - `rg "import \\{ buildAgentArgs \\} from './cursorRemoteLauncher'" cli/src/cursor/cursorRemoteLauncher.test.ts` matches.
    - `rg "expect\\(args2\\)\\.toContain\\('--yolo'\\)" cli/src/cursor/cursorRemoteLauncher.test.ts` matches.
    - `rg "expect\\(args2\\)\\.not\\.toContain\\('--mode'\\)" cli/src/cursor/cursorRemoteLauncher.test.ts` matches.
    - File contains at least 2 `it(` invocations.
    - `bun run --cwd cli test -- --run src/cursor/cursorRemoteLauncher.test.ts` exits 0.
  </acceptance_criteria>
  <done>Mid-session yolo + plan switch coverage on remote launcher passes; SC#4 "yolo + remote" anchor green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cursorLocalLauncher.test.ts — mid-session plan switch on local (mock spawnWithTerminalGuard)</name>
  <files>cli/src/cursor/cursorLocalLauncher.test.ts</files>
  <read_first>
    - cli/src/cursor/cursorLocal.ts (confirm exact `cursorLocal()` signature + that it calls spawnWithTerminalGuard with `args` field)
    - cli/src/utils/spawnWithTerminalGuard.ts or wherever spawnWithTerminalGuard is exported (mock target path resolution; researcher used '@/utils/spawnWithTerminalGuard')
    - cli/src/cursor/utils/cursorEventConverter.test.ts (vitest baseline; vi.mock pattern reference)
    - cli/src/runner/buildCliArgs.test.ts (args-shape assertion idiom)
    - .planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-PATTERNS.md (§NEW cursorLocalLauncher.test.ts)
    - .planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-RESEARCH.md (§Code Examples Test 2)
  </read_first>
  <behavior>
    - First call with mode='plan': spawnWithTerminalGuard is invoked once and its args array contains '--mode' and 'plan'
    - Second call without mode: spawnWithTerminalGuard's args array does NOT contain '--mode'
  </behavior>
  <action>
    Create cli/src/cursor/cursorLocalLauncher.test.ts. Mock `@/utils/spawnWithTerminalGuard` at module-load time using `vi.mock('@/utils/spawnWithTerminalGuard', () => ({ spawnWithTerminalGuard: vi.fn(async () => {}) }))`. Import `cursorLocal` from `./cursorLocal` (test exercises the args-layer at the cursorLocal layer rather than the BaseLocalLauncher machinery — RESEARCH §Code Examples Test 2 note). Use `beforeEach(() => vi.clearAllMocks())`. Single `it()` case "emits --mode plan then no --mode flag when switched back to default": call `cursorLocal({ abort: ctrl.signal, chatId: null, path: '/tmp', mode: 'plan' })`, assert `vi.mocked(spawnWithTerminalGuard).mock.calls[0][0].args` contains `['--mode', 'plan']`; then call `cursorLocal({ abort: ctrl.signal, chatId: null, path: '/tmp' })` (no mode), assert `vi.mocked(spawnWithTerminalGuard).mock.calls[1][0].args` does NOT contain '--mode'. If during read_first the actual `cursorLocal` signature differs (e.g., requires additional fields like `cwd`), adjust the call options to match the real signature while keeping the assertion semantics — the planner intent is to verify args-array shape across two sequential turns. Use vitest's `vi`, `describe`, `it`, `expect`, `beforeEach` imports explicitly (globals = false).
  </action>
  <verify>
    <automated>bun run --cwd cli test -- --run src/cursor/cursorLocalLauncher.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File cli/src/cursor/cursorLocalLauncher.test.ts exists.
    - `rg "vi.mock\\('@/utils/spawnWithTerminalGuard'" cli/src/cursor/cursorLocalLauncher.test.ts` matches.
    - `rg "mock.calls\\[0\\]\\[0\\].args" cli/src/cursor/cursorLocalLauncher.test.ts` matches.
    - `rg "expect\\(.*\\)\\.not\\.toContain\\('--mode'\\)" cli/src/cursor/cursorLocalLauncher.test.ts` matches.
    - `bun run --cwd cli test -- --run src/cursor/cursorLocalLauncher.test.ts` exits 0.
  </acceptance_criteria>
  <done>Mid-session plan ↔ default switch on local launcher passes via mocked spawnWithTerminalGuard; SC#4 plan anchor green.</done>
</task>

<task type="auto">
  <name>Task 3: Extend scripts/check-no-cut-agents.sh with Phase 6 block + madge guard; pin madge devDep</name>
  <files>scripts/check-no-cut-agents.sh, package.json</files>
  <read_first>
    - scripts/check-no-cut-agents.sh (full — confirm Phase-5 block at lines 124-134 and the existing rg invocation pattern)
    - package.json (root — verify whether `madge` is already in devDependencies)
    - .planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-PATTERNS.md (§MODIFIED scripts/check-no-cut-agents.sh — exact block to append)
    - .planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-RESEARCH.md (§Pitfall 1 — `--extensions ts,tsx` is non-negotiable; §Open Question 1 — pin madge devDep)
    - .gitignore (confirm the fixture filename `.phase6-guard-fixture.txt` will not be silently committed if the cleanup step is skipped — if the file is not gitignored, the cleanup step is the only safeguard, so the implementer must run it)
  </read_first>
  <action>
    Append a new "Phase-6 ripgrep sweeps + madge guard" block at the end of `scripts/check-no-cut-agents.sh`, BEFORE any final `exit 0`. The block must enforce four invariants and one anchor count, in this order:

    (1) `permissionModeToAgentArgs` zero-tolerance across `cli/src shared/src hub/src web/src` — exit 1 with a clear error message if any hit; the error must instruct contributors to use `cli/src/agent/modeConfig.permissionModeToCursorArgs` instead.

    (2) `permissionMode as string` zero-tolerance scoped to the two launcher files (`cli/src/cursor/cursorLocalLauncher.ts cli/src/cursor/cursorRemoteLauncher.ts`) — exit 1 if any hit; error must reference `permissionModeToCursorArgs` accepting `PermissionMode | undefined` directly.

    (3) `permissionModeToCursorArgs` definition lives only in `cli/src/agent/modeConfig.ts` — strict variant: `rg -n '^export function permissionModeToCursorArgs|^function permissionModeToCursorArgs' cli/src shared/src hub/src web/src | grep -v 'cli/src/agent/modeConfig\\.ts'` must produce empty output; exit 1 otherwise.

    (4) `madge --circular --extensions ts,tsx cli/src/cursor` must exit 0; invoke via `npx --no-install madge` once `madge` is added to root devDependencies (next bullet) — exit 1 with an error pointing the contributor at the likely cause (a new import from `cli/src/cursor/modes.ts` to `loop`/`session`/`launcher`).

    (5) `@implements (SessionContext|LocalAdapter|RemoteAdapter|LaunchPolicy) \\(Phase 6 SC#1\\)` anchor count over `cli/src` must be ≥ 4; exit 1 with a clear message otherwise (CRITICAL: use `grep -c` on filtered files to avoid the comment-self-invalidation pitfall called out in <task_breakdown> — the pattern is unique enough that the bare `awk -F: '{s+=$2} END {print s+0}'` aggregation across `rg -c ...` output is safe; do NOT add a `grep -v '^#'` filter that would strip the JSDoc lines themselves). Each ✅ confirmation echoes after a passing check, matching the existing Phase-5 block voice.

    DO NOT extend the existing `WHITELIST` / `PHASE4_WHITELIST` arrays — selectivity comes from scoping the search to `cli/src` and the strict-variant `grep -v` exclusion (D-109).

    In root `package.json`: if `madge` is NOT already under root `devDependencies`, add the latest stable version. If `madge` is already declared in any workspace, prefer pinning it at the root devDependencies level so `npx --no-install madge` resolves deterministically (Open Question 1). Do NOT change any other dependency.

    Adversarial proof — fixture-based, NOT mutate-and-revert. The earlier draft of this task asked for a temporary mutation of `cli/src/cursor/cursorRemoteLauncher.ts` to inject a fake `permissionModeToAgentArgs` and revert it; this is forbidden because a partial-write or process-kill mid-revert leaves the working tree dirty and breaks subsequent waves. Instead, prove the guard fails-closed using a temporary fixture file OUTSIDE `cli/src/cursor`:

      (i) Create `scripts/.phase6-guard-fixture.txt` (path is outside any source tree the production guard scopes to) containing the exact line `function permissionModeToAgentArgs(){}` plus a literal `permissionMode as string` line.
      (ii) Invoke the guard's keyword regexes against an explicit include-set that adds the fixture, e.g.:
            `rg -n 'permissionModeToAgentArgs' cli/src shared/src hub/src web/src scripts/.phase6-guard-fixture.txt`
            (or run a `bash -c` that pipes the fixture content into a `rg` invocation reading from stdin via `--no-filename` — whichever the implementer finds cleanest).
            Confirm the regex matches the fixture (proving the pattern itself is correct), then run the production guard (`bash scripts/check-no-cut-agents.sh`) WITHOUT the fixture in scope to confirm exit 0.
      (iii) Delete `scripts/.phase6-guard-fixture.txt` immediately after the assertion. Add the fixture path to a `.gitignore`-equivalent ad-hoc `git check-ignore` confirmation OR simply rely on its `.phase6-guard-fixture.txt` filename being unmistakable and removed before commit.

    No `cli/src/cursor/*.ts` file may be modified, written, or reverted by this task. The working tree must remain clean (`git status --porcelain` shows no `cli/src/cursor/*.ts` entries) at task completion.
  </action>
  <verify>
    <automated>bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "Phase-6" scripts/check-no-cut-agents.sh` ≥ 1.
    - `rg "PHASE6_DUPLICATE_HELPER" scripts/check-no-cut-agents.sh` matches OR an equivalent `permissionModeToAgentArgs` rg invocation exists in the new block.
    - `rg "permissionMode as string" scripts/check-no-cut-agents.sh` matches (the pattern literal in the guard).
    - `rg "madge --circular --extensions ts,tsx cli/src/cursor" scripts/check-no-cut-agents.sh` matches.
    - `rg "@implements" scripts/check-no-cut-agents.sh` matches (the anchor-count check).
    - `bash scripts/check-no-cut-agents.sh` exits 0 (all four ripgrep keywords clean and madge clean).
    - `npx --no-install madge --circular --extensions ts,tsx cli/src/cursor` exits 0 (deterministic — confirms madge is resolved from local node_modules if added to devDeps).
    - Phase gate: `bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh` exits 0.
    - Adversarial sanity (fixture-based, NO source-file mutation): create `scripts/.phase6-guard-fixture.txt` containing `function permissionModeToAgentArgs(){}` and a `permissionMode as string` line; run the guard's keyword regexes against an explicit include-set that adds the fixture; confirm the regex MATCHES the fixture content (pattern is correct), then run the production guard `bash scripts/check-no-cut-agents.sh` (without the fixture in scope) and confirm exit 0; delete `scripts/.phase6-guard-fixture.txt`. NO `cli/src/cursor/*.ts` file is modified at any point. Working-tree check: `git status --porcelain cli/src/cursor` returns empty AND `ls scripts/.phase6-guard-fixture.txt 2>/dev/null` returns nothing before commit. (Document the fixture-based result in the task SUMMARY.)
  </acceptance_criteria>
  <done>The Phase 6 source-guard block is operational, all four D-108 keywords + madge + JSDoc anchor count are enforced from CI, `madge` is reachable via `npx --no-install`, and the full phase gate (`bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh`) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future contributor → cli runtime | A future PR could re-introduce a duplicate `permissionModeToAgentArgs` helper, a new silent-fallback branch on unknown mode, an `as string` cast that launders unsanitized RPC payload, or an import that recreates the `loop ↔ session ↔ launcher` cycle. The guard script is the only line of defense against this regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | RPC mode payload → cli launcher args | mitigate | Plan 02 already replaced the bare `Error('Invalid permission mode')` with typed `UnknownPermissionModeError`. This plan's Task 1+2 tests assert that args-array shape on each turn matches the strict mapping — preventing a misclassified-mode-as-trusted-input regression where, e.g., an unknown `'bypassPermissions'` legacy literal would silently pass through and produce an empty fragment instead of throwing. |
| T-06-04-02 | Tampering | scripts/check-no-cut-agents.sh — guard bypass | mitigate | Task 3 binds the four D-108 ripgrep keywords + madge + JSDoc anchor count into the existing source-guard script. `--extensions ts,tsx` is mandatory (Pitfall 1: madge silently exits 0 without it). Anchor count ≥ 4 prevents accidental JSDoc deletion during future renames. |
| T-06-04-03 | Tampering | Future cycle re-introduction | mitigate | Madge invocation in the guard fails closed; researcher Pitfall 3 also recommends the in-source LEAF MODULE comment header on `cli/src/cursor/modes.ts` (already required by Plan 01) as a second hint. |
</threat_model>

<verification>
- `bun typecheck` exits 0.
- `bun run test` exits 0 (full suite, including the two new launcher test files).
- `bash scripts/check-no-cut-agents.sh` exits 0.
- `npx --no-install madge --circular --extensions ts,tsx cli/src/cursor` exits 0.
- `rg "permissionModeToAgentArgs" cli/src shared/src hub/src web/src` returns 0 matches.
- `rg "permissionMode as string" cli/src/cursor/cursorLocalLauncher.ts cli/src/cursor/cursorRemoteLauncher.ts` returns 0 matches.
- Total `rg -c "@implements (SessionContext|LocalAdapter|RemoteAdapter|LaunchPolicy) \\(Phase 6 SC#1\\)" cli/src` aggregate ≥ 4.
- 3 net-new tests added across phases 02 (modeConfig unit, 6 cases) and 04 (remote 2 cases, local 1 case) — total ≥ 9 new test cases / 3 new test files.
</verification>

<success_criteria>
- Phase 6 phase gate (`bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh && npx --no-install madge --circular --extensions ts,tsx cli/src/cursor`) is green end-to-end.
- All five SC#1–#5 ROADMAP success criteria are satisfied (single shared kit, zero madge cycles, typed UnknownPermissionModeError, mid-session yolo + plan tests, ripgrep finds zero copy-paste).
- The guard is fail-closed: any future regression on the four D-108 keywords or madge cycle count surfaces as a non-zero exit code in CI.
- Existing `bun run test` suite remains at the Phase 5 baseline (532 tests + new additions; no skips, no deletions).
</success_criteria>

<output>
Create `.planning/phases/06-agent-runtime-shared-kit-mode-hardening/06-04-SUMMARY.md` when done, recording:
- Final test count (Phase 5 baseline + new cases).
- Final ripgrep counts for all four D-108 keywords (expected: 0 / 0 / 0 / single-file definition).
- madge output (expected: 0 cycles).
- Adversarial guard self-test result (fixture-based: fixture file `scripts/.phase6-guard-fixture.txt` created with the forbidden tokens; guard regexes matched the fixture content; production `bash scripts/check-no-cut-agents.sh` exited 0 without the fixture in scope; fixture deleted; NO `cli/src/cursor/*.ts` file mutated at any point).
- Confirmation that phase gate is green.
</output>
