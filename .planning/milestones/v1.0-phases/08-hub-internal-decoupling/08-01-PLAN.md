---
phase: 08-hub-internal-decoupling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hub/src/sync/sessionCache.ts
  - hub/src/sync/sessionRepository.ts
  - hub/src/sync/sessionLivenessService.ts
  - hub/src/sync/sessionConfigService.ts
  - hub/src/sync/sessionMergeService.ts
  - hub/src/sync/sessionRepository.test.ts
  - hub/src/sync/sessionLivenessService.test.ts
  - hub/src/sync/sessionConfigService.test.ts
  - hub/src/sync/sessionMergeService.test.ts
  - hub/src/sync/sessionCache.test.ts
  - hub/src/sync/sessionModel.test.ts
  - hub/src/sync/aliveEvents.test.ts
autonomous: true
requirements: [REFH-01]
must_haves:
  truths:
    - "SC#1 (D-129): SessionCache replaced by sessionRepository / sessionLivenessService / sessionConfigService / sessionMergeService; no file in hub/src/sync/ exceeds 400 lines"
    - "D-129: SessionCache class name and public method signatures unchanged — callers do not change imports"
    - "D-129: sessionRepository is the only holder of the Store session handle; liveness/config/merge access Store only via repository"
    - "D-131: sessionMergeService.mergeSessionData retains existing Store.transaction boundary — no withTransaction abstraction introduced"
    - "SC#5: bun typecheck exits 0 and cd hub && bun test exits 0 after slice 1"
  artifacts:
    - path: "hub/src/sync/sessionRepository.ts"
      provides: "SessionRepository class — getters, refreshSession, reloadAll, resolveSessionAccess, narrow Store setters"
    - path: "hub/src/sync/sessionLivenessService.ts"
      provides: "SessionLivenessService class — handleSessionAlive, markMessageQueued, applyBackgroundTaskDelta, recordSessionActivity, handleSessionEnd, expireInactive"
    - path: "hub/src/sync/sessionConfigService.ts"
      provides: "SessionConfigService class — applySessionConfig, renameSession, deleteSession"
    - path: "hub/src/sync/sessionMergeService.ts"
      provides: "SessionMergeService class — mergeSessions, mergeSessionHistory, deduplicateByAgentSessionId; mergeSessionData stays private"
    - path: "hub/src/sync/sessionCache.ts"
      provides: "Thin SessionCache facade composing the 4 services; public method signatures preserved"
  key_links:
    - from: "hub/src/sync/sessionCache.ts"
      to: "sessionRepository.ts / sessionLivenessService.ts / sessionConfigService.ts / sessionMergeService.ts"
      via: "constructor composition + per-method delegate"
      pattern: "this\\.(repository|liveness|config|merge)\\."
    - from: "hub/src/sync/syncEngine.ts"
      to: "hub/src/sync/sessionCache.ts"
      via: "new SessionCache(store, this.eventPublisher) at syncEngine.ts:71 — unchanged"
      pattern: "new SessionCache\\("
---

<objective>
Split the 774-line `hub/src/sync/sessionCache.ts` god-class into four single-responsibility services (`sessionRepository`, `sessionLivenessService`, `sessionConfigService`, `sessionMergeService`), and retrofit `SessionCache` as a thin facade that composes them. Redistribute the existing test cases from `sessionCache.test.ts` / `sessionModel.test.ts` / `aliveEvents.test.ts` into per-service test files; leave the original `sessionCache.test.ts` reduced to facade-level smoke.

Purpose: REFH-01 — first file split of Phase 8. The 4-service decomposition mechanically lifts already-clustered method groups; preserving the facade keeps every caller (SyncEngine at `syncEngine.ts:71`, socket handlers, routes) unchanged. This unlocks the SyncEngine sub-facade work in Plan 08-02 by lowering `hub/src/sync/` file sizes below the SC#1 400-line budget.

Output: 4 new service files, 4 new per-service test files, a slimmed `sessionCache.ts` facade, redistributed test cases, and `bun typecheck` + `cd hub && bun test` green at the end of the plan.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hub-internal-decoupling/08-CONTEXT.md
@.planning/phases/08-hub-internal-decoupling/08-RESEARCH.md

@hub/src/sync/sessionCache.ts
@hub/src/sync/syncEngine.ts

<interfaces>
<!-- Method bucket map per 08-RESEARCH.md §"SessionCache Method Inventory" — copy bucket assignments verbatim. -->

`sessionCache.ts` public methods → service buckets:

- repository: `getSessions`, `getSession`, `resolveSessionAccess` (overloaded), `getActiveSessions`, `getOrCreateSession` (overloaded), `refreshSession`, `reloadAll`, plus narrow Store setters (`setSessionModel`, `setSessionModelReasoningEffort`, `setSessionEffort`, `updateSessionMetadata`, `setSessionTodos`, `setSessionTeamState`, `updateSessionAgentState`, `deleteSession`).
- liveness: `handleSessionAlive`, `markMessageQueued`, `applyBackgroundTaskDelta`, `recordSessionActivity`, `handleSessionEnd`, `expireInactive`.
- config: `applySessionConfig`, `renameSession`, `deleteSession` (config-level delete that emits `session-removed` event).
- merge: public `mergeSessions`, `mergeSessionHistory`, `deduplicateByAgentSessionId`; private `mergeSessionData`, `mergeSessionMetadata`, `mergeAgentState`, `extractAgentSessionId` stay private/non-exported inside `sessionMergeService.ts`.

SessionCache facade composition (from 08-RESEARCH.md §"Service composition skeleton inside the new `sessionCache.ts` facade"):

```typescript
export class SessionCache {
    private readonly repository: SessionRepository
    private readonly liveness: SessionLivenessService
    private readonly config: SessionConfigService
    private readonly merge: SessionMergeService

    constructor(store: Store, publisher: EventPublisher) {
        this.repository = new SessionRepository(store, publisher)
        this.liveness = new SessionLivenessService(this.repository, publisher)
        this.config = new SessionConfigService(this.repository, publisher)
        this.merge = new SessionMergeService(this.repository, publisher)
    }
    // each existing public method becomes one delegate line
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 service files + retrofit SessionCache as facade</name>
  <read_first>
    - hub/src/sync/sessionCache.ts (full file — 774 lines; source of every method body to redistribute)
    - hub/src/sync/syncEngine.ts (read lines 60–95 for the single production construction site `new SessionCache(store, this.eventPublisher)` and to confirm public method signatures preserved)
    - hub/src/sync/eventPublisher.ts (read to know `EventPublisher` import path used by services)
    - .planning/phases/08-hub-internal-decoupling/08-RESEARCH.md §"SessionCache Method Inventory" + §"Pitfall 3: mergeSessionData private" + §"Pitfall 4: per-call recordSessionActivity injection"
  </read_first>
  <action>
Create 4 new service files in `hub/src/sync/` and rewrite `sessionCache.ts` as a thin facade.

1. `hub/src/sync/sessionRepository.ts` — new file. Class `SessionRepository`. Constructor `(store: Store, publisher: EventPublisher)`. Owns the **only** `Store` reference among the four services (D-129). Move method bodies for `getSessions`, `getSession`, `resolveSessionAccess` (preserve overload signatures verbatim from sessionCache.ts:33–43), `getActiveSessions`, `getOrCreateSession` (preserve overloads from :49–63), `refreshSession`, `reloadAll`. Add narrow Store-setter passthroughs that the other services will call (`setSessionModel`, `setSessionModelReasoningEffort`, `setSessionEffort`, `updateSessionMetadata`, `setSessionTodos`, `setSessionTeamState`, `updateSessionAgentState`, `deleteSession`); planner discretion: alternatively expose a `getStore(): Store` accessor — pick the accessor form if the per-setter list grows past ~8 wrappers; document the chosen form at the top of the file.

2. `hub/src/sync/sessionLivenessService.ts` — new file. Class `SessionLivenessService`. Constructor `(repository: SessionRepository, publisher: EventPublisher)`. Move method bodies for `handleSessionAlive` (from :150), `markMessageQueued` (:247), `applyBackgroundTaskDelta` (:274), `recordSessionActivity` (:290), `handleSessionEnd` (:323), `expireInactive` (:342). Replace `this.store.*` calls with `this.repository.*` (or `this.repository.getStore().*`). Per D-131 do **NOT** introduce a `withTransaction` helper. Note: the `inactivityTimer` `setInterval` at `syncEngine.ts:81` is **not** touched in this plan; it stays in syncEngine.ts and continues to call `sessionCache.expireInactive()` (which now delegates to liveness). Timer-to-scheduler migration is Plan 08-02.

3. `hub/src/sync/sessionConfigService.ts` — new file. Class `SessionConfigService`. Constructor `(repository: SessionRepository, publisher: EventPublisher)`. Move method bodies for `applySessionConfig` (:359), `renameSession` (:425), `deleteSession` (:452). Access Store via `this.repository`.

4. `hub/src/sync/sessionMergeService.ts` — new file. Class `SessionMergeService`. Constructor `(repository: SessionRepository, publisher: EventPublisher)`. Move method bodies for public `mergeSessions` (:475), `mergeSessionHistory` (:479), `deduplicateByAgentSessionId` (:691). Move `mergeSessionData` (:490), `mergeSessionMetadata` (:617), `mergeAgentState` (:661), `extractAgentSessionId` (:684) as **private** methods inside the class (Pitfall 3 — keep private). Move the coalescing `deduplicateInProgress` / `deduplicatePending` `Set<string>` fields (sessionCache.ts:14–15) into this service. Existing `Store.transaction(...)` call inside `mergeSessionData` stays inside this file unchanged (D-131).

5. Rewrite `hub/src/sync/sessionCache.ts` as a thin facade per the skeleton in `<interfaces>` above. Constructor signature `(store: Store, publisher: EventPublisher)` unchanged. Every existing public method becomes a single delegate line to `this.repository.X()` / `this.liveness.X()` / `this.config.X()` / `this.merge.X()`. Public method signatures **must match the originals byte-for-byte** including overloads of `resolveSessionAccess` and `getOrCreateSession` — verify by side-by-side diff against the pre-split file. Per D-130 the class name stays `SessionCache`. Per Pitfall 4 keep `recordSessionActivity` as a thin delegate to `this.liveness.recordSessionActivity(...)` so the constructor-time callback at `syncEngine.ts:77` still resolves.

Update imports in callers only if a type re-export was lost in the move (e.g., if `Session` or `SessionListener` was re-exported from `sessionCache.ts`). Do NOT touch callers otherwise (SyncEngine, socket handlers, routes).

Add a top-of-file JSDoc to `sessionCache.ts` stating "Facade: see sessionRepository / sessionLivenessService / sessionConfigService / sessionMergeService for implementation. Class name preserved per D-130."
  </action>
  <acceptance_criteria>
    - File `hub/src/sync/sessionRepository.ts` exists with `export class SessionRepository`.
    - File `hub/src/sync/sessionLivenessService.ts` exists with `export class SessionLivenessService`.
    - File `hub/src/sync/sessionConfigService.ts` exists with `export class SessionConfigService`.
    - File `hub/src/sync/sessionMergeService.ts` exists with `export class SessionMergeService`.
    - `rg "private readonly store" hub/src/sync/sessionLivenessService.ts hub/src/sync/sessionConfigService.ts hub/src/sync/sessionMergeService.ts` returns 0 lines (D-129: only sessionRepository holds Store).
    - `wc -l hub/src/sync/sessionCache.ts` < 400.
    - `wc -l hub/src/sync/sessionRepository.ts` < 400.
    - `wc -l hub/src/sync/sessionLivenessService.ts` < 400.
    - `wc -l hub/src/sync/sessionConfigService.ts` < 400.
    - `wc -l hub/src/sync/sessionMergeService.ts` < 400.
    - `rg "new SessionCache\(|class SessionCache" hub/src/ --glob '!**/*.test.ts'` returns exactly 2 lines (the class declaration in sessionCache.ts + the construction at syncEngine.ts:71) — D-143 #2.
    - `rg "export (async )?function mergeSessionData|public mergeSessionData|mergeSessionData\("  hub/src/sync/sessionMergeService.ts` shows `mergeSessionData` declared without `public` / `export` (private) — Pitfall 3.
    - `rg "withTransaction" hub/src/sync/` returns 0 lines — D-131.
    - `bun typecheck` exits 0.
    - `cd hub && bun test src/sync/sessionCache.test.ts src/sync/sessionModel.test.ts src/sync/aliveEvents.test.ts` exits 0 (existing tests still pass against the facade).
  </acceptance_criteria>
  <verify>
    <automated>bun typecheck && cd hub && bun test src/sync/sessionCache.test.ts src/sync/sessionModel.test.ts src/sync/aliveEvents.test.ts</automated>
  </verify>
  <done>SessionCache split into 4 services + thin facade; all original method signatures preserved; existing tests pass; per-file budget under 400 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Redistribute test cases into 4 per-service test files</name>
  <read_first>
    - hub/src/sync/sessionCache.test.ts (full file — facade-construction cases live here)
    - hub/src/sync/sessionModel.test.ts (full file — 21 SessionCache construction sites per 08-RESEARCH.md "SessionCache Construction Sites" table)
    - hub/src/sync/aliveEvents.test.ts (full file — 6 SessionCache construction sites; alive/end/idle paths)
    - hub/src/sync/sessionRepository.ts, sessionLivenessService.ts, sessionConfigService.ts, sessionMergeService.ts (created in Task 1 — to know public method names tests should target)
    - .planning/phases/08-hub-internal-decoupling/08-RESEARCH.md §"Wave 0 Gaps"
  </read_first>
  <action>
Redistribute test cases from the 3 existing test files into 4 new per-service test files, then reduce the originals to facade-level smoke.

1. Create `hub/src/sync/sessionRepository.test.ts` — move all cases that exercise getter / refresh / reload / resolveSessionAccess / getOrCreateSession behavior from `sessionModel.test.ts`. Construct directly via `new SessionRepository(new Store(':memory:'), createPublisher(events))` (not via SessionCache).

2. Create `hub/src/sync/sessionLivenessService.test.ts` — move all cases that exercise `handleSessionAlive`, `markMessageQueued`, `applyBackgroundTaskDelta`, `recordSessionActivity`, `handleSessionEnd`, `expireInactive` behavior — primarily from `aliveEvents.test.ts` and the alive/end-event cases in `sessionModel.test.ts`. Construct via `new SessionLivenessService(new SessionRepository(...), publisher)`.

3. Create `hub/src/sync/sessionConfigService.test.ts` — move cases that exercise `applySessionConfig`, `renameSession`, `deleteSession` from `sessionModel.test.ts`.

4. Create `hub/src/sync/sessionMergeService.test.ts` — move cases that exercise `mergeSessions`, `mergeSessionHistory`, `deduplicateByAgentSessionId` — primarily merge / dedup cases from `sessionModel.test.ts`.

5. Reduce `hub/src/sync/sessionCache.test.ts` to facade-level smoke only: keep at most a handful of cases that prove the facade delegates correctly (one per service bucket: a getter, an alive event, a config write, a merge). Drop cases that have moved to per-service files.

6. Reduce `hub/src/sync/sessionModel.test.ts` likewise — keep only facade-shape regressions (e.g., end-to-end session lifecycle case that crosses multiple services). All single-service cases must have moved.

7. Reduce `hub/src/sync/aliveEvents.test.ts` likewise — once alive/end cases live in sessionLivenessService.test.ts, this file should be slim or be considered for deletion in a future phase (do **not** delete here; just remove cases that have moved).

Helper imports: each new per-service test file likely needs `createPublisher` / `events` test fixtures currently colocated in the existing test files — extract them once into a shared `hub/src/sync/__test-fixtures__/publisher.ts` only if duplication exceeds 30 lines; otherwise inline them per file (planner discretion).

Do not change behavior; just relocate cases. Every case that previously passed must still pass after relocation.
  </action>
  <acceptance_criteria>
    - File `hub/src/sync/sessionRepository.test.ts` exists and contains `new SessionRepository(` construction.
    - File `hub/src/sync/sessionLivenessService.test.ts` exists and contains `new SessionLivenessService(` construction.
    - File `hub/src/sync/sessionConfigService.test.ts` exists and contains `new SessionConfigService(` construction.
    - File `hub/src/sync/sessionMergeService.test.ts` exists and contains `new SessionMergeService(` construction.
    - `rg "describe|test\(" hub/src/sync/sessionCache.test.ts | wc -l` is materially smaller than before redistribution (smoke-level only; expect under ~15 test/describe declarations).
    - `cd hub && bun test src/sync/sessionRepository.test.ts` exits 0.
    - `cd hub && bun test src/sync/sessionLivenessService.test.ts` exits 0.
    - `cd hub && bun test src/sync/sessionConfigService.test.ts` exits 0.
    - `cd hub && bun test src/sync/sessionMergeService.test.ts` exits 0.
    - `cd hub && bun test src/sync/` exits 0 (full sync test directory green; per-service + remaining facade smoke).
    - `bun typecheck` exits 0.
    - `bun run test` exits 0 (full workspace gate).
  </acceptance_criteria>
  <verify>
    <automated>bun typecheck && bun run test</automated>
  </verify>
  <done>4 per-service test files exist and pass; old test files reduced to facade smoke; full workspace `bun run test` and `bun typecheck` green.</done>
</task>

</tasks>

<verification>
- `bun typecheck` exits 0.
- `bun run test` exits 0 (full workspace).
- `cd hub && bun test src/sync/` exits 0 with at least 4 new per-service test files reporting passes.
- `wc -l hub/src/sync/sessionCache.ts hub/src/sync/sessionRepository.ts hub/src/sync/sessionLivenessService.ts hub/src/sync/sessionConfigService.ts hub/src/sync/sessionMergeService.ts` — each line shows a value under 400.
- `rg "new SessionCache\(|class SessionCache" hub/src/ --glob '!**/*.test.ts' | wc -l` is exactly 2.
- `rg "private readonly store|this\.store\." hub/src/sync/sessionLivenessService.ts hub/src/sync/sessionConfigService.ts hub/src/sync/sessionMergeService.ts` returns 0 lines (Store flows through repository).
</verification>

<success_criteria>
- SessionCache decomposed into 4 services + thin facade (REFH-01).
- Public method signatures of `SessionCache` byte-identical to pre-split state (zero caller change).
- Per-service test files exist, each constructing the service directly, all green.
- File-budget gate green (every `hub/src/sync/session*.ts` under 400 lines).
- Workspace gate green (`bun typecheck` + `bun run test`).
</success_criteria>

<output>
Create `.planning/phases/08-hub-internal-decoupling/08-01-SUMMARY.md` when done.
</output>
