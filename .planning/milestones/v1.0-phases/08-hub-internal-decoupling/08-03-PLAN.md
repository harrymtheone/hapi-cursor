---
phase: 08-hub-internal-decoupling
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - hub/src/web/middleware/route-helpers.ts
  - hub/src/web/middleware/apiRouteError.ts
  - hub/src/web/middleware/apiRouteError.test.ts
  - hub/src/web/middleware/auth.ts
  - hub/src/web/server.ts
  - hub/src/web/routes/sessions.ts
  - hub/src/web/routes/sessions/index.ts
  - hub/src/web/routes/sessions/lifecycle.ts
  - hub/src/web/routes/sessions/config.ts
  - hub/src/web/routes/sessions/upload.ts
  - hub/src/web/routes/sessions/read.ts
  - hub/src/web/routes/sessions/__tests__/lifecycle.test.ts
  - hub/src/web/routes/sessions/__tests__/config.test.ts
  - hub/src/web/routes/sessions/__tests__/upload.test.ts
  - hub/src/web/routes/sessions/__tests__/read.test.ts
  - hub/src/web/routes/sessions.test.ts
autonomous: true
requirements: [REFH-03]
must_haves:
  truths:
    - "SC#3 (D-135): hub/src/web/routes/sessions.ts is replaced by routes/sessions/{lifecycle,config,upload,read,index}.ts; createSessionsRoutes(getSyncEngine) signature unchanged so hub/src/web/server.ts call site at line ~89 needs no edit"
    - "SC#3 (D-136): a single hub/src/web/middleware/route-helpers.ts exports Hono middleware factories parseJsonBody(schema) / withEngine / withSession(idParam) / withActiveSession(idParam) / withMachine(idParam); WebAppEnv['Variables'] extended with engine / session / machine / body fields"
    - "SC#3 (D-137): hub/src/web/middleware/apiRouteError.ts exports class ApiRouteError extends HTTPException with readonly code: string and optional details; createWebApp registers an app.onError handler that emits { error: { code, message, details? } } and the registration runs before any app.route(...) call"
    - "SC#1: every file under hub/src/web/routes/sessions/ is under 250 lines"
    - "SC#5: bun typecheck and bun run test green; the redistributed sessions tests in hub/src/web/routes/sessions/__tests__/ all pass"
    - "D-135: the original 14-handler sessions.ts file is deleted (research factual correction overrides CONTEXT's 17-handler count)"
  artifacts:
    - path: "hub/src/web/middleware/route-helpers.ts"
      provides: "withEngine, withSession, withActiveSession, withMachine, parseJsonBody — Hono middleware factories"
    - path: "hub/src/web/middleware/apiRouteError.ts"
      provides: "class ApiRouteError extends HTTPException + registerApiErrorHandler(app)"
    - path: "hub/src/web/routes/sessions/index.ts"
      provides: "createSessionsRoutes(getSyncEngine) — unchanged external signature; composes 4 sub-apps via app.route('/', sub)"
    - path: "hub/src/web/routes/sessions/{lifecycle,config,upload,read}.ts"
      provides: "Per-responsibility route handlers"
  key_links:
    - from: "hub/src/web/server.ts::createWebApp"
      to: "hub/src/web/middleware/apiRouteError.ts::registerApiErrorHandler"
      via: "single app.onError registration immediately after `new Hono<WebAppEnv>()`, before any app.route(...) call"
      pattern: "registerApiErrorHandler\\(app\\)"
    - from: "hub/src/web/routes/sessions/{lifecycle,config,upload,read}.ts"
      to: "hub/src/web/middleware/route-helpers.ts"
      via: "import { withEngine, withSession, withActiveSession, withMachine, parseJsonBody }"
      pattern: "from .*middleware/route-helpers"
    - from: "hub/src/web/routes/sessions/index.ts::createSessionsRoutes"
      to: "createLifecycleRoutes / createConfigRoutes / createUploadRoutes / createReadRoutes"
      via: "app.route('/', subApp)"
      pattern: "app\\.route\\('/'"
---

<objective>
Split the 14-handler / 467-line `hub/src/web/routes/sessions.ts` into 4 responsibility-keyed files plus a composition `index.ts`, introduce one new `route-helpers.ts` middleware file (`withEngine` / `withSession` / `withActiveSession` / `withMachine` / `parseJsonBody`) consumed by every handler, and centralize HTTP error responses through `ApiRouteError extends HTTPException` + a global `app.onError` registered inside `createWebApp`. Redistribute the existing route tests into per-sub-file test files and add a new `apiRouteError.test.ts` that pins the unified `{ error: { code, message, details? } }` JSON shape.

Purpose: REFH-03. Sessions route concerns currently mix lifecycle, config, upload, read — and every handler hand-rolls its own `return c.json({ error }, 400)` plus `if (X instanceof Response) return X` guards. Pulling injection into Hono middleware (writing to `c.set(...)`) and centralizing the error shape replaces ~150 LOC of boilerplate with a single onError handler and 4 short sub-files of pure business logic. The `createSessionsRoutes(getSyncEngine)` factory keeps its external signature unchanged so `hub/src/web/server.ts:89` does not need editing.

Output: 2 new middleware files (`route-helpers.ts`, `apiRouteError.ts`); 5 new sub-route files (`sessions/index.ts`, `lifecycle.ts`, `config.ts`, `upload.ts`, `read.ts`); 4 new per-sub-file test files + 1 new `apiRouteError.test.ts`; deletion of the original `sessions.ts` + `sessions.test.ts` (tests redistributed). `WebAppEnv['Variables']` extended in `hub/src/web/middleware/auth.ts`. `createWebApp` registers the global error handler.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hub-internal-decoupling/08-CONTEXT.md
@.planning/phases/08-hub-internal-decoupling/08-RESEARCH.md
@.planning/phases/08-hub-internal-decoupling/08-01-SUMMARY.md

@hub/src/web/routes/sessions.ts
@hub/src/web/routes/sessions.test.ts
@hub/src/web/server.ts
@hub/src/web/middleware/auth.ts
@hub/src/web/routes/guards.ts

<interfaces>
<!-- ApiRouteError snippet — verbatim from 08-RESEARCH.md §"`ApiRouteError` class snippet". -->

```typescript
// hub/src/web/middleware/apiRouteError.ts
import { HTTPException } from 'hono/http-exception'
import type { ContentfulStatusCode } from 'hono/utils/http-status'

export class ApiRouteError extends HTTPException {
    readonly code: string
    readonly details?: unknown

    constructor(
        status: ContentfulStatusCode,
        code: string,
        details?: unknown,
        message?: string
    ) {
        super(status, { message: message ?? code })
        this.code = code
        this.details = details
    }
}

import type { Hono } from 'hono'
import type { WebAppEnv } from './auth'

export function registerApiErrorHandler(app: Hono<WebAppEnv>): void {
    app.onError((err, c) => {
        if (err instanceof ApiRouteError) {
            return c.json(
                {
                    error: {
                        code: err.code,
                        message: err.message,
                        ...(err.details !== undefined ? { details: err.details } : {})
                    }
                },
                err.status
            )
        }
        if (err instanceof HTTPException) {
            return err.getResponse()
        }
        console.error('[Hub] Unhandled error in route:', err)
        return c.json({ error: { code: 'internal-error', message: 'Internal server error' } }, 500)
    })
}
```

WebAppEnv extension (08-RESEARCH.md §"WebAppEnv typing change (D-136)"):

```typescript
// hub/src/web/middleware/auth.ts (extend existing definition at lines 5–9)
import type { SyncEngine, Session, Machine } from '../../sync/syncEngine'

export type WebAppEnv = {
    Variables: {
        userId: number
        engine: SyncEngine
        session: Session
        machine: Machine
        body: unknown
    }
}
```

Per 08-RESEARCH.md the planner picks `body: unknown` plus per-route `const body = c.get('body') as z.infer<typeof schema>` for minimal churn.

Handler-to-sub-file mapping — verbatim from 08-RESEARCH.md §"`sessions.ts` Handler Inventory" (factual correction: 14 handlers, not 17):

| # | Verb | Path | Sub-file | Body schema |
|---|------|------|----------|-------------|
| 1 | GET | /sessions | read | — |
| 2 | GET | /sessions/:id | read | — |
| 3 | POST | /sessions/:id/resume | lifecycle | resumeBodySchema |
| 4 | POST | /sessions/:id/upload | upload | uploadSchema (JSON base64; NOT multipart) |
| 5 | POST | /sessions/:id/upload/delete | upload | uploadDeleteSchema |
| 6 | POST | /sessions/:id/abort | lifecycle | — (requireActive) |
| 7 | POST | /sessions/:id/archive | lifecycle | — (requireActive) |
| 8 | POST | /sessions/:id/switch | lifecycle | — (requireActive) |
| 9 | POST | /sessions/:id/permission-mode | config | permissionModeSchema |
| 10 | POST | /sessions/:id/model | config | modelSchema (requireActive) |
| 11 | PATCH | /sessions/:id | lifecycle | renameSessionSchema |
| 12 | DELETE | /sessions/:id | lifecycle | — (custom 409 if active) |
| 13 | GET | /sessions/:id/slash-commands | read | — |
| 14 | GET | /sessions/:id/skills | read | — |

Sub-file budgets (08-RESEARCH.md §"Sub-file budget projections"):
- lifecycle.ts ≈ 100 LOC post-helper-rewrite (handlers 3, 6, 7, 8, 11, 12).
- config.ts ≈ 40 LOC (handlers 9, 10).
- upload.ts ≈ 50 LOC (handlers 4, 5).
- read.ts ≈ 100 LOC (handlers 1, 2, 13, 14 — slash-commands merge logic is heavy and not easily helper-able).
- index.ts ≈ 30 LOC (composition only).

ApiRouteError codes used in helpers:
- `parseJsonBody` failure → `ApiRouteError(400, 'invalid-body', issues)` where `issues = parseResult.error.issues`.
- `withEngine` null → `ApiRouteError(503, 'engine-unavailable')`.
- `withSession(idParam)` not-found → `ApiRouteError(404, 'not-found')`.
- `withActiveSession(idParam)` inactive → `ApiRouteError(409, 'session-not-active')` (or whatever the existing sessions.ts uses; cross-check with the original handler bodies and preserve the existing status + code semantics).
- `withMachine(idParam)` not-found → `ApiRouteError(404, 'not-found')`.

Composition example for `sessions/index.ts` (08-RESEARCH.md §"Pattern 2: Hono `app.route('/path', subApp)` composition"):

```typescript
export function createSessionsRoutes(getSyncEngine: () => SyncEngine | null): Hono<WebAppEnv> {
    const app = new Hono<WebAppEnv>()
    app.route('/', createLifecycleRoutes(getSyncEngine))
    app.route('/', createConfigRoutes(getSyncEngine))
    app.route('/', createUploadRoutes(getSyncEngine))
    app.route('/', createReadRoutes(getSyncEngine))
    return app
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create route-helpers.ts + apiRouteError.ts, extend WebAppEnv, register app.onError</name>
  <read_first>
    - hub/src/web/middleware/auth.ts (existing middleware factory + WebAppEnv definition; extend in place)
    - hub/src/web/server.ts (createWebApp factory — find the `new Hono<WebAppEnv>()` line and the existing `app.route('/api', createSessionsRoutes(...))` call at line ~89)
    - hub/src/web/routes/guards.ts (existing Response-union guards — KEEP intact per D-144; new helpers are middleware-shaped replacements)
    - hub/src/web/routes/sessions.ts (read enough to confirm the error codes / status codes the existing handlers return — preserve those in ApiRouteError invocations)
    - .planning/phases/08-hub-internal-decoupling/08-RESEARCH.md §"ApiRouteError class snippet" + §"WebAppEnv typing change (D-136)" + §"Helper Location Decision" + §"Pitfall 6: app.onError placement order"
  </read_first>
  <action>
1. Create `hub/src/web/middleware/apiRouteError.ts` with the `ApiRouteError` class and `registerApiErrorHandler(app)` factory — copy the snippet in `<interfaces>` verbatim. The class extends `HTTPException` from `hono/http-exception`, adds `readonly code: string` and `readonly details?: unknown`. The handler emits `{ error: { code, message, ...(details !== undefined ? { details } : {}) } }` with the exception's `status`. Non-`ApiRouteError` `HTTPException` falls through to `err.getResponse()` (Hono default). Anything else returns a generic 500 `{ error: { code: 'internal-error', message: 'Internal server error' } }` and logs the underlying error to `console.error('[Hub] Unhandled error in route:', err)` so stacks stay server-side.

2. Create `hub/src/web/middleware/route-helpers.ts` with 5 factory functions, each returning `MiddlewareHandler<WebAppEnv>`. Follow the existing pattern in `hub/src/web/middleware/auth.ts` (`createAuthMiddleware(jwtSecret)` returning a `MiddlewareHandler<WebAppEnv>` that `await next()`s after `c.set(...)`):
   - `withEngine(getSyncEngine: () => SyncEngine | null)` — calls `getSyncEngine()`; if null throws `new ApiRouteError(503, 'engine-unavailable')`; otherwise `c.set('engine', engine); await next()`.
   - `withSession(idParam: string = 'id')` — reads `c.req.param(idParam)`; calls `c.get('engine').resolveSessionAccess(id)` (the existing pattern from `routes/guards.ts::requireSessionFromParam`); if no session throws `new ApiRouteError(404, 'not-found')`; otherwise `c.set('session', session); await next()`. Note: requires `withEngine` to have run earlier in the chain.
   - `withActiveSession(idParam: string = 'id')` — composes the `withSession` lookup then enforces active-state per the original `sessions.ts` semantics (read it to know whether the existing `requireActive` returns 404 or 409 and what code string the new error should carry; preserve the existing status + code semantics — research §"How to avoid" notes "Preserved from existing `requireSessionFromParam`"). On failure throw `new ApiRouteError(<existing status>, '<existing code or session-not-active>')`.
   - `withMachine(idParam: string = 'machineId')` — symmetric to `withSession` but against `engine.getMachine(id)`; throws `new ApiRouteError(404, 'not-found')` if missing.
   - `parseJsonBody<TSchema extends z.ZodTypeAny>(schema: TSchema)` — calls `await c.req.json()` inside a try/catch (JSON parse failure → `new ApiRouteError(400, 'invalid-body', undefined, 'Body is not valid JSON')`); runs `schema.safeParse(body)`; on failure throws `new ApiRouteError(400, 'invalid-body', parseResult.error.issues)`; on success `c.set('body', parseResult.data); await next()`.

3. Extend `hub/src/web/middleware/auth.ts` `WebAppEnv` `Variables` block per the `<interfaces>` snippet — add `engine: SyncEngine`, `session: Session`, `machine: Machine`, `body: unknown`. Import `SyncEngine`, `Session`, `Machine` types from `../../sync/syncEngine` (these type re-exports survive the Plan 08-02 split because `syncEngine.ts` re-exports `Session` / `Machine` from its public surface — verify with a quick read; if a type was relocated, follow the new path).

4. Update `hub/src/web/server.ts::createWebApp`:
   - Import `registerApiErrorHandler` from `./middleware/apiRouteError`.
   - Immediately after the `new Hono<WebAppEnv>()` construction and BEFORE any `app.route(...)` call (Pitfall 6), call `registerApiErrorHandler(app)`. Do not add any other behavioral changes in this file.

5. Do NOT touch the existing `hub/src/web/routes/guards.ts` `requireSyncEngine` / `requireSession` / `requireSessionFromParam` / `requireMachine` helpers — they are still used by `messages.ts`, `machines.ts`, `permissions.ts` (Pitfall 5; D-144). Plan 08-04 may revisit if and only if research surfaces unused-by-the-end-of-phase code.

6. Confirm Hono import paths resolve: `import { HTTPException } from 'hono/http-exception'` and `import type { ContentfulStatusCode } from 'hono/utils/http-status'`. Both verified present in `node_modules/hono` per 08-RESEARCH.md §"Environment Availability".
  </action>
  <acceptance_criteria>
    - File `hub/src/web/middleware/apiRouteError.ts` exists and exports `class ApiRouteError extends HTTPException` and `function registerApiErrorHandler(app: Hono<WebAppEnv>): void`.
    - `rg "extends HTTPException" hub/src/web/middleware/apiRouteError.ts | wc -l` returns 1.
    - File `hub/src/web/middleware/route-helpers.ts` exists and exports `withEngine`, `withSession`, `withActiveSession`, `withMachine`, `parseJsonBody`.
    - `rg "^export (const|function) (withEngine|withSession|withActiveSession|withMachine|parseJsonBody)\\b" hub/src/web/middleware/route-helpers.ts | wc -l` returns 5.
    - `hub/src/web/middleware/auth.ts` `WebAppEnv['Variables']` contains `engine`, `session`, `machine`, `body` fields in addition to existing `userId`.
    - `hub/src/web/server.ts` calls `registerApiErrorHandler(app)` before any `app.route(` call (verify by line numbers: rg `-n` for `new Hono<WebAppEnv>`, `registerApiErrorHandler`, and the first `app.route(` — registration line must be strictly between them).
    - `rg "throw new ApiRouteError\\(" hub/src/web/middleware/route-helpers.ts | wc -l` ≥ 5 (one per helper failure path; parseJsonBody may have 2 — JSON parse + schema fail).
    - `bun typecheck` exits 0.
    - `cd hub && bun test` exits 0 (existing tests must not regress; new test added in Task 3).
  </acceptance_criteria>
  <verify>
    <automated>bun typecheck && cd hub && bun test</automated>
  </verify>
  <done>Middleware helpers, ApiRouteError, and global onError handler land; WebAppEnv extended; full hub test suite still green.</done>
</task>

<task type="auto">
  <name>Task 2: Split sessions.ts into routes/sessions/{lifecycle,config,upload,read,index}.ts using helpers</name>
  <read_first>
    - hub/src/web/routes/sessions.ts (full file — 467 lines, 14 handlers; this task moves bodies into 5 new files and deletes this one)
    - hub/src/web/middleware/route-helpers.ts (created in Task 1; check helper signatures)
    - hub/src/web/middleware/apiRouteError.ts (created in Task 1; check ApiRouteError constructor signature)
    - hub/src/web/server.ts (current `app.route('/api', createSessionsRoutes(getSyncEngine))` call site at ~line 89 must continue to work with the new index.ts)
    - .planning/phases/08-hub-internal-decoupling/08-RESEARCH.md §"`sessions.ts` Handler Inventory" + §"Recommended Project Structure" + §"Sub-file budget projections"
  </read_first>
  <action>
1. Create `hub/src/web/routes/sessions/index.ts` exporting `createSessionsRoutes(getSyncEngine: () => SyncEngine | null): Hono<WebAppEnv>` — the function signature is **unchanged** from the original `sessions.ts`. Body: `const app = new Hono<WebAppEnv>(); app.route('/', createLifecycleRoutes(getSyncEngine)); app.route('/', createConfigRoutes(getSyncEngine)); app.route('/', createUploadRoutes(getSyncEngine)); app.route('/', createReadRoutes(getSyncEngine)); return app;` (verbatim from `<interfaces>` §composition example).

2. Create `hub/src/web/routes/sessions/lifecycle.ts` exporting `createLifecycleRoutes(getSyncEngine)`. Move handler bodies for resume, abort, archive, switch, PATCH /:id, DELETE /:id from the original `sessions.ts` (handlers 3, 6, 7, 8, 11, 12 per the inventory in `<interfaces>`). Rewrite each handler to use middleware injection:
   - Chain `withEngine(getSyncEngine)` first (so `c.get('engine')` works in subsequent middleware).
   - For routes that need a session: `withSession('id')` (or `withActiveSession('id')` for routes that require an active session — match the original `requireActive` invocations: abort, archive, switch).
   - For routes with a body: `parseJsonBody(<schema>)` and inside the handler `const body = c.get('body') as z.infer<typeof schemaName>`.
   - Replace every `return c.json({ error }, status)` with `throw new ApiRouteError(status, '<kebab-case-code>', details?)`. Preserve the original status code + message intent; pick the code identifier conservatively (e.g., `'not-found'`, `'invalid-body'`, `'session-active'`, `'engine-unavailable'`). DELETE /:id's custom 409 for active sessions becomes `throw new ApiRouteError(409, 'session-active')` (or whatever code string the existing handler used — preserve string semantics).
   - Inline the original Zod schemas (`resumeBodySchema`, `renameSessionSchema`, etc.) into this file or move them to a small `hub/src/web/routes/sessions/schemas.ts` shared module if more than one sub-file uses the same schema (planner discretion; keep the schemas inline for first cut).

3. Create `hub/src/web/routes/sessions/config.ts` exporting `createConfigRoutes(getSyncEngine)`. Handlers 9 (`POST /:id/permission-mode`) and 10 (`POST /:id/model`). Same middleware pattern. `POST /:id/model` uses `withActiveSession` (per the original `requireActive`). Bodies parsed with `permissionModeSchema` and `modelSchema` respectively.

4. Create `hub/src/web/routes/sessions/upload.ts` exporting `createUploadRoutes(getSyncEngine)`. Handlers 4 (`POST /:id/upload`) and 5 (`POST /:id/upload/delete`). Use `parseJsonBody(uploadSchema)` and `parseJsonBody(uploadDeleteSchema)` — note 08-RESEARCH.md §"Note on `/upload`" confirms both routes use JSON-encoded base64, NOT multipart; CONTEXT's mention of "multipart" is incorrect. Preserve the 50 MB cap (`MAX_UPLOAD_BYTES = 50 * 1024 * 1024`) — keep `estimateBase64Bytes(body.content) > MAX_UPLOAD_BYTES` check inside the handler body AFTER `parseJsonBody` (the cap is a business rule beyond schema validation, per 08-RESEARCH.md §"Security Domain V8/V12"); on overflow throw `new ApiRouteError(413, 'payload-too-large')`.

5. Create `hub/src/web/routes/sessions/read.ts` exporting `createReadRoutes(getSyncEngine)`. Handlers 1 (`GET /sessions`), 2 (`GET /:id`), 13 (`GET /:id/slash-commands`), 14 (`GET /:id/skills`). The slash-commands merge logic is heavy and not easily helper-able (research §"Sub-file budget projections"); leave it in-line and use `withSession` for `:id`.

6. Delete `hub/src/web/routes/sessions.ts`. The composition file is now `routes/sessions/index.ts`; `hub/src/web/server.ts:89` imports `createSessionsRoutes` from `./routes/sessions/` (the index resolution) and the path resolves to `./routes/sessions/index.ts` automatically — verify the import path in server.ts works (the existing path was `./routes/sessions` which resolves to either `sessions.ts` or `sessions/index.ts`; both resolve to the same import string). If the existing import was `from './routes/sessions.ts'` with an explicit `.ts`, change it to `from './routes/sessions/index.ts'` (or strip the extension).

7. Do NOT delete `hub/src/web/routes/sessions.test.ts` yet — that happens in Task 3 alongside the test redistribution. Until Task 3 lands, the old test file may temporarily fail; gate Task 2's `bun run test` acceptance to allow `hub/src/web/routes/sessions.test.ts` failures only (use `bun test --bail=0` or run targeted file lists) **OR** commit Task 2 + Task 3 together. Recommended: land Tasks 2 and 3 as a single atomic commit pair (Task 2 deletes old code, Task 3 creates new tests, single workspace gate at end).
  </action>
  <acceptance_criteria>
    - File `hub/src/web/routes/sessions.ts` no longer exists (`ls hub/src/web/routes/sessions.ts 2>&1 | grep -c "No such file"` returns 1).
    - Directory `hub/src/web/routes/sessions/` exists with at least 5 files: `index.ts`, `lifecycle.ts`, `config.ts`, `upload.ts`, `read.ts`.
    - `wc -l hub/src/web/routes/sessions/index.ts` < 250.
    - `wc -l hub/src/web/routes/sessions/lifecycle.ts` < 250.
    - `wc -l hub/src/web/routes/sessions/config.ts` < 250.
    - `wc -l hub/src/web/routes/sessions/upload.ts` < 250.
    - `wc -l hub/src/web/routes/sessions/read.ts` < 250.
    - `rg "^export function createSessionsRoutes\\(" hub/src/web/routes/sessions/index.ts` returns exactly 1 line.
    - `rg "return c\\.json\\(\\s*\\{\\s*error" hub/src/web/routes/sessions/` returns 0 lines (all error responses go through `throw new ApiRouteError(...)`).
    - `rg "throw new ApiRouteError\\(" hub/src/web/routes/sessions/` returns at least 10 lines (covers the missing-session / missing-body / invalid-body / engine-unavailable / payload-too-large failure paths).
    - `rg "from .*middleware/route-helpers" hub/src/web/routes/sessions/ | wc -l` ≥ 4 (lifecycle, config, upload, read all import helpers).
    - `bun typecheck` exits 0.
    - Workspace gate is allowed to be deferred to Task 3 if the old `hub/src/web/routes/sessions.test.ts` still references symbols that have moved; if the same commit lands Tasks 2 + 3 together, then `bun run test` must exit 0 at that combined commit.
  </acceptance_criteria>
  <verify>
    <automated>bun typecheck</automated>
  </verify>
  <done>14 handlers redistributed across 4 sub-files; original sessions.ts deleted; createSessionsRoutes signature unchanged; per-sub-file budget under 250 lines.</done>
</task>

<task type="auto">
  <name>Task 3: Redistribute sessions.test.ts into 4 per-sub-file test files + add apiRouteError.test.ts</name>
  <read_first>
    - hub/src/web/routes/sessions.test.ts (full file — current route tests; the source of cases to redistribute)
    - hub/src/web/routes/sessions/{index,lifecycle,config,upload,read}.ts (created in Task 2 — to know which sub-file each test now targets)
    - hub/src/web/middleware/route-helpers.ts (created in Task 1 — for direct error-shape tests of withEngine / withSession / parseJsonBody)
    - hub/src/web/middleware/apiRouteError.ts (created in Task 1)
    - .planning/phases/08-hub-internal-decoupling/08-RESEARCH.md §"Phase Requirements → Test Map" REFH-03 row + §"Wave 0 Gaps"
  </read_first>
  <action>
1. Create `hub/src/web/routes/sessions/__tests__/lifecycle.test.ts` — move all cases from `sessions.test.ts` that exercise resume, abort, archive, switch, PATCH /:id, DELETE /:id behavior. Keep the existing test framework imports + fixtures (likely a Hono test client + an in-memory SyncEngine mock factory; reuse the same fixture from the original `sessions.test.ts`).

2. Create `hub/src/web/routes/sessions/__tests__/config.test.ts` — move cases for permission-mode and model.

3. Create `hub/src/web/routes/sessions/__tests__/upload.test.ts` — move cases for upload + upload/delete, including the 50 MB cap regression (must verify `413 payload-too-large` is now returned with the unified error shape).

4. Create `hub/src/web/routes/sessions/__tests__/read.test.ts` — move cases for GET /sessions, GET /:id, slash-commands, skills.

5. Create `hub/src/web/middleware/apiRouteError.test.ts` — new file. At least 5 cases:
   - `parseJsonBody` middleware with a body that fails schema validation returns HTTP 400 and JSON body `{ error: { code: 'invalid-body', message: 'invalid-body', details: <issues array> } }` (details length > 0).
   - `parseJsonBody` middleware with a body that is not valid JSON at all returns HTTP 400 and `code: 'invalid-body'`.
   - `withEngine` with a `getSyncEngine` returning null returns HTTP 503 and `code: 'engine-unavailable'`.
   - `withSession` with an unknown session id returns HTTP 404 and `code: 'not-found'`.
   - A handler that throws a plain non-`ApiRouteError` `Error` results in HTTP 500 and `code: 'internal-error'` (stack does NOT leak in the response body — assert `details` absent).

6. Delete `hub/src/web/routes/sessions.test.ts` (its cases now live under `sessions/__tests__/`).

7. If any test fixture was duplicated across the 4 new sub-test files, extract it once to `hub/src/web/routes/sessions/__tests__/_fixtures.ts` (planner discretion — only extract if duplication exceeds ~30 lines per file).

Use `bun:test` (existing pattern in the codebase) — `import { describe, test, expect } from 'bun:test'` and the existing Hono testing pattern from the original sessions.test.ts (likely `app.request('/path', { method, body })`).

Verify post-task: `bun run test` is fully green across the workspace, no skips, no excluded test files.
  </action>
  <acceptance_criteria>
    - Files `hub/src/web/routes/sessions/__tests__/lifecycle.test.ts`, `config.test.ts`, `upload.test.ts`, `read.test.ts` exist.
    - File `hub/src/web/middleware/apiRouteError.test.ts` exists with at least 5 test cases.
    - File `hub/src/web/routes/sessions.test.ts` no longer exists.
    - `cd hub && bun test src/web/routes/sessions/__tests__/` exits 0.
    - `cd hub && bun test src/web/middleware/apiRouteError.test.ts` exits 0.
    - `rg "code: 'invalid-body'|code: \"invalid-body\"" hub/src/web/middleware/apiRouteError.test.ts | wc -l` ≥ 2 (covers schema fail + JSON parse fail).
    - `rg "code: 'engine-unavailable'|code: \"engine-unavailable\"" hub/src/web/middleware/apiRouteError.test.ts | wc -l` ≥ 1.
    - `rg "code: 'not-found'|code: \"not-found\"" hub/src/web/middleware/apiRouteError.test.ts | wc -l` ≥ 1.
    - `rg "code: 'internal-error'|code: \"internal-error\"" hub/src/web/middleware/apiRouteError.test.ts | wc -l` ≥ 1.
    - `bun typecheck` exits 0.
    - `bun run test` exits 0 (full workspace, including the 4 new sub-file tests + the apiRouteError contract test).
  </acceptance_criteria>
  <verify>
    <automated>bun typecheck && bun run test</automated>
  </verify>
  <done>Sessions tests redistributed across 4 sub-file tests; ApiRouteError contract test pins the unified error JSON shape; old sessions.test.ts deleted; full workspace gate green.</done>
</task>

</tasks>

<verification>
- `bun typecheck` exits 0 and `bun run test` exits 0.
- `wc -l hub/src/web/routes/sessions/*.ts` — every line shows a value under 250 (SC#1 / D-135 budget).
- `rg "from .*middleware/route-helpers" hub/src/web/routes/sessions/ | wc -l` ≥ 4 (all four sub-files use the helpers).
- `rg "registerApiErrorHandler\\(" hub/src/web/server.ts | wc -l` ≥ 1 (global handler registered).
- `rg "return c\\.json\\(\\s*\\{\\s*error" hub/src/web/routes/sessions/` returns 0 lines (no hand-rolled error JSON in sub-files).
- `cd hub && bun test src/web/middleware/apiRouteError.test.ts src/web/routes/sessions/__tests__/` exits 0.
</verification>

<success_criteria>
- `sessions.ts` deleted and replaced by `routes/sessions/{lifecycle,config,upload,read,index}.ts` (REFH-03 + SC#3).
- `createSessionsRoutes(getSyncEngine)` external signature unchanged so `hub/src/web/server.ts` import + call site at line ~89 is untouched.
- Single `hub/src/web/middleware/route-helpers.ts` exports the 5 Hono middleware factories; `WebAppEnv['Variables']` extended.
- `ApiRouteError extends HTTPException` + central `app.onError` register inside `createWebApp` BEFORE any `app.route(...)` call (Pitfall 6).
- Per-sub-file size budget under 250 lines (D-135 + SC#1).
- Sessions tests redistributed; `apiRouteError.test.ts` pins the unified error JSON shape.
- `bun typecheck` + `bun run test` green (SC#5).
</success_criteria>

<output>
Create `.planning/phases/08-hub-internal-decoupling/08-03-SUMMARY.md` when done.
</output>
