---
phase: 02-cut-external-integration-channels
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - cli/src/commands/notify.ts
  - cli/src/terminal/TerminalManager.ts
  - hub/src/index.ts
  - scripts/check-no-cut-agents.sh
  - bun.lock
autonomous: true
requirements: [CUT-06, CUT-07, CUT-08]
user_setup: []

must_haves:
  truths:
    - "Phase 2 SC#1: bun typecheck + bun run test both green at repo root"
    - "Phase 2 SC#2: bash scripts/check-no-cut-agents.sh exits 0 with extended PATTERN '\\b(claude|codex|gemini|opencode|telegram|serverchan|elevenlabs|grammy)\\b'"
    - "Phase 2 SC#3: package.json files declare no grammy or @elevenlabs/react; hub/src/index.ts notificationChannels array references neither Telegram nor ServerChan"
    - "Phase 2 SC#4: TELEGRAM_BOT_TOKEN / TELEGRAM_NOTIFICATION / SERVERCHAN_SENDKEY / SERVERCHAN_NOTIFICATION not read anywhere in cli/src hub/src web/src shared/src"
    - "Phase 2 SC#5: /api/auth body schema accepts only access-token; access-token positive case test stays green"
    - "All five D-30 commits land in order: CUT-06 hub-side → CUT-06 web-side → CUT-07 → CUT-08 → cleanup, each individually green (D-31)"
    - "bun install --frozen-lockfile exits 0 after bun.lock regen"
    - "cli/src/terminal/TerminalManager.ts SENSITIVE_ENV_KEYS no longer contains 'TELEGRAM_BOT_TOKEN'"
    - "cli/src/commands/notify.ts error message no longer references Telegram"
  artifacts:
    - path: "scripts/check-no-cut-agents.sh"
      provides: "Phase 2 ripgrep guard with extended PATTERN + Phase-2 whitelist additions"
      contains: "PATTERN='\\\\b(claude|codex|gemini|opencode|telegram|serverchan|elevenlabs|grammy)\\\\b'"
    - path: "bun.lock"
      provides: "Regenerated after bun install; frozen-lockfile passes (grammy + @elevenlabs/react resolutions removed)"
  key_links:
    - from: "package.json (root)"
      to: "scripts/check-no-cut-agents.sh"
      via: "test:guard chained into root test"
      pattern: "test:guard"
    - from: "cli/src/terminal/TerminalManager.ts"
      to: "(removed) TELEGRAM_BOT_TOKEN entry in SENSITIVE_ENV_KEYS"
      via: "env-var redaction set"
      pattern: "SENSITIVE_ENV_KEYS"
---

<objective>
Final cleanup commit closing all Phase 2 SC threads (D-30 commit #5):
- Strip CLI residuals per D-33 + RESEARCH §"CLI Crosscut Inventory": rewrite `cli/src/commands/notify.ts:9` error message to drop Telegram reference; remove `'TELEGRAM_BOT_TOKEN'` from `cli/src/terminal/TerminalManager.ts:34` `SENSITIVE_ENV_KEYS` Set.
- Clean any residual Telegram-themed startup banner output in `hub/src/index.ts` (Discretion item; zero-tolerance under D-27).
- Extend `scripts/check-no-cut-agents.sh` PATTERN with Phase 2 keywords + whitelist Phase-2-deferred docs surfaces (RESEARCH §"Ripgrep Guard Script Delta"). Remove the `--glob '!shared/src/voice.ts'` whitelist line (file gone in 02-03).
- Regenerate `bun.lock` via `bun install`; verify `bun install --frozen-lockfile` is clean.
- Per RESEARCH §"Commit Dependency Ordering": PATTERN extension + CLI residual cleanup MUST land atomically in this single commit.

Purpose: Final atomic commit in D-30 sequence (#5); closes Phase 2 SC#1..#5.
Output: `chore(phase-02): final cleanup + ripgrep guard update` commit. `bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh` all exit 0.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cut-external-integration-channels/02-CONTEXT.md
@.planning/phases/02-cut-external-integration-channels/02-RESEARCH.md
@.planning/phases/02-cut-external-integration-channels/02-VALIDATION.md
@.planning/phases/02-cut-external-integration-channels/02-04-SUMMARY.md
@.planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI residual cleanup (TerminalManager + notify) + hub banner residuals (D-33)</name>
  <files>cli/src/terminal/TerminalManager.ts, cli/src/commands/notify.ts, hub/src/index.ts</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-CONTEXT.md (D-27, D-33, Discretion line about hub banner cleanup)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"CLI Crosscut Inventory" (full table + actions)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"Open Questions" #1 (notify.ts disposition)
    - cli/src/terminal/TerminalManager.ts (focus L34 SENSITIVE_ENV_KEYS Set entry)
    - cli/src/commands/notify.ts (full file — small placeholder)
    - hub/src/index.ts (entire file — verify any remaining Telegram-themed banner / QR / log line that 02-01 missed)
  </read_first>
  <action>
    1. **`cli/src/terminal/TerminalManager.ts`**: locate the `SENSITIVE_ENV_KEYS` Set declaration around line 34. Remove the literal `'TELEGRAM_BOT_TOKEN'` entry. Other entries (e.g. cloud-provider tokens) stay untouched.
    2. **`cli/src/commands/notify.ts`**: per RESEARCH Open Q #1 + D-33, change line 9 (`console.error(chalk.gray('Use Telegram notifications from hapi-hub instead.'))`) to a non-Telegram-referencing message. Recommendation: `chalk.gray('The notify command was removed in this fork.')`. (Outright file deletion is also acceptable per Open Q #1; if the planner chooses that, also drop `notifyCommand` from `cli/src/commands/registry.ts` and remove any test referencing it. Default: in-place message rewrite — minimum diff.)
    3. **`hub/src/index.ts`**: re-read full file to surface any residual Telegram-tagged startup-banner / log / comment line that 02-01 left behind (e.g. an old console line referencing Telegram bot URL; a JSDoc top-of-file comment naming "HAPI Telegram Bot"). Scrub all hits per zero-tolerance D-27. After this task, `rg -ni 'telegram' hub/src/index.ts | wc -l` must be 0.
    4. Run `bun typecheck` mid-task — must exit 0. (`bun run test` runs in Task 4.)
  </action>
  <verify>
    <automated>! rg -n "TELEGRAM_BOT_TOKEN" cli/src/terminal/TerminalManager.ts && ! rg -ni 'telegram' cli/src/commands/notify.ts && ! rg -ni 'telegram' hub/src/index.ts && bun typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "'TELEGRAM_BOT_TOKEN'" cli/src/terminal/TerminalManager.ts | wc -l` is 0
    - `rg -ni 'telegram' cli/src/commands/notify.ts | wc -l` is 0
    - `rg -ni 'telegram' hub/src/index.ts | wc -l` is 0
    - `rg -n "TELEGRAM_BOT_TOKEN|TELEGRAM_NOTIFICATION|SERVERCHAN_SENDKEY|SERVERCHAN_NOTIFICATION" cli/src/ hub/src/ web/src/ shared/src/ | wc -l` is 0 (SC#4 final)
    - `bun typecheck` exits 0
  </acceptance_criteria>
  <done>CLI residuals stripped (D-33). Hub index.ts banner cleaned. SC#4 secret-name reads now zero across all four packages.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ripgrep guard PATTERN + Phase-2 whitelist updates</name>
  <files>scripts/check-no-cut-agents.sh</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"Ripgrep Guard Script Delta" (PATTERN + whitelist additions/removals)
    - .planning/phases/02-cut-external-integration-channels/02-CONTEXT.md (D-27, D-28, D-29)
    - scripts/check-no-cut-agents.sh (current Phase-1-final state)
  </read_first>
  <action>
    1. Edit `scripts/check-no-cut-agents.sh` PATTERN line:
       - Before: `PATTERN='\b(claude|codex|gemini|opencode)\b'`
       - After: `PATTERN='\b(claude|codex|gemini|opencode|telegram|serverchan|elevenlabs|grammy)\b'`
    2. Whitelist removals (file gone after 02-03):
       - Remove the line `--glob '!shared/src/voice.ts'` from the WHITELIST array (the file was deleted in 02-03 — keeping the whitelist line is dead config and a future-confusion vector).
    3. Whitelist additions (Phase-2 deferred / Phase-12 territory):
       - The existing Phase-1 whitelist already covers `.planning/**`, `CHANGELOG.md`, `docs/**`, `website/**`, `cli/NOTICE`, `cli/README.md`, `hub/README.md`, `web/README.md`, `README.md`, `CONTRIBUTING.md`, `AGENTS.md`, `refactor.md`, `.cursor/rules/**`. These are sufficient for Phase 2 docs/marketing surfaces.
       - Verify `docs/public/schemas/settings.schema.json` is covered by the existing `--glob '!docs/**'`. If it is (it is — `docs/public/schemas/...` is under `docs/`), no new explicit entry needed. If `docs/public/**` is NOT covered (path verification at execution time), add `--glob '!docs/public/schemas/**'` with comment `# Phase 10/12 territory: schema regenerated from settings.ts`.
    4. Update the bottom error-message echo to mention the broader keyword set (replace "Non-Cursor agent literals" with "Non-Cursor / external-channel literals" + add a hint about Phase-12 docs deferral). RESEARCH §"Ripgrep Guard Script Delta" step 5 has the recommended wording.
    5. Update the script's top-of-file comment header to reflect the Phase-2 keyword extension (categories: cut-agents from Phase 1 + integration-channels from Phase 2).
    6. Keep the rest of the script identical (the `set -euo pipefail`, the `if rg -i ...; then exit 1; fi` block, the success echo at end, executable bit). Do NOT rename the file (RESEARCH §"Ripgrep Guard Script Delta" risk note: keeping the name avoids touching `package.json:25` `test:guard` command).
    7. Run `bash scripts/check-no-cut-agents.sh` standalone — MUST exit 0. If it fires, the remaining hits identify residual code that earlier 02-0X commits missed; fix those FIRST in this same commit (do not relax the whitelist further).
  </action>
  <verify>
    <automated>rg -nq "PATTERN='\\\\\\\\b\\(claude\\|codex\\|gemini\\|opencode\\|telegram\\|serverchan\\|elevenlabs\\|grammy\\)\\\\\\\\b'" scripts/check-no-cut-agents.sh && ! rg -n "shared/src/voice.ts" scripts/check-no-cut-agents.sh && bash scripts/check-no-cut-agents.sh && test -x scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "telegram|serverchan|elevenlabs|grammy" scripts/check-no-cut-agents.sh` is at least 1 (PATTERN line carries all four)
    - `grep -c "claude|codex|gemini|opencode" scripts/check-no-cut-agents.sh` is at least 1 (Phase-1 keywords preserved)
    - `rg -n "shared/src/voice.ts" scripts/check-no-cut-agents.sh | wc -l` is 0 (dead whitelist line removed)
    - `bash scripts/check-no-cut-agents.sh` exits 0 with the extended PATTERN — proves zero non-whitelisted hits across all four cut keywords (Phase 2 SC#2 final)
    - `test -x scripts/check-no-cut-agents.sh` (executable bit preserved)
    - Script's hardcoded error message updated to reference the broader keyword set (verify by reading the script)
  </acceptance_criteria>
  <done>Ripgrep guard PATTERN + whitelist match Phase-2-final form; script passes against the new wider keyword set.</done>
</task>

<task type="auto">
  <name>Task 3: bun install + lockfile regen + frozen-lockfile check</name>
  <files>bun.lock</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"Standard Stack" (grammy ^1.38.4 + @elevenlabs/react ^0.13.0 already removed in earlier commits)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"Runtime State Inventory" row "Build artifacts / installed packages"
    - .planning/ROADMAP.md Phase 2 SC#3
    - hub/package.json (verify no grammy)
    - web/package.json (verify no @elevenlabs/react)
    - shared/package.json (verify no `./voice` subpath export)
    - package.json (root)
  </read_first>
  <action>
    1. Run `rg -n '"grammy"|"@elevenlabs/react"' package.json hub/package.json cli/package.json web/package.json shared/package.json` — must return ZERO hits (sanity verification that earlier commits did their job).
    2. Run `bun install` from repo root — refreshes `bun.lock` to drop transitive resolutions of `grammy` and `@elevenlabs/react`.
    3. Run `bun install --frozen-lockfile` — must exit 0 (proves the lockfile is in sync with all five `package.json` files).
    4. Verify `bun.lock` no longer references the removed packages: `! rg -n 'grammy|@elevenlabs/react' bun.lock`.
    5. Stage `bun.lock` (likely the only file changed by step 2).
  </action>
  <verify>
    <automated>! rg -n '"grammy"|"@elevenlabs/react"' package.json hub/package.json cli/package.json web/package.json shared/package.json && bun install --frozen-lockfile && ! rg -n '\bgrammy\b|@elevenlabs/react' bun.lock</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '"grammy"|"@elevenlabs/react"' package.json hub/package.json cli/package.json web/package.json shared/package.json | wc -l` is 0 (SC#3 final)
    - `bun install --frozen-lockfile` exits 0
    - `rg -n '\bgrammy\b|@elevenlabs/react' bun.lock | wc -l` is 0
    - `bun.lock` exists at repo root
  </acceptance_criteria>
  <done>Lockfile regenerated; frozen-lockfile check clean; SC#3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 4: Final phase gate — typecheck + test + guard + commit</name>
  <files>(commit only)</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-CONTEXT.md (D-30 commit #5 + D-31)
    - .planning/ROADMAP.md Phase 2 SC#1..#5
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"Phase 2 Success Criteria → Validation Map"
  </read_first>
  <action>
    1. `bun typecheck` from repo root — must exit 0 (SC#1).
    2. `bun run test` from repo root — must exit 0 (SC#1; chains test:guard via root scripts).
    3. `bash scripts/check-no-cut-agents.sh` — must exit 0 standalone (SC#2).
    4. `bun install --frozen-lockfile` — must exit 0 (SC#3 lockfile).
    5. SC#4 final check: `! rg -n 'TELEGRAM_BOT_TOKEN|TELEGRAM_NOTIFICATION|SERVERCHAN_SENDKEY|SERVERCHAN_NOTIFICATION' cli/src hub/src web/src shared/src`.
    6. SC#5 final check: `rg -A 5 'authBodySchema' hub/src/web/routes/auth.ts` shows the schema is `z.object({ accessToken: z.string() })` (no union). The existing access-token positive-case test (whichever file covers it — `auth.test.ts` or socket/server integration test per RESEARCH §"Wave 0 Gaps") is part of `bun run test` and stays green.
    7. Commit:
       ```
       git add -A
       git commit -m "$(cat <<'EOF'
chore(phase-02): final cleanup + ripgrep guard update

- Strip TELEGRAM_BOT_TOKEN from cli/src/terminal/TerminalManager.ts SENSITIVE_ENV_KEYS (D-33)
- Rewrite cli/src/commands/notify.ts error message to drop Telegram reference (D-33)
- Scrub residual Telegram-themed banner / comments in hub/src/index.ts (D-27 zero-tolerance)
- Extend scripts/check-no-cut-agents.sh PATTERN with telegram|serverchan|elevenlabs|grammy (D-29)
- Drop dead --glob '!shared/src/voice.ts' whitelist line (file removed in CUT-07 commit)
- bun install → bun.lock refreshed; bun install --frozen-lockfile clean (SC#3)

Closes: Phase 2 (CUT-06, CUT-07, CUT-08 complete; SC#1..#5 all green)
Refs: D-22, D-27, D-28, D-29, D-30 commit #5, D-31, D-32, D-33
EOF
)"
       ```
    8. Confirm `git log --oneline -5` shows exactly five commits in D-30 order: CUT-06 hub-side → CUT-06 web-side → CUT-07 → CUT-08 → cleanup. Confirm `git status --porcelain` is empty.
  </action>
  <verify>
    <automated>bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh && bun install --frozen-lockfile && ! rg -n 'TELEGRAM_BOT_TOKEN|TELEGRAM_NOTIFICATION|SERVERCHAN_SENDKEY|SERVERCHAN_NOTIFICATION' cli/src hub/src web/src shared/src && git log -1 --format='%s' | grep -q 'final cleanup + ripgrep guard update'</automated>
  </verify>
  <acceptance_criteria>
    - **SC#1:** `bun typecheck && bun run test` both exit 0
    - **SC#2:** `bash scripts/check-no-cut-agents.sh` exits 0 with extended PATTERN
    - **SC#3:** `! rg -n '"grammy"|"@elevenlabs/react"' package.json hub/package.json cli/package.json web/package.json shared/package.json` AND `notificationChannels` array in `hub/src/index.ts` references only PushNotificationChannel
    - **SC#4:** `! rg -n 'TELEGRAM_BOT_TOKEN|TELEGRAM_NOTIFICATION|SERVERCHAN_SENDKEY|SERVERCHAN_NOTIFICATION' cli/src hub/src web/src shared/src`
    - **SC#5:** `hub/src/web/routes/auth.ts` `authBodySchema` is `z.object({ accessToken: z.string() })` and access-token positive-case test stays green within `bun run test`
    - `git log -1 --format='%s'` matches `chore(phase-02): final cleanup + ripgrep guard update`
    - `git log --oneline -5` shows five commits in D-30 order
    - `git status --porcelain` empty
  </acceptance_criteria>
  <done>Phase 2 closes; all five SC threads green; 5 commits in D-30 order; D-31 per-commit gate satisfied on each.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-1 | Information disclosure | TELEGRAM_BOT_TOKEN no longer redacted in terminal sessions after removal from `SENSITIVE_ENV_KEYS` | accept (low) | The token has no remaining consumer after Phase 2; users with stale env-vars set on their shell would briefly see them in CLI terminal output. Single-user Tailscale topology + no telegram code path means there's no exfiltration vector. Documented behavior. |
| T-02-05-2 | Tampering | Ripgrep guard PATTERN regression — if PATTERN is malformed, guard silently passes despite hits | mitigate (medium) | Task 2 step 7 explicitly runs the guard standalone and asserts exit 0. If the script syntax breaks, `set -euo pipefail` causes a non-zero exit; CI sees the failure. Manual verification of the exact PATTERN string in `<acceptance_criteria>`. |
| T-02-05-3 | Tampering | Stale `bun.lock` carries `grammy` / `@elevenlabs/react` resolutions even after package.json deps removed | mitigate (medium) | Task 3 explicitly runs `bun install` and verifies `! rg -n 'grammy|@elevenlabs/react' bun.lock`. `bun install --frozen-lockfile` proves sync. |
| T-02-05-4 | Repudiation | Phase 2 commit history doesn't reflect the closure | mitigate (low) | Task 4 step 8 verifies five commits in D-30 order via `git log --oneline -5`. Atomic per-commit gate (D-31) ensures bisect-friendliness. |
| T-02-05-SC | Tampering | npm install of new packages | n/a | No new installs; only resolution shake-out via `bun install`. Existing Phase-1 + Phase-2 deletions only. |
</threat_model>

<verification>
- `bun typecheck` green from repo root (SC#1)
- `bun run test` green from repo root — chains cli/hub/web vitest + bun:test + test:guard (SC#1 + SC#2)
- `bash scripts/check-no-cut-agents.sh` exits 0 with extended PATTERN (SC#2 standalone)
- `cli/src/terminal/TerminalManager.ts` clean of `TELEGRAM_BOT_TOKEN` literal
- `cli/src/commands/notify.ts` error message Telegram-free
- `hub/src/index.ts` clean of telegram refs
- `! rg -n '"grammy"|"@elevenlabs/react"' **/package.json` (SC#3)
- `bun install --frozen-lockfile` exits 0 (SC#3 lockfile)
- `! rg -n 'TELEGRAM_BOT_TOKEN|TELEGRAM_NOTIFICATION|SERVERCHAN_SENDKEY|SERVERCHAN_NOTIFICATION' cli/src hub/src web/src shared/src` (SC#4)
- `hub/src/web/routes/auth.ts` `authBodySchema = z.object({ accessToken: z.string() })` (SC#5)
- 5 commits land in D-30 order; each individually green per D-31
</verification>

<success_criteria>
- All five Phase 2 success criteria (SC#1..#5) demonstrably green
- Five atomic commits in repo log: CUT-06 hub, CUT-06 web, CUT-07, CUT-08, cleanup (D-30)
- Ripgrep guard PATTERN extended to include telegram|serverchan|elevenlabs|grammy; whitelist-cleared dead voice.ts entry
- `bun.lock` regenerated and frozen-check clean
- Phase 2 ready to close → STATE.md update + advance to Phase 3
</success_criteria>

<output>
Create `.planning/phases/02-cut-external-integration-channels/02-05-SUMMARY.md` listing:
- Final SC#1..#5 verification results (each command + exit code)
- CLI residuals scrubbed (TerminalManager + notify.ts disposition: in-place rewrite vs file deletion)
- hub/src/index.ts banner audit outcome (lines removed if any)
- Ripgrep guard PATTERN before/after; whitelist diff (line added / line removed)
- bun.lock diff summary (file size delta; package count delta)
- Any deviations from RESEARCH.md / CONTEXT.md with rationale
- Hand-off to Phase 3: confirm namespace concept still in place (CUT-09 territory; not touched here); JWT payload still carries `{ uid, ns }` shape per RESEARCH §"D-24 Auth Schema Convergence"
</output>
