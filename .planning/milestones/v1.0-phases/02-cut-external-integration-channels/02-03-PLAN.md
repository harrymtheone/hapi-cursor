---
phase: 02-cut-external-integration-channels
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - hub/src/web/routes/voice.ts
  - hub/src/web/server.ts
  - web/src/realtime/
  - web/src/lib/voice-context.tsx
  - web/src/api/voice.ts
  - web/src/components/VoiceErrorBanner.tsx
  - web/src/components/SessionChat.tsx
  - web/src/components/AssistantChat/ComposerButtons.tsx
  - web/src/components/AssistantChat/HappyComposer.tsx
  - web/src/components/AssistantChat/StatusBar.tsx
  - web/src/App.tsx
  - web/src/lib/locales/en.ts
  - web/src/lib/locales/zh-CN.ts
  - web/src/lib/languages.ts
  - web/src/routes/settings/index.tsx
  - web/src/routes/settings/index.test.tsx
  - web/vite.config.ts
  - web/package.json
  - shared/src/voice.ts
  - shared/package.json
autonomous: true
requirements: [CUT-07]
user_setup: []

must_haves:
  truths:
    - "hub/src/web/routes/voice.ts and shared/src/voice.ts do not exist"
    - "web/src/realtime/ directory does not exist (D-19)"
    - "@elevenlabs/react not declared in web/package.json (SC#3)"
    - "shared/package.json no longer exports './voice' subpath"
    - "Composer voice button + StatusBar voice indicator + HappyComposer voice input mode + SessionChat <VoiceProvider> all removed (D-20)"
    - "Voice Assistant section in web/src/routes/settings/index.tsx deleted"
    - "All 18 'voice.*' / 'composer.voice' / 'settings.voice.*' i18n keys removed from each locale (en + zh-CN)"
    - "rg -ni 'elevenlabs' web/src/ shared/src/ hub/src/ returns 0 hits"
    - "bun typecheck && bun run test exit 0 after this commit (D-31)"
  artifacts:
    - path: "hub/src/web/routes/voice.ts"
      absent: true
    - path: "web/src/realtime"
      absent: true
    - path: "shared/src/voice.ts"
      absent: true
    - path: "web/src/lib/voice-context.tsx"
      absent: true
    - path: "web/src/api/voice.ts"
      absent: true
    - path: "web/src/components/VoiceErrorBanner.tsx"
      absent: true
    - path: "web/package.json"
      provides: "No @elevenlabs/react dep"
    - path: "shared/package.json"
      provides: "No ./voice subpath export"
  key_links:
    - from: "hub/src/web/server.ts"
      to: "(deleted) hub/src/web/routes/voice.ts"
      via: "Voice route registration removed"
      pattern: "voice"
    - from: "web/src/components/SessionChat.tsx"
      to: "(deleted) web/src/lib/voice-context.tsx"
      via: "VoiceProvider wrapper removed"
      pattern: "VoiceProvider"
---

<objective>
Commit #3 of D-30: physically delete the entire ElevenLabs voice surface across hub + web + shared per CUT-07. Drop `hub/src/web/routes/voice.ts` + its registration in `hub/src/web/server.ts`, the entire `web/src/realtime/` directory (D-19 verified all 8 importers serve voice), `web/src/lib/voice-context.tsx`, `web/src/api/voice.ts`, `web/src/components/VoiceErrorBanner.tsx`, voice provider/buttons/indicators in `SessionChat.tsx` + `AssistantChat/{ComposerButtons,HappyComposer,StatusBar}.tsx` + `App.tsx`, the entire Voice Assistant section + helpers in `web/src/routes/settings/index.tsx` (~L754-805+, helpers L276-405) + the corresponding `getElevenLabsSupportedLanguages` mock at `index.test.tsx` L91, the `elevenLabsCode` column + helpers in `web/src/lib/languages.ts`, the `@elevenlabs/react` vendor-chunk rule in `web/vite.config.ts` (L28-30), the `@elevenlabs/react` dep in `web/package.json`, and `shared/src/voice.ts` + the `./voice` subpath export in `shared/package.json` (L14). Drop the 18 voice-related i18n keys per locale per RESEARCH §"i18n Voice Key Inventory".

Purpose: Third atomic commit in D-30; closes CUT-07.
Output: One commit `feat(phase-02): CUT-07 remove ElevenLabs voice`. `bun typecheck && bun run test` green at commit (D-31).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cut-external-integration-channels/02-CONTEXT.md
@.planning/phases/02-cut-external-integration-channels/02-RESEARCH.md
@.planning/phases/02-cut-external-integration-channels/02-VALIDATION.md
@.planning/phases/02-cut-external-integration-channels/02-02-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete hub voice route + shared/src/voice.ts + shared package export</name>
  <files>hub/src/web/routes/voice.ts, hub/src/web/server.ts, shared/src/voice.ts, shared/package.json</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-CONTEXT.md (D-19, D-21)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"D-32 Settings Field Inventory" subsection §"`shared/` re-exports" (confirms shared/src/schemas.ts does NOT re-export voice)
    - hub/src/web/routes/voice.ts (entire file)
    - hub/src/web/server.ts (focus voice-route registration site)
    - shared/src/voice.ts (entire 257-line file)
    - shared/package.json (focus L14 `"./voice"` subpath export)
    - shared/src/schemas.ts (verify no voice re-exports)
  </read_first>
  <action>
    1. Delete `hub/src/web/routes/voice.ts` outright.
    2. In `hub/src/web/server.ts`: drop `import { createVoiceRoutes } from './routes/voice'` and the `app.route('/voice', createVoiceRoutes(...))` registration line.
    3. Delete `shared/src/voice.ts` outright.
    4. In `shared/package.json` L14: remove the `"./voice": "./src/voice.ts"` entry from the `exports` map. Other subpath exports stay.
    5. Verify `shared/src/schemas.ts` does NOT re-export voice — `rg -n 'voice|Voice' shared/src/schemas.ts` should be 0; if not, drop the offending re-export line.
    6. Run `cd hub && bun run typecheck` and `cd shared && bun run typecheck` mid-task — both must exit 0. Web typecheck stays red until Task 2 finishes consumers.
  </action>
  <verify>
    <automated>! test -f hub/src/web/routes/voice.ts && ! test -f shared/src/voice.ts && ! rg -n '\./voice' shared/package.json && ! rg -n 'createVoiceRoutes|routes/voice' hub/src/web/server.ts && cd hub && bun run typecheck && cd ../shared && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f hub/src/web/routes/voice.ts`
    - `! test -f shared/src/voice.ts`
    - `rg -n '"\./voice"' shared/package.json | wc -l` is 0
    - `rg -n 'createVoiceRoutes|routes/voice' hub/src/web/server.ts | wc -l` is 0
    - `rg -ni 'voice' shared/src/schemas.ts | wc -l` is 0
    - `cd hub && bun run typecheck` exits 0
    - `cd shared && bun run typecheck` exits 0
  </acceptance_criteria>
  <done>Hub voice route + shared voice module + shared package export gone; hub + shared typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete web/src/realtime/ + voice-context + api/voice + VoiceErrorBanner; strip voice from SessionChat / AssistantChat / App</name>
  <files>web/src/realtime/, web/src/lib/voice-context.tsx, web/src/api/voice.ts, web/src/components/VoiceErrorBanner.tsx, web/src/components/SessionChat.tsx, web/src/components/AssistantChat/ComposerButtons.tsx, web/src/components/AssistantChat/HappyComposer.tsx, web/src/components/AssistantChat/StatusBar.tsx, web/src/App.tsx</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-CONTEXT.md (D-19, D-20)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"D-19 Hypothesis Verification — web/src/realtime/ consumers" (8 files, all voice)
    - web/src/realtime/ (list contents before deletion)
    - web/src/lib/voice-context.tsx
    - web/src/api/voice.ts
    - web/src/components/VoiceErrorBanner.tsx
    - web/src/components/SessionChat.tsx (focus VoiceProvider wrap)
    - web/src/components/AssistantChat/ComposerButtons.tsx (voice mic button)
    - web/src/components/AssistantChat/HappyComposer.tsx (voice input mode)
    - web/src/components/AssistantChat/StatusBar.tsx (voice recording status)
    - web/src/App.tsx (focus <VoiceProvider> wrap + <VoiceErrorBanner/>)
  </read_first>
  <action>
    1. Delete entire directory `web/src/realtime/`.
    2. Delete `web/src/lib/voice-context.tsx`.
    3. Delete `web/src/api/voice.ts`.
    4. Delete `web/src/components/VoiceErrorBanner.tsx`.
    5. `web/src/components/SessionChat.tsx` (D-20): drop `VoiceProvider` import + wrapper; children render directly.
    6. `web/src/components/AssistantChat/ComposerButtons.tsx` (D-20): delete voice mic button JSX + `useVoice` import + handler.
    7. `web/src/components/AssistantChat/HappyComposer.tsx` (D-20): delete voice input mode branches + voice state + voice imports.
    8. `web/src/components/AssistantChat/StatusBar.tsx` (D-20): delete voice-recording status indicator + `useVoice`-derived reads.
    9. `web/src/App.tsx`: drop `<VoiceProvider>` wrap (replace with passthrough) and `<VoiceErrorBanner/>` mount + import.
    10. Per Discretion: do NOT add placeholder/disabled voice button. UI converges naturally.
    11. Run `cd web && bun run typecheck` mid-task — must reach 0 errors before continuing to Task 3.
  </action>
  <verify>
    <automated>! test -e web/src/realtime && ! test -f web/src/lib/voice-context.tsx && ! test -f web/src/api/voice.ts && ! test -f web/src/components/VoiceErrorBanner.tsx && ! rg -n 'VoiceProvider|VoiceErrorBanner|useVoice|voice-context' web/src/components/ web/src/App.tsx && cd web && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `! test -e web/src/realtime`
    - `! test -f web/src/lib/voice-context.tsx`
    - `! test -f web/src/api/voice.ts`
    - `! test -f web/src/components/VoiceErrorBanner.tsx`
    - `rg -n 'VoiceProvider|VoiceErrorBanner|useVoice|voice-context' web/src/components/ web/src/App.tsx | wc -l` is 0
    - `cd web && bun run typecheck` exits 0
  </acceptance_criteria>
  <done>Web realtime/voice-context/api modules + VoiceErrorBanner gone; chat composer + App tree no longer reference voice.</done>
</task>

<task type="auto">
  <name>Task 3: Strip voice from settings page + i18n + languages helpers + vite config + remove @elevenlabs/react dep; commit</name>
  <files>web/src/routes/settings/index.tsx, web/src/routes/settings/index.test.tsx, web/src/lib/locales/en.ts, web/src/lib/locales/zh-CN.ts, web/src/lib/languages.ts, web/vite.config.ts, web/package.json</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"i18n Voice Key Inventory" (full table — 18 keys × 2 locales)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"Standard Stack" (web/package.json:17 — @elevenlabs/react ^0.13.0)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"Test Fixture Stripping Plan" (settings/index.test.tsx L91 mock)
    - web/src/routes/settings/index.tsx (focus L754-805+ Voice Assistant section + L276-405 helpers + L32 imports)
    - web/src/routes/settings/index.test.tsx (focus L91 getElevenLabsSupportedLanguages mock)
    - web/src/lib/locales/en.ts (focus L325 'composer.voice', L338-351 'voice.*', L412-414 'settings.voice.*')
    - web/src/lib/locales/zh-CN.ts (focus L327 + L340-353 + L414-416)
    - web/src/lib/languages.ts (focus elevenLabsCode column + getElevenLabsSupportedLanguages helper)
    - web/vite.config.ts (focus L28-30 manualChunks @elevenlabs rule)
    - web/package.json (focus L17 @elevenlabs/react dep)
  </read_first>
  <action>
    1. `web/src/routes/settings/index.tsx`:
       - Delete the entire Voice Assistant section JSX (~L754-805+; locate by header text "Voice" / `t('settings.voice.title')`).
       - Delete the helper functions used only by that section (~L276-405; functions referencing `getElevenLabsSupportedLanguages`, voice agent config, etc.).
       - Drop voice-related imports at the top of the file (~L32 region; identify by `voice` / `elevenlabs` / `getElevenLabs*` substrings).
       - Verify after edit: `rg -ni 'voice|elevenlabs' web/src/routes/settings/index.tsx | wc -l` is 0.
    2. `web/src/routes/settings/index.test.tsx`: delete the `getElevenLabsSupportedLanguages: () => [...]` mock entry at L91 (RESEARCH §"Test Fixture Stripping Plan" tags this commit, not commit #5). Delete any test cases that exclusively cover the deleted Voice Assistant section.
    3. `web/src/lib/locales/en.ts`: delete the 18 keys per RESEARCH inventory — `'composer.voice'` (L325), `'voice.connecting' / 'voice.active' / 'voice.muted' / 'voice.error' / 'voice.mute' / 'voice.unmute' / 'voice.end' / 'voice.error.micPermission' / 'voice.error.network' / 'voice.error.notInitialized' / 'voice.error.startFailed' / 'voice.error.notAllowed' / 'voice.error.connection' / 'voice.dismiss'` (L338-351), `'settings.voice.title' / 'settings.voice.language' / 'settings.voice.autoDetect'` (L412-414).
    4. `web/src/lib/locales/zh-CN.ts`: mirror — same 18 keys at lines ~327, 340-353, 414-416.
    5. `web/src/lib/languages.ts`: drop the `elevenLabsCode` column from the language table + any `getElevenLabsSupportedLanguages` / similar exported helpers. Keep the rest of the language table intact.
    6. `web/vite.config.ts` L28-30: drop the manualChunks rule that emits a vendor-voice chunk for `@elevenlabs/react`. Other chunk rules stay.
    7. `web/package.json` L17: remove `"@elevenlabs/react": "^0.13.0"` from `dependencies`. Do NOT run `bun install` (commit #5 owns lockfile regen).
    8. Per-commit gate (D-31): from repo root, `bun typecheck && bun run test`. Both must exit 0.
    9. Commit with `feat(phase-02): CUT-07 remove ElevenLabs voice` and a body listing: deleted dirs/files, settings-page section + helpers removed, i18n keys removed (count + locales), languages.ts column removed, vite manualChunks rule dropped, `@elevenlabs/react` dep removed, `shared/src/voice.ts` + `./voice` subpath removed. Refs: `D-19 D-20 D-21 D-30 commit #3 D-31`.
  </action>
  <verify>
    <automated>! rg -ni 'elevenlabs' web/ shared/ hub/ && ! rg -n "'voice\.|composer\.voice|settings\.voice" web/src/lib/locales/ && ! rg -n '@elevenlabs/react' web/package.json && ! rg -n 'elevenLabsCode|getElevenLabsSupportedLanguages' web/src/ && bun typecheck && bun run test && git log -1 --format=%s | grep -Eq 'feat\(phase-02\): CUT-07 remove ElevenLabs voice'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -ni 'elevenlabs' web/ shared/ hub/ | wc -l` is 0 (D-27 voice scope; whitelist will catch any docs/website hits later in 02-05)
    - `rg -n "'voice\.|composer\.voice|settings\.voice" web/src/lib/locales/en.ts web/src/lib/locales/zh-CN.ts | wc -l` is 0
    - `rg -n '@elevenlabs/react' web/package.json | wc -l` is 0 (SC#3 voice-side)
    - `rg -n 'elevenLabsCode|getElevenLabsSupportedLanguages' web/src/ | wc -l` is 0
    - `rg -n 'manualChunks.*elevenlabs' web/vite.config.ts | wc -l` is 0
    - `rg -ni 'voice' web/src/routes/settings/index.tsx | wc -l` is 0
    - `bun typecheck` exits 0 (D-31, all packages)
    - `bun run test` exits 0 (D-31)
    - `git log -1 --format=%s` matches `feat(phase-02): CUT-07 remove ElevenLabs voice`
    - `git status --porcelain` empty
  </acceptance_criteria>
  <done>CUT-07 closed. Voice surface fully gone from settings UI, i18n, languages helpers, vite chunking, and web deps.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-1 | Information disclosure | Stale `xi-api-key` env-var read or settings.json secret left over after voice route deletion | mitigate (low) | Per RESEARCH §"Runtime State Inventory" + D-32, code stops reading the secret; user-set values become inert. No new ripgrep keyword for the secret name itself; commit #5 (02-05) extends guard PATTERN to include `elevenlabs` (catches any new code path). |
| T-02-03-2 | Tampering | Dead-code rehydration of `web/src/realtime/` if only some files deleted | mitigate (low) | Directory-level delete in Task 2 step 1; verified by `! test -e web/src/realtime`. |
| T-02-03-3 | Denial of service | Voice provider unwrapped from SessionChat → child components still calling `useVoice()` would throw | mitigate (low) | Task 2 explicitly removes every `useVoice` call site (Composer / Status / HappyComposer); typecheck would fail otherwise. `cd web && bun run typecheck` is mandatory mid-task. |
| T-02-03-4 | Repudiation / Confusion | `docs/public/schemas/settings.schema.json` still lists voice fields | accept (medium) | Per D-28 + RESEARCH Open Q #3, schema regen is owned by Phase 10/12; Phase 2 whitelists the path in 02-05's guard update. |
| T-02-03-SC | Tampering | npm install of new packages | n/a | No installs; pure deletion. |
</threat_model>

<verification>
- `bun typecheck` green from repo root (D-31, all 4 packages)
- `bun run test` green from repo root (D-31)
- `rg -ni 'elevenlabs' cli/src hub/src web/src shared/src | wc -l` = 0
- `rg -n '@elevenlabs/react' web/package.json` = 0 (SC#3 voice-side)
- File-existence assertions per task `<acceptance_criteria>`
- Single commit `feat(phase-02): CUT-07 remove ElevenLabs voice`
</verification>

<success_criteria>
- All three task `<acceptance_criteria>` blocks pass
- D-31 per-commit gate satisfied
- Phase 2 SC#1 voice-side, SC#3 voice-side (`@elevenlabs/react` gone) met
- `web/src/routes/settings/index.tsx` no longer references voice — covered by SC#1 typecheck path
</success_criteria>

<output>
Create `.planning/phases/02-cut-external-integration-channels/02-03-SUMMARY.md` listing:
- Files deleted (full paths)
- Settings page Voice Assistant section LOC removed (before/after)
- i18n keys removed (18 × 2 locales = 36 lines)
- vite manualChunks rule removed
- `@elevenlabs/react` removed from `web/package.json`
- `shared/src/voice.ts` + `./voice` subpath export removed
- D-31 gate result (bun typecheck + bun run test exit codes)
</output>
