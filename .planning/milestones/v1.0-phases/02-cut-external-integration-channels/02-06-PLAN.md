---
phase: 02-cut-external-integration-channels
plan: 06
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - web/src/api/client.ts
  - hub/src/web/middleware/auth.ts
  - web/src/lib/languages.ts
autonomous: true
requirements: [CUT-06, CUT-07]
user_setup: []

must_haves:
  truths:
    - "HI-01 closed: rg -n 'fetchVoiceToken' web/src/ returns 0 matches (orphan ElevenLabs token-fetch method removed from web/src/api/client.ts)"
    - "HI-02 closed: rg -nF \"path === '/api/bind'\" hub/src/web/middleware/auth.ts returns 0 matches (stale Telegram-bind auth-bypass clause removed)"
    - "HI-03 closed: test ! -f web/src/lib/languages.ts (entire 86-LOC ElevenLabs-voice language registry deleted; no consumers remain)"
    - "bun typecheck exits 0 from repo root after all three deletions"
    - "bun run test exits 0 from repo root after all three deletions"
    - "bash scripts/check-no-cut-agents.sh exits 0 (Phase-2 ripgrep guard still passes with no new hits introduced by these deletions)"
    - "rg -n 'from.*@/lib/languages|getLanguageDisplayName|findLanguageByCode|\\bLANGUAGES\\b' web/src/ returns 0 matches (HI-03 reachability check from VERIFICATION.md)"
  artifacts:
    - path: "web/src/api/client.ts"
      provides: "ApiClient class without the orphan fetchVoiceToken() method"
      pattern: "class ApiClient"
    - path: "hub/src/web/middleware/auth.ts"
      provides: "Auth middleware with only the /api/auth whitelist clause (no /api/bind residual)"
      pattern: "path === '/api/auth'"
  key_links:
    - from: "web/src/api/client.ts"
      to: "(removed) fetchVoiceToken() method body"
      via: "method deletion lines 508-518"
      pattern: "fetchVoiceToken"
    - from: "hub/src/web/middleware/auth.ts"
      to: "(removed) `|| path === '/api/bind'` whitelist clause"
      via: "condition simplification on line 20"
      pattern: "/api/bind"
    - from: "web/src/lib/languages.ts"
      to: "(removed) entire file"
      via: "file deletion"
      pattern: "languages.ts"
---

<objective>
Close the three High-severity dead-code remnants flagged by 02-VERIFICATION.md (status: gaps_found) so Phase 2's goal — "Telegram bot, ElevenLabs voice route, and ServerChan push channel are fully removed from hub, web, and shared" — is observably true:

1. **HI-01** — Delete the orphan `fetchVoiceToken()` method (lines 508-518) from `web/src/api/client.ts`. It targets the deleted `/api/voice/token` route, has zero callers in `web/src/`, and ships in the production bundle.
2. **HI-02** — Drop the stale `|| path === '/api/bind'` clause from the auth-bypass whitelist in `hub/src/web/middleware/auth.ts:20`. The `/api/bind` route was deleted in Phase 2 but the bypass clause survived (confused-deputy class defect).
3. **HI-03** — Delete `web/src/lib/languages.ts` (86 LOC: `Language` type, `LANGUAGES` constant of 47 entries, `getLanguageDisplayName`, `findLanguageByCode`). Grep across `web/src/` confirms zero importers — the file is fully unreachable post-CUT-07.

Purpose: Close VERIFICATION.md High-severity gaps so Phase 2 transitions from `status: gaps_found` to `status: verified`. Single atomic commit; minimum-diff.

Output: `chore(phase-02): close verification gaps HI-01..HI-03 (dead-code removal)` commit. `bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh` all exit 0. All three Grep post-conditions return 0 matches.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cut-external-integration-channels/02-CONTEXT.md
@.planning/phases/02-cut-external-integration-channels/02-VERIFICATION.md
@.planning/phases/02-cut-external-integration-channels/02-05-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HI-01 — Delete orphan fetchVoiceToken() method from web/src/api/client.ts</name>
  <files>web/src/api/client.ts</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-VERIFICATION.md §"HI-01" (full finding + reachability check)
    - web/src/api/client.ts (full file — confirm method spans exactly lines 508-518 before editing; locate the method block by signature `fetchVoiceToken` rather than relying solely on line numbers in case of prior edits)
  </read_first>
  <action>
    1. Open `web/src/api/client.ts`. Locate the `fetchVoiceToken()` method declaration (per VERIFICATION.md HI-01: lines 508-518). The method targets the already-deleted `/api/voice/token` route.
    2. Delete the entire method block from the opening `fetchVoiceToken(` line through the closing `}` of the method (inclusive). Remove any trailing blank line that the deletion would leave as a double-blank.
    3. Do NOT modify any other method on the `ApiClient` class. Do NOT touch imports — `fetchVoiceToken` referenced no unique imports per VERIFICATION.md (any types it used are shared with retained methods).
    4. Confirm minimum-diff: only the method body lines + its bracketing whitespace are removed; surrounding methods (the method directly before line 508 and the one starting after 518) are byte-identical.
    5. Run `bun typecheck` mid-task — must exit 0.
  </action>
  <verify>
    <automated>! rg -n 'fetchVoiceToken' web/src/ && bun typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'fetchVoiceToken' web/src/ | wc -l` is 0 (method name no longer appears anywhere under `web/src/`)
    - `rg -n '/api/voice/token' web/src/ | wc -l` is 0 (route literal also gone with the method)
    - `bun typecheck` exits 0 (no dangling references; ApiClient class still compiles)
    - `git diff --stat web/src/api/client.ts` shows only deletions, no insertions
  </acceptance_criteria>
  <done>HI-01 closed: orphan ElevenLabs token-fetch method removed; production bundle no longer ships dead `/api/voice/token` client code.</done>
</task>

<task type="auto">
  <name>Task 2: HI-02 — Drop /api/bind clause from hub/src/web/middleware/auth.ts whitelist</name>
  <files>hub/src/web/middleware/auth.ts</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-VERIFICATION.md §"HI-02" (confused-deputy framing + exact line)
    - hub/src/web/middleware/auth.ts (full file — small middleware; verify line 20 still matches `if (path === '/api/auth' || path === '/api/bind')` form before editing)
  </read_first>
  <action>
    1. Open `hub/src/web/middleware/auth.ts`. Locate line 20: `if (path === '/api/auth' || path === '/api/bind')` (per VERIFICATION.md HI-02).
    2. Edit the condition to drop the second clause. After edit, line 20 must read: `if (path === '/api/auth')` (preserve exact surrounding whitespace, brace style, and trailing `{` if present — minimum-diff).
    3. Do NOT touch any other line in the file. Do NOT remove the surrounding `if`/branch — only the `|| path === '/api/bind'` substring is excised.
    4. After edit, the substring `/api/bind` must not appear anywhere in `hub/src/web/middleware/auth.ts`.
    5. Run `bun typecheck` mid-task — must exit 0.
  </action>
  <verify>
    <automated>! rg -nF "/api/bind" hub/src/web/middleware/auth.ts && rg -nF "path === '/api/auth'" hub/src/web/middleware/auth.ts && bun typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -nF "path === '/api/bind'" hub/src/web/middleware/auth.ts | wc -l` is 0 (stale bypass clause removed)
    - `rg -nF "/api/bind" hub/src/web/middleware/auth.ts | wc -l` is 0 (no residual references anywhere in the file)
    - `rg -nF "path === '/api/auth'" hub/src/web/middleware/auth.ts | wc -l` is at least 1 (legitimate `/api/auth` whitelist clause preserved)
    - `bun typecheck` exits 0
    - `git diff --stat hub/src/web/middleware/auth.ts` shows a 1-line modification (no other lines changed)
  </acceptance_criteria>
  <done>HI-02 closed: confused-deputy auth-bypass clause for the deleted `/api/bind` route is gone; only the live `/api/auth` route remains on the whitelist.</done>
</task>

<task type="auto">
  <name>Task 3: HI-03 — Delete unimported web/src/lib/languages.ts (86 LOC)</name>
  <files>web/src/lib/languages.ts</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-VERIFICATION.md §"HI-03" (full reachability check: `Grep 'from.*@/lib/languages|getLanguageDisplayName|findLanguageByCode|LANGUAGES\b' web/src/` returns 0)
    - web/src/lib/languages.ts (read once to confirm exports match VERIFICATION.md's inventory: `Language` type, `LANGUAGES` constant, `getLanguageDisplayName`, `findLanguageByCode` — and nothing else lives in this file)
  </read_first>
  <action>
    1. Before deletion, run the VERIFICATION.md reachability check exactly as written to confirm zero importers remain at execution time:
       `rg -n 'from.*@/lib/languages|getLanguageDisplayName|findLanguageByCode|\bLANGUAGES\b' web/src/`
       This MUST return 0 matches. If it returns ANY match, STOP and surface the consumer to the user — do NOT proceed with deletion (a consumer would mean the file is reachable and Plan 02-03's SUMMARY was correct).
    2. Delete the file: `rm web/src/lib/languages.ts` (or equivalent file-deletion tool action). The file is fully self-contained per VERIFICATION.md — no other file in the repo imports from it.
    3. Do NOT delete the parent `web/src/lib/` directory (other modules under `web/src/lib/` are retained).
    4. Do NOT touch any other file in this task — HI-03's fix is exactly one file deletion.
    5. Run `bun typecheck` mid-task — must exit 0 (proves zero dangling imports across the workspace, confirming step 1's reachability check at the type-system level).
  </action>
  <verify>
    <automated>test ! -f web/src/lib/languages.ts && ! rg -n 'from.*@/lib/languages|getLanguageDisplayName|findLanguageByCode|\bLANGUAGES\b' web/src/ && bun typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f web/src/lib/languages.ts` (file no longer exists on disk)
    - `rg -n 'from.*@/lib/languages|getLanguageDisplayName|findLanguageByCode|\bLANGUAGES\b' web/src/ | wc -l` is 0 (no consumers, no symbol references)
    - `bun typecheck` exits 0 (no dangling imports across hub/web/cli/shared)
    - `git status --porcelain web/src/lib/languages.ts` shows a deletion (`D`)
  </acceptance_criteria>
  <done>HI-03 closed: 86 LOC of unimported ElevenLabs-voice language registry removed; web/src/lib/ contains only reachable modules.</done>
</task>

</tasks>

<verification>
- `bun typecheck` exits 0 from repo root
- `bun run test` exits 0 from repo root (chains cli/hub/web vitest + bun:test + test:guard)
- `bash scripts/check-no-cut-agents.sh` exits 0 (Phase-2 ripgrep guard with extended PATTERN still passes)
- HI-01: `rg -n 'fetchVoiceToken' web/src/ | wc -l` is 0
- HI-02: `rg -nF "path === '/api/bind'" hub/src/web/middleware/auth.ts | wc -l` is 0
- HI-03: `test ! -f web/src/lib/languages.ts`
- HI-03 reachability: `rg -n 'from.*@/lib/languages|getLanguageDisplayName|findLanguageByCode|\bLANGUAGES\b' web/src/ | wc -l` is 0
- `git status --porcelain` is empty after the closure commit
</verification>

<success_criteria>
- All three High-severity gaps from 02-VERIFICATION.md observably closed (post-conditions in `must_haves.truths` all hold)
- Single atomic commit lands the three minimum-diff deletions
- Phase 2 verifier rerun can transition `status: gaps_found` → `status: verified`
- No regression: existing Phase-2 SC#1..#5 (covered by 02-05) remain green (`bun typecheck`, `bun run test`, ripgrep guard)
- No ME-/LO-/IN- items touched (out-of-scope per gap_closure_scope)
</success_criteria>

<output>
Create `.planning/phases/02-cut-external-integration-channels/02-06-SUMMARY.md` listing:
- HI-01 closure: line-range deleted from `web/src/api/client.ts`; pre/post `rg fetchVoiceToken` counts
- HI-02 closure: exact before/after of line 20 in `hub/src/web/middleware/auth.ts`; pre/post `rg /api/bind` counts
- HI-03 closure: file deletion confirmation; pre/post reachability-grep counts
- Verification command results (`bun typecheck`, `bun run test`, `bash scripts/check-no-cut-agents.sh` — each with exit code)
- Confirmation that no ME-/LO-/IN- items were touched
- Hand-off note: Phase 2 ready for verifier rerun → expected `status: verified`
</output>
