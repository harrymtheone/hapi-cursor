---
phase: 02-cut-external-integration-channels
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - web/src/hooks/useTelegram.ts
  - web/src/hooks/useAuthSource.ts
  - web/src/hooks/useAuth.ts
  - web/src/hooks/usePlatform.ts
  - web/src/hooks/useTheme.ts
  - web/src/hooks/useViewportHeight.ts
  - web/src/main.tsx
  - web/src/router.tsx
  - web/src/sw.ts
  - web/src/App.tsx
  - web/src/index.css
  - web/src/components/SessionHeader.tsx
  - web/src/components/InstallPrompt.tsx
  - web/src/components/LoginPrompt.tsx
  - web/src/components/AssistantChat/messages/AssistantMessage.tsx
  - web/src/components/AssistantChat/messages/UserMessage.tsx
  - web/src/api/client.ts
  - web/src/lib/locales/en.ts
  - web/src/lib/locales/zh-CN.ts
autonomous: true
requirements: [CUT-06]
user_setup: []

must_haves:
  truths:
    - "web/src/hooks/useTelegram.ts does not exist"
    - "useAuthSource collapses to single source = access-token (D-18)"
    - "usePlatform Platform union no longer carries isTelegram field; narrowed per D-17"
    - "Web app entry (main.tsx / router.tsx / sw.ts / App.tsx) has no Telegram WebApp SDK init or Mini App routing branches"
    - "web/src/index.css contains no [data-telegram-app] selectors"
    - "web/src/lib/locales/{en,zh-CN}.ts contain no 'login.bind.*' keys"
    - "ApiClient bind() method removed from web/src/api/client.ts"
    - "rg -ni 'telegram' web/src/ returns 0 hits (D-27)"
    - "bun typecheck && bun run test exit 0 after this commit (D-31)"
    - "sw.ts disposition: planner defers mechanical-strip-vs-rewrite to executor per RESEARCH Open Q #2 — executor reads sw.ts in Task 2 step 3, picks the choice, and documents it in the commit body"
  artifacts:
    - path: "web/src/hooks/useTelegram.ts"
      absent: true
    - path: "web/src/hooks/useAuthSource.ts"
      provides: "Collapsed access-token single source (D-18) — file may stay with one source variant or be deleted; either acceptable"
    - path: "web/src/hooks/useAuth.ts"
      provides: "AuthSource union flattened; bind() method removed; isNotBoundError gone"
    - path: "web/src/hooks/usePlatform.ts"
      provides: "Platform shape no longer exposes isTelegram (D-17)"
    - path: "web/src/index.css"
      provides: "No [data-telegram-app] selectors"
  key_links:
    - from: "web/src/api/client.ts"
      to: "hub /api/auth"
      via: "authenticate({ accessToken }) — body type narrowed to single shape"
      pattern: "accessToken"
    - from: "web/src/components/InstallPrompt.tsx"
      to: "web/src/hooks/usePlatform.ts"
      via: "platform consumer; telegram branch removed"
      pattern: "usePlatform"
---

<objective>
Commit #2 of D-30: physically purge the Telegram WebApp platform branch from the web package end-to-end (D-16/D-17/D-18). Delete `useTelegram.ts`, collapse `useAuthSource` to access-token-only, narrow `usePlatform` union by dropping `isTelegram`, scrub telegram branches from `useTheme` / `useViewportHeight` / `useAuth` / `main.tsx` / `router.tsx` / `sw.ts` / `App.tsx` / `SessionHeader.tsx` / `InstallPrompt.tsx` / `LoginPrompt.tsx` / `AssistantChat/messages/{Assistant,User}Message.tsx` / `api/client.ts`, drop the two `[data-telegram-app]` CSS rule blocks in `index.css`, and remove the three `login.bind.*` i18n keys from each locale.

Purpose: Second atomic commit in D-30; closes web-side surface of CUT-06 — the runtime contract drift introduced by 02-01 (web's `AuthSource = { type: 'telegram' } | ...` survived 02-01's typecheck because of per-package isolation per RESEARCH §"Pattern 2") is now resolved.
Output: One commit `feat(phase-02): CUT-06 remove Telegram WebApp platform (web-side)`. `bun typecheck && bun run test` green at commit (D-31).

**Scope rationale: locked by CONTEXT D-30.** CUT-06 web-side intentionally lands as one commit (19 files modified) for bisect ergonomics — this is the explicit D-30 decision to split CUT-06 into hub-side / web-side rather than further splitting by sub-surface. The 19-file scope is not a missed split.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cut-external-integration-channels/02-CONTEXT.md
@.planning/phases/02-cut-external-integration-channels/02-RESEARCH.md
@.planning/phases/02-cut-external-integration-channels/02-VALIDATION.md
@.planning/phases/02-cut-external-integration-channels/02-01-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse hook layer (useTelegram delete, useAuthSource/useAuth/usePlatform/useTheme/useViewportHeight rewrites)</name>
  <files>web/src/hooks/useTelegram.ts, web/src/hooks/useAuthSource.ts, web/src/hooks/useAuth.ts, web/src/hooks/usePlatform.ts, web/src/hooks/useTheme.ts, web/src/hooks/useViewportHeight.ts</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-CONTEXT.md (D-16, D-17, D-18, Discretion lines about delete-vs-shell + usePlatform impl)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"Web Telegram Consumer Inventory" (full table — refs + actions per file)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"D-24 Auth Schema Convergence" (Web-side knock-on paragraph)
    - web/src/hooks/useTelegram.ts (entire 144-line file)
    - web/src/hooks/useAuthSource.ts (focus L7-22, L78-86, L106-131)
    - web/src/hooks/useAuth.ts (focus L5-7 AuthSource union, L29-34 getAuthPayload, L36-38 isNotBoundError, L136-158 bind())
    - web/src/hooks/usePlatform.ts (focus L18 isTelegram field, L43-71 haptic + getTelegramWebApp branches)
    - web/src/hooks/useTheme.ts
    - web/src/hooks/useViewportHeight.ts
  </read_first>
  <action>
    1. **Delete `web/src/hooks/useTelegram.ts`** outright (Discretion: per CONTEXT recommendation, no empty shell).
    2. **`web/src/hooks/useAuthSource.ts`** (D-18):
       - Drop the telegram-source branch (L7-22 type-union member, L78-86 detection, L106-131 extraction logic).
       - Collapse `AuthSource` to single variant — accept-token only. If after collapse the hook is now a trivial wrapper, either keep it as `() => ({ type: 'access-token' as const })` OR delete the file and have consumers (only `useAuth.ts` per D-24 web-side knock-on) inline the constant. Either choice is acceptable per Discretion; minimum-diff preferred.
       - Update consumers if file is deleted.
    3. **`web/src/hooks/useAuth.ts`**:
       - `AuthSource` union (L5-7) → single variant or deletion.
       - Delete `getAuthPayload` helper (L29-34) — no longer needs branching.
       - Delete `isNotBoundError` helper (L36-38) — bind concept gone.
       - Delete the `bind()` method (L136-158).
       - Drop `setNeedsBinding` state and any telegram error branches.
       - Caller of `authenticate(...)`: pass `{ accessToken: <token> }` directly.
    4. **`web/src/hooks/usePlatform.ts`** (D-17):
       - Remove `isTelegram` field from the returned `Platform` shape.
       - Remove `getTelegramWebApp` import + the haptic branch using it (L43-67 region).
       - Per D-17, narrow union to `'pwa' | 'browser'` — implementation detail (Discretion): use `matchMedia('(display-mode: standalone)')` for PWA detection, otherwise `'browser'`. Keep haptic interface as a stub if consumer expects it (no-op if no Vibration API), or remove if no consumer uses it (verify via `rg -n 'usePlatform\(\).*haptic|platform\.haptic' web/src/`).
    5. **`web/src/hooks/useTheme.ts`**: identify telegram branches via `rg -n 'telegram\|Telegram\|tgWebApp' web/src/hooks/useTheme.ts`; drop them. Theme falls back to system preference (`matchMedia('(prefers-color-scheme: dark)')`).
    6. **`web/src/hooks/useViewportHeight.ts`**: same treatment — drop telegram branch; rely on `window.visualViewport` / `window.innerHeight`.
    7. Run `cd web && bun run typecheck` mid-task to surface any consumer that still depends on a deleted symbol; fix consumers in Task 2.
  </action>
  <verify>
    <automated>! test -f web/src/hooks/useTelegram.ts && ! rg -ni 'telegram|tgWebApp|isTelegram' web/src/hooks/ && ! rg -n 'isNotBoundError|bind\(' web/src/hooks/useAuth.ts</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f web/src/hooks/useTelegram.ts`
    - `rg -ni 'telegram|tgWebApp|isTelegram' web/src/hooks/ | wc -l` is 0
    - `rg -n 'getAuthPayload|isNotBoundError' web/src/hooks/useAuth.ts | wc -l` is 0
    - `rg -n '\\bbind\\(' web/src/hooks/useAuth.ts | wc -l` is 0
    - `rg -n 'isTelegram' web/src/hooks/usePlatform.ts | wc -l` is 0
    - Typecheck deferred to Task 2 — hook-layer consumers (components, app entry) are rewritten there; running typecheck in isolation here would fail by design.
  </acceptance_criteria>
  <done>Hook layer telegram-free; AuthSource single-variant; Platform union narrowed.</done>
</task>

<task type="auto">
  <name>Task 2: Strip Telegram from app entry + components + ApiClient + i18n + CSS</name>
  <files>web/src/main.tsx, web/src/router.tsx, web/src/sw.ts, web/src/App.tsx, web/src/components/SessionHeader.tsx, web/src/components/InstallPrompt.tsx, web/src/components/LoginPrompt.tsx, web/src/components/AssistantChat/messages/AssistantMessage.tsx, web/src/components/AssistantChat/messages/UserMessage.tsx, web/src/api/client.ts, web/src/lib/locales/en.ts, web/src/lib/locales/zh-CN.ts, web/src/index.css</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"Web Telegram Consumer Inventory" (table rows for these 13 files)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"CSS Telegram Class Inventory" (4 lines: 85, 213, 214, 218, 219)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"Open Questions" Q2 (sw.ts: verify mechanical strip vs rewrite)
    - web/src/main.tsx (focus L21 tgWebAppData start_param, L41-50 SDK load, L43 dataset assign, L76 history switch)
    - web/src/router.tsx (focus L23 isTelegramApp import, L445/L500 gated routes)
    - web/src/sw.ts (telegram references — read full file to determine impact)
    - web/src/App.tsx (focus L4 import, L57+L104 theme bridge, L164 isTelegramApp(), L386-397 telegram error UI)
    - web/src/components/SessionHeader.tsx (focus L4 import, L128-129 early-return)
    - web/src/components/InstallPrompt.tsx (focus L10, L12, L29)
    - web/src/components/LoginPrompt.tsx
    - web/src/components/AssistantChat/messages/AssistantMessage.tsx (verify: cosmetic vs structural)
    - web/src/components/AssistantChat/messages/UserMessage.tsx (verify: cosmetic vs structural)
    - web/src/api/client.ts (ApiClient bind() method + authenticate() body type)
    - web/src/lib/locales/en.ts (L15, L17, L19 — login.bind.*)
    - web/src/lib/locales/zh-CN.ts (L15, L17, L19 — login.bind.*)
    - web/src/index.css (L85, L213-219)
  </read_first>
  <action>
    1. **`web/src/main.tsx`**: drop the Telegram SDK script-tag injection (~L41-50), the `tgWebAppData` start_param parsing (~L21), the `documentElement.dataset.telegramApp` assignment (~L43), and the conditional history-strategy switch (~L76). Keep a single browser/PWA history strategy (`createBrowserHistory` or `createHashHistory` — pick whichever is the non-telegram default existing in the file).
    2. **`web/src/router.tsx`**: drop `isTelegramApp` import (~L23). For each gated route block (~L445, ~L500), if the route is reachable from non-telegram entry, ungate (just remove the `if (isTelegramApp())` wrapper). If the route was telegram-exclusive (e.g. a Mini-App-only route), delete the entire `app.route(...)` registration. Verify by reading the route bodies before deciding.
    3. **`web/src/sw.ts`**: per RESEARCH Open Question #2, read the file fully first. If telegram refs only affect cache keys / debug strings, mechanical strip suffices. If they affect `start_url` or scope (PWA install behavior), rewrite to a single PWA scope (no telegram path). Document the choice in the commit body.
    4. **`web/src/App.tsx`**:
       - Drop `getTelegramWebApp` import (L4).
       - Drop the Telegram color-theme bridge calls (L57, L104).
       - Drop the `isTelegramApp()` early-return / branching at L164.
       - Drop the entire telegram error-UI block (L386-397) — the access-token login flow handles auth errors via `LoginPrompt`.
       - Drop `<VoiceProvider>` / `<VoiceErrorBanner/>` ONLY in commit #3 (02-03), not here. Don't touch voice in this commit.
    5. **`web/src/components/SessionHeader.tsx`**: drop `isTelegramApp` import (L4) + early-return (L128-129). Header always renders.
    6. **`web/src/components/InstallPrompt.tsx`**: drop `isTelegram` destructure from `usePlatform()` (L10, L12, L29). Prompt shows on browser/PWA only — its existing pwa/browser logic stays.
    7. **`web/src/components/LoginPrompt.tsx`**: drop the bind UI (Telegram bind form). Only the access-token form remains.
    8. **`web/src/components/AssistantChat/messages/{Assistant,User}Message.tsx`**: read each file, find telegram/bind/login refs (RESEARCH says "verify; drop if telegram-coupled"). If hits are in a code path triggered by `bind()` or telegram error UI, delete the path. If purely cosmetic strings, drop the strings. Re-run `cd web && bun run typecheck` after edits.
    9. **`web/src/api/client.ts`**: delete the `bind()` method on `ApiClient`. Narrow the `authenticate()` body type to `{ accessToken: string }` (drop the union with `{ initData: string }`). Drop any telegram-related types/imports.
    10. **`web/src/lib/locales/en.ts`** + **`zh-CN.ts`**: delete the three `login.bind.title` / `login.bind.submit` / `login.bind.submitting` keys (lines ~15, ~17, ~19 in each file). After deletion, `! rg "login\\.bind" web/src/lib/locales/` should hold.
    11. **`web/src/index.css`**:
        - Drop the `/* Primary colors - override fallbacks for non-Telegram browsers */` comment (L85) or rewrite it to drop "non-Telegram".
        - Delete the rule block at L213-214 (`html[data-telegram-app="true"], html[data-telegram-app="true"] body { ... }` — read forward to find the closing `}`).
        - The `:not([data-telegram-app="true"])` block at L218-219 — read its declarations; promote them to an unconditional `html, body { ... }` selector (or merge with the surrounding base rule); drop the `:not(...)` form. The post-edit selector list MUST contain zero `data-telegram-app` references.
    12. **Per-commit gate (D-31):** From repo root: `bun typecheck && bun run test`. Both must exit 0.
    13. Commit with `feat(phase-02): CUT-06 remove Telegram WebApp platform (web-side)` and a body listing: `useTelegram.ts` deletion, hook collapses, app-entry strip, component cleanups, `ApiClient.bind()` removal, i18n keys deleted, CSS rules removed, sw.ts disposition (mechanical strip vs rewrite). Refs: `D-16 D-17 D-18 D-30 commit #2 D-31`.
  </action>
  <verify>
    <automated>! rg -ni 'telegram|tgWebApp|isTelegramApp' web/src/ && ! rg "login\\.bind" web/src/lib/locales/ && ! rg "data-telegram-app" web/src/index.css && ! rg "\\bbind\\b" web/src/api/client.ts && bun typecheck && bun run test && git log -1 --format=%s | grep -Eq 'feat\(phase-02\): CUT-06 remove Telegram WebApp platform \(web-side\)'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -ni 'telegram|tgWebApp|isTelegramApp' web/src/ | wc -l` is 0 (D-27 web-side scope)
    - `rg -n 'login\\.bind' web/src/lib/locales/en.ts web/src/lib/locales/zh-CN.ts | wc -l` is 0
    - `rg -n 'data-telegram-app' web/src/index.css | wc -l` is 0
    - `rg -n '\\bbind\\b' web/src/api/client.ts | wc -l` is 0 (or only matches `xhr.bind`-style false positives — manually verify if any remain)
    - `! test -f web/src/hooks/useTelegram.ts` (carry-over from Task 1)
    - `bun typecheck` exits 0 (D-31, all packages)
    - `bun run test` exits 0 (D-31)
    - `git log -1 --format=%s` matches `feat(phase-02): CUT-06 remove Telegram WebApp platform (web-side)`
    - `git status --porcelain` empty
  </acceptance_criteria>
  <done>Web side fully Telegram-free. Hub + web together close CUT-06 except for residuals deferred to commit #5 (cli/* + ripgrep guard PATTERN extension).</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-1 | Auth schema drift | Web `ApiClient.authenticate()` body shape narrowed; client cached on a phone may still send `{ initData }` post-deploy → 400 | accept (medium) | Per D-25, negative-case auth coverage deferred to Phase 11 (REFT-03). Single-user Tailscale topology means PWA refresh on first re-auth gets the new bundle; no multi-tenant fan-out concern. |
| T-02-02-2 | Tampering | Telegram-only routes left registered without telegram entry → unreachable dead routes | mitigate (low) | Task 2 step 2 explicitly inspects each gated route body and either ungates (still useful) or deletes outright. Typecheck doesn't catch dead routes; manual inspection required at planning step Read. |
| T-02-02-3 | Information disclosure | `LoginPrompt.tsx` previously surfaced bind/Telegram error text; if only the form is removed but the text/i18n keys linger, leaks intent | mitigate (low) | Task 2 step 10 deletes the three `login.bind.*` keys; consumer file `LoginPrompt.tsx` step 7 deletes the form. The two are paired in same task. |
| T-02-02-4 | Stale-cache PWA | sw.ts may carry telegram cache-key namespacing; if not stripped, old PWA installs continue caching against telegram-tagged keys | mitigate (low) | Task 2 step 3 reads `sw.ts` fully and chooses between mechanical strip and rewrite per RESEARCH Open Q #2. Decision is documented in commit body. PWA bumps the SW version naturally on next deploy. |
| T-02-02-SC | Tampering | npm install of new packages | n/a | No installs; pure deletion + symbol scrubbing. |
</threat_model>

<verification>
- `bun typecheck` green from repo root (D-31)
- `bun run test` green from repo root (D-31; SC#5 access-token-only test path now hardened on both ends)
- `rg -ni 'telegram' web/src/ | wc -l` = 0 (D-27 web-side completion)
- `! test -f web/src/hooks/useTelegram.ts`
- File-existence + ripgrep assertions per task `<acceptance_criteria>`
- Single commit `feat(phase-02): CUT-06 remove Telegram WebApp platform (web-side)`
</verification>

<success_criteria>
- All task `<acceptance_criteria>` blocks pass
- D-31 per-commit gate satisfied
- Phase 2 SC#1 web-side, SC#5 (web client now strictly access-token-only) met
- CUT-06 fully closed across hub + web; residual CLI string + ripgrep PATTERN extension owned by 02-05
</success_criteria>

<output>
Create `.planning/phases/02-cut-external-integration-channels/02-02-SUMMARY.md` listing:
- Files deleted (full paths)
- Hook collapse summary (AuthSource → single source; Platform union narrowed)
- sw.ts disposition (mechanical vs rewrite per Open Q #2)
- i18n keys removed (count + keys)
- CSS rules removed (line ranges before/after)
- D-31 gate result (bun typecheck + bun run test exit codes)
- Hand-off note: ApiClient.bind() removed; LoginPrompt access-token-only
</output>
