---
phase: 09-web-internal-decoupling
plan: 2
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - web/src/lib/message-window-store.ts
  - web/src/lib/messageWindowState.ts
  - web/src/lib/messageWindowPaginationService.ts
  - web/src/lib/messageWindowMergeService.ts
  - web/src/lib/messageWindowSubscriptions.ts
  - web/src/lib/messageWindowPersistence.ts
  - web/src/lib/messageWindowState.test.ts
  - web/src/lib/messageWindowPaginationService.test.ts
  - web/src/lib/messageWindowMergeService.test.ts
  - web/src/lib/messageWindowSubscriptions.test.ts
  - web/src/lib/messageWindowPersistence.test.ts
  - web/src/components/SessionList.tsx
  - web/src/components/SessionList/SessionListHeader.tsx
  - web/src/components/SessionList/SessionListSearch.tsx
  - web/src/components/SessionList/SessionListItem.tsx
  - web/src/components/SessionList/SessionListEmpty.tsx
  - web/src/components/SessionList/useSessionListData.ts
  - web/src/components/SessionList/useSessionListSearch.ts
  - web/src/components/SessionList/useSessionListSelection.ts
  - web/src/components/SessionList/useSessionListKeyboard.ts
  - web/src/components/SessionList/SessionListHeader.test.tsx
  - web/src/components/SessionList/SessionListSearch.test.tsx
  - web/src/components/SessionList/SessionListItem.test.tsx
  - web/src/components/SessionList/SessionListEmpty.test.tsx
autonomous: true
requirements:
  - REFW-02
must_haves:
  truths:
    - "message-window-store.ts is a pure re-export facade; useMessageWindow + every public export name and signature unchanged."
    - "5 sub-modules (state / pagination / merge / subscriptions / persistence) each < 400 lines."
    - "Module-private Maps (states, listeners, pendingVisibilityCacheBySession) live ONLY in messageWindowState.ts; siblings consume them via exported accessors (getInternalState / updateInternalState / getInternalListeners)."
    - "SessionList.tsx default export unchanged; external callers (routes / Sidebar / etc.) require zero import changes."
    - "SessionList orchestrator < 250 lines; each SessionList sub-component < 250 lines."
    - "Every new sub-module / sub-component has a colocated *.test.* file."
    - "Existing web/src/lib/message-window-store.test.ts still PASSes without modification (validates the facade preserves the public API surface)."
  artifacts:
    - path: "web/src/lib/message-window-store.ts"
      provides: "Thin re-export facade — sole external entry point"
      contains: "export { } from './messageWindow"
    - path: "web/src/lib/messageWindowState.ts"
      provides: "State shape + module-private Maps + accessors + buildState + setAtBottom"
      contains: "const states = new Map"
    - path: "web/src/lib/messageWindowPaginationService.ts"
      provides: "fetchLatestMessages, fetchOlderMessages + trim helpers"
      contains: "export"
    - path: "web/src/lib/messageWindowMergeService.ts"
      provides: "ingestIncomingMessages, appendOptimisticMessage, updateMessageStatus, removeOptimisticMessage, markMessagesConsumed, flushPendingMessages"
      contains: "export"
    - path: "web/src/lib/messageWindowSubscriptions.ts"
      provides: "subscribeMessageWindow, clearMessageWindow, seedMessageWindowFromSession"
      contains: "export"
    - path: "web/src/lib/messageWindowPersistence.ts"
      provides: "persistState, hydrateState, schedulePersist, flushPersistedStates, clearPersistedState"
      contains: "sessionStorage"
    - path: "web/src/components/SessionList.tsx"
      provides: "Orchestrator (default export) — composes 4 hooks + 4 sub-components"
      contains: "export default"
    - path: "web/src/components/SessionList/SessionListHeader.tsx"
      provides: "Header sub-component"
      contains: "export"
    - path: "web/src/components/SessionList/SessionListSearch.tsx"
      provides: "Search input sub-component"
      contains: "export"
    - path: "web/src/components/SessionList/SessionListItem.tsx"
      provides: "Per-session list item sub-component"
      contains: "export"
    - path: "web/src/components/SessionList/SessionListEmpty.tsx"
      provides: "Empty-state sub-component"
      contains: "export"
  key_links:
    - from: "web/src/lib/messageWindow*Service.ts (and Persistence + Subscriptions)"
      to: "web/src/lib/messageWindowState.ts"
      via: "import { getInternalState, updateInternalState, getInternalListeners }"
      pattern: "from './messageWindowState'"
    - from: "web/src/lib/message-window-store.ts"
      to: "web/src/lib/messageWindowState.ts (and other 4 sub-modules)"
      via: "re-export"
      pattern: "export .* from './messageWindow"
    - from: "web/src/components/SessionList.tsx"
      to: "web/src/components/SessionList/*"
      via: "relative import"
      pattern: "from './SessionList/"
    - from: "web/src/hooks/useSSE.ts and web/src/routes/index.tsx etc."
      to: "web/src/lib/message-window-store.ts"
      via: "import { useMessageWindow, ... } (UNCHANGED)"
      pattern: "from '@/lib/message-window-store'"
---

<objective>
Slice 2 — Decompose `message-window-store.ts` (1088 lines) into a thin re-export facade + 5 sub-modules, and decompose `SessionList.tsx` (953 lines) into an orchestrator + 4 hooks + 4 sub-components. Pure structural refactor — every public surface preserved verbatim. No caller in the rest of `web/src/` changes a single import line.

Purpose: Bring two of the largest oversized files under the < 500-line budget while keeping the external API perfectly stable. Adopt RESEARCH Q8 Option A (5 sub-modules) for `message-window-store` because the 4-sub-module form lands `messageWindowState.ts` at ~430 lines (over the < 400 D-149 budget) — splitting persistence into a 5th sub-module solves it cleanly. For `SessionList.tsx`, follow D-148 two-step (hooks first → sub-components second), mirroring the `NewSession/index.tsx` orchestrator + sub-component layout.

Output: 5 new sub-module files + 1 facade rewrite for message-window-store; 4 new hook files + 4 new sub-component files + 1 orchestrator rewrite for SessionList; colocated tests for every new piece. Gate `bun run test:web` green (including the unmodified `message-window-store.test.ts` proving facade equivalence) + file-size budgets honored.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-web-internal-decoupling/09-CONTEXT.md
@.planning/phases/09-web-internal-decoupling/09-RESEARCH.md
@.planning/phases/09-web-internal-decoupling/09-PATTERNS.md
@.planning/phases/09-web-internal-decoupling/09-VALIDATION.md
@.planning/phases/09-web-internal-decoupling/09-01-SUMMARY.md
@.planning/phases/08-hub-internal-decoupling/08-CONTEXT.md
@AGENTS.md

<interfaces>
<!-- Public API surface of message-window-store that MUST remain re-exported identically -->
<!-- Derived from RESEARCH §Pattern 1 facade body (lines 277-303) -->

From web/src/lib/message-window-store.ts (current public exports — must remain externally observable):
```typescript
export type { MessageWindowState }
export const VISIBLE_WINDOW_SIZE
export const PENDING_WINDOW_SIZE
export function getMessageWindowState(sessionId: string): MessageWindowState
export function subscribeMessageWindow(sessionId, listener): () => void
export function clearMessageWindow(sessionId: string): void
export function seedMessageWindowFromSession(...): void
export function setAtBottom(sessionId: string, atBottom: boolean): void
export function fetchLatestMessages(...): Promise<...>
export function fetchOlderMessages(...): Promise<...>
export function ingestIncomingMessages(...): void
export function appendOptimisticMessage(...): void
export function updateMessageStatus(...): void
export function removeOptimisticMessage(...): void
export function markMessagesConsumed(...): void
export function flushPendingMessages(...): void
```

From web/src/components/SessionList.tsx (must remain unchanged):
```typescript
export default function SessionList(props: SessionListProps): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Decompose message-window-store.ts into 5 sub-modules + thin re-export facade</name>
  <files>web/src/lib/message-window-store.ts, web/src/lib/messageWindowState.ts, web/src/lib/messageWindowPaginationService.ts, web/src/lib/messageWindowMergeService.ts, web/src/lib/messageWindowSubscriptions.ts, web/src/lib/messageWindowPersistence.ts</files>
  <read_first>
    - `web/src/lib/message-window-store.ts` in full (1088 lines) — locate the 5 logical sections per RESEARCH Q8 boundaries.
    - `web/src/lib/message-window-store.test.ts` lines 1-30 — confirms test imports public API only via the facade path (so facade re-exports must match names exactly).
    - `web/src/hooks/useMessages.ts` lines 1-60 — confirms `useSyncExternalStore` consumer (NOT zustand; RESEARCH Pitfall 5).
    - RESEARCH.md §"Pattern 1: Facade re-export" lines 277-303 — verbatim facade body to write.
    - RESEARCH.md Q8 (lines 642-668) — line-range boundaries for each sub-module and Option A 5-file split rationale (`messageWindowState.ts` lands ~430 lines with only 4 sub-modules; extracting persistence gets it to ~280).
    - RESEARCH.md §Pitfall 1 (lines 346-350) — shared `Map` state ownership rule.
    - RESEARCH.md §"Code Examples Example 3" lines 1040-1072 — accessor pattern for `getInternalState` / `updateInternalState` / `getInternalListeners`.
    - PATTERNS.md §"web/src/lib/messageWindowState.ts (NEW)" lines 82-104 — Map-owner pattern.
    - PATTERNS.md §"web/src/lib/messageWindow{Pagination,Merge,Subscriptions,Persistence}Service.ts (NEW)" lines 107-119.
    - `hub/src/sync/sessionCache.ts` lines 1-25 (P8 D-129 facade intent — class composition; we adapt to module re-export form).
    - `hub/src/sync/sessionRepository.ts` lines 1-25 (Map-owner pattern reference).
  </read_first>
  <action>
    Split `web/src/lib/message-window-store.ts` into 5 sibling sub-modules + retain `message-window-store.ts` as a ~30-line pure re-export facade per RESEARCH Q8 Option A. (1) `messageWindowState.ts` owns: type definitions (`MessageWindowState`, `InternalState`, `PersistedMessageWindowState`, `PendingVisibilityCacheEntry`, `AsyncGenerationKind`); the `const states = new Map`, `const listeners = new Map`, `const pendingVisibilityCacheBySession = new Map` (module-private); `getState` / `createState` / `notify` / `notifyImmediate` / `setState` / `updateState`; generation helpers; `deriveSeqBounds` / `deriveOldestPosition`; `buildState`; pending-visibility-cache helpers; `scheduleNotify` / `flushNotifications`; window-size constants (`VISIBLE_WINDOW_SIZE`, `PENDING_WINDOW_SIZE`, `AGENT_RUN_WINDOW_SIZE`, `PAGE_SIZE`, `COLD_LOAD_*`); `setAtBottom`; `getMessageWindowState`; and exported internal accessors `getInternalState(sessionId)`, `updateInternalState(sessionId, updater, immediate?)`, `getInternalListeners(sessionId)`. (2) `messageWindowPersistence.ts` owns: `STORAGE_KEY_PREFIX`, `getStorageKey`, `isSessionStorageAvailable`, `toNullableNumber`, `shouldPersistState`, `persistState`, `clearPersistedState`, `flushPersistedStates`, `schedulePersist`, `hydrateState` — consuming accessors from `messageWindowState`. (3) `messageWindowPaginationService.ts` owns: pagination/trim helpers (`isAgentRunMessage`, `countRegularMessages`, `sameCursor`, `backfillColdLoadMessages`, `sliceForTrim`, `trimPreservingQueued`, `trimVisible`, `trimVisibleWithDropped`, `cursorUpdatesAfterAppendTrim`, `trimPending`) plus public `fetchLatestMessages` and `fetchOlderMessages`. (4) `messageWindowMergeService.ts` owns: `filterPendingAgainstVisible`, `isOptimisticMessage`, `mergeIntoPending` + public `ingestIncomingMessages`, `flushPendingMessages`, `appendOptimisticMessage`, `updateMessageStatus`, `removeOptimisticMessage`, `markMessagesConsumed`. (5) `messageWindowSubscriptions.ts` owns: `subscribeMessageWindow`, `clearMessageWindow`, `seedMessageWindowFromSession`. Sub-modules (2)-(5) MUST consume `getInternalState` / `updateInternalState` / `getInternalListeners` from `messageWindowState.ts` — they MUST NOT instantiate their own `Map`s (Pitfall 1). Rewrite `message-window-store.ts` to a pure `export { ... } from './messageWindowXxx'` facade exporting every name listed in the `<interfaces>` block above so external callers (`useMessages.ts`, `useSSE.ts`, `SessionChat.tsx`, etc.) see identical exports. Per D-150, do NOT change semantics: this is a literal file-split, not a rewrite.
  </action>
  <verify>
    <automated>bun typecheck &amp;&amp; bun run test:web -- message-window</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist: `wc -l web/src/lib/message-window-store.ts web/src/lib/messageWindowState.ts web/src/lib/messageWindowPaginationService.ts web/src/lib/messageWindowMergeService.ts web/src/lib/messageWindowSubscriptions.ts web/src/lib/messageWindowPersistence.ts` returns 6 lines.
    - `wc -l web/src/lib/message-window-store.ts` reports a count ≤ 80 (thin facade).
    - `wc -l web/src/lib/messageWindowState.ts` reports < 400.
    - `wc -l web/src/lib/messageWindowPaginationService.ts` reports < 400.
    - `wc -l web/src/lib/messageWindowMergeService.ts` reports < 400.
    - `wc -l web/src/lib/messageWindowSubscriptions.ts` reports < 400.
    - `wc -l web/src/lib/messageWindowPersistence.ts` reports < 400.
    - Exactly one occurrence of `const states = new Map` across the 5 sub-modules — must be in `messageWindowState.ts`: `grep -l "const states = new Map" web/src/lib/messageWindow*.ts` returns only `messageWindowState.ts`.
    - Same uniqueness check for `const listeners = new Map`.
    - `message-window-store.ts` is a re-export-only facade: every non-blank, non-comment line either starts with `export ` (followed by `{`, `*`, or `type`) or is an import line.
    - Existing `web/src/lib/message-window-store.test.ts` PASSes without modification (this is the contract proof that the facade preserves the public API): `bun run test:web -- message-window` exits 0.
    - `bun typecheck` exits 0.
  </acceptance_criteria>
  <done>Facade re-export-only; 5 sub-modules each under 400 lines; shared Maps live only in messageWindowState.ts; existing test file passes unmodified.</done>
</task>

<task type="auto">
  <name>Task 2.2: Add colocated sub-module tests for the 5 message-window sub-modules</name>
  <files>web/src/lib/messageWindowState.test.ts, web/src/lib/messageWindowPaginationService.test.ts, web/src/lib/messageWindowMergeService.test.ts, web/src/lib/messageWindowSubscriptions.test.ts, web/src/lib/messageWindowPersistence.test.ts</files>
  <read_first>
    - `web/src/lib/message-window-store.test.ts` in full — reuse setup patterns and test fixtures.
    - Each of the 5 newly created sub-modules from Task 2.1 — confirm exported accessor / public function names.
    - PATTERNS Shared Pattern F — RTL not required here (these are pure-function modules); plain `vitest` describe/it/expect suffices.
  </read_first>
  <action>
    For each of the 5 sub-modules, create a colocated `*.test.ts` covering at minimum: (a) `messageWindowState.test.ts` — `getInternalState` returns the same reference on repeat calls; `updateInternalState` mutates state visibly; `setAtBottom` toggles the flag. (b) `messageWindowPersistence.test.ts` — `persistState` writes to sessionStorage under the correct key prefix; `hydrateState` round-trips. (c) `messageWindowPaginationService.test.ts` — `trimVisible` keeps last N messages; `fetchLatestMessages` cursor math (use stub api). (d) `messageWindowMergeService.test.ts` — `appendOptimisticMessage` then `removeOptimisticMessage` leaves state empty; `ingestIncomingMessages` dedupes. (e) `messageWindowSubscriptions.test.ts` — `subscribeMessageWindow` listener fires on state change; `clearMessageWindow` resets state. Each test file imports from the sub-module directly (NOT via the facade) to prove sub-modules work standalone. Tests should be focused — 3-6 cases per file is sufficient; do not duplicate cases that already live in `message-window-store.test.ts` (which exercises the public surface).
  </action>
  <verify>
    <automated>bun run test:web -- messageWindow</automated>
  </verify>
  <acceptance_criteria>
    - All 5 test files exist (`wc -l` on the list returns 5 lines).
    - Each file contains at least 3 `it(` blocks: `grep -c "^    it(" web/src/lib/messageWindowState.test.ts` ≥ 3 (and same for the other 4).
    - Each test file imports from the sub-module under test, not the facade: `grep -c "from './messageWindowState'" web/src/lib/messageWindowState.test.ts` ≥ 1.
    - `bun run test:web -- messageWindow` exits 0.
  </acceptance_criteria>
  <done>Each sub-module has focused colocated tests that exercise its accessors / public functions directly.</done>
</task>

<task type="auto">
  <name>Task 2.3: Decompose SessionList.tsx into 4 hooks + 4 sub-components + orchestrator</name>
  <files>web/src/components/SessionList.tsx, web/src/components/SessionList/SessionListHeader.tsx, web/src/components/SessionList/SessionListSearch.tsx, web/src/components/SessionList/SessionListItem.tsx, web/src/components/SessionList/SessionListEmpty.tsx, web/src/components/SessionList/useSessionListData.ts, web/src/components/SessionList/useSessionListSearch.ts, web/src/components/SessionList/useSessionListSelection.ts, web/src/components/SessionList/useSessionListKeyboard.ts</files>
  <read_first>
    - `web/src/components/SessionList.tsx` in full (953 lines).
    - `web/src/components/NewSession/index.tsx` lines 1-30 (PATTERNS Shared Pattern B — orchestrator + colocated sub-components, no `index.ts` barrel, hooks as flat siblings).
    - `web/src/components/NewSession/ActionButtons.tsx`, `web/src/components/NewSession/MachineSelector.tsx`, `web/src/components/NewSession/SessionTypeSelector.tsx` lines 1-20 each (sub-component shape — named export, single component, types co-located).
    - `web/src/hooks/useDirectorySuggestions.ts` (PATTERNS — lifted-state hook pattern).
    - `web/src/components/SessionList.test.ts` (existing) and `web/src/components/SessionList.directory-action.test.tsx` (existing) to identify cases that move to sub-component tests.
    - CONTEXT D-148 (D-148: 4 hooks → 4 sub-components mixed sequence; SessionList stays as default export at the original path).
    - PATTERNS.md §"web/src/components/SessionList.tsx (orchestrator refactor)" lines 217-249.
  </read_first>
  <action>
    Step 1 — Hook extraction (preserves single-file but trims the orchestrator body): in `SessionList.tsx`, extract `useState` / `useEffect` / `useCallback` / `useMemo` chains into 4 named hooks living under `web/src/components/SessionList/`: (i) `useSessionListData.ts` — owns session/machine fetching + derived list state (consumes `useSessions` / `useMachines` from `@/hooks/queries`); (ii) `useSessionListSearch.ts` — owns query string state + filtered-list memo; (iii) `useSessionListSelection.ts` — owns selection state + selection callbacks; (iv) `useSessionListKeyboard.ts` — owns keyboard navigation (arrow keys, Enter, etc.). Each hook is a single named export. Step 2 — Sub-component extraction: extract JSX regions into 4 named-export components in the same `SessionList/` dir: `SessionListHeader.tsx` (title + actions row), `SessionListSearch.tsx` (search input), `SessionListItem.tsx` (one row in the list — receives session + machine props + selection state + handlers), `SessionListEmpty.tsx` (empty-state placeholder). Use relative imports (`./SessionList/SessionListHeader`) — no `index.ts` barrel (PATTERNS Anti-Pattern). Step 3 — rewrite `SessionList.tsx` as an orchestrator: imports the 4 hooks + 4 sub-components, composes them, returns the same default export with unchanged props signature. Do NOT change the default export name; do NOT introduce new public exports. Do NOT touch existing `SessionList.directory-action.test.tsx` (already focused). Per D-148, `SessionList` default export + external import paths are zero-change.
  </action>
  <verify>
    <automated>bun typecheck &amp;&amp; bun run test:web -- SessionList</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l web/src/components/SessionList.tsx` reports < 250.
    - All 4 hook files exist: `test -f web/src/components/SessionList/useSessionListData.ts` (and Search / Selection / Keyboard).
    - All 4 sub-component files exist: `test -f web/src/components/SessionList/SessionListHeader.tsx` (and Search / Item / Empty).
    - `wc -l web/src/components/SessionList/SessionListHeader.tsx` < 250 (and each other sub-component < 250).
    - `wc -l web/src/components/SessionList/useSessionListData.ts` < 250 (and each hook < 250).
    - `grep -c "^export default" web/src/components/SessionList.tsx` equals 1 (default export preserved).
    - No new `index.ts` barrel under `web/src/components/SessionList/`: `test ! -f web/src/components/SessionList/index.ts`.
    - `grep -rE "from '@/components/SessionList'" web/src` returns the same set of files as before (external import paths unchanged) — quick check: `grep -l "from '@/components/SessionList'" web/src -r | wc -l` matches pre-refactor count (record from Slice 1 baseline).
    - Existing `web/src/components/SessionList.directory-action.test.tsx` PASSes unchanged.
    - `bun typecheck` exits 0; `bun run test:web -- SessionList` exits 0.
  </acceptance_criteria>
  <done>SessionList orchestrator < 250 lines; 4 hooks + 4 sub-components live under SessionList/; default export and external imports preserved.</done>
</task>

<task type="auto">
  <name>Task 2.4: Colocated tests for 4 new SessionList sub-components + redistribute SessionList.test.ts cases</name>
  <files>web/src/components/SessionList/SessionListHeader.test.tsx, web/src/components/SessionList/SessionListSearch.test.tsx, web/src/components/SessionList/SessionListItem.test.tsx, web/src/components/SessionList/SessionListEmpty.test.tsx</files>
  <read_first>
    - `web/src/components/SessionList.test.ts` in full — identify which cases naturally belong to which sub-component.
    - PATTERNS Shared Pattern F — RTL setup with `vi.mock` for heavy children when needed.
    - `web/src/components/ToolCard/views/_results.test.tsx` lines 1-30 (RTL mock setup template).
    - Each newly created sub-component from Task 2.3 (confirm props shapes).
  </read_first>
  <action>
    Create 4 colocated `*.test.tsx` files under `web/src/components/SessionList/`, one per sub-component, using RTL `render(<Component {...mockProps} />)` patterns. For each: cover the visible-behavior cases that previously lived in `SessionList.test.ts` (e.g., empty-state rendering moves to `SessionListEmpty.test.tsx`; per-item click / selection moves to `SessionListItem.test.tsx`; search input change handler moves to `SessionListSearch.test.tsx`; header action button click moves to `SessionListHeader.test.tsx`). Keep cases that genuinely require the full orchestrator (cross-piece interactions like keyboard navigation across items) in the existing `SessionList.test.ts`. Use `vi.mock` for child components that pull in heavy dependencies. Minimum: ≥ 2 `it()` cases per new test file. The existing `SessionList.directory-action.test.tsx` stays unchanged (already focused).
  </action>
  <verify>
    <automated>bun run test:web -- SessionList</automated>
  </verify>
  <acceptance_criteria>
    - All 4 sub-component test files exist.
    - Each file has ≥ 2 `it(` blocks.
    - `bun run test:web -- SessionList` exits 0 (all SessionList + sub-component + directory-action tests pass).
  </acceptance_criteria>
  <done>Each SessionList sub-component has focused colocated tests; full SessionList test scope green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure refactor) | Internal module decomposition only; no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | message-window-store Map sharing | mitigate | Pitfall 1 enforcement: shared Maps live only in `messageWindowState.ts`; siblings consume `getInternalState` / `updateInternalState` accessors. Slice 4 D-158 #4 guard caps sub-module size at < 400 lines. Acceptance criteria 2.1 grep enforces single Map ownership. |
| T-09-04 | Tampering | message-window-store public API drift | mitigate | Existing `message-window-store.test.ts` runs unmodified against the facade; any name / signature drift breaks the test loudly. Listed in `<interfaces>` block as binding contract. |
| T-09-05 | Tampering | SessionList default export drift | mitigate | Acceptance criterion: `grep -c "^export default" SessionList.tsx == 1`; external callers detected by `grep -l "from '@/components/SessionList'" web/src -r` matching pre-refactor count. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Slice 2 installs 0 new packages. |
</threat_model>

<verification>
- `bun typecheck` exits 0.
- `bun run test:web` exits 0 (including unmodified `message-window-store.test.ts` and `SessionList.directory-action.test.tsx`).
- File-size budgets honored:
  - `web/src/lib/message-window-store.ts` < 80
  - Every `web/src/lib/messageWindow*.ts` (other than the facade and tests) < 400
  - `web/src/components/SessionList.tsx` < 250
  - Every `web/src/components/SessionList/*.tsx` (non-test) < 250
  - Every `web/src/components/SessionList/*.ts` (non-test) < 250
- No `index.ts` barrel under `web/src/components/SessionList/`.
- `message-window-store.ts` is re-export only (no executable statements).
</verification>

<success_criteria>
- REFW-02 (half): `message-window-store.ts` + `SessionList.tsx` both decomposed within budget; 5 store sub-modules each < 400 lines; 4 SessionList sub-components + 4 hooks each < 250 lines.
- Public surfaces fully preserved (default export of SessionList, every name re-exported by message-window-store facade).
- Colocated tests for every new sub-module / sub-component (RESEARCH Wave 0 gaps Slice 2 entries closed).
- Full suite green: `bun typecheck && bun run test`.
</success_criteria>

<output>
Create `.planning/phases/09-web-internal-decoupling/09-02-SUMMARY.md` when done.
</output>
