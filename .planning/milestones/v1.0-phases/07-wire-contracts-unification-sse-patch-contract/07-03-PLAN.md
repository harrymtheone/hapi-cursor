---
phase: 07-wire-contracts-unification-sse-patch-contract
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - cli/src/api/types.ts
  - cli/src/agent/sessionFactory.ts
  - cli/src/agent/types.ts
  - cli/src/cursor/runCursor.ts
  - web/src/types/api.ts
  - web/src/hooks/useSSE.ts
  - web/src/hooks/useSSE.test.tsx
  - web/src/components/SessionList.tsx
autonomous: true
requirements: [REFA-03, REFA-04]
tags: [cli, web, useSSE, type-collapse, sse-rewrite]

must_haves:
  truths:
    - "cli/src/api/types.ts declares zero local Zod wire schemas — MachineMetadataSchema, RunnerStateSchema, Machine type, MessageMetaSchema, UserMessageSchema, AgentMessageSchema, MessageContentSchema all become re-exports from @hapi/protocol/schemas / @hapi/protocol/types (REFA-03, D-111, D-112)"
    - "cli CreateSessionResponseSchema (and CreateMachineResponseSchema) no longer carries metadata: z.unknown().nullable() / agentState: z.unknown().nullable() — replaced with shared SessionSchema / MachineSchema reference per D-113"
    - "cli flavor: string is fully removed from SessionBootstrapOptions, buildSessionMetadata opts param, bootstrapExistingSession opts, runCursor.ts call sites, and setModel opts (cli/src/agent/types.ts:74) — D-122 cli writer-side complete"
    - "web/src/types/api.ts declares zero local Machine / RunnerState / SessionMetadataSummary / SessionsResponse / SessionResponse / MessagesResponse / MachinesResponse / SpawnResponse — all become re-exports from shared (REFA-03, D-114)"
    - "web/src/hooks/useSSE.ts: 7 narrow functions (hasUnknownSessionPatchKeys, getSessionPatch, isSessionRecord, isMachineRecord, isInactiveMachinePatch, isMachineMetadata, hasRecordShape) + local SessionPatch type + invalidation queue (queueSessionListInvalidation, queueSessionDetailInvalidation, queueMachinesInvalidation, flushInvalidations, scheduleInvalidationFlush, INVALIDATION_BATCH_MS, invalidationTimerRef, pendingInvalidationsRef) all DELETED. Replaced with SyncEventSchema.safeParse + discriminator branches; parse failure = console.error + drop (D-120, D-121, REFA-04)"
    - "web/src/hooks/useSSE.test.tsx exists with MockEventSource shim + cases covering: full session-added, single-field backgroundTaskCount patch (NO invalidate), each session-updated patch field, full machine-updated, inactive machine-updated patch, null machine-updated, parse-failure drop (no invalidate, no mutation, no throw) (D-127#2)"
    - "FlavorIcon component + FLAVOR_BADGES lookup in web/src/components/SessionList.tsx are deleted (Pitfall #4); render either no icon or a single fixed Cursor icon — UI choice documented in SUMMARY"
    - "Workspace bun typecheck + bun run test green; D-125 Slice 3 gate met"
  artifacts:
    - path: "cli/src/api/types.ts"
      provides: "re-exports of Machine, MachineMetadata, RunnerState, MessageMeta, UserMessage, AgentMessage, MessageContent from @hapi/protocol; narrowed CreateSessionResponseSchema referencing shared SessionSchema"
      contains: "from '@hapi/protocol"
    - path: "web/src/types/api.ts"
      provides: "re-exports of Machine, RunnerState, SessionsResponse, SessionResponse, MessagesResponse, MachinesResponse, SpawnResponse from @hapi/protocol"
    - path: "web/src/hooks/useSSE.ts"
      provides: "SyncEventSchema.safeParse-driven SSE consumer with discriminator branches; no narrow functions; no invalidation queue"
      contains: "SyncEventSchema.safeParse"
    - path: "web/src/hooks/useSSE.test.tsx"
      provides: "Vitest + MockEventSource integration test for handleSyncEvent against strictly typed event stream"
      min_lines: 120
  key_links:
    - from: "cli/src/api/types.ts"
      to: "@hapi/protocol/schemas"
      via: "wire schema re-export"
      pattern: "MachineMetadataSchema.*RunnerStateSchema.*from '@hapi/protocol"
    - from: "web/src/hooks/useSSE.ts"
      to: "@hapi/protocol/schemas"
      via: "SyncEventSchema runtime import for safeParse"
      pattern: "SyncEventSchema.*from '@hapi/protocol"
    - from: "web/src/hooks/useSSE.test.tsx"
      to: "web/src/hooks/useSSE.ts"
      via: "renderHook integration"
      pattern: "renderHook.*useSSE"

slice_gate:
  command: "bun typecheck && bun run test"
  must_pass: true
---

<objective>
Slice 3 of D-125: collapse cli + web wire-type mirrors onto the shared schemas shipped in 07-01, complete cli's writer-side flavor removal (D-122 remnants left after 07-01's cascade), and rewrite `web/src/hooks/useSSE.ts` to consume the canonical `SyncEventSchema` directly. The rewrite is the centerpiece of REFA-04: 7 hand-rolled narrow functions plus the invalidation queue (the fallback-to-refetch path) all disappear in favor of `safeParse` + discriminator branching. The `backgroundTaskCount` single-field patch — which today triggers `hasUnknownSessionPatchKeys = true` → integer list refetch on every emit — becomes a no-op cache mutation under the new contract.

Purpose: complete REFA-03 (cli + web mirrors gone) and REFA-04 (useSSE rewrite + strict-patch consumer); land the D-127#2 strictly-typed event-stream test that proves the bug is fixed by construction.

Output: cli + web `types.ts` files become thin re-export shells; cli writers fully purged of `flavor`; useSSE rewritten on top of `SyncEventSchema.safeParse`; new Vitest test file with MockEventSource shim covers every union branch; FlavorIcon + FLAVOR_BADGES deleted from SessionList.tsx (Pitfall #4 closeout).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-CONTEXT.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-01-SUMMARY.md

<interfaces>
Already-shipped in 07-01 (DO NOT re-do):
- shared/src/schemas.ts: `MachineSchema`, `MachineMetadataSchema`, `RunnerStateSchema`, `MessageMetaSchema`, `UserMessageSchema`, `AgentMessageSchema`, `MessageContentSchema`, `SessionPatchSchema`, `MachinePatchSchema`, tightened `SyncEventSchema` — all exported
- shared/src/responses.ts: `SessionsResponse`, `SessionResponse`, `MessagesResponse`, `MachinesResponse`, `SpawnResponse` exported
- shared/src/sessionSummary.ts: `SessionSummaryMetadata.flavor` deleted; `toSessionSummary` no longer copies `flavor`
- shared/src/modes.ts: `AGENT_MESSAGE_PAYLOAD_TYPE = 'cursor' as const`
- shared/src/types.ts + index.ts: surfacing complete
- cli/src/agent/sessionFactory.ts line 80 `flavor: options.flavor,` write deleted (lines 20, 54, 177 still carry the `flavor: string` field on opts types — Task 2 below removes those)
- web display readers (SessionChat/SessionHeader/SessionList/router) already collapsed to `'cursor'` constants (the FlavorIcon component + FLAVOR_BADGES table still exist — Task 5 below deletes those)

Still alive in 07-03 scope (verify before editing — line numbers may have shifted slightly after 07-01 deletions):
- cli/src/api/types.ts:32-55 — local `MachineMetadataSchema` (verified present at the start of this plan)
- cli/src/api/types.ts:59-73 — local `RunnerStateSchema`
- cli/src/api/types.ts:77-88 — local `Machine` TS type
- cli/src/api/types.ts:102-122 — `CreateSessionResponseSchema` with `metadata: z.unknown().nullable()` + `agentState: z.unknown().nullable()` (the D-113 narrow target)
- cli/src/api/types.ts:126-139 — `CreateMachineResponseSchema` with same unknown-typed metadata pattern (apply the same narrow per D-113 intent for parity)
- cli/src/api/types.ts:152-189 — local message wire schemas (`MessageMetaSchema`, `UserMessageSchema`, `AgentMessageSchema`, `MessageContentSchema`)
- cli/src/agent/sessionFactory.ts:20, 54, 177 — `flavor?: string` / `flavor: string` on option types (`SessionBootstrapOptions`, `buildSessionMetadata` opts, `bootstrapExistingSession` opts)
- cli/src/cursor/runCursor.ts:54, 59 — `flavor: 'cursor',` keys in two sessionFactory call sites
- cli/src/agent/types.ts:74 — `opts?: { flavor?: AgentFlavor }` on `setModel` signature
- web/src/types/api.ts:26-40 — local `SessionMetadataSummary`
- web/src/types/api.ts:50-64 — local `RunnerState`
- web/src/types/api.ts:66-77 — local `Machine`
- web/src/types/api.ts:89-101 — local `SessionsResponse`, `SessionResponse`, `MessagesResponse`, `MachinesResponse`
- web/src/types/api.ts:118-120 — local `SpawnResponse`
- web/src/hooks/useSSE.ts:33 — local `SessionPatch` type
- web/src/hooks/useSSE.ts:45-135 — 7 narrow functions (`hasRecordShape`, `isSessionRecord`, `getSessionPatch`, `hasUnknownSessionPatchKeys`, `isMachineMetadata`, `isMachineRecord`, `isInactiveMachinePatch`)
- web/src/hooks/useSSE.ts:189-194 + 294-348 + 625-636 — invalidation queue (refs, INVALIDATION_BATCH_MS, the 3 queue functions, flushInvalidations, scheduleInvalidationFlush, effect teardown)
- web/src/hooks/useSSE.ts:510-549 — current discriminator branch using narrows (to be rewritten on top of safeParse'd typed union)
- web/src/components/SessionList.tsx:498-499 + 594 — `FlavorIcon` definition + render site (Pitfall #4)

References:
- 07-PATTERNS.md "MODIFY cli/src/api/types.ts" + "MODIFY web/src/types/api.ts" + "REWRITE web/src/hooks/useSSE.ts" + "NEW web/src/hooks/useSSE.test.tsx" — exact pattern bodies + EventSource mock fallback
- 07-RESEARCH.md §6 (useSSE rewrite call-site map; the 3 queue-invalidation functions become unused) + §7 (flavor reader/writer site map) + §2 (duplicate type inventory with line ranges)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse cli/src/api/types.ts to re-exports of shared wire schemas + narrow CreateSessionResponseSchema / CreateMachineResponseSchema per D-113</name>
  <files>cli/src/api/types.ts</files>
  <read_first>
    - cli/src/api/types.ts (full file — confirm current line ranges for local declarations: MachineMetadataSchema (~32-55), RunnerStateSchema (~59-73), Machine (~77-88), CreateSessionResponseSchema (~102-122), CreateMachineResponseSchema (~126-139), MessageMetaSchema/UserMessageSchema/AgentMessageSchema/MessageContentSchema (~152-189))
    - shared/src/schemas.ts (post-07-01 — confirm all 9 schemas to import are exported: MachineSchema, MachineMetadataSchema, RunnerStateSchema, SessionSchema, MessageMetaSchema, UserMessageSchema, AgentMessageSchema, MessageContentSchema, plus existing AgentStateSchema/AttachmentMetadataSchema/MetadataSchema/PermissionModeSchema/TodosSchema already imported)
    - shared/src/types.ts (verify Machine, MachineMetadata, RunnerState, MessageMeta, UserMessage, AgentMessage, MessageContent are re-exported on the @hapi/protocol/types surface)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md "MODIFY cli/src/api/types.ts" — the existing re-export idiom at the top of the current file is the model
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md §2 (cli duplicate inventory) + §4 (placement decision)
  </read_first>
  <behavior>
    - cli/src/api/types.ts declares zero local `z.object(...)` Zod schemas for `MachineMetadataSchema`, `RunnerStateSchema`, `MessageMetaSchema`, `UserMessageSchema`, `AgentMessageSchema`, `MessageContentSchema`. These come from `@hapi/protocol/schemas`.
    - cli/src/api/types.ts declares no local `Machine`, `MachineMetadata`, `RunnerState`, `MessageMeta`, `UserMessage`, `AgentMessage`, `MessageContent` types — those come from `@hapi/protocol/types`.
    - `CreateSessionResponseSchema` is rewritten to `z.object({ session: SessionSchema })` (referencing shared `SessionSchema`); `GetSessionResponseSchema = CreateSessionResponseSchema` continues to compile.
    - `CreateMachineResponseSchema` is rewritten to `z.object({ machine: MachineSchema })` (parity narrow per D-113 intent — researcher's discretion: parity reduces unknowns from 2 to 0 in this file).
    - `Usage`, `CliMessagesResponse`, `CliMessagesResponseSchema`, the `LocalHandoffResponseSchema/LocalResumeTargetResponseSchema/ResumableSessionsResponseSchema` re-export block, and `SessionPermissionMode / SessionModel / SessionModelReasoningEffort / SessionEffort` aliases are preserved as-is.
    - Workspace `bun typecheck` green: any cli consumer reading e.g. `metadata.path` on a `CreateSessionResponse.session.metadata` now sees a typed `Metadata` (was `unknown`) — if any cli code did unsafe casts that the unknown previously allowed, the typecheck will surface the call site; fix each by reading the actual shared shape.
  </behavior>
  <action>
Delete the local `MachineMetadataSchema` block (with the `.transform` body), the local `RunnerStateSchema`, the local `Machine` TS type, and the local `MessageMetaSchema` / `UserMessageSchema` / `AgentMessageSchema` / `MessageContentSchema` blocks from `cli/src/api/types.ts`. Also delete the corresponding `export type MachineMetadata = z.infer<...>`, `export type RunnerState = z.infer<...>`, `export type MessageMeta = z.infer<...>`, `export type UserMessage = z.infer<...>`, `export type AgentMessage = z.infer<...>`, `export type MessageContent = z.infer<...>` lines (they live in shared now).

Extend the existing `export type { ... } from '@hapi/protocol/types'` block to add `Machine, MachineMetadata, RunnerState, MessageMeta, UserMessage, AgentMessage, MessageContent`. Extend the existing runtime `export { ... }` import-from-`@hapi/protocol/schemas` block (currently bringing AgentStateSchema/AttachmentMetadataSchema/MetadataSchema) to also re-export `MachineSchema, MachineMetadataSchema, RunnerStateSchema, MessageMetaSchema, UserMessageSchema, AgentMessageSchema, MessageContentSchema`. Add `SessionSchema` to the import set so it can be referenced inside `CreateSessionResponseSchema`.

Rewrite `CreateSessionResponseSchema` to `z.object({ session: SessionSchema })`. Keep the `export type CreateSessionResponse = z.infer<typeof CreateSessionResponseSchema>` line. Keep `export const GetSessionResponseSchema = CreateSessionResponseSchema` and its `export type` line. Rewrite `CreateMachineResponseSchema` to `z.object({ machine: MachineSchema })` and keep its `export type`. Do NOT touch `CliMessagesResponseSchema` (its `content: z.unknown()` is an out-of-scope wrapper not in D-114's lift list, and the `id/seq/createdAt/localId/content` shape is cli-internal).

Identifier-level non-goals: do NOT delete `LocalHandoffResponseSchema`/`LocalResumeTargetResponseSchema`/`ResumableSessionsResponseSchema` (those are P5/P6 territory, not Phase 7 collapse targets). Do NOT modify the `Usage` re-export. Do NOT touch `SessionPermissionMode / SessionModel / SessionModelReasoningEffort / SessionEffort` aliases.

Post-edit, run `cd cli && bun typecheck` and fix any consumer that previously relied on `metadata: unknown | null` by reading `metadata` as the shared `Metadata` shape (the common case is `session.metadata?.path` / `session.metadata?.host` — both legal on `Metadata`). If a consumer site is non-trivial (e.g. expects `unknown` for backward-compat reasons), STOP and report — do not paper over with `as` casts.
  </action>
  <verify>
    <automated>cd cli &amp;&amp; bun typecheck &amp;&amp; bun test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "export const MachineMetadataSchema = z\.object\(" cli/src/api/types.ts` returns 0 hits.
    - `rg -n "export const RunnerStateSchema = z\.object\(" cli/src/api/types.ts` returns 0 hits.
    - `rg -n "export type Machine = \{" cli/src/api/types.ts` returns 0 hits.
    - `rg -n "export const (UserMessageSchema|AgentMessageSchema|MessageContentSchema|MessageMetaSchema) = z\." cli/src/api/types.ts` returns 0 hits.
    - `rg -n "z\.unknown\(\)\.nullable\(\)" cli/src/api/types.ts` returns 0 hits (both metadata + agentState narrowed via SessionSchema reference; runnerState/metadata in CreateMachineResponseSchema narrowed via MachineSchema reference).
    - `rg -n "import .* SessionSchema.*from '@hapi/protocol" cli/src/api/types.ts` returns ≥ 1 hit.
    - `rg -n "MachineSchema|MachineMetadataSchema|RunnerStateSchema|MessageMetaSchema|UserMessageSchema|AgentMessageSchema|MessageContentSchema" cli/src/api/types.ts` shows these symbols ONLY in re-export / import lines, never in `z.object(` definitions.
    - `cd cli && bun typecheck` exits 0; `cd cli && bun test` exits 0.
  </acceptance_criteria>
  <done>cli/src/api/types.ts is a thin re-export + narrowed response-wrapper shell; REFA-03 cli-side complete (D-111, D-112, D-113).</done>
</task>

<task type="auto">
  <name>Task 2: Delete remaining cli-side flavor surface (SessionBootstrapOptions / buildSessionMetadata opts / bootstrapExistingSession opts / runCursor.ts call sites / cli/src/agent/types.ts setModel opts)</name>
  <files>cli/src/agent/sessionFactory.ts, cli/src/cursor/runCursor.ts, cli/src/agent/types.ts</files>
  <read_first>
    - cli/src/agent/sessionFactory.ts (full file — locate the three opts-type sites referenced by RESEARCH §7: `SessionBootstrapOptions` (~line 20), `buildSessionMetadata` opts param (~line 54), `bootstrapExistingSession` opts (~line 177). Also confirm the line-80 write of `flavor: options.flavor,` is already gone per 07-01-SUMMARY deviation #1.)
    - cli/src/cursor/runCursor.ts (locate the two `flavor: 'cursor',` keys at sessionFactory call sites near lines 54 and 59)
    - cli/src/agent/types.ts (locate the `setModel` signature carrying `opts?: { flavor?: AgentFlavor }` at ~line 74)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md §7 ("Writers — delete the write" table)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-01-SUMMARY.md "Auto-fixed Issues" #1 — confirms only the metadata-literal write was deleted in 07-01; the opts-type plumbing is still alive
  </read_first>
  <behavior>
    - `SessionBootstrapOptions`, `buildSessionMetadata` opts param, and `bootstrapExistingSession` opts no longer declare a `flavor: string` (or `flavor?: AgentFlavor`) field.
    - `cli/src/cursor/runCursor.ts` no longer passes `flavor: 'cursor'` to sessionFactory entry points.
    - `cli/src/agent/types.ts::setModel` no longer accepts `opts?: { flavor?: AgentFlavor }`. If the entire `opts` parameter was carrying only `flavor`, remove the parameter as well; if it carries other fields, narrow to those.
    - `cd cli && bun typecheck` + `cd cli && bun test` stay green; no test fixture remains that supplies a flavor opt to sessionFactory.
  </behavior>
  <action>
In `cli/src/agent/sessionFactory.ts`, delete the `flavor: string` (or `flavor?: string`) declaration from the `SessionBootstrapOptions` type, from the `buildSessionMetadata` opts parameter's inline shape, and from the `bootstrapExistingSession` opts parameter's inline shape. Where these option types reference each other (e.g. one extends another or composes via intersection), follow the structural reference and remove the field at every declaration site.

In `cli/src/cursor/runCursor.ts`, delete the two `flavor: 'cursor',` lines passed to sessionFactory call sites. Verify the surrounding object literal is still well-formed (no trailing-comma issues).

In `cli/src/agent/types.ts`, locate the `setModel` method signature. If `opts` carries only `flavor?: AgentFlavor`, delete the `opts` parameter entirely. If `opts` carries other fields, delete only the `flavor?: AgentFlavor` key. Update the corresponding implementation(s) of `setModel` (typically in `cli/src/agent/api.ts` or `cli/src/agent/apiSession.ts`) to match — `cd cli && bun typecheck` will surface every impl site.

Sweep any remaining `flavor` references in cli test fixtures that may have re-emerged: `rg -n "flavor:\s*'cursor'" cli/src/` should return 0 hits inside `metadata: { ... }` literals (resume-target literals are preserved — see Pitfall plan-checker note in PATTERNS).
  </action>
  <verify>
    <automated>cd cli &amp;&amp; bun typecheck &amp;&amp; bun test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "flavor:\s*string" cli/src/agent/sessionFactory.ts` returns 0 hits.
    - `rg -n "flavor:\s*'cursor'" cli/src/cursor/runCursor.ts` returns 0 hits.
    - `rg -n "flavor\?:\s*AgentFlavor" cli/src/agent/types.ts` returns 0 hits.
    - `rg -n "flavor:\s*['\"]cursor['\"]" cli/src/` (excluding `resume.ts`, `resume.test.ts`, `commands/resume.test.ts` where ResumableSession / LocalResumeTarget literals legitimately use a top-level `flavor` field per RESEARCH §7 plan-checker note) returns 0 hits in `metadata: { ... }` literals.
    - `cd cli && bun typecheck` exits 0; `cd cli && bun test` exits 0.
  </acceptance_criteria>
  <done>cli writer-side D-122 fully closed; no cli call path constructs or passes a flavor field/value anywhere outside the legitimate ResumableSession / LocalResumeTarget shapes owned by P5.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Collapse web/src/types/api.ts to re-exports + delete local Machine / RunnerState / SessionMetadataSummary / SessionsResponse / SessionResponse / MessagesResponse / MachinesResponse / SpawnResponse</name>
  <files>web/src/types/api.ts</files>
  <read_first>
    - web/src/types/api.ts (full file — confirm line ranges: SessionMetadataSummary ~26-40 (may already be partially edited by 07-01); RunnerState ~50-64; Machine ~66-77; SessionsResponse/SessionResponse/MessagesResponse/MachinesResponse ~89-101; SpawnResponse ~118-120; SyncEvent re-export ~240)
    - shared/src/types.ts (verify the symbols to re-export are exposed)
    - shared/src/responses.ts (verify Sessions/Session/Messages/Machines/Spawn Response types are exported)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md "MODIFY web/src/types/api.ts" + 07-RESEARCH.md §2 + §3
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-01-SUMMARY.md (web "key-files modified" list shows 07-01 did NOT touch web/src/types/api.ts; full collapse is this task's responsibility)
  </read_first>
  <behavior>
    - web/src/types/api.ts no longer carries local declarations of `Machine`, `RunnerState`, `SessionMetadataSummary`, `SessionsResponse`, `SessionResponse`, `MessagesResponse`, `MachinesResponse`, `SpawnResponse`. All come from `@hapi/protocol/types` re-exports.
    - The canonical name `SessionSummaryMetadata` (shared) replaces `SessionMetadataSummary` (web-local). Update web consumers that imported `SessionMetadataSummary` from `@/types/api` to import `SessionSummaryMetadata` from the same module — the re-export change makes this a name swap, not a deletion of the symbol.
    - Out-of-scope wrappers (`MachinePathsExistsResponse`, `GitCommandResponse`, `FileSearchResponse`, `ListDirectoryResponse`, `FileReadResponse`, `UploadFileResponse`, `DeleteUploadResponse`, `MachineListDirectoryResponse`, `GitStatusFiles`, `SlashCommandsResponse`, `SkillsResponse`, `PushVapidPublicKeyResponse`, `AuthResponse`) keep their web-local definitions (RESEARCH §3 explicitly defers these).
    - The `export type SyncEvent = ProtocolSyncEvent` line at ~240 stays untouched (it already re-exports the tightened union via @hapi/protocol).
    - `cd web && bun run typecheck` (or workspace `bun typecheck`) green.
  </behavior>
  <action>
Delete the local `SessionMetadataSummary` type declaration (~lines 26-40). Delete the local `RunnerState` (~50-64) and `Machine` (~66-77) TS types. Delete the local `SessionsResponse` / `SessionResponse` / `MessagesResponse` / `MachinesResponse` (~89-101). Delete the local `SpawnResponse` (~118-120).

Extend the existing `export type { ... } from '@hapi/protocol/types'` re-export block (lines ~9-24) to add: `Machine, MachineMetadata, RunnerState, SessionSummaryMetadata, SessionsResponse, SessionResponse, MessagesResponse, MachinesResponse, SpawnResponse, SessionPatch, MachinePatch`. (Verify `@hapi/protocol/types` re-exports all of these — if not, supplement with `import type { ... } from '@hapi/protocol/schemas'` or `from '@hapi/protocol/responses'` as the surfacing pattern requires.)

For the name swap: do NOT re-export `SessionMetadataSummary` as an alias of `SessionSummaryMetadata`. Instead, run `cd web && bun typecheck` and update each consumer (likely a small set: `SessionList.tsx`, `SessionHeader.tsx`, possibly `useSSE.ts`) to import the canonical `SessionSummaryMetadata` name. If the count of consumers is large enough that the rename is risky, fall back to adding `export type SessionMetadataSummary = SessionSummaryMetadata` as a transitional alias and note it in the SUMMARY — but prefer the clean rename if feasible (Phase 12 docs cleanup will sweep names later).

Keep the line `export type SyncEvent = ProtocolSyncEvent` untouched.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "^export type (Machine|RunnerState|SessionsResponse|SessionResponse|MessagesResponse|MachinesResponse|SpawnResponse|SessionMetadataSummary) = " web/src/types/api.ts` returns 0 hits (none of these may be local declarations).
    - `rg -n "from '@hapi/protocol/types'" web/src/types/api.ts` returns ≥ 1 hit, and that block re-exports `Machine, RunnerState, SessionsResponse, SessionResponse, MessagesResponse, MachinesResponse, SpawnResponse` at minimum.
    - `rg -n "export type SyncEvent = ProtocolSyncEvent" web/src/types/api.ts` returns 1 hit (preserved).
    - `cd web && bun run typecheck` exits 0 (or workspace `bun typecheck` exits 0 if web has no standalone command).
    - Workspace `bun run test` exits 0 (existing web tests should adapt automatically since they consume via the same import surface).
  </acceptance_criteria>
  <done>web/src/types/api.ts is a thin re-export shell for the wire contract + response wrappers; REFA-03 web-side complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Rewrite web/src/hooks/useSSE.ts — delete 7 narrow functions + SessionPatch local type + invalidation queue; replace with SyncEventSchema.safeParse + discriminator branches (REFA-04 centerpiece, D-120, D-121)</name>
  <files>web/src/hooks/useSSE.ts</files>
  <read_first>
    - web/src/hooks/useSSE.ts (full file — locate every deletion target enumerated in <interfaces>: SessionPatch local type line ~33; 7 narrow functions lines ~45-135; invalidation queue refs + INVALIDATION_BATCH_MS lines ~189-194; queueSessionListInvalidation / queueSessionDetailInvalidation / queueMachinesInvalidation / flushInvalidations / scheduleInvalidationFlush lines ~294-348; existing discriminator branch using narrows lines ~510-549; effect teardown invalidation cleanup lines ~625-636; the `as SyncEvent` cast at line ~573)
    - shared/src/schemas.ts (post-07-01 — confirm SyncEventSchema + SessionPatch type are exported)
    - shared/src/types.ts (confirm SyncEvent, Session, Machine, SessionSummary types are exported via @hapi/protocol)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md §6 (useSSE rewrite call-site map — the 7 narrow functions are only called inside useSSE.ts; the 3 queue-invalidation functions become unused after D-120) + RESEARCH Example 4 (safeParse skeleton)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md "REWRITE web/src/hooks/useSSE.ts" — the surgical delete list + the new safeParse + discriminator branch skeleton
    - web/src/lib/message-window-store (the cache-mutator skeletons at useSSE.ts lines ~350-472 must be preserved; only the narrow + invalidation infrastructure goes)
  </read_first>
  <behavior>
    - `useSSE.ts` no longer declares `type SessionPatch` (it imports `SessionPatch` from `@hapi/protocol/schemas`).
    - `useSSE.ts` no longer defines `hasRecordShape`, `isSessionRecord`, `getSessionPatch`, `hasUnknownSessionPatchKeys`, `isMachineMetadata`, `isMachineRecord`, `isInactiveMachinePatch` — zero hits via ripgrep inside this file.
    - `useSSE.ts` no longer defines `queueSessionListInvalidation`, `queueSessionDetailInvalidation`, `queueMachinesInvalidation`, `flushInvalidations`, `scheduleInvalidationFlush`, `INVALIDATION_BATCH_MS`, `invalidationTimerRef`, `pendingInvalidationsRef` — zero hits.
    - `handleMessage` now: JSON.parse → `SyncEventSchema.safeParse(parsed)` → on failure `console.error('[useSSE] dropped malformed event', result.error)` + early return (no refetch, no invalidate) → on success call `handleSyncEvent(result.data)`.
    - `handleSyncEvent` is typed against the `SyncEvent` discriminated union (no `unknown`, no narrow function calls). The discriminator branches use:
      - `session-added`: always full Session; `setQueryData(queryKeys.session(...), { session: data })` + `upsertSessionSummary(data)`.
      - `session-updated`: `if ('metadata' in data) { ... full Session path ... } else { patchSessionDetail + patchSessionSummary }` — both arms are statically typed via the union.
      - `machine-updated`: `if (data === null) removeMachine; else if ('id' in data) upsertMachine; else removeMachine` (the strict-patch arm only carries `active: false`, so treating it as "deactivate signal → remove" matches current production semantics — confirmed by 07-01 cascade work which already wired this path).
      - All other variants (session-removed, messages-consumed, messages-invalidated, message-received, message-cancelled, session-ended, connection-*, heartbeat, toast) keep their existing behavior — they were never inside the narrow-function blast radius.
    - The cache mutators `upsertSessionSummary / patchSessionSummary / patchSessionDetail / removeSessionSummary / upsertMachine / removeMachine` are preserved (only their parameter type sources change to the shared `SessionPatch` / `Session` / `Machine` types).
    - Effect-teardown no longer references the invalidation timer or pending-invalidations ref.
    - `cd web && bun run typecheck` + workspace `bun run test` both green (the new useSSE.test.tsx in Task 5 supplies the runtime evidence — this task only needs to compile and pass existing tests).
  </behavior>
  <action>
Delete the local `type SessionPatch = ...` declaration. Replace it with `import { SyncEventSchema, type SessionPatch } from '@hapi/protocol/schemas'` (extend the existing import block as needed).

Delete the 7 narrow functions (`hasRecordShape`, `isSessionRecord`, `getSessionPatch`, `hasUnknownSessionPatchKeys`, `isMachineMetadata`, `isMachineRecord`, `isInactiveMachinePatch`) and their bodies entirely.

Delete the invalidation infrastructure: `INVALIDATION_BATCH_MS` constant, `invalidationTimerRef`, `pendingInvalidationsRef` refs, `flushInvalidations` function, `scheduleInvalidationFlush` function, `queueSessionListInvalidation` / `queueSessionDetailInvalidation` / `queueMachinesInvalidation` functions, and any `clearTimeout(invalidationTimerRef.current)` cleanup inside the effect teardown.

Rewrite `handleMessage` per the safeParse skeleton in 07-PATTERNS.md "REWRITE web/src/hooks/useSSE.ts". The function MUST: (a) early-return if `message.data` is not a string; (b) try/catch JSON.parse → on throw early-return; (c) call `SyncEventSchema.safeParse(parsed)` → on `!result.success` call `console.error('[useSSE] dropped malformed event', result.error)` and early-return (D-120: NO refetch, NO invalidate); (d) on success call `handleSyncEvent(result.data)` where `result.data` is the typed `SyncEvent`.

Rewrite `handleSyncEvent(event: SyncEvent)` to use discriminator branching per PATTERNS Pattern B + RESEARCH Example 4. Replace the current narrow-function-driven branches at ~lines 510-549 with the union-typed branches enumerated under <behavior>. The mutator function bodies (`upsertSessionSummary`, `patchSessionSummary`, `patchSessionDetail`, `upsertMachine`, `removeMachine`, etc.) stay byte-identical — only their call-site param types now come from the shared union.

Remove the `as SyncEvent` cast at the old line ~573 (the safeParse already produced the typed value).

Run `cd web && bun run typecheck` after the rewrite. If consumers of `SessionPatch` outside `useSSE.ts` previously imported the local type from `'./useSSE'`, redirect them to `'@hapi/protocol/schemas'`. Existing web tests should continue to pass — if any test fails because it depended on a now-deleted narrow function being exported, fix the test to use `SyncEventSchema.safeParse` directly (and document this in the SUMMARY).

Identifier-level non-goals: do NOT touch the `useSSE` hook signature (`enabled / token / baseUrl / subscription / onEvent` parameters), the cache-mutator function bodies, the EventSource construction + reconnect logic, the heartbeat handling, the toast handling, the `messages-*` event handlers, or the `connection-*` event handlers. Those are out-of-scope for Phase 7 and owned by REFT-02 (Phase 11) and REFW-02 (Phase 9).
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "hasUnknownSessionPatchKeys|hasRecordShape|isSessionRecord|getSessionPatch|isMachineMetadata|isMachineRecord|isInactiveMachinePatch" web/src/hooks/useSSE.ts` returns 0 hits.
    - `rg -n "queueSessionListInvalidation|queueSessionDetailInvalidation|queueMachinesInvalidation|flushInvalidations|scheduleInvalidationFlush|INVALIDATION_BATCH_MS|invalidationTimerRef|pendingInvalidationsRef" web/src/hooks/useSSE.ts` returns 0 hits.
    - `rg -n "SyncEventSchema\.safeParse" web/src/hooks/useSSE.ts` returns ≥ 1 hit.
    - `rg -n "as SyncEvent" web/src/hooks/useSSE.ts` returns 0 hits.
    - `rg -n "type SessionPatch = " web/src/hooks/useSSE.ts` returns 0 hits (it is now imported, not declared locally).
    - `rg -n "import .* SyncEventSchema.*from '@hapi/protocol/schemas'" web/src/hooks/useSSE.ts` returns 1 hit.
    - `cd web && bun run typecheck` exits 0.
    - Workspace `bun run test` exits 0 (existing web tests continue passing; useSSE.test.tsx is added in Task 5).
  </acceptance_criteria>
  <done>useSSE consumes the canonical SyncEventSchema; the SC#2 / SC#3 "no fallback to refetch list" branch is structurally absent; the backgroundTaskCount-bug-by-construction is now a no-op cache mutation in the patch arm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create web/src/hooks/useSSE.test.tsx — Vitest + MockEventSource integration test covering full/patch/null/malformed event variants (D-127#2)</name>
  <files>web/src/hooks/useSSE.test.tsx</files>
  <read_first>
    - web/src/hooks/useSSE.ts (post-Task-4 — to learn the hook signature, the QueryClient integration, and which message-window-store functions to mock)
    - web/src/hooks/mutations/useSendMessage.test.tsx (analog for Vitest + renderHook + QueryClient harness; framework split per 07-PATTERNS Pattern D — Vitest, NOT bun:test)
    - shared/src/schemas.ts (post-07-01 — Session, Machine, SessionPatch, MachinePatch shapes for fixture construction)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md "NEW web/src/hooks/useSSE.test.tsx" — MockEventSource skeleton (no analog exists in web; use the fallback shim from RESEARCH § Environment Availability)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md "Validation Architecture" + "Wave 0 Requirements" + Pitfall #2 (the runtime contract test's scope vs the type checker)
  </read_first>
  <behavior>
    Test cases inside `describe('useSSE handleSyncEvent', () => { ... })`, Vitest framework (`vi.mock`, `vi.fn`, `vi.spyOn`):
    - it('full session-added → setQueryData(detail) + upsertSessionSummary'): dispatch a full Session-shaped session-added event; assert `queryClient.getQueryData(queryKeys.session(id))` equals `{ session: <fixture> }`; assert the summaries cache has the new entry.
    - it('session-updated with single-field backgroundTaskCount patch mutates summary but does NOT call invalidateQueries'): seed summaries cache; spy on `queryClient.invalidateQueries`; dispatch `{ type: 'session-updated', sessionId, data: { backgroundTaskCount: 3 } }`; assert the summary's backgroundTaskCount === 3; assert the spy was NEVER called. THIS IS THE SMOKING-GUN REGRESSION TEST — name it explicitly so it lights up in CI when the bug returns.
    - it('session-updated with each other patch field mutates correctly'): one assertion per remaining patch field (active, activeAt, thinking, updatedAt, permissionMode, model, modelReasoningEffort, effort) — can be a parameterized loop over the 8 fields with one fixture per field.
    - it('session-updated with full Session payload upserts both summary and detail'): dispatch a full Session under session-updated; assert both summary cache and detail cache reflect the new state.
    - it('full machine-updated → upsertMachine'): seed machines cache; dispatch a full Machine; assert upsertion.
    - it('machine-updated with { active: false } patch → removeMachine'): dispatch the strict-shape patch; assert removal.
    - it('machine-updated with data:null → removeMachine'): dispatch null; assert removal.
    - it('malformed event (unknown type) → console.error called, cache unchanged, invalidateQueries NOT called'): dispatch `{ type: 'invented-event', sessionId: 'x' }`; spy on `console.error` and `queryClient.invalidateQueries`; assert console.error was called ≥ 1; assert invalidate was NEVER called; assert the seeded caches are byte-identical to pre-dispatch.
    - it('malformed event (session-updated with unknownField patch) → drop + log + no mutation'): dispatch `{ type: 'session-updated', sessionId: 's', data: { unknownField: 1 } }`; assert console.error called; no cache mutation; no invalidate call.
    - it('JSON.parse failure → silent return, no log of "dropped malformed event", no mutation'): dispatch a non-JSON string via MockEventSource; assert the cache is unchanged.
  </behavior>
  <action>
Create `web/src/hooks/useSSE.test.tsx` following the Vitest + renderHook + QueryClient harness pattern from `web/src/hooks/mutations/useSendMessage.test.tsx`. Import from `vitest`, `@testing-library/react`, `@tanstack/react-query`. Mock `@/lib/message-window-store` with `vi.mock` per the existing analog (its functions: `clearMessageWindow`, `getMessageWindowState`, `ingestIncomingMessages`, `markMessagesConsumed`, `removeOptimisticMessage`, `updateMessageStatus` — all stubbed as `vi.fn()`).

At the top of the file, define `class MockEventSource` per the PATTERNS shim: instance tracking via static `instances` array, `onmessage / onopen / onerror` properties, `readyState`, a `close()`, and a `dispatch(payload)` helper that synthesizes `{ data: JSON.stringify(payload) }` into `onmessage`. Provide a static `reset()`. In a `beforeEach`, call `MockEventSource.reset()` and patch `globalThis.EventSource = MockEventSource as unknown as typeof EventSource`. Restore in `afterEach` if needed.

Build minimal valid Session + Machine fixture builders at the top of the file (one function each that returns a fully-shaped Session/Machine with sensible defaults; tests override only the fields they care about). NEVER include a `flavor` key in any metadata fixture (the field is gone from shared schema).

Implement each test case enumerated under <behavior>. For the smoking-gun backgroundTaskCount case, name the `it()` exactly: `it('session-updated patch with only backgroundTaskCount mutates summary without invalidate (Phase 7 REFA-04 regression guard)', ...)`.

Use `renderHook(() => useSSE({ enabled: true, token: 't', baseUrl: 'http://x', subscription: { all: true }, onEvent: vi.fn() }), { wrapper: createWrapper() })`. After mount, `MockEventSource.instances[0]` is the active source; call `.dispatch(event)` on it to push events. Wrap assertions in `await waitFor(() => { ... })` because React Query mutations are async.

For the cache-unchanged assertions, snapshot `queryClient.getQueryData(...)` before dispatch and `expect(...).toEqual(snapshot)` after.

For the `console.error` spy, use `vi.spyOn(console, 'error').mockImplementation(() => {})` in `beforeEach` and assert call count in the relevant cases. Restore in `afterEach`.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun test src/hooks/useSSE.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `web/src/hooks/useSSE.test.tsx` exists.
    - `rg -n "from 'vitest'" web/src/hooks/useSSE.test.tsx` returns ≥ 1 hit; `rg -n "from 'bun:test'" web/src/hooks/useSSE.test.tsx` returns 0 hits.
    - `rg -n "class MockEventSource" web/src/hooks/useSSE.test.tsx` returns 1 hit.
    - `rg -n "backgroundTaskCount" web/src/hooks/useSSE.test.tsx` returns ≥ 2 hits (the smoking-gun regression test + its fixture).
    - `rg -n "Phase 7 REFA-04 regression guard" web/src/hooks/useSSE.test.tsx` returns 1 hit (the named smoking-gun test).
    - `rg -n "invalidateQueries" web/src/hooks/useSSE.test.tsx` shows the spy + at least one `not.toHaveBeenCalled` assertion (no-invalidate invariant).
    - `rg -n "flavor:" web/src/hooks/useSSE.test.tsx` returns 0 hits (no flavor in any metadata fixture).
    - `cd web && bun test src/hooks/useSSE.test.tsx` exits 0; reported test count ≥ 9 distinct `it()` cases.
  </acceptance_criteria>
  <done>D-127#2 satisfied; the backgroundTaskCount-by-construction fix has runtime evidence; parse-failure-drop invariant has runtime evidence; all union branches covered.</done>
</task>

<task type="auto">
  <name>Task 6: Delete FlavorIcon component + FLAVOR_BADGES table in web/src/components/SessionList.tsx (Pitfall #4 closeout)</name>
  <files>web/src/components/SessionList.tsx</files>
  <read_first>
    - web/src/components/SessionList.tsx (locate `FlavorIcon` definition + `FLAVOR_BADGES` lookup table near lines 498-499 and the render site near 594; confirm the 07-01 cascade already replaced the `flavor` prop value reads with `'cursor'` literals at the call site)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md §7 (the FlavorIcon callout — Pitfall #4) + RESEARCH Pitfall #4
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-01-SUMMARY.md "Auto-fixed Issues" #8 (notes the cascade preserved FlavorIcon/FLAVOR_BADGES for slice 3 to handle)
  </read_first>
  <behavior>
    - `FlavorIcon` component definition is deleted from `SessionList.tsx`.
    - `FLAVOR_BADGES` lookup table is deleted.
    - The render site that previously emitted `<FlavorIcon flavor="cursor" />` is either: (a) deleted entirely (no icon rendered), or (b) replaced with a fixed Cursor icon (a single inline `<img>` or a different existing icon component). Researcher's recommendation is option (b) to retain visual identity — choose whichever keeps the test suite green and document the choice in SUMMARY.
    - The two existing SessionList tests (`SessionList.test.ts`, `SessionList.directory-action.test.tsx`) continue to pass.
    - `cd web && bun run typecheck` + workspace `bun run test` green.
  </behavior>
  <action>
Delete the `FlavorIcon` functional component declaration and the `FLAVOR_BADGES` const lookup table from `SessionList.tsx`. Locate the render site where `<FlavorIcon ... />` is emitted (the 07-01 cascade pinned its prop to `flavor="cursor"`).

Pick option (a) or option (b):
- Option (a): delete the `<FlavorIcon />` element entirely from the row template; the row no longer renders an agent icon. Simpler; matches the Cursor-only world.
- Option (b): replace with a fixed Cursor icon. If a Cursor icon already exists in `web/src/components/` or `web/src/assets/`, import and render it; otherwise use any existing brand icon component that conveys "Cursor agent" in a single fixed glyph.

Document the choice in the SUMMARY. Either option preserves the existing test contract (the tests don't assert on FlavorIcon specifically — they assert on session list semantics like search/filter/order).

Run `cd web && bun run test src/components/SessionList.test.ts src/components/SessionList.directory-action.test.tsx` to confirm no regression.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run typecheck &amp;&amp; bun test src/components/SessionList</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "FlavorIcon" web/src/components/SessionList.tsx` returns 0 hits.
    - `rg -n "FLAVOR_BADGES" web/src/components/SessionList.tsx` returns 0 hits.
    - `rg -n "FlavorIcon|FLAVOR_BADGES" web/src/` returns 0 hits anywhere (confirms no other file imports these symbols).
    - `cd web && bun run typecheck` exits 0.
    - `cd web && bun test src/components/SessionList` exits 0 (both existing SessionList tests still pass).
  </acceptance_criteria>
  <done>Pitfall #4 closed; web has no flavor-driven UI rendering surface; SessionList renders either no agent icon or a fixed Cursor brand mark.</done>
</task>

<task type="auto">
  <name>Task 7: Slice 3 gate — workspace typecheck + full test suite + new useSSE.test.tsx green</name>
  <files>(verification only — no edits)</files>
  <read_first>
    - cli/src/api/types.ts (post-Tasks 1-2)
    - web/src/types/api.ts (post-Task-3)
    - web/src/hooks/useSSE.ts (post-Task-4)
    - web/src/hooks/useSSE.test.tsx (post-Task-5)
    - web/src/components/SessionList.tsx (post-Task-6)
  </read_first>
  <action>
Run the slice-3 gate at the workspace root. Expected test count: 07-02's baseline + the new useSSE.test.tsx cases. If any test regressed, investigate the regression before declaring slice 3 complete — common culprits would be a missed consumer of a deleted narrow function, a `SessionMetadataSummary` → `SessionSummaryMetadata` rename miss, or a test fixture that still passes `flavor: 'cursor'` inside `metadata: { ... }`.
  </action>
  <verify>
    <automated>bun typecheck &amp;&amp; bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `bun typecheck` (workspace root) exits 0.
    - `bun run test` (workspace root) exits 0.
    - The Phase-7 REFA-04 regression-guard test (`session-updated patch with only backgroundTaskCount mutates summary without invalidate`) appears in passing output.
    - `rg -n "hasUnknownSessionPatchKeys" cli/src hub/src web/src shared/src` returns 0 hits.
    - `rg -n "getSessionPatch" web/src/hooks` returns 0 hits.
    - `rg -n "^export type Machine = \{" cli/src web/src` returns 0 hits.
    - `rg -n "RunnerStateSchema = z\.object|MachineMetadataSchema = z\.object" cli/src web/src` returns 0 hits.
    - `rg -n "FlavorIcon" web/src` returns 0 hits.
  </acceptance_criteria>
  <done>Slice 3 of D-125 green; REFA-03 fully closed (cli + web mirrors gone, single shared source); REFA-04 fully implemented (useSSE rewritten + smoking-gun regression test green); only slice 4 (guard script + final ripgrep gate) remains.</done>
</task>

</tasks>

<verification>
- `cd cli && bun typecheck && bun test` covers Tasks 1-2.
- `cd web && bun run typecheck` covers Task 3.
- `cd web && bun run typecheck` covers Task 4 (no new test in this task; Task 5 supplies the runtime coverage).
- `cd web && bun test src/hooks/useSSE.test.tsx` covers Task 5.
- `cd web && bun test src/components/SessionList` covers Task 6.
- `bun typecheck && bun run test` (workspace root) covers Task 7 slice gate.
</verification>

<success_criteria>
- D-125 Slice 3 gate met: workspace `bun typecheck && bun run test` green.
- REFA-03 complete: `MachineMetadataSchema`, `RunnerStateSchema`, `MessageMeta/User/AgentMessage/MessageContent` schemas, `Machine` / `RunnerState` types, response wrappers, and `SessionSummaryMetadata` exist ONLY in `shared/`; cli + web + hub are pure re-exports.
- REFA-04 complete: `hasUnknownSessionPatchKeys` and the 6 sibling narrow functions are deleted; the invalidation queue is deleted; useSSE consumes `SyncEventSchema.safeParse` and discriminator branches; the `backgroundTaskCount` regression-guard test passes (the smoking-gun bug is fixed by construction).
- D-113 satisfied: `CreateSessionResponseSchema` + `CreateMachineResponseSchema` no longer carry `z.unknown().nullable()` placeholders.
- D-122 fully closed on cli writer side: zero `flavor:` field declarations or writes in `cli/src/agent/` or `cli/src/cursor/runCursor.ts` (resume-target shapes preserved per RESEARCH plan-checker note).
- D-114 fully wired on web read side: `web/src/types/api.ts` is a thin re-export shell.
- D-127#2 satisfied: `web/src/hooks/useSSE.test.tsx` exists with runtime evidence for every union branch + the parse-failure-drop invariant.
- Pitfall #4 closed: FlavorIcon + FLAVOR_BADGES deleted from SessionList.
</success_criteria>

<output>
Create `.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-03-SUMMARY.md` documenting:
- The `SessionMetadataSummary` → `SessionSummaryMetadata` rename strategy chosen (clean rename vs transitional alias) + the affected consumer list.
- FlavorIcon disposition (option (a) deleted, or option (b) replaced with which fixed icon).
- Test count delta vs 07-02's baseline + the smoking-gun regression test's verbatim name.
- Any consumer that previously relied on cli `CreateSessionResponse.session.metadata` being `unknown` and how it was migrated to the typed `Metadata` shape.
- Any test fixture that needed flavor stripping during the cli-side cleanup.
</output>
