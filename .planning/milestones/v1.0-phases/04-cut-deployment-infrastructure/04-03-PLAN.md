---
phase: 04-cut-deployment-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/src/ui/logger.ts
  - cli/src/ui/logger.test.ts
  - cli/src/ui/doctor.ts
  - cli/src/ui/doctor.test.ts
autonomous: true
requirements:
  - CUT-11
user_setup: []
must_haves:
  truths:
    - "CLI logger writes to console/local files only and never uploads logs to HAPI_API_URL."
    - "The dangerous remote-log env flag no longer changes runtime behavior."
    - "Doctor output no longer surfaces the remote-log toggle."
    - "Legitimate direct-connect HAPI_API_URL usage remains outside the removed logger upload path."
  artifacts:
    - path: "cli/src/ui/logger.ts"
      provides: "Local-only CLI logger without remote fetch/upload path"
    - path: "cli/src/ui/logger.test.ts"
      provides: "Regression test proving dangerous env + HAPI_API_URL does not call fetch"
    - path: "cli/src/ui/doctor.ts"
      provides: "Doctor diagnostics without dangerous remote-log toggle"
    - path: "cli/src/ui/doctor.test.ts"
      provides: "Regression test proving doctor output omits the dangerous toggle"
  key_links:
    - from: "cli/src/ui/logger.ts"
      to: "local log file"
      via: "appendFileSync"
      pattern: "appendFileSync\\("
    - from: "cli/src/ui/doctor.ts"
      to: "configuration"
      via: "diagnostic output"
      pattern: "configuration\\."
---

<objective>
Remove the upstream remote-log upload channel from CLI diagnostics while preserving local logging and useful doctor output.

Purpose: CUT-11 and decisions D-57 through D-60 require eliminating accidental secret exfiltration through remote logs without breaking normal direct CLI-to-hub configuration.
Output: `logger.ts` has no dangerous upload branch, `doctor.ts` no longer advertises the toggle, and focused tests cover both behaviors.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cut-deployment-infrastructure/04-CONTEXT.md
@.planning/phases/04-cut-deployment-infrastructure/04-RESEARCH.md
@.planning/phases/04-cut-deployment-infrastructure/04-VALIDATION.md
@.planning/phases/04-cut-deployment-infrastructure/04-PATTERNS.md
@cli/src/ui/logger.ts
@cli/src/ui/doctor.ts
@cli/src/commands/auth.test.ts
@cli/src/utils/spawnHappyCLI.test.ts
@cli/src/api/api.extraHeaders.test.ts

<interfaces>
From `04-PATTERNS.md`: keep `logger.debug/info/warn/error` local file behavior through `appendFileSync`; delete `dangerouslyUnencryptedServerLoggingUrl`, `sendToRemoteServer`, `fetch`, and the fire-and-forget remote branch.

From `04-PATTERNS.md`: keep `doctor.ts` legitimate `HAPI_API_URL`, local logs dir, config, and runner diagnostics; delete only `DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING` display.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 04-03-01: Make CLI logger local-only with regression coverage</name>
  <read_first>
    - `.planning/phases/04-cut-deployment-infrastructure/04-CONTEXT.md` decisions D-57, D-59, D-60, D-65, D-67
    - `.planning/phases/04-cut-deployment-infrastructure/04-VALIDATION.md` Wave 0 Requirements
    - `.planning/phases/04-cut-deployment-infrastructure/04-PATTERNS.md` sections `cli/src/ui/logger.ts` and `cli/src/ui/logger.test.ts`
    - `cli/src/ui/logger.ts`
    - `cli/src/utils/spawnHappyCLI.test.ts`
    - `cli/src/api/api.extraHeaders.test.ts`
  </read_first>
  <files>cli/src/ui/logger.ts, cli/src/ui/logger.test.ts</files>
  <action>Create `cli/src/ui/logger.test.ts` if missing. Test first that `logger.debug/info/warn/error` or a new `Logger` instance writes to a temp local log file and does not call `globalThis.fetch` even when both `DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING` and `HAPI_API_URL` are set. Then remove from `cli/src/ui/logger.ts` the dangerous env gate, remote URL field, remote warning output, `sendToRemoteServer`, `fetch` usage, and any comments describing remote upload per D-57/D-60/D-67. Preserve console/local file logging, log file path defaults, debug-only append error reporting, and legitimate direct-connect `HAPI_API_URL` uses elsewhere per D-59.</action>
  <verify>
    <automated>cd cli && bun run test src/ui/logger.test.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `cd cli && bun run test src/ui/logger.test.ts --runInBand` exits 0.
    - `cli/src/ui/logger.ts` contains no `DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING`, `dangerouslyUnencryptedServerLoggingUrl`, `sendToRemoteServer`, or `fetch(` remote upload path.
    - The new test sets `HAPI_API_URL` and proves `globalThis.fetch` is not called by logger methods.
    - Local log file assertions prove the logger still writes non-empty local entries.
  </acceptance_criteria>
  <done>CLI logging remains useful locally and cannot upload logs to the hub/API URL.</done>
</task>

<task type="auto">
  <name>Task 04-03-02: Remove doctor remote-log toggle display with coverage</name>
  <read_first>
    - `.planning/phases/04-cut-deployment-infrastructure/04-CONTEXT.md` decisions D-58, D-59, D-60, D-65, D-67
    - `.planning/phases/04-cut-deployment-infrastructure/04-VALIDATION.md` Wave 0 Requirements
    - `.planning/phases/04-cut-deployment-infrastructure/04-PATTERNS.md` sections `cli/src/ui/doctor.ts` and `cli/src/ui/doctor.test.ts`
    - `cli/src/ui/doctor.ts`
    - `cli/src/commands/auth.test.ts`
  </read_first>
  <files>cli/src/ui/doctor.ts, cli/src/ui/doctor.test.ts</files>
  <action>Create `cli/src/ui/doctor.test.ts` if missing. Use the existing console spy and ANSI-strip pattern from `cli/src/commands/auth.test.ts`. Remove `DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING` from `getEnvironmentInfo()` and from printed doctor output per D-58. Preserve useful diagnostics such as `HAPI_API_URL` direct-connect visibility, `HAPI_HOME`, config paths, local logs dir, runner/process diagnostics, and connection status per D-59. Do not add a replacement export/upload feature per D-60.</action>
  <verify>
    <automated>cd cli && bun run test src/ui/doctor.test.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `cd cli && bun run test src/ui/doctor.test.ts --runInBand` exits 0.
    - `cli/src/ui/doctor.ts` contains no `DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING` literal and no remote-log toggle label.
    - Doctor output test proves the dangerous flag is absent even when set in `process.env`.
    - Doctor output or environment info still includes legitimate `HAPI_API_URL` direct-connect diagnostics if they existed before this task.
  </acceptance_criteria>
  <done>Doctor no longer advertises the removed remote-log upload channel while retaining local diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI runtime -> local log file | Agent/session output is persisted under local `~/.hapi/logs`. |
| CLI runtime -> network | Removed boundary; logger no longer sends logs to `HAPI_API_URL`. |
| Environment -> CLI diagnostics | Env vars influence diagnostic output and must not revive removed upload behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | `cli/src/ui/logger.ts` | mitigate | Remove dangerous env gate, remote URL field, `fetch` upload path, and remote logging warning per D-57. |
| T-04-08 | Information Disclosure | `cli/src/ui/doctor.ts` | mitigate | Remove dangerous remote-log toggle from env info and doctor output while preserving direct-connect diagnostics per D-58/D-59. |
| T-04-09 | Repudiation | local-only logs | accept | Local logs remain append-only best-effort diagnostics; this phase intentionally does not add export/upload features per D-60. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package install is planned in this slice. |

## ASVS L1 Concern Mapping

| ASVS Category | Applicability | Plan Control |
|---------------|---------------|--------------|
| V2 Authentication | preserve | Keep legitimate direct-connect `HAPI_API_URL` and auth-related CLI behavior outside the removed logger upload path. |
| V3 Session Management | not changed | Logger and doctor cleanup must not alter session creation, recovery, or hub session semantics. |
| V4 Access Control | preserve | Do not introduce a replacement upload/export endpoint or bypass path; diagnostics remain local/manual. |
| V5 Input Validation | preserve | Env-driven diagnostics are narrowed by deleting the dangerous flag path, not by adding new env input handling. |
| V6 Cryptography | not changed | No cryptographic behavior is added; local log persistence remains filesystem-only. |
| V9 Communications | mitigate | Remove the CLI runtime -> network log upload boundary so logs are not sent over `HAPI_API_URL`. |
</threat_model>

<verification>
Focused gates:
- `cd cli && bun run test src/ui/logger.test.ts --runInBand`
- `cd cli && bun run test src/ui/doctor.test.ts --runInBand`

Plan-level gate: `bun typecheck && bun run test`.
</verification>

<success_criteria>
- `cli/src/ui/logger.ts` no longer forwards logs to `HAPI_API_URL`.
- `cli/src/ui/doctor.ts` no longer surfaces `DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING`.
- Local logging and direct-connect diagnostics remain intact.
</success_criteria>

<output>
Create `.planning/phases/04-cut-deployment-infrastructure/04-03-SUMMARY.md` when done.
</output>
