---
phase: 04-cut-deployment-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hub/src/configuration.ts
  - hub/src/config/settings.ts
  - hub/src/config/serverSettings.ts
  - hub/src/config/serverSettings.test.ts
autonomous: true
requirements:
  - CUT-10
user_setup: []
must_haves:
  truths:
    - "Hub configuration no longer reads or reports HAPI_RELAY_* values."
    - "HAPI_PUBLIC_URL remains the supported public URL setting for user-managed Tailscale."
    - "Old relay settings fields fail explicitly if they are represented in settings validation."
    - "Startup configuration summaries contain no relay/tunnel disabled state."
  artifacts:
    - path: "hub/src/config/serverSettings.ts"
      provides: "Relay-free env/file/default settings loader with HAPI_PUBLIC_URL preserved"
    - path: "hub/src/config/settings.ts"
      provides: "Settings shape without relay fields"
    - path: "hub/src/configuration.ts"
      provides: "Configuration facade without relay settings output"
    - path: "hub/src/config/serverSettings.test.ts"
      provides: "Regression coverage for relay field rejection when legacy keys are represented"
  key_links:
    - from: "hub/src/config/serverSettings.ts"
      to: "hub/src/configuration.ts"
      via: "loadServerSettings result"
      pattern: "loadServerSettings\\("
    - from: "hub/src/config/serverSettings.test.ts"
      to: "hub/src/config/serverSettings.ts"
      via: "old field rejection"
      pattern: "rejects old .* settings fields"
---

<objective>
Converge the hub configuration surface after relay deletion while preserving the neutral public URL setting.

Purpose: CUT-10 and decisions D-53 through D-56 require removing relay env/settings semantics without silently accepting stale configuration and without pulling Phase 10's broader config cleanup forward.
Output: Hub config no longer reads `HAPI_RELAY_*` or relay settings, `HAPI_PUBLIC_URL` remains, and settings tests cover any represented legacy relay keys.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cut-deployment-infrastructure/04-CONTEXT.md
@.planning/phases/04-cut-deployment-infrastructure/04-RESEARCH.md
@.planning/phases/04-cut-deployment-infrastructure/04-VALIDATION.md
@.planning/phases/04-cut-deployment-infrastructure/04-PATTERNS.md
@hub/src/configuration.ts
@hub/src/config/settings.ts
@hub/src/config/serverSettings.ts
@hub/src/config/serverSettings.test.ts

<interfaces>
From `04-PATTERNS.md`: keep the `Settings` interface narrow and extend the existing `OLD_SETTINGS_FIELDS` / `rejectOldSettingsFields(settings, settingsFile)` mechanism only if old relay fields are represented.

From `04-PATTERNS.md`: preserve `HAPI_PUBLIC_URL` env/file/default precedence and `sources.publicUrl`; remove relay env/default/source reporting and `HAPI_OFFICIAL_WEB_URL` relay semantics if they are only used for hosted relay web.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 04-02-01: Remove relay env and settings fields while preserving HAPI_PUBLIC_URL</name>
  <read_first>
    - `.planning/phases/04-cut-deployment-infrastructure/04-CONTEXT.md` decisions D-53, D-54, D-55, D-56, D-59
    - `.planning/phases/04-cut-deployment-infrastructure/04-RESEARCH.md` sections `Runtime State Inventory`, `Common Pitfalls`, and `Security Domain`
    - `hub/src/config/settings.ts`
    - `hub/src/config/serverSettings.ts`
    - `hub/src/configuration.ts`
  </read_first>
  <files>hub/src/configuration.ts, hub/src/config/settings.ts, hub/src/config/serverSettings.ts</files>
  <action>Delete any config schema fields, env reads, defaults, source reporting, and startup-summary data for `HAPI_RELAY_API`, `HAPI_RELAY_AUTH`, and `HAPI_RELAY_FORCE_TCP` per D-53/D-55. Remove `HAPI_OFFICIAL_WEB_URL` runtime use if it only feeds hosted relay web behavior per D-54. Keep `HAPI_PUBLIC_URL`, `publicUrl`, `sources.publicUrl`, listen host/port, CORS, VAPID, CLI token, owner/machine settings, and Phase 10-owned compatibility aliases unchanged. Do not add `.passthrough()`, migration shims, silent ignore behavior, replacement tunnel configuration, or generic config cleanup outside relay fields.</action>
  <verify>
    <automated>bun typecheck && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `hub/src/configuration.ts hub/src/config/settings.ts hub/src/config/serverSettings.ts` contain no `HAPI_RELAY_`, `relayApi`, `relayAuth`, `relayForceTcp`, or relay/tunnel disabled status fields.
    - `hub/src/config/serverSettings.ts` still reads `HAPI_PUBLIC_URL` and records `sources.publicUrl = 'env'` or an equivalent source marker.
    - Phase 10-owned keys such as `serverUrl`, `webapp*`, runtime SQLite migrations, and config singleton mutability are not refactored by this task unless typecheck exposes a direct relay dependency.
    - `bun typecheck && bun run test` exits 0.
  </acceptance_criteria>
  <done>Hub config accepts the current Tailscale public URL path and no longer exposes relay env/settings semantics.</done>
</task>

<task type="auto">
  <name>Task 04-02-02: Add explicit rejection tests for legacy relay settings if represented</name>
  <read_first>
    - `.planning/phases/04-cut-deployment-infrastructure/04-CONTEXT.md` decisions D-53 and D-56
    - `.planning/phases/04-cut-deployment-infrastructure/04-VALIDATION.md` Wave 0 Requirements
    - `.planning/phases/04-cut-deployment-infrastructure/04-PATTERNS.md` section `hub/src/config/serverSettings.test.ts`
    - `hub/src/config/serverSettings.ts`
    - `hub/src/config/serverSettings.test.ts`
  </read_first>
  <files>hub/src/config/serverSettings.ts, hub/src/config/serverSettings.test.ts</files>
  <action>If implementation finds legacy relay settings keys represented in the settings schema or historical settings rejection list, add those exact keys to the existing old-field rejection mechanism and test them in `hub/src/config/serverSettings.test.ts`. Candidate keys from the phase artifacts are `relayApi`, `relayAuth`, `relayForceTcp`, `relayEnabled`, and any persisted settings equivalents of `HAPI_RELAY_API`, `HAPI_RELAY_AUTH`, or `HAPI_RELAY_FORCE_TCP` discovered in source. If no relay settings keys are represented anywhere in config settings source, add a focused test or assertion proving `HAPI_RELAY_*` env vars do not affect `loadServerSettings()` output and note that no persisted relay keys existed to reject. Do not silently strip unknown fields or add compatibility passthrough.</action>
  <verify>
    <automated>cd hub && bun test src/config/serverSettings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd hub && bun test src/config/serverSettings.test.ts` exits 0.
    - If persisted legacy relay keys are present in source, a settings file containing those keys throws an explicit error naming the unsupported fields.
    - If no persisted legacy relay keys are present in source, a test proves `HAPI_RELAY_API`, `HAPI_RELAY_AUTH`, and `HAPI_RELAY_FORCE_TCP` do not appear in the returned settings/config result.
    - `serverSettings.test.ts` retains existing old `webapp*` rejection coverage; Phase 10-owned aliases are not removed here.
  </acceptance_criteria>
  <done>Config validation has concrete automated coverage for the relay-settings convergence required by D-53/D-56.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment -> Hub config | Process env values enter runtime configuration. |
| Settings file -> Hub config | User-editable JSON under `~/.hapi/settings.json` enters runtime configuration. |
| Hub config -> startup behavior | Loaded config controls listen/public URL behavior and startup display. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Security Misconfiguration | `hub/src/config/serverSettings.ts` | mitigate | Remove `HAPI_RELAY_*` reads and stale relay settings; add explicit rejection/no-effect tests per D-53/D-56. |
| T-04-05 | Information Disclosure | `HAPI_OFFICIAL_WEB_URL` relay semantics | mitigate | Remove hosted relay URL semantics while preserving only `HAPI_PUBLIC_URL` for Tailscale per D-54. |
| T-04-06 | Tampering | settings JSON old relay fields | mitigate | Fail explicitly if represented legacy relay keys are found; no `.passthrough()`, silent ignore, or migration shim. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package install is planned in this slice. |

## ASVS L1 Concern Mapping

| ASVS Category | Applicability | Plan Control |
|---------------|---------------|--------------|
| V2 Authentication | preserve | Do not modify `CLI_API_TOKEN`, JWT secret, or auth env/settings behavior while removing relay config. |
| V3 Session Management | preserve | Keep session and SSE/socket configuration behavior unchanged; relay settings must not alter session state. |
| V4 Access Control | preserve | Leave existing auth middleware and access checks untouched while deleting relay-only configuration. |
| V5 Input Validation | mitigate | Explicitly reject represented legacy relay settings or prove `HAPI_RELAY_*` env vars have no effect; no passthrough or silent ignore. |
| V6 Cryptography | preserve | Keep JWT/VAPID secret generation and storage unchanged; only remove relay/hosted-web URL config semantics. |
| V9 Communications | mitigate | Preserve `HAPI_PUBLIC_URL` for user-managed Tailscale while removing hosted relay communication settings. |
</threat_model>

<verification>
Plan-level gate: `bun typecheck && bun run test`.

Focused config gate: `cd hub && bun test src/config/serverSettings.test.ts`.
</verification>

<success_criteria>
- `HAPI_RELAY_*` and relay settings are gone from hub configuration source.
- `HAPI_PUBLIC_URL` remains load-bearing and tested.
- Old relay config is not silently accepted when represented in settings validation.
</success_criteria>

<output>
Create `.planning/phases/04-cut-deployment-infrastructure/04-02-SUMMARY.md` when done.
</output>
