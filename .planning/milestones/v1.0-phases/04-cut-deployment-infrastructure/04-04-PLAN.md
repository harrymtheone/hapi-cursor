---
phase: 04-cut-deployment-infrastructure
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - package.json
  - hub/package.json
  - bun.lock
  - hub/scripts/download-tunwg.ts
  - hub/tools/tunwg/
  - cli/src/runtime/assets.ts
  - cli/src/runtime/embeddedAssets.bun.ts
  - cli/src/types/assetImports.d.ts
  - scripts/check-no-cut-agents.sh
autonomous: true
requirements:
  - CUT-10
  - CUT-11
user_setup: []
must_haves:
  truths:
    - "bun run build:single-exe succeeds without downloading, extracting, chmodding, or embedding tunwg."
    - "qrcode and @types/qrcode are removed after QR runtime deletion."
    - "The existing runtime asset pipeline still handles ripgrep and difftastic."
    - "Phase 04 forbidden keywords are blocked by the guard and explicit scans."
    - "The full phase gate passes: bun typecheck, bun run test, bun run build:single-exe, and keyword scans."
  artifacts:
    - path: "package.json"
      provides: "Single-exe scripts without download:tunwg"
    - path: "hub/package.json"
      provides: "Hub dependencies without qrcode or @types/qrcode"
    - path: "bun.lock"
      provides: "Lockfile aligned with package removals"
    - path: "hub/scripts/download-tunwg.ts"
      provides: "Deleted tunwg download script"
      state: "absent"
    - path: "hub/tools/tunwg/"
      provides: "Deleted tracked tunwg runtime asset directory"
      state: "absent"
    - path: "cli/src/runtime/assets.ts"
      provides: "Compiled runtime assets without tunwg readiness/path/chmod helpers"
    - path: "cli/src/runtime/embeddedAssets.bun.ts"
      provides: "Embedded asset manifest containing only non-tunwg runtime tools"
    - path: "scripts/check-no-cut-agents.sh"
      provides: "Fail-closed guard for Phase 04 keywords"
  key_links:
    - from: "package.json"
      to: "hub/scripts/download-tunwg.ts"
      via: "removed build script dependency"
      pattern: "download:tunwg"
    - from: "cli/src/runtime/embeddedAssets.bun.ts"
      to: "cli/src/runtime/assets.ts"
      via: "loadEmbeddedAssets and ensureRuntimeAssets"
      pattern: "loadEmbeddedAssets"
    - from: "package.json"
      to: "scripts/check-no-cut-agents.sh"
      via: "test:guard"
      pattern: "test:guard"
---

<objective>
Remove tunwg build/runtime assets, remove QR-only package dependencies, and install the final Phase 04 guard and full validation gate.

Purpose: CUT-10/CUT-11 require the single-exe path and guard suite to prove no bundled relay binary, relay env, hosted web, or dangerous remote-log channel remains.
Output: Build scripts and runtime assets are tunwg-free, lockfile is clean, guard scans are extended, and the phase gate is executable.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cut-deployment-infrastructure/04-CONTEXT.md
@.planning/phases/04-cut-deployment-infrastructure/04-RESEARCH.md
@.planning/phases/04-cut-deployment-infrastructure/04-VALIDATION.md
@.planning/phases/04-cut-deployment-infrastructure/04-PATTERNS.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-07-SUMMARY.md
@package.json
@hub/package.json
@cli/src/runtime/assets.ts
@cli/src/runtime/embeddedAssets.bun.ts
@cli/src/types/assetImports.d.ts
@scripts/check-no-cut-agents.sh

<interfaces>
From `04-PATTERNS.md`: preserve `cli/src/runtime/assets.ts` archive handling for `difftastic-${platformDir}.tar.gz` and `ripgrep-${platformDir}.tar.gz`; delete only `isTunwgReady`, `ensureTunwgExecutable`, `getTunwgPath`, and tunwg readiness dependencies.

From `03-07-SUMMARY.md`: keep the guard fail-closed `rg` resolution pattern and add a narrow phase-specific scan instead of weakening earlier phase checks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 04-04-01: Remove tunwg build scripts, runtime assets, and QR packages</name>
  <read_first>
    - `.planning/phases/04-cut-deployment-infrastructure/04-CONTEXT.md` decisions D-51, D-61, D-62, D-63, D-64
    - `.planning/phases/04-cut-deployment-infrastructure/04-RESEARCH.md` sections `Standard Stack`, `Runtime State Inventory`, and `Common Pitfalls`
    - `.planning/phases/04-cut-deployment-infrastructure/04-PATTERNS.md` sections `Runtime Asset Boundary` and `package.json`
    - `package.json`
    - `hub/package.json`
    - `hub/scripts/download-tunwg.ts`
    - `hub/tools/tunwg/`
    - `cli/src/runtime/assets.ts`
    - `cli/src/runtime/embeddedAssets.bun.ts`
    - `cli/src/types/assetImports.d.ts`
  </read_first>
  <files>package.json, hub/package.json, bun.lock, hub/scripts/download-tunwg.ts, hub/tools/tunwg/, cli/src/runtime/assets.ts, cli/src/runtime/embeddedAssets.bun.ts, cli/src/types/assetImports.d.ts</files>
  <action>Delete `hub/scripts/download-tunwg.ts` and `hub/tools/tunwg/` per D-49/D-61/D-63. Remove root `download:tunwg` scripts and remove `download:tunwg` from `build:single-exe` and `build:single-exe:all` so those scripts only build web, generate embedded web assets, and compile CLI/hub per D-61. In `cli/src/runtime/assets.ts`, remove tunwg readiness/path/chmod helpers and keep ripgrep/difftastic archive extraction unchanged per D-62. In `cli/src/runtime/embeddedAssets.bun.ts` and `cli/src/types/assetImports.d.ts`, remove tunwg imports/asset declarations while preserving archive/license declarations for remaining tools. Remove `qrcode` and `@types/qrcode` from `hub/package.json` and regenerate `bun.lock` using Bun because Plan 01 removed QR runtime usage per D-51. Do not refactor npm/Homebrew release flow unless a file directly downloads, caches, checksums, or embeds tunwg per D-63.</action>
  <verify>
    <automated>bun typecheck && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f hub/scripts/download-tunwg.ts` and `test ! -d hub/tools/tunwg` succeed.
    - Root `package.json` contains no `download:tunwg`, and `build:single-exe` / `build:single-exe:all` do not mention tunwg.
    - `hub/package.json` and `bun.lock` contain no `qrcode` or `@types/qrcode` package entries after lockfile regeneration.
    - `cli/src/runtime/assets.ts`, `cli/src/runtime/embeddedAssets.bun.ts`, and `cli/src/types/assetImports.d.ts` contain no tunwg runtime asset references while preserving ripgrep and difftastic handling.
    - `bun typecheck && bun run test` exits 0.
  </acceptance_criteria>
  <done>Single-exe build inputs and compiled runtime asset code no longer depend on tunwg or QR-only dependencies.</done>
</task>

<task type="auto">
  <name>Task 04-04-02: Extend fail-closed guard for Phase 04 keywords</name>
  <read_first>
    - `.planning/phases/04-cut-deployment-infrastructure/04-CONTEXT.md` decisions D-65, D-66, D-67, D-68
    - `.planning/phases/04-cut-deployment-infrastructure/04-VALIDATION.md` Wave 0 Requirements
    - `.planning/phases/04-cut-deployment-infrastructure/04-PATTERNS.md` section `scripts/check-no-cut-agents.sh`
    - `.planning/phases/03-cut-multi-user-namespace-isolation/03-07-SUMMARY.md`
    - `scripts/check-no-cut-agents.sh`
  </read_first>
  <files>scripts/check-no-cut-agents.sh</files>
  <action>Extend `scripts/check-no-cut-agents.sh` with a separate Phase 04 scan for hard zero-tolerance patterns `tunwg`, `HAPI_RELAY_`, and `DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING` per D-65. Add plan-level sweep patterns for `relay-mode`, `relayMode`, `officialWebUrl`, `app.hapi.run`, `download-tunwg`, `--relay`, and `--no-relay` so deleted hosted-web and CLI relay surfaces do not linger. Preserve existing Phase 1/2 and Phase 3 checks and the fail-closed `rg` resolution from Phase 03. Keep the Phase 04 whitelist tight: default allowed history is `.planning/codebase/` and `CHANGELOG.md`; explicitly justify only `scripts/check-no-cut-agents.sh` because the guard must contain forbidden patterns, and `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, plus `.planning/phases/04-cut-deployment-infrastructure/` because GSD source-of-truth artifacts necessarily name the features being removed. Do not whitelist `docs/`, `website/`, `README.md`, package source directories, or broad `.planning/` beyond these named artifacts.</action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-no-cut-agents.sh` exits 0 after Tasks 04-01 through 04-04 cleanup are complete.
    - The guard still fails closed if neither PATH `rg` nor Cursor bundled ripgrep is available.
    - Phase 04 guard output includes a success line for no deployment-infrastructure / remote-log residue.
    - The only Phase 04 whitelist entries beyond `.planning/codebase/` and `CHANGELOG.md` are explicitly named in the script comments and match the justification in this task action.
    - No `docs/**`, `website/**`, root README, or runtime source directory is whitelisted for Phase 04 patterns.
  </acceptance_criteria>
  <done>Phase 04 has an automated, fail-closed keyword guard that preserves prior phase checks and blocks regression.</done>
</task>

<task type="auto">
  <name>Task 04-04-03: Run final build and explicit keyword gate</name>
  <read_first>
    - `.planning/phases/04-cut-deployment-infrastructure/04-CONTEXT.md` decisions D-64, D-65, D-66, D-68
    - `.planning/phases/04-cut-deployment-infrastructure/04-VALIDATION.md` full suite and manual-only verification sections
    - `package.json`
    - `scripts/check-no-cut-agents.sh`
  </read_first>
  <files>package.json, scripts/check-no-cut-agents.sh</files>
  <action>Run the final phase gate from the repo root: `bun typecheck`, `bun run test`, `bun run build:single-exe`, and `bash scripts/check-no-cut-agents.sh` per D-64/D-68. Capture whether build output mentions tunwg download, extraction, chmod, or missing `hub/tools/tunwg`; any such output is a blocker and must be fixed in Task 04-04-01 code paths. Run explicit ripgrep scans using the same `RG_BIN` resolution as the guard for hard patterns `tunwg|HAPI_RELAY_|DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING` and plan-level sweep patterns `relay-mode|relayMode|officialWebUrl|app.hapi.run|download-tunwg|--relay|--no-relay`, with only the justified planning/guard/history exclusions from Task 04-04-02. If local stale `dist-exe/` or `~/.hapi/runtime` artifacts cause false positives, delete only the stale generated artifact and rerun; do not whitelist stale build output.</action>
  <verify>
    <automated>bun typecheck && bun run test && bun run build:single-exe && bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bun typecheck` exits 0.
    - `bun run test` exits 0 and includes `test:guard`.
    - `bun run build:single-exe` exits 0 and its output does not run or mention `download:tunwg`, `download-tunwg`, `hub/tools/tunwg`, tunwg extraction, or tunwg chmod.
    - Explicit ripgrep scan for `tunwg|HAPI_RELAY_|DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING` returns no matches outside the Task 04-04-02 justified exclusions.
    - Explicit ripgrep scan for `relay-mode|relayMode|officialWebUrl|app.hapi.run|download-tunwg|--relay|--no-relay` returns no runtime source/build-script matches outside the Task 04-04-02 justified exclusions.
  </acceptance_criteria>
  <done>Phase 04 closes with green typecheck, tests, single-exe build, guard, and explicit keyword scans.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Hub no longer ships a built-in WireGuard/TLS tunnel binary or upstream remote-log upload channel | 04-01, 04-03, 04-04 | COVERED | Runtime tunnel, remote logger, build assets, and guard are covered. |
| REQ | CUT-10 | Delete tunnel binary/TLS gate, `HAPI_RELAY_*`, relay-mode web code | 04-01, 04-02, 04-04 | COVERED | Runtime, config, build/runtime assets, and guard split by file ownership. |
| REQ | CUT-11 | Delete dangerous remote-log upload stream and doctor toggle | 04-03, 04-04 | COVERED | Logger/doctor cleanup plus guard hard pattern. |
| RESEARCH | - | Preserve neutral `HAPI_PUBLIC_URL` / local URL output | 04-01, 04-02 | COVERED | D-50/D-54 actions preserve public URL. |
| RESEARCH | - | Preserve rg/difftastic runtime asset mechanism | 04-04 | COVERED | D-62 action keeps non-tunwg assets. |
| RESEARCH | - | Extend existing guard instead of adding custom scanner | 04-04 | COVERED | Uses `scripts/check-no-cut-agents.sh`. |
| RESEARCH | - | Add logger and doctor Wave 0 tests | 04-03 | COVERED | `cli/src/ui/logger.test.ts` and `doctor.test.ts`. |
| RESEARCH | - | Add serverSettings legacy relay test if represented | 04-02 | COVERED | Conditional exact behavior planned. |
| CONTEXT | D-49 | Physically delete relay/tunwg/TLS gate, no flags/stubs | 04-01, 04-04 | COVERED | `hub/src/tunnel/`, `hub/tools/tunwg/`, script/assets deleted. |
| CONTEXT | D-50 | Preserve `HAPI_PUBLIC_URL` / local URL output | 04-01, 04-02 | COVERED | Startup/config actions require it. |
| CONTEXT | D-51 | Delete tunnel QR rendering and `qrcode` dependency | 04-01, 04-04 | COVERED | Runtime QR removal plus package removal. |
| CONTEXT | D-52 | Delete relay-mode web entry | 04-01 | COVERED | `hub/src/web/server.ts` and `web/src/lib/relay-mode*`. |
| CONTEXT | D-53 | Delete `HAPI_RELAY_*` and relay settings; no passthrough | 04-02, 04-04 | COVERED | Config source plus guard hard scan. |
| CONTEXT | D-54 | Keep `HAPI_PUBLIC_URL`; remove hosted-web semantics | 04-02 | COVERED | Config action handles official hosted web URL. |
| CONTEXT | D-55 | No relay/tunnel disabled startup summary | 04-01, 04-02 | COVERED | Startup/config summary removed. |
| CONTEXT | D-56 | Update relay field tests/schema inputs this phase | 04-02 | COVERED | `serverSettings.test.ts` conditional coverage. |
| CONTEXT | D-57 | Logger deletes remote upload path, keeps local logs | 04-03 | COVERED | Logger task and test. |
| CONTEXT | D-58 | Doctor deletes remote-log toggle prompt | 04-03 | COVERED | Doctor task and test. |
| CONTEXT | D-59 | Do not blindly delete legitimate `HAPI_API_URL` uses | 04-03 | COVERED | Logger/doctor tasks explicitly preserve direct-connect use. |
| CONTEXT | D-60 | Do not add replacement upload/export feature | 04-03 | COVERED | Logger/doctor actions prohibit replacement features. |
| CONTEXT | D-61 | `build:single-exe` no longer downloads/packages tunwg | 04-04 | COVERED | Package script and final build gate. |
| CONTEXT | D-62 | Remove only tunwg runtime asset surface | 04-04 | COVERED | rg/difftastic preserved. |
| CONTEXT | D-63 | Clean tunwg-specific release/CI/asset surface only | 04-04 | COVERED | Release edits limited to direct tunwg references. |
| CONTEXT | D-64 | `bun run build:single-exe` hard gate | 04-04 | COVERED | Final task includes build gate. |
| CONTEXT | D-65 | Zero tolerance keywords and relay-mode sweeps | 04-04 | COVERED | Guard hard patterns and sweep patterns. |
| CONTEXT | D-66 | Tight whitelist, explicit extra justifications | 04-04 | COVERED | Guard task lists allowed exceptions and prohibits docs/website source whitelists. |
| CONTEXT | D-67 | Clean source comments/JSDoc; defer long-form docs | 04-01, 04-02, 04-03, 04-04 | COVERED | Each action cleans touched source comments; docs/website not edited. |
| CONTEXT | D-68 | Four execution slices with final build+guard | 04-01, 04-02, 04-03, 04-04 | COVERED | Four-plan wave structure matches decision. |

Deferred ideas from `04-CONTEXT.md` are intentionally not planned: Phase 10 generic config cleanup, Phase 12 long-form README/docs/website prose cleanup, alternative tunnel/log export/deployment helper features.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build scripts -> network/filesystem | Single-exe build previously downloaded and embedded a tunnel binary. |
| Compiled CLI runtime -> embedded assets | Compiled runtime extracts bundled tools under `~/.hapi/runtime`. |
| Guard script -> repository source | Shell scanner decides whether forbidden deleted-feature residue remains. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering / Supply Chain | `hub/scripts/download-tunwg.ts`, `package.json` | mitigate | Delete tunwg download script and remove build script dependency per D-61/D-63. |
| T-04-11 | Information Disclosure | `cli/src/runtime/embeddedAssets.bun.ts`, compiled assets | mitigate | Remove tunwg embedded assets while preserving rg/difftastic per D-62. |
| T-04-12 | Security Misconfiguration | `scripts/check-no-cut-agents.sh` | mitigate | Add fail-closed Phase 04 guard for relay/tunwg/remote-log residue with explicit tight whitelist per D-65/D-66. |
| T-04-13 | Information Disclosure | startup QR dependency `qrcode` | mitigate | Remove QR-only package dependencies after Plan 01 deletes QR runtime per D-51. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are installed; package-manager operation only removes `qrcode` and `@types/qrcode` and regenerates `bun.lock`. |

## ASVS L1 Concern Mapping

| ASVS Category | Applicability | Plan Control |
|---------------|---------------|--------------|
| V2 Authentication | preserve | Guard/build cleanup must not remove or rewrite auth packages, JWT handling, or `CLI_API_TOKEN` paths. |
| V3 Session Management | preserve | Runtime asset and build-script cleanup must not alter session/socket/SSE packages or generated web asset serving. |
| V4 Access Control | preserve | Keep access-control code out of the keyword whitelist; any runtime source match remains a guard failure. |
| V5 Input Validation | mitigate | Fail-closed keyword guard validates repository state against removed relay/remote-log inputs and stale hosted-web symbols. |
| V6 Cryptography | mitigate by deletion | Remove tunwg download/embed/extract path instead of carrying bundled relay cryptographic assets forward. |
| V9 Communications | mitigate | Final build and scans prove no bundled relay communication path or remote-log network channel remains. |
</threat_model>

<verification>
Focused gates:
- `bun typecheck && bun run test`
- `bash scripts/check-no-cut-agents.sh`
- `bun run build:single-exe`

Phase gate:
- `bun typecheck && bun run test && bun run build:single-exe && bash scripts/check-no-cut-agents.sh`
- Explicit ripgrep hard-pattern scan for `tunwg|HAPI_RELAY_|DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING` with only justified planning/guard/history exclusions.
- Explicit ripgrep sweep for `relay-mode|relayMode|officialWebUrl|app.hapi.run|download-tunwg|--relay|--no-relay` with only justified planning/guard/history exclusions.
</verification>

<success_criteria>
- `bun run build:single-exe` succeeds without tunwg download or extraction.
- `hub/scripts/download-tunwg.ts` and `hub/tools/tunwg/` are absent.
- Runtime assets still support ripgrep and difftastic.
- The guard and explicit scans enforce Phase 04 zero-tolerance residues.
- Full phase validation passes.
</success_criteria>

<output>
Create `.planning/phases/04-cut-deployment-infrastructure/04-04-SUMMARY.md` when done.
</output>
