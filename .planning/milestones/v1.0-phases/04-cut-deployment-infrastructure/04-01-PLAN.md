---
phase: 04-cut-deployment-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hub/src/index.ts
  - hub/src/web/server.ts
  - hub/src/tunnel/
  - web/src/lib/relay-mode*
autonomous: true
requirements:
  - CUT-10
user_setup: []
must_haves:
  truths:
    - "Hub startup cannot start, wait for, log, or stop a built-in relay tunnel."
    - "Hub startup still prints local and HAPI_PUBLIC_URL text for Tailscale users."
    - "The web server always serves the hub-local embedded/static PWA rather than redirecting to a hosted relay web entry."
    - "Source comments and JSDoc touched in hub/web runtime files contain no relay/tunnel feature ghosts."
  artifacts:
    - path: "hub/src/tunnel/"
      provides: "Deleted tunnel implementation directory"
      state: "absent"
    - path: "hub/src/index.ts"
      provides: "Hub startup without TunnelManager, TLS gate, relay args, direct-access QR, or token-bearing hosted URL"
    - path: "hub/src/web/server.ts"
      provides: "Hono app that serves local embedded/static assets without relayMode or officialWebUrl options"
    - path: "web/src/lib/relay-mode*"
      provides: "Deleted hosted relay-mode web helpers"
      state: "absent"
  key_links:
    - from: "hub/src/index.ts"
      to: "hub/src/web/server.ts"
      via: "startWebServer options"
      pattern: "startWebServer\\("
    - from: "hub/src/index.ts"
      to: "HAPI_PUBLIC_URL"
      via: "configuration publicUrl output"
      pattern: "config\\.publicUrl"
---

<objective>
Remove the runtime tunnel and hosted relay-web startup path while preserving the neutral local/public URL path needed for user-managed Tailscale access.

Purpose: CUT-10 requires physical removal of the built-in WireGuard/TLS relay and web relay entry without feature flags, disabled banners, or no-op stubs.
Output: Hub and web runtime source no longer import, instantiate, or expose relay/tunnel behavior; local/public URL output remains.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cut-deployment-infrastructure/04-CONTEXT.md
@.planning/phases/04-cut-deployment-infrastructure/04-RESEARCH.md
@.planning/phases/04-cut-deployment-infrastructure/04-VALIDATION.md
@.planning/phases/04-cut-deployment-infrastructure/04-PATTERNS.md
@hub/src/index.ts
@hub/src/web/server.ts
@hub/src/tunnel/
@web/src/lib/

<interfaces>
From `04-PATTERNS.md`: preserve `hub/src/index.ts` local URL output through `config.listenPort` and `config.publicUrl`; delete `resolveRelayFlag`, `tunnelManager`, relay CORS merge, `TunnelManager.start()`, `waitForTunnelTlsReady`, QR direct-access generation, and `tunnelManager?.stop()`.

From `04-PATTERNS.md`: preserve `hub/src/web/server.ts` route mounting, CORS, embedded asset serving, and dist fallback; delete `relayMode`, `officialWebUrl`, and the hosted-web root response.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 04-01-01: Delete hub tunnel startup and QR runtime path</name>
  <read_first>
    - `.planning/phases/04-cut-deployment-infrastructure/04-CONTEXT.md` decisions D-49, D-50, D-51, D-55, D-65, D-67
    - `.planning/phases/04-cut-deployment-infrastructure/04-PATTERNS.md` sections `hub/src/index.ts` and `Deletion targets`
    - `hub/src/index.ts`
    - `hub/src/tunnel/`
  </read_first>
  <files>hub/src/index.ts, hub/src/tunnel/</files>
  <action>Delete `hub/src/tunnel/` outright per D-49. In `hub/src/index.ts`, remove imports and runtime code for `TunnelManager`, `waitForTunnelTlsReady`, `resolveRelayFlag`, relay API/auth/forceTcp env reads, relay CORS additions, tunnel start/stop lifecycle, tokenized direct-access URL construction, QR rendering, and any `[Tunnel]` startup/status output per D-49/D-51/D-55. Preserve local hub startup order, `config.listenPort`, `config.publicUrl`, `HAPI_PUBLIC_URL` semantics, existing Socket.IO/SSE/store/notification setup, and plain text URL output per D-50. Do not add `--relay` compatibility errors, feature flags, no-op stubs, or a "relay disabled" banner.</action>
  <verify>
    <automated>bun typecheck && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d hub/src/tunnel` succeeds.
    - `hub/src/index.ts` has no `TunnelManager`, `waitForTunnelTlsReady`, `resolveRelayFlag`, `HAPI_RELAY_`, `HAPI_OFFICIAL_WEB_URL`, `QRCode`, `directAccessUrl`, `--relay`, or `--no-relay` runtime source references.
    - `hub/src/index.ts` still prints local URL text using `config.listenPort` and public URL text using `config.publicUrl` or equivalent neutral naming.
    - `bun typecheck && bun run test` exits 0.
  </acceptance_criteria>
  <done>Hub startup has no built-in tunnel lifecycle or token-bearing QR/direct URL path, and Tailscale users still receive a usable local/public URL.</done>
</task>

<task type="auto">
  <name>Task 04-01-02: Remove hosted relay web server branch and web relay helpers</name>
  <read_first>
    - `.planning/phases/04-cut-deployment-infrastructure/04-CONTEXT.md` decisions D-49, D-52, D-65, D-67
    - `.planning/phases/04-cut-deployment-infrastructure/04-PATTERNS.md` sections `hub/src/web/server.ts` and `web/src/lib/relay-mode*`
    - `hub/src/web/server.ts`
    - `web/src/lib/`
  </read_first>
  <files>hub/src/web/server.ts, web/src/lib/relay-mode*</files>
  <action>In `hub/src/web/server.ts`, remove `relayMode` and `officialWebUrl` from `WebServerOptions`, `createWebApp`, `startWebServer`, and all hosted-web root response logic per D-52. Keep health, auth/API route mounting, CORS, embedded asset serving, and dist fallback behavior unchanged. Delete any `web/src/lib/relay-mode*` files and update their import/call sites if present so the Web PWA uses current origin or explicit hub URL only. Clean touched inline comments/JSDoc that still describe relay/tunnel behavior per D-67.</action>
  <verify>
    <automated>bun typecheck && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `hub/src/web/server.ts` has no `relayMode`, `officialWebUrl`, `app.hapi.run`, hosted-web relay HTML, or early relay return branch.
    - `web/src/lib/relay-mode*` files are absent, or `find web/src/lib -maxdepth 1 -name 'relay-mode*'` returns no files if `find` is used manually outside the agent.
    - `hub/src/web/server.ts` still serves embedded assets and fallback `web/dist` assets.
    - `bun typecheck && bun run test` exits 0.
  </acceptance_criteria>
  <done>Hub web serving is local/embedded-only and the web codebase no longer contains a relay-mode entry helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Hub web server | Untrusted browser requests enter Hono routes and static serving. |
| Hub startup -> external network | Removed boundary; hub no longer opens a bundled relay tunnel to an upstream service. |
| Startup logs -> user terminal | Startup output may expose URLs or tokens if direct access strings are built incorrectly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure / Spoofing | `hub/src/index.ts`, `hub/src/tunnel/` | mitigate | Delete `TunnelManager`, TLS gate, relay env reads, tokenized direct-access URL construction, and QR output per D-49/D-51. |
| T-04-02 | Information Disclosure | `hub/src/web/server.ts` hosted relay branch | mitigate | Delete `relayMode` and `officialWebUrl` branch so the web server cannot route users to `app.hapi.run` relay mode per D-52. |
| T-04-03 | Information Disclosure | startup URL output | mitigate | Preserve only neutral local/public URL text; do not include `config.cliApiToken` in any startup URL or QR path. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package install is planned in this slice; qrcode removal happens in Plan 04. |

## ASVS L1 Concern Mapping

| ASVS Category | Applicability | Plan Control |
|---------------|---------------|--------------|
| V2 Authentication | preserve | Keep existing `CLI_API_TOKEN` / JWT auth behavior unchanged while deleting token-bearing relay direct-access URL generation. |
| V3 Session Management | preserve | Keep existing Hono, Socket.IO, SSE, and session startup wiring intact; only remove tunnel lifecycle and hosted relay-web branch. |
| V4 Access Control | preserve | Do not weaken existing auth middleware or socket checks while removing relay startup paths. |
| V5 Input Validation | not expanded | No new request/config input is added; relay CLI arg parser is deleted rather than replaced with a new parsing surface. |
| V6 Cryptography | mitigate by deletion | Remove bundled WireGuard/TLS relay code instead of introducing replacement cryptographic handling. |
| V9 Communications | mitigate | Remove bundled public relay/hosted-web communications path and keep user-managed local/Tailscale URL output only. |
</threat_model>

<verification>
Plan-level gate: `bun typecheck && bun run test`.

Additional executor checks:
- `test ! -d hub/src/tunnel`
- Runtime source has no `TunnelManager`, `waitForTunnelTlsReady`, `relayMode`, `officialWebUrl`, `app.hapi.run`, `--relay`, or `--no-relay` in `hub/src/index.ts hub/src/web/server.ts web/src/lib`.
</verification>

<success_criteria>
- CUT-10 runtime tunnel and hosted relay-web entry are physically removed.
- `HAPI_PUBLIC_URL` and local URL output remain available for Tailscale use.
- No source comments/JSDoc touched by this plan preserve relay/tunnel feature descriptions.
</success_criteria>

<output>
Create `.planning/phases/04-cut-deployment-infrastructure/04-01-SUMMARY.md` when done.
</output>
