---
phase: 01-cut-non-cursor-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/src/claude/
  - cli/src/commands/claude.ts
  - cli/src/commands/hookForwarder.ts
  - cli/src/agent/runners/runAgentSession.ts
  - cli/src/agent/runners/runAgentSession.test.ts
  - cli/src/agent/sessionFactory.ts
  - cli/src/agent/sessionFactory.test.ts
  - cli/src/agent/utils.ts
  - cli/src/agent/utils.test.ts
  - cli/src/api/apiMachine.ts
  - cli/src/api/apiMachine.test.ts
  - cli/src/api/types.ts
  - cli/src/commands/resume.ts
  - cli/src/commands/resume.test.ts
  - cli/src/runner/run.ts
  - cli/src/runner/buildCliArgs.test.ts
  - hub/src/sync/syncEngine.ts
  - hub/src/sync/sessionCache.ts
  - hub/src/sync/sessionModel.test.ts
  - hub/src/sync/teams.ts
  - hub/src/web/routes/sessions.ts
  - hub/src/web/routes/sessions.test.ts
  - hub/src/web/routes/permissions.ts
  - hub/src/web/routes/machines.ts
  - hub/package.json
  - shared/src/schemas.ts
  - shared/src/sessionSummary.ts
  - shared/src/messages.ts
  - shared/src/models.ts
  - shared/src/models.test.ts
  - shared/src/types.ts
  - shared/src/resume.test.ts
  - web/src/lib/sessionModelLabel.ts
  - web/src/lib/sessionModelLabel.test.ts
  - web/src/lib/assistant-runtime.test.ts
  - scripts/check-no-cut-agents.sh
  - package.json
  - .planning/phases/01-cut-non-cursor-agents/01-WAVE0-FINDINGS.md
autonomous: true
requirements: [CUT-01]
user_setup: []

must_haves:
  truths:
    - "Phase 1 SC#1: bun typecheck and bun run test both pass after CUT-01 commit"
    - "After CUT-01 commit: `bash scripts/check-no-cut-agents.sh` exits 0 — the broad-then-narrow whitelist has had its CUT-01 TEMP entries removed, so any leaked `claude` literal would surface here. Final tightening lands in 01-05."
    - "Phase 1 SC#4: cli/src/commands/hookForwarder.ts is removed"
    - "Phase 1 SC#5: package.json files declare no @anthropic-ai/* dependency (vacuously verified)"
    - "Phase 1 cross-commit ordering: runAgentSession.ts dies in CUT-01 (it imports from cli/src/claude/)"
  artifacts:
    - path: "scripts/check-no-cut-agents.sh"
      provides: "Ripgrep guard script with full whitelist (will be tightened in 01-05)"
      contains: "PATTERN='\\\\b(claude|codex|gemini|opencode)\\\\b'"
    - path: "cli/src/claude/"
      provides: "Must NOT exist"
      absent: true
    - path: "cli/src/commands/claude.ts"
      provides: "Must NOT exist"
      absent: true
    - path: "cli/src/commands/hookForwarder.ts"
      provides: "Must NOT exist"
      absent: true
    - path: "cli/src/agent/runners/runAgentSession.ts"
      provides: "Must NOT exist (dead after CUT-01 per RESEARCH §'5-commit precondition order' #1)"
      absent: true
  key_links:
    - from: "cli/src/agent/sessionFactory.ts"
      to: "shared/src/schemas.ts"
      via: "cursorSessionId only (claudeSessionId field removed)"
      pattern: "cursorSessionId"
---

<objective>
Execute CUT-01: physically remove the Claude Code agent (vendored SDK + all consumers + tests). Also remove `cli/src/agent/runners/runAgentSession.ts` because it imports from `cli/src/claude/` and would break typecheck if left for CUT-03 (see RESEARCH.md §"5-commit precondition order" row #1 + §"Pitfall 1").

Wave 0 tasks (assumption verification A1/A2/A4 + Q3 inspection + ripgrep guard scaffold) run FIRST — they gate every subsequent deletion decision. Then the Claude deletion lands.

Purpose: First atomic commit in the 5-commit D-14 sequence; must pass `bun typecheck && bun run test` per D-15.
Output: `feat(phase-01): CUT-01 remove Claude Code agent` commit; Claude code & tests gone; consumer branches rewritten Cursor-only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md
@.planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md
@.planning/phases/01-cut-non-cursor-agents/01-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 — verify assumptions A1/A2/A4 + Q3; scaffold ripgrep guard</name>
  <files>scripts/check-no-cut-agents.sh, package.json, .planning/phases/01-cut-non-cursor-agents/01-WAVE0-FINDINGS.md</files>
  <read_first>
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"ripgrep guard script" (full whitelist + bash template, lines ~404-476)
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"Assumptions Log" (A1-A7) + §"Open Questions" (Q1-Q3)
    - .planning/phases/01-cut-non-cursor-agents/01-VALIDATION.md §"Wave 0 Requirements"
    - .planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md §"Implementation Decisions" (D-12, D-13)
    - cli/src/agent/loopBase.ts
    - cli/src/agent/sessionBase.ts
    - cli/scripts/build-executable.ts
    - cli/src/runtime/embeddedAssets.bun.ts
    - shared/src/resume.ts
    - package.json (root)
  </read_first>
  <action>
    Perform these inspections and record findings in `01-WAVE0-FINDINGS.md` (this file is a transient phase artifact, not user-facing docs — D-07 forbids new user docs, not in-phase findings ledgers).

    **W0.0 — HEAD inventory (FIRST sub-step, runs before everything else):**
    Goal: produce the source-of-truth ownership ledger that drives the scaffold whitelist (later sub-step) and every per-CUT "Rewrite multi-flavor consumers" task. This step is a measurement, not a decision — the table contents drive subsequent ownership.

    - Run a ripgrep inventory of every flavor-literal hit at HEAD, excluding the four runtime dirs (they are pure deletion and never need a TEMP-WIDE entry), the permanent whitelist roots, and infra noise:

        ```bash
        rg -l --type ts --type tsx --type js --type yaml --type json \
            -e '\b(claude|codex|gemini|opencode)\b' \
            -g '!cli/src/claude/**' -g '!cli/src/codex/**' \
            -g '!cli/src/gemini/**' -g '!cli/src/opencode/**' \
            -g '!cli/src/agent/backends/**' \
            -g '!.planning/**' -g '!CHANGELOG.md' \
            -g '!shared/src/{flavors,flavors.test,modes,resume,voice}.ts' \
            -g '!cli/NOTICE' -g '!docs/**' -g '!website/**' \
            -g '!node_modules/**' -g '!dist/**' -g '!bun.lock' \
            -g '!.git/**' -g '!**/.gitignore' \
            -g '!scripts/check-no-cut-agents.sh'
        ```

        Also include the four runtime dirs themselves in a SEPARATE invocation (just for accounting; they get blanket TEMP-CUT-XX coverage, not per-file TEMP-WIDE):

        ```bash
        rg -l -e '\b(claude|codex|gemini|opencode)\b' \
            cli/src/claude cli/src/codex cli/src/gemini cli/src/opencode cli/src/agent/backends
        ```

    - Write `.planning/phases/01-cut-non-cursor-agents/01-WAVE0-FINDINGS.md`. Required sections:

        - `## HEAD inventory` — one row per file emitted by the FIRST ripgrep invocation above:
            ```
            | path | hit count | dominant flavor(s) | category (a/b/c/d from RESEARCH §"Business-consumer rewrite map") | proposed owner-commit |
            ```
            Hit count = `rg -c -e '\b(claude|codex|gemini|opencode)\b' <path>`.
            Dominant flavor(s) = which of claude/codex/gemini/opencode contributes the majority of hits (use `rg -c` per literal).
            Proposed owner-commit decision rule:
              - All hits Claude-only or Claude-dominant (>70% of hits) → `CUT-01`
              - All hits Codex-only or Codex-dominant → `CUT-02`
              - All hits Gemini-only or Gemini-dominant → `CUT-03`
              - All hits OpenCode-only or OpenCode-dominant → `CUT-04`
              - Multi-flavor (no single flavor >70%) → `01-05-cleanup` (deferred multi-flavor consumer)
              - Category (c) "structural-to-union — Phase-5 territory" → `whitelist-permanent` (add explicit permanent whitelist entry instead of TEMP-WIDE)
              - File scheduled for outright deletion by a CUT (e.g. `.github/workflows/codex-pr-review.yml`, `cli/src/commands/{claude,codex,gemini,opencode}.ts`, dev scripts in `scripts/dev/`) → owner-commit = the deletion CUT; no rewrite task needed.

        - `## Runtime-dir inventory` — one row per file inside `cli/src/{claude,codex,gemini,opencode}/` and `cli/src/agent/backends/` (these get blanket TEMP-CUT-XX coverage). Just file paths; no ownership column needed.

        - `## Assumption verifications` — the A1/A2/A4/Q2/Q3 findings collected in steps 2-5 below.

        Acceptance: file count in `## HEAD inventory` must equal the line count of the first `rg -l` invocation above (verifies completeness). Every row's `proposed owner-commit` is one of `CUT-01`, `CUT-02`, `CUT-03`, `CUT-04`, `01-05-cleanup`, or `whitelist-permanent`.

    2. **A1 verification:** Run `rg -n 'AgentRegistry' cli/src/` and confirm only `cli/src/agent/AgentRegistry.ts` (definition) and `cli/src/agent/runners/runAgentSession.ts` (consumer) appear. Any other hit ⇒ stop and ask user.

    2. **A2 verification:** Run `rg -n 'permissionAdapter|PermissionAdapter' cli/src/cursor/ cli/src/agent/loopBase.ts cli/src/agent/sessionBase.ts cli/src/cursor/cursorRemoteLauncher.ts cli/src/cursor/cursorLocalLauncher.ts`. If zero hits in `cli/src/cursor/` → mark `cli/src/agent/permissionAdapter.{ts,test.ts}` for deletion in 01-03. If hits exist → annotate in task #5 of 01-03 to keep the file and rewrite consumer branches.

    3. **A4 verification:** Read `shared/src/resume.ts` and `shared/src/schemas.ts`. Confirm Zod schemas use `.optional()` (NOT `.strict()`) on the `*SessionId` object schemas. If any uses `.strict()` → record in commit body and split out a follow-up sub-task to add `.passthrough()` before removing fields.

    4. **Q3 inspection:** Read `cli/scripts/build-executable.ts` and `cli/src/runtime/embeddedAssets.bun.ts`. Confirm no string literal or import reference to `cli/src/{claude,codex,gemini,opencode}/`. If any reference exists → record in findings file and add a fix to task 4 below.

    4.5. **W0.5 — Inspect `cli/src/agent/loopBase.ts` + `sessionBase.ts` (Q2 resolution):**
        - Run `rg -n "claude|codex|gemini|opencode|flavor" cli/src/agent/loopBase.ts cli/src/agent/sessionBase.ts`.
        - Read each matched context.
        - Classify every hit using RESEARCH.md §"Business-consumer rewrite map" categories:
          (a) delete-the-branch (flavor-arm tied to a CUT agent),
          (b) rewrite to Cursor-only,
          (c) structural-to-union — Phase-5 territory (keep + whitelist),
          (d) comment-only — drop flavor name, keep structure.

    5. **Record Wave 0 findings** in NEW file `.planning/phases/01-cut-non-cursor-agents/01-WAVE0-FINDINGS.md`. One row per inspected file:
        ```
        | path | category | hit count | action | belongs to commit |
        ```
        Cover at minimum: `cli/src/agent/loopBase.ts`, `cli/src/agent/sessionBase.ts`, `cli/src/agent/permissionAdapter.ts` (A2), `cli/scripts/build-executable.ts` (Q3), `cli/src/runtime/embeddedAssets.bun.ts` (Q3). Findings flow into subsequent plans' task decisions.

        Decision rules:
        - All hits category (a) → file gets edited in the CUT commit that owns the flavor; if hits span multiple CUTs, file appears in each plan's `files_modified`.
        - Any hit category (c) → add file path to guard whitelist with explicit `# Phase-5 territory` comment (treat like `shared/src/{flavors,modes,resume,voice}.ts`).
        - Block subsequent plan tasks (01-02..04) on this file existing.

    6. **Scaffold ripgrep guard** at `scripts/check-no-cut-agents.sh` using the bash template in RESEARCH.md §"ripgrep guard script" (PATTERN, WHITELIST array, inverted `if rg ...; then exit 1; fi`, trailing success echo). Make it executable (`chmod +x`).

        Use the **BROAD scaffold whitelist** — it MUST be a TRUE SUPERSET of HEAD so `bash scripts/check-no-cut-agents.sh` exits 0 against the current working tree BEFORE any deletion (acceptance criterion below). Each Phase-1-temporary entry is tagged with one of:
          - `# TEMP-CUT-XX: drop after commit #N` for the four runtime-dir blanket globs (CUT-XX plan removes both the dir and its whitelist entry)
          - `# TEMP-WIDE: owner=CUT-XX` for consumer files where the W0.0 inventory assigned that owner-commit (the CUT plan's shrink-whitelist step removes its TEMP-WIDE entries after the consumer rewrite lands)
          - `# TEMP-WIDE: owner=01-05-cleanup` for multi-flavor consumers deferred to 01-05 Task 2

        **Required permanent + TEMP-CUT entries (write these verbatim):**

        ```
        WHITELIST=(
          # Permanent (Phase-1 final form)
          --glob '!.planning/**'
          --glob '!CHANGELOG.md'
          --glob '!shared/src/flavors.ts'
          --glob '!shared/src/flavors.test.ts'
          --glob '!shared/src/modes.ts'
          --glob '!shared/src/resume.ts'
          --glob '!shared/src/voice.ts'
          --glob '!cli/NOTICE'                       # Phase 12 owns
          --glob '!cli/README.md'                    # Phase 12 owns
          --glob '!**/.gitignore'
          --glob '!docs/**'                          # Phase 12 owns
          --glob '!website/**'                       # Phase 12 owns
          --glob '!node_modules/**'
          --glob '!dist/**'
          --glob '!bun.lock'
          --glob '!.git/**'
          --glob '!scripts/check-no-cut-agents.sh'
          # TEMP-CUT-01: drop after 01-01 commit
          --glob '!cli/src/claude/**'
          --glob '!cli/src/commands/claude.ts'
          --glob '!cli/src/commands/hookForwarder.ts'
          --glob '!cli/src/agent/runners/runAgentSession.ts'
          --glob '!cli/src/agent/runners/runAgentSession.test.ts'
          # TEMP-CUT-02: drop after 01-02 commit
          --glob '!cli/src/codex/**'
          --glob '!cli/src/commands/codex.ts'
          --glob '!cli/src/commands/codex.test.ts'
          --glob '!.github/workflows/codex-pr-review.yml'
          --glob '!.github/workflows/codex-mention-response.yml'
          --glob '!scripts/dev/self-test-codex-web-env.sh'
          --glob '!scripts/dev/seed-codex-web-fixture.ts'
          # TEMP-CUT-03: drop after 01-03 commit
          --glob '!cli/src/gemini/**'
          --glob '!cli/src/agent/backends/**'
          --glob '!cli/src/commands/gemini.ts'
          # TEMP-CUT-04: drop after 01-04 commit
          --glob '!cli/src/opencode/**'
          --glob '!cli/src/commands/opencode.ts'
          # TEMP-WIDE: covers consumer-file hits enumerated in 01-WAVE0-FINDINGS.md §"HEAD inventory".
          # Use BROAD directory globs where possible (cheaper than per-file lines) so the scaffold is
          # provably a TRUE SUPERSET of HEAD. Each entry annotated with the owner-commit that drops it
          # via its shrink-whitelist step. Per-CUT shrink steps either remove the broad glob entirely
          # (when ALL files under it are flavor-clean post-rewrite) OR narrow it (e.g., replace
          # 'web/src/components/**' with the specific subset still needing coverage).
          #
          # cli/src consumer surfaces — TEMP-WIDE owner per W0.0 inventory
          --glob '!cli/src/commands/**'                       # TEMP-WIDE: owner=01-05-cleanup (registry, resume, mcp, runner — multi-flavor)
          --glob '!cli/src/agent/**'                          # TEMP-WIDE: owner=01-05-cleanup (sessionFactory, messageConverter, utils, AgentRegistry, runners, types, etc. — multi-flavor / dead-after-CUT-03)
          --glob '!cli/src/api/**'                            # TEMP-WIDE: owner=01-05-cleanup (apiMachine, types, apiSession — multi-flavor)
          --glob '!cli/src/runner/**'                         # TEMP-WIDE: owner=01-05-cleanup (run.ts + README.md + buildCliArgs.test.ts — multi-flavor)
          --glob '!cli/src/cursor/**'                         # TEMP-WIDE: owner=01-05-cleanup (cursorRemoteLauncher codex var, runCursor — partial-multi-flavor)
          --glob '!cli/src/modules/**'                        # TEMP-WIDE: owner=01-05-cleanup (codexModels, opencodeModels, skills, slashCommands, rpcTypes, BasePermissionHandler — multi-flavor)
          --glob '!cli/src/parsers/**'                        # TEMP-WIDE: owner=01-05-cleanup (specialCommands — multi-flavor)
          --glob '!cli/src/ui/**'                             # TEMP-WIDE: owner=01-05-cleanup (CodexDisplay, GeminiDisplay, OpencodeDisplay, RemoteModeDisplay, logger, messageFormatterInk — multi-flavor; per-flavor Display deletions assigned to corresponding CUT)
          --glob '!cli/src/utils/**'                          # TEMP-WIDE: owner=01-05-cleanup (attachmentFormatter — multi-flavor)
          --glob '!cli/src/lib.ts'                            # TEMP-WIDE: owner=01-05-cleanup (re-exports)
          # hub/src consumer surfaces
          --glob '!hub/src/sync/**'                           # TEMP-WIDE: owner=01-05-cleanup (syncEngine, sessionCache, rpcGateway, todos, teams, aliveEvents — multi-flavor)
          --glob '!hub/src/socket/handlers/cli/**'            # TEMP-WIDE: owner=CUT-02 (collaborationMode field — Codex-only)
          --glob '!hub/src/web/routes/**'                     # TEMP-WIDE: owner=01-05-cleanup (sessions, machines, permissions — multi-flavor)
          --glob '!hub/src/push/**'                           # TEMP-WIDE: owner=CUT-02 (pushNotificationChannel.test.ts — codex flavor fixture)
          --glob '!hub/package.json'                          # TEMP-WIDE: owner=CUT-01 ("Claude Code" in description)
          --glob '!hub/README.md'                             # TEMP-WIDE: owner=01-05-cleanup (descriptive prose)
          # shared/src consumer surfaces (schemas/sessionSummary/messages/models/types/socket/resume.test — non-Phase-5 territory)
          --glob '!shared/src/schemas.ts'                     # TEMP-WIDE: owner=01-05-cleanup (per-flavor sessionId fields cleared progressively)
          --glob '!shared/src/sessionSummary.ts'              # TEMP-WIDE: owner=01-05-cleanup (?? chain)
          --glob '!shared/src/messages.ts'                    # TEMP-WIDE: owner=CUT-01 (VISIBLE_CLAUDE_SYSTEM_SUBTYPES)
          --glob '!shared/src/models.ts'                      # TEMP-WIDE: owner=01-05-cleanup (CLAUDE_MODEL_*, GEMINI_MODEL_*)
          --glob '!shared/src/models.test.ts'                 # TEMP-WIDE: owner=01-05-cleanup
          --glob '!shared/src/types.ts'                       # TEMP-WIDE: owner=01-05-cleanup (re-exports)
          --glob '!shared/src/socket.ts'                      # TEMP-WIDE: owner=CUT-02 (collaborationMode field)
          --glob '!shared/src/resume.test.ts'                 # TEMP-WIDE: owner=01-05-cleanup (matrix tests)
          # web/src consumer surfaces — these are the most multi-flavor; broad coverage owned by 01-05
          --glob '!web/src/lib/**'                            # TEMP-WIDE: owner=01-05-cleanup (codexSlashCommands → agentSlashCommands rename in 01-05; sessionModelLabel multi-flavor; assistant-runtime model fixtures; message-window-store; query-keys; locales)
          --glob '!web/src/api/**'                            # TEMP-WIDE: owner=01-05-cleanup (client.ts ~24 hits multi-flavor)
          --glob '!web/src/chat/**'                           # TEMP-WIDE: owner=01-05-cleanup (modelConfig, normalize, presentation, reconcile, reducer*, subagentTool, toolGroups, types — multi-flavor)
          --glob '!web/src/components/**'                     # TEMP-WIDE: owner=01-05-cleanup (SessionChat ~45 hits multi-flavor, SessionList, AssistantChat/*, NewSession/*, ToolCard/* — all multi-flavor)
          --glob '!web/src/hooks/**'                          # TEMP-WIDE: owner=01-05-cleanup (queries/useCodexModels, useOpencodeModels*, useSlashCommands, mutations/useSpawnSession, useSessionActions, useActiveSuggestions — multi-flavor; per-flavor hooks deletion assigned to corresponding CUT)
          --glob '!web/src/realtime/**'                       # TEMP-WIDE: owner=01-05-cleanup (voiceHooks, realtimeClientTools, contextFormatters — multi-flavor)
          --glob '!web/src/router.tsx'                        # TEMP-WIDE: owner=01-05-cleanup
          --glob '!web/src/types/**'                          # TEMP-WIDE: owner=01-05-cleanup (api.ts)
          --glob '!web/README.md'                             # TEMP-WIDE: owner=01-05-cleanup (Phase 12 owns full README polish — interim entry until 01-05 confirms Phase 12 deferral)
          # .github surfaces
          --glob '!.github/workflows/issue-auto-response.yml' # TEMP-WIDE: owner=01-05-cleanup (A7 audit in 01-05 Task 2)
          --glob '!.github/prompts/**'                        # TEMP-WIDE: owner=CUT-02 (codex-pr-review.md prompt — likely deletes with codex workflows)
          # Root markdown / misc
          --glob '!README.md'                                 # TEMP-WIDE: owner=01-05-cleanup (Phase 12 owns full README polish — interim until 01-05 confirms Phase 12 deferral)
          --glob '!CONTRIBUTING.md'                           # TEMP-WIDE: owner=01-05-cleanup (Phase 12 deferral)
          --glob '!AGENTS.md'                                 # TEMP-WIDE: owner=01-05-cleanup (Phase 12 deferral; references claude/codex/gemini in repo layout description)
          --glob '!refactor.md'                               # TEMP-WIDE: owner=01-05-cleanup (likely working notes — 01-05 audits whether to delete or whitelist)
          --glob '!.cursor/rules/**'                          # TEMP-WIDE: owner=01-05-cleanup (gsd-workflow.mdc — Phase 12 deferral)
        )
        ```

        **Inventory cross-check (REQUIRED before proceeding):** every row in `01-WAVE0-FINDINGS.md` §"HEAD inventory" MUST be covered by the WHITELIST above (either by an explicit permanent entry, a TEMP-CUT-XX runtime-dir glob, or a TEMP-WIDE consumer-dir glob). If `bash scripts/check-no-cut-agents.sh` fails after writing the script, the failure output lists the uncovered files — append a TEMP-WIDE entry per file (with `# TEMP-WIDE: owner=01-05-cleanup` if uncertain about the dominant flavor) until the guard exits 0. Do NOT loosen permanent entries or skip the inventory cross-check.

        This makes the guard pass at the pre-deletion baseline (commit #1 starting state) AND remains permissive while consumer-rewrites are in progress. Each subsequent plan ends with a "shrink whitelist" task that removes (or narrows) the TEMP-WIDE entries owned by that commit.

    7. **Wire test:guard into root package.json:** Read root `package.json`. Add npm script `"test:guard": "bash scripts/check-no-cut-agents.sh"`. Append `&& bun run test:guard` to the existing root `"test"` script (the one that chains cli/hub/web). Do NOT touch per-workspace test scripts.

    8. Run `bash scripts/check-no-cut-agents.sh` once — it MUST exit 0 right now (because all 4 CUT references still legitimately exist in source). This baseline-validates the script before any deletion.
  </action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh; test -x scripts/check-no-cut-agents.sh; rg -q '"test:guard"' package.json; bun typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/check-no-cut-agents.sh` (executable)
    - `grep -q 'PATTERN=' scripts/check-no-cut-agents.sh` (script has the regex)
    - `rg -q '"test:guard"' package.json` (npm script wired)
    - `rg -q 'bun run test:guard' package.json` (chained into root test)
    - `bash scripts/check-no-cut-agents.sh` exits 0 against the CURRENT working tree BEFORE any deletion (whitelist is a TRUE SUPERSET of HEAD — every file in 01-WAVE0-FINDINGS.md §"HEAD inventory" is covered)
    - `bun typecheck` exits 0 (no code changes yet)
    - `test -f .planning/phases/01-cut-non-cursor-agents/01-WAVE0-FINDINGS.md`
    - `01-WAVE0-FINDINGS.md` contains a `## HEAD inventory` section whose row count equals the line count of `rg -l ...` (the first invocation in W0.0 above) — verifies completeness
    - Every row in `## HEAD inventory` has a `proposed owner-commit` value from the set `{CUT-01, CUT-02, CUT-03, CUT-04, 01-05-cleanup, whitelist-permanent}`
    - A1/A2/A4/Q2/Q3 findings present under `## Assumption verifications`
  </acceptance_criteria>
  <done>HEAD inventory recorded with owner-commit assignments; guard script scaffolded with TRUE-superset whitelist + chained into `bun run test`; A1/A2/A4/Q2/Q3 verified; baseline green BEFORE any deletion.</done>
</task>

<task type="auto">
  <name>Task 1: Delete cli/src/claude/ tree + Claude commands + runAgentSession</name>
  <files>cli/src/claude/, cli/src/commands/claude.ts, cli/src/commands/hookForwarder.ts, cli/src/agent/runners/runAgentSession.ts, cli/src/agent/runners/runAgentSession.test.ts</files>
  <read_first>
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"CUT-01 deletion map" → "Source dirs / files to delete outright" + "Tests deleted with parents (D-05)"
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"Common Pitfalls" → Pitfall 1
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"5-commit precondition order" row #1
    - .planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md §"Implementation Decisions" D-01, D-05, D-14
    - cli/src/agent/runners/runAgentSession.ts (to confirm `@/claude/` imports before deleting)
    - cli/src/commands/claude.ts
    - cli/src/commands/hookForwarder.ts
  </read_first>
  <action>
    Recursive deletions (use `rm -rf` for the dir, plain `rm` for files):

    1. `rm -rf cli/src/claude/` — entire tree including subdirs `sdk/`, `utils/`, files claudeLocal.ts, claudeRemote.ts, claudeRemoteLauncher.ts, loop.ts, session.ts, runClaude.ts, effort.ts, model.ts, types.ts, registerKillSessionHandler.ts AND every `*.test.ts` under that dir.

    2. `rm cli/src/commands/claude.ts` (defines `claudeCommand`).

    3. `rm cli/src/commands/hookForwarder.ts` (Claude hook-protocol bridge — SC#4 explicit).

    4. `rm cli/src/agent/runners/runAgentSession.ts` + `rm cli/src/agent/runners/runAgentSession.test.ts` (moved up from CUT-03 — sole consumer of `AgentRegistry` and imports `@/claude/utils/startHappyServer` + `@/claude/registerKillSessionHandler`; deleting it now keeps CUT-01's typecheck green per D-15).

    5. After deletion run `ls cli/src/agent/runners/` — if empty, `rmdir cli/src/agent/runners/`.

    DO NOT edit `cli/src/commands/registry.ts` yet — its broken imports are tolerated by `bun typecheck` only if it still references these files. So we must ALSO remove those imports + map entries in this task (see task 2). The clean separation is: this task deletes files; next task fixes registry.ts + consumers.

    Per D-01 (minimal cut): do NOT touch `cli/src/agent/AgentRegistry.ts` here even though it becomes orphan — it stays until CUT-03 (deleted in 01-03) so this commit's diff stays scoped to Claude. `bun typecheck` will still pass because AgentRegistry.ts has no broken imports (it doesn't import from `cli/src/claude/`).
  </action>
  <verify>
    <automated>! test -d cli/src/claude; ! test -f cli/src/commands/claude.ts; ! test -f cli/src/commands/hookForwarder.ts; ! test -f cli/src/agent/runners/runAgentSession.ts</automated>
  </verify>
  <acceptance_criteria>
    - `! test -d cli/src/claude` (entire dir gone)
    - `! test -f cli/src/commands/claude.ts`
    - `! test -f cli/src/commands/hookForwarder.ts` (SC#4)
    - `! test -f cli/src/agent/runners/runAgentSession.ts`
    - `! test -f cli/src/agent/runners/runAgentSession.test.ts`
    - `rg -l 'claudeLocal|claudeRemote|registerKillSessionHandler' cli/src/claude/ 2>/dev/null | wc -l` equals 0 (dir gone)
  </acceptance_criteria>
  <done>All Claude source + tests deleted; runAgentSession removed; `cli/src/agent/runners/` empty-dir cleanup done.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Claude consumers — registry, agent, hub, shared, web</name>
  <files>cli/src/commands/registry.ts, cli/src/agent/sessionFactory.ts, cli/src/agent/sessionFactory.test.ts, cli/src/agent/utils.ts, cli/src/agent/utils.test.ts, cli/src/api/apiMachine.ts, cli/src/api/apiMachine.test.ts, cli/src/api/types.ts, cli/src/commands/resume.ts, cli/src/commands/resume.test.ts, cli/src/runner/run.ts, cli/src/runner/buildCliArgs.test.ts, hub/src/sync/syncEngine.ts, hub/src/sync/sessionCache.ts, hub/src/sync/sessionModel.test.ts, hub/src/sync/teams.ts, hub/src/web/routes/sessions.ts, hub/src/web/routes/sessions.test.ts, hub/src/web/routes/permissions.ts, hub/src/web/routes/machines.ts, hub/package.json, shared/src/schemas.ts, shared/src/sessionSummary.ts, shared/src/messages.ts, shared/src/models.ts, shared/src/models.test.ts, shared/src/types.ts, shared/src/resume.test.ts, web/src/lib/sessionModelLabel.ts, web/src/lib/sessionModelLabel.test.ts, web/src/lib/assistant-runtime.test.ts</files>
  <read_first>
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"CUT-01 deletion map" → "Tests OUTSIDE deleted dirs that need Claude rewrites (D-06)" + "Business-consumer Claude rewrites (D-03)" + "CUT-01 misc"
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"Business-consumer rewrite map" rows tagged "#1" or "Claude"
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"Hidden Landmines" #3 (assistant-runtime.test.ts model fixtures), #4 (hub/package.json description)
    - .planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md D-03, D-06, D-08, D-09
    - cli/src/commands/registry.ts (current state)
    - hub/src/sync/syncEngine.ts (recoverClaude* methods)
    - shared/src/schemas.ts (claudeSessionId field)
    - shared/src/models.ts (CLAUDE_MODEL_* exports)
    - shared/src/messages.ts (VISIBLE_CLAUDE_SYSTEM_SUBTYPES + helpers)
    - web/src/lib/assistant-runtime.test.ts (model fixture strings)
  </read_first>
  <action>
    Edits in dependency order (bottom-up):

    1. **`cli/src/commands/registry.ts`** (per D-09): remove the `import { claudeCommand } from './claude'` line and any `claudeCommand` entry in the command map. Per D-01, leave codex/gemini/opencode imports + entries intact (they die in 01-02..04). Default fallback rewrite is in 01-05 — for now if the default still points at `claudeCommand`, swap it to `cursorCommand` (already imported). If pointing at `cursorCommand` already, no change.

    2. **`cli/src/commands/resume.ts`** (line 6-10 area): drop the import line for `ClaudePermissionMode` (if present). Drop any `flavor === 'claude'` branch + fall-through. Keep imports for codex/gemini/opencode (die later).

    3. **`cli/src/commands/resume.test.ts`**: strip Claude-flavored test cases (`flavor: 'claude'` fixtures) per D-06.

    4. **`cli/src/runner/run.ts`**: per RESEARCH §"Business-consumer rewrite map" (23 hits), strip Claude spawn-path branches. Inspect file; remove `case 'claude':` / `if (flavor === 'claude')` arms; remove `import.*from '@/claude/'` lines (any are now broken).

    5. **`cli/src/runner/buildCliArgs.test.ts`**: strip Claude cases from the per-flavor matrix (D-06).

    6. **`cli/src/agent/sessionFactory.ts`** lines 96-99: delete the line that preserves `claudeSessionId` from the input. Keep `cursorSessionId` (line 100). Per CUT-01 only the Claude line goes; the codex/gemini/opencode lines stay (die later).

    7. **`cli/src/agent/sessionFactory.test.ts`** (D-06): strip Claude flavor cases from the matrix.

    8. **`cli/src/agent/utils.ts` / `utils.test.ts`**: only Claude-named comments need touching here (full comment cleanup deferred to 01-05; do the minimum to keep ripgrep guard happy when the whitelist tightens). For now leave (still pre-tighten phase).

    9. **`cli/src/api/apiMachine.ts` + `apiMachine.test.ts`**: strip Claude branches + fixtures (per RESEARCH rewrite map rows). Specifically remove `flavor === 'claude'` arms and `claudeModelPreset` payload references; rewrite test fixtures to Cursor-only.

    10. **`cli/src/api/types.ts`**: drop any `ClaudePermissionMode` / `ClaudeModelPreset` re-exports.

    11. **`hub/src/sync/syncEngine.ts`** (per CUT-01 deletion map):
        - Delete `recoverClaudeSessionIdFromMessages`, `extractClaudeSessionId`, `normalizeClaudeSessionId`, `persistRecoveredClaudeSessionId` functions outright.
        - In `getCanonicalAgentSessionId`: remove the `flavor === 'claude'` arm; default to `cursorSessionId`.
        - Replace any `flavor ?? 'claude'` defaults with `flavor ?? 'cursor'` (per D-03).

    12. **`hub/src/sync/sessionCache.ts`** lines ~702-706: strip `claudeSessionId` from the per-flavor session-id picker chain. Keep the other 3 for now (die later).

    13. **`hub/src/sync/teams.ts`** line ~11: replace any Claude-named comment with flavor-neutral wording (or just delete the comment).

    14. **`hub/src/sync/sessionModel.test.ts`** (97 hits): strip Claude fixtures from the matrix per D-06. Codex/Gemini/OpenCode fixtures stay (die in their CUTs).

    15. **`hub/src/web/routes/sessions.ts`**: rewrite `flavor ?? 'claude'` defaults → `'cursor'`. Delete the Claude-only `effort` route (and any Claude-specific endpoint surfaces). Delete any `recoverClaude*` callers.

    16. **`hub/src/web/routes/sessions.test.ts`**: delete Claude-effort test cases.

    17. **`hub/src/web/routes/permissions.ts`**: `flavor ?? 'claude'` → `'cursor'`.

    18. **`hub/src/web/routes/machines.ts`** (per D-03): in the `z.enum([...])` for `flavor`, drop the `'claude'` literal. Keep `'cursor', 'codex', 'gemini', 'opencode'` (the rest die in their CUTs).

    19. **`hub/package.json`** description: replace `"Telegram Bot client for HAPI - control Claude Code sessions"` → `"Telegram Bot client for HAPI - control sessions"` (drop "Claude Code"; the "Telegram" mention is Phase-2's problem, leave it).

    20. **`shared/src/schemas.ts`** (line ~36): delete the `claudeSessionId: z.string().optional()` field from the session-summary-ish schema. Keep `codexSessionId / geminiSessionId / opencodeSessionId` lines for now.

    21. **`shared/src/sessionSummary.ts`** (line ~36-39): drop `claudeSessionId` from the `?? ` fallback chain. Chain becomes e.g. `cursorSessionId ?? codexSessionId ?? geminiSessionId ?? opencodeSessionId`.

    22. **`shared/src/messages.ts`**: delete the exports `VISIBLE_CLAUDE_SYSTEM_SUBTYPES`, `isClaudeChatVisibleSystemSubtype`, `isClaudeChatVisibleMessage`. Confirm no Cursor caller (grep should hit only Claude-deleted dirs + this file).

    23. **`shared/src/models.ts`**: delete `CLAUDE_MODEL_LABELS`, `CLAUDE_MODEL_PRESETS`, `ClaudeModelPreset`, `isClaudeModelPreset`, `getClaudeModelLabel`. Keep Codex/Gemini/OpenCode model exports (die later).

    24. **`shared/src/models.test.ts`**: drop Claude-specific test cases.

    25. **`shared/src/types.ts`**: drop the `ClaudePermissionMode` / `ClaudeModelPreset` re-exports.

    26. **`shared/src/resume.test.ts`**: strip Claude flavor cases from any matrix.

    27. **`web/src/lib/sessionModelLabel.ts` + `.test.ts`**: remove the `ClaudeModelPreset` import + the Claude branch of the label-resolution `switch`/`if`. Add a flavor-neutral default or rely on Cursor branch only.

    28. **`web/src/lib/assistant-runtime.test.ts`** (per Pitfall 3 + Landmine 3): rewrite model-name fixtures. Replace bare strings like `'claude-sonnet-4-6'`, `'claude-haiku-4-5-20251001'` with flavor-neutral analogues like `'sonnet'`, `'composer'`, or `'haiku'` (per D-06). The TESTS are model-dedup tests; flavor-name in the fixture is incidental.

    After each cluster of edits run `bun typecheck` from the touched workspace + `bun run test` from that workspace, then root `bun typecheck && bun run test` once at the end.

    29. **Shrink guard whitelist — drop CUT-01-owned TEMP entries.** Edit `scripts/check-no-cut-agents.sh`:
        - Remove every `--glob` line tagged `# TEMP-CUT-01:` (runtime-dir blanket).
        - Remove every `--glob` line tagged `# TEMP-WIDE: owner=CUT-01` (e.g., `hub/package.json`, `shared/src/messages.ts`) — these files are now flavor-clean after this task's edits.
        - For broad consumer-dir TEMP-WIDE entries owned by `01-05-cleanup` (e.g., `cli/src/agent/**`, `web/src/components/**`): KEEP them — they still contain Codex/Gemini/OpenCode hits not yet stripped.
        - Cross-check: `rg -c '\bclaude\b' <file>` returns 0 for every Claude-owned file just edited (excluding wire literal constants in `shared/src/modes.ts`).

    Run `bash scripts/check-no-cut-agents.sh` — MUST still exit 0 after this shrink. If it fails, a Claude hit leaked outside whitelist; investigate the failing path BEFORE committing.
  </action>
  <checkpoint_strategy>
    Touches ~30 files across cli/agent, cli/api, cli/commands, cli/runner, hub/sync, hub/web, shared, web. Single atomic commit, but mid-task feedback is required to avoid deep-debug at the end:

    Cluster order (run `bun typecheck` from listed workspace after each; proceed only if green):
    1. **cli/agent + cli/api + cli/commands + cli/runner** (steps 1-10) → `cd cli && bun run typecheck`
    2. **shared/src** (steps 20-26) → `cd shared && bun run typecheck && bun run test`
    3. **hub/src** (steps 11-19) → `cd hub && bun run typecheck && bun run test`
    4. **web/src** (steps 27-28) → `cd web && bun run typecheck`
    5. **Whitelist shrink** (step 29) → `bash scripts/check-no-cut-agents.sh` (must exit 0)
    6. **Root** → `bun typecheck && bun run test` (final gate)

    If any cluster typecheck fails, fix within the cluster before moving on; do not accumulate compile errors across clusters.
  </checkpoint_strategy>
  <verify>
    <automated>bun typecheck && bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - `! rg 'recoverClaudeSessionIdFromMessages|extractClaudeSessionId|normalizeClaudeSessionId|persistRecoveredClaudeSessionId' hub/src/`
    - `! rg 'VISIBLE_CLAUDE_SYSTEM_SUBTYPES|isClaudeChatVisibleSystemSubtype|isClaudeChatVisibleMessage' shared/src/`
    - `! rg 'CLAUDE_MODEL_LABELS|CLAUDE_MODEL_PRESETS|ClaudeModelPreset|isClaudeModelPreset|getClaudeModelLabel' shared/src/ cli/src/ hub/src/ web/src/`
    - `! rg 'claudeSessionId' shared/src/schemas.ts shared/src/sessionSummary.ts hub/src/sync/sessionCache.ts cli/src/agent/sessionFactory.ts`
    - `! rg 'claudeCommand' cli/src/commands/registry.ts`
    - `! rg 'Claude Code' hub/package.json`
    - `! rg "flavor === 'claude'|flavor ?? 'claude'" cli/ hub/ shared/ web/`
    - `bun typecheck` exits 0
  </acceptance_criteria>
  <done>All Claude consumer branches/imports/exports rewritten Cursor-only or removed. `bun typecheck` green.</done>
</task>

<task type="auto">
  <name>Task 2.5: Rewrite W0.0-inventory consumers assigned to CUT-01</name>
  <files>(determined by 01-WAVE0-FINDINGS.md §"HEAD inventory" filtered to owner-commit=CUT-01)</files>
  <read_first>
    - .planning/phases/01-cut-non-cursor-agents/01-WAVE0-FINDINGS.md §"HEAD inventory"
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"Business-consumer rewrite map" categories (a/b/d)
  </read_first>
  <action>
    Open `01-WAVE0-FINDINGS.md` and filter `## HEAD inventory` to rows where `proposed owner-commit = CUT-01`.

    For every such row NOT already covered by Task 2's explicit step list (Task 2 enumerates the known-from-research consumer set; W0.0 may surface additional Claude-dominant files):
      - Apply RESEARCH §"Business-consumer rewrite map" category to that file:
          (a) delete-the-branch → remove the `flavor === 'claude'` / `case 'claude':` / Claude-named arm
          (b) rewrite to Cursor-only → swap Claude-named identifier/branch for Cursor equivalent
          (d) comment-only → drop Claude name from comment, keep structure
      - Run `bun typecheck` from the touched workspace after each file's edits.

    After all CUT-01 rows are processed:
      - Verify `rg -c '\bclaude\b' <file>` returns 0 for every file just edited (excluding any legitimate Phase-5-territory union reference).
      - Update `01-WAVE0-FINDINGS.md` §"HEAD inventory" rows: append a "Status" column entry `done-CUT-01` to each processed row (the table becomes a running ledger).

    If a row's hits turn out to be multi-flavor (Claude + others) — e.g., the executor finds Codex/Gemini arms in the same file — DO NOT rewrite the non-Claude arms here. Strip only the Claude arm; reassign the row's owner-commit to `01-05-cleanup` (multi-flavor) in `01-WAVE0-FINDINGS.md` so subsequent CUTs / 01-05 pick up the remaining arms. Note the reassignment in the commit body.
  </action>
  <checkpoint_strategy>
    Process rows in clusters by workspace (cli → shared → hub → web), running `bun typecheck` per cluster. Skip the task entirely if W0.0 inventory has zero unhandled CUT-01 rows.
  </checkpoint_strategy>
  <verify>
    <automated>bun typecheck && bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - Every `01-WAVE0-FINDINGS.md` row with `owner-commit = CUT-01` is marked `done-CUT-01` OR reassigned to `01-05-cleanup` (with rationale in commit body)
    - `bun typecheck` exits 0
    - `bash scripts/check-no-cut-agents.sh` exits 0
  </acceptance_criteria>
  <done>W0.0 inventory CUT-01 rows fully processed; ledger updated; typecheck + guard green.</done>
</task>

<task type="auto">
  <name>Task 3: Run test suite + guard + commit CUT-01</name>
  <files>(none modified; runs scripts only)</files>
  <read_first>
    - .planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md D-14 (commit #1 message) + D-15 (per-commit green mandate)
    - .planning/phases/01-cut-non-cursor-agents/01-VALIDATION.md §"Sampling Rate"
  </read_first>
  <action>
    From repo root run sequentially:

    1. `bun typecheck` — must exit 0. If failure, fix via task 2 follow-up; do NOT proceed.
    2. `bun run test` — must exit 0 (chains cli/hub/web Vitest + `test:guard`). If guard fails, the whitelist is too tight or a non-Claude reference leaked; inspect output and tighten whitelist OR fix code. Note: guard will likely still pass since 01-01's whitelist is the broad form (covers all 4 CUTs); narrow whitelist comes in 01-05.
    3. `bash scripts/check-no-cut-agents.sh` directly — confirm 0 exit.

    Stage + commit:
    `git add -A && git commit -m 'feat(phase-01): CUT-01 remove Claude Code agent

    - Delete cli/src/claude/ tree (including vendored SDK)
    - Delete cli/src/commands/{claude,hookForwarder}.ts
    - Delete cli/src/agent/runners/runAgentSession.{ts,test.ts} (dead — was sole consumer that imported from @/claude/; moved up from CUT-03 per RESEARCH §Pitfall 1)
    - Strip Claude branches from sessionFactory, syncEngine (recover* methods), sessionCache picker, schemas.claudeSessionId, shared models/messages
    - Rewrite assistant-runtime.test.ts model fixtures to flavor-neutral
    - hub/package.json: drop \"Claude Code\" from description
    - Scaffold scripts/check-no-cut-agents.sh + wire test:guard into root test script

    Closes: CUT-01 (Phase 1)
    Refs: D-01, D-03, D-05, D-09, D-14 commit #1'`
  </action>
  <verify>
    <automated>bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh && git log -1 --format='%s' | grep -q 'CUT-01'</automated>
  </verify>
  <acceptance_criteria>
    - `bun typecheck` exits 0
    - `bun run test` exits 0 (includes test:guard via root chain)
    - `bash scripts/check-no-cut-agents.sh` exits 0
    - `git log -1 --format='%s'` matches `feat(phase-01): CUT-01 remove Claude Code agent`
    - `git status --porcelain` is empty (everything committed)
  </acceptance_criteria>
  <done>CUT-01 atomic commit landed; D-15 per-commit-green gate passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — this phase is pure deletion of agent runtimes that already existed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-N1 | — | — | n/a | **No new threats — phase is pure deletion; no new code paths introduced. ASVS verification deferred to Phase 12 (VRFY-02 / VRFY-03).** Per RESEARCH.md §"Security Domain": V12 Files & Resources attack surface (`cli/src/claude/sdk/` streaming over `cross-spawn`) is REMOVED by this plan, net negative attack surface. |
</threat_model>

<verification>
- `bun typecheck` green
- `bun run test` green (chains cli → hub → web + test:guard)
- `bash scripts/check-no-cut-agents.sh` green
- ripgrep `\bclaude\b` confined to whitelist (`.planning/codebase/`, `CHANGELOG.md`, `shared/src/{flavors,flavors.test,modes,resume,voice}.ts`, `cli/NOTICE`, docs/, website/, node_modules/, dist/, bun.lock, .git/) — verify with `bash scripts/check-no-cut-agents.sh`
- File-existence: `cli/src/claude/`, `cli/src/commands/claude.ts`, `cli/src/commands/hookForwarder.ts`, `cli/src/agent/runners/runAgentSession.ts` all absent
</verification>

<success_criteria>
- Plan-1 commit `feat(phase-01): CUT-01 remove Claude Code agent` is on HEAD with `bun typecheck && bun run test && bun run test:guard` all green (D-15)
- ripgrep guard script exists and is chained into `bun run test`
- Wave 0 assumption verifications (A1/A2/A4 + Q3) documented in commit body
- `runAgentSession.ts` deleted in this commit (NOT deferred to CUT-03) — verified by `! test -f cli/src/agent/runners/runAgentSession.ts`
</success_criteria>

<output>
Create `.planning/phases/01-cut-non-cursor-agents/01-01-SUMMARY.md` when done. Include:
- Findings from A1/A2/A4 + Q3
- Final list of files deleted (paths)
- Final list of consumer files rewritten (paths + line-count delta)
- Guard whitelist as-shipped (will tighten in 01-05)
- Any deviations from RESEARCH.md with rationale
</output>
