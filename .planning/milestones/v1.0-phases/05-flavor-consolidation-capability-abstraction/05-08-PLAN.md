---
phase: 05-flavor-consolidation-capability-abstraction
plan: 08
type: execute
wave: 8
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06, 05-07]
files_modified:
  - scripts/check-no-cut-agents.sh
autonomous: true
requirements_addressed: [CUT-05, REFA-01]
must_haves:
  truths:
    - "All `# === Phase-5 territory` whitelist entries are removed from `scripts/check-no-cut-agents.sh`."
    - "The guard script post-filters exactly one residue line — `shared/src/modes.ts:.*AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const` — per D-85 (line-anchored exception, NOT whole-file whitelist)."
    - "A new `PHASE5_IDENTIFIER_PATTERN` rg invocation enforces zero hits for `\\bisCodexFamilyFlavor\\b`, `\\bCodexCollaborationMode\\b`, `\\bgetCodexCollaboration\\w*\\b`, and `\\b(CLAUDE|CODEX|GEMINI|OPENCODE)_PERMISSION_MODES\\b` across `cli/src`, `hub/src`, `web/src`, `shared/src`."
    - "A new `flavor === '<literal>'` sweep enforces zero hits for non-cursor literals (`flavor === 'claude'|'codex'|'gemini'|'opencode'`); `flavor === 'cursor'` (if any survives in scope) is post-filtered through."
    - "`bun typecheck`, `bun run test`, and `bash scripts/check-no-cut-agents.sh` all exit 0 from the repo root (final phase gate; SC#1, SC#2, SC#3, SC#4 all met)."
    - "`build:single-exe` is NOT part of this phase's verification (D-88; deferred to Phase 12 milestone gate)."
  artifacts:
    - path: "scripts/check-no-cut-agents.sh"
      provides: "Phase-5-collapsed whitelist + three new pattern sweeps with line-anchored AGENT_MESSAGE_PAYLOAD_TYPE post-filter."
  key_links:
    - from: "scripts/check-no-cut-agents.sh"
      to: "shared/src/modes.ts:6 (AGENT_MESSAGE_PAYLOAD_TYPE)"
      via: "post-filter grep"
      pattern: "grep -v.*AGENT_MESSAGE_PAYLOAD_TYPE"
---

<objective>
Slice 4 — final phase gate. Collapse the Phase-5-territory whitelist in `scripts/check-no-cut-agents.sh` to zero entries; add the three Phase-5 sweep patterns (D-84 #1 collapsed via post-filter on existing `PATTERN`; D-84 #2 `flavor === '<literal>'` non-cursor sweep; D-84 #3 `PHASE5_IDENTIFIER_PATTERN`); add the line-anchored `AGENT_MESSAGE_PAYLOAD_TYPE` post-filter exception per D-85. Run the full repo gate: `bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh` exits 0.

Purpose: lands SC#3 (zero hardcoded `flavor ===` / capability gates) and the phase-completion verification. After this slice, the only Cursor-relevant residue tracked by the guard is the wire literal `AGENT_MESSAGE_PAYLOAD_TYPE = 'codex'` at `shared/src/modes.ts:6`, allowed via a post-filter grep targeting the JSDoc-anchored line.
Output: phase-gate green; all four SCs met.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-CONTEXT.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-PATTERNS.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-01-PLAN.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-07-PLAN.md

@scripts/check-no-cut-agents.sh
@shared/src/modes.ts

<interfaces>
Existing guard script structure (per `05-PATTERNS.md` §`scripts/check-no-cut-agents.sh`):
- `RG_BIN` resolution (lines 23–30) — KEEP verbatim.
- Top-level `PATTERN='\\b(claude|codex|gemini|opencode|telegram|serverchan|elevenlabs|grammy)\\b'` — KEEP verbatim.
- `WHITELIST` array — currently contains a `# === Phase-5 territory` block (lines 48–117) that this slice DELETES.
- Phase-3 (`PHASE3_PATTERN`) and Phase-4 (`PHASE4_HARD_PATTERN` / `PHASE4_SWEEP_PATTERN`) sibling blocks (lines 134–183) — KEEP verbatim.
- Final ✅/❌ message pair — UPDATE to drop the "Phase-5 territory" carve-out language.

The new sweep additions follow the **sibling-block pattern** established by Phase 4: each new pattern gets its own constant, its own `rg` invocation, and its own ✅/❌ message pair.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse Phase-5 whitelist; add `AGENT_MESSAGE_PAYLOAD_TYPE` line-anchored post-filter on the existing `PATTERN` sweep</name>
  <files>scripts/check-no-cut-agents.sh</files>
  <read_first>
    - `scripts/check-no-cut-agents.sh` (whole file — lines 23–30 RG_BIN; lines 32–46 PATTERN + WHITELIST start; lines 48–117 `# === Phase-5 territory` whitelist block to DELETE; lines 134–183 Phase 3/4 sibling pattern triplets to MIRROR; lines 148–157 final message pair)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-PATTERNS.md` §"`scripts/check-no-cut-agents.sh`" lines 532–638 (preservation + delete + sibling-block + post-filter patterns)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md` §"Ripgrep guard additions (concrete per D-84 + D-85)" lines 394–444
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-CONTEXT.md` §D-84, §D-85 (whitelist must collapse; line-anchored exception only)
  </read_first>
  <behavior>
    - Delete every `# === Phase-5 territory` line and every `--glob '!&lt;path&gt;'` immediately under it (lines 48–117 in the current script). The `WHITELIST` array shrinks to its non-Phase-5 entries (`.planning/`, `CHANGELOG.md`, `**/.gitignore`, `node_modules/`, `dist/`, `bun.lock`, `.git/`, `scripts/check-no-cut-agents.sh` self-exclusion, plus any prior-phase entries) — verify by reading the original block boundaries.
    - Replace the existing main `rg` invocation with a post-filter pattern per RESEARCH §"Ripgrep guard additions" lines 406–411:
      - Capture survivors: `SURVIVORS=$("$RG_BIN" -n -i "${WHITELIST[@]}" "$PATTERN" . || true)`
      - Filter the legitimate residue: `SURVIVORS_FILTERED=$(echo "$SURVIVORS" | grep -v "shared/src/modes.ts:.*AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const" || true)`
      - Fail if filtered survivors remain: print + `exit 1`.
    - Update the ✅/❌ message pair (current lines 148–157) to drop the "Phase-5 territory" carve-out language (the carve-out is gone). The new message points only at Phase-12 docs/marketing surfaces (which are still Phase-12-territory and remain in the surviving whitelist via `.planning/` / `CHANGELOG.md` / `docs/` / `website/` globs — preserve these).
    - 4-space indent — but `scripts/check-no-cut-agents.sh` is shell, so preserve the file's existing 2-space indent style.
  </behavior>
  <action>
    Edit `scripts/check-no-cut-agents.sh` in place. Remove the Phase-5 whitelist block. Replace the main `rg` invocation with the SURVIVORS / SURVIVORS_FILTERED pattern (per RESEARCH §"Ripgrep guard additions" code excerpt). Update the failure message to drop "Phase-5 territory" wording. Keep the `RG_BIN` resolution, the top-level `PATTERN`, the Phase-3 / Phase-4 sibling pattern blocks, and any non-Phase-5 whitelist entries verbatim. Preserve the file's existing shell-script indent style. After editing, run `bash scripts/check-no-cut-agents.sh` from repo root — must exit 0. If a residue is found that the post-filter doesn't catch, treat it as a regression in plans 02–07 (cross-plan correction): do NOT add a new whitelist entry for it.
  </action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <done>
    Phase-5 whitelist deleted from script. Post-filter exempts the `AGENT_MESSAGE_PAYLOAD_TYPE` line. Failure message updated. Guard script exits 0 from repo root.
  </done>
  <acceptance_criteria>
    - `rg -n "Phase-5 territory" scripts/check-no-cut-agents.sh` returns zero hits.
    - `rg -n "shared/src/flavors\\.ts|shared/src/modes\\.ts|shared/src/resume\\.ts|shared/src/schemas\\.ts" scripts/check-no-cut-agents.sh | rg -v 'AGENT_MESSAGE_PAYLOAD_TYPE'` returns zero hits in the whitelist block (i.e. the Phase-5 globs are gone).
    - `rg -n "AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const" scripts/check-no-cut-agents.sh` returns at least one hit (post-filter expression).
    - `bash scripts/check-no-cut-agents.sh` exit code 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add `PHASE5_IDENTIFIER_PATTERN` sweep + `flavor === '<literal>'` non-cursor sweep as sibling blocks</name>
  <files>scripts/check-no-cut-agents.sh</files>
  <read_first>
    - `scripts/check-no-cut-agents.sh` (post Task 1 — verify Phase 3/4 sibling block pattern at the existing line range)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-PATTERNS.md` §"Guard-Script Extension" lines 700–704 (sibling block style); lines 599–622 (target invocation shape)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md` §"Additional Phase-5 identifier checks" lines 419–433; §"`flavor === '...'` non-cursor literal check" lines 435–444
  </read_first>
  <behavior>
    - Add a new sibling block after the Phase-4 block:
      ```
      PHASE5_IDENTIFIER_PATTERN='\bisCodexFamilyFlavor\b|\bCodexCollaborationMode\b|\bgetCodexCollaboration\w*\b|\b(CLAUDE|CODEX|GEMINI|OPENCODE)_PERMISSION_MODES\b'
      ```
      Run via `"$RG_BIN" -n "$PHASE5_IDENTIFIER_PATTERN" cli/src hub/src web/src shared/src`; if it finds any hits → ❌ exit 1; otherwise → ✅ message.
    - Add a second sibling block for the `flavor === '<literal>'` sweep — use the simple post-filter form (RESEARCH lines 437–444):
      - Run `FLAVOR_BRANCH=$("$RG_BIN" -n "flavor\s*===\s*['\"]" cli/src hub/src web/src shared/src || true)`.
      - Post-filter the cursor narrow: `FLAVOR_BRANCH_FILTERED=$(echo "$FLAVOR_BRANCH" | grep -v "=== 'cursor'" || true)`.
      - Per D-84 #2 the ideal target is even `flavor === 'cursor'` collapses away; this slice's guard accepts cursor narrow if any legitimately survives (e.g. a metadata-driven cursor-gate inside `shared/src/`), but flags non-cursor literals.
      - Fail if `FLAVOR_BRANCH_FILTERED` non-empty → ❌ exit 1; otherwise → ✅.
    - Each new block has its own ✅/❌ message pair (mirroring Phase 4 sibling blocks).
    - Preserve shell-script existing indent.
  </behavior>
  <action>
    Append both sibling blocks after the existing Phase-4 block in `scripts/check-no-cut-agents.sh`. Each block declares its `PATTERN` constant, runs `rg` over `cli/src hub/src web/src shared/src`, applies post-filter where needed, and prints ✅ or ❌ + `exit 1`. Run `bash scripts/check-no-cut-agents.sh` from repo root — must exit 0. If either new sweep flags a hit, the hit is a regression in plans 02–07; trace it and fix at the source (delete or rewrite consumer), not by extending the whitelist.
  </action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <done>
    Two new sibling blocks present. `PHASE5_IDENTIFIER_PATTERN` enforces deletion of `isCodexFamilyFlavor` / `CodexCollaborationMode` / `getCodexCollaboration*` / `(CLAUDE|CODEX|GEMINI|OPENCODE)_PERMISSION_MODES`. `flavor === '<literal>'` sweep enforces zero non-cursor branches. Guard exits 0 from repo root.
  </done>
  <acceptance_criteria>
    - `rg -n "PHASE5_IDENTIFIER_PATTERN" scripts/check-no-cut-agents.sh` returns at least one hit.
    - `rg -n "isCodexFamilyFlavor|CodexCollaborationMode|getCodexCollaboration|\\(CLAUDE\\|CODEX\\|GEMINI\\|OPENCODE\\)_PERMISSION_MODES" scripts/check-no-cut-agents.sh` returns at least one hit (the pattern declaration).
    - `rg -n "flavor\\\\s\\*===\\\\s\\*\\['\\\"\\]" scripts/check-no-cut-agents.sh` returns at least one hit (the new sweep pattern).
    - `rg -n "=== 'cursor'" scripts/check-no-cut-agents.sh` returns at least one hit (the post-filter exemption).
    - `bash scripts/check-no-cut-agents.sh` exit code 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Final phase gate — `bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh` from repo root green</name>
  <files>scripts/check-no-cut-agents.sh</files>
  <read_first>
    - `.planning/ROADMAP.md` §"Phase 5: Flavor consolidation + capability abstraction" — Success Criteria #1–#4
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-CONTEXT.md` §D-88 (no `build:single-exe`)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-VALIDATION.md` §Validation Sign-Off (this slice flips it green)
  </read_first>
  <behavior>
    - Run the three commands sequentially from repo root: `bun typecheck`, `bun run test`, `bash scripts/check-no-cut-agents.sh`. All must exit 0.
    - Verify SC#1 by `rg -n "export type AgentFlavor\\s*=\\s*'cursor'" shared/src/modes.ts` returns exactly one hit.
    - Verify SC#2 by inspecting `shared/src/flavors.ts` — single `cursor` row with all 7 D-73 slots populated.
    - Verify SC#3 by the guard script's three sweeps (main + identifier + branch) all green.
    - Verify SC#4 by `cd shared && bun run test flavors.test.ts` covering the 23 D-87 cases (cases 14 + 17 already tightened in plan 07).
    - Do NOT run `build:single-exe` (D-88).
    - 4-space indent (script is shell — preserve existing style).
  </behavior>
  <action>
    No file edits expected unless the gate fails. If the gate fails, identify which SC failed and which prior plan owns the regression; reopen that plan's SUMMARY for follow-up. If the gate passes, this task only writes the SUMMARY (next section) — no source edits.
  </action>
  <verify>
    <automated>bun typecheck &amp;&amp; bun run test &amp;&amp; bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <done>
    Repo-root gate green: typecheck + tests + guard all exit 0. SC#1–#4 verified. `build:single-exe` NOT run (per D-88).
  </done>
  <acceptance_criteria>
    - `bun typecheck` exit code 0.
    - `bun run test` exit code 0.
    - `bash scripts/check-no-cut-agents.sh` exit code 0.
    - `rg -n "export type AgentFlavor\\s*=\\s*'cursor'" shared/src/modes.ts` returns exactly 1 hit (SC#1).
    - `rg -n "cursor:\\s*\\{" shared/src/flavors.ts` returns ≥ 1 hit and `rg -n "claude:\\s*\\{|codex:\\s*\\{|gemini:\\s*\\{|opencode:\\s*\\{" shared/src/flavors.ts` returns 0 hits (SC#2).
    - Phase-completion gate green; `05-VALIDATION.md` Sign-Off ready to flip.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guard script ↔ source tree | the guard is the last line of defense against re-introduction of non-cursor flavor literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering | future PR re-introduces `'claude'` literal | mitigate | Three guard sweeps (main + identifier + branch) reject any new non-cursor literal; CI runs the script on every push |
| T-05-08-02 | Tampering | drift in `AGENT_MESSAGE_PAYLOAD_TYPE` line position | mitigate | Post-filter targets the JSDoc-anchored content (`AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const`), not a fixed line number — survives line-position drift |
</threat_model>

<verification>
- `bun typecheck` from repo root green.
- `bun run test` from repo root green.
- `bash scripts/check-no-cut-agents.sh` from repo root green.
- SC#1, SC#2, SC#3, SC#4 all verified per Task 3 acceptance.
</verification>

<success_criteria>
- `scripts/check-no-cut-agents.sh` Phase-5 whitelist removed.
- Three Phase-5 sweep patterns active (main `PATTERN` + post-filter, `PHASE5_IDENTIFIER_PATTERN`, `flavor === '<literal>'` sweep).
- Line-anchored post-filter for `AGENT_MESSAGE_PAYLOAD_TYPE` works (D-85).
- Phase-completion gate green: `bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh` exits 0.
- `build:single-exe` NOT run (D-88).
</success_criteria>

<output>
Create `.planning/phases/05-flavor-consolidation-capability-abstraction/05-08-SUMMARY.md` when done. Record: whitelist diff (lines removed), new sibling-block excerpts (pattern constants + invocations), post-filter line, repo-root gate result for each of the three commands, and explicit confirmation that SC#1–#4 are all met. Flip `nyquist_compliant: true` in `05-VALIDATION.md` if appropriate.
</output>
