---
phase: 05-flavor-consolidation-capability-abstraction
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - web/src/components/NewSession/AgentSelector.tsx
  - web/src/components/NewSession/preferences.ts
  - web/src/components/NewSession/preferences.test.ts
  - web/src/components/NewSession/types.ts
  - web/src/components/NewSession/types.test.ts
  - web/src/components/NewSession/index.tsx
  - web/src/components/NewSession/ClaudeEffortSelector.tsx
  - web/src/components/NewSession/ReasoningEffortSelector.tsx
  - web/src/components/NewSession/OpencodeModelSelector.tsx
  - web/src/components/NewSession/opencodeModelsGate.ts
  - web/src/components/NewSession/opencodeModelsGate.test.ts
  - web/src/components/AssistantChat/StatusBar.tsx
  - web/src/components/AssistantChat/HappyComposer.tsx
  - web/src/components/AssistantChat/codexReasoningEffortOptions.ts
  - web/src/components/AssistantChat/codexReasoningEffortOptions.test.ts
  - web/src/components/AssistantChat/claudeEffortOptions.ts
  - web/src/components/AssistantChat/claudeEffortOptions.test.ts
  - web/src/components/AssistantChat/claudeModelOptions.ts
  - web/src/components/AssistantChat/claudeModelOptions.test.ts
  - web/src/components/AssistantChat/modelOptions.ts
  - web/src/components/AssistantChat/modelOptions.test.ts
  - web/src/components/AssistantChat/messages/CodexReviewCard.tsx
  - web/src/components/AssistantChat/messages/AssistantMessage.tsx
  - web/src/components/AssistantChat/messages/QueuedMessagesBar.tsx
  - web/src/components/AssistantChat/messages/MessageMetadata.test.ts
  - web/src/components/SessionChat.tsx
  - web/src/components/SessionList.tsx
  - web/src/components/SessionList.test.ts
  - web/src/components/SessionList.directory-action.test.tsx
autonomous: true
requirements_addressed: [CUT-05, REFA-01]
must_haves:
  truths:
    - "`web/src/components/NewSession/` contains no `claude` / `codex` / `gemini` / `opencode` literals; `AgentType` collapses to `'cursor'`; `AgentSelector` is deleted (single-agent UI has no choice)."
    - "Codex / Claude / Opencode effort + model selector files and their tests are deleted (verified unreachable post Slice 2a)."
    - "AssistantChat (`StatusBar`, `HappyComposer`, `messages/*`) no longer imports `getCodexCollaborationModeLabel` / `getCodexCollaborationModeOptions` / `CodexCollaborationMode`; collaboration-mode UI surface removed."
    - "`SessionList.tsx` `FLAVOR_BADGES` collapses to a single cursor row; default flavor falls back to `'cursor'`."
  artifacts:
    - path: "web/src/components/NewSession/types.ts"
      provides: "AgentType narrowed to 'cursor'; codex/claude effort + option arrays removed."
    - path: "web/src/components/AssistantChat/StatusBar.tsx"
      provides: "Status bar without codex collaboration / fast-mode / reasoning branches."
    - path: "web/src/components/AssistantChat/HappyComposer.tsx"
      provides: "Composer without collaboration-mode prop / codex options branch."
    - path: "web/src/components/SessionList.tsx"
      provides: "FlavorIcon + flavor-badge map collapsed to cursor."
  key_links:
    - from: "web/src/components/AssistantChat/HappyComposer.tsx"
      to: "shared/src/flavors.ts (getPermissionModeOptionsForFlavor)"
      via: "import"
      pattern: "getPermissionModeOptionsForFlavor"
---

<objective>
Slice 2b — collapse the new-session, assistant-chat, and session-list UI surface to Cursor. Delete the now-dead non-cursor selectors / effort option files / Codex review card; narrow `AgentType` to `'cursor'`; rewrite `index.tsx` to drop codex branches; rewrite `StatusBar.tsx` and `HappyComposer.tsx` to drop `getCodexCollaborationMode*` usage and `agentFlavor === 'codex'` branches; rewrite `SessionList.tsx` `FLAVOR_BADGES` to a single cursor row.

Purpose: leaves the AssistantChat / NewSession family Cursor-only so plan 04 (chat/lib/hooks/api) and plan 07 (shared delete) can proceed without UI consumers blocking the schema cut.
Output: NewSession + AssistantChat + SessionList trees free of all non-cursor literals and codex collaboration plumbing.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-CONTEXT.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-PATTERNS.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-01-PLAN.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-02-PLAN.md

@shared/src/flavors.ts
@web/src/components/NewSession/types.ts
@web/src/components/NewSession/index.tsx
@web/src/components/AssistantChat/StatusBar.tsx
@web/src/components/AssistantChat/HappyComposer.tsx
@web/src/components/SessionList.tsx

<interfaces>
From `shared/src/flavors.ts` (post Slice 1a):
- `getCapability(flavor, key)` lookup
- `isKnownFlavor(flavor)` type guard
- `getFlavorLabel(flavor)` (returns `'Cursor'` for cursor, `'Unknown'` otherwise)

From `shared/src/modes.ts`:
- `getPermissionModeOptionsForFlavor(flavor)` — KEEP using; internal already routes to capability table after Slice 1a's helper rewrite.
- `getCodexCollaborationModeLabel`, `getCodexCollaborationModeOptions`, `CodexCollaborationMode` — STILL exported until plan 07; this slice removes ALL imports of these symbols so plan 07 can delete them.

`AgentType` (web/src/components/NewSession/types.ts) currently `'claude' | 'codex' | 'cursor' | 'gemini' | 'opencode'`; this slice narrows it to `'cursor'`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead non-cursor selectors / effort + model option files; narrow `AgentType` and rewrite `NewSession/index.tsx` codex branches</name>
  <files>web/src/components/NewSession/AgentSelector.tsx, web/src/components/NewSession/preferences.ts, web/src/components/NewSession/preferences.test.ts, web/src/components/NewSession/types.ts, web/src/components/NewSession/types.test.ts, web/src/components/NewSession/index.tsx, web/src/components/NewSession/ClaudeEffortSelector.tsx, web/src/components/NewSession/ReasoningEffortSelector.tsx, web/src/components/NewSession/OpencodeModelSelector.tsx, web/src/components/NewSession/opencodeModelsGate.ts, web/src/components/NewSession/opencodeModelsGate.test.ts, web/src/components/AssistantChat/codexReasoningEffortOptions.ts, web/src/components/AssistantChat/codexReasoningEffortOptions.test.ts, web/src/components/AssistantChat/claudeEffortOptions.ts, web/src/components/AssistantChat/claudeEffortOptions.test.ts, web/src/components/AssistantChat/claudeModelOptions.ts, web/src/components/AssistantChat/claudeModelOptions.test.ts, web/src/components/AssistantChat/messages/CodexReviewCard.tsx</files>
  <read_first>
    - `web/src/components/NewSession/types.ts` (lines 1, 3–4, 22, 30 — `AgentType` union, `CodexReasoningEffort`, `ClaudeEffort` types, `CODEX_REASONING_EFFORT_OPTIONS`, `CLAUDE_EFFORT_OPTIONS` arrays)
    - `web/src/components/NewSession/index.tsx` (lines 107, 281–282, 361–364 — `agent === 'codex'` branches for codex models gate, reasoning effort, model selector)
    - `web/src/components/NewSession/AgentSelector.tsx` (line 17 — hardcoded 5-literal agent array; whole file deleted)
    - `web/src/components/NewSession/preferences.ts` (lines 6, 17 — `VALID_AGENTS` + default `'claude'`)
    - `web/src/components/NewSession/ClaudeEffortSelector.tsx` (line 13 — `if (props.agent !== 'claude') return null`)
    - `web/src/components/NewSession/ReasoningEffortSelector.tsx` (line 13 — `if (props.agent !== 'codex') return null`)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-PATTERNS.md` §"Codex* renderer files (deletion-candidate, file-delete)" lines 510–528 (NewSession deletion targets list)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md` §"D. web/" rows for `NewSession/*`, `AssistantChat/codexReasoningEffortOptions.ts`, `AssistantChat/claudeEffortOptions.ts`, `AssistantChat/claudeModelOptions.ts`, `AssistantChat/messages/CodexReviewCard.tsx`
  </read_first>
  <behavior>
    - Files DELETED whole (verified unreachable per RESEARCH §"D. web/" + Phase-1 carry-overs):
      - `web/src/components/NewSession/AgentSelector.tsx`
      - `web/src/components/NewSession/preferences.ts` (and `.test.ts`)
      - `web/src/components/NewSession/ClaudeEffortSelector.tsx`
      - `web/src/components/NewSession/ReasoningEffortSelector.tsx`
      - `web/src/components/NewSession/OpencodeModelSelector.tsx`
      - `web/src/components/NewSession/opencodeModelsGate.ts` (and `.test.ts`)
      - `web/src/components/AssistantChat/codexReasoningEffortOptions.ts` (and `.test.ts`)
      - `web/src/components/AssistantChat/claudeEffortOptions.ts` (and `.test.ts`)
      - `web/src/components/AssistantChat/claudeModelOptions.ts` (and `.test.ts`)
      - `web/src/components/AssistantChat/messages/CodexReviewCard.tsx` (and `.test.tsx` if present)
    - `NewSession/types.ts`: `AgentType` collapses from 5-literal union to `'cursor'`. Delete `CodexReasoningEffort`, `ClaudeEffort` type aliases and `CODEX_REASONING_EFFORT_OPTIONS`, `CLAUDE_EFFORT_OPTIONS` arrays + tests in `types.test.ts`.
    - `NewSession/index.tsx`: delete `agent === 'codex'` branches at lines 107 (codex models gate), 281–282 (reasoning effort), 361–364 (codex model selector). Drop imports of deleted components.
    - 4-space indent. No backward-compat stub.
  </behavior>
  <action>
    Delete files using the Delete tool. Edit `NewSession/types.ts` to narrow `AgentType` and remove obsolete type aliases / option arrays. Edit `NewSession/index.tsx` to remove imports of deleted components and remove every `agent === 'codex'` / `agent === 'claude'` / `agent === 'opencode'` branch — after deletion all that should remain is the cursor path. Run `cd web && bun typecheck` after each batch of deletes; fix follow-on errors by removing the importer (do not stub). 4-space indent.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun typecheck &amp;&amp; bun run test --run web/src/components/NewSession web/src/components/AssistantChat</automated>
  </verify>
  <done>
    All listed files deleted. `AgentType` narrowed to `'cursor'`. `NewSession/index.tsx` has no codex/claude/gemini/opencode branches. Tests for the affected sub-suites green.
  </done>
  <acceptance_criteria>
    - For each deletion-candidate file path above: `test ! -f &lt;path&gt;` exits 0.
    - `rg -n "AgentType\\s*=\\s*'cursor'" web/src/components/NewSession/types.ts` returns at least one hit OR `rg -n "type AgentType\\s*=\\s*'cursor'" web/src/components/NewSession/types.ts` returns one hit.
    - `rg -ni '\\b(claude|codex|gemini|opencode)\\b' web/src/components/NewSession/` returns zero hits.
    - `cd web && bun typecheck` exit code 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite `AssistantChat/StatusBar.tsx` + `HappyComposer.tsx` — drop `CodexCollaborationMode*` imports and `agentFlavor === 'codex'` branches; collapse to cursor</name>
  <files>web/src/components/AssistantChat/StatusBar.tsx, web/src/components/AssistantChat/HappyComposer.tsx, web/src/components/AssistantChat/modelOptions.ts, web/src/components/AssistantChat/modelOptions.test.ts, web/src/components/AssistantChat/messages/AssistantMessage.tsx, web/src/components/AssistantChat/messages/QueuedMessagesBar.tsx, web/src/components/AssistantChat/messages/MessageMetadata.test.ts</files>
  <read_first>
    - `web/src/components/AssistantChat/StatusBar.tsx` (imports lines 2, 5, 9; codex branches lines 114–128, 141, 189–205 — `formatCodexReasoningLabel`, `isCodexFastMode`, `displayCollaborationMode`, `codexReasoningLabel`, `codexFastMode`, `goalLabel`)
    - `web/src/components/AssistantChat/HappyComposer.tsx` (imports lines 1, 15, 48; `collaborationMode` prop + change handler at lines 64, 283, 291; `agentFlavor === 'codex' ? getCodexCollaborationModeOptions() : []` at line 474)
    - `web/src/components/AssistantChat/modelOptions.ts` (line 4 — `ModelOption = ClaudeComposerModelOption` alias) + `modelOptions.test.ts` (lines 6, 25 — `getModelOptionsForFlavor('claude')`)
    - `web/src/components/AssistantChat/messages/AssistantMessage.tsx`, `QueuedMessagesBar.tsx`, `MessageMetadata.test.ts` — flavor refs (read each for current call sites)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md` §"D. web/" StatusBar / HappyComposer / messages rows
  </read_first>
  <behavior>
    - `StatusBar.tsx`: drop imports `getCodexCollaborationModeLabel`, `isPermissionModeAllowedForFlavor` keep (capability lookup); delete `formatCodexReasoningLabel`, `isCodexFastMode` helpers; delete `displayCollaborationMode = props.agentFlavor === 'codex' ? ... : null` branch and the `codexReasoningLabel` / `codexFastMode` / `goalLabel` blocks. `agentFlavor` prop narrows to `'cursor' | undefined` or is removed.
    - `HappyComposer.tsx`: drop import `getCodexCollaborationModeOptions`; KEEP `getPermissionModeOptionsForFlavor` (it now routes to the capability table). Delete the `collaborationMode` prop, change handler, and `agentFlavor === 'codex' ? getCodexCollaborationModeOptions() : []` branch. Replace with `[]` constant; downstream UI of collaboration-mode picker dead-eliminates.
    - `modelOptions.ts`: rewrite `ModelOption` to either `CursorModelOption` (if a Cursor type exists) or a no-op alias `ModelOption = never`; delete `getModelOptionsForFlavor` per-flavor branches. Tests updated to cursor-only or deleted (no model UI ships until CURS-01 / Milestone 2).
    - `messages/AssistantMessage.tsx`, `QueuedMessagesBar.tsx`, `MessageMetadata.test.ts`: inspect each for `agentFlavor === 'codex' / 'claude'` branches and either delete the branch or rewrite to the cursor-only path; remove deleted `CodexReviewCard` import if present.
    - 4-space indent. No backward-compat shim.
  </behavior>
  <action>
    Edit each file in place. After dropping imports of `getCodexCollaborationMode*` / `CodexCollaborationMode` / `formatCodexReasoningLabel` / `isCodexFastMode`, run `cd web && bun typecheck`; fix every error by deleting the consumer code (not stubbing). For `modelOptions.ts`, narrow to a cursor-only export OR delete the file if no consumer remains (verify by `rg "from '.*modelOptions'" web/src/`). Test files: update assertions to cover only the cursor path; if the test file's reason for existence was per-flavor matrix, delete it. 4-space indent.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun typecheck &amp;&amp; bun run test --run web/src/components/AssistantChat</automated>
  </verify>
  <done>
    StatusBar / HappyComposer / modelOptions / messages files no longer import `CodexCollaborationMode*` or `getCodexCollaborationMode*`; `agentFlavor === 'codex'` branches gone. AssistantChat sub-suite green.
  </done>
  <acceptance_criteria>
    - `rg -n 'getCodexCollaborationMode|CodexCollaborationMode' web/src/components/AssistantChat/` returns zero hits.
    - `rg -n "agentFlavor\\s*===\\s*'codex'|agentFlavor\\s*!==\\s*'codex'" web/src/components/AssistantChat/` returns zero hits.
    - `rg -n "formatCodexReasoningLabel|isCodexFastMode" web/src/components/AssistantChat/` returns zero hits.
    - `cd web && bun typecheck` exit code 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rewrite `SessionChat.tsx` and `SessionList.tsx` flavor branches; collapse FLAVOR_BADGES</name>
  <files>web/src/components/SessionChat.tsx, web/src/components/SessionList.tsx, web/src/components/SessionList.test.ts, web/src/components/SessionList.directory-action.test.tsx</files>
  <read_first>
    - `web/src/components/SessionChat.tsx` (lines 7, 81, 132, 136, 139, 284, 494, 498, 518, 523 — `CodexCollaborationMode` import; `block.tool.name === 'CodexAgent'`; multiple `agentFlavor === 'codex'` / `!== 'codex'` branches; `handleCollaborationModeChange`)
    - `web/src/components/SessionList.tsx` (lines 491–520 — `FLAVOR_BADGES` 4-entry map + `FlavorIcon` `(flavor ?? 'claude').trim().toLowerCase()`)
    - `web/src/components/SessionList.test.ts` (line 92 — `flavor: 'codex'` fixture)
    - `web/src/components/SessionList.directory-action.test.tsx` (line 53 — `flavor: 'codex'` fixture)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md` §"D. web/" SessionChat + SessionList rows
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md` §"Surprises" #1 (FLAVOR_BADGES)
  </read_first>
  <behavior>
    - `SessionChat.tsx`: drop `CodexCollaborationMode` import, the `block.tool.name === 'CodexAgent'` rendering branch (already covered by the deleted ToolCard registry in plan 02 — confirm it's a separate inline branch here and delete), all `agentFlavor === 'codex'` and `agentFlavor !== 'codex'` branches, and `handleCollaborationModeChange` handler.
    - `SessionList.tsx`: collapse `FLAVOR_BADGES` to `{ cursor: <existing cursor badge> }`. Change `(flavor ?? 'claude').trim().toLowerCase()` to `(flavor ?? 'cursor')`. Drop any `'claude' | 'cursor'` union default param.
    - `SessionList.test.ts` + `SessionList.directory-action.test.tsx`: rewrite `flavor: 'codex'` fixtures to `flavor: 'cursor'`.
    - 4-space indent.
  </behavior>
  <action>
    Edit each file in place. `SessionChat.tsx` — delete codex-related blocks line by line, run typecheck after each, fix follow-on errors by deleting consumers. `SessionList.tsx` — collapse the badge map to a single cursor row and simplify the icon-resolution chain. Test fixtures: bulk-replace `'codex'` (and any `'claude'`/`'gemini'`/`'opencode'`) with `'cursor'`.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun typecheck &amp;&amp; bun run test --run web/src/components/SessionList web/src/components/SessionChat</automated>
  </verify>
  <done>
    `SessionChat.tsx` no longer references CodexCollaborationMode / CodexAgent / `agentFlavor === 'codex'`. `SessionList.tsx` `FLAVOR_BADGES` is a single cursor row. Tests pass.
  </done>
  <acceptance_criteria>
    - `rg -n 'CodexCollaborationMode|CodexAgent|handleCollaborationModeChange' web/src/components/SessionChat.tsx` returns zero hits.
    - `rg -n "agentFlavor\\s*[!=]==\\s*'(codex|claude|gemini|opencode)'" web/src/components/SessionChat.tsx` returns zero hits.
    - `rg -n "FLAVOR_BADGES" web/src/components/SessionList.tsx` shows a single cursor row OR the constant is deleted entirely.
    - `rg -n "flavor:\\s*'(codex|claude|gemini|opencode)'" web/src/components/SessionList.test.ts web/src/components/SessionList.directory-action.test.tsx` returns zero hits.
    - `cd web && bun typecheck` exit code 0.
    - Slice gate: `bun typecheck && bun run test` from repo root exit code 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wire (metadata.flavor, agent type from server) → React UI state | flavor strings from SSE may be historical non-cursor values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | NewSession agent type from URL/storage | mitigate | `AgentType` narrows to `'cursor'`; preferences storage deleted; tampered values rejected at type boundary (Zod schemas in plan 04 / 07) |
| T-05-03-02 | Information disclosure | SessionList rendering historical flavor | mitigate | FlavorIcon falls back to `'cursor'` (`?? 'cursor'`) for unknown values; no PII surface |
</threat_model>

<verification>
- `cd web && bun typecheck` green.
- `cd web && bun run test --run web/src/components/{NewSession,AssistantChat,SessionList,SessionChat}` green.
- `bun typecheck && bun run test` from repo root green (slice hard gate per directive #8).
- `rg -ni '\\b(claude|codex|gemini|opencode)\\b' web/src/components/{NewSession,AssistantChat,SessionList.tsx,SessionChat.tsx}` returns zero hits.
</verification>

<success_criteria>
- All NewSession non-cursor selectors / preferences / effort + model option files deleted.
- `AgentType` narrowed to `'cursor'`.
- AssistantChat StatusBar / HappyComposer / messages / modelOptions free of CodexCollaborationMode + codex flavor branches.
- SessionChat no longer carries codex collaboration / CodexAgent rendering branches.
- SessionList FLAVOR_BADGES single-row.
- Slice-gate `bun typecheck && bun run test` green.
</success_criteria>

<output>
Create `.planning/phases/05-flavor-consolidation-capability-abstraction/05-03-SUMMARY.md` when done. Record: deleted files (with LoC), AgentType narrow line, StatusBar/HappyComposer line ranges removed, SessionList FLAVOR_BADGES collapse.
</output>
