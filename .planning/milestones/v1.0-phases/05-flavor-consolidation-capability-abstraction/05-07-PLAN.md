---
phase: 05-flavor-consolidation-capability-abstraction
plan: 07
type: execute
wave: 7
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06]
files_modified:
  - shared/src/modes.ts
  - shared/src/flavors.ts
  - shared/src/flavors.test.ts
  - shared/src/schemas.ts
  - shared/src/resume.ts
  - shared/src/resume.test.ts
  - shared/src/types.ts
autonomous: true
requirements_addressed: [CUT-05, REFA-01]
must_haves:
  truths:
    - "`AgentFlavor` is the literal type `'cursor'` (single-line union; D-69, SC#1)."
    - "`shared/src/modes.ts` no longer exports `CLAUDE_PERMISSION_MODES`, `CODEX_PERMISSION_MODES`, `CODEX_COLLABORATION_MODES`, `GEMINI_PERMISSION_MODES`, `OPENCODE_PERMISSION_MODES`, `CodexCollaborationMode*`, `getCodexCollaborationMode*`, or `ClaudePermissionMode` / `CodexPermissionMode` / `GeminiPermissionMode` / `OpencodePermissionMode` types (D-77)."
    - "`PERMISSION_MODES` collapses to Cursor's four modes (`['default', 'plan', 'ask', 'yolo']`); `PermissionMode` is an alias of `CursorPermissionMode` (D-78)."
    - "`shared/src/flavors.ts` `FLAVOR_CAPS` and `FLAVOR_LABELS` each contain a single `cursor` row; `isCodexFamilyFlavor` is deleted (D-82)."
    - "`shared/src/schemas.ts` no longer references `CodexCollaborationModeSchema` and `SessionSchema.collaborationMode` is removed; `shared/src/resume.ts` `AgentFlavorSchema = z.enum(['cursor'])` (or `z.literal('cursor')`) and `LocalResumeTargetSchema.collaborationMode` is removed."
    - "`shared/src/types.ts` re-export list no longer mentions `ClaudePermissionMode`, `CodexCollaborationMode`, `CodexCollaborationModeOption`, or any non-cursor permission-mode type."
    - "`shared/src/flavors.test.ts` two `TODO(plan 05-07-PLAN.md)` cases (cases 14 and 17) tightened: `getFlavorLabel('claude') === 'Unknown'` and `isKnownFlavor('claude') === false`."
    - "`AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const` retained at `shared/src/modes.ts:6` with JSDoc anchor (D-81 — wire-protocol legacy literal owned by Phase 7)."
  artifacts:
    - path: "shared/src/modes.ts"
      provides: "AgentFlavor narrowed to 'cursor'; cursor-only PERMISSION_MODES + helpers; AGENT_MESSAGE_PAYLOAD_TYPE preserved."
    - path: "shared/src/flavors.ts"
      provides: "Single-row FLAVOR_CAPS + FLAVOR_LABELS; isCodexFamilyFlavor deleted."
    - path: "shared/src/schemas.ts"
      provides: "Wire schemas free of CodexCollaborationModeSchema and SessionSchema.collaborationMode."
    - path: "shared/src/resume.ts"
      provides: "AgentFlavorSchema narrowed; collaborationMode field removed."
  key_links:
    - from: "shared/src/modes.ts (AgentFlavor)"
      to: "shared/src/flavors.ts (FLAVOR_CAPS, FLAVOR_LABELS keys)"
      via: "type"
      pattern: "Record<AgentFlavor"
---

<objective>
Slice 1b — close the door. Now that every cli/hub/web consumer has stopped importing the deletion-target shared symbols (plans 02–06), narrow `AgentFlavor = 'cursor'`, delete the four non-cursor `*_PERMISSION_MODES` constants + types, delete the entire `CodexCollaborationMode*` surface, collapse `PERMISSION_MODES` / `PERMISSION_MODE_LABELS` / `PERMISSION_MODE_TONES` to Cursor's four modes (D-78 condition met per RESEARCH §"Surprises" #9), collapse `FLAVOR_CAPS` and `FLAVOR_LABELS` to single cursor rows, delete `isCodexFamilyFlavor`, narrow `AgentFlavorSchema` and remove `SessionSchema.collaborationMode` + `LocalResumeTargetSchema.collaborationMode`, prune `shared/src/types.ts` re-exports. Tighten the two TODO cases in `shared/src/flavors.test.ts` (14, 17).

Preserve: `AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const` at `shared/src/modes.ts:6` with a JSDoc anchor that the source guard's post-filter pins on (D-81 / D-85). Preserve: `CURSOR_PERMISSION_MODES`, `CursorPermissionMode`, `getFlavorLabel` (`'Unknown'` fallback), `isKnownFlavor`, `getCapabilities`, `getCapability`, `hasCapability`, `supportsModelChange`, `supportsEffort`, `Capabilities` (legacy const if still used) — all per D-75 / D-79 / D-83.

Purpose: lands SC#1 (`AgentFlavor` narrows to `'cursor'`). After this slice, the only remaining work is the guard-script collapse + final ripgrep verification (plan 08).
Output: `shared/` Cursor-only at the type and Zod level; full repo `bun typecheck && bun run test` green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-CONTEXT.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-PATTERNS.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-01-PLAN.md

@shared/src/modes.ts
@shared/src/flavors.ts
@shared/src/flavors.test.ts
@shared/src/schemas.ts
@shared/src/resume.ts
@shared/src/types.ts

<interfaces>
Public exports KEPT from `shared/src/modes.ts`:
- `AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const` (with JSDoc anchor for guard post-filter — D-81)
- `CURSOR_PERMISSION_MODES`, `CursorPermissionMode` (D-79)
- `PERMISSION_MODES = CURSOR_PERMISSION_MODES` (or literal `['default','plan','ask','yolo'] as const`)
- `PermissionMode` = `CursorPermissionMode` (D-78)
- `PERMISSION_MODE_LABELS`, `PERMISSION_MODE_TONES` (4-row maps)
- `AgentFlavor = 'cursor'` (D-69)
- `getPermissionModesForFlavor(flavor)`, `getPermissionModeOptionsForFlavor(flavor)`, `isPermissionModeAllowedForFlavor(flavor, mode)` — bodies route through `getCapability(flavor, 'permissionModes')` (D-77)

Public exports KEPT from `shared/src/flavors.ts`:
- `FlavorCapabilities`, `FLAVOR_CAPS` (single cursor row), `FLAVOR_LABELS` (single cursor row)
- `Capabilities`, `Capability` (compat — keep if still referenced by callers; otherwise delete)
- `isKnownFlavor`, `hasCapability`, `getFlavorLabel`, `supportsModelChange`, `supportsEffort`, `getCapabilities`, `getCapability`

Public exports DELETED from `shared/src/modes.ts`:
- `CLAUDE_PERMISSION_MODES`, `ClaudePermissionMode`
- `CODEX_PERMISSION_MODES`, `CodexPermissionMode`
- `CODEX_COLLABORATION_MODES`, `CodexCollaborationMode`
- `CodexCollaborationModeOption`, `CODEX_COLLABORATION_MODE_LABELS`
- `getCodexCollaborationModeLabel`, `getCodexCollaborationModeOptions`
- `GEMINI_PERMISSION_MODES`, `GeminiPermissionMode`
- `OPENCODE_PERMISSION_MODES`, `OpencodePermissionMode`

Public exports DELETED from `shared/src/flavors.ts`:
- `isCodexFamilyFlavor`
- All non-cursor rows of `FLAVOR_CAPS` and `FLAVOR_LABELS` (placeholder rows added in slice 1a)

Public exports DELETED / NARROWED from `shared/src/schemas.ts`, `shared/src/resume.ts`, `shared/src/types.ts`:
- `CodexCollaborationModeSchema` (schemas.ts)
- `SessionSchema.collaborationMode` field (schemas.ts)
- `AgentFlavorSchema` narrowed to `z.enum(['cursor'])` or `z.literal('cursor')` (resume.ts)
- `LocalResumeTargetSchema.collaborationMode` field (resume.ts)
- `ClaudePermissionMode`, `CodexCollaborationMode`, `CodexCollaborationModeOption` re-exports (types.ts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow `AgentFlavor` and delete non-cursor permission-mode + collaboration-mode constants/types/helpers in `shared/src/modes.ts`</name>
  <files>shared/src/modes.ts</files>
  <read_first>
    - `shared/src/modes.ts` (whole file — every line listed in 05-RESEARCH §"A. shared/" rows for `modes.ts`; line 6 `AGENT_MESSAGE_PAYLOAD_TYPE` — KEEP; lines 8–21 — non-cursor `*_PERMISSION_MODES` constants/types — DELETE; lines 23–24 `CURSOR_PERMISSION_MODES` — KEEP; lines 26–36 `PERMISSION_MODES` — COLLAPSE to cursor; line 38 `AgentFlavor` — NARROW; lines 40–62 labels/tones — COLLAPSE; lines 70–78 codex collab options/labels — DELETE; lines 88–90 `getCodexCollaborationModeLabel` — DELETE; lines 92–106 `getPermissionModesForFlavor` — REWRITE on capability lookup; lines 108–125 — narrow internals; line 120–125 `getCodexCollaborationModeOptions` — DELETE)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-PATTERNS.md` §"`shared/src/modes.ts`" lines 144–240 (preservation + delete + helper rewrite patterns; circular dependency caveat at line 237)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md` §"A. shared/" `modes.ts` rows; §"Pattern: TypeScript-compiler-driven call-site discovery"
  </read_first>
  <behavior>
    - Line 6: KEEP `AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const`. Add or extend JSDoc: explicit "wire-protocol legacy literal — owned by Phase 7; do not change" anchor that the guard post-filter (`grep -v "AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const"`) targets.
    - Delete `CLAUDE_PERMISSION_MODES`, `ClaudePermissionMode`, `CODEX_PERMISSION_MODES`, `CodexPermissionMode`, `CODEX_COLLABORATION_MODES`, `CodexCollaborationMode`, `GEMINI_PERMISSION_MODES`, `GeminiPermissionMode`, `OPENCODE_PERMISSION_MODES`, `OpencodePermissionMode` (lines 8–21).
    - KEEP `CURSOR_PERMISSION_MODES` and `CursorPermissionMode` verbatim (D-79).
    - Collapse `PERMISSION_MODES` to literal `['default', 'plan', 'ask', 'yolo'] as const` (or `CURSOR_PERMISSION_MODES`); `PermissionMode = CursorPermissionMode` (D-78).
    - Narrow `AgentFlavor` to `'cursor'` (single-line literal type — line 38).
    - Collapse `PERMISSION_MODE_LABELS` and `PERMISSION_MODE_TONES` from 8 entries to 4 (`Record<PermissionMode, string>`).
    - Delete `CodexCollaborationModeOption`, `CODEX_COLLABORATION_MODE_LABELS`, `getCodexCollaborationModeLabel`, `getCodexCollaborationModeOptions` (lines 70–78, 88–90, 120–125).
    - Rewrite `getPermissionModesForFlavor(flavor)` body to `return getCapability(flavor, 'permissionModes') ?? CURSOR_PERMISSION_MODES`. The circular-dependency caveat (PATTERNS line 237): `flavors.ts` imports `CURSOR_PERMISSION_MODES` from `modes.ts`, and `modes.ts` would now import `getCapability` from `flavors.ts`. Resolve by inlining the cursor default in the fallback (no `flavors.ts` import in `modes.ts`): `return getCapability(flavor, 'permissionModes') ?? (CURSOR_PERMISSION_MODES as readonly PermissionMode[])` — but this still requires `getCapability`. **Decision:** the cleanest fix is to rewrite `getPermissionModesForFlavor` without using `getCapability`: since `AgentFlavor` is now `'cursor'`, the body collapses to `return CURSOR_PERMISSION_MODES`. Document this in the action.
    - `getPermissionModeOptionsForFlavor` body collapses to map over `CURSOR_PERMISSION_MODES`.
    - `isPermissionModeAllowedForFlavor(flavor, mode)` body collapses to `return CURSOR_PERMISSION_MODES.includes(mode)`.
    - 4-space indent. TypeScript strict.
  </behavior>
  <action>
    Edit `shared/src/modes.ts` in place. Line 6: extend JSDoc with the anchor phrase "wire-protocol legacy literal — owned by Phase 7; do not change" so the guard post-filter has a stable line to whitelist. Delete each non-cursor permission-mode and collaboration-mode block. Collapse `PERMISSION_MODES` / `PermissionMode` / `PERMISSION_MODE_LABELS` / `PERMISSION_MODE_TONES` to cursor's four modes. Narrow `AgentFlavor` to `export type AgentFlavor = 'cursor'`. Rewrite the three `getPermissionMode*ForFlavor` helpers to bodies that return cursor-only values directly (avoiding the modes ↔ flavors circular import). Run `cd shared && bun typecheck`. Do NOT touch `flavors.ts` in this task. 4-space indent.
  </action>
  <verify>
    <automated>cd shared &amp;&amp; bun typecheck</automated>
  </verify>
  <done>
    `shared/src/modes.ts` retains only Cursor-relevant constants + helpers + the wire-literal `AGENT_MESSAGE_PAYLOAD_TYPE` (with JSDoc anchor). All non-cursor symbols deleted. Typecheck green.
  </done>
  <acceptance_criteria>
    - `rg -n "(CLAUDE|CODEX|GEMINI|OPENCODE)_PERMISSION_MODES" shared/src/modes.ts` returns zero hits.
    - `rg -n "CodexCollaborationMode|getCodexCollaboration" shared/src/modes.ts` returns zero hits.
    - `rg -n "export type AgentFlavor\\s*=\\s*'cursor'" shared/src/modes.ts` returns exactly one hit.
    - `rg -n "AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const" shared/src/modes.ts` returns exactly one hit.
    - `rg -n "wire-protocol legacy literal" shared/src/modes.ts` returns at least one hit (JSDoc anchor for guard post-filter).
    - `cd shared && bun typecheck` exit code 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Collapse `FLAVOR_CAPS` / `FLAVOR_LABELS` to single cursor row, delete `isCodexFamilyFlavor`, tighten test cases 14 + 17</name>
  <files>shared/src/flavors.ts, shared/src/flavors.test.ts</files>
  <read_first>
    - `shared/src/flavors.ts` (post Slice 1a — placeholder non-cursor rows; `isCodexFamilyFlavor` still present)
    - `shared/src/flavors.test.ts` (post Slice 1a — cases 14 and 17 currently assert legacy behavior with `TODO(plan 05-07-PLAN.md)` markers)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-PATTERNS.md` §"`shared/src/flavors.ts`" lines 29–141 (Set→Record done in slice 1a; this slice removes the placeholder rows and `isCodexFamilyFlavor`)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-01-PLAN.md` §"<interfaces>" (notes the placeholder row strategy and the test TODOs)
  </read_first>
  <behavior>
    - `flavors.ts`: now that `AgentFlavor` narrows to `'cursor'`, `FLAVOR_CAPS: Record<AgentFlavor, FlavorCapabilities>` and `FLAVOR_LABELS: Record<AgentFlavor, string>` only need a `cursor` row — delete the `claude` / `codex` / `gemini` / `opencode` placeholder rows added in slice 1a.
    - Delete `isCodexFamilyFlavor` function (D-82) — it has zero remaining consumers after plan 02 rewrote `PermissionFooter.tsx`.
    - Optionally delete the legacy `Capabilities` const and `Capability` string-union if no consumer remains (verify via `rg "Capabilities\\.|Capability\\b" cli/src/ hub/src/ web/src/ shared/src/`); keep if any consumer remains, otherwise delete.
    - `flavors.test.ts`: tighten case 14 to `expect(getFlavorLabel('claude')).toBe('Unknown')` and case 17 to `expect(isKnownFlavor('claude')).toBe(false)`. Remove the two `TODO(plan 05-07-PLAN.md)` comments.
    - 4-space indent.
  </behavior>
  <action>
    Edit `shared/src/flavors.ts` to remove the four placeholder rows from `FLAVOR_CAPS` and `FLAVOR_LABELS`, leaving only the `cursor` row. Delete the `isCodexFamilyFlavor` export. Run `rg "Capabilities\\.(ModelChange|Effort)|Capability\\b" cli/src/ hub/src/ web/src/ shared/src/` — if zero hits outside `shared/src/flavors.ts`, delete the legacy `Capabilities` const and `Capability` type alias too; otherwise keep them. Update `flavors.test.ts` cases 14 and 17 to the post-Slice-1b expectations and remove the two `TODO(plan 05-07-PLAN.md)` comments. 4-space indent.
  </action>
  <verify>
    <automated>cd shared &amp;&amp; bun typecheck &amp;&amp; bun run test flavors.test.ts</automated>
  </verify>
  <done>
    `flavors.ts` has single-row `FLAVOR_CAPS` + `FLAVOR_LABELS`; `isCodexFamilyFlavor` deleted. Test cases 14 + 17 tightened; TODO comments removed. Tests green.
  </done>
  <acceptance_criteria>
    - `rg -n "claude:\\s*\\{|codex:\\s*\\{|gemini:\\s*\\{|opencode:\\s*\\{" shared/src/flavors.ts` returns zero hits.
    - `rg -n "isCodexFamilyFlavor" shared/src/flavors.ts shared/src/types.ts` returns zero hits.
    - `rg -n "TODO\\(plan 05-07-PLAN\\.md\\)" shared/src/flavors.test.ts` returns zero hits.
    - `rg -n "isKnownFlavor\\('claude'\\)" shared/src/flavors.test.ts` shows the assertion equals `false`.
    - `cd shared && bun typecheck` exit code 0.
    - `cd shared && bun run test flavors.test.ts` exit code 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Narrow Zod `AgentFlavorSchema`, delete `CodexCollaborationModeSchema`, remove `collaborationMode` fields, prune `shared/src/types.ts` re-exports</name>
  <files>shared/src/schemas.ts, shared/src/resume.ts, shared/src/resume.test.ts, shared/src/types.ts</files>
  <read_first>
    - `shared/src/schemas.ts` (line 2 — `import { CODEX_COLLABORATION_MODES, PERMISSION_MODES }`; line 5 — `CodexCollaborationModeSchema`; line 50 — `MetadataSchema.flavor: z.string().nullish()` (KEEP per RESEARCH §"Wire-layer narrow safety"); line 203 — `SessionSchema.collaborationMode`)
    - `shared/src/resume.ts` (line 2 — `import { CodexCollaborationModeSchema }`; line 4 — `AgentFlavorSchema = z.enum([...])`; line 20 — `collaborationMode: CodexCollaborationModeSchema.optional()`)
    - `shared/src/resume.test.ts` (lines 10, 31 — `flavor: 'codex'` / `'claude'` fixtures)
    - `shared/src/types.ts` (line 21 — re-export `AGENT_MESSAGE_PAYLOAD_TYPE`; lines 23–32 — re-exports of deleted types; line 34 — `ClaudeModelPreset`)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md` §"A. shared/" rows for `schemas.ts`, `resume.ts`, `types.ts`; §"Wire-layer narrow safety analysis" §1, §2, §5 (MetadataSchema.flavor stays z.string(); AgentFlavorSchema narrow safe; collaborationMode delete safe)
    - Assumption A6 (RESEARCH §"Assumptions Log") — confirm no Cursor consumer of `ClaudeModelPreset` before deleting.
  </read_first>
  <behavior>
    - `schemas.ts`: drop `CODEX_COLLABORATION_MODES` from import; delete `CodexCollaborationModeSchema` (line 5); KEEP `PERMISSION_MODES` import and `MetadataSchema.flavor: z.string().nullish()` (RESEARCH explicitly says do not narrow this Zod field). Delete `SessionSchema.collaborationMode` field (line 203 — RESEARCH §"Wire-layer narrow safety" §5 confirms safe).
    - `resume.ts`: drop `CodexCollaborationModeSchema` import (line 2); narrow `AgentFlavorSchema` to `z.enum(['cursor'])` (or `z.literal('cursor')`) — RESEARCH §"Wire-layer narrow safety" §2 confirms safe. Delete `LocalResumeTargetSchema.collaborationMode` field (line 20).
    - `resume.test.ts`: rewrite `flavor: 'codex'` / `'claude'` fixtures to `flavor: 'cursor'`.
    - `types.ts`: drop re-exports of `ClaudePermissionMode`, `CodexCollaborationMode`, `CodexCollaborationModeOption`, and any other non-cursor permission-mode type (lines 23–32). KEEP `AGENT_MESSAGE_PAYLOAD_TYPE` re-export (line 21). For `ClaudeModelPreset` (line 34): verify Assumption A6 by `rg "ClaudeModelPreset" cli/src/ hub/src/ web/src/`; if zero remaining consumers, delete the re-export and consider deleting `shared/src/models.ts` exports (RESEARCH defers this to Phase 12 if uncertain — KEEP `models.ts` itself untouched here per scope).
    - 4-space indent.
  </behavior>
  <action>
    Edit each file in place. After narrowing `AgentFlavorSchema` and deleting `CodexCollaborationModeSchema`, run `cd shared && bun typecheck` then `bun typecheck && bun run test` from repo root — by this point every consumer (plans 02–06) has migrated, so the typecheck must be green. If a hub or web consumer surfaces a missed reference, treat it as a regression: do not stub; trace the import and delete or narrow at the consumer (cross-plan correction). Verify Assumption A6 before deleting the `ClaudeModelPreset` re-export. 4-space indent.
  </action>
  <verify>
    <automated>cd shared &amp;&amp; bun typecheck &amp;&amp; bun run test &amp;&amp; cd .. &amp;&amp; bun typecheck &amp;&amp; bun run test</automated>
  </verify>
  <done>
    Wire schemas (`schemas.ts`, `resume.ts`) free of `CodexCollaborationModeSchema` and `collaborationMode` fields; `AgentFlavorSchema` narrowed; `types.ts` re-exports pruned. Full repo typecheck + tests green.
  </done>
  <acceptance_criteria>
    - `rg -n "CodexCollaborationModeSchema|CodexCollaborationMode\\b" shared/src/schemas.ts shared/src/resume.ts shared/src/types.ts` returns zero hits.
    - `rg -n "collaborationMode" shared/src/schemas.ts shared/src/resume.ts` returns zero hits.
    - `rg -n "AgentFlavorSchema\\s*=" shared/src/resume.ts` shows `z.enum(['cursor'])` or `z.literal('cursor')`.
    - `rg -n "MetadataSchema.flavor:\\s*z.string\\(\\)" shared/src/schemas.ts` returns at least one hit (KEEP — narrow safety).
    - `rg -n "ClaudePermissionMode|CodexCollaborationMode|CodexCollaborationModeOption" shared/src/types.ts` returns zero hits.
    - `bun typecheck && bun run test` from repo root exit code 0 (slice gate).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zod schemas (`MetadataSchema`, `AgentFlavorSchema`, `SessionSchema`, `LocalResumeTargetSchema`) → wire / SSE / SQLite | historical records may carry deleted enum values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | historical metadata.flavor (z.string) | mitigate | `MetadataSchema.flavor: z.string().nullish()` UNCHANGED — accepts any string; consumers compare via `=== 'cursor'` (D-76 graceful degrade); RESEARCH §"Wire-layer narrow safety" §1 + §3 confirm safe |
| T-05-07-02 | Tampering | historical resume payload with non-cursor flavor | mitigate | `AgentFlavorSchema` narrowed to `z.enum(['cursor'])` rejects non-cursor; per AGENTS.md "no backward compatibility" — user starts a new session; resume payloads do not persist across sessions (RESEARCH §"Wire-layer narrow safety" §2) |
| T-05-07-03 | Tampering | SQLite row with `collaborationMode: 'plan'` | mitigate | Field deleted from `SessionSchema`; Zod 4 tolerates unknown keys by default — verified by RESEARCH §"Wire-layer narrow safety" §5 + Assumption A8 (no `.strict()` in `shared/src/`) |
</threat_model>

<verification>
- `cd shared && bun typecheck` green.
- `bun typecheck && bun run test` from repo root green (slice hard gate per directive #8).
- `rg -ni '\\b(claude|codex|gemini|opencode)\\b' shared/src/ | rg -v "AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const|wire-protocol legacy literal"` returns zero hits.
- `rg -n "isCodexFamilyFlavor|CodexCollaborationMode|(CLAUDE|CODEX|GEMINI|OPENCODE)_PERMISSION_MODES" cli/src hub/src web/src shared/src` returns zero hits.
</verification>

<success_criteria>
- `AgentFlavor = 'cursor'` (SC#1).
- All non-cursor permission-mode + collaboration-mode + family-flavor symbols deleted from `shared/`.
- `MetadataSchema.flavor` stays wide (Zod string); `AgentFlavorSchema` narrowed; `SessionSchema.collaborationMode` and `LocalResumeTargetSchema.collaborationMode` deleted.
- `flavors.test.ts` cases 14, 17 tightened; TODOs removed.
- `AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const` retained at `shared/src/modes.ts:6` with JSDoc anchor (D-81).
- Slice-gate `bun typecheck && bun run test` green.
</success_criteria>

<output>
Create `.planning/phases/05-flavor-consolidation-capability-abstraction/05-07-SUMMARY.md` when done. Record: list of deleted shared symbols, AgentFlavor narrow line, schemas/resume field deletions, types.ts re-export changes, A6 verification result for `ClaudeModelPreset`, and confirmation that the JSDoc anchor at `modes.ts:6` exists.
</output>
