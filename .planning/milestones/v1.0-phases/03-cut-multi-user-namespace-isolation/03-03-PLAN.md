---
phase: 03-cut-multi-user-namespace-isolation
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - hub/src/web/routes/auth.ts
  - hub/src/web/middleware/auth.ts
  - hub/src/web/routes/cli.ts
  - hub/src/web/routes/guards.ts
  - hub/src/web/routes/sessions.ts
  - hub/src/web/routes/machines.ts
  - hub/src/web/routes/events.ts
  - hub/src/web/routes/push.ts
  - hub/src/web/routes/sessions.test.ts
  - hub/src/sse/sseManager.ts
  - hub/src/sse/sseManager.test.ts
  - hub/src/sync/eventPublisher.ts
  - hub/src/sync/syncEngine.ts
autonomous: true
requirements: [CUT-09]
requirements_addressed: [CUT-09]
user_setup: []
must_haves:
  truths:
    - "Web auth signs/verifies `{ uid }` only, `WebAppEnv` exposes no namespace, and every Hono route/guard callsite stops reading `c.get('namespace')` in this same plan (D-35, D-36)."
    - "Events route subscription shape and SSEManager subscription/filtering shape are cleaned up together, preserving all/sessionId/machineId relevance filters (D-43)."
    - "EventPublisher constructor cleanup and SyncEngine constructor/callsite cleanup happen together; events are no longer enriched with namespace before broadcast (D-42)."
    - "Route Zod validation, session/machine relevance checks, and active-session checks remain intact."
    - "`bun typecheck && bun run test` exits 0 after this plan commit (D-47)."
  artifacts:
    - path: "hub/src/web/routes/guards.ts"
      provides: "Owner-only route access guards"
      contains: "requireSessionFromParam"
    - path: "hub/src/web/middleware/auth.ts"
      provides: "Namespace-free WebAppEnv and JWT validation"
      contains: "WebAppEnv"
    - path: "hub/src/sse/sseManager.ts"
      provides: "Namespace-free SSE subscription shape with relevance filters"
      contains: "shouldSend"
    - path: "hub/src/sync/eventPublisher.ts"
      provides: "Namespace-free event publishing"
      contains: "emit"
  key_links:
    - from: "hub/src/web/routes/guards.ts"
      to: "hub/src/sync/syncEngine.ts"
      via: "owner-only session/machine methods"
      pattern: "resolveSessionAccess"
    - from: "hub/src/web/routes/events.ts"
      to: "hub/src/sse/sseManager.ts"
      via: "namespace-free subscription object"
      pattern: "subscribe"
    - from: "hub/src/sync/syncEngine.ts"
      to: "hub/src/sync/eventPublisher.ts"
      via: "constructor without namespace resolver"
      pattern: "new EventPublisher"
---

<objective>
Move web auth/routes, event publishing, and SSE subscriptions onto namespace-free owner-only contracts.

Purpose: Continue the cut as a single atomic web/realtime contract slice after Plan 03-02 established owner-only APIs. WebAppEnv narrowing cannot be split from Hono `c.get('namespace')` cleanup, the events route cannot be split from SSEManager subscription-shape cleanup, and EventPublisher constructor cleanup cannot be split from SyncEngine construction per D-47.
Output: `{ uid }` web JWTs, namespace-free WebAppEnv, migrated Hono routes/guards, namespace-free SSE subscription shape, namespace-free EventPublisher/SyncEngine construction, and full typecheck/test green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-RESEARCH.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-01-SUMMARY.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-02-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>03-03-01: Migrate web auth, routes, and guards atomically</name>
  <files>hub/src/web/routes/auth.ts, hub/src/web/middleware/auth.ts, hub/src/web/routes/cli.ts, hub/src/web/routes/guards.ts, hub/src/web/routes/sessions.ts, hub/src/web/routes/machines.ts, hub/src/web/routes/events.ts, hub/src/web/routes/push.ts, hub/src/web/routes/sessions.test.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-35, D-36, D-39, D-43, D-46, D-47)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md sections for auth middleware and `hub/src/web/routes/*.ts`
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-02-SUMMARY.md
  </read_first>
  <behavior>
    - `/api/auth` signs JWT payload `{ uid }` only.
    - `WebAppEnv` exposes `userId` only; auth middleware does not parse or set namespace.
    - Route guards call `resolveSessionAccess(sessionId)` and `getMachine(machineId)` without namespace arguments.
    - List/read/write routes do not call `c.get('namespace')`.
    - Session active checks and 404/409 response conventions remain unchanged where still applicable.
    - Events route subscribes by all/sessionId/machineId only; SSEManager internals are migrated in Task 03-03-02 of this same plan.
  </behavior>
  <action>Narrow `hub/src/web/middleware/auth.ts` to `{ uid }` JWT validation and `WebAppEnv.Variables.userId` only, and update `/api/auth` signing accordingly. In the same task, update every Hono route/guard namespace callsite listed in `<files>` to use Plan 03-02 owner-only SyncEngine APIs. Remove namespace reads, `c.set('namespace')`, `c.get('namespace')`, and access-denied because namespace mismatch. Preserve request/query/body Zod validation, sort order, active-session checks, and existing status conventions. This task intentionally has 9 files because splitting WebAppEnv narrowing from route cleanup breaks typecheck under D-47.</action>
  <verify>
    <automated>bun test hub/src/web/routes/sessions.test.ts && ! rg -n 'ns:|c\.set\('"'"'namespace'"'"'|c\.get\('"'"'namespace'"'"'|parsedToken\.namespace|parsedToken\.baseToken|Session access denied|Machine access denied' hub/src/web/routes hub/src/web/middleware/auth.ts && bun typecheck && bun run test</automated>
  </verify>
  <done>Web auth, WebAppEnv, route guards, and route callsites are namespace-free and route tests/full suite are green.</done>
</task>

<task type="auto" tdd="true">
  <name>03-03-02: Collapse events route, SSEManager, EventPublisher, and SyncEngine together</name>
  <files>hub/src/web/routes/events.ts, hub/src/sse/sseManager.ts, hub/src/sse/sseManager.test.ts, hub/src/sync/eventPublisher.ts, hub/src/sync/syncEngine.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-42, D-43, D-47)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md sections for events route, EventPublisher, SSEManager, and SyncEngine
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-02-SUMMARY.md
  </read_first>
  <behavior>
    - Events route passes subscription objects without namespace.
    - SSEManager subscription state contains no namespace and keeps all/sessionId/machineId relevance checks.
    - EventPublisher emits the provided event directly and no longer accepts a namespace resolver.
    - SyncEngine constructs EventPublisher without a namespace resolver and deletes `resolveNamespace`.
  </behavior>
  <action>Rewrite `hub/src/web/routes/events.ts` subscription creation and `hub/src/sse/sseManager.ts` subscription state in one atomic change so the route never subscribes with namespace to a manager that no longer accepts namespace. Remove EventPublisher namespace resolver injection and event enrichment, and update `hub/src/sync/syncEngine.ts` construction/callsite cleanup in the same task. Preserve SSE `message-received` sessionId filtering, machineId/sessionId detail filtering, `all`, and global `connection-changed` behavior. This co-location is required by D-42/D-43/D-47 and prevents a transient typecheck/test break.</action>
  <verify>
    <automated>bun test hub/src/sse/sseManager.test.ts && ! rg -n 'resolveNamespace|eventNamespace|subscription\.namespace|connection\.namespace|namespace,' hub/src/web/routes/events.ts hub/src/sse hub/src/sync/eventPublisher.ts hub/src/sync/syncEngine.ts && bun typecheck && bun run test</automated>
  </verify>
  <done>Events route, SSEManager, EventPublisher, and SyncEngine no longer carry namespace and the repository remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web routes -> SyncEngine | Authenticated browser route requests access session and machine state. |
| EventPublisher -> SSE clients | Server-side events cross to browser EventSource clients. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | web route payload handlers | mitigate | Preserve Zod validation while removing namespace arguments. |
| T-03-07 | Information Disclosure | route access checks and SSE filtering | mitigate | Preserve existence, active-state, and sessionId/machineId relevance checks; remove only obsolete tenant filtering. |
| T-03-08 | Information Disclosure | `hub/src/sync/eventPublisher.ts`, `hub/src/sse/sseManager.ts` | mitigate | Broadcast raw namespace-free events and keep relevance filtering in SSEManager. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs are required; stop for package audit if one appears. |
</threat_model>

<verification>
- Route and SSE targeted tests exit 0.
- Focused web/realtime `rg` gates show no migrated callsite namespace access.
- `bun typecheck && bun run test` exits 0 after the plan commit.
</verification>

<success_criteria>
- D-35, D-36, D-39, D-42, and D-43 are implemented for web routes and realtime fan-out.
- Old namespace definitions may still exist only in socket/client/store contracts owned by later plans.
- No task modifies 10 or more files.
</success_criteria>

<output>
Create `.planning/phases/03-cut-multi-user-namespace-isolation/03-03-SUMMARY.md` when done. Include migrated web/realtime APIs, remaining temporary namespace internals, targeted tests, focused `rg` counts, and full suite exit codes.
</output>
