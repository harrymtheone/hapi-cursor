---
phase: 03-cut-multi-user-namespace-isolation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - hub/src/store/sessions.ts
  - hub/src/store/sessionStore.ts
  - hub/src/store/machines.ts
  - hub/src/store/machineStore.ts
  - hub/src/store/pushSubscriptions.ts
  - hub/src/store/pushStore.ts
  - hub/src/store/versionedUpdates.ts
  - hub/src/sync/sessionCache.ts
  - hub/src/sync/machineCache.ts
  - hub/src/sync/syncEngine.ts
autonomous: true
requirements: [CUT-09]
requirements_addressed: [CUT-09]
user_setup: []
must_haves:
  truths:
    - "Namespace-free store/cache facade methods exist for sessions, machines, and push subscriptions while old internals coexist temporarily (D-39, D-46)."
    - "SyncEngine exposes owner-only access methods that downstream routes and sockets can call without namespace (D-39)."
    - "No runtime default namespace, compatibility parser, or hidden shim is introduced (D-37)."
    - "`bun typecheck && bun run test` exits 0 after this plan commit (D-47)."
  artifacts:
    - path: "hub/src/store/sessionStore.ts"
      provides: "Namespace-free session store facade"
      contains: "getSession"
    - path: "hub/src/store/machineStore.ts"
      provides: "Namespace-free machine store facade"
      contains: "getMachine"
    - path: "hub/src/store/pushStore.ts"
      provides: "Namespace-free push subscription facade"
      contains: "getPushSubscriptions"
    - path: "hub/src/sync/syncEngine.ts"
      provides: "Owner-only sync facade"
      contains: "resolveSessionAccess"
  key_links:
    - from: "hub/src/sync/syncEngine.ts"
      to: "hub/src/sync/sessionCache.ts"
      via: "owner-only session access"
      pattern: "resolveSessionAccess"
    - from: "hub/src/sync/sessionCache.ts"
      to: "hub/src/store/sessionStore.ts"
      via: "namespace-free facade methods"
      pattern: "getSession"
---

<objective>
Add namespace-free store/cache/sync facades without deleting the old SQL shape yet.

Purpose: Give later route, socket, SSE, and schema cleanup plans green owner-only APIs to migrate onto before physical namespace columns and old methods are removed. This split preserves D-47 and avoids a 50+ file contract cut.
Output: Owner-only facade methods for store/cache/sync, old namespace-bearing internals still present only as temporary unused implementation detail, and a green full suite.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-RESEARCH.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-01-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>03-02-01: Add owner-only store facades</name>
  <files>hub/src/store/sessions.ts, hub/src/store/sessionStore.ts, hub/src/store/machines.ts, hub/src/store/machineStore.ts, hub/src/store/versionedUpdates.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-38, D-39, D-46, D-47)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md section for `hub/src/store/*`
  </read_first>
  <behavior>
    - New session and machine store methods support id/tag/version operations without namespace arguments.
    - Versioned update helper can update by `id` and expected version only for owner-only callsites.
    - Existing namespace-aware SQL and methods may remain temporarily to keep unmigrated callsites green, but new methods do not call `DEFAULT_NAMESPACE` or synthesize a namespace fallback.
  </behavior>
  <action>Add namespace-free methods next to the existing namespace-aware methods in session and machine SQL modules/wrappers. Add a namespace-free path in `versionedUpdates.ts` for id/version-only updates, leaving old namespace predicates available only for still-unmigrated callsites. Do not remove physical namespace columns or users table in this plan; Plan 03-06 owns physical schema deletion and offline migration.</action>
  <verify>
    <automated>bun test hub/src/store/*.test.ts && rg -n 'getSession\(|getMachine\(|expectedVersion' hub/src/store/sessionStore.ts hub/src/store/machineStore.ts hub/src/store/versionedUpdates.ts && bun typecheck && bun run test</automated>
  </verify>
  <done>Store wrappers expose namespace-free session/machine/versioned update APIs and the repository remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>03-02-02: Add owner-only push store facade</name>
  <files>hub/src/store/pushSubscriptions.ts, hub/src/store/pushStore.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-38, D-39, D-46, D-47)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-RESEARCH.md §"Runtime State Inventory"
  </read_first>
  <behavior>
    - Push store exposes namespace-free subscription insert/list/delete methods for later push service migration.
    - Existing namespace-scoped push methods may remain temporarily but are not the new facade.
    - Endpoint uniqueness is not physically changed until Plan 03-06.
  </behavior>
  <action>Add owner-only push store methods that take only subscription data or endpoint identifiers. Keep expired-subscription cleanup behavior intact. Do not dedupe old namespace data in runtime code; duplicate endpoint handling belongs to the offline migration in Plan 03-06 per D-41.</action>
  <verify>
    <automated>bun test hub/src/store/*.test.ts && rg -n 'getPushSubscriptions|removePushSubscription|upsertPushSubscription' hub/src/store/pushStore.ts hub/src/store/pushSubscriptions.ts && bun typecheck && bun run test</automated>
  </verify>
  <done>Push store has namespace-free facades ready for push service migration and the full suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>03-02-03: Add owner-only cache and SyncEngine APIs</name>
  <files>hub/src/sync/sessionCache.ts, hub/src/sync/machineCache.ts, hub/src/sync/syncEngine.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-39, D-42, D-46, D-47)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md section for cache and SyncEngine
  </read_first>
  <behavior>
    - `SessionCache`, `MachineCache`, and `SyncEngine` expose namespace-free methods for list, get, create, update, and access resolution.
    - Access resolution returns not-found/inactive semantics, not namespace access-denied semantics.
    - Old namespace methods may remain temporarily until Plans 03-03 through 03-05 migrate all callsites.
  </behavior>
  <action>Add owner-only cache and SyncEngine methods that delegate to the new store facades. For access checks, prefer existing session/machine existence and active-state behavior; do not preserve namespace mismatch branches in new APIs. Keep current event emission behavior unchanged in this plan because EventPublisher/SSE cleanup is Plan 03-03.</action>
  <verify>
    <automated>bun test hub/src/sync/sessionModel.test.ts hub/src/sync/aliveEvents.test.ts && rg -n 'resolveSessionAccess|getSessions|getMachines|getOrCreateSession|getOrCreateMachine' hub/src/sync/sessionCache.ts hub/src/sync/machineCache.ts hub/src/sync/syncEngine.ts && bun typecheck && bun run test</automated>
  </verify>
  <done>SyncEngine has owner-only APIs for downstream route/socket migration and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SyncEngine -> Store | Validated route/socket operations cross into persistent state. |
| Store -> SQLite | Facade methods translate owner-only semantics into the current physical schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | `hub/src/store/versionedUpdates.ts` | mitigate | Preserve optimistic version checks while adding id/version-only update paths. |
| T-03-05 | Information Disclosure | `hub/src/sync/sessionCache.ts`, `hub/src/sync/machineCache.ts` | mitigate | New owner-only APIs expose only single-owner state and no namespace fallback. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs are required; stop for package audit if one appears. |
</threat_model>

<verification>
- Targeted store and sync tests exit 0.
- `bun typecheck && bun run test` exits 0 after the facade commit.
</verification>

<success_criteria>
- D-39 is implemented as new callable owner-only facades without deleting old internals prematurely.
- Later plans can migrate callsites without needing default namespace or compatibility parsing.
- No task modifies 10 or more files.
</success_criteria>

<output>
Create `.planning/phases/03-cut-multi-user-namespace-isolation/03-02-SUMMARY.md` when done. Include new facade names, which old namespace methods remain temporarily, targeted test output, and full `bun typecheck` / `bun run test` exit codes.
</output>
