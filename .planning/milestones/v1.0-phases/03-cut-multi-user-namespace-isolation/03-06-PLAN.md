---
phase: 03-cut-multi-user-namespace-isolation
plan: 06
type: execute
wave: 6
depends_on: ["03-05"]
files_modified:
  - hub/src/store/index.ts
  - hub/src/store/types.ts
  - hub/src/store/sessions.ts
  - hub/src/store/sessionStore.ts
  - hub/src/store/machines.ts
  - hub/src/store/machineStore.ts
  - hub/src/store/pushSubscriptions.ts
  - hub/src/store/pushStore.ts
  - hub/src/store/versionedUpdates.ts
  - hub/src/store/users.ts
  - hub/src/store/userStore.ts
  - hub/src/store/namespace.test.ts
  - hub/scripts/migrate-namespace-isolation.ts
  - hub/scripts/migrate-namespace-isolation.test.ts
autonomous: true
requirements: [CUT-09]
requirements_addressed: [CUT-09]
user_setup: []
must_haves:
  truths:
    - "Runtime SQLite schema version is 10 and contains no namespace columns, namespace indexes, users table, or `UNIQUE(namespace, endpoint)` (D-38, D-40)."
    - "Store APIs and SQL helpers accept no namespace parameters and old namespace facade methods are deleted (D-39)."
    - "Offline migration entry `hub/scripts/migrate-namespace-isolation.ts` migrates v9 namespace-shaped data to v10 without runtime compatibility (D-41)."
    - "`bun typecheck && bun run test` exits 0 after this plan commit (D-47)."
  artifacts:
    - path: "hub/src/store/index.ts"
      provides: "Schema v10 namespace-free runtime tables"
      contains: "SCHEMA_VERSION"
    - path: "hub/scripts/migrate-namespace-isolation.ts"
      provides: "Offline v9-to-v10 namespace-collapse migration"
      contains: "migrate"
    - path: "hub/scripts/migrate-namespace-isolation.test.ts"
      provides: "Synthetic v9 migration coverage outside final source guard scope"
      contains: "user_version"
  key_links:
    - from: "hub/scripts/migrate-namespace-isolation.ts"
      to: "hub/src/store/index.ts"
      via: "schema version and table shape"
      pattern: "10"
    - from: "hub/src/store/versionedUpdates.ts"
      to: "hub/src/store/sessionStore.ts"
      via: "id/version-only updates"
      pattern: "expectedVersion"
---

<objective>
Delete physical store namespace state and add the offline v9-to-v10 migration entry.

Purpose: Complete D-38 through D-41 after all runtime callsites and shared/client DTOs are already namespace-free. The migration fixture lives under `hub/scripts` so final D-48 source guard can still require zero `namespace|:ns` in `hub/src`.
Output: Runtime schema v10, deleted users store/table, namespace-free store SQL, offline migration script/test, and a green full suite.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-RESEARCH.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-05-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>03-06-01: Delete runtime namespace schema and users store</name>
  <files>hub/src/store/index.ts, hub/src/store/types.ts, hub/src/store/users.ts, hub/src/store/userStore.ts, hub/src/store/namespace.test.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-38, D-40, D-41, D-47)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-RESEARCH.md §"Runtime State Inventory"
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md section for `hub/src/store/*`
  </read_first>
  <behavior>
    - `SCHEMA_VERSION` is 10.
    - Runtime required tables exclude `users`.
    - Runtime `sessions`, `machines`, and `push_subscriptions` DDL has no namespace column/index/constraint.
    - `Store` no longer imports or exposes `UserStore`.
  </behavior>
  <action>Update runtime schema DDL to version 10 with no namespace columns/indexes and no users table. Remove `UserStore` from store composition and delete `hub/src/store/users.ts` plus `hub/src/store/userStore.ts` if they have no live non-Telegram callsites. Replace `hub/src/store/namespace.test.ts` with namespace-free store schema tests or remove it if equivalent coverage moves to script tests. Do not add runtime compatibility migration branches to `Store.initSchema()`.</action>
  <verify>
    <automated>bun test hub/src/store/*.test.ts && test ! -e hub/src/store/users.ts && test ! -e hub/src/store/userStore.ts && ! rg -n 'users|UserStore|StoredUser|UNIQUE\(namespace|idx_.*namespace|namespace TEXT' hub/src/store/index.ts hub/src/store/types.ts && bun typecheck && bun run test</automated>
  </verify>
  <done>Runtime store schema and store composition are physically namespace-free and user-store-free.</done>
</task>

<task type="auto" tdd="true">
  <name>03-06-02: Delete old store namespace methods and params</name>
  <files>hub/src/store/sessions.ts, hub/src/store/sessionStore.ts, hub/src/store/machines.ts, hub/src/store/machineStore.ts, hub/src/store/pushSubscriptions.ts, hub/src/store/pushStore.ts, hub/src/store/versionedUpdates.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-38, D-39, D-47)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-02-SUMMARY.md
  </read_first>
  <behavior>
    - Store helper and wrapper method signatures contain no namespace parameters.
    - SQL predicates and inserts do not reference namespace.
    - Versioned update helper filters by id and expected version only.
    - Push subscriptions use endpoint-only uniqueness in runtime SQL helpers.
  </behavior>
  <action>Now that all consumers use the Plan 03-02 owner-only facades, delete the temporary namespace-aware methods and SQL branches. Update session, machine, push, and versioned update helpers to match the namespace-free runtime schema exactly. Preserve optimistic update/version-mismatch behavior and push expired-endpoint cleanup.</action>
  <verify>
    <automated>bun test hub/src/store/*.test.ts && ! rg -n 'namespace|ByNamespace|access-denied' hub/src/store/sessions.ts hub/src/store/sessionStore.ts hub/src/store/machines.ts hub/src/store/machineStore.ts hub/src/store/pushSubscriptions.ts hub/src/store/pushStore.ts hub/src/store/versionedUpdates.ts && bun typecheck && bun run test</automated>
  </verify>
  <done>Store SQL helpers and wrappers have no namespace methods or parameters and the repository remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>03-06-03: Add offline namespace isolation migration</name>
  <files>hub/scripts/migrate-namespace-isolation.ts, hub/scripts/migrate-namespace-isolation.test.ts, hub/src/store/index.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-38, D-40, D-41, D-47)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-RESEARCH.md §"Open Questions (RESOLVED)"
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md §"No Analog Found"
  </read_first>
  <behavior>
    - Script requires an explicit DB path and never defaults to the developer's live DB.
    - Script transactionally rebuilds v9 namespace-shaped sessions, machines, messages, and push subscriptions into v10 single-owner tables.
    - Script drops `users`, removes namespace columns/indexes, dedupes push subscriptions by endpoint, and sets `PRAGMA user_version = 10`.
    - `Store.initSchema()` does not import, shell out to, or invoke the offline script.
  </behavior>
  <action>Create `hub/scripts/migrate-namespace-isolation.ts` and colocated script test using `bun:sqlite`. The test should build a synthetic v9 fixture DB under a temp path, run the migration function or CLI entry, and assert data preservation, dropped users table, no runtime namespace columns, endpoint-only push uniqueness, and user_version 10. Keep the migration out of `hub/src` so Plan 03-07 can enforce zero namespace source matches in `hub/src` while still preserving migration coverage.</action>
  <verify>
    <automated>bun test hub/scripts/migrate-namespace-isolation.test.ts hub/src/store/*.test.ts && test -f hub/scripts/migrate-namespace-isolation.ts && ! rg -n 'migrate-namespace-isolation|namespace.*migration' hub/src/store/index.ts && bun typecheck && bun run test</automated>
  </verify>
  <done>Offline migration exists, is tested against a v9 fixture, is not invoked at runtime, and the repository remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store -> SQLite | Runtime state crosses into durable namespace-free tables. |
| Offline migration script -> SQLite DB | Explicit DB path is rewritten from v9 namespace schema to v10 single-owner schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | `hub/src/store/index.ts` | mitigate | Schema version 10 rejects old runtime shape; tests assert no namespace/user tables remain. |
| T-03-13 | Tampering | `hub/scripts/migrate-namespace-isolation.ts` | mitigate | Require explicit DB path, run in a transaction, and test synthetic fixture DBs only. |
| T-03-14 | Denial of Service | push subscription migration | mitigate | Collapse duplicate endpoints deterministically and cover with a fixture. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs are required; stop for package audit if one appears. |
</threat_model>

<verification>
- Store and migration targeted tests exit 0.
- Runtime store source scans show no namespace/user-store residue.
- `bun typecheck && bun run test` exits 0 after the plan commit.
</verification>

<success_criteria>
- D-38 through D-41 are fully implemented without runtime schema compatibility.
- CUT-09 success criterion #4 is satisfied.
- No task modifies 10 or more files.
</success_criteria>

<output>
Create `.planning/phases/03-cut-multi-user-namespace-isolation/03-06-SUMMARY.md` when done. Include runtime schema version, deleted user-store files, migration fixture shape, migrated row counts, runtime non-invocation check, targeted tests, focused `rg` counts, and full suite exit codes.
</output>
