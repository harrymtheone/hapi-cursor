---
id: 12-04
phase: 12-docs-cleanup-milestone-verification
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md
  - .planning/phases/12-docs-cleanup-milestone-verification/manual-tailscale/
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - CHANGELOG.md
autonomous: false
requirements: [VRFY-01, VRFY-02, VRFY-03, VRFY-04]
must_haves:
  truths:
    - "Full automated gate suite is run end-to-end and outputs captured: bun typecheck, bun run test (per-runner counts), madge --circular for cli/hub/web, scripts/check-no-cut-agents.sh, ripgrep absence sweep over 9 keywords (claude/codex/gemini/opencode/telegram/serverchan/elevenlabs/tunwg/namespace) with explicit whitelist .planning/codebase/ + CHANGELOG.md + git history (D-04 + CUT-12 SC#4)."
    - "Coverage snapshot is collected (Vitest --coverage for cli + web; hub bun:test coverage attempted, marked unavailable if bun does not support — D-09) and tabled against the Phase 11 baseline captured in 11-DISCUSSION-LOG.md."
    - "Manual Tailscale + Cursor + phone scenario (D-05) is executed by a human with phone on the Tailnet, all 8 (or 9) steps recorded ✅/❌ with timestamps + screenshots stored under .planning/phases/12-*/manual-tailscale/ named step-NN-slug.png (D-06)."
    - "12-VERIFICATION.md exists with the exact section structure of D-04: ## Automated gates → ## Coverage snapshot → ## Manual Tailscale scenario → ## ROADMAP reconciliation → ## Outstanding (Milestone 2 backlog) → ## Sign-off."
    - "ROADMAP.md Phase 7, 8, 9, 11 stale checkboxes are reconciled against commit history (D-12 + §specifics list): Phase 7 plans 07-02/03/04 flipped to [x] with dates; Phase 8 plans 08-02/03/04 same; Phase 9 plan 09-04 same; Phase 11 plans 11-04/05 same; Progress table 'Plans Complete' column synced."
    - "PROJECT.md Key Decisions table appended with a Milestone 1 sign-off entry; CHANGELOG.md top has a Milestone 1 entry summarising the 12 phases; STATE.md flipped to Milestone 1 complete (status + progress fields)."
    - "Phase-12 guard from 12-01 stays green; verify.yml from 12-03 stays green on the final commit."
  artifacts:
    - path: ".planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md"
      provides: "Milestone 1 sign-off report (D-04 structure)"
      contains: "Sign-off"
    - path: ".planning/phases/12-docs-cleanup-milestone-verification/manual-tailscale/"
      provides: "Screenshot evidence directory for SC#5"
    - path: ".planning/ROADMAP.md"
      provides: "Reconciled Progress table + Phase 7/8/9/11 plan checkboxes"
    - path: ".planning/STATE.md"
      provides: "Milestone 1 status = complete"
      contains: "Milestone 1"
    - path: "CHANGELOG.md"
      provides: "Milestone 1 changelog entry at top of file"
      contains: "Milestone 1"
  key_links:
    - from: "12-VERIFICATION.md"
      to: "manual-tailscale/step-NN-slug.png"
      via: "relative-path embeds in the Manual Tailscale section"
      pattern: "manual-tailscale/step-"
    - from: "ROADMAP.md Progress table"
      to: "commit history (git log)"
      via: "checkbox-vs-commit reconciliation"
      pattern: "(7|8|9|11).*Complete"
---

<objective>
Run the full automated gate suite, collect coverage, execute the manual Tailscale + phone scenario, write the single 12-VERIFICATION.md sign-off report, reconcile ROADMAP stale checkboxes against commit history, and flip the milestone state (PROJECT.md Key Decisions + CHANGELOG.md + STATE.md). Plan is split into autonomous prep tasks (Tasks 1, 2, 4, 5, 6) and one human-gated manual scenario task (Task 3, autonomous=false — requires human + phone on the Tailnet).

Purpose: VRFY-01/02/03/04 closure + Milestone 1 sign-off.
Output: 12-VERIFICATION.md + screenshot evidence + reconciled ROADMAP + state-page flipped to milestone complete.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-docs-cleanup-milestone-verification/12-CONTEXT.md
@.planning/phases/12-docs-cleanup-milestone-verification/12-01-SUMMARY.md
@.planning/phases/12-docs-cleanup-milestone-verification/12-02-SUMMARY.md
@.planning/phases/12-docs-cleanup-milestone-verification/12-03-SUMMARY.md
@.planning/phases/11-test-gap-fill/11-CONTEXT.md
@.planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md
@CHANGELOG.md
@scripts/check-no-cut-agents.sh
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated gate suite + capture outputs</name>
  <files>.planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md (created — automated gates section only)</files>
  <read_first>
    - 12-CONTEXT.md §decisions D-04 (VERIFICATION.md structure — Automated gates section bullets) + CUT-12 SC#4 (9-keyword ripgrep + whitelist)
    - scripts/check-no-cut-agents.sh (to enumerate the Phase guard block titles for the gate output listing)
    - package.json (which `bun run test:*` subcommands exist, so per-runner counts are reportable)
    - .planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md (Phase 11 baseline coverage numbers — needed in Task 2; reading now so the verification doc layout is planned coherently)
  </read_first>
  <action>
    Create `.planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md` with the full D-04 section skeleton (all 6 sections present as `## ` headers, with placeholder "_pending_" or "_pending Task N_" text under the sections this task will not yet fill). For the `## Automated gates` section, run each gate and capture exact outputs verbatim. (a) `bun typecheck`: run from repo root; paste last ~20 lines + the exit code line `# exit: 0`. (b) `bun run test`: run from repo root; capture per-runner totals — extract Vitest cli + web test counts and bun:test hub + shared counts from the output; record as a small table (runner | scope | tests | pass | fail). (c) `madge --circular`: run `bun run madge:check` and paste output; per-scope expectation is "0 circular" or similar — record each of cli/src, hub/src, web/src verdict on its own line; if any cycle appears, do NOT flag PASS — record as gate FAIL and stop, do not proceed to Task 2 until cycle is investigated. (d) `scripts/check-no-cut-agents.sh`: run, paste tail showing the per-Phase `✅ Phase N guard PASS` lines (Phase 1 through Phase 12); list each block title in a one-line summary. (e) Ripgrep absence sweep — for each of the 9 keywords (claude, codex, gemini, opencode, telegram, serverchan, elevenlabs, tunwg, namespace) run `rg -nci '\b<keyword>\b' --glob '!.planning/codebase/**' --glob '!CHANGELOG.md' --glob '!.git/**' --glob '!.planning/phases/**' --glob '!AGENTS.md' .` (whitelist per CUT-12 SC#4 + AGENTS.md "Don't reintroduce" rules-line carve-out — note the carve-out explicitly in the VERIFICATION doc); record hit count per keyword + a one-line whitelist rationale; expected: 0 hits for all 9 (any non-zero = investigate before sign-off). Also annotate "lint not configured — deferred to Milestone 2 (D-08)" inline per CONTEXT.md guidance. Commit the partially-filled VERIFICATION.md after this task; subsequent tasks fill remaining sections.
  </action>
  <verify>
    <automated>test -s .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md &amp;&amp; grep -q "## Automated gates" .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md &amp;&amp; grep -q "## Coverage snapshot" .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md &amp;&amp; grep -q "## Manual Tailscale scenario" .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md &amp;&amp; grep -q "## ROADMAP reconciliation" .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md &amp;&amp; grep -q "## Outstanding" .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md &amp;&amp; grep -q "## Sign-off" .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md &amp;&amp; bash scripts/check-no-cut-agents.sh &amp;&amp; bun run typecheck &amp;&amp; bun run test &amp;&amp; bun run madge:check</automated>
  </verify>
  <done>12-VERIFICATION.md exists with all 6 D-04 sections as headers; Automated gates section is filled with verbatim outputs for typecheck, test (per-runner counts), madge (per-scope), guard (per-phase title list), ripgrep absence sweep (9 keywords + whitelist rationale + lint-not-configured note); all gates PASS or any FAIL is explicitly flagged and execution stopped for investigation.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage snapshot vs Phase 11 baseline</name>
  <files>.planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md (Coverage snapshot section)</files>
  <read_first>
    - .planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md — Phase 11 end-state baseline numbers (D-09 cross-reference target)
    - .planning/phases/11-test-gap-fill/11-CONTEXT.md D-188 (coverage policy: capture not gate)
    - cli/package.json + web/package.json — confirm `vitest run --coverage` invocation surface (whether `@vitest/coverage-v8` is wired)
    - hub/package.json — check if bun:test coverage flag exists (D-09 Claude's Discretion: verify in practice; if unavailable, mark unavailable)
  </read_first>
  <action>
    Run coverage on each scope: (a) `cd cli && bun run vitest run --coverage` (or whatever invocation cli/package.json exposes — if `--coverage` requires `@vitest/coverage-v8` and it's missing, attempt `bun add -d -w cli @vitest/coverage-v8` ONLY if a Phase 11 SUMMARY recorded the same plumbing; otherwise mark "unavailable — missing @vitest/coverage-v8" with a Milestone 2 follow-up). (b) `cd web && bun run vitest run --coverage` same protocol. (c) hub: attempt bun:test coverage (e.g. `bun test --coverage` from hub/); if bun version does not produce a coverage report, mark "unavailable — bun:test coverage not supported at pinned version". Build a markdown table in the Coverage snapshot section with columns: `scope | metric (lines / statements / functions / branches) | Phase 11 baseline | Phase 12 after | delta | verdict`. Pull Phase 11 baseline numbers verbatim from 11-DISCUSSION-LOG.md. Verdict column: GREEN if delta ≥ 0, RED if regression, N/A if unavailable. Any RED delta is recorded in the Outstanding section (Task 5) as Milestone 2 priority — does NOT block Milestone 1 sign-off per D-09 (coverage is not a gate this phase).
  </action>
  <verify>
    <automated>grep -A 50 "## Coverage snapshot" .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md | grep -E "(cli|hub|web)" | head -1</automated>
  </verify>
  <done>Coverage snapshot table filled with cli + web measured numbers vs Phase 11 baseline; hub coverage attempted and either filled or marked unavailable with rationale; any regression flagged for Outstanding section.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Manual Tailscale + Cursor + phone end-to-end scenario (HUMAN-REQUIRED)</name>
  <files>.planning/phases/12-docs-cleanup-milestone-verification/manual-tailscale/, .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md (Manual Tailscale scenario section)</files>
  <read_first>
    - 12-CONTEXT.md §decisions D-05 (checklist steps verbatim) + D-06 (screenshot naming convention) + §specifics (checklist template table)
    - ROADMAP.md §Phase 12 SC#5 (the scenario being verified — 8-step canonical phrasing)
    - Just-written root README.md (Tailscale quickstart) + cli/README.md (`hapi runner` / `hapi hub` commands) — the operator follows these exact docs; doc errors discovered during the manual run become test failures
  </read_first>
  <action>
    NOT AUTONOMOUS — requires a human operator + phone on the same Tailnet as the dev machine. The AI executor MUST pause here and prompt the human to perform the scenario.

    Human checklist (D-05 canonical 8 steps, with the SC#5 explicit ninth-step second-round confirmation):

    1. On dev machine: start `hapi runner` per cli/README. Screenshot terminal → `manual-tailscale/step-01-runner-up.png`.
    2. On dev machine: start `hapi hub` (must be reachable on Tailnet). Screenshot terminal → `step-02-hub-up.png`.
    3. On phone (same Tailnet): open the Web PWA at the Tailscale URL. Screenshot phone → `step-03-pwa-loaded.png`.
    4. In PWA: create a new Cursor session. Screenshot → `step-04-session-created.png`.
    5. Complete one round of interaction (send user message; observe Cursor agent response). Screenshot → `step-05-first-round.png`.
    6. On dev machine: kill the hub process. Record the exact mechanism used (SIGINT vs SIGKILL vs `pkill -9` — D-05 Claude's Discretion notes this MUST be recorded in the notes column). Capture terminal evidence → `step-06-hub-killed.txt` or `.png`.
    7. On dev machine: restart hub. Screenshot terminal → `step-07-hub-restarted.png`.
    8. On phone: confirm session state recovered (message history visible + machine list intact). Screenshot → `step-08-state-recovered.png`.
    9. Complete a second round of interaction (send another user message; observe response). Screenshot → `step-09-second-round.png`.

    For each step, record ✅ or ❌, timestamp, notes (kill mechanism for step 6 is mandatory). Use the §specifics table template. Place all screenshots under `.planning/phases/12-docs-cleanup-milestone-verification/manual-tailscale/`; if any file exceeds 1MB, compress with `oxipng` or `pngquant` before commit (D-06). Fill the `## Manual Tailscale scenario` section of 12-VERIFICATION.md with: executor name, date/time, the 9-row table with ✅/❌/notes/screenshot path, overall PASS/FAIL, one-line rationale.

    AI executor MUST NOT mark this task complete without the human providing the filled checklist + screenshots committed. If the human cannot perform the scenario in this session, the executor records "BLOCKED: awaiting human Tailscale scenario" in the SUMMARY and surfaces this to the orchestrator — milestone sign-off (Task 6) cannot proceed without this.
  </action>
  <verify>
    <automated>test -d .planning/phases/12-docs-cleanup-milestone-verification/manual-tailscale &amp;&amp; ls .planning/phases/12-docs-cleanup-milestone-verification/manual-tailscale/ | grep -c "step-" | awk '{exit ($1 &gt;= 8)?0:1}' &amp;&amp; grep -A 5 "## Manual Tailscale scenario" .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md | grep -qE "Executed by|executor"</automated>
    <human-check>Operator confirms: all 9 steps executed in order; ✅/❌ recorded honestly; step-6 kill mechanism noted; PASS/FAIL verdict reflects actual observed behavior; screenshots match the textual step descriptions.</human-check>
  </verify>
  <done>manual-tailscale/ contains at least 8 step-NN screenshots (step-06 may be .txt terminal capture); 12-VERIFICATION.md Manual Tailscale section has 9-row checklist filled with ✅/❌ + timestamps + screenshot paths + executor name + overall PASS/FAIL + rationale.</done>
</task>

<task type="auto">
  <name>Task 4: ROADMAP stale [x] reconciliation against commit history</name>
  <files>.planning/ROADMAP.md, .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md (ROADMAP reconciliation section)</files>
  <read_first>
    - .planning/ROADMAP.md (full file) — current §Phase 7/8/9/11 plan checkboxes + Progress table
    - .planning/STATE.md — authoritative completion state (55/55 plans complete)
    - 12-CONTEXT.md §specifics ROADMAP reconciliation list (Phase 7: 07-02/03/04; Phase 8: 08-02/03/04; Phase 9: 09-04; Phase 11: 11-04/05 — confirm each against `git log --oneline --all | grep -E "(07-02|07-03|07-04|08-02|08-03|08-04|09-04|11-04|11-05)"`)
  </read_first>
  <action>
    Run `git log --oneline --all --grep="07-02\|07-03\|07-04\|08-02\|08-03\|08-04\|09-04\|11-04\|11-05"` and inspect file-level commits to confirm each plan landed. For each confirmed-complete plan currently marked `[ ]` in ROADMAP.md, flip the checkbox to `[x]` and append the completion date in the format used elsewhere in the file (look for "(completed YYYY-MM-DD)" style on similar lines — match it). Update the Progress table rows for Phase 7/8/9/11 to reflect the actual completed plan count (e.g. Phase 7: `1/4 In Progress` → `4/4 Complete + 2026-MM-DD`; Phase 8: `0/TBD Not started` → `4/4 Complete`; Phase 9: `0/TBD Not started` → `4/4 Complete`; Phase 11: `3/5 In Progress` → `5/5 Complete + 2026-MM-DD`). Also fix the "Phase 8: 0/TBD" and "Phase 9: 0/TBD" placeholders to the real "4/4" — these are roadmap rendering bugs, not real status. In the VERIFICATION.md `## ROADMAP reconciliation` section, record a before/after table per phase listing each affected plan + checkbox state change + commit SHA evidence; also list any SC that, during verification, was discovered NOT to be met (none expected per STATE.md, but if any surfaces, record it here AND in the Outstanding section).
  </action>
  <verify>
    <automated>grep -c "^- \[ \] 07-0[234]-PLAN" .planning/ROADMAP.md | awk '{exit ($1==0)?0:1}' &amp;&amp; grep -c "^- \[ \] 08-0[234]-PLAN" .planning/ROADMAP.md | awk '{exit ($1==0)?0:1}' &amp;&amp; grep -c "^- \[ \] 09-04-PLAN" .planning/ROADMAP.md | awk '{exit ($1==0)?0:1}' &amp;&amp; grep -c "^- \[ \] 11-0[45]-PLAN" .planning/ROADMAP.md | awk '{exit ($1==0)?0:1}'</automated>
  </verify>
  <done>ROADMAP.md Phase 7/8/9/11 plan checkboxes reflect commit history reality (all listed plans flipped to `[x]`); Progress table rows updated to "N/N Complete" for these phases; VERIFICATION.md ROADMAP reconciliation section has before/after table with commit SHA evidence; any newly-discovered SC gap is recorded in both reconciliation section + Outstanding section.</done>
</task>

<task type="auto">
  <name>Task 5: Outstanding (Milestone 2 backlog) section + final Sign-off</name>
  <files>.planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md (Outstanding + Sign-off sections)</files>
  <read_first>
    - 12-CONTEXT.md §deferred (canonical deferral list: lint, coverage CI gate, independent docs site, playwright e2e, AGENTS.md ↔ .cursor/rules sync, real-gap SC follow-ups, multi-CI, screenshot auto-compress)
    - Tasks 1-4 outputs in 12-VERIFICATION.md so far (any gate FAIL, coverage regression, or ROADMAP-uncovered SC gap must roll forward into Outstanding)
    - .planning/PROJECT.md (project policy framing for the rationale paragraph)
  </read_first>
  <action>
    Fill `## Outstanding (Milestone 2 backlog)` with a bulleted list combining: (a) the canonical deferred items from CONTEXT.md §deferred (lint, coverage CI gate, etc.) — each with a one-line rationale and a "→ Milestone 2" tag; (b) any real SC gaps surfaced in Task 4; (c) any coverage regressions flagged in Task 2. Each item is a candidate for Milestone 2 backlog and gets a stable identifier (e.g. "M2-BL-01: introduce biome lint"). Fill `## Sign-off` with the verdict — PASS if all 5 SC are met and the manual scenario passed; PARTIAL PASS if 4/5 SC met with a documented non-blocking shortfall; FAIL if any blocking gap. Required content: verdict word (PASS / PARTIAL PASS / FAIL), one-line rationale referencing the gate evidence (e.g. "All 5 SC met; automated gates green on commit <sha>; manual Tailscale scenario PASS on 2026-MM-DD by <name>"), and one-line milestone closing statement.
  </action>
  <verify>
    <automated>grep -A 10 "## Outstanding" .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md | grep -qE "(M2-BL|Milestone 2|deferred)" &amp;&amp; grep -A 5 "## Sign-off" .planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md | grep -qE "(PASS|PARTIAL PASS|FAIL)"</automated>
  </verify>
  <done>Outstanding section enumerates all deferred items + any newly-surfaced gaps with M2-BL- IDs and rationale; Sign-off section has a clear verdict + one-line rationale citing gate evidence + closing statement.</done>
</task>

<task type="auto">
  <name>Task 6: Flip milestone state — PROJECT.md + CHANGELOG.md + STATE.md</name>
  <files>.planning/PROJECT.md, CHANGELOG.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md — locate the Key Decisions table; observe its row format
    - CHANGELOG.md — observe the top-of-file entry format (date headers, version sections)
    - .planning/STATE.md — frontmatter (gsd_state_version, milestone, status, progress fields) + Current Position section + Session Continuity section
    - 12-VERIFICATION.md (fully written by Tasks 1-5) — sign-off verdict is the input that gates this task
  </read_first>
  <action>
    If 12-VERIFICATION.md sign-off = FAIL, STOP — do not flip milestone state; surface to orchestrator. If PASS or PARTIAL PASS, proceed. (a) Append a new row to PROJECT.md Key Decisions table summarising Milestone 1 closure: date, decision id (e.g. M1-CLOSE), one-line description ("Milestone 1 (Refactor & Slim-Down) signed off — 12 phases, 59 plans, Cursor-only codebase + automated gate CI + manual Tailscale verified — see 12-VERIFICATION.md"). (b) Prepend a new entry to the top of CHANGELOG.md titled `## Milestone 1 — Refactor & Slim-Down (<sign-off date>)`; under it list the 12 phases with one-line outcome each (concise; pull from ROADMAP.md phase descriptions, do NOT just dump SUMMARYs); end with a pointer line to `.planning/phases/12-docs-cleanup-milestone-verification/12-VERIFICATION.md` for evidence. (c) Update STATE.md frontmatter: `status: complete` (was `planning`); `stopped_at: Milestone 1 complete`; `last_updated` + `last_activity` to today's date; `progress.completed_plans` to actual final count (currently 55 — bump if Phase 12 plans count differently); `progress.percent: 100`. Update STATE.md `## Current Position` section: Phase: 12 / Plan: 12-04 / Status: Complete. Update `## Session Continuity` resume file pointer to 12-VERIFICATION.md. After all edits, run the full local gate one final time: `bash scripts/check-no-cut-agents.sh && bun run typecheck && bun run test && bun run madge:check` — must all pass. Commit all three files together with message `docs(12-04): milestone 1 sign-off`.
  </action>
  <verify>
    <automated>grep -qE "(M1-CLOSE|Milestone 1.*sign|Milestone 1.*close)" .planning/PROJECT.md &amp;&amp; head -30 CHANGELOG.md | grep -qE "Milestone 1" &amp;&amp; grep -qE "^status: complete" .planning/STATE.md &amp;&amp; grep -qE "Milestone 1 complete" .planning/STATE.md &amp;&amp; bash scripts/check-no-cut-agents.sh &amp;&amp; bun run typecheck &amp;&amp; bun run test &amp;&amp; bun run madge:check</automated>
  </verify>
  <done>PROJECT.md Key Decisions has Milestone 1 closure row; CHANGELOG.md top has Milestone 1 entry with 12-phase summary + VERIFICATION pointer; STATE.md flipped to status: complete with current position = 12 / 12-04 / Complete and resume pointer to 12-VERIFICATION.md; full local gate green on the final commit.</done>
</task>

</tasks>

<verification>
- `bun run typecheck && bun run test && bun run madge:check && bash scripts/check-no-cut-agents.sh` green on final commit.
- 12-VERIFICATION.md has all 6 D-04 sections filled (Automated gates / Coverage snapshot / Manual Tailscale scenario / ROADMAP reconciliation / Outstanding / Sign-off).
- manual-tailscale/ contains ≥ 8 step screenshots.
- ROADMAP.md Phase 7/8/9/11 plan checkboxes + Progress table accurately reflect commit history.
- PROJECT.md Key Decisions appended with Milestone 1 closure row; CHANGELOG.md has Milestone 1 entry; STATE.md frontmatter `status: complete` + progress 100%.
- GitHub Actions verify workflow (from 12-03) green on the final commit (PR or push run URL recorded in SUMMARY).
</verification>

<success_criteria>
Milestone 1 formally closed: VRFY-01/02/03/04 all met with documented evidence; manual SC#5 PASS; any non-blocking shortfalls captured as Milestone 2 backlog; project state pages reflect milestone completion; CI is the durable gate going forward.
</success_criteria>

<output>
Create `.planning/phases/12-docs-cleanup-milestone-verification/12-04-SUMMARY.md` — record: gate outputs summary, coverage table snapshot, manual scenario executor + date + verdict, count of ROADMAP checkbox flips with SHA evidence, list of Milestone 2 backlog items created, final commit SHA + verify workflow run URL.
</output>
