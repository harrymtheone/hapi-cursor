---
id: 12-02
phase: 12-docs-cleanup-milestone-verification
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - README.md
  - AGENTS.md
  - cli/README.md
  - hub/README.md
  - web/README.md
autonomous: true
requirements: [CUT-12]
must_haves:
  truths:
    - "Root README sells Cursor Agent mobile/remote control over Tailscale as the single product story — no Claude/Codex/Gemini/OpenCode/Telegram/ServerChan/ElevenLabs mentions (CUT-12 SC#1 + D-01 product framing)."
    - "AGENTS.md is rebuilt from scratch as a ≤100-line Cursor-only repo navigation aid for AI coding agents, with a CLI↔Hub↔Web architecture sketch (no Telegram, no multi-agent) and a single bottom pointer to .cursor/rules/ (D-01 + D-02)."
    - "Each of cli/README.md, hub/README.md, web/README.md describes its package's role, install, `bun run` script table, key module entry points, and how to run tests — written from 0, not scrubbed (D-03)."
    - "Every README quickstart was actually walked locally by the executor: each `bun run` script in the table was invoked and observed; any mismatch with reality was fixed in the README before commit (D-11 12-02 cadence)."
    - "No surviving link to website/, docs/guide, docs/superpowers, or refactor.md in any of the 5 markdown files."
    - "Phase-12 guard from 12-01 stays green after edits (no accidental reintroduction of forbidden tokens)."
  artifacts:
    - path: "AGENTS.md"
      provides: "Cursor-only AI-agent repo navigation, ≤100 lines"
      contains: "Cursor"
    - path: "README.md"
      provides: "Root product README (Cursor Agent mobile remote + Tailscale quickstart)"
      contains: "Tailscale"
    - path: "cli/README.md"
      provides: "CLI package README"
    - path: "hub/README.md"
      provides: "Hub package README"
    - path: "web/README.md"
      provides: "Web package README"
  key_links:
    - from: "AGENTS.md"
      to: ".cursor/rules/"
      via: "single bottom-of-file pointer line"
      pattern: "\\.cursor/rules"
    - from: "README.md (root)"
      to: "cli/, hub/, web/"
      via: "package layout + quickstart references"
      pattern: "(cli/|hub/|web/)"
---

<objective>
Rewrite the 5 README surfaces + AGENTS.md from zero against the Cursor-only post-cut codebase. Order: root README → AGENTS.md → cli/README.md → hub/README.md → web/README.md. Each file is walked end-to-end against the live codebase (quickstart commands actually invoked, `bun run` script tables sourced from each package.json verbatim).

Purpose: D-01 + D-02 + D-03 — establish accurate, lean docs surface that any future AI coding agent or human reader can trust.
Output: 5 rewritten markdown files, Phase-12 guard still green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-docs-cleanup-milestone-verification/12-CONTEXT.md
@.planning/phases/12-docs-cleanup-milestone-verification/12-01-SUMMARY.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/INTEGRATIONS.md
@package.json
@cli/package.json
@hub/package.json
@web/package.json
@scripts/check-no-cut-agents.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite root README.md from zero</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state — read once to confirm it is multi-agent residue; do not preserve any structure)
    - 12-CONTEXT.md §specifics + §decisions D-01 (product framing: Cursor Agent mobile parity with desktop IDE + single-user Tailscale quickstart)
    - .planning/PROJECT.md (project policy: Cursor-only, single-user Tailscale, no backward compat)
    - .planning/codebase/STRUCTURE.md (cli/shared/hub/web layout)
    - .planning/codebase/ARCHITECTURE.md (CLI↔Hub↔Web)
    - package.json (root scripts — for quickstart's `bun install`, `bun run dev`, `bun run test`, `bun run typecheck` invocations)
    - cli/src/commands/* (to verify the `hapi runner` / `hapi hub` commands the quickstart references actually exist with those names)
    - 12-01-SUMMARY.md (any surviving website/ or docs/ references captured during deletion that this rewrite must NOT carry forward)
  </read_first>
  <action>
    Replace the entire `README.md`. Required sections in this order, no extras: (1) one-line tagline naming the product as "HAPI Cursor Edition — remote-control the Cursor coding agent from your phone or laptop over Tailscale"; (2) "What this is / What this is not" — explicitly state single-user Tailscale deployment, not a multi-agent platform, not a hosted SaaS; (3) "Repo layout" listing exactly cli / hub / web / shared with one-line role each (mirror .planning/codebase/STRUCTURE.md facts, do NOT mention website/ or docs/); (4) "Quickstart" — exact shell commands for `bun install`, start the runner on the dev machine (`bun run dev` or the concrete `hapi runner` / `hapi hub` invocations as they actually exist in cli/src/commands), Tailscale URL hint, opening the PWA on phone; (5) "Development" — `bun typecheck`, `bun run test`, link to AGENTS.md for AI-agent contributors; (6) "License" stub if a LICENSE file exists, else omit. NO references to claude/codex/gemini/opencode/telegram/serverchan/elevenlabs/tunwg/namespace/website/docs/refactor.md (Phase-12 guard would catch these but write defensively). After writing, actually run the quickstart commands you cite (`bun install`, the dev command(s), `bun run test`) and patch the README if reality diverges.
  </action>
  <verify>
    <automated>test -s README.md &amp;&amp; ! grep -iqE '\b(claude|codex|gemini|opencode|telegram|serverchan|elevenlabs|tunwg|namespace)\b' README.md &amp;&amp; ! grep -qE '(website/|docs/guide|docs/superpowers|refactor\.md)' README.md &amp;&amp; grep -q Tailscale README.md &amp;&amp; grep -q Cursor README.md</automated>
  </verify>
  <done>Root README replaced with Cursor-only Tailscale-focused content covering all 6 required sections; quickstart commands verified by actual local invocation; no forbidden tokens present.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild AGENTS.md from zero (≤100 lines)</name>
  <files>AGENTS.md</files>
  <read_first>
    - 12-CONTEXT.md §decisions D-01 (AGENTS.md scope: AI-agent repo navigation, NOT product doc) + D-02 (no duplication of .cursor/rules/) + §specifics (suggested skeleton)
    - .planning/codebase/STRUCTURE.md / STACK.md / ARCHITECTURE.md / TESTING.md / CONVENTIONS.md (facts to compress into AGENTS.md sections)
    - .cursor/rules/ directory listing — confirm existence of gitnexus.mdc and any siblings so the bottom pointer line is correct
    - HEAD version of AGENTS.md via `git show HEAD:AGENTS.md` (currently in `D` state) — read once as NEGATIVE reference; do not copy multi-agent / Telegram / docs/guide content forward
    - Newly-written README.md (Task 1) — AGENTS.md must reference root README as the canonical first-read
  </read_first>
  <action>
    Create fresh `AGENTS.md` (overwrite if file exists). Hard cap: ≤100 lines including blank lines. Required sections in this order: (a) one-line work-style note (telegraph style, drop grammar, noun phrases ok — matches HEAD style per CONTEXT.md §code_context); (b) one-paragraph orientation ("Short guide for AI coding agents in this repo. Read root README first, then package READMEs as needed."); (c) `## What this repo is` — one paragraph: Cursor Agent mobile/web remote control, single user, Tailscale-deployed, not a multi-agent platform; (d) `## Repo layout` — 4-line table or list: cli / hub / web / shared with one-line role each (do NOT list website/ or docs/); (e) `## Architecture` — single-line ASCII: `CLI ──Socket.IO── Hub ──SSE/REST── Web (PWA)`; (f) `## Commands` — `bun install`, `bun typecheck`, `bun run test`, `bun run madge:check` (added by 12-03 — note in SUMMARY if 12-03 not yet merged; reference it anyway as the canonical name per D-10), `bash scripts/check-no-cut-agents.sh`; (g) `## Rules` — bullets covering: no backward compatibility (single-user, no install base), TypeScript strict, pragmatism over over-engineering, `@/*` path alias per package (./src/*), 4-space indentation, "Don't reintroduce: claude / codex / gemini / opencode / telegram / serverchan / elevenlabs / tunwg / namespace" — these literal tokens appear here as the AGENTS.md "do not bring these back" reminder, and the Phase-12 guard's ripgrep absence sweep MUST whitelist AGENTS.md for this line (record this carve-out in the SUMMARY so 12-04 Task 1 honours it); (h) `## See also` — pointer lines to `.cursor/rules/` (Cursor IDE-specific rules) and `.planning/` (GSD working directory — do not edit). After writing, run `wc -l AGENTS.md` and confirm ≤100.
  </action>
  <verify>
    <automated>test -s AGENTS.md &amp;&amp; awk 'END{exit (NR&lt;=100)?0:1}' AGENTS.md &amp;&amp; grep -q "Cursor" AGENTS.md &amp;&amp; grep -q "\.cursor/rules" AGENTS.md &amp;&amp; grep -q "Repo layout" AGENTS.md &amp;&amp; grep -q "bun typecheck" AGENTS.md &amp;&amp; ! grep -q "Telegram" AGENTS.md &amp;&amp; ! grep -qE "(Claude|Codex|Gemini|OpenCode) (runtime|agent|wrapper)" AGENTS.md</automated>
  </verify>
  <done>AGENTS.md exists, ≤100 lines, contains all 8 required sections, no Telegram / multi-agent narrative, bottom pointer to .cursor/rules/ present; SUMMARY records the §Rules "Don't reintroduce" whitelist carve-out that 12-04 ripgrep sweep must honour.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite cli/README.md from zero</name>
  <files>cli/README.md</files>
  <read_first>
    - cli/README.md (current state — read once to confirm it is multi-agent residue; D-03 = full rewrite, no scrub)
    - cli/package.json (full `scripts` block — every `bun run` row in the README table is sourced from this file verbatim)
    - cli/src/commands/* (list each command file — these are the user-facing CLI verbs; pull names + one-line descriptions from the actual implementations)
    - cli/src/index.ts or main entry (confirm install/build output binary name)
    - .planning/codebase/STRUCTURE.md cli section (key modules: agent/, cursor/, runner/, commands/, configuration.ts, etc.)
    - .planning/codebase/TESTING.md (cli uses Vitest)
    - Newly-written root README.md (for cross-link target)
  </read_first>
  <action>
    Replace `cli/README.md` entirely. Required sections in this order: (1) one-line role statement ("hapi CLI — Cursor Agent wrapper, runner, and hub launcher for HAPI Cursor Edition"); (2) "Install / Build" — exact commands (`bun install` from repo root, `bun run --cwd cli build` if a build script exists, or document the single-exe build path from root `package.json` if that's the user path); (3) "Commands" — bullet list of every command exposed by cli/src/commands/ with one-line description sourced from the actual command implementation (not invented); explicitly call out `hapi runner` and `hapi hub` since the root README quickstart depends on these names; (4) "`bun run` scripts" — a markdown table with columns `script | command | purpose`, one row per entry in cli/package.json `scripts`; (5) "Key modules" — short bulleted list of cli/src/{agent,cursor,runner,commands,configuration.ts} with one-line role each (facts from codebase/STRUCTURE.md); (6) "Tests" — `cd cli && bun run test` runs Vitest; co-located `*.test.ts` per CONVENTIONS.md. After writing, invoke every `bun run` script listed in the table at least once (`bun run --cwd cli <script>` — at minimum typecheck + test); if any script fails for reasons unrelated to incidental sandbox limits, fix the README to reflect reality. No forbidden tokens (claude/codex/gemini/opencode/telegram/serverchan/elevenlabs/tunwg/namespace/website/docs/refactor.md).
  </action>
  <verify>
    <automated>test -s cli/README.md &amp;&amp; ! grep -iqE '\b(claude|codex|gemini|opencode|telegram|serverchan|elevenlabs|tunwg|namespace)\b' cli/README.md &amp;&amp; grep -qE "(bun run|bun install)" cli/README.md &amp;&amp; grep -q "hapi" cli/README.md &amp;&amp; cd cli &amp;&amp; bun run typecheck &amp;&amp; bun run test</automated>
  </verify>
  <done>cli/README.md replaced; every section present; bun run scripts table matches cli/package.json verbatim; key modules list matches .planning/codebase/STRUCTURE.md; tests + typecheck green from cli/ cwd.</done>
</task>

<task type="auto">
  <name>Task 4: Rewrite hub/README.md from zero</name>
  <files>hub/README.md</files>
  <read_first>
    - hub/README.md (current state — read once as negative reference)
    - hub/package.json (full `scripts` block)
    - hub/src/index.ts (main entry — server boot, port/host env vars, Socket.IO + SSE + REST surfaces)
    - .planning/codebase/STRUCTURE.md hub section (sync/, web/routes/, sse/, socket/, store/, notifications/)
    - .planning/codebase/ARCHITECTURE.md (Hub's role between CLI and Web)
    - .planning/codebase/STACK.md (hub uses bun:test, SQLite via store layer)
    - .planning/codebase/TESTING.md (hub runner = bun:test, NOT Vitest)
  </read_first>
  <action>
    Replace `hub/README.md`. Required sections: (1) role statement ("hapi hub — HTTP + Socket.IO + SSE server with SQLite persistence; brokers between CLI runners and Web PWA clients"); (2) "Install / Run" — `bun install` from root, `cd hub && bun run dev` for dev mode, env vars consumed at startup (HAPI_LISTEN_HOST/HAPI_LISTEN_PORT and any auth tokens — read hub/src/configuration.ts to enumerate); (3) "`bun run` scripts" — markdown table sourced from hub/package.json `scripts` verbatim; (4) "Key modules" — short bulleted list of hub/src/{sync,web/routes,sse,socket,store,notifications,configuration.ts} with one-line role each; (5) "Tests" — `cd hub && bun run test` runs bun:test (NOT Vitest — note this explicitly per TESTING.md cross-runner rule); co-located `*.test.ts` per CONVENTIONS.md; (6) "Health / smoke" — one-liner curl or note pointing at the hub's health endpoint if one exists (check hub/src/web/routes/ for /health or similar; if none, omit this section rather than invent). No forbidden tokens. Walk the quickstart: `cd hub && bun run typecheck && bun run test` must complete green.
  </action>
  <verify>
    <automated>test -s hub/README.md &amp;&amp; ! grep -iqE '\b(claude|codex|gemini|opencode|telegram|serverchan|elevenlabs|tunwg|namespace)\b' hub/README.md &amp;&amp; grep -qE "(Socket\.IO|SSE)" hub/README.md &amp;&amp; grep -qE "(SQLite|store)" hub/README.md &amp;&amp; cd hub &amp;&amp; bun run typecheck &amp;&amp; bun run test</automated>
  </verify>
  <done>hub/README.md replaced; all sections present; bun run scripts table matches hub/package.json; cross-runner note (bun:test, not Vitest) explicit; tests + typecheck green from hub/ cwd.</done>
</task>

<task type="auto">
  <name>Task 5: Rewrite web/README.md from zero + final guard sweep</name>
  <files>web/README.md</files>
  <read_first>
    - web/README.md (current state — read once as negative reference)
    - web/package.json (full `scripts` block — `dev`, `build`, `test`, `typecheck`)
    - web/vite.config.ts (PWA plugin, build output dir)
    - .planning/codebase/STRUCTURE.md web section (components/, hooks/, routes/, realtime-removed, lib/)
    - .planning/codebase/ARCHITECTURE.md (web is the React PWA consuming hub via SSE + REST)
    - .planning/codebase/TESTING.md (web uses Vitest)
    - scripts/check-no-cut-agents.sh (run after all 5 rewrites to confirm guard still green)
  </read_first>
  <action>
    Replace `web/README.md`. Required sections: (1) role statement ("hapi web — React PWA for remote-controlling Cursor sessions via the hub from phone or browser"); (2) "Install / Dev / Build" — `bun install` from root, `cd web && bun run dev` for dev server, `bun run build` for production PWA bundle; (3) "`bun run` scripts" — markdown table sourced from web/package.json `scripts` verbatim; (4) "Key modules" — bulleted list of web/src/{components,hooks,routes,lib,types} with one-line role each (facts from STRUCTURE.md); (5) "Tests" — `cd web && bun run test` runs Vitest; co-located `*.test.tsx` per CONVENTIONS.md; (6) "PWA notes" — one paragraph noting service worker behaviour (vite-plugin-pwa) + that the app is served from the hub in single-exe deployments (reference root README quickstart for the Tailscale path). No forbidden tokens. Walk: `cd web && bun run typecheck && bun run test && bun run build` (build step optional if it's slow — skip with note in SUMMARY if you defer it). After all 5 rewrites land, run `bash scripts/check-no-cut-agents.sh` from repo root — must exit 0. Then run `bun run typecheck && bun run test` from root for the final plan-end gate.
  </action>
  <verify>
    <automated>test -s web/README.md &amp;&amp; ! grep -iqE '\b(claude|codex|gemini|opencode|telegram|serverchan|elevenlabs|tunwg|namespace)\b' web/README.md &amp;&amp; grep -qE "PWA" web/README.md &amp;&amp; cd web &amp;&amp; bun run typecheck &amp;&amp; bun run test &amp;&amp; cd .. &amp;&amp; bash scripts/check-no-cut-agents.sh &amp;&amp; bun run typecheck &amp;&amp; bun run test</automated>
  </verify>
  <done>web/README.md replaced; all sections present; bun run scripts table matches web/package.json; tests + typecheck green from web/ cwd AND from repo root; Phase-12 guard from 12-01 still passes (no forbidden tokens reintroduced); root typecheck + test green.</done>
</task>

</tasks>

<verification>
- All 5 markdown files (README.md, AGENTS.md, cli/README.md, hub/README.md, web/README.md) exist and contain no forbidden tokens (claude/codex/gemini/opencode/telegram/serverchan/elevenlabs/tunwg/namespace) outside the AGENTS.md §Rules "Don't reintroduce" line (whitelist carve-out documented in SUMMARY for 12-04 sweep).
- No references to website/, docs/guide, docs/superpowers, refactor.md in any of the 5 files.
- AGENTS.md ≤ 100 lines.
- Each README's `bun run` scripts table matches the corresponding package.json verbatim.
- `bash scripts/check-no-cut-agents.sh` exits 0.
- `bun run typecheck && bun run test` green from repo root.
</verification>

<success_criteria>
Documentation surface is Cursor-only, accurate against the live codebase, and verified by actually running the documented commands. 12-03 can layer CI on top with confidence the docs reflect reality.
</success_criteria>

<output>
Create `.planning/phases/12-docs-cleanup-milestone-verification/12-02-SUMMARY.md` — record: line counts per file, AGENTS.md whitelist carve-out for 12-04 sweep (the §Rules "Don't reintroduce" line literally lists the 9 forbidden tokens), bun run script table sources, any quickstart-vs-reality mismatches caught and fixed.
</output>
