---
phase: 11-test-gap-fill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md
autonomous: true
requirements: [REFT-01, REFT-02, REFT-03]

must_haves:
  truths:
    - "Phase 10 (pre-Phase-11) line-coverage numbers for the five SUT scopes are recorded in 11-DISCUSSION-LOG.md (or 'no baseline available' note is recorded when capture fails)"
    - "The active checkout is never mutated during capture (worktree-only mode — no `git stash`, no `git checkout` on the active tree, so parallel Wave 1 plans 11-02/11-03/11-04 see an untouched working tree); the `/tmp/hapi-baseline-cov` worktree is removed on completion"
  artifacts:
    - path: ".planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md"
      provides: "Phase 10 coverage baseline numbers under heading `## Phase 10 Coverage Baseline (captured 2026-05-XX)`"
      contains: "Phase 10 Coverage Baseline"
  key_links:
    - from: "11-DISCUSSION-LOG.md `## Phase 10 Coverage Baseline`"
      to: "11-DISCUSSION-LOG.md `## Phase 11 Coverage After` (written by plan 11-05)"
      via: "human-readable diff comparison by verify-phase / reader"
      pattern: "Phase 10 Coverage Baseline"
---

<objective>
Capture Phase 10 line-coverage numbers for the five Phase 11 SUT scopes
(`cli/src/cursor/`, `cli/src/agent/`, `hub/src/web/routes/auth.ts`,
`hub/src/sse/`, `web/src/hooks/useSSE.ts`) from `main` (or equivalent
pre-Phase-11 ref) and record them in `11-DISCUSSION-LOG.md`. These numbers
become the baseline for the SC#4 non-regression assertion performed in
plan 11-05.

Purpose: Roadmap §Phase 11 Success Criterion #4 requires coverage for the
five scopes to "not regress versus Phase 10". RESEARCH § Code Examples
(Open Question #3) and the orchestrator override 2026-05-23 confirm no
Phase 10 baseline is stored anywhere in `.planning/`, so plan 11-01 must
capture it directly from the pre-Phase-11 ref.

Output: A `## Phase 10 Coverage Baseline (captured YYYY-MM-DD)` section
inside `11-DISCUSSION-LOG.md` listing one line-coverage number per scope,
plus the exact ref (commit SHA) the numbers were captured from. If the
capture is impractical (worktree conflicts, broken main, etc.) the
section is still written with `unavailable — Phase 11 numbers will be the
new baseline` per RESEARCH Open Question #3 recommendation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-test-gap-fill/11-CONTEXT.md
@.planning/phases/11-test-gap-fill/11-RESEARCH.md
@.planning/phases/11-test-gap-fill/11-VALIDATION.md
@.planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture Phase 10 coverage baseline from main and record in 11-DISCUSSION-LOG.md</name>
  <files>.planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md</files>
  <read_first>
    - .planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md (existing structure)
    - .planning/phases/11-test-gap-fill/11-RESEARCH.md § Code Examples → "Coverage snapshot command (end-of-phase per D-188)" + § Open Questions #3
    - .planning/codebase/TESTING.md § Coverage (only `cli/` has coverage provider preconfigured)
  </read_first>
  <action>
    Per orchestrator decision 2026-05-23 and RESEARCH Open Question #3: capture
    line-coverage for the five Phase 11 SUT scopes from the pre-Phase-11 ref.
    Implementation:

    1. Record current branch + HEAD SHA so we can return: `git rev-parse --abbrev-ref HEAD` and `git rev-parse HEAD`.
    2. Capture mode is **worktree-only** (parallel-wave-safe — Plan 11-01 runs in Wave 1 alongside 11-02/11-03/11-04 with `depends_on: []`, so the active checkout MUST NOT be mutated):
       - `git worktree add /tmp/hapi-baseline-cov main` (or the equivalent pre-Phase-11 ref — the tip of `main` per orchestrator override).
       - If worktree creation fails for any reason (path exists, ref invalid, etc.), record `unavailable — worktree creation failed (<reason>)` for every scope in the DISCUSSION-LOG table, mark capture mode `worktree (failed)`, and exit cleanly. Do NOT fall back to `git stash` / `git checkout` on the active tree — that would corrupt the parallel Wave 1 plans.
    3. Inside the worktree at `/tmp/hapi-baseline-cov` install deps if needed (`bun install`) and run the three coverage commands EXACTLY as RESEARCH § Code Examples specifies. Note: `tools:unpack` is the cli package's bundled-tool extraction step required by `cli/package.json#scripts.test`; we invoke it explicitly here because `vitest run --coverage` bypasses the `test` script wrapper. It is ONLY needed for the `cd cli && ...` command — `hub` and `web` do not have it and MUST NOT be prefixed with it.
       - `cd cli && bun run tools:unpack && bun run vitest run --coverage --reporter=text --coverage.include='src/cursor/**' --coverage.include='src/agent/**'`
       - `cd hub && bun test --coverage 2>&1 | grep -E '(auth\.ts|sse/)'`
       - `cd web && bun run vitest run --coverage --reporter=text --coverage.include='src/hooks/useSSE.ts'`
    4. Extract line-coverage percentages (or raw covered/total when only those are reported) for:
       - `cli/src/cursor/` (aggregate)
       - `cli/src/agent/` (aggregate)
       - `hub/src/web/routes/auth.ts` (single file)
       - `hub/src/sse/` (aggregate)
       - `web/src/hooks/useSSE.ts` (single file)
    5. Remove the worktree (`git worktree remove /tmp/hapi-baseline-cov`, or `git worktree remove --force` if needed) and confirm `git status` on the active branch matches step 1 (no source-tree changes — the parallel Wave 1 plans must see an untouched working tree).
    6. Append the following heading to `11-DISCUSSION-LOG.md` (use today's date in YYYY-MM-DD):

       ```
       ## Phase 10 Coverage Baseline (captured YYYY-MM-DD)

       Captured from: `main` @ <SHA-short>
       Capture mode: worktree (or `worktree (failed) — <reason>` if creation failed)

       | Scope | Line Coverage | Source command |
       |-------|---------------|----------------|
       | cli/src/cursor/ | <NN.NN%> or `unavailable — <reason>` | `cd cli && bun run vitest run --coverage --coverage.include='src/cursor/**'` |
       | cli/src/agent/ | <NN.NN%> | `cd cli && bun run vitest run --coverage --coverage.include='src/agent/**'` |
       | hub/src/web/routes/auth.ts | <NN.NN%> or `no provider` | `cd hub && bun test --coverage` |
       | hub/src/sse/ | <NN.NN%> or `no provider` | `cd hub && bun test --coverage` |
       | web/src/hooks/useSSE.ts | <NN.NN%> | `cd web && bun run vitest run --coverage --coverage.include='src/hooks/useSSE.ts'` |

       Notes:
       - hub coverage provider is not preconfigured (TESTING.md § Coverage) — record `bun test --coverage` numbers as best-effort. If output yields no percentage, write `unavailable — bun:test coverage not enforced` per RESEARCH § Code Examples.
       - If any scope's number cannot be obtained, write `unavailable — <reason>` (per RESEARCH Open Question #3 fallback). Plan 11-05 will then declare Phase 11 numbers the new baseline.
       ```

    7. Per D-188: NO CI gate is added by this task. Numbers are advisory only and will be diffed by plan 11-05.
    8. NEVER use `git push --force`. NEVER modify `git config`. NEVER edit code on `main` — this task is read-only against `main`.

    No production source files are touched. Only `11-DISCUSSION-LOG.md` changes.
  </action>
  <verify>
    <automated>grep -q '^## Phase 10 Coverage Baseline' .planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md &amp;&amp; git status --porcelain | grep -v '^?? \|^ M .planning/' | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md` contains a `## Phase 10 Coverage Baseline (captured YYYY-MM-DD)` heading.
    - Beneath the heading: a markdown table with exactly 5 rows, one per scope listed above. Each row's `Line Coverage` cell is either a percentage (e.g. `73.45%`), an absolute covered/total fraction (e.g. `124/170`), or the literal string `unavailable — <reason>`.
    - The note line beneath the table identifies `main` SHA-short used for capture.
    - `git status` after the task shows only `.planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md` modified (no stray source-tree changes, no leftover worktree at `/tmp/hapi-baseline-cov`).
    - `git worktree list` does NOT include `/tmp/hapi-baseline-cov` after task completion.
    - Active checkout is unchanged: `git rev-parse --abbrev-ref HEAD` equals the recorded pre-capture branch name, and `git rev-parse HEAD` equals the recorded pre-capture SHA (worktree-only capture must not mutate the active tree — required for parallel-wave safety with 11-02/11-03/11-04).
  </acceptance_criteria>
  <done>Phase 10 baseline captured (or marked `unavailable` with reason) in 11-DISCUSSION-LOG.md and working tree restored to its pre-task state.</done>
</task>

</tasks>

<verification>
- `grep -A 20 '## Phase 10 Coverage Baseline' .planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md` shows the heading, captured-from line, and 5-row table.
- `git status` shows only the DISCUSSION-LOG modification — no `cli/`, `hub/`, `web/`, or worktree residue.
- `git rev-parse --abbrev-ref HEAD` equals the pre-capture branch name.
</verification>

<success_criteria>
Five-scope Phase 10 baseline numbers (or explicit `unavailable` markers) are persisted in 11-DISCUSSION-LOG.md, ready for plan 11-05's non-regression comparison; working tree is clean and on the phase branch.
</success_criteria>

<output>
Create `.planning/phases/11-test-gap-fill/11-01-SUMMARY.md` recording: the SHA captured from, the five numbers recorded (or `unavailable` reasons), confirmation that capture mode was worktree-only (or `worktree (failed) — <reason>` if creation failed and all scopes were marked unavailable), and any deviations from the planned commands.
</output>
