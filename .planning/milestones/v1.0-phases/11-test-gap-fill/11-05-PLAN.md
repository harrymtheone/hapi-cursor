---
phase: 11-test-gap-fill
plan: 05
type: execute
wave: 2
depends_on: [11-01, 11-02, 11-03, 11-04]
files_modified:
  - scripts/check-no-cut-agents.sh
  - .planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md
autonomous: true
requirements: [REFT-01, REFT-02, REFT-03]

must_haves:
  truths:
    - "`scripts/check-no-cut-agents.sh` ends with a Phase 11 guard block (REFT-01 + cross-runner imports) that exits 0 on the current tree (D-179 — Phase 11 guard not overlapping Phase 5/6/10 patterns; orchestrator override 2026-05-23 provides exact pattern)"
    - "Guard sub-check #1: ripgrep for `permissionMode\\s*===\\s*['\"]` scoped to `cli/src hub/src web/src shared/src`, excluding `cli/src/agent/`, `cli/src/cursor/`, and `*.test.ts(x)` — yields zero hits today and fails the build the moment any new hardcoded permissionMode literal comparison lands outside the two legitimate homes (REFT-01 source-of-truth invariant)"
    - "Guard sub-check #2: ripgrep for `from\\s+['\"]bun:test['\"]` in `cli/src` and `web/src` yields zero (Vitest-only packages); ripgrep for `from\\s+['\"]vitest['\"]` in `hub/src` and `shared/src` yields zero (bun:test-only packages) — TESTING.md cross-runner anti-pattern enforced"
    - "Phase 11 guard block pattern is verified disjoint from Phase 5 (`flavor\\s*===`), Phase 6 (`permissionModeToAgentArgs`, `as string`), and Phase 10 (config field names) — RESEARCH § Code Examples confirmed"
    - "Phase 11 coverage numbers for the five scopes are recorded in 11-DISCUSSION-LOG.md under `## Phase 11 Coverage After` (D-188)"
    - "Non-regression assertion vs Phase 10 baseline is performed and recorded — green or explicit gap reason per Phase 10 baseline availability (orchestrator override 2026-05-23 end-of-phase requirement)"
    - "Per-package full suites green (`bun run test`), `bun typecheck` per package green, full guard script green — Phase 11 ready for verify-phase (D-189 slice cadence completes)"
  artifacts:
    - path: "scripts/check-no-cut-agents.sh"
      provides: "Phase 11 guard block appended before the final `echo \"✅ Phase 10 guard PASS.\"`; closes with `echo \"✅ Phase 11 guard PASS (REFT-01..03).\"`"
      contains: "Phase 11 — REFT guards"
    - path: ".planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md"
      provides: "`## Phase 11 Coverage After` section + non-regression diff vs Phase 10 baseline (or explicit `no baseline` note)"
      contains: "Phase 11 Coverage After"
  key_links:
    - from: "scripts/check-no-cut-agents.sh Phase 11 block"
      to: "cli/src/agent/permissionMatrix.test.ts + cli/src/agent/modeConfig.ts (legitimate homes)"
      via: "ripgrep glob excludes `cli/src/agent/**` and `cli/src/cursor/**` so matrix + SUT survive the sweep"
      pattern: "cli/src/agent"
    - from: "11-DISCUSSION-LOG.md `## Phase 11 Coverage After`"
      to: "11-DISCUSSION-LOG.md `## Phase 10 Coverage Baseline` (written by plan 11-01)"
      via: "table-by-table diff in DISCUSSION-LOG"
      pattern: "Phase 10 Coverage Baseline"
---

<objective>
Close the phase: append the Phase 11 ripgrep guard block to
`scripts/check-no-cut-agents.sh` (D-179, exact pattern from RESEARCH §
Code Examples + orchestrator override 2026-05-23 — scoped to
`cli/src hub/src web/src shared/src`, excluding `cli/src/{agent,cursor}/`
+ `*.test.*`, verified disjoint from Phase 1–10 patterns); recapture
Phase 11 coverage numbers under `## Phase 11 Coverage After` in
`11-DISCUSSION-LOG.md`; diff against the Phase 10 baseline captured by
plan 11-01 and assert non-regression. Run the full per-package suites +
typecheck + guard script as the phase gate.

Purpose: Closes ROADMAP §Phase 11 Success Criterion #1 enforcement
mechanism (matrix is the single source of truth for `PermissionMode →
CLI flag` decisions), CONTEXT D-179 (Phase 11 guard not overlapping Phase
5/6/10), CONTEXT D-188 (coverage baseline recorded, no CI gate added),
CONTEXT D-189 (slice cadence — each slice green; this is the closing
sweep), and ROADMAP §Phase 11 SC#4 (coverage non-regression).

Output: Phase 11 guard block appended to the existing guard script;
DISCUSSION-LOG augmented with after-coverage numbers + non-regression
assertion; full phase gate green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-test-gap-fill/11-CONTEXT.md
@.planning/phases/11-test-gap-fill/11-RESEARCH.md
@.planning/phases/11-test-gap-fill/11-VALIDATION.md
@.planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md
@.planning/codebase/TESTING.md

# Existing guard script
@scripts/check-no-cut-agents.sh

# SUMMARYs from prior plans (read for context, NOT modified)
@.planning/phases/11-test-gap-fill/11-01-SUMMARY.md
@.planning/phases/11-test-gap-fill/11-02-SUMMARY.md
@.planning/phases/11-test-gap-fill/11-03-SUMMARY.md
@.planning/phases/11-test-gap-fill/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Phase 11 guard block to scripts/check-no-cut-agents.sh</name>
  <files>scripts/check-no-cut-agents.sh</files>
  <read_first>
    - scripts/check-no-cut-agents.sh (full file — confirm tail ends with `echo "✅ Phase 10 guard PASS."`; new block goes immediately BEFORE that line, mirroring Phase 5/6/10 cadence)
    - .planning/phases/11-test-gap-fill/11-RESEARCH.md § Code Examples → "Phase 11 guard block" (exact bash snippet to adopt verbatim; pattern verified disjoint from Phase 5/6/10)
    - .planning/phases/11-test-gap-fill/11-CONTEXT.md D-179 + § Specifics (guard caveats: do NOT use raw `permissionMode` substring; use structural pattern; do NOT scan test files)
  </read_first>
  <action>
    Open `scripts/check-no-cut-agents.sh`. Locate the final line `echo "✅ Phase 10 guard PASS."`. Insert the Phase 11 guard block IMMEDIATELY BEFORE that line, exactly per RESEARCH § Code Examples. Concretely the block must:

    1. Open with a comment header `# ===== Phase 11 — REFT guards =====` followed by the rationale comment (purpose: closes REFT-01..REFT-03; sub-check #1 = hardcoded permissionMode literals outside cli/src/{agent,cursor}/; sub-check #2 = cross-runner import sanity).
    2. Define `PHASE11_BRANCH_PATTERN='permissionMode\s*===\s*['\''"]'`. Use the orchestrator-decided pattern from override 2026-05-23 verbatim. Do NOT widen, narrow, or alter quoting.
    3. Define `PHASE11_SOURCE_DIRS=(cli/src hub/src web/src shared/src)`.
    4. Sub-check #1 (REFT-01 source-of-truth invariant): `"$RG_BIN" -n "$PHASE11_BRANCH_PATTERN" "${PHASE11_SOURCE_DIRS[@]}" --glob '!**/*.test.ts' --glob '!**/*.test.tsx' --glob '!cli/src/agent/**' --glob '!cli/src/cursor/**' 2>/dev/null || true` captured into `PHASE11_BRANCH_HITS`. If non-empty: print hits, print failure message ("hardcoded permissionMode === '<literal>' outside cli/src/{agent,cursor}/. Route the decision through permissionModeToCursorArgs (cli/src/agent/modeConfig.ts) or getCapability(...) so the matrix test in permissionMatrix.test.ts owns the contract."), `exit 1`. If empty: `echo "✅ Phase 11 #1: no permissionMode === '<literal>' branches outside cli/src/{agent,cursor}/."`.
    5. Sub-check #2 (TESTING.md cross-runner anti-pattern): `"$RG_BIN" -n "from\s+['\"]bun:test['\"]" cli/src web/src 2>/dev/null || true` into `PHASE11_BAD_BUNTEST` → non-empty fails with "'bun:test' imported in cli/ or web/ (must use Vitest)". Then `"$RG_BIN" -n "from\s+['\"]vitest['\"]" hub/src shared/src 2>/dev/null || true` into `PHASE11_BAD_VITEST` → non-empty fails with "'vitest' imported in hub/ or shared/ (must use bun:test)". Both empty: `echo "✅ Phase 11 #2: test-runner imports respect package boundaries."`.
    6. Close with `echo "✅ Phase 11 guard PASS (REFT-01..03)."` BEFORE the existing `echo "✅ Phase 10 guard PASS."`.

    Do NOT modify any pattern, glob, or message belonging to Phase 1–10 blocks. Disjointness with Phase 5/6/10 patterns is verified in RESEARCH § Code Examples — the new block contributes only the `permissionMode\s*===` structural pattern and the cross-runner import check, both absent from earlier blocks.

    Do NOT introduce new whitelist entries. The exclusion globs (`cli/src/agent/**`, `cli/src/cursor/**`, `**/*.test.ts(x)`) ARE the whitelist for sub-check #1.

    After editing, `chmod +x scripts/check-no-cut-agents.sh` should already be set — confirm with `test -x scripts/check-no-cut-agents.sh`. Do NOT change the shebang or `set -euo pipefail`.
  </action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-no-cut-agents.sh` exits 0.
    - Script output contains the two literal lines `✅ Phase 11 #1: no permissionMode === '<literal>' branches outside cli/src/{agent,cursor}/.` and `✅ Phase 11 #2: test-runner imports respect package boundaries.` and `✅ Phase 11 guard PASS (REFT-01..03).`.
    - `grep -c "===== Phase 11 — REFT guards =====" scripts/check-no-cut-agents.sh` returns exactly 1.
    - `grep -E "PHASE11_BRANCH_PATTERN='permissionMode" scripts/check-no-cut-agents.sh` returns exactly 1 match (the structural pattern is in place — no raw `permissionMode` substring search).
    - `grep -c "permissionMode\b" scripts/check-no-cut-agents.sh` is small (≤ 3 — only the pattern variable + failure message references, not a raw substring sweep).
    - The new Phase 11 guard block is inserted ahead of the script's final success message: `grep -n 'guard PASS' scripts/check-no-cut-agents.sh` shows the `Phase 11 guard PASS (REFT-01..03).` line at a LOWER line number than the script-final `Phase 10 guard PASS.` line (the existing Phase 10 PASS remains the last echo as the script-level OK marker; the Phase 11 PASS is appended immediately before it).
    - Mutation test (manual mental check, executor performs and reverts before commit): adding `if (permissionMode === 'plan')` to a temporary file at `cli/src/foo.ts`, re-running the guard, MUST fail with the Phase 11 #1 message. Adding `import { describe } from 'bun:test'` to a temporary `cli/src/bar.ts`, re-running, MUST fail with the Phase 11 #2 message. Executor reverts both before committing.
    - `git diff --stat scripts/check-no-cut-agents.sh` shows only additive changes (no Phase 1–10 line modified).
  </acceptance_criteria>
  <done>Phase 11 guard block appended; full guard script green; mutation simulations confirm both sub-checks fire on the right violations.</done>
</task>

<task type="auto">
  <name>Task 2: Capture Phase 11 coverage numbers + non-regression diff in 11-DISCUSSION-LOG.md; run full phase gate</name>
  <files>.planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md</files>
  <read_first>
    - .planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md (read the `## Phase 10 Coverage Baseline` section written by plan 11-01 — extract the five baseline numbers, or note `unavailable`)
    - .planning/phases/11-test-gap-fill/11-RESEARCH.md § Code Examples → "Coverage snapshot command (end-of-phase per D-188)"
    - .planning/phases/11-test-gap-fill/11-CONTEXT.md D-188 (record numbers, no CI gate) + D-189 (slice cadence — closing slice MUST be green)
  </read_first>
  <action>
    Two parts: run coverage + write results, then run the full phase gate.

    Part A — Phase 11 coverage capture (on the current Phase 11 branch HEAD, not on main):
    1. Run the three coverage commands from RESEARCH § Code Examples, on the CURRENT branch (Phase 11 tip with all four prior plans merged). Note: `tools:unpack` is the cli package's bundled-tool extraction step required by `cli/package.json#scripts.test`; we invoke it explicitly because `vitest run --coverage` bypasses the `test` script wrapper. The `tools:unpack` prefix appears ONLY on the `cd cli && ...` command — `hub` and `web` do not have it and MUST NOT be prefixed with it.
       - `cd cli && bun run tools:unpack && bun run vitest run --coverage --reporter=text --coverage.include='src/cursor/**' --coverage.include='src/agent/**'`
       - `cd hub && bun test --coverage 2>&1 | grep -E '(auth\.ts|sse/)'`
       - `cd web && bun run vitest run --coverage --reporter=text --coverage.include='src/hooks/useSSE.ts'`
    2. Extract line-coverage numbers (or `no provider` fallbacks) for the same five scopes as plan 11-01: `cli/src/cursor/`, `cli/src/agent/`, `hub/src/web/routes/auth.ts`, `hub/src/sse/`, `web/src/hooks/useSSE.ts`.
    3. Append to `11-DISCUSSION-LOG.md`:

       ```
       ## Phase 11 Coverage After (captured YYYY-MM-DD)

       Captured from: Phase 11 branch HEAD `<SHA-short>`

       | Scope | Phase 11 Line Coverage | Phase 10 Baseline | Delta | Non-Regression |
       |-------|------------------------|-------------------|-------|----------------|
       | cli/src/cursor/ | <NN.NN%> | <from Plan 11-01> | <+NN.NN pp or `n/a`> | ✅ / ❌ / n/a |
       | cli/src/agent/ | <NN.NN%> | ... | ... | ... |
       | hub/src/web/routes/auth.ts | ... | ... | ... | ... |
       | hub/src/sse/ | ... | ... | ... | ... |
       | web/src/hooks/useSSE.ts | ... | ... | ... | ... |

       **Non-regression verdict:** <GREEN — all scopes ≥ baseline within 0.5 pp tolerance>
       OR
       **Non-regression verdict:** <DEFERRED — Phase 10 baseline `unavailable` for N/5 scopes; Phase 11 numbers declared the new baseline per RESEARCH Open Question #3>
       OR
       **Non-regression verdict:** <RED — scope X regressed by N pp; investigate before phase close>

       Per D-188: no CI gate added. Non-regression is a manual assertion at phase close.
       Per orchestrator override 2026-05-23: assertion is recorded in this section.
       ```

    4. Tolerance for `✅`: Phase 11 number ≥ baseline − 0.5 percentage points (small jitter allowed for test-only additions that don't touch production paths). Anything worse → `❌` and write a one-line investigation note in DISCUSSION-LOG.
    5. If baseline is `unavailable` for a scope: mark `n/a` in the non-regression column and note in the verdict (DEFERRED path acceptable per RESEARCH Open Question #3 + orchestrator override).

    Part B — Full phase gate (D-189 closing slice — every package independently green):
    1. `cd cli && bun typecheck` → must exit 0.
    2. `cd hub && bun typecheck` → must exit 0.
    3. `cd web && bun typecheck` → must exit 0.
    4. `cd shared && bun typecheck` (if applicable; skip if no script) → must exit 0.
    5. `bun run test` (from repo root) → must exit 0 (runs cli + hub + web sequentially per TESTING.md).
    6. `bash scripts/check-no-cut-agents.sh` → must exit 0 (Phase 11 block now in place from Task 1).
    7. Record gate results in `11-DISCUSSION-LOG.md` under a `## Phase 11 Gate Results (YYYY-MM-DD)` heading: one bullet per command with `✅` / `❌`. ANY ❌ → STOP, do NOT mark the plan done, surface the failure to the user.

    No production source files are touched by this task. Only `11-DISCUSSION-LOG.md` is modified.
  </action>
  <verify>
    <automated>bun run test &amp;&amp; bash scripts/check-no-cut-agents.sh &amp;&amp; grep -q '^## Phase 11 Coverage After' .planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md &amp;&amp; grep -q '^## Phase 11 Gate Results' .planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/11-test-gap-fill/11-DISCUSSION-LOG.md` contains BOTH `## Phase 11 Coverage After (captured YYYY-MM-DD)` AND `## Phase 11 Gate Results (YYYY-MM-DD)` headings.
    - Phase 11 Coverage After section has a 5-row table with the same scope set as Phase 10 baseline; Delta column populated (or `n/a`); Non-Regression column populated.
    - Non-regression verdict line is one of: GREEN / DEFERRED (with rationale) / RED (with investigation note). If RED, the plan MUST NOT be marked done — investigation results recorded and user notified.
    - Phase 11 Gate Results section lists at least: `bun run test` ✅, `bash scripts/check-no-cut-agents.sh` ✅, and per-package `bun typecheck` ✅.
    - `git status` shows only `11-DISCUSSION-LOG.md` modified (plus Task 1's `scripts/check-no-cut-agents.sh`); no stray source-tree changes.
    - `bun run test` exits 0 (root command).
    - `bash scripts/check-no-cut-agents.sh` exits 0 (closing Phase 11 + Phase 10 PASS messages both shown).
  </acceptance_criteria>
  <done>Phase 11 coverage numbers recorded, non-regression verdict reached (GREEN or DEFERRED), full phase gate green; Phase 11 ready for verify-phase.</done>
</task>

</tasks>

<verification>
- `bash scripts/check-no-cut-agents.sh` exits 0 and prints all Phase 1–11 PASS lines.
- `bun run test` exits 0 from repo root.
- `cd cli && bun typecheck` + `cd hub && bun typecheck` + `cd web && bun typecheck` all exit 0.
- `cd cli && bun run vitest run src/agent/permissionMatrix.test.ts` green (REFT-01).
- `cd hub && bun test src/web/routes/auth.test.ts src/web/middleware/auth.test.ts` green (REFT-03).
- `cd web && bun run vitest run src/hooks/useSSE.test.tsx` green (REFT-02, including new convergence block).
- `11-DISCUSSION-LOG.md` contains both new Phase 11 sections (Coverage After + Gate Results) with non-regression verdict GREEN or DEFERRED (RED would block phase close).
</verification>

<success_criteria>
Phase 11 closed end-to-end: matrix test owns REFT-01 contract enforced by guard sub-check #1; cross-runner anti-pattern enforced by sub-check #2; coverage numbers captured for both Phase 10 and Phase 11; non-regression verdict recorded; full phase gate green.
</success_criteria>

<output>
Create `.planning/phases/11-test-gap-fill/11-05-SUMMARY.md` recording: the Phase 11 guard block was appended with the orchestrator-specified pattern, mutation tests confirmed both sub-checks fire, Phase 11 coverage table + non-regression verdict (with values), full gate command results, and any deviations.
</output>
