---
phase: 11-test-gap-fill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/src/agent/permissionMatrix.test.ts
autonomous: true
requirements: [REFT-01]

must_haves:
  truths:
    - "A single matrix test asserts every Cursor PermissionMode row maps to the exact `CursorArgsFragment` returned by `permissionModeToCursorArgs` (D-177 — strict `toEqual`, never partial match)"
    - "Adding a new `PermissionMode` literal to `shared/src/modes.ts` without adding a matrix row fails `bun typecheck` (D-176 — type-exhaustive `satisfies Record<PermissionMode, ExpectedSpec>`)"
    - "Removing a key from the matrix while it remains in `CURSOR_PERMISSION_MODES` fails at runtime (D-176 — `expect(Object.keys(MATRIX).sort()).toEqual([...CURSOR_PERMISSION_MODES].sort())`)"
    - "Matrix keys are also cross-checked against `FLAVOR_CAPS.cursor.permissionModes` so the capability table and SUT mapping cannot silently drift (RESEARCH § Pattern 1)"
    - "The new file is dedicated to REFT-01 (D-178 — `modeConfig.test.ts` keeps its `UnknownPermissionModeError` and behavioral cases untouched)"
  artifacts:
    - path: "cli/src/agent/permissionMatrix.test.ts"
      provides: "Cursor permission contract matrix (REFT-01) — type-exhaustive + runtime key cross-check + per-row deep-equal"
      min_lines: 40
      contains: "satisfies Record<PermissionMode, ExpectedSpec>"
  key_links:
    - from: "cli/src/agent/permissionMatrix.test.ts"
      to: "cli/src/agent/modeConfig.ts::permissionModeToCursorArgs"
      via: "direct import + per-row `expect(...).toEqual(...)`"
      pattern: "permissionModeToCursorArgs"
    - from: "cli/src/agent/permissionMatrix.test.ts"
      to: "shared/src/modes.ts::CURSOR_PERMISSION_MODES"
      via: "`import { CURSOR_PERMISSION_MODES } from '@hapi/protocol/modes'` (RESEARCH § Pattern 1 — must NOT hardcode the array locally)"
      pattern: "CURSOR_PERMISSION_MODES"
    - from: "cli/src/agent/permissionMatrix.test.ts"
      to: "shared/src/flavors.ts::FLAVOR_CAPS.cursor.permissionModes"
      via: "`import { FLAVOR_CAPS } from '@hapi/protocol/flavors'` capability-table alignment cross-check"
      pattern: "FLAVOR_CAPS\\.cursor\\.permissionModes"
---

<objective>
Add `cli/src/agent/permissionMatrix.test.ts` implementing REFT-01:
a Cursor `PermissionMode → CursorArgsFragment` matrix that is
(a) type-exhaustive at compile time, (b) runtime key-set verified
against `CURSOR_PERMISSION_MODES` and `FLAVOR_CAPS.cursor.permissionModes`,
and (c) row-asserted with strict `toEqual` against
`permissionModeToCursorArgs`. No production code changes.

Purpose: Closes REFT-01 and ROADMAP §Phase 11 Success Criterion #1
("adding a new mode without a matrix row fails the test").

Output: New file `cli/src/agent/permissionMatrix.test.ts`. No edits to
`cli/src/agent/modeConfig.ts` or `cli/src/agent/modeConfig.test.ts`
(D-178: complementary, non-overlapping).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-test-gap-fill/11-CONTEXT.md
@.planning/phases/11-test-gap-fill/11-RESEARCH.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md

# Source-of-truth files (DO NOT modify in this plan)
@cli/src/agent/modeConfig.ts
@cli/src/agent/modeConfig.test.ts
@cli/src/cursor/modes.ts
@shared/src/modes.ts
@shared/src/flavors.ts

<interfaces>
<!-- Extracted from cli/src/agent/modeConfig.ts (Read in planning phase) -->
From `cli/src/agent/modeConfig.ts`:
- `export type CursorArgsFragment = { mode?: 'plan' | 'ask'; yolo?: boolean }`
- `export function permissionModeToCursorArgs(mode: PermissionMode | undefined): CursorArgsFragment`

From `cli/src/cursor/modes.ts`:
- `export type PermissionMode = CursorPermissionMode` (re-export chain → shared/src/modes.ts)

From `shared/src/modes.ts`:
- `export const CURSOR_PERMISSION_MODES = ['default', 'plan', 'ask', 'yolo'] as const`
- `export type CursorPermissionMode = (typeof CURSOR_PERMISSION_MODES)[number]`

From `shared/src/flavors.ts`:
- `FLAVOR_CAPS.cursor.permissionModes: readonly CursorPermissionMode[]`

Ground-truth row mapping (RESEARCH § M1, verified from modeConfig.ts source):
| input | return |
|-------|--------|
| `undefined` | `{}` — exercised separately, NOT in the `Record<PermissionMode, …>` literal |
| `'default'` | `{}` |
| `'plan'` | `{ mode: 'plan' }` |
| `'ask'` | `{ mode: 'ask' }` |
| `'yolo'` | `{ yolo: true }` |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create cli/src/agent/permissionMatrix.test.ts implementing REFT-01 matrix</name>
  <files>cli/src/agent/permissionMatrix.test.ts</files>
  <read_first>
    - cli/src/agent/modeConfig.ts (SUT — exact return values per mode)
    - cli/src/agent/modeConfig.test.ts (existing tests — must NOT overlap; this file owns matrix coverage, the existing file owns `UnknownPermissionModeError` + behavioral path coverage)
    - cli/src/cursor/modes.ts (PermissionMode type chain)
    - shared/src/modes.ts (CURSOR_PERMISSION_MODES source-of-truth array)
    - shared/src/flavors.ts (FLAVOR_CAPS.cursor.permissionModes — capability table to cross-check)
    - .planning/codebase/TESTING.md § Vitest pattern (cli / web) and § Anti-patterns
    - .planning/codebase/CONVENTIONS.md (4-space indent, single quotes, named imports, `@hapi/protocol/*` aliases)
    - .planning/phases/11-test-gap-fill/11-RESEARCH.md § Pattern 1 (reference layout) and § M1 (ground-truth table)
  </read_first>
  <behavior>
    - Test 1 (compile-time exhaustiveness, D-176): a `MATRIX` const declared `as const satisfies Record<PermissionMode, ExpectedSpec>` exists. If anyone adds a 5th literal to `CURSOR_PERMISSION_MODES` without extending MATRIX, `bun typecheck` fails.
    - Test 2 (runtime key-set cross-check vs. `CURSOR_PERMISSION_MODES`, D-176 second prong): `expect(Object.keys(MATRIX).sort()).toEqual([...CURSOR_PERMISSION_MODES].sort())` — passes today, fails the moment matrix keys drift from the imported array.
    - Test 3 (capability-table alignment, RESEARCH § Pattern 1): `expect(Object.keys(MATRIX).sort()).toEqual([...FLAVOR_CAPS.cursor.permissionModes].sort())` — guards against `FLAVOR_CAPS` drifting from `CURSOR_PERMISSION_MODES`.
    - Tests 4–7 (per-row strict equality, D-177): for each `[mode, spec]` entry, `expect(permissionModeToCursorArgs(mode)).toEqual(spec.expectedArgs)`. Exact rows per RESEARCH § M1: `default → {}`, `plan → { mode: 'plan' }`, `ask → { mode: 'ask' }`, `yolo → { yolo: true }`.
    - Test 8 (`undefined` carve-out, RESEARCH § Anti-patterns): a dedicated `it('returns {} for undefined')` because `undefined` is not a `PermissionMode` member and would break `satisfies` exhaustiveness if included as a Record key.
  </behavior>
  <action>
    Create `cli/src/agent/permissionMatrix.test.ts` (new file, Vitest, co-located per
    TESTING.md). Implementation requirements:

    - Import from `'vitest'`: `describe`, `expect`, `it` (NEVER `bun:test` per TESTING.md anti-pattern; enforced by Phase 11 guard).
    - Import `permissionModeToCursorArgs` and the `CursorArgsFragment` type from `'./modeConfig'`.
    - Import the `PermissionMode` type from `'@/cursor/modes'`.
    - Import `CURSOR_PERMISSION_MODES` from `'@hapi/protocol/modes'`. Do NOT hardcode `['default','plan','ask','yolo']` locally — RESEARCH § Anti-patterns explicitly forbids this (defeats the guard).
    - Import `FLAVOR_CAPS` from `'@hapi/protocol/flavors'`.
    - Define `type ExpectedSpec = { expectedArgs: CursorArgsFragment }`.
    - Define `const MATRIX = { default: { expectedArgs: {} }, plan: { expectedArgs: { mode: 'plan' } }, ask: { expectedArgs: { mode: 'ask' } }, yolo: { expectedArgs: { yolo: true } } } as const satisfies Record<PermissionMode, ExpectedSpec>`.
    - One outer `describe('Cursor permission contract matrix (REFT-01)', …)` block.
    - Tests as enumerated in `<behavior>`. The per-row loop must iterate `Object.entries(MATRIX) as Array<[PermissionMode, ExpectedSpec]>` and dynamically name each `it` (`mode '${mode}' produces ${JSON.stringify(spec.expectedArgs)} (D-177)`).
    - Add a top-of-file JSDoc anchoring `REFT-01` and listing D-176 / D-177 / D-178 so future readers immediately see why this file is separate from `modeConfig.test.ts`.
    - Follow CONVENTIONS.md: 4-space indent, single quotes, named imports only, no default export.
    - File MUST NOT touch `modeConfig.ts`, `modeConfig.test.ts`, or any other production file (D-178, D-190).
  </action>
  <verify>
    <automated>cd cli &amp;&amp; bun run vitest run src/agent/permissionMatrix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cli/src/agent/permissionMatrix.test.ts` exists.
    - `cd cli && bun run vitest run src/agent/permissionMatrix.test.ts` exits 0 with at least 7 passing tests (1 key cross-check vs CURSOR_PERMISSION_MODES + 1 vs FLAVOR_CAPS + 4 per-row + 1 undefined carve-out — implementations may legitimately split or merge but total passing count MUST be ≥ 7).
    - `bun typecheck` (or `cd cli && bun typecheck`) exits 0; the `satisfies Record<PermissionMode, ExpectedSpec>` clause compiles.
    - `grep -E "from\s+'bun:test'" cli/src/agent/permissionMatrix.test.ts` returns no matches (Phase 11 guard #2 prep).
    - `grep -E "\\['default'\\s*,\\s*'plan'\\s*,\\s*'ask'\\s*,\\s*'yolo'\\]" cli/src/agent/permissionMatrix.test.ts` returns no matches (no hardcoded literal array — must use `CURSOR_PERMISSION_MODES` import).
    - `git diff --name-only` shows only `cli/src/agent/permissionMatrix.test.ts` added; `modeConfig.ts` and `modeConfig.test.ts` unmodified.
    - Mutation simulation: temporarily renaming the `yolo` key in MATRIX to `yolo2` causes `bun typecheck` to fail (exhaustiveness violated). This is a manual mental check by the executor before commit — no automated assertion required, but the executor MUST confirm by running the mutation locally and reverting before committing.
  </acceptance_criteria>
  <done>REFT-01 matrix file committed; all matrix assertions pass; `modeConfig.ts` + `modeConfig.test.ts` unchanged.</done>
</task>

</tasks>

<verification>
- `cd cli && bun run vitest run src/agent/permissionMatrix.test.ts` green.
- `cd cli && bun run vitest run src/agent/` green (no regression in `modeConfig.test.ts`).
- `cd cli && bun typecheck` green.
- `git diff --stat HEAD~1 HEAD` after commit shows exactly one new file.
</verification>

<success_criteria>
REFT-01 closed: Cursor permission contract matrix exists, is type-exhaustive, runtime-key-checked against the canonical source-of-truth array AND the capability table, and per-row asserted with strict `toEqual`. Adding a new mode literal to `shared/src/modes.ts` without extending MATRIX fails typecheck.
</success_criteria>

<output>
Create `.planning/phases/11-test-gap-fill/11-02-SUMMARY.md` recording: number of tests in the new file, confirmation that `modeConfig.test.ts` was untouched, and the executor's mutation-check result (temporary rename caused typecheck failure → revert succeeded).
</output>
