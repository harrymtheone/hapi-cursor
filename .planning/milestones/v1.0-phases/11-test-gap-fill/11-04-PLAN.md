---
phase: 11-test-gap-fill
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/hooks/useSSE.ts
  - web/src/hooks/useSSE.test.tsx
autonomous: true
requirements: [REFT-02]

must_haves:
  truths:
    - "`useSSE.ts` exports the five existing backoff constants (`HEARTBEAT_STALE_MS`, `HEARTBEAT_WATCHDOG_INTERVAL_MS`, `RECONNECT_BASE_DELAY_MS`, `RECONNECT_MAX_DELAY_MS`, `RECONNECT_JITTER_MS`) — values UNCHANGED (D-190 carve-out adopted per orchestrator override 2026-05-23)"
    - "REFT-02 reconnect tests import those constants from `./useSSE` and drive `vi.advanceTimersByTimeAsync` off them — no test-local duplicates"
    - "REFT-02 test asserts that after `emitError()` on the active `MockEventSource`, a new instance is constructed within `RECONNECT_BASE_DELAY_MS + RECONNECT_JITTER_MS` of fake-timer advance"
    - "REFT-02 test asserts that after dropped intermediate events + reconnect + authoritative snapshot dispatch, the TanStack Query `sessions` cache equals the server's authoritative snapshot (cache convergence, D-181 / D-183 — never asserts intermediate patch shape)"
    - "`MockEventSource` (in `useSSE.test.tsx`) gains `emitOpen()` and `emitError()` methods — mock-only additions, do NOT alter `useSSE` behavior"
    - "The CONTEXT.md case `'达到 retry budget 上限后停止重连'` is DROPPED — there is no MAX_RETRIES in `useSSE.ts` (RESEARCH § M2 ground-truth correction adopted by orchestrator override 2026-05-23); replaced by the bounded-window convergence assertion above"
    - "Existing `useSSE.test.tsx` tests (including the Phase-7 `backgroundTaskCount` regression) remain green — no harness reshape outside the new REFT-02 describe block"
    - "`vi.useFakeTimers()` is scoped to the REFT-02 describe block only (RESEARCH § Pitfall 3 — never leaks into the existing top-level describe)"
  artifacts:
    - path: "web/src/hooks/useSSE.ts"
      provides: "Module-level `export const` on five backoff constants (D-190 carve-out)"
      contains: "export const RECONNECT_BASE_DELAY_MS"
    - path: "web/src/hooks/useSSE.test.tsx"
      provides: "REFT-02 reconnect convergence describe block + MockEventSource emitOpen/emitError"
      contains: "REFT-02"
  key_links:
    - from: "web/src/hooks/useSSE.test.tsx (REFT-02 block)"
      to: "web/src/hooks/useSSE.ts backoff constants"
      via: "named import `{ RECONNECT_BASE_DELAY_MS, RECONNECT_JITTER_MS, RECONNECT_MAX_DELAY_MS }`"
      pattern: "RECONNECT_BASE_DELAY_MS"
    - from: "REFT-02 test"
      to: "MockEventSource"
      via: "`globalThis.EventSource = MockEventSource` monkey-patch (existing seam, RESEARCH § M2 recommendation)"
      pattern: "globalThis\\.EventSource"
---

<objective>
Close REFT-02: add SSE reconnect / patch-loss convergence coverage to
`web/src/hooks/useSSE.test.tsx`, driven off the five existing backoff
constants in `useSSE.ts` which this plan minimally exports (D-190 carve-out,
no value changes). The MockEventSource already monkey-patched onto
`globalThis.EventSource` in the existing test file gains `emitOpen()` and
`emitError()` methods to drive the `onerror → reconnect → onopen` cycle.

Tests assert final TanStack Query cache state only (D-181 / D-183 — never
intermediate patch shape), so they survive Phase 7's pending useSSE
rewrite.

Purpose: Closes REFT-02 and ROADMAP §Phase 11 Success Criterion #2
("SSE reconnect / patch-loss invariant test simulates dropped events plus
reconnection; the front-end query cache converges to the authoritative
server state within a bounded retry budget").

Output: Two files modified — `useSSE.ts` (five `const` → `export const`,
zero value changes) and `useSSE.test.tsx` (mock extension + new describe
block).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-test-gap-fill/11-CONTEXT.md
@.planning/phases/11-test-gap-fill/11-RESEARCH.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md

@web/src/hooks/useSSE.ts
@web/src/hooks/useSSE.test.tsx

<interfaces>
<!-- Extracted from web/src/hooks/useSSE.ts in planning phase -->
From `web/src/hooks/useSSE.ts` (lines 27–31, module-local consts to be exported AS-IS):
- `const HEARTBEAT_STALE_MS = 90_000`
- `const HEARTBEAT_WATCHDOG_INTERVAL_MS = 10_000`
- `const RECONNECT_BASE_DELAY_MS = 1_000`
- `const RECONNECT_MAX_DELAY_MS = 30_000`
- `const RECONNECT_JITTER_MS = 500`

Reconnect schedule (lines 151–163, behavior pinned by test, NOT modified): `delay = min(RECONNECT_MAX_DELAY_MS, RECONNECT_BASE_DELAY_MS * 2^attempt) + uniform(0, RECONNECT_JITTER_MS)`. `attempt` resets to 0 on successful `onopen`. NO `MAX_RETRIES` — hook retries indefinitely (RESEARCH § M2 — ground-truth correction).

Reconnect triggers (test can drive these):
- `onerror` with `readyState === EventSource.CLOSED` → `requestReconnect('closed')` (lines 414–418).
- Heartbeat watchdog (lines 423–434).
- Visibility recovery (lines 440–447).

MockEventSource (existing, in `useSSE.test.tsx` lines 18–48): supports `addEventListener`/`dispatchEvent`/`readyState`/`close` and tracks instances in `MockEventSource.instances`. Lacks `emitOpen`/`emitError` — Task 2 adds them.

Test seam: `beforeEach(() => { globalThis.EventSource = MockEventSource as unknown as typeof EventSource })` + `afterEach` restore (lines 159–178). KEEP — do NOT switch to factory-param seam (RESEARCH § M2 recommendation, honors D-190).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export the five backoff constants from useSSE.ts (D-190 carve-out — no value changes)</name>
  <files>web/src/hooks/useSSE.ts</files>
  <read_first>
    - web/src/hooks/useSSE.ts (lines 27–31 — the five const declarations to convert)
    - .planning/phases/11-test-gap-fill/11-CONTEXT.md D-190 (sole production-code exception this phase)
    - .planning/phases/11-test-gap-fill/11-RESEARCH.md § M2 → "D-190 minimal export (recommended)"
  </read_first>
  <action>
    Edit `web/src/hooks/useSSE.ts` lines 27–31. For each of the following five
    declarations, prepend `export` (only). Do NOT change any numeric value, do
    NOT rename, do NOT reorder, do NOT move to a separate module.

    Before → After:
    - `const HEARTBEAT_STALE_MS = 90_000` → `export const HEARTBEAT_STALE_MS = 90_000`
    - `const HEARTBEAT_WATCHDOG_INTERVAL_MS = 10_000` → `export const HEARTBEAT_WATCHDOG_INTERVAL_MS = 10_000`
    - `const RECONNECT_BASE_DELAY_MS = 1_000` → `export const RECONNECT_BASE_DELAY_MS = 1_000`
    - `const RECONNECT_MAX_DELAY_MS = 30_000` → `export const RECONNECT_MAX_DELAY_MS = 30_000`
    - `const RECONNECT_JITTER_MS = 500` → `export const RECONNECT_JITTER_MS = 500`

    Nothing else changes in this file. No other lines edited, no imports added,
    no JSDoc churn (a single one-line `// D-190: exported for REFT-02 testability — values unchanged` immediately above the block is permitted but not required).

    Per D-190: this is the SOLE permitted production code change in Phase 11.
    Any additional edit to `useSSE.ts` is scope creep — open an issue instead.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun typecheck &amp;&amp; bun run vitest run src/hooks/useSSE.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^export const HEARTBEAT_STALE_MS = 90_000\b" web/src/hooks/useSSE.ts` matches exactly 1 line.
    - `grep -E "^export const HEARTBEAT_WATCHDOG_INTERVAL_MS = 10_000\b" web/src/hooks/useSSE.ts` matches exactly 1 line.
    - `grep -E "^export const RECONNECT_BASE_DELAY_MS = 1_000\b" web/src/hooks/useSSE.ts` matches exactly 1 line.
    - `grep -E "^export const RECONNECT_MAX_DELAY_MS = 30_000\b" web/src/hooks/useSSE.ts` matches exactly 1 line.
    - `grep -E "^export const RECONNECT_JITTER_MS = 500\b" web/src/hooks/useSSE.ts` matches exactly 1 line.
    - `git diff web/src/hooks/useSSE.ts` shows ONLY 5 changed lines (each adds the `export ` prefix); no other diff hunks. Verify with `git diff --stat` → exactly one file, ≤ 6 insertions / ≤ 5 deletions.
    - `cd web && bun typecheck` exits 0.
    - `cd web && bun run vitest run src/hooks/useSSE.test.tsx` exits 0 (existing tests still green — no behavioral change).
  </acceptance_criteria>
  <done>Five constants exported as-is; no other diff; web suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend MockEventSource with emitOpen/emitError and add REFT-02 reconnect convergence describe block</name>
  <files>web/src/hooks/useSSE.test.tsx</files>
  <read_first>
    - web/src/hooks/useSSE.test.tsx (full file — existing harness lines 18–48 (MockEventSource), 123–157 (createHarness/mountUseSSE if present, otherwise the existing renderHook setup), 159–178 (beforeEach/afterEach EventSource monkey-patch). Identify the existing helpers the new describe block can reuse — DO NOT duplicate harness code.)
    - web/src/hooks/useSSE.ts (after Task 1 — lines 27–31 are now `export const`; reconnect logic at 145–185 and 414–434 is the behavior under test, NOT modified)
    - .planning/phases/11-test-gap-fill/11-CONTEXT.md D-180 / D-181 / D-182 / D-183 / D-190
    - .planning/phases/11-test-gap-fill/11-RESEARCH.md § M2 (ground truth + MockEventSource gaps + bounded-budget assertion shape) + § Pattern 2 (reference structure) + § Pitfall 3 (fake-timer leakage) + § Pitfall 6 (waitFor under fake timers)
    - .planning/codebase/TESTING.md § React Component Testing (web) — `<I18nProvider>`, fresh `QueryClient` per test
  </read_first>
  <behavior>
    Two new behavior groups inside a single new `describe('useSSE reconnect convergence (REFT-02)', …)` block, scoped fake timers:

    Group A — bounded reconnect window:
    - `it('reconnects within bounded backoff after onerror + readyState=CLOSED')` — after first `emitOpen()` on the active mock, call `emitError()` (which sets `readyState = CLOSED` and fires `onerror`). Assert `MockEventSource.instances.length === 1` immediately, then `await act(async () => await vi.advanceTimersByTimeAsync(RECONNECT_BASE_DELAY_MS + RECONNECT_JITTER_MS))`, then assert `MockEventSource.instances.length === 2` AND `activeSource() !== firstInstance`. The "bounded budget" is the wall-clock window per attempt (RESEARCH § M2 — there is no max-attempt count).

    Group B — cache convergence after dropped events + reconnect:
    - `it('cache converges to authoritative snapshot after dropped intermediate events + reconnect')`:
      1. Seed `queryClient.setQueryData(queryKeys.sessions, { sessions: [A] })` (or whichever shape the existing harness uses — read it from the file).
      2. Open first MockEventSource via `emitOpen()`. Dispatch a `session-added` event for session B → cache holds `[A, B]` (sorted by `sortSessionSummaries`).
      3. `emitError()` → advance timers past backoff → second instance opens.
      4. Between the error and the second `emitOpen()`, server-side events that would have flowed to instance 1 are simply NEVER dispatched (this models the "dropped" intermediate events).
      5. On the second instance, after `emitOpen()`, dispatch an authoritative reconciliation event — either a `session-updated` for A with `updatedAt = 9999` (a number higher than any prior `updatedAt`), or whatever the existing harness uses for server-snapshot reconciliation. (Researcher recommendation: a single `session-updated(A, updatedAt=9999)` is sufficient to prove cache convergence without asserting patch shape — D-183.)
      6. Assert final cache `queryClient.getQueryData(queryKeys.sessions)` equals the expected sorted array (A with updated fields + B). Do NOT inspect any intermediate state, do NOT assert anything about `hasUnknownSessionPatchKeys` or `setQueryData` argument shape (D-183 → Phase-7 safety).

    Optional Group C (recommended if existing harness supports it cheaply):
    - `it('normal reconnect: error → backoff → open does not break subsequent event handling')` — after one full error+reconnect cycle, dispatch a fresh `session-added` for session C on the second instance and assert it lands in cache. Proves the reconnect path doesn't break event processing.

    Pitfall guards:
    - `vi.useFakeTimers()` / `vi.useRealTimers()` MUST be paired inside this describe's `beforeEach`/`afterEach` only. Do NOT enable fake timers in the existing top-level describe (RESEARCH § Pitfall 3).
    - Replace `waitFor(...)` (which polls real timers) with `await act(async () => await vi.advanceTimersByTimeAsync(N))` followed by synchronous `expect(...)` (RESEARCH § Pitfall 6).
    - Always wrap timer advances + state-mutating mock dispatches in `act(async () => { … })`.
  </behavior>
  <action>
    Edit `web/src/hooks/useSSE.test.tsx`. Two edits:

    Edit A — extend MockEventSource (within its existing class declaration, around lines 18–48):
    - Add two methods (mock-only — do NOT change `useSSE.ts`):
      - `emitOpen(): void` — sets `this.readyState = MockEventSource.OPEN`; calls `this.onopen?.(new Event('open'))` (or matching event constructor used elsewhere in the mock). Implementer reads the mock's existing event shape and matches it.
      - `emitError(): void` — sets `this.readyState = MockEventSource.CLOSED`; calls `this.onerror?.(new Event('error'))`.
    - Per RESEARCH § M2, these mirror the dispatcher shape used by `EventSource` so `useSSE`'s registered handlers fire identically to production.
    - Keep all existing methods and the `globalThis.EventSource = MockEventSource as unknown as typeof EventSource` monkey-patch in `beforeEach` untouched.

    Edit B — append new describe block at the bottom of the file (sibling to existing `describe('useSSE handleSyncEvent', …)`, NOT nested):
    - Imports to add at top of file (if not already imported):
      - From `'vitest'`: ensure `vi` is in the existing import; if not, add it.
      - From `'./useSSE'`: `RECONNECT_BASE_DELAY_MS, RECONNECT_JITTER_MS, RECONNECT_MAX_DELAY_MS` (named imports). NEVER define local copies of these numbers — RESEARCH § Anti-patterns / Pattern 2.
    - Structure (RESEARCH § Pattern 2 reference layout):

      ```
      describe('useSSE reconnect convergence (REFT-02)', () => {
          beforeEach(() => { vi.useFakeTimers() })
          afterEach(() => { vi.useRealTimers() })

          it('reconnects within bounded backoff after onerror + readyState=CLOSED', async () => { ... })
          it('cache converges to authoritative snapshot after dropped intermediate events + reconnect', async () => { ... })
          // optional Group C it(...)
      })
      ```

    - Reuse the existing harness helpers (the `createHarness` / `mountUseSSE` / `activeSource` pattern from the existing file — if those names differ, use whatever the existing file uses). Do NOT duplicate `QueryClient` setup, do NOT duplicate `<I18nProvider>` wrappers.
    - Per D-181 / D-183, assertions in Group B MUST terminate on `queryClient.getQueryData(...)` equality. NEVER on `setQueryData` spy arguments, NEVER on `hasUnknownSessionPatchKeys` call counts, NEVER on intermediate patch shape.
    - Add a top-of-block JSDoc comment anchoring `REFT-02`, `D-180`, `D-181`, `D-182`, `D-183`, `D-190`, and the orchestrator override 2026-05-23 (no `MAX_RETRIES` — bounded-window assertion replaces the dropped retry-budget-exhaustion case).

    Per D-190: do NOT modify `useSSE.ts` in this task (Task 1 already covered that). Do NOT touch any other production file.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run vitest run src/hooks/useSSE.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && bun run vitest run src/hooks/useSSE.test.tsx` exits 0.
    - Vitest output shows a top-level `useSSE reconnect convergence (REFT-02)` describe block with ≥ 2 passing tests (Group A bounded-window + Group B cache-convergence). Group C is optional — do not assert its presence.
    - All pre-existing tests in `useSSE.test.tsx` (including the Phase-7 `backgroundTaskCount` regression) remain green — same vitest output shows no test removed.
    - `grep -c "RECONNECT_BASE_DELAY_MS\|RECONNECT_JITTER_MS\|RECONNECT_MAX_DELAY_MS" web/src/hooks/useSSE.test.tsx` returns ≥ 3 imports/uses (constants are imported, not duplicated).
    - `grep -E "^\s*(const|let|var)\s+(RECONNECT|HEARTBEAT)_[A-Z_]+\s*=\s*[0-9]" web/src/hooks/useSSE.test.tsx` returns no matches (no local duplicates of backoff constants).
    - `grep -c "hasUnknownSessionPatchKeys\|setQueryData.*toHaveBeenCalled" web/src/hooks/useSSE.test.tsx` does NOT increase relative to HEAD (D-183 — no patch-shape assertions added).
    - `grep -c "emitOpen\|emitError" web/src/hooks/useSSE.test.tsx` ≥ 4 (mock methods defined + used at least once each).
    - `grep -c "MAX_RETRIES" web/src/hooks/useSSE.test.tsx web/src/hooks/useSSE.ts` returns 0 (orchestrator override: case dropped, constant does not exist).
    - `vi.useFakeTimers()` calls appear ONLY inside the new REFT-02 describe block — verify: the file's top-level (non-REFT-02) describe blocks have no `vi.useFakeTimers` references added by this task. `git diff web/src/hooks/useSSE.test.tsx` confirms the fake-timer block is scoped.
    - `cd web && bun typecheck` exits 0.
  </acceptance_criteria>
  <done>REFT-02 cache convergence under fake-timers green; MockEventSource extended; existing tests unaffected; fake timers scoped; no local constant duplicates.</done>
</task>

</tasks>

<verification>
- `cd web && bun run vitest run src/hooks/useSSE.test.tsx` green, with ≥ 2 new REFT-02 tests passing and all pre-existing tests passing.
- `cd web && bun typecheck` green.
- `cd web && bun run vitest run` (full web suite) green.
- `git diff --stat` shows exactly two files modified: `web/src/hooks/useSSE.ts` (5 lines, all `export` additions) and `web/src/hooks/useSSE.test.tsx` (additive: mock methods + new describe block).
- `git diff web/src/hooks/useSSE.ts` shows only `export` insertions — no value, identifier, or order changes.
</verification>

<success_criteria>
REFT-02 closed: SSE reconnect bounded-window pinned, cache convergence after dropped events + reconnect pinned, both assertions driven off the SUT's own exported constants. Test will survive Phase 7's useSSE rewrite because it asserts only final cache state (D-183).
</success_criteria>

<output>
Create `.planning/phases/11-test-gap-fill/11-04-SUMMARY.md` recording: the exact 5-line diff to `useSSE.ts` (confirm no other lines touched), the test count added to `useSSE.test.tsx`, confirmation that `vi.useFakeTimers` is scoped only to the new describe, and confirmation that the dropped `MAX_RETRIES` case (per orchestrator override) does not appear anywhere in the test file.
</output>
