---
phase: 05-flavor-consolidation-capability-abstraction
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - web/src/components/ToolCard/codexAgents.ts
  - web/src/components/ToolCard/views/CodexDiffView.tsx
  - web/src/components/ToolCard/views/CodexPatchView.tsx
  - web/src/components/ToolCard/views/_all.tsx
  - web/src/components/ToolCard/views/_results.tsx
  - web/src/components/ToolCard/views/_results.test.tsx
  - web/src/components/ToolCard/trace.tsx
  - web/src/components/ToolCard/trace.test.tsx
  - web/src/components/ToolCard/groupedPresentation.ts
  - web/src/components/ToolCard/groupedPresentation.test.ts
  - web/src/components/ToolCard/ToolCard.tsx
  - web/src/components/ToolCard/ToolCard.test.ts
  - web/src/components/ToolCard/knownTools.test.tsx
  - web/src/components/ToolCard/PermissionFooter.tsx
autonomous: true
requirements_addressed: [CUT-05, REFA-01]
must_haves:
  truths:
    - "ToolCard renderer registry no longer references CodexAgent / CodexBash / CodexPatch / CodexDiff / CodexReasoning / spawn_agent / send_input / resume_agent / wait_agent / close_agent."
    - "PermissionFooter no longer imports `isCodexFamilyFlavor`; tone selection reads `getCapability(metadata?.flavor, 'permissionToneCopy')` and the codex JSX subtree, `codexApprove` / `codexAbort` helpers, and `permission.mode === 'acceptEdits'` UI branch are deleted."
    - "`web/src/components/ToolCard/codexAgents.ts`, `views/CodexDiffView.tsx`, `views/CodexPatchView.tsx` files are deleted (verified unreachable per RESEARCH A2)."
    - "`web/src/components/ToolCard/` ripgrep for `\\b(claude|codex|gemini|opencode)\\b` (excluding test fixtures using `AGENT_MESSAGE_PAYLOAD_TYPE`) returns zero hits."
  artifacts:
    - path: "web/src/components/ToolCard/PermissionFooter.tsx"
      provides: "Cursor-tone-only permission footer; capability lookup at the single tone-decision site."
    - path: "web/src/components/ToolCard/views/_all.tsx"
      provides: "Tool view registry with codex entries removed."
    - path: "web/src/components/ToolCard/views/_results.tsx"
      provides: "Result-view router with codex result-view branches removed."
  key_links:
    - from: "web/src/components/ToolCard/PermissionFooter.tsx"
      to: "shared/src/flavors.ts (getCapability)"
      via: "import + call"
      pattern: "getCapability\\(.*'permissionToneCopy'"
---

<objective>
Slice 2a — collapse the ToolCard subsystem to Cursor-only. Delete the dead Codex* renderer files and registry rows (RESEARCH §"Surprises" #3 and Risk 2 — "verified unreachable: Cursor emits no `Codex*` tool names"; rationale anchor in 05-PATTERNS §Codex* renderer files). Rewrite `PermissionFooter.tsx` to read `permissionToneCopy` from the capability table (D-73) and drop the `isCodexFamilyFlavor` import + the `acceptEdits` UI branch (RESEARCH §"Surprises" #9, #10).

Purpose: take the largest single chunk of dead-code surface (≈10 files, ≈1000 LoC) off the board so subsequent web slices have a smaller working set; eliminate the last `isCodexFamilyFlavor` consumer so plan 07 can delete the helper.
Output: ToolCard subsystem free of all Codex* / Claude / Gemini / OpenCode literals; PermissionFooter consuming only `getCapability`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-CONTEXT.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-PATTERNS.md
@.planning/phases/05-flavor-consolidation-capability-abstraction/05-01-PLAN.md

@shared/src/flavors.ts
@web/src/components/ToolCard/PermissionFooter.tsx
@web/src/components/ToolCard/views/_all.tsx
@web/src/components/ToolCard/views/_results.tsx

<interfaces>
From `shared/src/flavors.ts` (post Slice 1a):
- `getCapability(flavor, 'permissionToneCopy'): 'cursor' | 'codex' | null`
- `isKnownFlavor(flavor): flavor is AgentFlavor`

The new PermissionFooter must NOT import `isCodexFamilyFlavor` (still exported from `flavors.ts` until plan 07, but the import goes away here so plan 07 can delete the symbol).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead Codex* renderer files; purge codex entries from view/result registries and grouped tool presentation</name>
  <files>web/src/components/ToolCard/codexAgents.ts, web/src/components/ToolCard/views/CodexDiffView.tsx, web/src/components/ToolCard/views/CodexPatchView.tsx, web/src/components/ToolCard/views/_all.tsx, web/src/components/ToolCard/views/_results.tsx, web/src/components/ToolCard/views/_results.test.tsx, web/src/components/ToolCard/trace.tsx, web/src/components/ToolCard/trace.test.tsx, web/src/components/ToolCard/groupedPresentation.ts, web/src/components/ToolCard/groupedPresentation.test.ts, web/src/components/ToolCard/ToolCard.tsx, web/src/components/ToolCard/ToolCard.test.ts, web/src/components/ToolCard/knownTools.test.tsx</files>
  <read_first>
    - `web/src/components/ToolCard/views/_all.tsx` (lines 1–123 — registry shape; codex imports lines 4–5, 16–19; `CodexAgentView` lines 38–74; `toolViewRegistry` rows 76–114)
    - `web/src/components/ToolCard/views/_results.tsx` (lines 97–292, 492–770 — `CodexBashOutput`, `extractCodexBashDisplay`, `parseCodexBashOutput`, `CodexBashResultView`, `CodexPatchResultView`, `CodexReasoningResultView`, `CodexDiffResultView`, `CodexAgentResultView`)
    - `web/src/components/ToolCard/codexAgents.ts` (whole file — 320 lines, all deleted)
    - `web/src/components/ToolCard/views/CodexDiffView.tsx`, `views/CodexPatchView.tsx` (whole files, deleted)
    - `web/src/components/ToolCard/trace.tsx` (lines 63, 109 — `if (block.tool.name === 'CodexAgent')` branches)
    - `web/src/components/ToolCard/groupedPresentation.ts` (lines 60, 67 — `'CodexBash'`, `'CodexPatch'`, `'CodexDiff'` literals in grouped tool detection)
    - `web/src/components/ToolCard/ToolCard.tsx` (line 29 — `TERMINAL_RELATED_TOOL_NAMES = new Set([..., 'CodexBash', ...])`)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-PATTERNS.md` §"Codex* renderer files (deletion-candidate, file-delete)" (lines 461–528 — exact files to delete + registry rows)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md` §"D. web/" rows for `_all.tsx`, `_results.tsx`, `trace.tsx`, `groupedPresentation.ts`, `ToolCard.tsx`, `knownTools.test.tsx`
  </read_first>
  <behavior>
    - Files DELETED outright: `web/src/components/ToolCard/codexAgents.ts`, `web/src/components/ToolCard/views/CodexDiffView.tsx`, `web/src/components/ToolCard/views/CodexPatchView.tsx`. Verification anchor (RESEARCH A2): `rg 'CodexAgent|CodexBash|CodexPatch|CodexDiff|CodexReasoning' cli/src/cursor/ shared/src/` returns zero hits.
    - `views/_all.tsx`: drop the imports of `CodexDiffCompactView` / `CodexDiffFullView` / `CodexPatchView` and the codex-agents helpers; remove the local `CodexAgentView` component definition; remove every Codex* / `spawn_agent` / `send_input` / `resume_agent` / `wait_agent` / `close_agent` row from `toolViewRegistry` and `toolFullViewRegistry`. Surviving registry contains only Edit / MultiEdit / Write / TodoWrite / update_plan / AskUserQuestion / ExitPlanMode / RequestUserInput entries.
    - `views/_results.tsx`: delete `CodexBashOutput`, `extractCodexBashDisplay`, `parseCodexBashOutput`, `CodexBashResultView`, `CodexPatchResultView`, `CodexReasoningResultView`, `CodexDiffResultView`, `CodexAgentResultView` plus their dispatch entries; mirror deletions in `views/_results.test.tsx` (codex test cases at lines 150 / 316–325 / 411 / 423).
    - `trace.tsx` (and `trace.test.tsx`): drop `if (block.tool.name === 'CodexAgent')` branches at lines 63 and 109; remove the corresponding test assertions at `trace.test.tsx:98` and `:110`.
    - `groupedPresentation.ts`: drop `'CodexBash'`, `'CodexPatch'`, `'CodexDiff'` literals from grouped tool detection at lines 60 and 67; mirror in `groupedPresentation.test.ts:46`.
    - `ToolCard.tsx`: remove `'CodexBash'` from `TERMINAL_RELATED_TOOL_NAMES` (line 29); remove its test references in `ToolCard.test.ts:6`.
    - `knownTools.test.tsx`: delete `'CodexAgent'` test cases.
    - No new code added. No backward-compat stub left (per AGENTS.md "no backward compatibility" + Phase 02 D-22/D-32 precedent).
  </behavior>
  <action>
    Use Delete for the three whole-file deletes (`codexAgents.ts`, `views/CodexDiffView.tsx`, `views/CodexPatchView.tsx`). For the rest, edit in place: remove import lines, remove the named component / helper / row, and remove matching test cases. After each edit, run `cd web && bun typecheck` to surface follow-on errors and fix in the same task. The registry deletions in `_all.tsx` and `_results.tsx` are the load-bearing edits — every surviving consumer (e.g. `trace.tsx`, `ToolCard.tsx`) lines up its tool-name match against those registries; deleting the registry rows is sufficient to dead-eliminate consumers, but explicit `if (block.tool.name === 'CodexAgent')` branches must also be deleted (TS won't error on dead branches matching string literals). 4-space indent. No fenced code in this action.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun typecheck &amp;&amp; bun run test --run web/src/components/ToolCard</automated>
  </verify>
  <done>
    Three files deleted. Registries / dispatchers / grouped-presentation / terminal-tool set / matching tests no longer mention Codex* literals. `cd web && bun typecheck` exit code 0; ToolCard test sub-suite green.
  </done>
  <acceptance_criteria>
    - `test ! -f web/src/components/ToolCard/codexAgents.ts && test ! -f web/src/components/ToolCard/views/CodexDiffView.tsx && test ! -f web/src/components/ToolCard/views/CodexPatchView.tsx` exits 0.
    - `rg -n 'Codex(Agent|Bash|Patch|Diff|Reasoning)' web/src/components/ToolCard/ | grep -v '^#'` returns zero matches.
    - `rg -n "'spawn_agent'|'send_input'|'resume_agent'|'wait_agent'|'close_agent'" web/src/components/ToolCard/views/_all.tsx` returns zero matches.
    - `cd web && bun typecheck` exit code 0.
    - `cd web && bun run test --run web/src/components/ToolCard` exit code 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite `PermissionFooter.tsx` — delete codex tone branch + acceptEdits UI; consume `getCapability(flavor, 'permissionToneCopy')`</name>
  <files>web/src/components/ToolCard/PermissionFooter.tsx</files>
  <read_first>
    - `web/src/components/ToolCard/PermissionFooter.tsx` (whole file — current `isCodexSession` helper lines 25–28; codex JSX block lines 215–238; `codexApprove` / `codexAbort` lines 171–189; `!codex` guards lines 138–139; `permission.mode === 'acceptEdits'` line 43; `approvePermission(..., 'acceptEdits')` line 151; `isCodexFamilyFlavor` import line 7)
    - `shared/src/flavors.ts` (post Slice 1a — `getCapability` helper signature)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-PATTERNS.md` §"`web/src/components/ToolCard/PermissionFooter.tsx`" (lines 353–378 — pattern + target shape)
    - `.planning/phases/05-flavor-consolidation-capability-abstraction/05-RESEARCH.md` §"Surprises" #9 (lines ~63 — acceptEdits/bypassPermissions UI removal) and #10 (lines ~64 — `toolName.startsWith('Codex')` unreachable post-Phase-1)
  </read_first>
  <behavior>
    - `isCodexFamilyFlavor` import removed; `isCodexSession` helper deleted entirely.
    - Tone decision: `const tone = getCapability(props.metadata?.flavor, 'permissionToneCopy') ?? 'cursor'`. The `tone === 'codex'` branch dead-eliminates for Cursor — delete the entire codex JSX subtree (lines 215–238), `codexApprove` / `codexAbort` helper functions (lines 171–189), and the two `!codex` guards (lines 138–139). Keep only the cursor-tone JSX path.
    - `permission.mode === 'acceptEdits'` UI branch (line 43) deleted — Cursor uses only `default` / `plan` / `ask` / `yolo` (RESEARCH §"Surprises" #9, D-78 condition met).
    - `approvePermission(..., 'acceptEdits')` call (line 151) replaced with the cursor-equivalent (`'default'`) OR — per RESEARCH §"Surprises" #9 — that call site is dropped along with the deleted JSX branch. Confirm by re-reading line 151 in context after deleting the codex JSX block; if the call only existed under the `acceptEdits` branch, it disappears with that branch.
    - `toolName.startsWith('Codex')` fallback (originally inside `isCodexSession`) is gone with the helper deletion (D-84#3 zero-hit requirement).
    - Imports cleaned up: drop `isCodexFamilyFlavor`, drop any now-unused permission-mode constants, add `getCapability` from `@hapi/protocol`.
    - 4-space indent.
  </behavior>
  <action>
    Edit `web/src/components/ToolCard/PermissionFooter.tsx` in place. Remove the `isCodexFamilyFlavor` import. Replace the `isCodexSession` helper with a single `const tone = getCapability(props.metadata?.flavor, 'permissionToneCopy') ?? 'cursor'` declaration scoped where the old helper's call sites used the boolean — for Cursor, all `tone === 'codex'` branches dead-eliminate, so delete those branches outright instead of leaving conditional JSX. Delete `codexApprove`, `codexAbort`, `!codex` guards, the codex JSX block, the `permission.mode === 'acceptEdits'` UI branch, and the `'acceptEdits'` argument to `approvePermission`. Run `cd web && bun typecheck` after each delete to keep the file compilable mid-edit. Verify the file contains zero `codex` (case-insensitive) literals and zero `acceptEdits` literals when done. 4-space indent. No fenced code in this action.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun typecheck &amp;&amp; bun run test --run web/src/components/ToolCard</automated>
  </verify>
  <done>
    `PermissionFooter.tsx` reads tone via `getCapability` only; codex JSX / helpers / acceptEdits UI / approvePermission `'acceptEdits'` argument all deleted. `isCodexFamilyFlavor` no longer imported. ToolCard test sub-suite green.
  </done>
  <acceptance_criteria>
    - `rg -ni 'codex' web/src/components/ToolCard/PermissionFooter.tsx` returns zero matches.
    - `rg -n 'isCodexFamilyFlavor' web/src/components/ToolCard/PermissionFooter.tsx` returns zero matches.
    - `rg -n "acceptEdits|bypassPermissions" web/src/components/ToolCard/PermissionFooter.tsx` returns zero matches.
    - `rg -n "getCapability\\(.*'permissionToneCopy'" web/src/components/ToolCard/PermissionFooter.tsx` returns at least one match.
    - `cd web && bun typecheck` exit code 0.
    - Slice gate: `bun typecheck && bun run test` from repo root exit code 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wire (metadata.flavor / tool.name) → web UI | tool-name strings and flavor values arrive via SSE from hub; historical events may carry codex tool names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information disclosure | rendering historical Codex tool blocks | mitigate | After registry purge, codex tool blocks fall through to the generic ToolCard view (graceful degrade per D-76); no PII surface, no exception thrown |
| T-05-02-02 | Tampering | malicious metadata.flavor in SSE | mitigate | `getCapability(unknown, ...)` returns `null`; tone defaults to `'cursor'` via `?? 'cursor'`; codex JSX deleted so tampered `'codex'` value would still render Cursor tone |
</threat_model>

<verification>
- `cd web && bun typecheck` green.
- `cd web && bun run test --run web/src/components/ToolCard` green.
- `bun typecheck && bun run test` from repo root green (slice hard gate per directive #8).
- `rg -ni '\\b(claude|codex|gemini|opencode)\\b' web/src/components/ToolCard/` returns zero hits OR only hits in test fixtures using `AGENT_MESSAGE_PAYLOAD_TYPE` constant import (none expected this slice).
</verification>

<success_criteria>
- ToolCard subsystem contains zero references to Codex* tool names, codex JSX, `isCodexFamilyFlavor`, or `acceptEdits` UI.
- PermissionFooter consumes `getCapability(flavor, 'permissionToneCopy')` and renders cursor tone unconditionally for Cursor metadata.
- Three deletion-candidate files removed.
- Slice-gate `bun typecheck && bun run test` green.
</success_criteria>

<output>
Create `.planning/phases/05-flavor-consolidation-capability-abstraction/05-02-SUMMARY.md` when done. Record: deleted files, registry rows removed, PermissionFooter line-range deletions, and confirmation of `rg -ni 'codex' web/src/components/ToolCard/` zero hits.
</output>
