---
phase: 02-skills-visibility-and-session-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/schemas.ts
  - shared/src/types.ts
  - shared/src/schemas.test.ts
  - shared/src/skillPolicy.ts
  - shared/src/skillPolicy.test.ts
  - cli/src/modules/common/skills.ts
  - cli/src/modules/common/skills.test.ts
  - cli/src/modules/common/handlers/skills.ts
autonomous: true
requirements:
  - SKIL-01
must_haves:
  truths:
    - "D-03: Skill inventory wire shape is defined once in @hapi/protocol with source, invocation mode, validity, and pathHint."
    - "D-04: Invalid skill metadata appears in discovery results with safe invalidReason copy."
    - "D-01/D-02: Discovery scans .cursor/skills and .agents/skills at project and user roots with nested SKILL.md walk and project-over-user dedupe."
    - "D-07: Missing policy rows implicitly mean inherited at the protocol helper layer."
  artifacts:
    - path: "shared/src/schemas.ts"
      provides: "SkillSummarySchema, SkillPolicyStateSchema, SkillPolicyMapSchema, ListSkillsResponseSchema, MetadataSchema.skillPolicy"
      exports: ["SkillSummarySchema", "SkillPolicyStateSchema", "ListSkillsResponseSchema"]
    - path: "shared/src/skillPolicy.ts"
      provides: "Effective policy resolution for autocomplete and preamble"
      exports: ["isSkillSuggestible", "isEffectivelyAllowed"]
    - path: "cli/src/modules/common/skills.ts"
      provides: "Cursor-aligned nested discovery"
  key_links:
    - from: "cli/src/modules/common/handlers/skills.ts"
      to: "@hapi/protocol/schemas"
      via: "ListSkillsResponse import"
      pattern: "ListSkillsResponse"
    - from: "cli/src/modules/common/skills.ts"
      to: "shared/src/skillPolicy.ts"
      via: "isEffectivelyAllowed for tests only if needed"
      pattern: "SkillSummary"
---

<objective>
Establish shared skill contracts and expand CLI discovery to Cursor-aligned roots before Hub or Web consume them.

Purpose: SKIL-01 and locked D-01–D-05/D-17 require one protocol source of truth and honest inventory from local skill files.
Output: Zod schemas, policy helper module, extended `listSkills`, updated RPC handler types.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-skills-visibility-and-session-policy/02-CONTEXT.md
@.planning/phases/02-skills-visibility-and-session-policy/02-RESEARCH.md
@.planning/phases/02-skills-visibility-and-session-policy/02-PATTERNS.md
@.planning/phases/02-skills-visibility-and-session-policy/02-VALIDATION.md
@shared/src/schemas.ts
@cli/src/modules/common/skills.ts
@cli/src/modules/common/skills.test.ts
@cli/src/modules/common/handlers/skills.ts
</context>

<execution_preconditions>
- Before editing `listSkills`, `extractSkillSummary`, or shared schema exports, run GitNexus `impact` with `repo: "hapi-cursor"`, `target: "listSkills"`, `direction: "upstream"`. Record risk in SUMMARY. Stop on HIGH/CRITICAL.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared SkillSummary and skillPolicy schemas</name>
  <files>shared/src/schemas.ts, shared/src/types.ts, shared/src/schemas.test.ts, shared/src/skillPolicy.ts, shared/src/skillPolicy.test.ts</files>
  <read_first>
    - shared/src/schemas.ts
    - shared/src/types.ts
    - shared/src/schemas.test.ts
    - .planning/phases/02-skills-visibility-and-session-policy/02-CONTEXT.md
    - .planning/phases/02-skills-visibility-and-session-policy/02-PATTERNS.md
  </read_first>
  <behavior>
    - SkillSummarySchema accepts valid rows with name, source project|user, optional description, optional invocationMode auto|manual, valid true, optional pathHint.
    - SkillSummarySchema accepts invalid rows with valid false and invalidReason string; name still required (min 1).
    - SkillPolicyStateSchema accepts only inherited, enabled, disabled.
    - MetadataSchema includes optional skillPolicy as z.record of SkillPolicyState; strict object — no passthrough on SessionPatchSchema.
    - isSkillSuggestible(name, policy) returns false only when state is disabled; missing key is inherited (include).
    - isEffectivelyAllowed matches suggestible for valid skills; enabled includes even when invocationMode is manual.
  </behavior>
  <action>
    Per D-03, D-06, D-07, D-17: add SkillPolicyStateSchema, SkillPolicyMapSchema, SkillSummarySchema (strict), ListSkillsResponseSchema with success, optional skills array, optional error. Extend MetadataSchema with skillPolicy optional field. Export types through shared/src/types.ts. Create shared/src/skillPolicy.ts with pure helpers isSkillSuggestible and isEffectivelyAllowed (document that Cursor may still auto-invoke — D-10). Add schema tests for valid/invalid SkillSummary, skillPolicy on metadata parse, and helper edge cases. Do not add skillPolicy to SessionPatchSchema.
  </action>
  <verify>
    <automated>cd shared && bun test schemas.test.ts skillPolicy.test.ts</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: MetadataSchema includes skillPolicy optional record.
    - Source assertion: SessionPatchSchema unchanged strictness — unknown patch fields still rejected in existing tests.
    - Test assertion: invalid SkillSummary with valid false parses; disabled policy helper returns false for suggestible.
  </acceptance_criteria>
  <done>Protocol layer exposes skill inventory and policy types with tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand CLI listSkills discovery</name>
  <files>cli/src/modules/common/skills.ts, cli/src/modules/common/skills.test.ts</files>
  <read_first>
    - cli/src/modules/common/skills.ts
    - cli/src/modules/common/skills.test.ts
    - .planning/phases/02-skills-visibility-and-session-policy/02-RESEARCH.md
    - .planning/phases/02-skills-visibility-and-session-policy/02-PATTERNS.md
  </read_first>
  <behavior>
    - Project roots include .agents/skills and .cursor/skills under each directory walked to git root (D-01).
    - User roots include ~/.agents/skills and ~/.cursor/skills (D-01).
    - Nested SKILL.md anywhere under a skills root yields one skill; name from frontmatter name or parent directory basename (D-02).
    - disable-model-invocation: true maps to invocationMode manual; absent maps to optional auto or omit per frontmatter (D-04 / research A2).
    - Invalid YAML frontmatter still emits a row with valid false, invalidReason, fallback name from directory (D-04).
    - pathHint: repo-relative for project skills; user skills use ~/.cursor/skills/... or ~/.agents/skills/... never full /home/... (D-03).
    - Dedupe Map: project skills processed before user; same name keeps project copy (D-02).
  </behavior>
  <action>
    Per D-01, D-02, D-04: replace listTopLevelSkillDirs-only walk with recursive collectSkillMdFiles per root; refactor extractSkillSummary to return @hapi/protocol SkillSummary shape including source, valid, invalidReason, pathHint, invocationMode. Remove local SkillSummary interface; import types from protocol. Extend skills.test.ts with fixtures for .cursor/skills, nested SKILL.md, invalid frontmatter visible in output, pathHint redaction, and cross-root dedupe. Keep listSkills(workingDirectory?) signature for RPC.
  </action>
  <verify>
    <automated>cd cli && bun run test -- skills.test</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: nested .cursor/skills/SKILL.md discovered.
    - Test assertion: invalid skill appears with valid false and invalidReason.
    - Test assertion: project skill wins over user skill with same name.
    - Source assertion: getUserSkillsRoots and getProjectSkillsRoots include .cursor/skills paths.
  </acceptance_criteria>
  <done>CLI discovery matches Cursor-documented layout and SKIL-01 inventory fields.</done>
</task>

<task type="auto">
  <name>Task 3: Wire listSkills RPC to shared response shape</name>
  <files>cli/src/modules/common/handlers/skills.ts, cli/src/modules/common/skills.ts</files>
  <read_first>
    - cli/src/modules/common/handlers/skills.ts
    - shared/src/schemas.ts
    - hub/src/web/routes/sessions/read.ts
  </read_first>
  <action>
    Per D-05 and D-19: update handlers/skills.ts to use ListSkillsResponse and SkillSummary from @hapi/protocol (remove duplicate request/response interfaces from skills.ts or re-export protocol types only). Ensure listSkills return value satisfies ListSkillsResponseSchema shape on success and rpcError on failure. Run GitNexus impact on registerHandler listSkills before edit if handler signature changes.
  </action>
  <verify>
    <automated>cd cli && bun run test -- skills</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: handlers/skills.ts imports ListSkillsResponse from protocol, not local duplicate.
    - Typecheck passes across cli and shared.
  </acceptance_criteria>
  <done>RPC listSkills returns shared-shaped inventory for Hub GET /sessions/:id/skills (D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem → CLI | Untrusted SKILL.md content parsed in listSkills (T-02-01) |
| CLI → Hub/Web | Skill inventory JSON over authenticated session API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | pathHint in SkillSummary | mitigate | Repo-relative or ~/.cursor/... only; never emit full home path (D-03) |
| T-02-02 | Tampering | SKILL.md frontmatter | accept | Read-only discovery; no file writes in this plan |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |

</threat_model>

<verification>
- `cd cli && bun run test -- skills.test`
- `cd shared && bun test schemas.test.ts skillPolicy.test.ts`
- `bun run typecheck`
</verification>

<success_criteria>
- Shared SkillSummary and skillPolicy types exist and are strict.
- CLI discovers nested skills from .cursor and .agents roots with invalid rows visible.
- listSkills RPC types align with @hapi/protocol for downstream Hub/Web plans.
</success_criteria>

<output>
Create `.planning/phases/02-skills-visibility-and-session-policy/02-01-SUMMARY.md` when done
</output>
