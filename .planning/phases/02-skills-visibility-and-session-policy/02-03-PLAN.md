---
phase: 02-skills-visibility-and-session-policy
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - web/src/types/api.ts
  - web/src/api/client.ts
  - web/src/hooks/mutations/useSessionActions.ts
  - web/src/hooks/queries/useSkills.ts
  - web/src/hooks/queries/useSkills.test.ts
autonomous: true
requirements:
  - SKIL-02
  - SKIL-03
must_haves:
  truths:
    - "D-05: Web uses @hapi/protocol SkillSummary; duplicate removed from web/src/types/api.ts."
    - "D-12: $skill autocomplete omits disabled skills; enabled and inherited include per effective policy."
    - "D-18: recent-skills localStorage affects ordering only, never policy."
    - "D-08: Policy mutation reaches Hub and session cache updates via mutation + SSE metadata branch."
  artifacts:
    - path: "web/src/api/client.ts"
      provides: "setSkillPolicy, applySkillPolicy, resetSkillPolicy"
    - path: "web/src/hooks/queries/useSkills.ts"
      provides: "Policy-aware getSuggestions"
    - path: "web/src/hooks/queries/useSkills.test.ts"
      provides: "Disabled filter regression tests"
  key_links:
    - from: "web/src/hooks/queries/useSkills.ts"
      to: "session.metadata.skillPolicy"
      via: "isSkillSuggestible from @hapi/protocol"
      pattern: "skillPolicy"
    - from: "web/src/hooks/useSSE.ts"
      to: "queryKeys.session"
      via: "metadata in session-updated"
      pattern: "'metadata' in event.data"
---

<objective>
Wire Web discovery and policy mutations to shared contracts and filter composer skill suggestions by session policy.

Purpose: Connect SKIL-02 persistence to Web cache and implement SKIL-03 autocomplete honesty (D-05, D-08, D-12, D-18).
Output: ApiClient policy methods, useSessionActions mutation, policy-filtered useSkills, Vitest coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-skills-visibility-and-session-policy/02-CONTEXT.md
@.planning/phases/02-skills-visibility-and-session-policy/02-UI-SPEC.md
@web/src/hooks/queries/useSkills.ts
@web/src/api/client.ts
@web/src/hooks/mutations/useSessionActions.ts
@web/src/hooks/useSSE.ts
@web/src/lib/recent-skills.ts
@shared/src/skillPolicy.ts
</context>

<execution_preconditions>
- Plans 02-01 and 02-02 complete (protocol types + Hub route).
- GitNexus impact on useSkills and ApiClient.getSkills before edits.
</execution_preconditions>

<tasks>

<task type="auto">
  <name>Task 1: ApiClient and remove duplicate SkillSummary</name>
  <files>web/src/types/api.ts, web/src/api/client.ts, web/src/hooks/mutations/useSessionActions.ts</files>
  <read_first>
    - web/src/types/api.ts
    - web/src/api/client.ts
    - web/src/hooks/mutations/useSessionActions.ts
    - hub/src/web/routes/sessions/skillPolicy.ts
  </read_first>
  <action>
    Per D-05, D-08: delete SkillSummary and SkillsResponse duplicates from web/src/types/api.ts (lines ~149–158); import SkillSummary, ListSkillsResponse, SkillPolicyState from @hapi/protocol/types. Update getSkills return type to ListSkillsResponse. Add setSkillPolicy(sessionId, { name, state }), setSkillPolicyMap(sessionId, skillPolicy), and resetSkillPolicy(sessionId) matching Hub route shape from 02-02. Add skillPolicyMutation and resetSkillPolicyMutation on useSessionActions mirroring permissionMutation — onSuccess invalidateSession query. Do not store policy in localStorage (D-18).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: web/src/types/api.ts has no local SkillSummary type.
    - Source assertion: client.ts exports skill policy POST methods.
    - Typecheck passes for web package.
  </acceptance_criteria>
  <done>Web HTTP layer uses shared skill types and can mutate policy (D-05, D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Policy-aware useSkills suggestions</name>
  <files>web/src/hooks/queries/useSkills.ts, web/src/hooks/queries/useSkills.test.ts</files>
  <read_first>
    - web/src/hooks/queries/useSkills.ts
    - shared/src/skillPolicy.ts
    - web/src/lib/recent-skills.ts
    - .planning/phases/02-skills-visibility-and-session-policy/02-UI-SPEC.md
  </read_first>
  <behavior>
    - getSuggestions(query, skillPolicy) excludes disabled skills via isSkillSuggestible.
    - enabled includes skill even when valid false excluded from suggestions (only valid skills in list).
    - inherited includes discovered valid skills.
    - Recent skills reorder only among allowed skills; disabled recent names never appear (D-12, D-18).
  </behavior>
  <action>
    Per D-12: extend useSkills to accept optional skillPolicy from session metadata (caller passes session.metadata?.skillPolicy). Import isSkillSuggestible from shared skillPolicy module. Filter skills array before mapping to suggestions in both empty-query and fuzzy branches. Add useSkills.test.ts with table cases: disabled omitted, enabled present, inherited default, invalid skill never suggested. Export helper getEffectiveSkillSuggestions(skills, policy, query) if needed for tests.
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/hooks/queries/useSkills.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: disabled skill name not in suggestions.
    - Test assertion: enabled skill appears when discovery includes it.
    - Source assertion: recent-skills.ts unchanged except imports if any.
  </acceptance_criteria>
  <done>Composer $ autocomplete respects session policy (SKIL-03 partial, D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub API → Web TanStack cache | Session metadata including skillPolicy |
| localStorage recent-skills | UX only; must not override policy (T-02-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Client-side filter only | accept | Document HAPI-only enforcement; Hub persists truth; CLI preamble in 02-04 |
| T-02-07 | Spoofing | Policy mutation | mitigate | ApiClient uses authenticated request helper only |
| T-02-SC | Tampering | npm installs | accept | No new packages |

</threat_model>

<verification>
- `cd web && bunx vitest run src/hooks/queries/useSkills.test.ts -x`
- `bun run typecheck`
</verification>

<success_criteria>
- No duplicate SkillSummary in web types.
- useSkills filters disabled skills from $ autocomplete.
- Policy mutations wired through useSessionActions.
</success_criteria>

<output>
Create `.planning/phases/02-skills-visibility-and-session-policy/02-03-SUMMARY.md` when done
</output>
