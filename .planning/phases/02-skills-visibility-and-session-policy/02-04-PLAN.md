---
phase: 02-skills-visibility-and-session-policy
plan: 04
type: execute
wave: 3
depends_on:
  - "02-01"
files_modified:
  - cli/src/cursor/runCursor.ts
  - cli/src/cursor/runCursor.test.ts
  - cli/src/cursor/skillPolicyPreamble.ts
  - cli/src/cursor/skillPolicyPreamble.test.ts
autonomous: true
requirements:
  - SKIL-03
must_haves:
  truths:
    - "D-10: Turn construction applies HAPI session policy only; no skill file or global Cursor config mutation."
    - "D-13: Deterministic preamble from discovery list + metadata.skillPolicy prepended to user message batches when non-empty allow-list."
    - "D-11: No code path claims Cursor hard enforcement; preamble labeled HAPI session skill policy."
  artifacts:
    - path: "cli/src/cursor/skillPolicyPreamble.ts"
      provides: "buildSkillPolicyPreamble"
    - path: "cli/src/cursor/runCursor.ts"
      provides: "onUserMessage preamble prepend"
  key_links:
    - from: "cli/src/cursor/runCursor.ts"
      to: "metadata.skillPolicy"
      via: "session metadata on spawn/resume"
      pattern: "skillPolicy"
    - from: "skillPolicyPreamble.ts"
      to: "shared/src/skillPolicy.ts"
      via: "isEffectivelyAllowed"
      pattern: "isEffectivelyAllowed"
---

<objective>
Apply soft skill policy enforcement at CLI turn construction via deterministic message preamble.

Purpose: SKIL-03 requires honest HAPI-only enforcement when Cursor CLI has no per-session suppress API (D-10, D-11, D-13; research confirmed).
Output: preamble builder, runCursor integration, Vitest coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-skills-visibility-and-session-policy/02-CONTEXT.md
@.planning/phases/02-skills-visibility-and-session-policy/02-RESEARCH.md
@cli/src/cursor/runCursor.ts
@cli/src/cursor/runCursor.test.ts
@shared/src/skillPolicy.ts
</context>

<execution_preconditions>
- Plan 02-01 complete (SkillSummary + isEffectivelyAllowed).
- GitNexus impact on runCursor and onUserMessage handler before edit.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildSkillPolicyPreamble module</name>
  <files>cli/src/cursor/skillPolicyPreamble.ts, cli/src/cursor/skillPolicyPreamble.test.ts</files>
  <read_first>
    - .planning/phases/02-skills-visibility-and-session-policy/02-RESEARCH.md
    - shared/src/skillPolicy.ts
    - cli/src/modules/common/skills.ts
  </read_first>
  <behavior>
    - Returns null when no valid allowed skills after policy filter.
    - Returns single line prefix: [HAPI session skill policy] Allowed skills: a, b, c. sorted by name.
    - Disabled skills excluded; enabled includes; inherited includes valid discovered skills.
    - Invalid skills (valid false) never listed.
  </behavior>
  <action>
    Per D-13: create skillPolicyPreamble.ts importing SkillSummary and isEffectivelyAllowed from protocol/shared. Export buildSkillPolicyPreamble(skills, policy). Add vitest table tests for empty, all disabled, mixed policy. Wording must not say Cursor enforced (D-11).
  </action>
  <verify>
    <automated>cd cli && bunx vitest run src/cursor/skillPolicyPreamble.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: disabled-only map returns null preamble.
    - Test assertion: output contains HAPI session skill policy marker string.
    - Source assertion: no file writes to skill directories.
  </acceptance_criteria>
  <done>Preamble builder is tested and deterministic (D-13).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire preamble into runCursor onUserMessage</name>
  <files>cli/src/cursor/runCursor.ts, cli/src/cursor/runCursor.test.ts</files>
  <read_first>
    - cli/src/cursor/runCursor.ts
    - cli/src/cursor/runCursor.test.ts
    - cli/src/api/api.ts
  </read_first>
  <action>
    Per D-10: in runCursor session.onUserMessage path, read metadata.skillPolicy from session context (same metadata object used for path/machine). Before forwarding user text, call listSkills(workingDirectory) once per session start or cache skills list on session object to avoid per-keystroke disk walk — planner discretion: cache at runCursor start after cwd known. Prepend buildSkillPolicyPreamble result to formatted user message when non-null (single prepend per message batch, not per attachment). Extend runCursor.test.ts with mock listSkills + policy asserting formatted text includes preamble. Do not mutate SKILL.md or Cursor global config.
  </action>
  <verify>
    <automated>cd cli && bunx vitest run src/cursor/runCursor.test.ts src/cursor/skillPolicyPreamble.test.ts -x</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: user message with disabled policy still sends but preamble excludes disabled skill name.
    - Source assertion: no agent CLI skill deny flags introduced.
  </acceptance_criteria>
  <done>CLI turn construction reflects session policy overlay (D-10, SKIL-03 enforcement layer).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User message → Cursor agent | Preamble injected by HAPI CLI (T-02-08) |
| metadata.skillPolicy → CLI | Session metadata from Hub handoff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | Preamble injection | mitigate | Fixed prefix format; only allowed skill names from local discovery; no user-controlled preamble text |
| T-02-09 | Information disclosure | Preamble lists skill names | accept | Names already visible in UI; no paths in preamble |
| T-02-SC | Tampering | npm installs | accept | No new packages |

</threat_model>

<verification>
- `cd cli && bunx vitest run src/cursor/skillPolicyPreamble.test.ts src/cursor/runCursor.test.ts -x`
- `bun run typecheck`
</verification>

<success_criteria>
- User messages optionally include HAPI skill policy preamble derived from session metadata.
- No Cursor-enforced claims in CLI code.
- Tests cover preamble presence and absence.
</success_criteria>

<output>
Create `.planning/phases/02-skills-visibility-and-session-policy/02-04-SUMMARY.md` when done
</output>
