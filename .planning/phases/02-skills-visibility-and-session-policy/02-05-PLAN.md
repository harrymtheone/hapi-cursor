---
phase: 02-skills-visibility-and-session-policy
plan: 05
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - web/src/routes/settings/_sections/SkillsSection.tsx
  - web/src/routes/settings/skills.tsx
  - web/src/routes/settings/index.tsx
  - web/src/router.tsx
  - web/src/hooks/useAppGoBack.ts
  - web/src/lib/locales/en.ts
  - web/src/lib/locales/zh-CN.ts
autonomous: true
requirements:
  - SKIL-01
must_haves:
  truths:
    - "D-15: /settings/skills is read-only catalog — source, invocation mode, validity, pathHint; no policy toggles."
    - "User can browse skill inventory without an active session policy sheet (uses session-scoped discovery with documented session id source)."
    - "Settings index row links to skills catalog with count or error subtitle."
  artifacts:
    - path: "web/src/routes/settings/skills.tsx"
      provides: "SkillsCatalogPage read-only"
    - path: "web/src/routes/settings/_sections/SkillsSection.tsx"
      provides: "Settings index drill-down row"
  key_links:
    - from: "web/src/routes/settings/skills.tsx"
      to: "GET /sessions/:id/skills"
      via: "useSkills query"
      pattern: "getSkills"
    - from: "web/src/router.tsx"
      to: "/settings/skills"
      via: "settingsSkillsRoute"
      pattern: "/settings/skills"
---

<objective>
Ship read-only skills catalog settings drill-down mirroring model settings chrome without policy controls.

Purpose: SKIL-01 browse surface and D-15 separation from session policy toggles.
Output: SkillsSection, SkillsCatalogPage, route registration, i18n settings.skills.* keys.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-skills-visibility-and-session-policy/02-UI-SPEC.md
@.planning/phases/02-skills-visibility-and-session-policy/02-PATTERNS.md
@web/src/routes/settings/models.tsx
@web/src/routes/settings/_sections/ModelsSection.tsx
@web/src/hooks/queries/useSkills.ts
@web/src/router.tsx
@web/src/hooks/useAppGoBack.ts
</context>

<execution_preconditions>
- Plan 02-03 complete (useSkills + protocol types).
</execution_preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Settings navigation and i18n shell</name>
  <files>web/src/routes/settings/_sections/SkillsSection.tsx, web/src/routes/settings/index.tsx, web/src/router.tsx, web/src/hooks/useAppGoBack.ts, web/src/lib/locales/en.ts, web/src/lib/locales/zh-CN.ts</files>
  <read_first>
    - web/src/routes/settings/_sections/ModelsSection.tsx
    - web/src/routes/settings/models.tsx
    - web/src/router.tsx
    - web/src/hooks/useAppGoBack.ts
    - .planning/phases/02-skills-visibility-and-session-policy/02-UI-SPEC.md
  </read_first>
  <action>
    Per D-15: add SkillsSection button navigating to /settings/skills (mirror ModelsSection). Register settingsSkillsRoute in router.tsx parented like settingsModelsRoute. Add useAppGoBack branch for /settings/skills → /settings. Add settings.skills.* i18n keys in en.ts and zh-CN.ts per UI-SPEC copywriting table (title, rowTitle, catalog labels, footer note that policy is per-session). Wire SkillsSection into settings index below models section.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: router defines path /settings/skills.
    - Source assertion: settings index imports and renders SkillsSection.
    - i18n keys exist in both en and zh-CN for settings.skills.title.
  </acceptance_criteria>
  <done>Settings entry and route exist for skills catalog (D-15 shell).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Read-only SkillsCatalogPage</name>
  <files>web/src/routes/settings/skills.tsx, web/src/routes/settings/skills.test.tsx</files>
  <read_first>
    - web/src/routes/settings/models.tsx
    - web/src/hooks/queries/useSkills.ts
    - .planning/phases/02-skills-visibility-and-session-policy/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Page shows loading, error+retry, empty, and list states.
    - Rows show name, description clamp, source pill, invocationMode when set, invalidReason when valid false, pathHint muted.
    - No tri-state controls; no PATCH calls (test spies api.setSkillPolicy not called).
    - Discovery uses active session id from router/session context — show inline note if session-scoped per UI-SPEC.
  </behavior>
  <action>
    Per D-15 and SKIL-01: implement SkillsCatalogPage with header/back matching models.tsx without Save button. Use useSkills(api, sessionId) where sessionId is most recent active session from existing session list hook or route search param — document choice in component comment. Sort project before user then name. Add skills.test.tsx asserting read-only (no policy mutation mock calls). Top callout: enforcement labels apply in active session policy (SKIL-03 honesty, default HAPI explanation).
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/routes/settings/skills.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: render catalog does not call setSkillPolicy.
    - Test assertion: invalid skill row shows invalidReason text.
    - Manual-check: footer note states policy is per-session only.
  </acceptance_criteria>
  <done>User can browse discovered skills from settings without editing files (SKIL-01, D-15).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings page → Hub GET skills | Read-only inventory; session id scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Information disclosure | pathHint display | mitigate | Render pathHint from protocol only; never concatenate raw cwd |
| T-02-11 | Spoofing | Wrong session skills list | accept | Session-scoped API by design; note in UI when not active session |
| T-02-SC | Tampering | npm installs | accept | No new packages |

</threat_model>

<verification>
- `cd web && bunx vitest run src/routes/settings/skills.test.tsx -x`
- `bun run typecheck`
</verification>

<success_criteria>
- /settings/skills reachable from settings index.
- Catalog is read-only with full SKIL-01 metadata fields.
- No policy toggles on settings page (D-15).
</success_criteria>

<output>
Create `.planning/phases/02-skills-visibility-and-session-policy/02-05-SUMMARY.md` when done
</output>
