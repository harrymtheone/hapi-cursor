---
phase: 02-skills-visibility-and-session-policy
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - hub/src/sync/sessionConfigService.ts
  - hub/src/sync/sessionConfigService.test.ts
  - hub/src/sync/syncEngine.ts
  - hub/src/sync/syncEngineSession.ts
  - hub/src/web/routes/sessions/skillPolicy.ts
  - hub/src/web/routes/sessions/index.ts
  - hub/src/web/routes/sessions/__tests__/skillPolicy.test.ts
  - hub/src/sync/sessionMergeService.ts
  - hub/src/sync/sessionMergeService.test.ts
  - hub/src/web/routes/sessions/__tests__/read.test.ts
autonomous: true
requirements:
  - SKIL-02
must_haves:
  truths:
    - "D-06: skillPolicy lives on session metadata JSON in SQLite, not machine metadata or web localStorage."
    - "D-08: Authenticated Hub route updates policy and emits full session with metadata via refreshSession."
    - "D-09: skillPolicy survives session resume merge round-trip."
    - "D-07: Clearing explicit policy entries returns implicit inherited for all skills."
  artifacts:
    - path: "hub/src/web/routes/sessions/skillPolicy.ts"
      provides: "POST /sessions/:id/skill-policy"
    - path: "hub/src/sync/sessionConfigService.ts"
      provides: "updateSessionSkillPolicy and resetSessionSkillPolicy"
  key_links:
    - from: "hub/src/web/routes/sessions/skillPolicy.ts"
      to: "sessionConfigService.updateSessionMetadata"
      via: "versioned metadata write"
      pattern: "updateSessionMetadata"
    - from: "sessionConfigService"
      to: "sessionRepository.refreshSession"
      via: "full Session SSE"
      pattern: "refreshSession"
---

<objective>
Persist and mutate per-session skill policy through Hub with versioned metadata writes and resume-safe merge.

Purpose: SKIL-02 requires Hub-backed tri-state policy without CLI file mutation (D-06–D-09, D-19).
Output: skill-policy route, SessionConfigService methods, merge helper extension, bun:test coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-skills-visibility-and-session-policy/02-CONTEXT.md
@.planning/phases/02-skills-visibility-and-session-policy/02-RESEARCH.md
@.planning/phases/02-skills-visibility-and-session-policy/02-PATTERNS.md
@hub/src/sync/sessionConfigService.ts
@hub/src/web/routes/sessions/config.ts
@hub/src/sync/sessionMergeService.ts
@hub/src/web/routes/sessions/read.ts
@shared/src/schemas.ts
</context>

<execution_preconditions>
- Run GitNexus impact on `renameSession`, `applySessionConfig`, and `refreshSession` before editing sessionConfigService (repo: hapi-cursor, direction upstream).
- Plan 02-01 must be merged: MetadataSchema.skillPolicy and SkillPolicyStateSchema exist.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionConfigService skill policy writes</name>
  <files>hub/src/sync/sessionConfigService.ts, hub/src/sync/sessionConfigService.test.ts, hub/src/sync/syncEngine.ts, hub/src/sync/syncEngineSession.ts</files>
  <read_first>
    - hub/src/sync/sessionConfigService.ts
    - hub/src/sync/sessionConfigService.test.ts
    - hub/src/web/routes/sessions/config.ts
    - .planning/phases/02-skills-visibility-and-session-policy/02-PATTERNS.md
  </read_first>
  <behavior>
    - applySkillPolicy(sessionId, { name, state }) merges one key into metadata.skillPolicy.
    - applySkillPolicyBatch(sessionId, skillPolicy map) replaces entire map when body contains full policy object.
    - resetSessionSkillPolicy(sessionId) removes skillPolicy key from metadata (all implicit inherited per D-07, D-16).
    - version-mismatch throws concurrent modification error (mirror renameSession).
    - Successful write calls refreshSession so SSE includes metadata on session-updated.
  </behavior>
  <action>
    Per D-06, D-08, D-17, D-19: add methods on SessionConfigService mirroring renameSession metadata path — do not use applySessionConfig for skillPolicy. Expose via SyncEngine facade (syncEngine.ts / syncEngineSession.ts). Add sessionConfigService.test.ts cases asserting SQLite metadata contains skillPolicy after update and cache refresh. Zod-validate incoming SkillPolicyState and cap map size (e.g. max 200 keys, name max 128 chars) per RESEARCH security table T-02-02.
  </action>
  <verify>
    <automated>cd hub && bun test sessionConfigService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: applySkillPolicy persists { name: state } under metadata.skillPolicy.
    - Test assertion: reset clears skillPolicy; subsequent read has undefined map.
    - Source assertion: applySessionConfig unchanged for model/permissionMode only.
  </acceptance_criteria>
  <done>Hub service layer can versioned-write skillPolicy and refresh session (D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST skill-policy route and auth wiring</name>
  <files>hub/src/web/routes/sessions/skillPolicy.ts, hub/src/web/routes/sessions/index.ts, hub/src/web/routes/sessions/__tests__/skillPolicy.test.ts</files>
  <read_first>
    - hub/src/web/routes/sessions/config.ts
    - hub/src/web/routes/sessions/read.ts
    - hub/src/web/routes/sessions/__tests__/read.test.ts
    - shared/src/schemas.ts
  </read_first>
  <behavior>
    - POST /sessions/:id/skill-policy accepts either { name, state } or { skillPolicy: Record } or { reset: true } — pick one documented shape in route file and test both single and batch if supporting both.
    - withEngine, withSession, parseJsonBody with strict Zod; 409 on version mismatch via ApiRouteError.
    - GET /sessions/:id/skills unchanged path; extend read.test.ts fixture skills to protocol SkillSummary[] not string[].
  </behavior>
  <action>
    Per D-08: create skillPolicy.ts route module; register in sessions index. Body schema: SkillPolicyState for single toggle; optional SkillPolicyMapSchema for batch; dedicated reset action for bulk inherited (D-16). Return { ok: true }. Add skillPolicy.test.ts with mock engine asserting applySkillPolicy called and 409 on conflict. Fix read.test.ts listSkills mock to return protocol-shaped skills per D-05.
  </action>
  <verify>
    <automated>cd hub && bun test skillPolicy.test.ts read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: POST skill-policy returns 200 and triggers engine apply.
    - Test assertion: oversized skillPolicy map rejected with 400.
    - Test assertion: GET skills test uses object skills not string array.
  </acceptance_criteria>
  <done>Authenticated session API exposes skill policy mutations (SKIL-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resume merge preserves skillPolicy</name>
  <files>hub/src/sync/sessionMergeService.ts, hub/src/sync/sessionMergeService.test.ts</files>
  <read_first>
    - hub/src/sync/sessionMergeService.ts
    - hub/src/sync/sessionMergeService.test.ts
    - .planning/research/PITFALLS.md
    - .planning/phases/02-skills-visibility-and-session-policy/02-RESEARCH.md
  </read_first>
  <behavior>
    - mergeSessionMetadata deep-merges skillPolicy: keys from incoming session win on conflict; keys only on stored session preserved if absent in incoming.
    - Resume round-trip test: set policy on session A, resume handoff, metadata.skillPolicy still contains toggles (D-09).
  </behavior>
  <action>
    Per D-09 and RESEARCH open question 3: extend mergeSessionMetadata (or equivalent) to union skillPolicy records. Add sessionMergeService.test.ts resume scenario with skillPolicy on both sides. Document that Phase 5 INTG-01 will E2E reload; this plan delivers hub unit evidence required by PITFALLS table.
  </action>
  <verify>
    <automated>cd hub && bun test sessionMergeService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: merged metadata contains union of skillPolicy keys with incoming precedence.
    - Test assertion: resume fixture retains disabled skill entry after merge.
  </acceptance_criteria>
  <done>Policy survives hub resume merge path (D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web → Hub POST skill-policy | Authenticated client mutates session metadata (T-02-03) |
| Hub → SQLite | Versioned metadata JSON persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | POST /skill-policy | mitigate | withSession + auth middleware; Zod body; metadata version check |
| T-02-04 | Denial of service | skillPolicy map size | mitigate | Max keys and name length on write schema |
| T-02-05 | Elevation | Cross-session policy write | mitigate | withSession binds :id to authorized session only |
| T-02-SC | Tampering | npm installs | accept | No new packages |

</threat_model>

<verification>
- `cd hub && bun test skillPolicy.test.ts sessionConfigService.test.ts sessionMergeService.test.ts`
- `bun run typecheck`
</verification>

<success_criteria>
- Hub persists skillPolicy on session metadata with versioned updates and SSE refresh.
- Resume merge retains policy keys.
- GET skills route compatible with shared SkillSummary shape.
</success_criteria>

<output>
Create `.planning/phases/02-skills-visibility-and-session-policy/02-02-SUMMARY.md` when done
</output>
