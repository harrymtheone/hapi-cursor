---
phase: 02-skills-visibility-and-session-policy
plan: 06
type: execute
wave: 5
depends_on:
  - "02-03"
  - "02-05"
files_modified:
  - web/src/components/AssistantChat/SkillsPolicySheet.tsx
  - web/src/components/AssistantChat/SkillPolicyRow.tsx
  - web/src/components/AssistantChat/SkillTriStateControl.tsx
  - web/src/components/AssistantChat/EnforcementBadge.tsx
  - web/src/components/AssistantChat/useHappyComposerState.ts
  - web/src/components/AssistantChat/HappyComposer.tsx
  - web/src/components/AssistantChat/SkillsPolicySheet.test.tsx
  - web/src/lib/locales/en.ts
  - web/src/lib/locales/zh-CN.ts
autonomous: true
requirements:
  - SKIL-02
  - SKIL-03
must_haves:
  truths:
    - "D-14: Active session composer exposes skills policy sheet from composer/status area."
    - "D-16: Tri-state inherited/enabled/disabled per skill plus bulk reset all to inherited."
    - "D-11: Every row shows HAPI session policy badge; Cursor enforced never shown in v1.1."
    - "D-04: Invalid skills listed with policy control disabled and fix-metadata hint."
    - "User sets session skill policy without editing SKILL.md or global Cursor config."
  artifacts:
    - path: "web/src/components/AssistantChat/SkillsPolicySheet.tsx"
      provides: "Session policy bottom sheet UI"
    - path: "web/src/components/AssistantChat/EnforcementBadge.tsx"
      provides: "HAPI session policy pill"
  key_links:
    - from: "SkillsPolicySheet"
      to: "useSessionActions skillPolicyMutation"
      via: "optimistic PATCH on tri-state change"
      pattern: "setSkillPolicy"
    - from: "HappyComposer"
      to: "useSkills filtered suggestions"
      via: "session.metadata.skillPolicy"
      pattern: "skillPolicy"
---

<objective>
Deliver primary session skills policy sheet with honest enforcement labeling and composer integration.

Purpose: SKIL-02 session toggles and SKIL-03 enforcement UX (D-11, D-14, D-16) on mobile PWA.
Output: SkillsPolicySheet, tri-state controls, composer affordance, session.skills.* i18n, component tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-skills-visibility-and-session-policy/02-UI-SPEC.md
@.planning/phases/02-skills-visibility-and-session-policy/02-PATTERNS.md
@web/src/components/AssistantChat/HappyComposerOverlays.tsx
@web/src/components/AssistantChat/useHappyComposerState.ts
@web/src/hooks/mutations/useSessionActions.ts
@web/src/hooks/queries/useSkills.ts
</context>

<execution_preconditions>
- Plans 02-03 and 02-05 complete (mutations + settings link target).
- GitNexus impact on useHappyComposerState before edit.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Policy sheet components and enforcement badge</name>
  <files>web/src/components/AssistantChat/SkillsPolicySheet.tsx, web/src/components/AssistantChat/SkillPolicyRow.tsx, web/src/components/AssistantChat/SkillTriStateControl.tsx, web/src/components/AssistantChat/EnforcementBadge.tsx, web/src/components/AssistantChat/SkillsPolicySheet.test.tsx</files>
  <read_first>
    - .planning/phases/02-skills-visibility-and-session-policy/02-UI-SPEC.md
    - web/src/components/AssistantChat/HappyComposerOverlays.tsx
    - web/src/components/ui/dialog.tsx
  </read_first>
  <behavior>
    - Radix Dialog bottom sheet layout per UI-SPEC (max-h 85vh, safe area).
    - SkillPolicyRow: name, description, source, invocationMode, EnforcementBadge default HAPI session policy only (D-11).
    - SkillTriStateControl: radiogroup with inherited/enabled/disabled; disabled when skill.valid false.
    - Footer: link to /settings/skills; Reset all to inherited calls resetSkillPolicy.
    - Search input when skill count > 8.
  </behavior>
  <action>
    Per D-11, D-14, D-16, D-04: implement components per 02-UI-SPEC sections A and B. EnforcementBadge renders only HAPI session policy copy in v1.1 — do not render Cursor enforced unless backend adds verified enforcement field (research: none). Wire tri-state onChange to skillPolicyMutation with optimistic UI and row pending opacity. Invalid rows: aria-disabled policy control + session.skills.invalidPolicyHint i18n. Add SkillsPolicySheet.test.tsx: badge text, invalid row disabled control, reset calls reset mutation. Sort list project before user then name.
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/components/AssistantChat/SkillsPolicySheet.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: EnforcementBadge text matches HAPI session policy i18n key.
    - Test assertion: invalid skill tri-state is disabled.
    - Test assertion: reset footer triggers resetSkillPolicy mutation mock.
    - Source assertion: no Cursor enforced string in component unless gated by future enforcement prop.
  </acceptance_criteria>
  <done>Session sheet implements tri-state policy UX with honest badges (SKIL-02, SKIL-03).</done>
</task>

<task type="auto">
  <name>Task 2: Composer affordance and wire policy to autocomplete</name>
  <files>web/src/components/AssistantChat/useHappyComposerState.ts, web/src/components/AssistantChat/HappyComposer.tsx, web/src/lib/locales/en.ts, web/src/lib/locales/zh-CN.ts</files>
  <read_first>
    - web/src/components/AssistantChat/useHappyComposerState.ts
    - web/src/components/AssistantChat/HappyComposer.tsx
    - web/src/hooks/queries/useSkills.ts
    - .planning/phases/02-skills-visibility-and-session-policy/02-UI-SPEC.md
  </read_first>
  <action>
    Per D-14: add skillsSheetOpen boolean + toggle to useHappyComposerState (separate from settingsOverlay model|permission per UI-SPEC). Add composer toolbar icon button min 44px touch target with aria-label Session skills policy; optional accent dot when any skill has non-inherited policy in session.metadata.skillPolicy. Render SkillsPolicySheet when open. Pass session.metadata?.skillPolicy into useSkills getSuggestions at composer autocomplete wiring site. Add session.skills.* i18n keys (sheet title, tri-state labels, inherited helper, enforcement, reset). Disable sheet controls when controlsDisabled matches model picker gate. Do not add skills tri-state to HappyComposerOverlays floating overlay (UI-SPEC: sheet only).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
    <automated>cd web && bunx vitest run src/hooks/queries/useSkills.test.ts src/components/AssistantChat/SkillsPolicySheet.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: HappyComposer opens SkillsPolicySheet from toolbar control.
    - Source assertion: autocomplete path passes skillPolicy from session metadata.
    - i18n session.skills keys present in en and zh-CN.
  </acceptance_criteria>
  <done>User sets session skill policy from active chat without file edits (SKIL-02, D-14).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session sheet → Hub POST | Policy mutations for session id |
| UI copy → User | Must not overclaim Cursor enforcement (T-02-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Spoofing | False Cursor enforced badge | mitigate | Hardcode HAPI-only badge until verified CLI hook exists (D-11) |
| T-02-13 | Tampering | Optimistic UI desync | mitigate | invalidateSession on mutation success; rely on SSE metadata full replace |
| T-02-SC | Tampering | npm installs | accept | No new packages |

</threat_model>

<verification>
- `cd web && bunx vitest run src/components/AssistantChat/SkillsPolicySheet.test.tsx src/hooks/queries/useSkills.test.ts -x`
- `bun run typecheck`
- Manual: open session sheet on PWA, toggle disabled, confirm $ omits skill (02-VALIDATION manual row)
</verification>

<success_criteria>
- Session skills sheet reachable from composer with tri-state and bulk reset.
- Enforcement labeling honest (HAPI session policy only).
- Composer autocomplete uses persisted policy from session metadata.
</success_criteria>

<output>
Create `.planning/phases/02-skills-visibility-and-session-policy/02-06-SUMMARY.md` when done
</output>
