---
phase: 01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi
plan: 03
type: execute
wave: 3
depends_on:
  - 01.1-01
  - 01.1-02
files_modified:
  - web/src/components/AssistantChat/ModelPickerOverlay.tsx
  - web/src/components/AssistantChat/ModelPickerOverlay.test.tsx
  - web/src/components/AssistantChat/HappyComposerOverlays.tsx
  - web/src/components/AssistantChat/HappyComposer.tsx
  - web/src/components/AssistantChat/HappyComposer.test.tsx
  - web/src/components/AssistantChat/useHappyComposerState.ts
  - web/src/components/AssistantChat/useHappyComposerState.test.ts
  - web/src/components/SessionChat.tsx
  - web/src/components/SessionChat.test.tsx
  - web/src/components/AssistantChat/StatusBar.tsx
  - web/src/components/AssistantChat/StatusBar.test.tsx
  - web/src/router.tsx
  - web/src/lib/locales/en.ts
  - web/src/lib/locales/zh-CN.ts
  - .planning/phases/01-cursor-runtime-config-contract/01-UAT.md
autonomous: true
requirements:
  - CURS-05
must_haves:
  truths:
    - "Composer picker shows Auto + visible families; Edit → Options composes raw id (D-08, D-09, D-10, D-14)."
    - "Footer links to /settings/models (D-11)."
    - "StatusBar shows family · option summary, not raw id (D-12)."
    - "Only discovered variant ids reach setModel (D-10, T-01.1-03)."
    - "Phase 1 UAT items 2–3 reflect Auto-only launch and in-session picker (D-19, D-20)."
  artifacts:
    - path: "web/src/components/AssistantChat/ModelPickerOverlay.tsx"
      provides: "Two-panel Cursor-style model picker"
    - path: "web/src/components/AssistantChat/HappyComposerOverlays.tsx"
      provides: "Delegates model section to ModelPickerOverlay"
    - path: "web/src/components/AssistantChat/StatusBar.tsx"
      provides: "Family summary label with switch gating preserved"
    - path: ".planning/phases/01-cursor-runtime-config-contract/01-UAT.md"
      provides: "Updated tests 2–3 wording"
  key_links:
    - from: "web/src/components/AssistantChat/ModelPickerOverlay.tsx"
      to: "SessionChat.setModel"
      via: "onModelChange(composedId | null)"
      pattern: "onModelChange"
    - from: "web/src/router.tsx"
      to: "web/src/components/SessionChat.tsx"
      via: "modelFamilies prop"
      pattern: "modelFamilies"
    - from: "web/src/components/SessionChat.tsx"
      to: "web/src/components/AssistantChat/HappyComposer.tsx"
      via: "modelFamilies forwarded"
      pattern: "modelFamilies"
    - from: "web/src/components/AssistantChat/useHappyComposerState.ts"
      to: "ModelPickerOverlay"
      via: "canOpenModelSelector gate (runtimeModelSwitchSupported && isModelSwitchIdle)"
      pattern: "canOpenModelSelector"
    - from: "web/src/components/AssistantChat/StatusBar.tsx"
      to: "web/src/lib/cursorModelFamilies.ts"
      via: "formatFamilySummary(decomposeModelId)"
      pattern: "formatFamilySummary"
---

<objective>
Replace the flat composer model list with a Cursor Desktop-style family picker and update status copy, tests, and UAT.

Purpose: Completes CURS-05 in-session UX (D-08–D-12, D-15 composer, D-16, D-19, D-20) on top of Phase 1 `setModel` plumbing.
Output: `ModelPickerOverlay`, router prop changes, StatusBar family labels, UAT doc updates.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-01-SUMMARY.md
@.planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-02-SUMMARY.md
@.planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-CONTEXT.md
@.planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-RESEARCH.md
@.planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-PATTERNS.md
@.planning/sketches/model-picker-ux/variant-a-drill-down.html
@web/src/components/AssistantChat/HappyComposerOverlays.tsx
@web/src/components/AssistantChat/HappyComposer.tsx
@web/src/components/AssistantChat/HappyComposer.test.tsx
@web/src/components/AssistantChat/StatusBar.tsx
@web/src/components/AssistantChat/StatusBar.test.tsx
@web/src/components/AssistantChat/useHappyComposerHandlers.ts
@web/src/components/SessionChat.tsx
@web/src/router.tsx
@web/src/hooks/useVisibleModelFamilies.ts
@web/src/lib/cursorModelFamilies.ts
@.planning/phases/01-cursor-runtime-config-contract/01-UAT.md
</context>

<execution_preconditions>
- Run GitNexus `impact` upstream on `HappyComposerOverlays`, `StatusBar`, `SessionPage` in `router.tsx`, and `handleModelChange` before edits. Stop on HIGH/CRITICAL.
- Plans 01.1-01 and 01.1-02 complete.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ModelPickerOverlay and SessionPage wiring</name>
  <files>web/src/components/AssistantChat/ModelPickerOverlay.tsx, web/src/components/AssistantChat/ModelPickerOverlay.test.tsx, web/src/components/AssistantChat/HappyComposerOverlays.tsx, web/src/components/AssistantChat/HappyComposer.tsx, web/src/components/AssistantChat/HappyComposer.test.tsx, web/src/components/AssistantChat/useHappyComposerState.ts, web/src/components/AssistantChat/useHappyComposerState.test.ts, web/src/components/SessionChat.tsx, web/src/components/SessionChat.test.tsx, web/src/router.tsx, web/src/lib/locales/en.ts, web/src/lib/locales/zh-CN.ts</files>
  <read_first>
    - web/src/components/AssistantChat/HappyComposerOverlays.tsx
    - web/src/components/AssistantChat/HappyComposer.tsx
    - web/src/components/AssistantChat/HappyComposer.test.tsx
    - web/src/components/AssistantChat/useHappyComposerState.ts
    - web/src/components/AssistantChat/useHappyComposerState.test.ts
    - web/src/components/AssistantChat/useHappyComposerHandlers.ts
    - web/src/components/SessionChat.tsx
    - web/src/components/SessionChat.test.tsx
    - web/src/router.tsx
    - web/src/lib/cursorModelFamilies.ts
    - web/src/hooks/useVisibleModelFamilies.ts
    - web/src/components/ChatInput/FloatingOverlay.tsx
    - .planning/sketches/model-picker-ux/variant-a-drill-down.html
    - .planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-CONTEXT.md
  </read_first>
  <behavior>
    - Primary panel: Auto row + families where `isFamilyVisible(key)` and discovered (D-09, D-15).
    - Auto always shown when model switch is supported, even if zero visible families (D-09).
    - Edit opens Options: Thinking, Fast, Context, Effort — disabled when zero matching variants (D-14).
    - Selecting family without Options uses default compose rule from plan 01; session model decomposes for same-family default (RESEARCH A2 — RESOLVED).
    - `onModelChange(null)` for Auto; otherwise composed raw id only if in `family.variants` (D-10, D-16).
    - Footer navigates to `/settings/models` (D-11).
    - `handleModelChange` pipeline unchanged (close overlay, haptic) — do not bypass SessionChat switch state.
    - `canOpenModelSelector` uses `runtimeModelSwitchSupported && isModelSwitchIdle` (and `onModelChange`, `!controlsDisabled`) — NOT `availableModelOptions.length` or `modelFamilies.length`.
    - Remove flat `runtimeModelOptions` raw-id list from SessionPage (RESEARCH).
  </behavior>
  <action>
    Create `ModelPickerOverlay.tsx` inside `FloatingOverlay` (increase `maxHeight` to ~400 if needed). Props: `families: ModelFamily[]`, `currentModelId: string | null`, `onModelChange: (model: string | null) => void`, `controlsDisabled: boolean`, `visibleFamilies` filter already applied. Primary list: Auto + family rows with Select and Edit actions. Options sub-panel: toggles for thinking/fast/context1m/effort; call `composeVariantId`; disable Apply when null. Footer button `navigate({ to: '/settings/models' })` with `t('composer.modelPicker.manageVisible')`. Replace model block in `HappyComposerOverlays.tsx` with `<ModelPickerOverlay />`; keep permission block unchanged. Replace `availableModelOptions` with `modelFamilies?: ModelFamily[]` on `HappyComposer`, `SessionChat`, and `useHappyComposerState` — thread `modelFamilies` SessionChat → HappyComposer → HappyComposerOverlays → ModelPickerOverlay. In `useHappyComposerState.ts`: remove `hasRuntimeModelOptions` / `(availableModelOptions?.length ?? 0) > 0` from `canOpenModelSelector`; gate with `onModelChange && runtimeModelSwitchSupported && isModelSwitchIdle && !controlsDisabled` only. Update `useHappyComposerState.test.ts`: selector opens when `runtimeModelSwitchSupported` and idle even with `modelFamilies: []` (Auto-only path per D-09); remove tests that require non-empty options length for `canOpenModelSelector`. In `router.tsx` SessionPage: remove `runtimeModelOptions` useMemo that maps every raw id; compute `const families = useMemo(() => groupModelsIntoFamilies(cursorModels.result?.models ?? []), [...])`; filter with `useVisibleModelFamilies().isFamilyVisible`; pass `modelFamilies` to `SessionChat`. Update `SessionChat.test.tsx` to assert `modelFamilies` forwarded (not `availableModelOptions`). Add `composer.modelPicker.*` i18n keys (auto, options labels, manage link, edit, apply). Add `ModelPickerOverlay.test.tsx` and update `HappyComposer.test.tsx`: open model settings, see Auto + family row, open Options, assert `onModelChange` called with fixture raw id, assert Manage link href/route. Never pass `modelReasoningEffort` or effort fields to API (D-16). Implements D-08, D-09, D-10, D-11, D-14, D-15.
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/components/AssistantChat/ModelPickerOverlay.test.tsx src/components/AssistantChat/HappyComposer.test.tsx src/components/AssistantChat/useHappyComposerState.test.ts src/components/SessionChat.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `HappyComposerOverlays` no longer maps flat `modelOptions` radio list for Cursor models.
    - Source assertion: `onModelChange` receives only `null` or ids from `composeVariantId` / Auto.
    - Source assertion: `useHappyComposerState.ts` `canOpenModelSelector` does not reference `availableModelOptions?.length` or `modelFamilies?.length`.
    - Test assertion: `useHappyComposerState.test.ts` — `canOpenModelSelector` true when supported + idle with empty `modelFamilies` (Auto-only).
    - Test assertion: Options panel disables impossible combinations (mock family with single variant).
    - Test assertion: Manage visible models navigates to `/settings/models`.
    - Source assertion: `router.tsx` SessionPage does not build per-raw-id `runtimeModelOptions` array; passes `modelFamilies` to SessionChat.
    - Test assertion: `SessionChat.test.tsx` forwards `modelFamilies` to HappyComposer.
    - i18n assertion: `composer.modelPicker.*` keys in `en.ts` and `zh-CN.ts`.
  </acceptance_criteria>
  <done>In-session model switching uses Cursor Desktop-style family + Options picker submitting one raw id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatusBar family summary, tests, and UAT updates</name>
  <files>web/src/components/AssistantChat/StatusBar.tsx, web/src/components/AssistantChat/StatusBar.test.tsx, web/src/lib/locales/en.ts, web/src/lib/locales/zh-CN.ts, .planning/phases/01-cursor-runtime-config-contract/01-UAT.md</files>
  <read_first>
    - web/src/components/AssistantChat/StatusBar.tsx
    - web/src/components/AssistantChat/StatusBar.test.tsx
    - web/src/lib/cursorModelFamilies.ts
    - web/src/components/SessionChat.tsx
    - .planning/phases/01-cursor-runtime-config-contract/01-UAT.md
    - .planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-CONTEXT.md
    - .planning/notes/model-picker-ux-design.md
  </read_first>
  <behavior>
    - StatusBar shows family · option summary for gated model (D-12).
    - Pending switch still gates label via `previousModel` — no optimistic raw target label (pitfall 5).
    - Auto shows localized unspecified label when model null/empty.
    - UAT test 2: new session without model selector; Auto launch.
    - UAT test 3: explicit model only via in-session picker (D-19, D-20).
  </behavior>
  <action>
    Pass `modelFamilies` (or precomputed decompose helper) into `StatusBar` from `HappyComposer`/`SessionChat`. Replace raw `modelLabel` display: `const gatedModel = ...` (keep existing switch gating); `const summary = gatedModel ? formatFamilySummary(decomposeModelId(gatedModel, families), t) : t('newSession.model.autoUnspecified')`. Do not show raw id in primary label (D-12). Update `StatusBar.test.tsx` for family summary strings with fixture families. Update `.planning/phases/01-cursor-runtime-config-contract/01-UAT.md` items 2–3: document Auto-only new session and in-session family picker for explicit model selection; adjust CURS-02 launch wording per D-20 (model selection in-session only). Run full web test slice: `bun run test:web` and `bun run typecheck` as phase gate.
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/components/AssistantChat/StatusBar.test.tsx -x</automated>
    <automated>bun run typecheck</automated>
    <automated>bun run test:web</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `StatusBar.tsx` uses `formatFamilySummary` / `decomposeModelId` for display when families provided.
    - Source assertion: switch-pending gating on `previousModel` unchanged from Phase 1.
    - Test assertion: StatusBar shows family summary not raw fixture id string.
    - Doc assertion: `01-UAT.md` tests 2–3 updated for Auto-only launch and in-session picker.
    - Gate assertion: `bun run typecheck` and `bun run test:web` pass.
  </acceptance_criteria>
  <done>Composer status shows readable family summaries; Phase 1 UAT matches CURS-05 behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Picker UI → setModel RPC | Composed model id sent to Hub (T-01.1-03). |
| Discovery labels → composer DOM | Family names in overlay (T-01.1-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-02 | Spoofing | ModelPickerOverlay labels | mitigate | React text rendering only |
| T-01.1-03 | Tampering | `onModelChange` | mitigate | Call `composeVariantId` only; reject manual id strings not in `family.variants` |
| T-01.1-01 | Tampering | visibility filter | mitigate | Use `useVisibleModelFamilies`; do not read localStorage ad hoc in overlay |
| T-01.1-SC | Tampering | npm installs | accept | No new packages |

</threat_model>

<verification>
Run `cd web && bunx vitest run src/components/AssistantChat/ModelPickerOverlay.test.tsx src/components/AssistantChat/HappyComposer.test.tsx src/components/AssistantChat/StatusBar.test.tsx -x`, then `bun run typecheck` and `bun run test:web`. Update `01.1-VALIDATION.md` task IDs to 01.1-01..03 when executing.
</verification>

<success_criteria>
CURS-05 is fully delivered: visibility filter, Auto-only new sessions, and Desktop-style in-session picker with family summary status and updated UAT.
</success_criteria>

<output>
Create `.planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-03-SUMMARY.md` when done.
</output>
