---
phase: 01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/cursorModelFamilies.ts
  - web/src/lib/cursorModelFamilies.test.ts
  - web/src/hooks/useVisibleModelFamilies.ts
  - web/src/hooks/useVisibleModelFamilies.test.ts
autonomous: true
requirements:
  - CURS-05
must_haves:
  truths:
    - "Discovered Cursor models group into stable families with parseable option flags (D-01, D-13)."
    - "Compose/decompose returns only ids present in discovered variants (D-14, D-10)."
    - "Unset localStorage means all families visible; configured empty means hide all except Auto path (D-03, D-05)."
  artifacts:
    - path: "web/src/lib/cursorModelFamilies.ts"
      provides: "groupModelsIntoFamilies, composeVariantId, decomposeModelId, formatFamilySummary"
      exports: ["groupModelsIntoFamilies", "composeVariantId", "decomposeModelId"]
    - path: "web/src/lib/cursorModelFamilies.test.ts"
      provides: "Fixture coverage for Opus/Codex/Composer/Auto samples"
    - path: "web/src/hooks/useVisibleModelFamilies.ts"
      provides: "localStorage visibility filter API"
    - path: "web/src/hooks/useVisibleModelFamilies.test.ts"
      provides: "Unset vs configured-empty vs partial selection tests"
  key_links:
    - from: "web/src/lib/cursorModelFamilies.ts"
      to: "@hapi/protocol/types"
      via: "CursorModelSummary import"
      pattern: "CursorModelSummary"
    - from: "web/src/hooks/useVisibleModelFamilies.ts"
      to: "localStorage"
      via: "hapi-visible-model-families keys"
      pattern: "hapi-visible-model-families"
---

<objective>
Add pure family parsing/composition and a global visibility preference hook — the data layer for CURS-05.

Purpose: Settings and composer UI need shared, tested logic before any routes or overlays ship (D-01, D-02, D-03, D-05, D-13, D-14, D-17).
Output: `cursorModelFamilies.ts`, `useVisibleModelFamilies.ts`, and co-located Vitest suites.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-CONTEXT.md
@.planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-RESEARCH.md
@.planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-PATTERNS.md
@.planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-VALIDATION.md
@web/src/lib/agentSlashCommands.ts
@web/src/lib/agentSlashCommands.test.ts
@web/src/hooks/useComposerEnterBehavior.ts
@web/src/hooks/useRecentPaths.ts
@cli/src/cursor/modelDiscovery.test.ts
@shared/src/schemas.ts
</context>

<execution_preconditions>
- Before editing any existing symbol, run GitNexus `impact` with `repo: "hapi-cursor"`, `direction: "upstream"`. This plan only adds new files — no impact required unless refactoring shared exports.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Family grouping and variant composition module</name>
  <files>web/src/lib/cursorModelFamilies.ts, web/src/lib/cursorModelFamilies.test.ts</files>
  <read_first>
    - web/src/lib/agentSlashCommands.ts
    - web/src/lib/agentSlashCommands.test.ts
    - cli/src/cursor/modelDiscovery.test.ts
    - shared/src/schemas.ts
    - .planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-CONTEXT.md
    - .planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-RESEARCH.md
    - .planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-PATTERNS.md
  </read_first>
  <behavior>
    - `groupModelsIntoFamilies` excludes `id === 'auto'` from families (D-09).
    - Fixtures from RESEARCH live CLI samples: `composer-2`, `composer-2-fast`, `gpt-5.3-codex-high-fast`, `claude-opus-4-7-thinking-medium-fast`, `claude-4.6-opus-high-thinking`, `claude-4.6-sonnet-medium`.
    - `composeVariantId` returns `null` when no variant matches selection; never invents ids (D-14, T-01.1-03).
    - `decomposeModelId` round-trips composed ids for fixture families.
    - `formatFamilySummary` produces human-readable "Family · option" strings for StatusBar (D-12).
  </behavior>
  <action>
    Create `web/src/lib/cursorModelFamilies.ts` as a pure module (no React). Import `CursorModelSummary` from `@hapi/protocol/types`. Export types `ModelFamily`, `ModelOptionSelection`, and functions: `groupModelsIntoFamilies(models)`, `composeVariantId(family, selection)`, `decomposeModelId(id, families)`, `formatFamilySummary(decomposed, t?)` (accept optional i18n callback for effort labels). Per D-13 and RESEARCH algorithm: derive stable `key` from id prefixes (`claude-opus-4-7`, `claude-4.6-opus`, `gpt-5.3-codex`, `composer-2`); derive `displayName` from label tokens before `1M`/effort words; set per-variant flags for `thinking`, `fast`, `context1m`, `effort` from id/label heuristics. Default family-row selection rule (open question A2): when composing without explicit options, prefer variant with fewest modifiers (no `-thinking`, no `-fast`) then lexicographic id. Add `cursorModelFamilies.test.ts` with table-driven fixtures mirroring `modelDiscovery.test.ts` style — no static protocol catalog (D-17). Do not import `shared/src/models.ts` presets.
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/lib/cursorModelFamilies.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: module exports `groupModelsIntoFamilies`, `composeVariantId`, `decomposeModelId` as named functions.
    - Test assertion: `cursorModelFamilies.test.ts` covers all six RESEARCH fixture ids plus `auto` exclusion from families.
    - Test assertion: `composeVariantId` returns only ids that exist on `family.variants`.
    - Test assertion: impossible option combinations return `null`.
    - Source assertion: no Hub/protocol/schema changes (D-17).
  </acceptance_criteria>
  <done>Family parse/compose/decompose logic is tested and ready for Settings and composer consumers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Visible model families localStorage hook</name>
  <files>web/src/hooks/useVisibleModelFamilies.ts, web/src/hooks/useVisibleModelFamilies.test.ts</files>
  <read_first>
    - web/src/hooks/useComposerEnterBehavior.ts
    - web/src/hooks/useRecentPaths.ts
    - web/src/lib/cursorModelFamilies.ts
    - .planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-CONTEXT.md
    - .planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-RESEARCH.md
  </read_first>
  <behavior>
    - Missing `hapi-visible-model-families` key → `visibleKeys: null` (all families visible, D-03).
    - `hapi-visible-model-families-configured === 'true'` with `[]` → explicit hide-all families (D-05).
    - Malformed JSON → treat as unset (all visible), not throw (T-01.1-01).
    - `storage` event updates state across tabs (mirror `useComposerEnterBehavior`).
    - `isFamilyVisible(key)` returns true for all keys when `visibleKeys === null`.
  </behavior>
  <action>
    Create `useVisibleModelFamilies.ts` per D-02: keys `hapi-visible-model-families` (JSON string[]), optional `hapi-visible-model-families-configured` (`'true'`). Copy `isBrowser`, `safeGetItem`, `safeSetItem`, `safeRemoveItem` from `useComposerEnterBehavior.ts`. Parse JSON with try/catch; validate array elements are strings (Zod or `Array.isArray` + `every(typeof === 'string')`); drop non-strings. Export hook returning `{ visibleKeys, isConfigured, isFamilyVisible, setVisibleFamilies, clearFilter, summaryLabel }` where `summaryLabel(totalFamilies, visibleCount)` returns i18n-ready tokens: all / count / none. Export constants `VISIBLE_FAMILIES_STORAGE_KEY`, `VISIBLE_FAMILIES_CONFIGURED_KEY` for tests. Add `useVisibleModelFamilies.test.ts`: `beforeEach` clears localStorage; tests unset, configured-empty, partial list, malformed JSON fallback, and `StorageEvent` sync. Auto is never stored in visible keys (D-09).
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/hooks/useVisibleModelFamilies.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: storage keys match D-02 and D-05 exactly.
    - Test assertion: unset key yields `visibleKeys === null` and `isFamilyVisible('any') === true`.
    - Test assertion: configured flag + empty array yields `isFamilyVisible('claude-opus-4-7') === false`.
    - Test assertion: invalid JSON does not throw and falls back to all visible.
    - Test assertion: `setVisibleFamilies` writes configured flag when saving any explicit selection.
  </acceptance_criteria>
  <done>Global family visibility preference is persisted safely and distinguishable from default-all-visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → Web hook | Untrusted persisted JSON crosses into React state (T-01.1-01). |
| Discovery summaries → family parser | Model ids/labels from CLI output drive composed switch ids (T-01.1-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Tampering | `useVisibleModelFamilies` | mitigate | try/catch on JSON parse; validate string[]; malformed → unset/all-visible per D-03 |
| T-01.1-03 | Tampering | `composeVariantId` | mitigate | Return only ids from `family.variants`; never concatenate user-controlled suffixes into new ids |
| T-01.1-SC | Tampering | npm installs | accept | No new packages in this plan (RESEARCH Package Legitimacy Audit) |

</threat_model>

<verification>
Run `cd web && bunx vitest run src/lib/cursorModelFamilies.test.ts src/hooks/useVisibleModelFamilies.test.ts -x`, then `bun run typecheck:web`.
</verification>

<success_criteria>
Pure family and visibility modules exist with full unit coverage; downstream plans can import without duplicating parsing or localStorage semantics.
</success_criteria>

<output>
Create `.planning/phases/01.1-model-picker-ux-family-visibility-filter-auto-only-new-sessi/01.1-01-SUMMARY.md` when done.
</output>
