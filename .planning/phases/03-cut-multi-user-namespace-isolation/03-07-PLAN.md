---
phase: 03-cut-multi-user-namespace-isolation
plan: 07
type: execute
wave: 7
depends_on: ["03-06"]
files_modified:
  - scripts/check-no-cut-agents.sh
autonomous: true
requirements: [CUT-09]
requirements_addressed: [CUT-09]
user_setup: []
must_haves:
  truths:
    - "`scripts/check-no-cut-agents.sh` rejects `namespace` and `:ns` in `cli/src`, `hub/src`, `web/src`, and `shared/src` (D-48)."
    - "Explicit source scan `rg -n \"namespace|:ns\" cli/src hub/src web/src shared/src` returns 0 matches (D-48)."
    - "`bun typecheck && bun run test` exits 0 after this final guard commit (D-47)."
  artifacts:
    - path: "scripts/check-no-cut-agents.sh"
      provides: "Phase 03 namespace source guard"
      contains: "namespace|:ns"
  key_links:
    - from: "scripts/check-no-cut-agents.sh"
      to: "cli/src hub/src web/src shared/src"
      via: "forbidden keyword scan"
      pattern: "namespace|:ns"
---

<objective>
Install the final Phase 03 zero-keyword guard and run the complete phase gate.

Purpose: Make CUT-09 regression-proof after auth, routes, sockets, realtime, clients, store, and migration have all been cut in green slices.
Output: Guard update, explicit zero-match source scan, full suite green, and final source audit evidence.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-RESEARCH.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-01-SUMMARY.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-02-SUMMARY.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-03-SUMMARY.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-04-SUMMARY.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-05-SUMMARY.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-06-SUMMARY.md
@AGENTS.md
</context>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 3 | Hub treats every CLI/web connection as one user; namespace removed from auth, sockets, store, and caches | 03-01..03-07 | COVERED | Auth, facades, callsites, realtime, shared/client contracts, physical store, migration, guard. |
| REQ | CUT-09 | Delete namespace multi-user isolation | 03-01..03-07 | COVERED | Every plan lists CUT-09. |
| RESEARCH | Opaque token | `CLI_API_TOKEN` exact whole-token compare and colon-bearing config acceptance | 03-01 | COVERED | D-34 / Pitfall 1. |
| RESEARCH | Persistence | Namespace-free schema/store/cache plus no runtime migration branch | 03-02,03-05,03-06 | COVERED | Facades first, contract cleanup, physical schema deletion, offline migration. |
| RESEARCH | Realtime | Keep SSE session/machine relevance filters while removing tenant filter | 03-03 | COVERED | D-42/D-43 / Pitfall 3; events route and SSEManager cleanup are co-located. |
| RESEARCH | Routes/sockets | Route and Socket.IO handler namespace cleanup | 03-03,03-04 | COVERED | Web routes/realtime in 03-03; SocketData/server/CLI handlers in 03-04. |
| RESEARCH | Guard | Final `namespace|:ns` source guard and CLI/web mirror cleanup | 03-05,03-07 | COVERED | Client mirrors before zero-keyword guard. |
| CONTEXT | D-34 | Opaque CLI token | 03-01 | COVERED | Whole-token parser/config/auth compare. |
| CONTEXT | D-35-D-37 | JWT/WebAppEnv namespace removal; no fallback constant | 03-01,03-03 | COVERED | Opaque token handling starts in 03-01; `{ uid }` WebAppEnv cut is co-located with all Hono route cleanup in 03-03. |
| CONTEXT | D-38-D-41 | Store/schema/users/offline migration namespace removal | 03-02,03-06 | COVERED | Facades, physical deletion, script-only migration. |
| CONTEXT | D-42-D-43 | SyncEvent/SSE namespace removal with relevance filters | 03-03,03-05 | COVERED | EventPublisher/SSE route/manager first, shared schema deletion after callsites. |
| CONTEXT | D-44-D-45 | CLI/terminal socket namespace removal | 03-04 | COVERED | SocketData deletion, server writes, terminal, and CLI handlers are co-located. |
| CONTEXT | D-46-D-48 | Green slices and source zero tolerance | 03-01..03-07 | COVERED | Each plan verifies full suite; final guard enforces zero residue. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>03-07-01: Add namespace zero-keyword source guard</name>
  <files>scripts/check-no-cut-agents.sh</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-46, D-47, D-48)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-RESEARCH.md §"Don't Hand-Roll" Keyword guard and §"Validation Architecture"
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md §"`scripts/check-no-cut-agents.sh`"
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-06-SUMMARY.md
    - scripts/check-no-cut-agents.sh
  </read_first>
  <behavior>
    - Guard scans exactly `cli/src`, `hub/src`, `web/src`, and `shared/src` for `namespace` and `:ns`.
    - Guard has no broad source whitelist for real namespace matches.
    - Guard preserves existing Phase 1/2 forbidden literal checks.
  </behavior>
  <action>Extend `scripts/check-no-cut-agents.sh` with a Phase 03 scan over exactly `cli/src`, `hub/src`, `web/src`, and `shared/src` for `namespace` and `:ns`. Preserve existing Phase 1/2 guard behavior and whitelist style. Do not whitelist real source matches. If the source scan finds residual runtime matches, fix the residual source in the same task only when it is namespace residue from this phase; do not edit docs, planning files, or Phase 12-owned prose.</action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh && ! rg -n 'namespace|:ns' cli/src hub/src web/src shared/src && bun typecheck && bun run test</automated>
  </verify>
  <done>Guard rejects namespace residue in source scope, explicit source scan is clean, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guard script -> test suite | Source keyword policy gates future regressions. |
| Test fixtures -> implementation confidence | Stale fixtures can hide obsolete contract fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Repudiation | `scripts/check-no-cut-agents.sh` | mitigate | Add explicit namespace guard and record zero-match evidence in the summary. |
| T-03-16 | Information Disclosure | stale source/test fixtures | mitigate | Final explicit source scan must return zero matches in source scope. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs are required; stop for package audit if one appears. |
</threat_model>

<verification>
- `bash scripts/check-no-cut-agents.sh` exits 0.
- `rg -n "namespace|:ns" cli/src hub/src web/src shared/src` returns 0 matches.
- `bun typecheck && bun run test` exits 0.
</verification>

<success_criteria>
- D-48 zero-tolerance source guard is implemented.
- CUT-09 success criteria #2 and #5 are fully satisfied.
- Source audit proves GOAL, CUT-09, research findings, and D-34 through D-48 are covered.
- No task modifies 10 or more files.
</success_criteria>

<output>
Create `.planning/phases/03-cut-multi-user-namespace-isolation/03-07-SUMMARY.md` when done. Include final guard output, explicit source scan result, full `bun typecheck` / `bun run test` exit codes, and the source audit updated with actual evidence.
</output>
