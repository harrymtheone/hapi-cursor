---
phase: 03-cut-multi-user-namespace-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hub/src/utils/accessToken.ts
  - hub/src/utils/accessToken.test.ts
  - hub/src/config/cliApiToken.ts
  - hub/src/config/cliApiToken.test.ts
  - hub/src/web/routes/cli.ts
autonomous: true
requirements: [CUT-09]
requirements_addressed: [CUT-09]
user_setup: []
must_haves:
  truths:
    - "CLI_API_TOKEN is one opaque secret; colon-bearing values compare as whole strings (D-34)."
    - "`/api/cli/*` bearer auth compares the whole opaque token without deriving route namespace state (D-34, D-37)."
    - "WebAppEnv/JWT narrowing is deliberately deferred to Plan 03-03, where every Hono `c.get('namespace')` route callsite is migrated in the same green slice (D-36, D-47)."
    - "SocketData deletion is deliberately deferred to Plan 03-04, where `/cli` server writes and CLI socket handlers are migrated in the same green slice (D-44, D-47)."
    - "`bun typecheck && bun run test` exits 0 after this plan commit (D-47)."
  artifacts:
    - path: "hub/src/utils/accessToken.ts"
      provides: "Opaque access-token parser"
      contains: "parseAccessToken"
    - path: "hub/src/config/cliApiToken.ts"
      provides: "Colon-accepting CLI token config validation"
      contains: "CLI_API_TOKEN"
    - path: "hub/src/web/routes/cli.ts"
      provides: "Opaque token validation for CLI bearer routes without setting namespace"
      contains: "parseAccessToken"
  key_links:
    - from: "hub/src/web/routes/cli.ts"
      to: "hub/src/utils/accessToken.ts"
      via: "whole-token comparison"
      pattern: "constantTimeEquals"
---

<objective>
Cut namespace suffix parsing out of CLI token handling without narrowing web or socket identity contracts yet.

Purpose: Start the revision with the smallest safe green slice: token parsing/config and CLI bearer auth. WebAppEnv narrowing moves to Plan 03-03 and SocketData deletion moves to Plan 03-04 so each contract is removed with all consumers per D-47.
Output: Opaque token parsing/config, namespace-free `/api/cli/*` bearer auth, and a green full suite.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-RESEARCH.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>03-01-01: Make access tokens opaque</name>
  <files>hub/src/utils/accessToken.ts, hub/src/utils/accessToken.test.ts, hub/src/config/cliApiToken.ts, hub/src/config/cliApiToken.test.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-34, D-37, D-47)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md sections for `hub/src/utils/accessToken.ts` and `hub/src/config/cliApiToken.ts`
  </read_first>
  <behavior>
    - `parseAccessToken('foo:bar')` returns `'foo:bar'`, not `{ baseToken, namespace }`.
    - Empty or whitespace-only tokens still return `null`.
    - `CLI_API_TOKEN=foo:bar` is accepted as the complete secret and still gets weak-token warning behavior when applicable.
  </behavior>
  <action>Rewrite tests first for opaque token behavior, then change `parseAccessToken()` to return `string | null`. Delete `DEFAULT_NAMESPACE`, `ParsedAccessToken`, `baseToken`, and suffix parsing per D-34/D-37. Remove only the config-layer colon rejection; preserve existing token generation, settings persistence, and weak-token warning behavior.</action>
  <verify>
    <automated>bun test hub/src/utils/accessToken.test.ts hub/src/config/cliApiToken.test.ts && ! rg -n 'DEFAULT_NAMESPACE|ParsedAccessToken|baseToken|lastIndexOf' hub/src/utils hub/src/config && bun typecheck && bun run test</automated>
  </verify>
  <done>Colon-bearing CLI tokens are treated as one opaque secret and no token parser/config namespace symbols remain.</done>
</task>

<task type="auto" tdd="true">
  <name>03-01-02: Compare CLI bearer tokens as opaque secrets</name>
  <files>hub/src/web/routes/cli.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-34, D-37, D-47)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md sections for CLI route
  </read_first>
  <behavior>
    - `/api/cli/*` bearer auth validates the parsed opaque token directly against `configuration.cliApiToken`.
    - `/api/cli/*` does not derive or set namespace route state from token suffixes.
    - `hub/src/web/middleware/auth.ts` still owns WebAppEnv until Plan 03-03 migrates all route `c.get('namespace')` callsites in the same plan.
  </behavior>
  <action>Update CLI bearer route callsites to use the string returned by `parseAccessToken()` directly with `constantTimeEquals`. Remove `parsedToken.baseToken`, `parsedToken.namespace`, and `c.set('namespace')` from this route. Do not narrow `WebAppEnv` or JWT payload here; Plan 03-03 performs that atomic route contract cut with all Hono route/guard callsites per D-36/D-47.</action>
  <verify>
    <automated>! rg -n 'parsedToken\.(namespace|baseToken)|c\.set\('"'"'namespace'"'"'' hub/src/web/routes/cli.ts && bun typecheck && bun run test</automated>
  </verify>
  <done>CLI bearer route compares whole opaque tokens and no longer writes route namespace state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web/CLI clients -> Hub auth | Untrusted opaque tokens and JWTs cross into hub auth and socket middleware. |
| Terminal socket -> CLI session rooms | Browser terminal actions cross into active CLI session routing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | `hub/src/utils/accessToken.ts`, `/api/auth`, `/api/cli/*` | mitigate | Compare the entire opaque token with `constantTimeEquals`; test colon-bearing tokens. |
| T-03-02 | Elevation of Privilege | `hub/src/web/routes/cli.ts` | mitigate | Do not derive authority from token suffixes; compare the full opaque bearer token. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs are required; stop for package audit if one appears. |
</threat_model>

<verification>
- Each task runs its targeted tests and `bun typecheck && bun run test`.
- Focused `rg` gates show no token suffix parsing in files migrated by this plan.
</verification>

<success_criteria>
- D-34 and D-37 are implemented for token parsing/config and CLI bearer auth without default namespace behavior or compatibility parsing.
- D-35/D-36 and D-44/D-45 are intentionally implemented in Plans 03-03 and 03-04 respectively, where their consuming callsites are co-located.
- D-47 is satisfied: this plan commit is green under `bun typecheck && bun run test`.
- No task modifies 10 or more files.
</success_criteria>

<output>
Create `.planning/phases/03-cut-multi-user-namespace-isolation/03-01-SUMMARY.md` when done. Include token/config/CLI bearer behavior changes, targeted test output, focused `rg` counts, and full `bun typecheck` / `bun run test` exit codes.
</output>
