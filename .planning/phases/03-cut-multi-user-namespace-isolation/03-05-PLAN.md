---
phase: 03-cut-multi-user-namespace-isolation
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - shared/src/schemas.ts
  - shared/src/socket.ts
  - hub/src/sync/sessionCache.ts
  - hub/src/sync/machineCache.ts
  - hub/src/sync/syncEngine.ts
  - hub/src/sync/sessionModel.test.ts
  - hub/src/sync/aliveEvents.test.ts
  - cli/src/api/types.ts
  - cli/src/api/api.ts
  - cli/src/api/api.extraHeaders.test.ts
  - cli/src/agent/sessionFactory.test.ts
  - web/src/types/api.ts
  - web/src/hooks/useSSE.ts
autonomous: true
requirements: [CUT-09]
requirements_addressed: [CUT-09]
user_setup: []
must_haves:
  truths:
    - "Shared Session, SyncEvent, and SocketErrorReason contracts contain no namespace fields or namespace-missing reason (D-38, D-42, D-45)."
    - "Hub sync caches publish and cache namespace-free Session/Machine objects while store internals may still contain temporary namespace columns (D-39)."
    - "CLI and web response/event mirrors compile without Session.namespace or token `:ns` assumptions (D-48)."
    - "`bun typecheck && bun run test` exits 0 after this plan commit (D-47)."
  artifacts:
    - path: "shared/src/schemas.ts"
      provides: "Namespace-free Session and SyncEvent schemas"
      contains: "SessionSchema"
    - path: "shared/src/socket.ts"
      provides: "Namespace-free socket error reasons"
      contains: "SocketErrorReason"
    - path: "cli/src/api/types.ts"
      provides: "Namespace-free CLI response schemas"
      contains: "CreateSessionResponseSchema"
    - path: "web/src/types/api.ts"
      provides: "Namespace-free web API mirrors"
      contains: "Session"
  key_links:
    - from: "shared/src/schemas.ts"
      to: "hub/src/sync/sessionCache.ts"
      via: "Session object construction"
      pattern: "Session"
    - from: "shared/src/socket.ts"
      to: "hub/src/socket/handlers/cli/types.ts"
      via: "error reason compatibility"
      pattern: "not-found"
---

<objective>
Delete namespace from shared runtime contracts and client mirrors after callsites are migrated.

Purpose: Make the public DTO/event surface namespace-free as its own green slice, using the owner-only route/socket/realtime work from earlier plans. Store SQL can still carry temporary internal namespace columns until Plan 03-06 deletes the physical schema.
Output: Namespace-free shared schemas, hub cache objects, CLI/web mirrors, tests, and a green full suite.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-RESEARCH.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-03-SUMMARY.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-04-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>03-05-01: Delete namespace from shared contracts</name>
  <files>shared/src/schemas.ts, shared/src/socket.ts, hub/src/sync/sessionCache.ts, hub/src/sync/machineCache.ts, hub/src/sync/syncEngine.ts, hub/src/sync/sessionModel.test.ts, hub/src/sync/aliveEvents.test.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-38, D-39, D-42, D-45, D-47)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md sections for shared schemas/socket and sync caches
  </read_first>
  <behavior>
    - `SessionSchema` has no `namespace` property.
    - `SyncEventSchema` has no namespace base schema or optional event namespace.
    - `SocketErrorReason` cannot be `namespace-missing`.
    - Hub cache/session objects omit namespace even if temporary store rows still include internal namespace columns before Plan 03-06.
  </behavior>
  <action>Remove namespace from shared schemas and socket error types, then update hub sync caches and tests to construct namespace-free Session/Machine/event objects. Keep old store row namespace fields internal until Plan 03-06. Do not introduce compatibility DTO parsing or default namespace values.</action>
  <verify>
    <automated>bun test hub/src/sync/sessionModel.test.ts hub/src/sync/aliveEvents.test.ts && ! rg -n 'namespace|namespace-missing|SessionEventBaseSchema' shared/src hub/src/sync && bun typecheck && bun run test</automated>
  </verify>
  <done>Shared contracts and hub sync DTOs are namespace-free and full verification remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>03-05-02: Update CLI and web mirrors</name>
  <files>cli/src/api/types.ts, cli/src/api/api.ts, cli/src/api/api.extraHeaders.test.ts, cli/src/agent/sessionFactory.test.ts, web/src/types/api.ts, web/src/hooks/useSSE.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-38, D-42, D-48)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md sections for CLI and web mirrors
  </read_first>
  <behavior>
    - CLI response schemas and mappings do not expect or return `Session.namespace`.
    - CLI tests use namespace-free session fixtures.
    - Web API types and SSE hook event handling do not read namespace fields or assume `CLI_API_TOKEN:<namespace>` syntax.
  </behavior>
  <action>Remove namespace fields from CLI Zod mirrors, CLI API mappings, web API type mirrors, and SSE hook event assumptions. Update the listed tests/fixtures to namespace-free sessions. Do not unify all wire contracts into `shared/`; Phase 7 owns that broader refactor.</action>
  <verify>
    <automated>bun test cli/src/api/api.extraHeaders.test.ts cli/src/agent/sessionFactory.test.ts && ! rg -n 'namespace|:ns' cli/src/api cli/src/agent/sessionFactory.test.ts web/src/types/api.ts web/src/hooks/useSSE.ts && bun typecheck && bun run test</automated>
  </verify>
  <done>CLI/web mirrors are namespace-free and the repository remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub -> shared/client DTOs | Runtime state crosses package boundaries as validated schemas and API mirrors. |
| SSE stream -> web cache | Server events update browser query/cache state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | `shared/src/schemas.ts` | mitigate | Use Zod schemas as source of truth and run typecheck/tests after contract deletion. |
| T-03-11 | Information Disclosure | `web/src/hooks/useSSE.ts` | mitigate | Keep relevance-based cache updates while removing only namespace assumptions. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs are required; stop for package audit if one appears. |
</threat_model>

<verification>
- Sync, CLI, and focused source scans exit 0.
- `bun typecheck && bun run test` exits 0 after the shared/client contract commit.
</verification>

<success_criteria>
- D-38, D-42, D-45, and D-48 are implemented for shared DTOs and client mirrors.
- Physical store deletion remains isolated to Plan 03-06.
- No task modifies 10 or more files.
</success_criteria>

<output>
Create `.planning/phases/03-cut-multi-user-namespace-isolation/03-05-SUMMARY.md` when done. Include shared schema/socket changes, CLI/web mirror changes, remaining temporary store internals, targeted tests, focused `rg` counts, and full suite exit codes.
</output>
