---
phase: 03-cut-multi-user-namespace-isolation
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - hub/src/socket/server.ts
  - hub/src/socket/socketTypes.ts
  - hub/src/socket/handlers/terminal.ts
  - hub/src/socket/handlers/terminal.test.ts
  - hub/src/socket/handlers/cli/index.ts
  - hub/src/socket/handlers/cli/types.ts
  - hub/src/socket/handlers/cli/sessionHandlers.ts
  - hub/src/socket/handlers/cli/machineHandlers.ts
  - hub/src/socket/handlers/cli/terminalHandlers.ts
  - hub/src/visibility/visibilityTracker.ts
  - hub/src/push/pushNotificationChannel.ts
  - hub/src/push/pushNotificationChannel.test.ts
  - hub/src/push/pushService.ts
  - hub/src/notifications/notificationHub.test.ts
autonomous: true
requirements: [CUT-09]
requirements_addressed: [CUT-09]
user_setup: []
must_haves:
  truths:
    - "`/cli` Socket.IO auth compares the whole opaque token and writes no socket namespace data (D-44)."
    - "SocketData exposes no namespace field, and CLI socket handlers stop reading `socket.data.namespace` in this same plan (D-44, D-45)."
    - "Terminal JWT/access checks no longer depend on namespace mismatch (D-35, D-45)."
    - "Visibility and push fallback are global single-owner flows with no namespace lookup (D-43)."
    - "`bun typecheck && bun run test` exits 0 after this plan commit (D-47)."
  artifacts:
    - path: "hub/src/socket/server.ts"
      provides: "Namespace-free CLI token auth and terminal JWT validation"
      contains: "jwtPayloadSchema"
    - path: "hub/src/socket/socketTypes.ts"
      provides: "Namespace-free socket data contract"
      contains: "SocketData"
    - path: "hub/src/socket/handlers/cli/index.ts"
      provides: "Owner-only CLI socket access resolvers"
      contains: "resolveSessionAccess"
    - path: "hub/src/visibility/visibilityTracker.ts"
      provides: "Global visible-connection tracking"
      contains: "hasVisibleConnection"
    - path: "hub/src/push/pushService.ts"
      provides: "Owner-only push delivery"
      contains: "send"
  key_links:
    - from: "hub/src/socket/server.ts"
      to: "hub/src/socket/handlers/cli/index.ts"
      via: "SocketData without namespace"
      pattern: "socket.data"
    - from: "hub/src/push/pushNotificationChannel.ts"
      to: "hub/src/visibility/visibilityTracker.ts"
      via: "global visibility fallback"
      pattern: "hasVisibleConnection"
---

<objective>
Remove namespace from Socket.IO auth/handlers plus visibility tracking and push delivery.

Purpose: Cut the socket data contract atomically: `SocketData.namespace` deletion, `/cli` server writes, terminal JWT handling, and every CLI socket handler read must move together to preserve D-47. Visibility/push delivery then collapses to single-owner semantics against the namespace-free SSE surface from Plan 03-03.
Output: Namespace-free Socket.IO server/data/terminal/CLI handlers, namespace-free visibility and push services, tests, and a green full suite.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-RESEARCH.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md
@.planning/phases/03-cut-multi-user-namespace-isolation/03-03-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>03-04-01: Remove socket namespace data and handler reads atomically</name>
  <files>hub/src/socket/server.ts, hub/src/socket/socketTypes.ts, hub/src/socket/handlers/terminal.ts, hub/src/socket/handlers/terminal.test.ts, hub/src/socket/handlers/cli/index.ts, hub/src/socket/handlers/cli/types.ts, hub/src/socket/handlers/cli/sessionHandlers.ts, hub/src/socket/handlers/cli/machineHandlers.ts, hub/src/socket/handlers/cli/terminalHandlers.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-35, D-44, D-45, D-47)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md sections for `hub/src/socket/server.ts`, `hub/src/socket/socketTypes.ts`, terminal handler, and CLI handlers
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-02-SUMMARY.md
  </read_first>
  <behavior>
    - `/cli` Socket.IO auth parses the presented token and compares the complete opaque string to `configuration.cliApiToken`.
    - `/cli` Socket.IO auth does not assign `socket.data.namespace`.
    - `SocketData` has no `namespace?: string` property.
    - CLI handler access resolvers use session/machine id existence only and cannot emit `namespace-missing`.
    - Terminal Socket.IO JWT auth validates `{ uid }` only and terminal open/ready/write paths use session rooms without namespace comparison.
  </behavior>
  <action>Update `hub/src/socket/server.ts` so `/cli` Socket.IO auth uses the string returned by `parseAccessToken()` directly in `constantTimeEquals`, comparing the whole opaque token to `configuration.cliApiToken`, and delete the `socket.data.namespace = ...` write per D-44. Narrow the terminal JWT schema in the same file to `{ uid }` and stop writing namespace for terminal sockets per D-35/D-45. In the same task, update `hub/src/socket/socketTypes.ts` to remove `SocketData.namespace`, update terminal handler access to session active-state and room lookup only, and rewrite the CLI socket handler aggregator/handlers to remove reads from `socket.data.namespace` and call owner-only access/store facades. Preserve Zod payload parsing, room joins, versioned callbacks, terminal request routing, and active-session behavior. This task intentionally has 9 files because splitting SocketData deletion from server writes or CLI handlers breaks typecheck/tests under D-47.</action>
  <acceptance_criteria>
    - `hub/src/socket/server.ts` compares `/cli` auth using `constantTimeEquals(parsedToken, configuration.cliApiToken)` or the local equivalent whole-token expression; it never reads `parsedToken.baseToken` or writes `socket.data.namespace`.
    - `hub/src/socket/socketTypes.ts` no longer declares `namespace?: string` on `SocketData`.
    - `hub/src/socket/handlers/cli/*` has no `socket.data.namespace` or `namespace-missing`.
    - `hub/src/socket/handlers/terminal.ts` has no namespace mismatch or `session.namespace` branch.
  </acceptance_criteria>
  <verify>
    <automated>bun test hub/src/socket/handlers/terminal.test.ts && ! rg -n 'socket\.data\.namespace|namespace\?:|parsedToken\.(baseToken|namespace)|namespace-missing|session\.namespace|access-denied.*namespace|ByNamespace' hub/src/socket/server.ts hub/src/socket/socketTypes.ts hub/src/socket/handlers/terminal.ts hub/src/socket/handlers/terminal.test.ts hub/src/socket/handlers/cli && bun typecheck && bun run test</automated>
  </verify>
  <done>`/cli` socket auth, socket data typing, terminal sockets, and CLI socket handlers no longer depend on namespace, and the full repository remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>03-04-02: Collapse visibility and push delivery to single owner</name>
  <files>hub/src/visibility/visibilityTracker.ts, hub/src/push/pushNotificationChannel.ts, hub/src/push/pushNotificationChannel.test.ts, hub/src/push/pushService.ts, hub/src/notifications/notificationHub.test.ts</files>
  <read_first>
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-CONTEXT.md (D-38, D-43, D-47)
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-PATTERNS.md section for visibility and push services
    - .planning/phases/03-cut-multi-user-namespace-isolation/03-03-SUMMARY.md
  </read_first>
  <behavior>
    - VisibilityTracker tracks whether any connection is visible, not visible connections by namespace.
    - PushNotificationChannel uses global visibility/toast fallback and does not read `session.namespace`.
    - PushService sends to all current subscriptions via the Plan 03-02 namespace-free push store facade and removes expired endpoints directly.
  </behavior>
  <action>Rewrite visibility tracking to global single-owner semantics. Update push notification channel and push service to remove namespace parameters and call owner-only push store methods. Preserve expired-subscription cleanup, payload construction, and toast fallback behavior. Update affected tests to remove namespace fixtures and assert global visibility/push delivery behavior.</action>
  <verify>
    <automated>bun test hub/src/push/pushNotificationChannel.test.ts hub/src/notifications/notificationHub.test.ts && ! rg -n 'namespace|sendToNamespace|ByNamespace|session\.namespace' hub/src/visibility hub/src/push hub/src/notifications && bun typecheck && bun run test</automated>
  </verify>
  <done>Visibility and push delivery no longer depend on namespace and the repository remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Socket.IO clients -> Hub | CLI and browser terminal sockets cross into hub socket middleware and handlers. |
| Push channel -> browser push endpoints | Server notifications cross to subscribed endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Elevation of Privilege | `hub/src/socket/server.ts`, `hub/src/socket/socketTypes.ts`, `hub/src/socket/handlers/cli/*` | mitigate | Compare whole opaque socket auth tokens and remove namespace-bearing socket authority in the same plan as all handler reads. |
| T-03-09 | Denial of Service | `hub/src/push/pushService.ts` | mitigate | Preserve expired endpoint cleanup and bounded per-subscription send behavior. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs are required; stop for package audit if one appears. |
</threat_model>

<verification>
- Socket, push, and notification targeted tests exit 0.
- Focused socket/push `rg` gates show no namespace symbols in migrated files.
- `bun typecheck && bun run test` exits 0 after the plan commit.
</verification>

<success_criteria>
- D-44 and D-45 are implemented for Socket.IO surfaces; D-43 push/visibility behavior remains user-visible and namespace-free.
- No task modifies 10 or more files.
</success_criteria>

<output>
Create `.planning/phases/03-cut-multi-user-namespace-isolation/03-04-SUMMARY.md` when done. Include socket contract changes, push/visibility behavior changes, targeted tests, focused `rg` counts, and full suite exit codes.
</output>
