---
id: 12-01
phase: 12-docs-cleanup-milestone-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/
  - docs/
  - refactor.md
  - package.json
  - bun.lock
  - scripts/check-no-cut-agents.sh
autonomous: true
requirements: [CUT-12]
must_haves:
  truths:
    - "Repo no longer contains the website/ marketing site, docs/ VitePress site, or root refactor.md scratchpad (D-03 删 > 改)."
    - "Root Bun workspaces list contains exactly cli/shared/hub/web — no website or docs entries (D-03)."
    - "bun.lock no longer references website or docs workspace packages and is regenerated cleanly (D-03)."
    - "No README / source / planning doc still links to website/ paths, docs/guide, docs/superpowers, or refactor.md (CUT-12 SC#4 precursor)."
    - "scripts/check-no-cut-agents.sh has a fail-closed Phase-12 guard block that blocks reintroduction of website/, docs/superpowers, docs/guide paths and the refactor.md file (D-11 + D-126 pattern)."
    - "bun typecheck and bun run test stay green after the deletions (slice-independent green cadence, Phase 11 D-189)."
  artifacts:
    - path: "package.json"
      provides: "Root workspaces array without 'website' or 'docs'"
      contains: "\"workspaces\""
    - path: "bun.lock"
      provides: "Lockfile regenerated post-workspace cut"
    - path: "scripts/check-no-cut-agents.sh"
      provides: "Phase-12 guard block (named: 'Phase 12 …') appended to the existing tail"
      contains: "Phase 12"
  key_links:
    - from: "scripts/check-no-cut-agents.sh"
      to: "website/, docs/superpowers, docs/guide, refactor.md"
      via: "ripgrep fail-closed pattern"
      pattern: "(website/|docs/superpowers|docs/guide|refactor\\.md)"
    - from: "bun run test:guard"
      to: "scripts/check-no-cut-agents.sh"
      via: "package.json script invocation"
      pattern: "test:guard.*check-no-cut-agents"
---

<objective>
Delete the website/ marketing tree, docs/ VitePress tree, and root refactor.md scratchpad in one cut, scrub the root workspace + lockfile + inline references that mention them, and extend scripts/check-no-cut-agents.sh with a Phase-12 ripgrep guard block that blocks their reintroduction. Hold bun typecheck + bun run test green at end of plan so 12-02 can start clean.

Purpose: D-03 (delete > scrub) + CUT-12 docs-surface contraction.
Output: Cleaner repo root + extended guard script + green workspace gate.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-docs-cleanup-milestone-verification/12-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/INTEGRATIONS.md
@scripts/check-no-cut-agents.sh
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deletion grep + delete website/, docs/, refactor.md</name>
  <files>website/, docs/, refactor.md</files>
  <read_first>
    - 12-CONTEXT.md §domain + §canonical_refs (D-03 删 > 改 + grep-before-delete protocol)
    - package.json (root workspaces array currently contains "website" and "docs")
    - README.md, AGENTS.md (HEAD or git-show), cli/README.md, hub/README.md, web/README.md — check for surviving docs/ / website/ / refactor.md anchor links before deletion
    - .planning/codebase/INTEGRATIONS.md (cross-check no integration still references the docs site)
  </read_first>
  <action>
    Run `rg -n 'refactor\.md' --hidden -g '!.planning/**' -g '!.git/**'` and `rg -n '(website/|docs/\.vitepress|docs/guide|docs/superpowers)' --hidden -g '!.planning/**' -g '!.git/**' -g '!website/**' -g '!docs/**'` to surface any surviving external references outside the trees being deleted. Capture results to the executor's working notes. If any non-deletable file (README.md, AGENTS.md, CHANGELOG.md, root configs) references these paths, record the line for 12-02 README rewrite (do NOT patch READMEs in this plan — that's 12-02 scope per D-11). After capturing references, delete `website/` directory, `docs/` directory, and `refactor.md` file outright via `rm -rf website docs refactor.md`. Do not preserve any subset (D-03: no content scrub). The .planning/ tree is untouched (whitelisted via the rg --glob).
  </action>
  <verify>
    <automated>test ! -e website && test ! -e docs && test ! -e refactor.md && rg -n 'refactor\.md' --hidden -g '!.planning/**' -g '!.git/**' | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'</automated>
  </verify>
  <done>website/, docs/, refactor.md gone from working tree; pre-delete reference scan captured (any surviving README references are documented for 12-02 to clean during README rewrite).</done>
</task>

<task type="auto">
  <name>Task 2: Scrub root package.json workspaces + regen bun.lock + drop dead scripts</name>
  <files>package.json, bun.lock</files>
  <read_first>
    - package.json (root) — current `workspaces` array contains "website" and "docs"; current `scripts.build:site` references `bun run --cwd website build && bun run --cwd docs docs:build && cp -r docs/.vitepress/dist website/dist/public/docs`
    - bun.lock (root) — search for "website" and "docs" workspace entries that need eviction
    - .planning/codebase/STACK.md (Bun workspace convention)
  </read_first>
  <action>
    In root `package.json`: remove the strings `"website"` and `"docs"` from the `workspaces` array, leaving `["cli", "shared", "hub", "web"]` in that exact order. Delete the entire `scripts.build:site` entry (it composes `bun run --cwd website build && bun run --cwd docs docs:build && cp -r docs/.vitepress/dist website/dist/public/docs` — all three operands are gone after Task 1). Do not touch other scripts. Then run `bun install` at repo root to regenerate `bun.lock` against the trimmed workspace set; commit the resulting bun.lock. Do NOT add `madge:check` script here — that lands in 12-03 per D-10/D-11 slice boundary.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const w=p.workspaces;if(JSON.stringify(w)!==JSON.stringify(['cli','shared','hub','web']))throw new Error('workspaces='+JSON.stringify(w));if(p.scripts['build:site'])throw new Error('build:site still present');console.log('ok')" &amp;&amp; ! grep -E '"(website|docs)"' bun.lock | grep -v '^#'</automated>
  </verify>
  <done>Root workspaces is exactly ["cli","shared","hub","web"]; scripts.build:site removed; bun.lock regenerated and contains no website/docs workspace entries; `bun install --frozen-lockfile` succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Append Phase-12 ripgrep guard block to scripts/check-no-cut-agents.sh + green gate</name>
  <files>scripts/check-no-cut-agents.sh</files>
  <read_first>
    - scripts/check-no-cut-agents.sh (entire file) — study the Phase 9/10/11 guard block format: each block is a numbered sub-check with a clear `echo "❌ Phase N …"` failure message + `echo "✅ Phase N …"` success message, terminated by a phase-level `echo "✅ Phase N guard PASS"`. The Phase 11 block ends with `echo "✅ Phase 11 guard PASS (REFT-01..03)."` followed by `echo "✅ Phase 10 guard PASS."` (legacy tail line).
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/* (D-126 declarations/usages-vs-whitelist pattern reference)
    - 12-CONTEXT.md §decisions D-11 + Claude's Discretion (Phase 12 guard pattern must be complementary, not overlapping with prior phases)
  </read_first>
  <action>
    Append a new Phase-12 guard block to `scripts/check-no-cut-agents.sh` immediately above the trailing legacy `echo "✅ Phase 10 guard PASS."` line (or wherever the script currently terminates after Phase 11 — preserve the existing block ordering). The new block must contain at minimum these fail-closed sub-checks, each implemented with `"$RG_BIN" -n PATTERN --glob '!.planning/**' --glob '!.git/**' --glob '!CHANGELOG.md'` (whitelist from CUT-12 SC#4: planning snapshots + CHANGELOG.md + git history): (#1) zero hits for path literal `website/` anywhere in tracked source; (#2) zero hits for path literals `docs/guide` and `docs/superpowers` (both must be checked — both were live anchors in the deleted VitePress site); (#3) zero hits for the bare filename `refactor.md` (deleted scratchpad must not return); (#4) zero hits for the top-level directory names `website` or `docs` appearing in any tracked `package.json` `workspaces` array (regex: `"workspaces"[\s\S]{0,200}"(website|docs)"`). Each sub-check uses the established pattern: capture rg output to a local variable, if non-empty echo the offenders + an `❌ Phase 12 #N: <human-readable failure>` message + `exit 1`; on success echo `✅ Phase 12 #N: <human-readable success>`. Close the block with `echo "✅ Phase 12 guard PASS (CUT-12 docs surface)."`. Then run the full gate `bash scripts/check-no-cut-agents.sh && bun run typecheck && bun run test` to confirm slice green per D-11 plan-end gate.
  </action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh &amp;&amp; bun run typecheck &amp;&amp; bun run test</automated>
  </verify>
  <done>Guard script exits 0 with Phase 12 block printing its four sub-check ✅ lines + the Phase 12 PASS footer; typecheck + test stay green; reintroducing `website/`, `docs/guide`, `docs/superpowers`, `refactor.md`, or readding website/docs to workspaces would trip the corresponding sub-check (verified mentally against the regex).</done>
</task>

</tasks>

<verification>
- `test ! -e website && test ! -e docs && test ! -e refactor.md` — deletions landed.
- `cat package.json | jq -r '.workspaces | join(",")'` prints `cli,shared,hub,web`.
- `bun install --frozen-lockfile` succeeds on regenerated bun.lock.
- `bash scripts/check-no-cut-agents.sh` exits 0 and prints `✅ Phase 12 guard PASS (CUT-12 docs surface).`
- `bun run typecheck && bun run test` green (slice-independent green per D-189 cadence inherited).
</verification>

<success_criteria>
CUT-12 docs-surface deletion landed atomically with a fail-closed guard preventing regression. No production code touched. Workspace gate is green and 12-02 can begin README rewrites against a clean tree.
</success_criteria>

<output>
Create `.planning/phases/12-docs-cleanup-milestone-verification/12-01-SUMMARY.md` when done — record: files deleted (sizes/LoC if convenient), surviving README references captured for 12-02, Phase-12 guard sub-check IDs + patterns, gate outputs.
</output>
