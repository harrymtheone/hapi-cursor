---
id: 12-03
phase: 12-docs-cleanup-milestone-verification
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - package.json
  - .github/workflows/verify.yml
autonomous: true
requirements: [VRFY-01, VRFY-02, VRFY-03]
must_haves:
  truths:
    - "Root package.json exposes `madge:check` script that runs `madge --circular --extensions ts,tsx cli/src hub/src web/src` (D-10)."
    - "`.github/workflows/verify.yml` exists and runs on push + pull_request, executing in order: actions/checkout@v4 → oven-sh/setup-bun@v1 (bun version pinned to repo's current bun) → bun install --frozen-lockfile → bun typecheck → bun run test → bun run madge:check → bash scripts/check-no-cut-agents.sh (D-07)."
    - "All CI steps are also runnable locally with the same commands (VRFY-01 + VRFY-02 + VRFY-03 are reproducible from a clean clone)."
    - "CI is observed green on a real PR triggered by the commit that adds the workflow (D-11 12-03 'CI green on PR' acceptance — verified by the human triggering or observing the PR run)."
    - "Phase-12 guard from 12-01 stays green; no new forbidden tokens introduced by the workflow yaml or madge addition."
  artifacts:
    - path: "package.json"
      provides: "Root `madge:check` npm script"
      contains: "madge:check"
    - path: ".github/workflows/verify.yml"
      provides: "GitHub Actions workflow named 'verify' running typecheck + test + madge + guard"
      contains: "oven-sh/setup-bun"
  key_links:
    - from: ".github/workflows/verify.yml"
      to: "bun run madge:check"
      via: "workflow step invocation"
      pattern: "bun run madge:check"
    - from: ".github/workflows/verify.yml"
      to: "bash scripts/check-no-cut-agents.sh"
      via: "workflow step invocation"
      pattern: "check-no-cut-agents\\.sh"
---

<objective>
Add a single root-level `madge:check` script and a minimal GitHub Actions workflow that runs the full gate suite (install → typecheck → test → madge → guard) on every push and PR. Pin the bun version to the repo's current bun so CI matches local behaviour. Observe a green run on a real PR before declaring done.

Purpose: VRFY-01 (typecheck + test in CI), VRFY-02 (madge in CI), VRFY-03 (ripgrep guard in CI) all become push-gated rather than human-gated.
Output: One script + one workflow file + one observed green CI run.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-docs-cleanup-milestone-verification/12-CONTEXT.md
@.planning/phases/12-docs-cleanup-milestone-verification/12-01-SUMMARY.md
@.planning/phases/12-docs-cleanup-milestone-verification/12-02-SUMMARY.md
@.planning/codebase/STACK.md
@package.json
@scripts/check-no-cut-agents.sh
@scripts/check-no-circular-hub.sh
@scripts/check-no-circular-web.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add root `madge:check` script + local sanity invocation</name>
  <files>package.json</files>
  <read_first>
    - package.json (root) — current `scripts` block (after 12-01's `build:site` deletion); confirm `madge` already in `devDependencies` (yes, ^8.0.0 per current state)
    - 12-CONTEXT.md §decisions D-10 (script command verbatim: `madge --circular --extensions ts,tsx cli/src hub/src web/src`) + Claude's Discretion note that the per-phase `check-no-circular-hub.sh` and `check-no-circular-web.sh` scripts are preserved (do NOT delete; they are phase guards, complementary not duplicative)
    - scripts/check-no-circular-hub.sh and scripts/check-no-circular-web.sh — confirm they exist and remain untouched
  </read_first>
  <action>
    In root `package.json`, add a new entry to the `scripts` object: `"madge:check": "madge --circular --extensions ts,tsx cli/src hub/src web/src"` (exact string from D-10; do not paraphrase). Place it adjacent to other gate scripts (e.g. between `test` and `clean-session`) for readability. Do NOT delete or modify `scripts/check-no-circular-hub.sh` or `scripts/check-no-circular-web.sh` (per D-10 they coexist as phase-local guards). After saving, run `bun run madge:check` locally; it must report zero circular dependencies across all three scopes (Phase 6/8/9 madge cycles were closed by 06-01 / 08-02 / 09-04 per ROADMAP; expected output is three "0 cycles" lines or equivalent madge confirmation). If any cycle appears, that is a real regression — STOP, capture it, and bubble to the orchestrator before proceeding (do NOT bandage by tweaking the script).
  </action>
  <verify>
    <automated>node -e "const s=require('./package.json').scripts; if(s['madge:check']!=='madge --circular --extensions ts,tsx cli/src hub/src web/src') throw new Error('madge:check missing or wrong: '+s['madge:check']); console.log('ok')" &amp;&amp; bun run madge:check</automated>
  </verify>
  <done>`madge:check` script present with exact D-10 wording; local invocation reports zero cycles across cli/src + hub/src + web/src.</done>
</task>

<task type="auto">
  <name>Task 2: Create .github/workflows/verify.yml</name>
  <files>.github/workflows/verify.yml</files>
  <read_first>
    - 12-CONTEXT.md §specifics (verify.yml skeleton) + §decisions D-07 (CI step order + triggers) + Claude's Discretion note on bun pinning strategy
    - package.json root + cli/package.json + hub/package.json + web/package.json — to determine the bun version currently in use (check for an explicit `engines.bun`, `packageManager`, or `.bun-version` file; if none, derive from `which bun && bun --version` in the executor's local environment — note the version chosen in the SUMMARY)
    - Any existing `.github/workflows/*.yml` files (likely none — Phase 1 removed codex-*.yml workflows; the .github/workflows dir may not exist yet; if so, create it)
    - scripts/check-no-cut-agents.sh (workflow's final step shells out to this — confirm path)
  </read_first>
  <action>
    Determine the bun version to pin. Search the repo for any of: `engines.bun` in any package.json, `packageManager` field, `.bun-version` file, `.tool-versions` bun line, or `.nvmrc`-analog. If found, pin to that. If none found, query `bun --version` in the executor environment and pin to that exact major.minor.patch (record the chosen value + rationale in the SUMMARY). Create `.github/workflows/verify.yml` (mkdir the directory if absent). The file contents: workflow name `verify`; triggers on `push:` (all branches) and `pull_request:` (default); single job `verify` on `ubuntu-latest`; steps in this exact order — (1) `actions/checkout@v4`; (2) `oven-sh/setup-bun@v1` with `bun-version: <pinned>`; (3) `run: bun install --frozen-lockfile`; (4) `run: bun typecheck`; (5) `run: bun run test`; (6) `run: bun run madge:check`; (7) `run: bash scripts/check-no-cut-agents.sh`. No matrix, no caching beyond what setup-bun provides by default, no concurrency group, no manual dispatch (D-07: minimal CI). Validate the yaml parses (e.g. `bun -e "import('yaml').then(y=>y.default.parse(require('fs').readFileSync('.github/workflows/verify.yml','utf8')))"` or any local yaml linter). Commit the file + the package.json change from Task 1 together on a feature branch (or current branch if PR-driven), push, and open / observe a PR. Watch the CI run; if any step fails, fix the underlying issue OR fix the workflow definition (not by skipping the step). Done when one full PR run is green end-to-end.
  </action>
  <verify>
    <automated>test -f .github/workflows/verify.yml &amp;&amp; grep -q "oven-sh/setup-bun" .github/workflows/verify.yml &amp;&amp; grep -q "bun install --frozen-lockfile" .github/workflows/verify.yml &amp;&amp; grep -q "bun typecheck" .github/workflows/verify.yml &amp;&amp; grep -q "bun run test" .github/workflows/verify.yml &amp;&amp; grep -q "bun run madge:check" .github/workflows/verify.yml &amp;&amp; grep -q "bash scripts/check-no-cut-agents.sh" .github/workflows/verify.yml &amp;&amp; bun install --frozen-lockfile &amp;&amp; bun typecheck &amp;&amp; bun run test &amp;&amp; bun run madge:check &amp;&amp; bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <done>verify.yml exists with all 7 steps in correct order, bun version pinned (value + rationale captured in SUMMARY); local full-gate (install/typecheck/test/madge/guard) green; a real PR run on GitHub was observed green by the executor (record run URL or commit SHA + GitHub Actions run id in the SUMMARY — this is the D-11 12-03 "CI green on PR" acceptance gate).</done>
</task>

</tasks>

<verification>
- `bun run madge:check` reports zero cycles across cli/src + hub/src + web/src.
- `.github/workflows/verify.yml` parses as valid yaml and contains the 7 required steps in order.
- Full local gate (install/typecheck/test/madge/guard) green.
- A real GitHub Actions PR run for the commit introducing this workflow is observed green (URL recorded in SUMMARY).
- `bash scripts/check-no-cut-agents.sh` still green (no new forbidden tokens introduced).
</verification>

<success_criteria>
VRFY-01/02/03 graduated from local-only to push-gated. The verify workflow is the single source of CI truth for the rest of the milestone. Sets the stage for 12-04 to lean on automated gates with confidence.
</success_criteria>

<output>
Create `.planning/phases/12-docs-cleanup-milestone-verification/12-03-SUMMARY.md` — record: chosen bun version + how it was determined, exact `madge:check` script string, verify.yml step list, local gate outputs, GitHub PR run URL + run id (the green CI acceptance evidence).
</output>
