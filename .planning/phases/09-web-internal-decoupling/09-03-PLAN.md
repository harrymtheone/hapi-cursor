---
phase: 09-web-internal-decoupling
plan: 3
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - web/src/routes/settings/index.tsx
  - web/src/routes/settings/useSettingsState.ts
  - web/src/routes/settings/_sections/LanguageSection.tsx
  - web/src/routes/settings/_sections/DisplaySection.tsx
  - web/src/routes/settings/_sections/ChatSection.tsx
  - web/src/routes/settings/_sections/AboutSection.tsx
  - web/src/routes/settings/_sections/LanguageSection.test.tsx
  - web/src/routes/settings/_sections/DisplaySection.test.tsx
  - web/src/routes/settings/_sections/ChatSection.test.tsx
  - web/src/routes/settings/_sections/AboutSection.test.tsx
  - web/src/components/AssistantChat/HappyComposer.tsx
  - web/src/components/AssistantChat/HappyComposerOverlays.tsx
  - web/src/components/AssistantChat/useHappyComposerState.ts
  - web/src/components/AssistantChat/useHappyComposerHandlers.ts
  - web/src/components/AssistantChat/useHappyComposerState.test.ts
  - web/src/components/AssistantChat/useHappyComposerHandlers.test.ts
  - web/src/components/ToolCard/views/_results.tsx
  - web/src/components/ToolCard/views/_results.test.tsx
  - web/src/components/ToolCard/views/results/_resultHelpers.ts
  - web/src/components/ToolCard/views/results/BashResult.tsx
  - web/src/components/ToolCard/views/results/LineListResult.tsx
  - web/src/components/ToolCard/views/results/ReadResult.tsx
  - web/src/components/ToolCard/views/results/BashResult.test.tsx
  - web/src/components/ToolCard/views/results/LineListResult.test.tsx
  - web/src/components/ToolCard/views/results/ReadResult.test.tsx
autonomous: true
requirements:
  - REFW-02
must_haves:
  truths:
    - "routes/settings/index.tsx is an orchestrator < 300 lines; route entry signature unchanged (still default-export'd at the same path)."
    - "All 4 settings sections are extracted into _sections/{Language,Display,Chat,About}Section.tsx each < 300 lines, with colocated RTL tests."
    - "HappyComposer.tsx is an orchestrator < 300 lines; lifted state + handlers + overlays live in 3 sibling files."
    - "_results.tsx is a dispatcher < 250 lines; per-tool views ≥ 50 lines (Bash, LineList, Read) extracted to results/{X}Result.tsx; shared helpers extracted to _resultHelpers.ts."
    - "Re-exports preserved on _results.tsx (extractTextFromResult, getMutationResultRenderMode, getToolResultViewComponent) so external callers do not move."
    - "Slice 1 ToolCard.integration.test.tsx still PASSes (post-decomposition every knownTool still resolves to a renderer)."
    - "Slice 1 + Slice 2 tests remain green."
  artifacts:
    - path: "web/src/routes/settings/index.tsx"
      provides: "Route orchestrator (default export at same path)"
      contains: "export default"
    - path: "web/src/routes/settings/_sections/LanguageSection.tsx"
      provides: "Language settings section"
    - path: "web/src/routes/settings/_sections/DisplaySection.tsx"
      provides: "Display settings section"
    - path: "web/src/routes/settings/_sections/ChatSection.tsx"
      provides: "Chat settings section"
    - path: "web/src/routes/settings/_sections/AboutSection.tsx"
      provides: "About settings section"
    - path: "web/src/components/AssistantChat/HappyComposer.tsx"
      provides: "Composer orchestrator (default/named export unchanged)"
    - path: "web/src/components/AssistantChat/HappyComposerOverlays.tsx"
      provides: "Floating overlays (permission picker + model picker + autocomplete)"
    - path: "web/src/components/AssistantChat/useHappyComposerState.ts"
      provides: "Lifted state hook"
    - path: "web/src/components/AssistantChat/useHappyComposerHandlers.ts"
      provides: "Lifted handlers hook"
    - path: "web/src/components/ToolCard/views/_results.tsx"
      provides: "Result dispatcher + small inline views + registry exports"
    - path: "web/src/components/ToolCard/views/results/_resultHelpers.ts"
      provides: "Shared parse/render helpers"
    - path: "web/src/components/ToolCard/views/results/BashResult.tsx"
      provides: "Bash result view"
    - path: "web/src/components/ToolCard/views/results/LineListResult.tsx"
      provides: "LineList result view"
    - path: "web/src/components/ToolCard/views/results/ReadResult.tsx"
      provides: "Read result view"
  key_links:
    - from: "web/src/routes/settings/index.tsx"
      to: "web/src/routes/settings/_sections/{Language,Display,Chat,About}Section.tsx"
      via: "relative import + composition"
      pattern: "from './_sections/"
    - from: "web/src/components/AssistantChat/HappyComposer.tsx"
      to: "useHappyComposerState + useHappyComposerHandlers + HappyComposerOverlays"
      via: "relative import"
      pattern: "from './use\\|from './HappyComposerOverlays'"
    - from: "web/src/components/ToolCard/views/_results.tsx"
      to: "web/src/components/ToolCard/views/results/{Bash,LineList,Read}Result.tsx + _resultHelpers.ts"
      via: "relative import"
      pattern: "from './results/"
---

<objective>
Slice 3 — Decompose `routes/settings/index.tsx` (758), `AssistantChat/HappyComposer.tsx` (669), and `ToolCard/views/_results.tsx` (687) into orchestrators + hooks + sub-components. This closes REFW-02. Honors RESEARCH corrections: settings has flat **sections** (not tabs) → `_sections/`; HappyComposer needs only 3 new files (state hook + handlers hook + overlays component) because visual sections (StatusBar / ComposerButtons / AttachmentItem) are already extracted; `_results.tsx` extracts only the 3 views ≥ 40-50 lines per Claude's Discretion + RESEARCH Q7 (Bash 48, LineList 52, Read 42).

Purpose: Bring the last 3 oversized files under the 500-line budget while keeping route entries / component default exports stable and preserving `_results.tsx` re-exports (`extractTextFromResult`, `getMutationResultRenderMode`).

Output: 1 orchestrator rewrite + 1 state hook + 4 section sub-components + 4 colocated tests for settings; 1 orchestrator rewrite + 2 hooks + 1 overlays component + 2 hook tests for HappyComposer; 1 dispatcher rewrite + 1 helpers module + 3 per-tool result components + 3 colocated tests + redistributed cases for `_results.test.tsx`. Gate every target file < 500 lines, new sub-pieces < 300 lines, dispatcher < 250 lines; `bun run test:web` green; Slice 1 integration test still green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-web-internal-decoupling/09-CONTEXT.md
@.planning/phases/09-web-internal-decoupling/09-RESEARCH.md
@.planning/phases/09-web-internal-decoupling/09-PATTERNS.md
@.planning/phases/09-web-internal-decoupling/09-VALIDATION.md
@.planning/phases/09-web-internal-decoupling/09-01-SUMMARY.md
@.planning/phases/09-web-internal-decoupling/09-02-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Decompose routes/settings/index.tsx into orchestrator + 4 _sections + useSettingsState</name>
  <files>web/src/routes/settings/index.tsx, web/src/routes/settings/useSettingsState.ts, web/src/routes/settings/_sections/LanguageSection.tsx, web/src/routes/settings/_sections/DisplaySection.tsx, web/src/routes/settings/_sections/ChatSection.tsx, web/src/routes/settings/_sections/AboutSection.tsx, web/src/routes/settings/_sections/LanguageSection.test.tsx, web/src/routes/settings/_sections/DisplaySection.test.tsx, web/src/routes/settings/_sections/ChatSection.test.tsx, web/src/routes/settings/_sections/AboutSection.test.tsx</files>
  <read_first>
    - `web/src/routes/settings/index.tsx` in full (758 lines) — focus on the 4 section line ranges per RESEARCH Q5: Language (~400-452), Display (~454-610), Chat (~612-727), About (~729-753); header / back nav at the top; 8 `useState`/`useRef` dropdown-open flags + outside-click `useEffect` to lift.
    - RESEARCH.md Q5 (lines 549-577) — enumerated sections, lifted state hook plan, naming correction `_tabs/` → `_sections/`.
    - RESEARCH.md §Pitfall 4 — settings has NO tab UI; use "section" vocabulary.
    - PATTERNS.md §"web/src/routes/settings/index.tsx (orchestrator refactor)" lines 253-260.
    - `web/src/components/NewSession/index.tsx` lines 1-30 — orchestrator + sub-component layout reference.
    - `web/src/lib/use-translation.ts` — `useTranslation` hook used inside sections.
  </read_first>
  <action>
    Step 1 — Create `useSettingsState.ts` consolidating the 8 `useState`/`useRef` dropdown-open flags (`isOpen`, `isAppearanceOpen`, `isFontOpen`, `isTerminalFontOpen`, `isChatOpen`, `isTerminalToolDisplayOpen`, etc.) + the outside-click `useEffect` global listener. Export `useSettingsState(): { /* flags + setters + refs */ }`. Step 2 — Create 4 section sub-components under `web/src/routes/settings/_sections/`: `LanguageSection.tsx` (locale dropdown), `DisplaySection.tsx` (appearance + font sizes + session preview limit + chat surface colors), `ChatSection.tsx` (composer enter behavior + terminal tool display + tool background + user message background), `AboutSection.tsx` (website link + app version + protocol version). Move the inline SVG icons (`BackIcon`, `CheckIcon`, `ChevronDownIcon`, `MinusIcon`) and the `SessionPreviewLimitControl` / `ChatSurfaceColorControl` helpers either to `_sections/_icons.tsx` and inline beside their consumer respectively. Step 3 — Rewrite `index.tsx` as an orchestrator: header + back nav + vertically stack `<LanguageSection />`, `<DisplaySection />`, `<ChatSection />`, `<AboutSection />` inside the existing scroll container. Preserve the default export and route signature (TanStack Router still resolves `/settings` here). Step 4 — Create RTL colocated tests for each section using `vi.mock` (PATTERNS Shared Pattern F) — minimum 2 cases per section covering render + one interaction. Per D-151 + RESEARCH naming correction, do NOT use `_tabs/` directory name; use `_sections/`.
  </action>
  <verify>
    <automated>bun typecheck &amp;&amp; bun run test:web -- settings</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l web/src/routes/settings/index.tsx` reports < 300.
    - All 4 section files exist: `test -f web/src/routes/settings/_sections/LanguageSection.tsx` (and Display / Chat / About).
    - Each section file < 300 lines: `wc -l web/src/routes/settings/_sections/*Section.tsx` all show < 300.
    - `test -f web/src/routes/settings/useSettingsState.ts` succeeds.
    - 4 colocated test files exist: `web/src/routes/settings/_sections/{Language,Display,Chat,About}Section.test.tsx`.
    - Each test file has ≥ 2 `it(` blocks.
    - `grep -c "^export default" web/src/routes/settings/index.tsx` equals 1.
    - `test ! -d web/src/routes/settings/_tabs` (correct naming).
    - `bun typecheck` exits 0; `bun run test:web -- settings` exits 0.
  </acceptance_criteria>
  <done>Settings route orchestrator < 300 lines; 4 sections extracted with focused tests; route entry preserved.</done>
</task>

<task type="auto">
  <name>Task 3.2: Decompose HappyComposer.tsx into orchestrator + 2 hooks + 1 overlays component (+ hook tests)</name>
  <files>web/src/components/AssistantChat/HappyComposer.tsx, web/src/components/AssistantChat/HappyComposerOverlays.tsx, web/src/components/AssistantChat/useHappyComposerState.ts, web/src/components/AssistantChat/useHappyComposerHandlers.ts, web/src/components/AssistantChat/useHappyComposerState.test.ts, web/src/components/AssistantChat/useHappyComposerHandlers.test.ts</files>
  <read_first>
    - `web/src/components/AssistantChat/HappyComposer.tsx` in full (669 lines) — note JSX block lines 598-668 already small; bulk is hooks + handlers + `useMemo` overlays lines 74-596.
    - `web/src/components/AssistantChat/StatusBar.tsx`, `ComposerButtons.tsx`, `AttachmentItem.tsx` lines 1-30 each — confirm visual sections already extracted (RESEARCH Q6).
    - `web/src/components/AssistantChat/ComposerButtons.tsx` lines 1-10 — sibling-component shape reference.
    - RESEARCH.md Q6 (lines 580-603) — 3-piece split (state hook + handlers hook + overlays component); NOT 4-5 visual sections.
    - PATTERNS.md §"web/src/components/AssistantChat/HappyComposer.tsx" lines 263-281 — flat dir layout, no nested subdir.
  </read_first>
  <action>
    Step 1 — Create `useHappyComposerState.ts` lifting the state-setup region (current lines ~75-250): `useAssistantState` selectors, derived flags, `useMemo`'d derived values, schedule lift, draft persistence wiring. Export a single hook returning a stable object of state + setters consumed by the orchestrator. Step 2 — Create `useHappyComposerHandlers.ts` lifting the handlers region (current lines ~250-477): `handleChange`, `handleSelect`, `handleKeyDown`, `handlePaste`, `handleSubmit`, `handleAbort`, `handleSwitch`, `handleSend`, `handleSuggestionSelect`, `handlePermissionChange`, `handleModelChange`, `handleSettingsToggle`. Step 3 — Create `HappyComposerOverlays.tsx` extracting the overlays `useMemo` block (current lines 479-596) — permission picker + model picker float + autocomplete float — as a named-export component receiving the relevant state/handler props. Step 4 — Rewrite `HappyComposer.tsx` as orchestrator: imports the 2 hooks + the overlays component + existing sibling components (`StatusBar`, `ComposerButtons`, `AttachmentItem`, `Autocomplete`, `FloatingOverlay`, etc.); composes JSX + renders `<HappyComposerOverlays {...overlayProps} />`. Preserve the existing export (default or named — match current shape). Per RESEARCH Q6 do NOT create a 4th "send-controls" file (already done via `ComposerButtons`). Step 5 — Add colocated tests for the 2 hooks (state hook: derived flag toggles; handlers hook: `handleSend` / `handleAbort` callback behavior under stub state). Per RESEARCH Open Question 2, full RTL coverage of the component itself is optional — hook-level tests suffice.
  </action>
  <verify>
    <automated>bun typecheck &amp;&amp; bun run test:web -- HappyComposer</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l web/src/components/AssistantChat/HappyComposer.tsx` reports < 300.
    - All 3 new files exist: `test -f web/src/components/AssistantChat/HappyComposerOverlays.tsx` && `test -f web/src/components/AssistantChat/useHappyComposerState.ts` && `test -f web/src/components/AssistantChat/useHappyComposerHandlers.ts`.
    - Each new file < 300 lines.
    - 2 colocated hook test files exist; each has ≥ 2 `it(` blocks.
    - The original export of `HappyComposer` is unchanged (default vs named matches pre-refactor — verify by grepping caller `from '@/components/AssistantChat/HappyComposer'` imports continue to typecheck).
    - `bun typecheck` exits 0; `bun run test:web -- HappyComposer` exits 0.
  </acceptance_criteria>
  <done>HappyComposer orchestrator < 300 lines; state + handlers + overlays extracted; hook-level tests present.</done>
</task>

<task type="auto">
  <name>Task 3.3: Decompose _results.tsx into dispatcher + _resultHelpers.ts + 3 per-tool result components + test redistribution</name>
  <files>web/src/components/ToolCard/views/_results.tsx, web/src/components/ToolCard/views/_results.test.tsx, web/src/components/ToolCard/views/results/_resultHelpers.ts, web/src/components/ToolCard/views/results/BashResult.tsx, web/src/components/ToolCard/views/results/LineListResult.tsx, web/src/components/ToolCard/views/results/ReadResult.tsx, web/src/components/ToolCard/views/results/BashResult.test.tsx, web/src/components/ToolCard/views/results/LineListResult.test.tsx, web/src/components/ToolCard/views/results/ReadResult.test.tsx</files>
  <read_first>
    - `web/src/components/ToolCard/views/_results.tsx` in full (687 lines) — RESEARCH Q7 line-range map of each view + helpers.
    - `web/src/components/ToolCard/views/_results.test.tsx` in full — identify cases targeting Bash / LineList / Read views (move to colocated tests) vs cases for inline views (stay).
    - `web/src/components/ToolCard/views/EditView.tsx` lines 1-22 — PATTERNS Shared Pattern C (per-tool view component shape).
    - `web/src/components/ToolCard/views/_all.tsx` lines 1-62 — dispatcher shape reference (D-153: do not modify this file).
    - RESEARCH.md Q7 (lines 606-639) — view line counts + extraction threshold (≥ 50 lines OR borderline distinct concern); recommended extracted set: Bash (48), LineList (52), Read (42); ~380 lines of shared helpers move to `_resultHelpers.ts`.
    - PATTERNS.md §"web/src/components/ToolCard/views/_results.tsx" lines 285-324.
  </read_first>
  <action>
    Step 1 — Create `web/src/components/ToolCard/views/results/_resultHelpers.ts` containing the shared helpers currently at `_results.tsx` lines ~1-383: `extractTextFromResult`, `renderText`, `renderMarkdown`, `renderResultBody`, `renderPlainTextQuote`, `looksLikeJson`/`looksLikeHtml`/`parseStandaloneMarkdownCodeBlock`, `codeLanguageByExtension`, `inferCodeLanguage*`, `resultCodeBlockProps`, `placeholderForState`, `RawJsonDevOnly`, `extractStdoutStderr`, `extractReadFileContent`, `isReadFileToolCall`, `extractReadPathFromInput`, `renderReadTextResult`, `ResultMetaPill`, `ResultStatusPill`, `extractLineList`, `isProbablyMarkdownList`, `getMutationResultRenderMode`. Use named exports. Step 2 — Create `results/BashResult.tsx`, `results/LineListResult.tsx`, `results/ReadResult.tsx` each containing the corresponding view component (named export, single component matching `ToolResultViewProps` shape — confirm exact prop type from `_results.tsx` registry signature lines 660-687); each imports needed helpers from `./_resultHelpers`. Step 3 — Rewrite `_results.tsx` as dispatcher: import helpers from `./results/_resultHelpers` (re-export `extractTextFromResult` and `getMutationResultRenderMode` for external callers — PRESERVE these existing re-exports since other code consumes them); import 3 extracted views from `./results/`; keep the 6 small inline views (AskUserQuestion 12, Markdown 24, Mutation 31, TodoWrite 8, Skill 24, Generic 25); keep the registry + `getToolResultViewComponent` dispatcher function. Target size: < 250 lines. Step 4 — Redistribute test cases from `_results.test.tsx` for the 3 extracted views into colocated `results/{Bash,LineList,Read}Result.test.tsx`; reuse the existing RTL setup + `vi.mock` blocks (PATTERNS Shared Pattern F); cases for inline views stay in `_results.test.tsx`. Per D-153, do NOT touch `_all.tsx`, `ToolCard.tsx`, or `knownTools.tsx` (other than the Slice 1 testid edit). Per Claude's Discretion ≥ 50 threshold (RESEARCH Q7), do NOT extract the 6 small inline views.
  </action>
  <verify>
    <automated>bun typecheck &amp;&amp; bun run test:web -- _results &amp;&amp; bun run test:web -- ToolCard.integration</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l web/src/components/ToolCard/views/_results.tsx` reports < 250.
    - All 4 new files exist: `_resultHelpers.ts`, `BashResult.tsx`, `LineListResult.tsx`, `ReadResult.tsx` under `web/src/components/ToolCard/views/results/`.
    - Each new view file < 300 lines; `_resultHelpers.ts` < 500.
    - 3 colocated test files exist under `results/`; each has ≥ 1 `it(` block.
    - `grep -c "export function extractTextFromResult\|export { extractTextFromResult" web/src/components/ToolCard/views/_results.tsx` ≥ 1 (re-export preserved).
    - `grep -c "export function getMutationResultRenderMode\|export { getMutationResultRenderMode" web/src/components/ToolCard/views/_results.tsx` ≥ 1 (re-export preserved).
    - `bun typecheck` exits 0.
    - `bun run test:web -- _results` exits 0.
    - `bun run test:web -- ToolCard.integration` exits 0 (Slice 1 integration test still green post-decomposition).
  </acceptance_criteria>
  <done>_results.tsx dispatcher < 250 lines; 3 per-tool views + shared helpers extracted; existing test cases redistributed; Slice 1 integration test still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure refactor) | Internal component decomposition only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | _results.tsx re-export drift | mitigate | Acceptance criteria 3.3 grep for `extractTextFromResult` + `getMutationResultRenderMode` re-exports; preserves external callers. |
| T-09-07 | Tampering | settings/index.tsx route entry drift | mitigate | Acceptance criterion: `grep -c "^export default" web/src/routes/settings/index.tsx == 1`; route is the entry point TanStack Router resolves. |
| T-09-08 | Tampering | HappyComposer export drift | mitigate | Caller import sites continue to typecheck unchanged (Slice 4 full `bun typecheck` gate). |
| T-09-09 | Tampering | Slice 1 integration test regression | mitigate | Task 3.3 verify explicitly re-runs `ToolCard.integration` post-decomposition. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Slice 3 installs 0 new packages. |
</threat_model>

<verification>
- `bun typecheck` exits 0.
- `bun run test:web` exits 0 (settings + HappyComposer + _results + ToolCard.integration all green).
- File-size budgets:
  - `web/src/routes/settings/index.tsx` < 300
  - Every `web/src/routes/settings/_sections/*.tsx` < 300
  - `web/src/components/AssistantChat/HappyComposer.tsx` < 300
  - Every `web/src/components/AssistantChat/{HappyComposerOverlays.tsx, useHappyComposer*.ts}` < 300
  - `web/src/components/ToolCard/views/_results.tsx` < 250
  - Every `web/src/components/ToolCard/views/results/*.tsx` < 300
- No `_tabs/` directory exists under `web/src/routes/settings/`.
- Slice 1 ToolCard integration test still passes.
- Slice 2 message-window-store + SessionList tests still pass.
</verification>

<success_criteria>
- REFW-02 (closing half): settings, HappyComposer, and `_results.tsx` all under budget; new sub-pieces under 300 lines; colocated tests added.
- Public surfaces preserved: settings route default export; HappyComposer export shape; `_results.tsx` re-exports (`extractTextFromResult`, `getMutationResultRenderMode`).
- Full suite green: `bun typecheck && bun run test`.
- Slice 1 integration test green; Slice 2 tests green.
</success_criteria>

<output>
Create `.planning/phases/09-web-internal-decoupling/09-03-SUMMARY.md` when done.
</output>