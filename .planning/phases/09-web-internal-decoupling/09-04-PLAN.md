---
phase: 09-web-internal-decoupling
plan: 4
type: execute
wave: 4
depends_on:
  - 09-03
files_modified:
  - scripts/check-no-cut-agents.sh
autonomous: true
requirements:
  - REFW-01
  - REFW-02
  - REFW-03
must_haves:
  truths:
    - "scripts/check-no-cut-agents.sh tail-invokes scripts/check-no-circular-web.sh so the phase gate is a single command."
    - "Phase 9 D-158 sweep block enforces 7 numbered sub-checks (levenshtein dedup, base64 dedup, cycles via tail, file-size red lines, fallback testid anchor + reverse-assert, createApiQuery factory + ≥ 3 importers, madge cycles)."
    - "Phase gate command — bash scripts/check-no-cut-agents.sh + bash scripts/check-no-circular-web.sh + bun typecheck + bun run test — exits 0 across the whole repo."
    - "All Slices 1-3 outputs survive the guard (no rollback needed)."
  artifacts:
    - path: "scripts/check-no-cut-agents.sh"
      provides: "Phase-9 sweep block appended; tail-invokes the madge guard"
      contains: "Phase 9 — Web internal decoupling (D-158)"
  key_links:
    - from: "scripts/check-no-cut-agents.sh"
      to: "scripts/check-no-circular-web.sh"
      via: "bash $(dirname \"$0\")/check-no-circular-web.sh"
      pattern: "check-no-circular-web.sh"
---

<objective>
Slice 4 — Wire the Phase 9 zero-tolerance guard sweep block into `scripts/check-no-cut-agents.sh`, tail-invoke `scripts/check-no-circular-web.sh`, and prove the full phase gate green (`bun typecheck` + `bun run test` + both guard scripts exit 0).

Purpose: Lock in the structural invariants established by Slices 1-3 so any future regression (duplicated util, oversized file, missing testid, dropped factory user, new web cycle) fails CI loudly. Mirrors the P5 D-86 / P6 D-110 / P7 D-126 / P8 D-143 4-slice closing template.

Output: Phase 9 sweep block appended to `scripts/check-no-cut-agents.sh` immediately after the Phase 8 block; 7 numbered sub-checks per D-158 + RESEARCH Q11 skeleton; the script ends by tail-invoking `check-no-circular-web.sh` and echoing a Phase 9 PASS line. The full repo gate (`bash scripts/check-no-cut-agents.sh && bun typecheck && bun run test`) exits 0.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-web-internal-decoupling/09-CONTEXT.md
@.planning/phases/09-web-internal-decoupling/09-RESEARCH.md
@.planning/phases/09-web-internal-decoupling/09-PATTERNS.md
@.planning/phases/09-web-internal-decoupling/09-VALIDATION.md
@.planning/phases/09-web-internal-decoupling/09-01-SUMMARY.md
@.planning/phases/09-web-internal-decoupling/09-02-SUMMARY.md
@.planning/phases/09-web-internal-decoupling/09-03-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Append Phase 9 sweep block to scripts/check-no-cut-agents.sh + tail-invoke check-no-circular-web.sh</name>
  <files>scripts/check-no-cut-agents.sh</files>
  <read_first>
    - `scripts/check-no-cut-agents.sh` lines 1-30 (RG_BIN detection + helper patterns) + lines 286-387 (Phase 8 sweep block — RESEARCH Q11 verified template + tail-invocation pattern).
    - `scripts/check-no-circular-web.sh` (from Slice 1).
    - `scripts/check-no-circular-hub.sh` (mirror reference).
    - RESEARCH.md Q11 (lines 741-872) — literal Phase 9 sweep block body to paste; 7 numbered sub-checks; insertion point immediately after line 386 (Phase 8 PASS echo).
    - CONTEXT D-158 — 6 enumerated checks (#1 levenshtein, #2 estimateBase64Bytes, #3 madge cycles, #4 file-size red lines, #5 fallback testid, #6 createApiQuery factory) plus the tail-invocation as a 7th line.
    - PATTERNS.md §"scripts/check-no-cut-agents.sh (modified)" lines 494-528.
  </read_first>
  <action>
    Open `scripts/check-no-cut-agents.sh` and append the Phase 9 sweep block immediately after the existing Phase 8 PASS echo line (line ~386), before EOF. Use the literal block from RESEARCH Q11 (lines 770-872) with these 7 numbered sub-checks: (#1) `function levenshteinDistance` / `function levenshtein` appears exactly 1× in `web/src/` (must be `web/src/lib/fuzzyMatch.ts`) and 0× across `cli/src/`, `hub/src/`, `shared/src/`. (#2) `function estimateBase64Bytes` appears exactly 1× in `shared/src/` (must be `shared/src/uploads.ts`) and 0× across `cli/src/`, `hub/src/`, `web/src/`. (#3) File-size red lines via `wc -l` + `awk`: `SessionList.tsx`, `message-window-store.ts`, `settings/index.tsx`, `HappyComposer.tsx`, `_results.tsx`, `reducerTimeline.ts`, `ToolCard.tsx`, `knownTools.tsx` each < 500 lines; `views/_all.tsx` < 200 lines. (#4) `messageWindow*.ts` sub-modules under `web/src/lib/` (excluding tests) each < 400 lines (D-149 budget). (#5) `data-testid="tool-card-unknown-fallback"` appears exactly 1× in `web/src/components/ToolCard/knownTools.tsx` (NOT `_results.tsx` per RESEARCH Pitfall 3) AND `queryByTestId(...tool-card-unknown-fallback...)` is reverse-asserted in `web/src/components/ToolCard/ToolCard.integration.test.tsx`. (#6) `^export function createApiQuery` appears exactly 1× in `web/src/hooks/queries/_factory.ts` AND ≥ 3 other files in `web/src/hooks/queries/` import `createApiQuery` (excluding `_factory.ts` itself and `*.test.*`). (#7) Tail-invoke: `bash "$(dirname "$0")/check-no-circular-web.sh"`. End the block with `echo "✅ Phase 9 guard PASS (D-158 #1–#6 + madge zero cycles)."`. Use the existing `RG_BIN` ripgrep wrapper defined at the top of the script (lines 23-30); use `find … -maxdepth 1 -name '*.ts' ! -name '*.test.ts' -exec wc -l {} \; | awk '$1 >= N { print }'` for file-size budgets (P8 D-143 #5 template). The block must `exit 1` on any failure with a clear `❌` message naming the D-158 sub-check.
  </action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 9 — Web internal decoupling (D-158)" scripts/check-no-cut-agents.sh` ≥ 1.
    - `grep -c "function levenshteinDistance" scripts/check-no-cut-agents.sh` ≥ 1 (sub-check #1 present).
    - `grep -c "function estimateBase64Bytes" scripts/check-no-cut-agents.sh` ≥ 1 (sub-check #2 present).
    - `grep -c "tool-card-unknown-fallback" scripts/check-no-cut-agents.sh` ≥ 2 (positive + reverse-assert).
    - `grep -c "createApiQuery" scripts/check-no-cut-agents.sh` ≥ 2 (definition count + importer count).
    - `grep -c "check-no-circular-web.sh" scripts/check-no-cut-agents.sh` ≥ 1 (tail-invocation).
    - `grep -c "Phase 9 guard PASS" scripts/check-no-cut-agents.sh` ≥ 1.
    - Phase 8 block is NOT modified: `grep -c "Phase 8 guard PASS" scripts/check-no-cut-agents.sh` still equals 1 (or whatever the pre-Phase-9 count was).
    - `bash scripts/check-no-cut-agents.sh` exits 0; final stdout contains "Phase 9 guard PASS".
  </acceptance_criteria>
  <done>Phase 9 sweep block appended; phase gate single-command exits 0; all 7 sub-checks pass against the codebase produced by Slices 1-3.</done>
</task>

<task type="auto">
  <name>Task 4.2: Full repo phase gate — bun typecheck + bun run test + both guard scripts green</name>
  <files></files>
  <read_first>
    - Slice 1-3 SUMMARYs.
    - `scripts/check-no-cut-agents.sh` (after Task 4.1).
    - `scripts/check-no-circular-web.sh`.
  </read_first>
  <action>
    Run the full Phase 9 gate in this exact order and confirm each step exits 0. If anything fails, do NOT add whitelists — instead, root-cause the failing artifact and fix it (per D-159 and P8 precedent: guard is the contract, not negotiable). After everything is green, this slice is done. Do NOT modify any source code in this task; this is purely the gate. If a failure surfaces a legitimate Slice 1/2/3 omission, fix the underlying slice's plan output and re-run the gate.
  </action>
  <verify>
    <automated>bun typecheck &amp;&amp; bun run test &amp;&amp; bash scripts/check-no-circular-web.sh &amp;&amp; bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bun typecheck` exits 0.
    - `bun run test` exits 0 (cli + hub + web + shared all green).
    - `bash scripts/check-no-circular-web.sh` exits 0 (madge: 0 cycles in `web/src/`).
    - `bash scripts/check-no-cut-agents.sh` exits 0 (Phase 1-9 sweep blocks + tail-invocation of hub + web madge guards all green).
    - Final stdout from `check-no-cut-agents.sh` contains "Phase 9 guard PASS".
  </acceptance_criteria>
  <done>Phase 9 phase gate green; SC#1-#4 from ROADMAP all satisfied; REFW-01 + REFW-02 + REFW-03 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / commit gate | The guard scripts are the contract surface between this phase's structural invariants and future code changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | Future regression of util dedup (someone re-introduces a local `levenshteinDistance` or `estimateBase64Bytes`) | mitigate | D-158 sub-checks #1 + #2 fail the gate with explicit error pointing at the violating file. |
| T-09-11 | Tampering | Future regression of file-size budget (someone re-grows `SessionList.tsx` past 500 lines) | mitigate | D-158 sub-check #3 (red-line `awk` filter) fails the gate. |
| T-09-12 | Tampering | Future regression of `createApiQuery` adoption (someone deletes the factory or its users) | mitigate | D-158 sub-check #6 fails when definition count ≠ 1 or importer count < 3. |
| T-09-13 | Tampering | Future regression of integration test anchor (someone renames `tool-card-unknown-fallback`) | mitigate | D-158 sub-check #5 fails on missing anchor; reverse-assert in integration test additionally fails the test suite. |
| T-09-14 | Tampering | New circular dependency in `web/src/` | mitigate | D-158 sub-check #7 (tail-invocation of `check-no-circular-web.sh`) fails on any madge cycle. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Slice 4 installs 0 new packages. |
</threat_model>

<verification>
- `bun typecheck` exits 0.
- `bun run test` exits 0 (cli + hub + web + shared all green).
- `bash scripts/check-no-circular-web.sh` exits 0.
- `bash scripts/check-no-cut-agents.sh` exits 0; final line includes "Phase 9 guard PASS".
- Phase 8 sweep block + tail-invocation of `check-no-circular-hub.sh` still green (no regression).
</verification>

<success_criteria>
- ROADMAP SC#1 — `madge` reports 0 cycles inside `web/src/components/ToolCard/` AND `web/src/`; integration test asserts every `knownTools` entry resolves to a renderer.
- ROADMAP SC#2 — `SessionList.tsx`, `message-window-store.ts`, `reducerTimeline.ts`, `routes/settings/index.tsx`, `AssistantChat/HappyComposer.tsx` each < ~500 lines; each split unit has a focused colocated test.
- ROADMAP SC#3 — `levenshteinDistance` (web-only, per D-155), `estimateBase64Bytes`, `CursorPermissionMode` (done in P5/P7 per D-146), and `createApiQuery` deduplicated to single sources; ripgrep finds 0 duplicate implementations.
- ROADMAP SC#4 — `madge --circular web/src/` 0 cycles; `bun typecheck` + `bun run test` both green.
- Phase 9 phase gate is a single command: `bash scripts/check-no-cut-agents.sh && bun typecheck && bun run test`.
</success_criteria>

<output>
Create `.planning/phases/09-web-internal-decoupling/09-04-SUMMARY.md` when done.
</output>
