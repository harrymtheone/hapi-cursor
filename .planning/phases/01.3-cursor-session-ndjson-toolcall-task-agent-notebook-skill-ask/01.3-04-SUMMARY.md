---
phase: 01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask
plan: 04
subsystem: web-toolcard
tags: [tool-presentation, hydration, trace-deferral, d-14]

requires:
  - phase: 01.3-03
    provides: Hub infer + reconcile for advanced tool names on result-only windows
provides:
  - D-14 wire audit with Trace nesting deferral documented
  - Web hydration tests for five priority tools (TOOL-AUDIT-04)
affects: []

tech-stack:
  added: []
  patterns:
    - "Consumer-only Web alignment; CLI normalizers sufficient — no knownTools alias gaps"

key-files:
  created: []
  modified: []

key-decisions:
  - "Task/Agent Trace nesting deferred — wire lacks parent/child callId (D-14)"

patterns-established: []

requirements-completed: []

duration: 0min
completed: 2026-05-26
---

# Phase 01.3 Plan 04 Summary

**Wire audit defers Task/Agent Trace nesting; Web hydration tests lock five priority tool presentations against CLI-normalized inputs.**

## Deferrals (D-14 wire audit)

**Grep outcome (repo + `@hapi/protocol`):**

| Field searched | Protocol (`ToolCallProjectionSchema`) | CLI/hub/web wire |
|----------------|--------------------------------------|------------------|
| `parentCallId` | absent | absent |
| `parent_call_id` | absent | absent |
| `parentId` (tool-call) | absent | absent |

`ToolCallProjection` carries only `callId`, `name`, `input`, `status`, `result`, timestamps, and optional `permission` — no parent/child linkage between tool calls.

**Current Web behavior:** `reducerTimeline.ts` nests Trace children via `isSubagentToolName(name)` + sidechain `groups` map (`msg.id` → sidechain messages), not via wire parent call IDs. `trace.tsx` reads `block.children` populated by that reducer path.

**Decision:** Task/Agent nested Trace deferred — wire lacks parent/child callId (D-14). No `reducerTimeline` / `trace.tsx` structural changes in plan 04. Follow-up only if Cursor NDJSON adds explicit parent call linkage on tool-call payloads.

## Performance

- **Duration:** (in progress)
- **Started:** 2026-05-26T02:44:45Z
- **Completed:** (pending)
- **Tasks:** (pending)
- **Files modified:** (pending)

## Accomplishments

(pending task 2)

## Task Commits

(pending)

## Files Created/Modified

(pending)

## Deviations from Plan

(pending)

## Self-Check

(pending)

---
*Phase: 01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask*
*Completed: (pending)*
