---
phase: 01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask
verified: 2026-05-26T10:48:00Z
status: human_needed
score: 16/16
overrides_applied: 0
human_verification:
  - test: "Open a HAPI PWA session that contains Task, Agent, NotebookRead or NotebookEdit, Skill, and AskUserQuestion tool cards (live agent run or replayed session with those tools)."
    expected: "Each card shows the correct tool name and a meaningful title/subtitle (not generic 'Tool' or 'unknown'); AskUserQuestion shows the question footer when applicable."
    why_human: "Automated tests cover projection hydration and getToolPresentation only — layout, truncation, and real wire timing in the mobile PWA cannot be verified by grep or unit tests (01.3-VALIDATION.md Manual-Only: Web ToolCard presentation)."
---

# Phase 01.3: Cursor 活动审计 Verification Report

**Phase Goal:** Audit real Cursor session NDJSON and complete the `*ToolCall` mapping table so Web can render all agent activity types (Task/Agent/Notebook/Skill/AskUserQuestion and other native Cursor tool variants).

**Verified:** 2026-05-26T10:48:00Z  
**Status:** human_needed  
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Developer can enable `HAPI_CURSOR_NDJSON_CAPTURE=1` and receive redacted NDJSON under `~/.hapi/cursor-ndjson-capture/` | ✓ VERIFIED | `cursorNdjsonCapture.ts`: `isNdjsonCaptureEnabled()` accepts `1/true/yes`; `captureBaseDir()` → `cursor-ndjson-capture`; 12/12 unit tests pass |
| 2 | Capture runs on stdout ingest before `parseCursorEvent` (D-01) | ✓ VERIFIED | `cursorRemoteLauncher.ts` L215–218: `appendRedactedNdjsonLine` then `parseCursorEvent` |
| 3 | Committed repo fixtures cover five priority tools with started/completed pairs and `expectedName` | ✓ VERIFIED | `cursorToolCallNdjson.captured.ts`: taskToolCall (captured), editToolCall→NotebookEdit (captured), subagent/skill/ask/notebookRead (documented synthetics); 7 captured converter tests green |
| 4 | `taskToolCall` → Task; `agentToolCall`/`subagentToolCall` → Agent (distinct, D-07) | ✓ VERIFIED | `shared/src/cursor/toolCallMapping.ts` L21–23; mapping tests 13/13 pass |
| 5 | Notebook/Skill/AskUserQuestion keys on allowlist; `editToolCall`+.ipynb → NotebookEdit (D-06) | ✓ VERIFIED | Allowlist L24–26; `isNotebookEditVariant` + `resolveHapiToolName` L130–131 |
| 6 | Unmapped `*ToolCall` keys warn `[cursor-ndjson] unmapped:` then PascalCase (D-08) | ✓ VERIFIED | `warnUnmappedNativeKey` + `warnedUnmappedKeys` Set; test spy in `cursorToolCallMapping.test.ts` |
| 7 | Wire tool-call input uses canonical fields Web reads (description, prompt, notebook_path, skill, questions) | ✓ VERIFIED | `normalizeToolArgs/*` + `extractToolInput` → `normalizeToolInputForName`; `expectCanonicalInput` in converter tests |
| 8 | Shared mapping is single export `@hapi/protocol/cursor`; CLI re-exports (D-18) | ✓ VERIFIED | `shared/package.json` `"./cursor"`; `cli/.../cursorToolCallMapping.ts` re-export only |
| 9 | Hub `inferToolNameFromPayload` heals legacy `unknown` for five advanced tools (D-17) | ✓ VERIFIED | `toolCallProjection.ts` L130–151; infer tests 10/10 pass |
| 10 | Infer extended in same reconcile pass as 01.2-07; no new reconcile triggers (D-19, D-20) | ✓ VERIFIED | Advanced branches appended after Bash/Read heuristics; `messageService.ts` lazy `reconciledSessions` unchanged |
| 11 | Result-only `getMessagesPage` shows non-placeholder names for advanced tools (BUG-TOOL-01) | ✓ VERIFIED | `sessionModel.test.ts`: five `legacy unknown upgrades to *` cases pass (40/40 file) |
| 12 | Web hydration: Task/Agent/NotebookRead/Skill/AskUserQuestion show real name and non-placeholder title (D-13) | ✓ VERIFIED | `reducerTimeline.test.ts` describe `advanced tool presentation hydration (01.3-04)` — 30/30 pass |
| 13 | `isAskUserQuestionToolName('AskUserQuestion')` true; footer-eligible (D-15) | ✓ VERIFIED | Test at L612–616, L715–716 in `reducerTimeline.test.ts` |
| 14 | Agent results use GenericResultView — no AgentResultView (D-16) | ✓ VERIFIED | `rg AgentResultView web/src` — no matches; 01.3-04-SUMMARY confirms `_results.tsx` path |
| 15 | D-14 wire audit: Trace nesting deferred with documented outcome | ✓ VERIFIED | `01.3-04-SUMMARY.md` Deferrals: no `parentCallId` on wire; "Trace nesting deferred — wire lacks parent/child callId" |
| 16 | Phase goal: mapping table complete enough for Web to render five priority activity types | ✓ VERIFIED | End-to-end chain: shared allowlist → CLI normalizers → hub infer → web `getToolPresentation` tests |

**Score:** 16/16 truths verified (automated)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `cli/src/cursor/utils/cursorNdjsonCapture.ts` | Dev capture + redaction | ✓ VERIFIED | 94 lines; exports `isNdjsonCaptureEnabled`, `appendRedactedNdjsonLine`, `getCapturePathForSession` |
| `cli/src/cursor/utils/cursorNdjsonCapture.test.ts` | Unit tests | ✓ VERIFIED | 12 tests pass |
| `cli/src/cursor/utils/fixtures/cursorToolCallNdjson.captured.ts` | Five-tool fixtures | ✓ VERIFIED | 238 lines; `CURSOR_TOOL_CALL_NDJSON_CAPTURED_FIXTURES` with 6 entries (notebook edit via editToolCall) |
| `shared/src/cursor/toolCallMapping.ts` | Shared allowlist | ✓ VERIFIED | 150 lines; wired via package export + CLI re-export |
| `cli/src/cursor/utils/normalizeToolArgs/*` | Per-tool normalizers | ✓ VERIFIED | 8 files; 9 normalizer tests pass |
| `hub/src/sync/toolCallProjection.ts` | Advanced infer | ✓ VERIFIED | AskUserQuestion/Skill/Notebook/Task/Agent branches present |
| `web/src/chat/reducerTimeline.test.ts` | Hydration regression | ✓ VERIFIED | Advanced-tool describe block added |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `cursorRemoteLauncher.ts` | `cursorNdjsonCapture.ts` | `rl.on('line')` before parse | ✓ WIRED | L215–218 |
| `cursorEventConverter.ts` | `@hapi/protocol/cursor` | `resolveHapiToolName` | ✓ WIRED | Import L7; `extractToolName` L55 |
| `cursorEventConverter.ts` | `normalizeToolArgs` | `normalizeToolInputForName` | ✓ WIRED | `extractToolInput` L65–68 |
| `toolCallProjection.ts` | `mergeToolCallProjection` | `resolveProjectionName` → `inferToolNameFromPayload` | ✓ WIRED | L159–165 |
| `messageService.ts` | `reconcileSessionToolCalls` | lazy on first `getMessagesPage` | ✓ WIRED | L61–63 unchanged |
| `reducerTimeline.ts` | `knownTools` / `getToolPresentation` | projection map | ✓ WIRED | Tests assert presentation titles |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `cursorEventConverter` tool_call | `msg.input` | Native args → normalizers | Yes — canonical fields from fixture/capture shapes | ✓ FLOWING |
| Hub projection merge | `name` | `inferToolNameFromPayload(input)` | Yes — heuristics on stored input | ✓ FLOWING |
| Web ToolCard hydration test | `projection.name`, `projection.input` | Test `ToolCallProjection` map | Yes — canonical test payloads | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| NDJSON capture + redaction | `cd cli && bunx vitest run src/cursor/utils/cursorNdjsonCapture.test.ts` | 12/12 pass | ✓ PASS |
| Allowlist + unmapped warn | `cd cli && bunx vitest run src/cursor/utils/cursorToolCallMapping.test.ts` | 13/13 pass | ✓ PASS |
| Normalizers | `cd cli && bunx vitest run src/cursor/utils/normalizeToolArgs/normalizeToolArgs.test.ts` | 9/9 pass | ✓ PASS |
| Captured fixture convert | `cd cli && bunx vitest run src/cursor/utils/cursorEventConverter.test.ts` | 33/33 pass | ✓ PASS |
| Hub infer advanced tools | `cd hub && bun test src/sync/toolCallProjection.test.ts -t "infer"` | 10/10 pass | ✓ PASS |
| Legacy unknown reconcile | `cd hub && bun test src/sync/sessionModel.test.ts` | 40/40 pass | ✓ PASS |
| Web hydration | `cd web && bun run test -- src/chat/reducerTimeline.test.ts` | 30/30 pass | ✓ PASS |

### Probe Execution

Step 7c: SKIPPED — no phase-declared `scripts/*/tests/probe-*.sh` and not a migration/tooling probe phase.

### Requirements Coverage

| Requirement | Source Plan | Description (RESEARCH.md) | Status | Evidence |
|-------------|-------------|---------------------------|--------|----------|
| TOOL-AUDIT-01 | 01.3-01 | Dev NDJSON capture on ingest with redaction | ✓ SATISFIED | Module + launcher hook + tests |
| TOOL-AUDIT-02 | 01.3-01 | Redacted fixtures in repo for CI | ✓ SATISFIED | `cursorToolCallNdjson.captured.ts`; 2 captured + 4 documented synthetics per 01.3-01-SUMMARY |
| TOOL-AUDIT-03 | 01.3-02 | Allowlist + warn for unmapped keys | ✓ SATISFIED | `toolCallMapping.ts` + tests |
| TOOL-AUDIT-04 | 01.3-02, 01.3-04 | Five tools: name + usable presentation input | ✓ SATISFIED (automated) / ? HUMAN (visual) | CLI converter + web hydration tests green; PWA visual pass pending |
| TOOL-AUDIT-05 | 01.3-03 | Hub legacy unknown healed in one reconcile pass | ✓ SATISFIED | `inferToolNameFromPayload` + sessionModel integration |
| BUG-TOOL-01 | 01.3-03, 01.3-04 | Real tool identity after pagination/reload | ✓ SATISFIED (automated) | Hub result-only tests + web projection hydration |

**Note:** `TOOL-AUDIT-*` and `BUG-TOOL-01` are tracked in ROADMAP.md and phase RESEARCH.md; not present as lines in `.planning/REQUIREMENTS.md` (same pattern as phase 01.2).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None in phase-touched production paths | — | No TBD/FIXME/XXX; no stub `return null` handlers in mapping/capture/infer |

### Human Verification Required

#### 1. PWA ToolCard presentation (TOOL-AUDIT-04 visual)

**Test:** Run or replay a session in the HAPI PWA that includes Task, Agent, Notebook (read or edit), Skill, and AskUserQuestion tool activity.

**Expected:** Each card shows the resolved tool name and a meaningful title (not the generic `Tool` or `unknown` placeholder). AskUserQuestion cards show the question UI/footer when the tool is active or completed.

**Why human:** Presentation logic is covered by unit tests; mobile layout, truncation, and live SSE timing require visual confirmation per `01.3-VALIDATION.md`.

### Deferred Items (intentional, not gaps)

| # | Item | Addressed In | Evidence |
|---|------|--------------|----------|
| 1 | Task/Agent nested Trace UI via parent call IDs | Follow-up (post-01.3) | D-14: wire lacks `parentCallId` / `parent_call_id`; documented in `01.3-04-SUMMARY.md` |

### Gaps Summary

No automated gaps. All must-haves verified in code and tests. Phase status is **human_needed** solely for optional-but-recommended PWA visual confirmation of ToolCard presentation for the five priority tools.

---

_Verified: 2026-05-26T10:48:00Z_  
_Verifier: Claude (gsd-verifier)_
