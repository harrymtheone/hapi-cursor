---
phase: 01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask
plan: 04
type: execute
wave: 3
depends_on:
  - 01.3-03
files_modified:
  - web/src/components/ToolCard/askUserQuestion.ts
  - web/src/components/ToolCard/knownTools.tsx
  - web/src/chat/reducerTimeline.test.ts
autonomous: true
requirements:
  - TOOL-AUDIT-04
  - BUG-TOOL-01
must_haves:
  truths:
    - "Web ToolCard shows correct name + title/subtitle for five priority tools — not generic Tool placeholder (D-13)"
    - "AskUserQuestionFooter still renders for Cursor-resolved AskUserQuestion name (D-15)"
    - "Agent results use GenericResultView + markdown — no AgentResultView (D-16)"
    - "Task/Agent Trace nesting deferred with documented wire audit outcome if parent/child linkage absent (D-14)"
  artifacts:
    - path: "web/src/components/ToolCard/askUserQuestion.ts"
      provides: "isAskUserQuestionToolName covers AskUserQuestion"
    - path: "web/src/chat/reducerTimeline.test.ts"
      provides: "Hydration test for Task/Agent non-placeholder presentation"
  key_links:
    - from: "web/src/chat/reducerTimeline.ts"
      to: "knownTools"
      via: "projection map name + input"
      pattern: "getToolPresentation"
---

<objective>
Verify Web consumer alignment with CLI-normalized wire data; apply minimal alias fixes only where normalizers leave gaps; document Task/Agent Trace deferral per wire audit.

Purpose: Web is consumer-only for this phase — incremental presentation fixes, not new ToolCard paradigms (D-13–D-16, D-14).
Output: Optional tiny Web alias patches, reducer hydration tests, DEFERRAL note in SUMMARY if Trace nesting absent on wire.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask/01.3-02-SUMMARY.md
@.planning/phases/01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask/01.3-03-SUMMARY.md
@web/src/components/ToolCard/knownTools.tsx
@web/src/components/ToolCard/askUserQuestion.ts
@web/src/components/ToolCard/trace.tsx
@web/src/chat/reducerTimeline.ts
@web/src/chat/subagentTool.ts
</context>

<execution_preconditions>
- Plans 01.3-02 and 01.3-03 complete.
- Run GitNexus `impact` on `isAskUserQuestionToolName`, `getToolPresentation` if editing Web files.
</execution_preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Wire audit and Trace deferral documentation</name>
  <files>web/src/chat/reducerTimeline.ts, web/src/components/ToolCard/trace.tsx</files>
  <read_first>
    - web/src/chat/reducerTimeline.ts (isSubagentToolName usage ~118+)
    - web/src/chat/subagentTool.ts
    - @hapi/protocol types for tool-call wire messages
    - 01.3-CONTEXT.md D-14
  </read_first>
  <acceptance_criteria>
    - Grep wire/protocol for parentCallId, parent_call_id, parentId on tool-call payloads — outcome recorded in 01.3-04-SUMMARY.md
    - If no parent/child linkage on wire: SUMMARY states Trace nesting deferred to follow-up; no reducer/trace.tsx structural changes in this plan
    - If linkage exists: document only (do not implement nesting unless trivial one-line hook already present)
  </acceptance_criteria>
  <action>
    Audit D-14: search codebase and @hapi/protocol for parent call linkage fields on tool-call / tool-call-result wire payloads (parentCallId, parent_call_id, nested call_id references).

    Read trace.tsx and reducerTimeline.ts to confirm current behavior uses isSubagentToolName(name) only.

    Write findings to 01.3-04-SUMMARY.md Deferrals section:
    - If linkage absent: "Task/Agent nested Trace deferred — wire lacks parent/child callId (D-14)."
    - If linkage present but unused: note for follow-up quick task; still defer implementation this phase unless already wired.

    Do NOT add new Trace UI or reducer nesting logic in this task unless wire audit proves linkage AND existing code path is broken with a one-line fix (unlikely).
  </action>
  <verify>
    <automated>grep -qE "Trace nesting deferred|wire lacks parent" .planning/phases/01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask/01.3-04-SUMMARY.md</automated>
  </verify>
  <done>D-14 wire audit complete with explicit deferral or documented follow-up in SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Minimal Web alias fixes and hydration tests</name>
  <files>web/src/components/ToolCard/askUserQuestion.ts, web/src/components/ToolCard/knownTools.tsx, web/src/chat/reducerTimeline.test.ts</files>
  <read_first>
    - web/src/components/ToolCard/knownTools.tsx Task/Agent/Notebook/Skill entries
    - web/src/components/ToolCard/askUserQuestion.ts isAskUserQuestionToolName
    - web/src/lib/toolInputUtils.ts getInputStringAny
    - 01.3-CONTEXT.md D-12, D-13, D-15, D-16
  </read_first>
  <acceptance_criteria>
    - isAskUserQuestionToolName('AskUserQuestion') true; includes any PascalCase alias from capture if wire name differs (e.g. askUserQuestion only if observed)
    - No new AgentResultView; Agent uses existing GenericResultView path per D-16
    - reducerTimeline.test.ts: projection hydration for Task, Agent, NotebookRead, Skill, and AskUserQuestion with canonical inputs yields presentation title !== 'Tool' and !== 'unknown'
    - AskUserQuestion with questions[] hydrates footer-eligible tool name (D-15)
    - Web changes limited to alias list / getInputStringAny key additions only where Plan 02 normalizer gap documented in SUMMARY
  </acceptance_criteria>
  <behavior>
    - isAskUserQuestionToolName('AskUserQuestion') === true
    - getToolPresentation for Task input { description: 'Audit' } returns title containing 'Audit' or 'Task' but not literal placeholder 'Tool'
    - Hydration test: tool.name Task, input canonical → rendered block toolName Task
  </behavior>
  <action>
    Per D-13, D-15, D-16 — incremental Web only:

    1. askUserQuestion.ts: ensure isAskUserQuestionToolName includes 'AskUserQuestion' and Cursor-resolved variants if capture shows alternate wire name (minimal list extension).

    2. knownTools.tsx: ONLY if Plan 02 left normalizer gaps — add getInputStringAny alias keys on Task/Agent/Notebook/Skill entries (e.g. task_description). Prefer zero Web changes when CLI normalizers sufficient.

    3. reducerTimeline.test.ts: add hydration cases for all five priority tools (Task, Agent, NotebookRead, Skill, AskUserQuestion) mirroring 01.2-07 pattern — assert hydrated tool card name and non-placeholder title via getToolPresentation or DOM-free reducer output inspection used in existing tests.

    4. Confirm Agent result routing still uses GenericResultView (_results.tsx) — no new view file.

    Run: cd web && bun run test -- src/chat/reducerTimeline.test.ts && bun run typecheck from root.
  </action>
  <verify>
    <automated>cd web && bun run test -- src/chat/reducerTimeline.test.ts && cd .. && bun run typecheck</automated>
  </verify>
  <done>Web consumer verified; minimal aliases if needed; hydration tests green (D-12, D-13, D-15, D-16).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web renders stored wire input | No new secret exposure; presentation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3-07 | Tampering | Web alias expansion | mitigate | Aliases are read-only display keys; do not accept untrusted tool names from user input |
| T-01.3-SC | Tampering | npm/pip/cargo installs | accept | No new packages |

</threat_model>

<verification>
- cd web && bun run test -- src/chat/reducerTimeline.test.ts
- bun run typecheck
</verification>

<success_criteria>
- D-13, D-14 (audit+defer), D-15, D-16 satisfied
- TOOL-AUDIT-04 Web slice covered by hydration tests
- Human UAT checklist in 01.3-VALIDATION.md remains optional visual pass
</success_criteria>

<output>
Create `.planning/phases/01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask/01.3-04-SUMMARY.md` when done
</output>
