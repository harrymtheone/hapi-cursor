---
phase: 01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - cli/src/cursor/utils/cursorNdjsonCapture.ts
  - cli/src/cursor/utils/cursorNdjsonCapture.test.ts
  - cli/src/cursor/cursorRemoteLauncher.ts
  - cli/src/cursor/utils/fixtures/cursorToolCallNdjson.captured.ts
autonomous: false
requirements:
  - TOOL-AUDIT-01
  - TOOL-AUDIT-02
user_setup: []
must_haves:
  truths:
    - "Developer can enable HAPI_CURSOR_NDJSON_CAPTURE=1 and receive redacted full-session NDJSON files under ~/.hapi/cursor-ndjson-capture/"
    - "Capture runs on cursor-agent stdout ingest path before parseCursorEvent (D-01)"
    - "Committed repo fixtures exist for five priority tools with started/completed pairs and expectedName (D-04)"
  artifacts:
    - path: "cli/src/cursor/utils/cursorNdjsonCapture.ts"
      provides: "Dev-only NDJSON capture with redaction before disk write"
      exports: ["appendRedactedNdjsonLine", "getCapturePathForSession", "isNdjsonCaptureEnabled"]
    - path: "cli/src/cursor/utils/cursorNdjsonCapture.test.ts"
      provides: "Redaction and env-gate unit tests"
    - path: "cli/src/cursor/utils/fixtures/cursorToolCallNdjson.captured.ts"
      provides: "Redacted real-sample NDJSON fixtures for CI"
      contains: "CURSOR_TOOL_CALL_NDJSON_CAPTURED_FIXTURES"
  key_links:
    - from: "cli/src/cursor/cursorRemoteLauncher.ts"
      to: "cli/src/cursor/utils/cursorNdjsonCapture.ts"
      via: "rl.on('line') calls appendRedactedNdjsonLine before parseCursorEvent"
      pattern: "HAPI_CURSOR_NDJSON_CAPTURE"
---

<objective>
Implement dev-only NDJSON capture on the HAPI ingest path and commit redacted real-sample fixtures for the five priority Cursor tools.

Purpose: Lock exact *ToolCall keys and native arg shapes from production stream-json before allowlist/normalizer work (D-01–D-04, TOOL-AUDIT-01/02).
Output: `cursorNdjsonCapture.ts`, launcher hook, `cursorToolCallNdjson.captured.ts` with Task/Agent/Notebook/Skill/AskUserQuestion pairs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask/01.3-CONTEXT.md
@.planning/phases/01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask/01.3-RESEARCH.md
@.planning/phases/01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask/01.3-PATTERNS.md
@cli/src/cursor/cursorRemoteLauncher.ts
@cli/src/ui/logger.ts
@cli/src/configuration.ts
@cli/src/cursor/utils/fixtures/cursorToolCallNdjson.ts
</context>

<execution_preconditions>
- Run GitNexus `impact` on `runAgentProcess` / stdout line handler before editing (`repo: "hapi-cursor"`, `direction: "upstream"`).
- `agent` on PATH for human capture checkpoint (optional fallback: evidence-based synthetic fixtures documented in SUMMARY if capture blocked).
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NDJSON capture module and launcher hook</name>
  <files>cli/src/cursor/utils/cursorNdjsonCapture.ts, cli/src/cursor/utils/cursorNdjsonCapture.test.ts, cli/src/cursor/cursorRemoteLauncher.ts</files>
  <read_first>
    - cli/src/cursor/cursorRemoteLauncher.ts (runAgentProcess rl.on('line') ~195-201)
    - cli/src/ui/logger.ts (appendFileSync, HAPI_HOME, fail-silent pattern)
    - cli/src/configuration.ts (env flag patterns)
    - 01.3-CONTEXT.md D-01, D-02, D-03
  </read_first>
  <acceptance_criteria>
    - isNdjsonCaptureEnabled() true only when HAPI_CURSOR_NDJSON_CAPTURE is 1/true/yes (case-insensitive) per D-02
    - appendRedactedNdjsonLine writes under $HAPI_HOME/cursor-ndjson-capture/ or ~/.hapi/cursor-ndjson-capture/ when enabled
    - Redaction removes: Bearer/sk_api_/ghp_/xoxb- tokens, ~/.hapi and /home/* path segments, env secret-like key=value pairs
    - Capture hook runs on raw line before parseCursorEvent per D-01
    - When flag unset, zero disk writes and no perf impact on hot path
  </acceptance_criteria>
  <behavior>
    - isNdjsonCaptureEnabled() with env '1' returns true; unset returns false
    - redactNdjsonLine('{"token":"Bearer abc123"}') does not contain 'abc123'
    - redactNdjsonLine with /home/user/project/path redacts to placeholder path segment
    - appendRedactedNdjsonLine creates parent dir and appends newline-terminated line
    - appendRedactedNdjsonLine when disabled is no-op
  </behavior>
  <action>
    Create cli/src/cursor/utils/cursorNdjsonCapture.ts per D-01–D-03:
    - isNdjsonCaptureEnabled(): accept HAPI_CURSOR_NDJSON_CAPTURE in ['1','true','yes'] (lowercase trim).
    - captureBaseDir(): resolve HAPI_HOME (tilde expand) or join(homedir(), '.hapi'), then 'cursor-ndjson-capture'.
    - getCapturePathForSession(sessionKey: string): `${sessionKey}-${ISO-ish-safe-timestamp}.ndjsonl` under captureBaseDir(); mkdirSync recursive.
    - redactNdjsonLine(line: string): string — apply regex redaction for tokens/paths/secrets (planner discretion on exact rules); never throw; return original line if redaction fails.
    - appendRedactedNdjsonLine(capturePath, line): if !enabled return; try appendFileSync(redacted+'\n'); catch swallow unless DEBUG.

    Wire cursorRemoteLauncher.ts runAgentProcess: pass stable session key (cursorSessionId or 'no-session') into getCapturePathForSession once per process; in rl.on('line'), call appendRedactedNdjsonLine before parseCursorEvent per D-01.

    Co-locate cursorNdjsonCapture.test.ts with Vitest; mock fs or use temp dir under os.tmpdir().
  </action>
  <verify>
    <automated>cd cli && bunx vitest run src/cursor/utils/cursorNdjsonCapture.test.ts</automated>
  </verify>
  <done>Dev capture flag writes redacted NDJSON to ~/.hapi/cursor-ndjson-capture/ on ingest path only when enabled (D-01–D-03).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture live sessions for five priority tools</name>
  <files>~/.hapi/cursor-ndjson-capture/*.ndjsonl</files>
  <read_first>
    - 01.3-CONTEXT.md D-04, D-05, D-06
    - 01.3-VALIDATION.md Manual-Only Verifications (live capture session)
  </read_first>
  <acceptance_criteria>
    - At least one redacted capture file contains a started+completed pair for each: Task, Agent, Notebook (read or edit), Skill, AskUserQuestion
    - Notebook NDJSON key names recorded in capture notes (notebookReadToolCall vs notebookEditToolCall vs unified) for Plan 02 allowlist per D-06
    - No raw secrets in files copied into repo (human verifies redaction before commit)
  </acceptance_criteria>
  <action>
    Human runs HAPI/CLI sessions with HAPI_CURSOR_NDJSON_CAPTURE=1:
    1. Export HAPI_CURSOR_NDJSON_CAPTURE=1
    2. Start hapi agent session; trigger Cursor to use Task tool (subagent task), Agent/subagent launch, notebook read/edit, skill invocation, AskUserQuestion
    3. Confirm files appear under ~/.hapi/cursor-ndjson-capture/
    4. Copy redacted tool_call lines into workspace scratch for Task 3 (or attach paths in resume signal)

    If a tool cannot be triggered in one session, run additional short sessions until all five variants appear in capture files.
  </action>
  <how-to-verify>
    1. ls ~/.hapi/cursor-ndjson-capture/ — at least one .ndjsonl file
    2. rg 'tool_call' on capture files — find lines with *ToolCall keys for priority five
    3. Confirm no Bearer/ghp_/sk- substrings remain in lines to be committed
  </how-to-verify>
  <resume-signal>Captured — proceed with fixture commit (paste capture dir path or confirm files ready)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit redacted captured fixtures</name>
  <files>cli/src/cursor/utils/fixtures/cursorToolCallNdjson.captured.ts, cli/src/cursor/utils/cursorEventConverter.test.ts</files>
  <read_first>
    - cli/src/cursor/utils/fixtures/cursorToolCallNdjson.ts (CursorToolCallFixture type, toolCallEvent helper)
    - Capture files from Task 2 (human-provided paths)
    - 01.3-PATTERNS.md fixtures section
  </read_first>
  <acceptance_criteria>
    - cursorToolCallNdjson.captured.ts exports CURSOR_TOOL_CALL_NDJSON_CAPTURED_FIXTURES with entries for taskToolCall, agentToolCall or subagentToolCall, notebook variant(s), skillToolCall, askUserQuestionToolCall (keys match capture evidence per D-06)
    - Each entry has started, completed, callId, expectedName matching HAPI knownTools (Task, Agent, NotebookRead/NotebookEdit, Skill, AskUserQuestion) per D-05, D-07
    - All string paths/tokens in fixtures are synthetic/redacted (no real home dirs or API keys) per D-04
    - cursorEventConverter.test.ts has it.each over captured fixtures asserting parse+convert yields expectedName (tests may fail until Plan 02 — RED acceptable)
  </acceptance_criteria>
  <action>
    Create cli/src/cursor/utils/fixtures/cursorToolCallNdjson.captured.ts per D-04:
    - Reuse CursorToolCallFixture type from cursorToolCallNdjson.ts (import or duplicate export).
    - Build fixtures from Task 2 capture lines: extract minimal started/completed pair per native key; set expectedName per D-05/D-07 (Task and Agent separate; Notebook split if capture shows two keys).
    - If capture missing one variant: add COMMENT in file naming blocker and use minimal synthetic line matching capture shape for that key only (document in SUMMARY — do not invent keys contradicting capture).

    Add cursorEventConverter.test.ts describe('captured fixtures') with it.each(CURSOR_TOOL_CALL_NDJSON_CAPTURED_FIXTURES) asserting started line converts to tool_call with fixture.expectedName.

    Run scripts/check-no-cut-agents.sh — fixtures must not introduce forbidden agent literals.
  </action>
  <verify>
    <automated>cd cli && bunx vitest run src/cursor/utils/cursorEventConverter.test.ts -t "captured"</automated>
  </verify>
  <done>Redacted captured fixtures committed for CI regression (TOOL-AUDIT-02, D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cursor-agent stdout → local disk | Raw NDJSON may contain secrets; redaction before write (D-03) |
| capture files → git fixtures | Only redacted content committed (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3-01 | Information Disclosure | cursorNdjsonCapture | mitigate | Redact tokens/paths; dev flag only (D-02); fail-silent on write errors |
| T-01.3-02 | Tampering | NDJSON line reader | mitigate | Capture before parse; parseCursorEvent still ignores non-JSON lines |
| T-01.3-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan |

</threat_model>

<verification>
- cd cli && bunx vitest run src/cursor/utils/cursorNdjsonCapture.test.ts
- cd cli && bunx vitest run src/cursor/utils/cursorEventConverter.test.ts -t "captured"
- bash scripts/check-no-cut-agents.sh
</verification>

<success_criteria>
- TOOL-AUDIT-01 satisfied: capture module + hook + tests green
- TOOL-AUDIT-02 satisfied: captured fixtures file exists with five priority tool variants (or documented gap with synthetic evidence-shaped lines)
- D-01, D-02, D-03, D-04 implemented
</success_criteria>

<output>
Create `.planning/phases/01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask/01.3-01-SUMMARY.md` when done
</output>
