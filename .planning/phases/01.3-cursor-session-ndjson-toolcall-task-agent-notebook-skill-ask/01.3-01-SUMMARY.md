---
phase: 01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask
plan: 01
subsystem: cursor-ingest
tags: [ndjson, capture, fixtures, tool-call-mapping]

requires:
  - phase: 01.2
    provides: basic *ToolCall mapping + hub projection
provides:
  - Dev NDJSON capture on cursor-agent stdout ingest path
  - Redacted captured + synthetic fixture file for five priority tools
affects: [01.3-02, 01.3-03, 01.3-04]

tech-stack:
  added: []
  patterns:
    - "HAPI_CURSOR_NDJSON_CAPTURE=1 gates append-only redacted .ndjsonl under ~/.hapi/cursor-ndjson-capture/"
    - "Captured fixtures mix real wire (task, edit ipynb) + synthetic (agent, skill, ask, notebook read)"

key-files:
  created:
    - cli/src/cursor/utils/cursorNdjsonCapture.ts
    - cli/src/cursor/utils/cursorNdjsonCapture.test.ts
    - cli/src/cursor/utils/fixtures/cursorToolCallNdjson.captured.ts
  modified:
    - cli/src/cursor/cursorRemoteLauncher.ts
    - cli/src/cursor/utils/cursorEventConverter.test.ts

key-decisions:
  - "Capture dir: ~/.hapi/cursor-ndjson-capture/ session 13e5b3cd-2437-4395-a87a-53a8536b5b72"
  - "EditNotebook wire key is editToolCall on .ipynb — infer NotebookEdit in 01.3-02/03, not notebookEditToolCall"
  - "Zero wire for agent/subagent/skill/ask/notebookRead — synthetic fixtures with evidence-shaped args"
  - "taskToolCall args use subagentType (camelCase) on wire"

patterns-established:
  - "pendingMapping fixtures skip green name tests until Plan 01.3-02 allowlist/inference"

requirements-completed: [TOOL-AUDIT-01, TOOL-AUDIT-02]

duration: 45min
completed: 2026-05-26
---

# Phase 01.3 Plan 01 Summary

**Dev NDJSON capture on ingest plus redacted fixture pack documenting real taskToolCall and editToolCall (.ipynb) wire, with synthetics for tools not exposed in capture.**

## Capture audit (session 13e5b3cd-2437-4395-a87a-53a8536b5b72)

| Tool family | Wire key | Source |
|-------------|----------|--------|
| Task | `taskToolCall` | Captured (description, prompt, subagentType, model, agentId) |
| Notebook edit | `editToolCall` on `.ipynb` | Captured; result message mentions notebook cell index 0 |
| Agent | `agentToolCall` / `subagentToolCall` | **Zero wire** — synthetic fixture |
| Skill | `skillToolCall` | **Zero wire** — synthetic fixture |
| AskUserQuestion | `askUserQuestionToolCall` | **Zero wire** — synthetic fixture |
| Notebook read | `notebookReadToolCall` | **Zero wire** — synthetic fixture |

## Deferrals / follow-up

- **NotebookEdit:** Plan 01.3-02 must map `editToolCall` + `.ipynb` path + cell-update result → `NotebookEdit` (not plain `Edit`).
- **Agent:** Allowlist `subagentToolCall` → `Agent` (PascalCase yields `Subagent` today).
- **Normalizers:** `subagentType` camelCase on Task wire → canonical fields for Web.

## Task Commits

1. **Task 1: NDJSON capture module** — `866452b`, `0ec529d`
2. **Task 2: Human capture** — (checkpoint; no code commit)
3. **Task 3: Captured fixtures** — (this commit)

## Verification

- `cd cli && bunx vitest run src/cursor/utils/cursorNdjsonCapture.test.ts` — 12/12
- `cd cli && bunx vitest run src/cursor/utils/cursorEventConverter.test.ts -t "captured"` — 6/6
- `bash scripts/check-no-cut-agents.sh` — pass
