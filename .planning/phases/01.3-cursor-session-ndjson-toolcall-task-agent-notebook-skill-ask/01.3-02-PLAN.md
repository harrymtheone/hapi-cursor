---
phase: 01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask
plan: 02
type: execute
wave: 1
depends_on:
  - 01.3-01
files_modified:
  - shared/src/cursor/toolCallMapping.ts
  - shared/package.json
  - cli/src/cursor/utils/cursorToolCallMapping.ts
  - cli/src/cursor/utils/cursorToolCallMapping.test.ts
  - cli/src/cursor/utils/cursorEventConverter.ts
  - cli/src/cursor/utils/cursorEventConverter.test.ts
  - cli/src/cursor/utils/normalizeToolArgs/index.ts
  - cli/src/cursor/utils/normalizeToolArgs/normalizeTaskArgs.ts
  - cli/src/cursor/utils/normalizeToolArgs/normalizeAgentArgs.ts
  - cli/src/cursor/utils/normalizeToolArgs/normalizeNotebookReadArgs.ts
  - cli/src/cursor/utils/normalizeToolArgs/normalizeNotebookEditArgs.ts
  - cli/src/cursor/utils/normalizeToolArgs/normalizeSkillArgs.ts
  - cli/src/cursor/utils/normalizeToolArgs/normalizeAskUserQuestionArgs.ts
  - cli/src/cursor/utils/normalizeToolArgs/normalizeToolArgs.test.ts
autonomous: true
requirements:
  - TOOL-AUDIT-03
  - TOOL-AUDIT-04
must_haves:
  truths:
    - "taskToolCall and agentToolCall/subagentToolCall map to distinct HAPI names Task and Agent (D-07)"
    - "Notebook, Skill, AskUserQuestion allowlist entries match capture-derived NDJSON keys (D-06)"
    - "Unmapped *ToolCall keys log [cursor-ndjson] unmapped: {key} then PascalCase fallback (D-08)"
    - "Wire tool-call input uses canonical fields Web knownTools reads (description, prompt, notebook_path, skill, questions) per D-09–D-10"
  artifacts:
    - path: "shared/src/cursor/toolCallMapping.ts"
      provides: "Single allowlist + resolveHapiToolName for CLI and Hub"
      exports: ["CURSOR_TOOL_KEY_TO_HAPI_NAME", "findNativeToolCallVariant", "resolveHapiToolName"]
    - path: "cli/src/cursor/utils/normalizeToolArgs/index.ts"
      provides: "normalizeToolInputForName dispatch"
    - path: "cli/src/cursor/utils/cursorEventConverter.ts"
      provides: "extractToolInput calls normalizers after resolveHapiToolName"
  key_links:
    - from: "cli/src/cursor/utils/cursorEventConverter.ts"
      to: "@hapi/protocol/cursor"
      via: "resolveHapiToolName + normalizeToolInputForName"
      pattern: "normalizeToolInputForName"
    - from: "shared/package.json"
      to: "hub and cli consumers"
      via: "exports[\"./cursor\"]"
      pattern: "\"./cursor\""
---

<objective>
Move tool mapping to shared package, extend allowlist for five priority tools, add per-tool arg normalizers, and wire CLI conversion so Web receives canonical input shapes.

Purpose: CLI-first fix for empty ToolCard subtitles (D-05–D-12, D-18, TOOL-AUDIT-03/04).
Output: `@hapi/protocol/cursor` mapping module, normalizeToolArgs/*, green mapping/converter/captured fixture tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask/01.3-01-SUMMARY.md
@cli/src/cursor/utils/fixtures/cursorToolCallNdjson.captured.ts
@cli/src/cursor/utils/cursorToolCallMapping.ts
@cli/src/cursor/utils/cursorEventConverter.ts
@web/src/components/ToolCard/knownTools.tsx
@web/src/lib/toolInputUtils.ts
</context>

<execution_preconditions>
- Plan 01.3-01 complete (captured fixtures committed or documented keys in SUMMARY).
- Run GitNexus `impact` on `resolveHapiToolName`, `convertCursorEventToAgentMessage`, `extractToolInput` before editing.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared mapping module and allowlist extension</name>
  <files>shared/src/cursor/toolCallMapping.ts, shared/package.json, cli/src/cursor/utils/cursorToolCallMapping.ts, cli/src/cursor/utils/cursorToolCallMapping.test.ts</files>
  <read_first>
    - cli/src/cursor/utils/cursorToolCallMapping.ts (move verbatim)
    - cli/src/cursor/utils/fixtures/cursorToolCallNdjson.captured.ts (confirm native keys)
    - 01.3-CONTEXT.md D-05, D-06, D-07, D-08, D-18
    - shared/package.json exports pattern for ./messages
  </read_first>
  <acceptance_criteria>
    - shared/package.json exports "./cursor" → ./src/cursor/toolCallMapping.ts
    - cli/src/cursor/utils/cursorToolCallMapping.ts re-exports from @hapi/protocol/cursor (no duplicate logic)
    - Allowlist includes taskToolCall→Task, agentToolCall→Agent, subagentToolCall→Agent per D-07
    - Notebook keys match capture evidence (separate read/edit entries if capture shows two keys) per D-06
    - skillToolCall→Skill, askUserQuestionToolCall→AskUserQuestion (adjust keys to capture if different)
    - PREFERRED_NATIVE_TOOL_CALL_KEY_ORDER includes new keys in stable priority
    - resolveHapiToolName logs warn once per unmapped key via console or existing logger: `[cursor-ndjson] unmapped: ${key}` before PascalCase return per D-08
  </acceptance_criteria>
  <behavior>
    - CURSOR_TOOL_KEY_TO_HAPI_NAME.taskToolCall === 'Task'
    - CURSOR_TOOL_KEY_TO_HAPI_NAME.agentToolCall === 'Agent' and subagentToolCall === 'Agent'
    - resolveHapiToolName({ customWidgetToolCall: { args: {} } }) === 'CustomWidget' and warns unmapped
    - resolveHapiToolName({ taskToolCall: { args: {} } }) === 'Task' without unmapped warn
    - Existing readToolCall/grepToolCall mappings unchanged (regression)
  </behavior>
  <action>
    Move cli/src/cursor/utils/cursorToolCallMapping.ts to shared/src/cursor/toolCallMapping.ts (preserve exports). Add shared/package.json export `"./cursor": "./src/cursor/toolCallMapping.ts"`.

    Extend CURSOR_TOOL_KEY_TO_HAPI_NAME per D-05–D-07 using keys from 01.3-01 captured fixtures (not guessed pre-capture). Add PREFERRED_NATIVE_TOOL_CALL_KEY_ORDER entries for new keys after existing shell/grep entries.

    Implement D-08: when native key not in allowlist but pascalCaseFromToolCallKey succeeds, emit warn `[cursor-ndjson] unmapped: ${native.key}` (dedupe per key in module-level Set).

    Replace cli/src/cursor/utils/cursorToolCallMapping.ts with re-export from @hapi/protocol/cursor. Update cursorEventConverter.ts imports to @hapi/protocol/cursor.

    Extend cursorToolCallMapping.test.ts for five tools + unmapped warn spy test. Run hub typecheck import path only if hub touched in Plan 03 — here verify shared resolves: `cd shared && bun test` if tests added, else typecheck via root.
  </action>
  <verify>
    <automated>cd cli && bunx vitest run src/cursor/utils/cursorToolCallMapping.test.ts</automated>
  </verify>
  <done>Shared mapping is single source; five priority tools allowlisted; unmapped keys warn (D-05–D-08, D-18, TOOL-AUDIT-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-tool normalizers and converter wiring</name>
  <files>cli/src/cursor/utils/normalizeToolArgs/index.ts, cli/src/cursor/utils/normalizeToolArgs/normalizeTaskArgs.ts, cli/src/cursor/utils/normalizeToolArgs/normalizeAgentArgs.ts, cli/src/cursor/utils/normalizeToolArgs/normalizeNotebookReadArgs.ts, cli/src/cursor/utils/normalizeToolArgs/normalizeNotebookEditArgs.ts, cli/src/cursor/utils/normalizeToolArgs/normalizeSkillArgs.ts, cli/src/cursor/utils/normalizeToolArgs/normalizeAskUserQuestionArgs.ts, cli/src/cursor/utils/normalizeToolArgs/normalizeToolArgs.test.ts, cli/src/cursor/utils/cursorEventConverter.ts, cli/src/cursor/utils/cursorEventConverter.test.ts</files>
  <read_first>
    - web/src/components/ToolCard/knownTools.tsx (Task/Agent/Notebook/Skill field lists)
    - web/src/components/ToolCard/askUserQuestion.ts (questions[] shape)
    - cli/src/cursor/utils/cursorEventConverter.ts extractToolInput
    - 01.3-CONTEXT.md D-09, D-10, D-11, D-12
  </read_first>
  <acceptance_criteria>
    - normalizeToolInputForName dispatches by resolved HAPI name (not native key alone)
    - Task normalizer sets description and/or prompt from Cursor aliases (task_description, message, etc.)
    - Agent normalizer sets prompt and subagent_type; does not merge Task-specific description-only shape into Agent per D-07
    - NotebookRead sets notebook_path; NotebookEdit sets notebook_path + edit_mode when edit signals present
    - Skill sets skill string from capture aliases
    - AskUserQuestion preserves questions[] array; only reshape if capture differs from Web parser per D-11
    - extractToolInput returns normalized object for native *ToolCall variants
    - Captured fixture tests: each priority tool has non-empty canonical field (description|prompt|notebook_path|skill|questions length>0)
  </acceptance_criteria>
  <behavior>
    - normalizeTaskArgs({ task_description: 'x' }) includes description: 'x'
    - normalizeAgentArgs({ prompt: 'go', subagent_type: 'explore' }) preserves both keys
    - normalizeAskUserQuestionArgs({ questions: [{ question: 'Q?' }] }) preserves questions array
    - convert captured taskToolCall started → tool_call name Task with input containing description or prompt
    - convert captured askUserQuestionToolCall → name AskUserQuestion with questions array
  </behavior>
  <action>
    Create cli/src/cursor/utils/normalizeToolArgs/ per D-09–D-10:
    - index.ts: normalizeToolInputForName(hapiName, nativeKey, args) switch on hapiName
    - normalizeTaskArgs.ts: pickString aliases → description, name, team_name, prompt
    - normalizeAgentArgs.ts: prompt, subagent_type, description aliases
    - normalizeNotebookReadArgs.ts / normalizeNotebookEditArgs.ts: notebook_path from path/notebook_path; edit_mode/old_string for edit
    - normalizeSkillArgs.ts: skill field aliases
    - normalizeAskUserQuestionArgs.ts: preserve questions[] per D-11; minimal transform only if capture shows different envelope

    Update cursorEventConverter.ts extractToolInput: after findNativeToolCallVariant + resolveHapiToolName, return normalizeToolInputForName(name, native.key, native.variant.args ?? {}).

    Add normalizeToolArgs.test.ts table per tool. Extend cursorEventConverter.test.ts captured describe to assert canonical input fields (not just name).

    D-12: only add Web getInputStringAny aliases if normalizer cannot cover capture shape; prefer CLI-only.
  </action>
  <verify>
    <automated>cd cli && bunx vitest run src/cursor/utils/normalizeToolArgs/normalizeToolArgs.test.ts && bunx vitest run src/cursor/utils/cursorEventConverter.test.ts -t "captured"</automated>
  </verify>
  <done>Five priority tools convert with correct names and canonical wire input (TOOL-AUDIT-04, D-09–D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NDJSON args → wire input | Normalizers output plain objects; no eval (V5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3-03 | Tampering | resolveHapiToolName | mitigate | Allowlist-first; PascalCase only after warn (D-08) |
| T-01.3-04 | Tampering | normalizeToolInputForName | mitigate | Pass-through unknown tools unchanged; no name inference from args |
| T-01.3-SC | Tampering | npm/pip/cargo installs | accept | No new packages |

</threat_model>

<verification>
- cd cli && bunx vitest run src/cursor/utils/cursorToolCallMapping.test.ts src/cursor/utils/normalizeToolArgs/normalizeToolArgs.test.ts src/cursor/utils/cursorEventConverter.test.ts
- bun run typecheck
</verification>

<success_criteria>
- TOOL-AUDIT-03 and TOOL-AUDIT-04 automated tests green
- D-05 through D-12 and D-18 satisfied
- Captured fixture converter tests pass (names + canonical input)
</success_criteria>

<output>
Create `.planning/phases/01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask/01.3-02-SUMMARY.md` when done
</output>
