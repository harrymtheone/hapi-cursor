---
phase: 01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask
plan: 03
type: execute
wave: 2
depends_on:
  - 01.3-02
files_modified:
  - hub/src/sync/toolCallProjection.ts
  - hub/src/sync/toolCallProjection.test.ts
  - hub/src/sync/sessionModel.test.ts
autonomous: true
requirements:
  - TOOL-AUDIT-05
  - BUG-TOOL-01
must_haves:
  truths:
    - "Legacy Hub projections with name unknown upgrade to Task/Agent/Notebook/Skill/AskUserQuestion when stored input matches canonical post-normalize shapes (D-17)"
    - "inferToolNameFromPayload extended in same reconcile pass as 01.2-07 Bash/Read heuristics — no second reconcile refactor (D-19)"
    - "Lazy reconcile on first getMessagesPage per session per process unchanged (D-20)"
    - "Result-only page window returns non-placeholder tool name for advanced tools after reconcile"
  artifacts:
    - path: "hub/src/sync/toolCallProjection.ts"
      provides: "inferToolNameFromPayload advanced-tool branches"
      contains: "AskUserQuestion"
    - path: "hub/src/sync/toolCallProjection.test.ts"
      provides: "infer tests for five advanced tools"
    - path: "hub/src/sync/sessionModel.test.ts"
      provides: "Integration: unknown → Task/Agent after reconcile"
  key_links:
    - from: "hub/src/sync/toolCallProjection.ts"
      to: "mergeToolCallProjection"
      via: "resolveProjectionName calls inferToolNameFromPayload"
      pattern: "inferToolNameFromPayload"
    - from: "hub/src/sync/messageService.ts"
      to: "reconcileSessionToolCalls"
      via: "lazy trigger unchanged"
      pattern: "reconciledSessions"
---

<objective>
Extend Hub inferToolNameFromPayload for five advanced tools and prove legacy unknown rows heal on lazy reconcile — completing 01.2-07 carryover in one pass.

Purpose: Stored sessions ingested before CLI normalizers still show placeholders until Hub upgrades projections from canonical input shapes (D-17–D-20, TOOL-AUDIT-05, BUG-TOOL-01).
Output: Extended infer helper, hub unit + sessionModel integration tests; existing Bash/Read infer tests remain green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-07-PLAN.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-07-SUMMARY.md
@hub/src/sync/toolCallProjection.ts
@hub/src/sync/toolCallProjection.test.ts
@hub/src/sync/sessionModel.test.ts
@hub/src/sync/messageService.ts
@shared/src/cursor/toolCallMapping.ts
</context>

<execution_preconditions>
- Plan 01.3-02 complete (CLI emits canonical input for new ingests).
- Run GitNexus `impact` on `inferToolNameFromPayload`, `mergeToolCallProjection`, `reconcileSessionToolCalls` before editing.
- Do NOT add hub-start full scan or new reconcile triggers (D-20).
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend inferToolNameFromPayload for advanced tools</name>
  <files>hub/src/sync/toolCallProjection.ts, hub/src/sync/toolCallProjection.test.ts</files>
  <read_first>
    - hub/src/sync/toolCallProjection.ts inferToolNameFromPayload lines 105-134
    - 01.3-RESEARCH.md Pattern 3 canonical field table
    - 01.3-CONTEXT.md D-17, D-19
    - cli normalizeToolArgs output shapes from Plan 02
  </read_first>
  <acceptance_criteria>
    - questions[] non-empty array → AskUserQuestion
    - skill string field → Skill
    - notebook_path without edit signals → NotebookRead
    - notebook_path with edit_mode or old_string/new_string/edits → NotebookEdit
    - description or team_name → Task (before generic prompt-only Agent)
    - prompt + subagent_type → Agent
    - prompt only (no description/team_name) → Agent per RESEARCH ambiguous rule
    - Existing Bash/Grep/Read/Write/Edit/TodoWrite cases unchanged (01.2-07 regression)
    - infer returns null when only ambiguous empty object
    - mergeToolCallProjection still never overwrites non-placeholder names
  </acceptance_criteria>
  <behavior>
    - inferToolNameFromPayload({ questions: [{ question: 'Pick?' }] }, null) === 'AskUserQuestion'
    - inferToolNameFromPayload({ skill: 'gitnexus-guide' }, null) === 'Skill'
    - inferToolNameFromPayload({ notebook_path: '/nb.ipynb' }, null) === 'NotebookRead'
    - inferToolNameFromPayload({ notebook_path: '/nb.ipynb', edit_mode: 'replace' }, null) === 'NotebookEdit'
    - inferToolNameFromPayload({ description: 'Run tests' }, null) === 'Task'
    - inferToolNameFromPayload({ prompt: 'x', subagent_type: 'explore' }, null) === 'Agent'
    - inferToolNameFromPayload({ command: 'ls' }, null) === 'Bash' (regression)
    - mergeToolCallProjection start unknown + input { description: 'x' } → name Task
  </behavior>
  <action>
    Extend inferToolNameFromPayload in hub/src/sync/toolCallProjection.ts per D-17, D-19 — add branches AFTER existing 01.2-07 heuristics, using canonical wire fields (post-CLI-normalize), not NDJSON key parsing:

    Order within inObj block (avoid false positives):
    1. Keep command/pattern/path/todos existing logic untouched
    2. if Array.isArray(questions) && questions.length > 0 → AskUserQuestion
    3. if payloadString skill → Skill
    4. if payloadString notebook_path: if edit_mode or old_string in inObj or edits array → NotebookEdit; else NotebookRead
    5. if description or team_name → Task
    6. if prompt && subagent_type → Agent
    7. if prompt only → Agent

    Do NOT import cli/; optional import from @hapi/protocol/cursor only if needed for shared helpers — infer uses input shape only per D-18.

    Add toolCallProjection.test.ts cases under describe inferToolNameFromPayload for each advanced tool + merge unknown→Task/AskUserQuestion. Keep infer helper under ~40 new lines; total infer still under 80 lines added to 01.2-07 base or split helper if needed.

    D-20: no changes to messageService.ts reconcile trigger.
  </action>
  <verify>
    <automated>cd hub && bun test src/sync/toolCallProjection.test.ts -t "infer"</automated>
  </verify>
  <done>Hub heals legacy unknown rows for five advanced tools via existing reconcile path (TOOL-AUDIT-05, D-17, D-19).</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for advanced-tool result-only windows</name>
  <files>hub/src/sync/sessionModel.test.ts</files>
  <read_first>
    - hub/src/sync/sessionModel.test.ts existing toolCalls page enrichment tests
    - 01.2-07-PLAN.md Task 2 pattern (unknown + command → Bash)
    - 01.3-CONTEXT.md D-17
  </read_first>
  <acceptance_criteria>
    - Synthetic ingest: tool-call wire message name unknown with input { description: 'audit task' }; after reconcileSessionToolCalls + getMessagesPage, toolCalls[callId].name === 'Task'
    - unknown + questions[] → AskUserQuestion on result-only window
    - unknown + { prompt: 'go', subagent_type: 'explore' } → Agent
    - unknown + { notebook_path: '/x' } without edit signals → NotebookRead
    - unknown + { skill: 'gitnexus-exploring' } → Skill
    - Phase gate (plan-level verification block): bun run typecheck && bun run test passes
  </acceptance_criteria>
  <action>
    Extend hub/src/sync/sessionModel.test.ts per BUG-TOOL-01 / 01.2-07 gap pattern (table-driven or separate cases):

    1. Task: unknown + { description, prompt } → name Task on result-only window.
    2. AskUserQuestion: unknown + questions[] → AskUserQuestion.
    3. Agent: unknown + { prompt, subagent_type } without description → Agent.
    4. NotebookRead: unknown + { notebook_path } without edit_mode/old_string → NotebookRead.
    5. Skill: unknown + { skill } → Skill.

    Run hub tests only at task verify; full monorepo suite is plan-level verification.

    Do not mark human UAT pass — automated coverage only.
  </action>
  <verify>
    <automated>cd hub && bun test src/sync/sessionModel.test.ts</automated>
  </verify>
  <done>Result-only pagination path shows real advanced tool names for legacy and new ingests (BUG-TOOL-01 carryover closed for 01.3 scope).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub infer from stored payload | Must not overwrite explicit non-placeholder names (T-01.2-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3-05 | Tampering | inferToolNameFromPayload | mitigate | Return null when ambiguous; Task before prompt-only Agent; never downgrade names |
| T-01.3-06 | Information Disclosure | infer heuristics | accept | Operates on already-stored session data |
| T-01.3-SC | Tampering | npm/pip/cargo installs | accept | No new packages |

</threat_model>

<verification>
- cd hub && bun test src/sync/toolCallProjection.test.ts src/sync/sessionModel.test.ts
- bun run typecheck && bun run test
</verification>

<success_criteria>
- TOOL-AUDIT-05 and BUG-TOOL-01 integration tests green
- D-17, D-19, D-20 satisfied
- 01.2-07 Bash/Read infer regressions unchanged
</success_criteria>

<output>
Create `.planning/phases/01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask/01.3-03-SUMMARY.md` when done
</output>
