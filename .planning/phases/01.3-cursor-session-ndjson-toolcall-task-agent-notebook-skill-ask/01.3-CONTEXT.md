# Phase 01.3: Cursor 活动审计 — Context

**Gathered:** 2026-05-26
**Status:** Ready for planning

<domain>
## Phase Boundary

Audit real Cursor session NDJSON from the HAPI/CLI ingest path and complete the `*ToolCall` → HAPI `knownTools` mapping table so Web renders Task, Agent, Notebook, Skill, AskUserQuestion (and audit-discovered variants) with correct names and usable input — not generic "Tool" placeholders. Includes dev capture tooling, redacted fixture commits, per-tool args normalization, incremental Web presentation fixes, and legacy `unknown` projection backfill (接 Phase 01.2-07).

Out of scope: Skills session policy (Phase 2), MCP inventory/toggle (Phase 3), new ToolCard UI paradigms, AskUserQuestion interaction redesign beyond alias/mapping fixes.

</domain>

<decisions>
## Implementation Decisions

### NDJSON sample capture
- **D-01:** Capture from **HAPI/CLI runtime** — cursor-agent stdout NDJSON stream on the ingest path (not manual Cursor IDE file export as primary source).
- **D-02:** **Dev-only flag** `HAPI_CURSOR_NDJSON_CAPTURE=1` writes captures to `~/.hapi/cursor-ndjson-capture/`; no always-on production logging.
- **D-03:** Persist **full session NDJSON** with **redaction** (paths, tokens, secrets stripped) before disk write.
- **D-04:** Commit **redacted samples** into `cli/src/cursor/utils/fixtures/` alongside existing `cursorToolCallNdjson.ts` (CI-regression friendly).

### Mapping table coverage
- **D-05:** **Priority five** — Task, Agent, Notebook (Read/Edit), Skill, AskUserQuestion — plus any additional unmapped `*ToolCall` keys discovered during audit.
- **D-06:** **Notebook keys** — confirm real NDJSON key names from capture first (`notebookReadToolCall` / `notebookEditToolCall` vs single `notebookToolCall`); map accordingly after evidence.
- **D-07:** **Task vs Agent** — separate allowlist entries (e.g. `taskToolCall` → `Task`, `agentToolCall` / `subagentToolCall` → `Agent`); do not collapse to one HAPI name.
- **D-08:** **Unmapped keys after audit** — keep PascalCase fallback (`fooToolCall` → `Foo`) and add **warn log** `[cursor-ndjson] unmapped: {key}` for visibility.

### Args field normalization
- **D-09:** **CLI layer primary** — normalize Cursor native args to HAPI-canonical input shapes in `cursorEventConverter` / dedicated helpers; Web should not guess Cursor-specific field names.
- **D-10:** **Per-tool `normalizeXxxArgs()`** functions (testable, one per mapped variant) rather than blanket dual-write or Web-only aliases.
- **D-11:** **AskUserQuestion** — verify shape from capture first; default **preserve** Cursor `questions[]` if Web `AskUserQuestionView` already parses it.
- **D-12:** **Skill** — verify from capture; extend Web `getInputStringAny` aliases only if needed; minimal CLI changes.

### Web presentation depth
- **D-13:** **Incremental tiers** — eliminating "Tool" placeholder with correct **name + title/subtitle** is the hard requirement; dedicated result views only where clearly broken or missing.
- **D-14:** **Task/Agent Trace nesting** — verify whether child tool calls already arrive on HAPI wire with parent/child linkage; if not, **defer** nested Trace work to a follow-up (do not block mapping delivery).
- **D-15:** **AskUserQuestion footer** — keep existing `AskUserQuestionFooter`; ensure mapping + `isAskUserQuestionToolName` alias list covers Cursor-resolved names.
- **D-16:** **Agent results** — `GenericResultView` + markdown branch when result is text; no new `AgentResultView` in this phase.

### Legacy session backfill
- **D-17:** **New ingest must be correct**; additionally upgrade legacy Hub projections with `name: 'unknown'` via reconcile (衔接 01.2-07).
- **D-18:** **Shared mapping logic** — extract or share `cursorToolCallMapping` allowlist so Hub reconcile reuses the same rules as CLI (not duplicate heuristics).
- **D-19:** **01.3 承接 01.2-07 剩余** — legacy unknown recovery + five advanced tool mappings in one reconcile pass (avoid two reconcile refactors).
- **D-20:** **Lazy reconcile on read** — trigger on first `getMessagesPage` per session per Hub process (matches Phase 01.2 D-04 pattern); no hub-start full scan.

### Claude's Discretion
- Exact redaction rules for capture files.
- Notebook key split vs unified mapping after sample review.
- Whether mapping module lives in `shared/` vs Hub importing CLI utility (prefer single source in `@hapi/protocol` or shared if Hub cannot import `cli/`).
- Per-tool normalize function file layout.
- Web alias additions vs CLI-only normalization tradeoffs per tool after fixture review.
- Task/Agent wire nesting investigation outcome and deferral wording in plans.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Roadmap & debug context
- `.planning/ROADMAP.md` — Phase 01.3 goal, dependency on 01.2
- `.planning/debug/tool-call-placeholder-unknown-name.md` — Root-cause analysis motivating audit
- `.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md` — Hub projection, reconcile, ingest decisions
- `.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-06-SUMMARY.md` — Existing basic *ToolCall mapping + fixtures

### CLI mapping & conversion (primary change surface)
- `cli/src/cursor/utils/cursorToolCallMapping.ts` — Allowlist + PascalCase fallback (extend here)
- `cli/src/cursor/utils/cursorEventConverter.ts` — NDJSON → HAPI wire; add normalize hooks
- `cli/src/cursor/utils/fixtures/cursorToolCallNdjson.ts` — Existing synthetic fixtures; add redacted real samples
- `cli/src/cursor/utils/cursorToolCallMapping.test.ts` — Mapping unit tests
- `cli/src/cursor/utils/cursorEventConverter.test.ts` — Converter integration tests

### Hub projection & legacy recovery
- `hub/src/sync/toolCallProjection.ts` — Merge/upsert; extend reconcile name inference
- `hub/src/sync/toolCallProjection.test.ts` — Projection tests
- `hub/src/store/toolCalls.ts` — Durable `tool_calls` store
- `.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-07-PLAN.md` — Legacy unknown recovery (01.3 承接)

### Web presentation (consumer — verify, minimal change)
- `web/src/components/ToolCard/knownTools.tsx` — Task, Agent, NotebookRead/Edit, Skill, AskUserQuestion registry
- `web/src/components/ToolCard/views/_results.tsx` — Result view routing
- `web/src/components/ToolCard/trace.tsx` — Task Trace children (verify nesting)
- `web/src/components/ToolCard/askUserQuestion.ts` — Tool name alias detection
- `web/src/chat/reducerTimeline.ts` — Task nested tool-call handling (~118+)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `CURSOR_TOOL_KEY_TO_HAPI_NAME` + `resolveHapiToolName()` — Extend allowlist; PascalCase fallback already handles unmapped `*ToolCall` suffix keys.
- `CURSOR_TOOL_CALL_NDJSON_FIXTURES` — Pattern for started/completed NDJSON pairs + `expectedName`.
- Web `knownTools` entries for Task, Agent, NotebookRead, NotebookEdit, Skill, AskUserQuestion — Presentation exists; gap is CLI name + args shape.
- Hub `reconcileSessionToolCalls` + `inferToolNameFromPayload` (01.2-07) — Backfill hook for legacy rows.

### Established Patterns
- **Allowlist-first mapping** (01.2-06) — No guessing tool identity from args alone.
- **Hub canonical projection** (01.2) — CLI fix is upstream root cause; Hub reconcile heals stored history.
- **Co-located fixture tests** — RED/GREEN per NDJSON variant before Web UAT.

### Integration Points
- CLI: cursor-agent stdout → `parseCursorEvent` → `convertCursorEventToAgentMessage` → Hub ingest.
- Hub: message ingest upsert → `tool_calls` table → `getMessagesPage` enrichment → SSE.
- Web: projection map + reducer → `knownTools` title/subtitle → ToolCard / Trace / AskUserQuestion footer.

</code_context>

<specifics>
## Specific Ideas

- User chose HAPI runtime capture + dev flag + redacted full-session dumps.
- User deferred most "you decide" items to planner/researcher judgment with recommendations captured in D-04–D-20.
- Phase explicitly names Task / Agent / Notebook / Skill / AskUserQuestion — treat as minimum bar, not maximum scope.

</specifics>

<deferred>
## Deferred Ideas

- Task/Agent **nested Trace** if wire lacks parent/child linkage — follow-up quick task or sub-plan after capture audit.
- New dedicated **AgentResultView** — use Generic + markdown until proven insufficient.
- Always-on NDJSON logging in production — rejected (dev flag only).
- Manual Cursor IDE session file export as primary capture path — rejected (HAPI ingest path preferred).

</deferred>

---

*Phase: 01.3-cursor-session-ndjson-toolcall-task-agent-notebook-skill-ask*
*Context gathered: 2026-05-26 via discuss-phase (中文)*
