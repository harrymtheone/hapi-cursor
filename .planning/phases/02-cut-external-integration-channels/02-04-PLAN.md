---
phase: 02-cut-external-integration-channels
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - hub/src/serverchan/
  - hub/src/index.ts
  - hub/src/config/settings.ts
  - hub/src/config/serverSettings.ts
  - hub/src/configuration.ts
autonomous: true
requirements: [CUT-08]
user_setup: []

must_haves:
  truths:
    - "hub/src/serverchan/ directory does not exist"
    - "hub/src/index.ts no longer imports ServerChanChannel or pushes it into notificationChannels"
    - "All serverChan* fields removed from hub/src/config/{settings,serverSettings}.ts and hub/src/configuration.ts"
    - "SERVERCHAN_SENDKEY / SERVERCHAN_NOTIFICATION not read in hub/src/ (SC#4)"
    - "After this commit, notificationChannels array initializes to [PushNotificationChannel] only (length 1; D-22 verified)"
    - "bun typecheck && bun run test exit 0 after this commit (D-31)"
  artifacts:
    - path: "hub/src/serverchan"
      absent: true
    - path: "hub/src/index.ts"
      provides: "notificationChannels array contains exactly one channel constructor: PushNotificationChannel"
      contains: "PushNotificationChannel"
  key_links:
    - from: "hub/src/index.ts"
      to: "hub/src/notifications/notificationHub.ts"
      via: "notificationChannels array — ServerChan entry removed"
      pattern: "notificationChannels"
---

<objective>
Commit #4 of D-30: physically delete the ServerChan push channel per CUT-08. Remove the `hub/src/serverchan/` directory (channel + tests), the `ServerChanChannel` import + `notificationChannels.push(...)` block + any banner output in `hub/src/index.ts` (RESEARCH L484-486 region), and all `serverChan*` fields in `hub/src/config/{settings,serverSettings}.ts` + `hub/src/configuration.ts` per RESEARCH inventory. CLI residual handling (`cli/src/commands/notify.ts` ServerChan reference, if any) is OUT OF SCOPE for this commit per RESEARCH §"CLI Crosscut Inventory" (zero serverchan refs in `cli/src/` confirmed); commit #4 has no CLI side-effect.

Purpose: Fourth atomic commit in D-30; closes CUT-08.
Output: One commit `feat(phase-02): CUT-08 remove ServerChan channel`. `bun typecheck && bun run test` green at commit (D-31). After this commit, `notificationChannels` is `[PushNotificationChannel]` length 1 (D-22 confirmed).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cut-external-integration-channels/02-CONTEXT.md
@.planning/phases/02-cut-external-integration-channels/02-RESEARCH.md
@.planning/phases/02-cut-external-integration-channels/02-VALIDATION.md
@.planning/phases/02-cut-external-integration-channels/02-03-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete hub/src/serverchan/ + remove ServerChan registration in hub/src/index.ts</name>
  <files>hub/src/serverchan/, hub/src/index.ts</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-CONTEXT.md (D-22, D-23)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"D-22 Hypothesis Verification" (notificationChannels array post-collapse)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"NotificationChannels post-collapse (commit #4)" (target shape)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"CLI Crosscut Inventory" (no serverchan in cli/src/ — confirmed)
    - hub/src/serverchan/channel.ts
    - hub/src/serverchan/channel.test.ts
    - hub/src/index.ts (focus L199-219 channel array assembly + any banner lines)
  </read_first>
  <action>
    1. Delete entire directory `hub/src/serverchan/` (`channel.ts`, `channel.test.ts`, plus any other files).
    2. In `hub/src/index.ts`:
       - Remove `import { ServerChanChannel } from './serverchan/channel'` (or whatever the import path is — verify by reading).
       - Remove the `if (config.serverChanSendKey && config.serverChanNotification) { notificationChannels.push(new ServerChanChannel(config.serverChanSendKey, config.publicUrl)) }` block (RESEARCH L484-486).
       - Remove any startup banner output that prints ServerChan-related status (mirror of the Telegram bind-URL banner cleanup in commit #1).
    3. After this task, the `notificationChannels: NotificationChannel[]` array initializer is just `[ new PushNotificationChannel(pushService, sseManager, visibilityTracker, config.publicUrl) ]`. Verify by reading the post-edit file.
  </action>
  <verify>
    <automated>! test -e hub/src/serverchan && ! rg -ni 'serverchan|ServerChanChannel' hub/src/index.ts && cd hub && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `! test -e hub/src/serverchan`
    - `rg -ni 'serverchan|ServerChanChannel' hub/src/index.ts | wc -l` is 0
    - `rg -n "from '\.\/serverchan" hub/src/index.ts | wc -l` is 0
    - `rg -A 20 'notificationChannels:' hub/src/index.ts | rg -c 'PushNotificationChannel'` is 1 (channel array reduced to web push only — D-22 verified)
    - `cd hub && bun run typecheck` exits 0
  </acceptance_criteria>
  <done>ServerChan directory + import + registration gone. Channel array length 1.</done>
</task>

<task type="auto">
  <name>Task 2: Strip serverChan* fields from settings.ts / serverSettings.ts / configuration.ts; commit</name>
  <files>hub/src/config/settings.ts, hub/src/config/serverSettings.ts, hub/src/configuration.ts</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"D-32 Settings Field Inventory (concrete)" — table "Fields to delete in commit #4 (ServerChan)" (exact field+line)
    - .planning/phases/02-cut-external-integration-channels/02-CONTEXT.md (D-32 — no .passthrough() fallback)
    - hub/src/config/settings.ts
    - hub/src/config/serverSettings.ts
    - hub/src/configuration.ts
  </read_first>
  <action>
    1. `hub/src/config/settings.ts`: remove `serverChanSendKey?: string` and `serverChanNotification?: boolean` from the `Settings` interface (RESEARCH L17-18). Remove from any Zod schema in the file. Do NOT add `.passthrough()` (D-32).
    2. `hub/src/config/serverSettings.ts`: remove the following per RESEARCH inventory — `serverChanSendKey: string | null` (L18) and `serverChanNotification: boolean` (L19) interface fields, source-tracking keys (L31-32), defaulting blocks (L108-109 + L143-169), return-value keys (L242-243). Verify no other internal helper still references these.
    3. `hub/src/configuration.ts`: remove JSDoc env-var docs for `SERVERCHAN_SENDKEY` + `SERVERCHAN_NOTIFICATION` (L12-13). Remove `ConfigSources` keys `serverChanSendKey`, `serverChanNotification` (L38-39). Remove `Configuration` fields `serverChanSendKey`, `serverChanNotification` (L58-61). Remove constructor copies (L111-112).
    4. Per-commit gate (D-31): from repo root, `bun typecheck && bun run test`. Both must exit 0.
    5. Commit with `feat(phase-02): CUT-08 remove ServerChan channel` and a body listing: deleted dir + import + registration, settings fields removed (D-32), `notificationChannels` reduced to length 1 (D-22 confirmed). Refs: `D-22 D-23 D-30 commit #4 D-31 D-32`.
  </action>
  <verify>
    <automated>! rg -ni 'serverchan' hub/src/config/ hub/src/configuration.ts && ! rg -n 'SERVERCHAN_SENDKEY|SERVERCHAN_NOTIFICATION' hub/src/ && bun typecheck && bun run test && git log -1 --format=%s | grep -Eq 'feat\(phase-02\): CUT-08 remove ServerChan channel'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -ni 'serverchan' hub/src/config/settings.ts hub/src/config/serverSettings.ts hub/src/configuration.ts | wc -l` is 0
    - `rg -n 'SERVERCHAN_SENDKEY|SERVERCHAN_NOTIFICATION' hub/src/ | wc -l` is 0 (SC#4 serverchan-side)
    - `rg -ni 'serverchan' hub/src/ | wc -l` is 0
    - `bun typecheck` exits 0 (D-31, all packages)
    - `bun run test` exits 0 (D-31)
    - `git log -1 --format=%s` matches `feat(phase-02): CUT-08 remove ServerChan channel`
    - `git status --porcelain` empty
  </acceptance_criteria>
  <done>CUT-08 closed. ServerChan fully gone from hub config + index. CLI side has no serverchan refs (RESEARCH-confirmed); commit #5 owns Telegram CLI residuals only.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-1 | Information disclosure | Stale `SERVERCHAN_SENDKEY` env-var read after deletion | mitigate (medium) | Field deleted in Task 2; commit #5 (02-05) extends ripgrep guard to include `serverchan` keyword, catching any new regression. User-set env var becomes inert. |
| T-02-04-2 | Denial of service | Removing channel registration without removing implementation → import errors | mitigate (low) | Same commit removes both the import + the directory + the push call. Typecheck would fail otherwise. |
| T-02-04-3 | Tampering | Dead-code rehydration: `hub/src/serverchan/channel.ts` left on disk after `index.ts` stops importing it | mitigate (low) | Directory-level delete in Task 1 step 1; verified by `! test -e hub/src/serverchan`. |
| T-02-04-SC | Tampering | npm install of new packages | n/a | No installs; pure deletion. |
</threat_model>

<verification>
- `bun typecheck` green from repo root (D-31, all 4 packages)
- `bun run test` green from repo root (D-31)
- `! test -e hub/src/serverchan`
- `rg -ni 'serverchan' hub/src/ | wc -l` = 0
- `rg -n 'SERVERCHAN_SENDKEY|SERVERCHAN_NOTIFICATION' hub/src/` = 0 (SC#4 serverchan-side)
- `notificationChannels` array initializer contains exactly one channel constructor (PushNotificationChannel) — D-22 verification
- Single commit `feat(phase-02): CUT-08 remove ServerChan channel`
</verification>

<success_criteria>
- All two task `<acceptance_criteria>` blocks pass
- D-31 per-commit gate satisfied
- Phase 2 SC#1 serverchan-side, SC#3 (channel array no ServerChan ref), SC#4 (SERVERCHAN_* env-var reads gone) met for ServerChan keyword
</success_criteria>

<output>
Create `.planning/phases/02-cut-external-integration-channels/02-04-SUMMARY.md` listing:
- Files deleted (full paths)
- `notificationChannels` array length before/after this commit (expect 2 → 1)
- Settings fields removed (D-32 inventory subset for ServerChan)
- D-31 gate result (bun typecheck + bun run test exit codes)
- Hand-off note: ripgrep PATTERN extension + bun.lock regen + CLI residual cleanup pending in 02-05
</output>
