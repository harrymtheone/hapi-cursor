---
phase: 02-cut-external-integration-channels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hub/src/telegram/
  - hub/src/web/telegramInitData.ts
  - hub/src/web/routes/bind.ts
  - hub/src/web/routes/auth.ts
  - hub/src/web/server.ts
  - hub/src/web/routes/messages.ts
  - hub/src/sync/syncEngine.ts
  - hub/src/sync/messageService.ts
  - hub/src/sync/messageService.test.ts
  - hub/src/sync/rpcGateway.ts
  - hub/src/index.ts
  - hub/src/config/settings.ts
  - hub/src/config/serverSettings.ts
  - hub/src/configuration.ts
  - hub/package.json
autonomous: true
requirements: [CUT-06]
user_setup: []

must_haves:
  truths:
    - "hub/src/telegram/ directory does not exist"
    - "hub/src/web/telegramInitData.ts and hub/src/web/routes/bind.ts do not exist"
    - "/api/auth body schema is z.object({ accessToken: z.string() }) only — telegram initData branch removed (D-24)"
    - "hub/src/index.ts notificationChannels array no longer pushes HappyBot (Telegram channel)"
    - "hub/package.json declares no grammy dependency (SC#3)"
    - "TELEGRAM_BOT_TOKEN / TELEGRAM_NOTIFICATION not read in hub/src/ (SC#4)"
    - "bun typecheck && bun run test exit 0 after this commit (D-31)"
  artifacts:
    - path: "hub/src/telegram"
      provides: "Must NOT exist (entire dir deleted)"
      absent: true
    - path: "hub/src/web/telegramInitData.ts"
      absent: true
    - path: "hub/src/web/routes/bind.ts"
      absent: true
    - path: "hub/src/web/routes/auth.ts"
      provides: "Single-source access-token auth handler; body schema collapsed to z.object({ accessToken })"
      contains: "accessToken"
    - path: "hub/package.json"
      provides: "No grammy dep; description not Telegram-themed"
  key_links:
    - from: "hub/src/web/server.ts"
      to: "hub/src/web/routes/auth.ts"
      via: "createAuthRoutes registration (no longer takes Store param)"
      pattern: "createAuthRoutes"
    - from: "hub/src/index.ts"
      to: "hub/src/notifications/notificationHub.ts"
      via: "notificationChannels array — Telegram entry removed"
      pattern: "notificationChannels"
---

<objective>
Commit #1 of D-30: physically delete the Telegram bot stack on the hub side and converge `/api/auth` to a single access-token branch (D-24). Removes `hub/src/telegram/`, `hub/src/web/telegramInitData.ts`, `hub/src/web/routes/bind.ts`, telegram branch in `auth.ts`, HappyBot init + telegram channel push + bind URL banner in `hub/src/index.ts`, all `telegram*` fields in `hub/src/config/{settings,serverSettings}.ts` + `hub/src/configuration.ts`, the `grammy` dep in `hub/package.json`, and incidental `'telegram-bot'` literals / JSDoc / comments in `hub/src/sync/{syncEngine,messageService,rpcGateway,messageService.test}.ts` + `hub/src/web/routes/messages.ts` (per RESEARCH §"Test Fixture Stripping Plan").

Purpose: First atomic commit in the D-30 sequence; satisfies hub-side surface of CUT-06.
Output: One commit `feat(phase-02): CUT-06 remove Telegram bot (hub-side)`. `bun typecheck && bun run test` green at commit (D-31). Web-side telegram code stays runtime-dead but typed-valid until 02-02 (per RESEARCH §"Commit Dependency Ordering").
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cut-external-integration-channels/02-CONTEXT.md
@.planning/phases/02-cut-external-integration-channels/02-RESEARCH.md
@.planning/phases/02-cut-external-integration-channels/02-VALIDATION.md
@.planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Telegram subsystem files + bind/auth route surgery</name>
  <files>hub/src/telegram/, hub/src/web/telegramInitData.ts, hub/src/web/routes/bind.ts, hub/src/web/routes/auth.ts, hub/src/web/server.ts</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-CONTEXT.md (D-19, D-22, D-24, D-26)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"D-24 Auth Schema Convergence" (post-collapse code)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"D-26 Hypothesis Verification" (socket no-op)
    - hub/src/web/routes/auth.ts (current state)
    - hub/src/web/server.ts (route registration sites)
    - hub/src/web/routes/bind.ts (entire file — to be deleted)
    - hub/src/web/telegramInitData.ts (entire file — to be deleted)
  </read_first>
  <action>
    1. Delete entire directory `hub/src/telegram/` (`bot.ts`, `bot.test.ts`, `callbacks.ts`, `renderer.ts`, `sessionView.ts`, plus any other files).
    2. Delete `hub/src/web/telegramInitData.ts` (109-line HMAC validator).
    3. Delete `hub/src/web/routes/bind.ts` (70-line bind route).
    4. In `hub/src/web/routes/auth.ts`:
       - Remove `import { validateTelegramInitData } from '../telegramInitData'` and the `import type { Store } from '../../store'` (no longer used after telegram branch removal — verify no other use within file).
       - Remove `telegramAuthSchema` const and replace `authBodySchema = z.union([telegramAuthSchema, accessTokenAuthSchema])` with `authBodySchema = z.object({ accessToken: z.string() })`.
       - Drop the `if ('accessToken' in parsed.data)` discriminator and the entire `else { /* telegram */ }` branch. The handler body becomes the access-token-only flow shown in RESEARCH §"D-24 Auth Schema Convergence".
       - Remove `store` parameter from `createAuthRoutes(store: Store)` — function signature becomes parameter-less (or whatever remains needed).
    5. In `hub/src/web/server.ts`:
       - Drop `createBindRoutes` import + its `app.route('/bind', createBindRoutes(...))` registration line.
       - Update `createAuthRoutes` call site to match the new (no-`store`) signature.
    6. `hub/src/socket/server.ts`: per D-26 RESEARCH verification, no telegram branch exists — confirm via `rg -n 'telegram' hub/src/socket/` returning zero. If any hit appears, delete it; if zero, no edit.
  </action>
  <verify>
    <automated>! test -e hub/src/telegram && ! test -f hub/src/web/telegramInitData.ts && ! test -f hub/src/web/routes/bind.ts && ! rg -n 'telegram|telegramInitData|TelegramAuth|initData' hub/src/web/routes/auth.ts && ! rg -n 'createBindRoutes|telegramInitData' hub/src/web/server.ts && rg -nq 'authBodySchema' hub/src/web/routes/auth.ts && cd hub && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `! test -e hub/src/telegram`
    - `! test -f hub/src/web/telegramInitData.ts`
    - `! test -f hub/src/web/routes/bind.ts`
    - `rg -c '^(?!#)' hub/src/web/routes/auth.ts | grep -q .` — file still exists with code
    - `rg -n 'telegram|initData' hub/src/web/routes/auth.ts | wc -l` is 0
    - `rg -n "z\.union" hub/src/web/routes/auth.ts | wc -l` is 0 (schema collapsed)
    - `rg -nq "z\.object\(\{\s*accessToken" hub/src/web/routes/auth.ts`
    - `! rg -n 'createBindRoutes|telegramInitData' hub/src/web/server.ts`
    - `! rg -n 'telegram' hub/src/socket/`
    - `cd hub && bun run typecheck` exits 0
  </acceptance_criteria>
  <done>Telegram bot directory + initData validator + bind route physically gone; `/api/auth` collapsed to single access-token schema; server.ts no longer registers bind. Hub typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove HappyBot init + Telegram channel push + bind URL banner from hub/src/index.ts</name>
  <files>hub/src/index.ts, hub/src/notifications/notificationHub.ts, hub/src/notifications/notificationTypes.ts</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"D-22 Hypothesis Verification" (channel array post-collapse)
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"NotificationChannels post-collapse" code example
    - .planning/phases/02-cut-external-integration-channels/02-CONTEXT.md (D-22, D-23, Discretion line on banner cleanup)
    - hub/src/index.ts (focus L199-219 channel assembly + startup banner region)
    - hub/src/notifications/notificationHub.ts (channel iteration; should be untouched per D-22/D-23 — verify)
    - hub/src/notifications/notificationTypes.ts (NotificationChannel interface preserved)
  </read_first>
  <action>
    1. In `hub/src/index.ts`:
       - Remove `HappyBot` import (from `./telegram/bot`) and any `import { ... } from './telegram/...'`.
       - Remove the `if (config.telegramEnabled && config.telegramBotToken) { happyBot = new HappyBot({...}); if (config.telegramNotification) notificationChannels.push(happyBot) }` block (RESEARCH L487-493 region).
       - Remove the `let happyBot: HappyBot | null = null` declaration (or whatever the variable name is) and any subsequent `happyBot.start() / happyBot.stop()` lifecycle calls.
       - Remove startup banner output that prints the Telegram bind URL (the QR / "Open the Telegram Mini App at https://..." style line — Discretion item RESEARCH "yes, clean per zero-tolerance"). Keep banner lines that pertain to web push / public URL.
       - The `notificationChannels` array initial value stays `[ new PushNotificationChannel(...) ]`. After this task, only the `if (config.serverChanSendKey ...)` push remains — that one is removed in 02-04 (CUT-08).
    2. `hub/src/notifications/notificationHub.ts`: per D-22 + RESEARCH, this file has zero telegram refs — verify with `rg -n 'telegram' hub/src/notifications/notificationHub.ts | wc -l == 0`. If non-zero, drop the offending lines (the `NotificationChannel` interface itself stays per D-22).
    3. `hub/src/notifications/notificationTypes.ts`: same verification; if any telegram-tagged type field exists, drop it. Interface contract (the four optional methods) stays.
  </action>
  <verify>
    <automated>! rg -n 'HappyBot|telegram' hub/src/index.ts && ! rg -n 'telegram' hub/src/notifications/ && cd hub && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'HappyBot|telegram' hub/src/index.ts | wc -l` is 0
    - `rg -n "from '\.\/telegram" hub/src/index.ts | wc -l` is 0
    - `rg -n 'telegram' hub/src/notifications/notificationHub.ts | wc -l` is 0
    - `rg -n 'telegram' hub/src/notifications/notificationTypes.ts | wc -l` is 0
    - `rg -nq 'PushNotificationChannel' hub/src/index.ts` (preserved)
    - `cd hub && bun run typecheck` exits 0
  </acceptance_criteria>
  <done>HappyBot init + Telegram channel registration + bind URL banner removed from hub startup. NotificationChannel interface preserved (Phase 8 territory).</done>
</task>

<task type="auto">
  <name>Task 3: Strip TELEGRAM_* fields from settings.ts / serverSettings.ts / configuration.ts; remove grammy dep</name>
  <files>hub/src/config/settings.ts, hub/src/config/serverSettings.ts, hub/src/configuration.ts, hub/package.json</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"D-32 Settings Field Inventory (concrete)" — table "Fields to delete in commit #1 (Telegram)" (exact field+line)
    - .planning/phases/02-cut-external-integration-channels/02-CONTEXT.md (D-32 — no `.passthrough()` fallback; old settings.json fails fast)
    - hub/src/config/settings.ts
    - hub/src/config/serverSettings.ts
    - hub/src/configuration.ts
    - hub/package.json
  </read_first>
  <action>
    1. `hub/src/config/settings.ts`: remove fields `telegramBotToken?: string` and `telegramNotification?: boolean` from the `Settings` interface (RESEARCH lines 15-16). Remove from any Zod schema in the same file if present. Do NOT add `.passthrough()` (D-32).
    2. `hub/src/config/serverSettings.ts`: remove the following per RESEARCH inventory — `telegramBotToken` and `telegramNotification` interface fields (L16-17), source-tracking keys (L29-30), defaulting blocks (L106 + L116-127, L107 + L130-141), return-value object keys (L240-241). Verify no other internal helper still references these.
    3. `hub/src/configuration.ts`: remove JSDoc env-var docs lines for `TELEGRAM_BOT_TOKEN` + `TELEGRAM_NOTIFICATION` (L10-11) and the Mini App reference in `HAPI_PUBLIC_URL` JSDoc (L16) — rewrite the public-URL doc to reference web PWA only. Remove `ConfigSources` keys `telegramBotToken`, `telegramNotification` (L36-37). Remove `Configuration` fields `telegramBotToken`, `telegramEnabled`, `telegramNotification` (L49-55). Remove constructor copies (L108-110).
    4. `hub/package.json`: remove `"grammy": "^1.38.4"` from `dependencies` (L20). Rewrite `"description"` (L5) — drop "Telegram Bot client for HAPI - control sessions" and replace with a Cursor-only one-line description (e.g. `"Hub server for HAPI Cursor agent control"`).
    5. Do NOT run `bun install` here — lockfile regen happens in commit #5 (02-05) per D-30.
  </action>
  <verify>
    <automated>! rg -ni 'telegram' hub/src/config/ hub/src/configuration.ts && ! rg -n 'grammy' hub/package.json && cd hub && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -ni 'telegram' hub/src/config/settings.ts hub/src/config/serverSettings.ts hub/src/configuration.ts | wc -l` is 0
    - `rg -n '"grammy"' hub/package.json | wc -l` is 0
    - `rg -ni 'telegram' hub/package.json | wc -l` is 0 (description rewritten)
    - `rg -n 'TELEGRAM_BOT_TOKEN|TELEGRAM_NOTIFICATION' hub/src/ | wc -l` is 0 (SC#4 partial)
    - `cd hub && bun run typecheck` exits 0
  </acceptance_criteria>
  <done>All `telegram*` settings fields gone; `grammy` removed from `hub/package.json`; description Cursor-only.</done>
</task>

<task type="auto">
  <name>Task 4: Cleanse incidental telegram literals in hub/src/sync/* + hub/src/web/routes/messages.ts; commit</name>
  <files>hub/src/sync/syncEngine.ts, hub/src/sync/messageService.ts, hub/src/sync/messageService.test.ts, hub/src/sync/rpcGateway.ts, hub/src/web/routes/messages.ts</files>
  <read_first>
    - .planning/phases/02-cut-external-integration-channels/02-RESEARCH.md §"Test Fixture Stripping Plan" (table with exact lines + dispositions)
    - .planning/phases/02-cut-external-integration-channels/02-CONTEXT.md (D-27 zero-tolerance includes comments)
    - hub/src/sync/syncEngine.ts (focus L2 file header JSDoc + L301 `sentFrom` union)
    - hub/src/sync/messageService.ts (focus L351 `sentFrom` union + L355 comment)
    - hub/src/sync/messageService.test.ts (focus L766 comment)
    - hub/src/sync/rpcGateway.ts (focus L95 'User aborted via Telegram Bot' string)
    - hub/src/web/routes/messages.ts (focus L30 comment)
  </read_first>
  <action>
    1. `hub/src/sync/syncEngine.ts` L2 JSDoc: rewrite `Sync Engine for HAPI Telegram Bot (Direct Connect)` → `Sync Engine for HAPI hub` (or similar Cursor/web-only wording). L301 `sentFrom?: 'telegram-bot' | 'webapp'` → collapse to `sentFrom?: 'webapp'`. Verify no consumer relies on the `'telegram-bot'` literal value via `rg -n "'telegram-bot'" hub/src/ web/src/ shared/src/ cli/src/` — expect 1-2 hits in `messageService.ts` only (handled below). If a deeper consumer surfaces, drop the field entirely.
    2. `hub/src/sync/messageService.ts` L351: same `sentFrom` union collapse to `'webapp'`. L355 comment `// Defence-in-depth invariant for non-REST callers (Telegram bot, MCP,` → drop "Telegram bot, " (becomes `... non-REST callers (MCP, ...)`).
    3. `hub/src/sync/messageService.test.ts` L766 comment `// Zod layer, but non-REST callers (Telegram bot, MCP, internal) reach` → drop "Telegram bot, " (becomes `... (MCP, internal) reach`). Also verify no test asserts on the `'telegram-bot'` literal — `rg -n "'telegram-bot'" hub/src/sync/messageService.test.ts` should return 0.
    4. `hub/src/sync/rpcGateway.ts` L95: change string `'User aborted via Telegram Bot'` → `'User aborted via webapp'`.
    5. `hub/src/web/routes/messages.ts` L30 comment `// Cap scheduledAt at 7 days from now to prevent zombie rows.  REST/Telegram/` → drop "Telegram/" (becomes `... REST/...`).
    6. **Per-commit gate (D-31):** From repo root: `bun typecheck && bun run test`. Both must exit 0. (Note: ripgrep guard PATTERN extension does NOT happen here per RESEARCH §"Commit Dependency Ordering" — the existing Phase-1 PATTERN `\b(claude|codex|gemini|opencode)\b` does not match `telegram`, so guard stays green.)
    7. Commit with `feat(phase-02): CUT-06 remove Telegram bot (hub-side)` and a body listing: deleted dirs/files, `/api/auth` schema collapse (D-24), `notificationChannels` Telegram entry removal, settings fields removed (D-32), `grammy` dep removed, sync-layer comment/string cleanups, D-26 socket no-op confirmation. Refs: `D-22 D-24 D-26 D-30 commit #1 D-31 D-32`.
  </action>
  <verify>
    <automated>! rg -ni 'telegram' hub/src/sync/ hub/src/web/routes/messages.ts && ! rg -n "'telegram-bot'" hub/src/ && bun typecheck && bun run test && git log -1 --format=%s | grep -Eq 'feat\(phase-02\): CUT-06 remove Telegram bot \(hub-side\)'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -ni 'telegram' hub/src/sync/ | wc -l` is 0
    - `rg -ni 'telegram' hub/src/web/routes/messages.ts | wc -l` is 0
    - `rg -n "'telegram-bot'" hub/src/ shared/src/ cli/src/ web/src/ | wc -l` is 0
    - `bun typecheck` exits 0 (D-31 hub+cli+web+shared)
    - `bun run test` exits 0 (D-31, includes existing access-token positive case per SC#5)
    - `git log -1 --format=%s` matches `feat(phase-02): CUT-06 remove Telegram bot (hub-side)`
    - `git status --porcelain` empty
  </acceptance_criteria>
  <done>Hub side fully Telegram-free except for residuals in CLI (commit #5). Commit landed; D-31 per-commit gate satisfied.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-1 | Information disclosure | Stale `TELEGRAM_BOT_TOKEN` env-var read after deletion | mitigate (medium) | Field + JSDoc deleted in Task 3; commit #5 (02-05) extends ripgrep guard to enforce zero hits going forward. CLI `SENSITIVE_ENV_KEYS` entry still redacts the token in terminal sessions until commit #5 (02-05) drops it. |
| T-02-01-2 | Tampering / Auth schema drift | `/api/auth` body schema rejects formerly-valid `{ initData }` shape; web client may still send old shape until 02-02 | accept (medium) | Per D-25, negative-case auth coverage deferred to Phase 11 (REFT-03). Phase 2 commit #1 only guarantees access-token positive case stays green; Web-side telegram code becomes runtime-dead-but-typed-valid until 02-02 (per RESEARCH §"Commit Dependency Ordering" — per-package typecheck isolation makes this safe). |
| T-02-01-3 | Tampering | Dead-code rehydration: orphaned import paths to `./telegram/*` could let a future dev re-wire | mitigate (low) | Directory-level delete (not just import removal) per Task 1. Verify with `! test -e hub/src/telegram`. CI ripgrep guard extension in 02-05 closes the regression window. |
| T-02-01-4 | Denial of service | Removing channel registration without removing implementation → runtime crash | mitigate (low) | Same commit removes `HappyBot` import + `notificationChannels.push(happyBot)` + the `hub/src/telegram/` dir together (Task 1+2). Typecheck would fail otherwise. |
| T-02-01-SC | Tampering | npm install of new packages | n/a | No installs; pure deletion. Package legitimacy gate not applicable per RESEARCH §"Package Legitimacy Audit". |
</threat_model>

<verification>
- `bun typecheck` green from repo root (D-31, all four packages)
- `bun run test` green from repo root (D-31; covers SC#5 access-token positive case)
- File-existence assertions per `<acceptance_criteria>` of each task
- `rg -ni 'telegram' hub/src/` = 0 lines (commit #1 hub-side scope; CLI residuals owned by commit #5)
- `rg -n 'grammy' hub/package.json` = 0 (SC#3 hub-side partial)
- `rg -n 'TELEGRAM_BOT_TOKEN|TELEGRAM_NOTIFICATION' hub/src/` = 0 (SC#4 hub-side partial)
- Single commit `feat(phase-02): CUT-06 remove Telegram bot (hub-side)`
</verification>

<success_criteria>
- All four task `<acceptance_criteria>` blocks pass
- D-31 per-commit gate satisfied (bun typecheck + bun run test exit 0 standalone on this commit)
- Phase 2 SC#1 hub-side, SC#3 hub-side (grammy), SC#4 hub-side (TELEGRAM_*), SC#5 (access-token-only schema) are partially / fully met by this commit
- Web-side telegram references intentionally remain — addressed by 02-02
</success_criteria>

<output>
Create `.planning/phases/02-cut-external-integration-channels/02-01-SUMMARY.md` listing:
- Files deleted (full paths)
- `/api/auth` schema before/after diff highlight
- `notificationChannels` array length before/after this commit
- D-26 socket no-op confirmation (rg result)
- D-31 gate result (bun typecheck + bun run test exit codes)
- Hand-off note: web telegram refs still present; addressed by 02-02
</output>
