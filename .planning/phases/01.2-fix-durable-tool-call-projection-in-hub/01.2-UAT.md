---
status: diagnosed
phase: 01.2-fix-durable-tool-call-projection-in-hub
source:
  - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-01-SUMMARY.md
  - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-02-SUMMARY.md
  - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-03-SUMMARY.md
  - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-04-SUMMARY.md
  - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-05-SUMMARY.md
  - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-VERIFICATION.md
started: 2026-05-26T00:00:00Z
updated: 2026-05-26T12:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: 停止正在运行的 Hub/CLI 服务，清除临时状态（如有 schema v12 升级需求则按 hub/README 重建 DB）。从全新启动 Hub + CLI + PWA。Hub 正常启动无报错，SQLite schema v12 加载成功，PWA 可访问，会话列表/API 返回正常数据。
result: pass

### 2. Live pagination after long session
expected: 在 PWA 中打开一个已执行多次 tool call 的长会话，滚动到较旧的分页区域（tool-call 起始消息已不在可见窗口，仅显示 tool result）。刷新页面后，检查 tool activity 卡片是否显示真实工具名和输入（如 Bash、Read），而非通用 "Tool" 占位符。
result: issue
reported: "除了readfile，其他的toolcall还是和以前一样是占位符，也就是标题为"tool"，input和result都是{}"
severity: major

### 3. SSE reconnect mid-run
expected: 在会话运行中触发 tool call，然后通过 Tailscale 断开并重连网络（或模拟 SSE 断连）。重连后，进行中及最近完成的 tool activity 卡片应保留正确的工具名、输入和状态，不应退化为 "Tool" 占位符。
result: issue
reported: "除了readfile，其他的toolcall还是和以前一样是占位符，也就是标题为"tool"，input和result都是{}"
severity: major

## Summary

total: 3
passed: 1
issues: 2
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "分页/刷新后，result-only 窗口中的 tool activity 卡片应显示真实工具名和输入，而非 'Tool' 占位符"
  status: failed
  reason: "User reported: 除了readfile，其他的toolcall还是和以前一样是占位符，也就是标题为"tool"，input和result都是{}"
  severity: major
  test: 2
  root_cause: "cursorEventConverter.ts 仅解析 readToolCall/writeToolCall/function，其余 Cursor *ToolCall 变体（grepToolCall、editToolCall、shellToolCall 等）返回 name='unknown'，Hub 投影忠实持久化后 Web 将其视为占位符"
  artifacts:
    - path: "cli/src/cursor/utils/cursorEventConverter.ts"
      issue: "extractToolName/extractToolInput/extractToolResult 缺少 Cursor 原生 *ToolCall 分支"
    - path: "web/src/chat/reducerTools.ts"
      issue: "isPlaceholderToolName 将 'unknown' 视为占位符，导致显示 'Tool'"
  missing:
    - "扩展 cursorEventConverter 通用 *ToolCall 解析与 knownTools 名称映射"
    - "为各 Cursor tool 类型添加 NDJSON fixture 测试"
    - "（可选）Hub reconcile 从 result payload 恢复历史 unknown 投影"
  debug_session: ".planning/debug/tool-call-placeholder-unknown-name.md"

- truth: "SSE 重连后，tool activity 卡片应保留正确的工具名、输入和状态，不应退化为 'Tool' 占位符"
  status: failed
  reason: "User reported: 除了readfile，其他的toolcall还是和以前一样是占位符，也就是标题为"tool"，input和result都是{}"
  severity: major
  test: 3
  root_cause: "同测试 2 — 根因在 CLI 转换层，非 SSE/投影传输；重连后从 Hub SQLite 加载的投影 name 仍为 'unknown'"
  artifacts:
    - path: "cli/src/cursor/utils/cursorEventConverter.ts"
      issue: "同上"
  missing:
    - "同上"
  debug_session: ".planning/debug/tool-call-placeholder-unknown-name.md"
