---
phase: 01.2-fix-durable-tool-call-projection-in-hub
verified: 2026-05-26T14:30:00Z
status: passed
score: 15/15 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: passed
  previous_score: 7/7
  gaps_closed:
    - "CLI cursorEventConverter emitted name='unknown' for grepToolCall/shellToolCall/editToolCall/globToolCall/lsToolCall/todoToolCall — fixed in 01.2-06 via cursorToolCallMapping + generic extractors"
    - "Hub legacy rows with name='unknown' not upgraded on reconcile — fixed in 01.2-07 via inferToolNameFromPayload in mergeToolCallProjection"
    - "check-no-cut-agents.sh guard failure (phase 01.1 cursorModelFamilies) — guard now passes (exit 0)"
  gaps_remaining: []
  regressions: []
  human_uat_completed: 2026-05-26T14:30:00Z
human_verification:
  - test: "Live pagination after long session (UAT test 2 re-run)"
    expected: "Scroll to older tool results in PWA; tool activity cards show real tool names and inputs (Bash, Grep, Edit, etc.) — not generic 'Tool' with empty {} — after reload and pagination past result-only window"
    result: pass
    verified: 2026-05-26T14:30:00Z
  - test: "SSE reconnect mid-run (UAT test 3 re-run)"
    expected: "Disconnect/reconnect Tailscale during active session; in-flight and recent tool cards retain correct identity after reconnect"
    result: pass
    verified: 2026-05-26T14:30:00Z
---

# Phase 01.2: Fix Durable Tool Call Projection in Hub — Verification Report

**Phase Goal:** Fix durable tool call projection in Hub (BUG-TOOL-01) — real tool names in pagination/reload/SSE paths  
**Verified:** 2026-05-26T09:10:00Z  
**Status:** passed — 15/15 must-haves verified (human UAT tests 2 & 3 re-run passed 2026-05-26)  
**Re-verification:** Yes — after gap-closure plans 01.2-06 and 01.2-07

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Protocol: `ToolCallProjectionSchema`, `MessagesResponse.toolCalls`, `tool-call-projection-updated` SSE arm | ✓ VERIFIED | `shared/src/schemas.ts` (280–296, 480–484); `shared/src/responses.ts:16`; schema tests pass |
| 2 | Hub SQLite v12 `tool_calls` table with upsert/read | ✓ VERIFIED | `hub/src/store/index.ts` SCHEMA_VERSION=12; `hub/src/store/toolCalls.ts`; 41 projection store tests pass |
| 3 | Hub merge/extract converges out-of-order start/result | ✓ VERIFIED | `hub/src/sync/toolCallProjection.ts`; 29+ unit tests in `toolCallProjection.test.ts` |
| 4 | Ingest upserts projection; SSE `tool-call-projection-updated` before `message-received` | ✓ VERIFIED | `hub/src/socket/handlers/cli/sessionHandlers.ts:135–148` |
| 5 | `getMessagesPage` returns page-scoped `toolCalls` after reconcile gate | ✓ VERIFIED | `hub/src/sync/messageService.ts`; SPEC-5 test `call-ro-1` → `CursorBash` passes |
| 6 | Web merges per-page `toolCalls` and patches from SSE | ✓ VERIFIED | `web/src/lib/toolProjectionStore.ts`; `messageWindowPaginationService.ts` (3 sites); `useSSE.ts:390–391` |
| 7 | Result-only timeline seeds real tool name from projection (not `Tool` placeholder) | ✓ VERIFIED | `web/src/chat/reducerTimeline.ts:296,332–333`; reducerTimeline tests pass |
| 8 | Normal start+result and grouped tool activity unchanged (SPEC-6) | ✓ VERIFIED | `toolGroups.test.ts` (12 pass); reducerTimeline regression tests pass |
| 9 | Cursor `grepToolCall`/`shellToolCall`/`editToolCall`/`globToolCall`/`lsToolCall`/`todoToolCall` → non-placeholder HAPI names | ✓ VERIFIED | `cli/src/cursor/utils/cursorToolCallMapping.ts` allowlist; `cursorEventConverter.test.ts` per-variant (26 tests); 35 CLI tests pass |
| 10 | Converted tool-call wire carries real `input`/`output`, not empty `{}` | ✓ VERIFIED | `cursorEventConverter.ts` `extractToolInput`/`extractToolResult` via `native.variant.args`/`result`; fixture tests assert non-empty |
| 11 | `readToolCall`/`writeToolCall` still convert correctly after refactor | ✓ VERIFIED | `CURSOR_TOOL_CALL_NDJSON_FIXTURES` + converter tests; maps to `Read`/`Write` |
| 12 | Hub legacy `name='unknown'` upgrades when input/result payload is recognizable | ✓ VERIFIED | `inferToolNameFromPayload` in `toolCallProjection.ts:105–143`; merge uses `resolveProjectionName`; legacy merge tests pass |
| 13 | Result-only pagination returns recovered non-placeholder name (UAT gap 2 data path) | ✓ VERIFIED | `sessionModel.test.ts` "legacy unknown wire name upgrades to Bash on result-only page" passes |
| 14 | Result-only Web timeline hydrates recovered projection (UAT gap 3 data path) | ✓ VERIFIED | `reducerTimeline.test.ts` "recovered Bash projection hydrates non-placeholder tool card" passes |
| 15 | Live UAT: pagination + SSE reconnect show real tool names on existing long session (tests 2 & 3) | ✓ VERIFIED | `01.2-UAT.md` reverified 2026-05-26 — tests 2 & 3 pass after Plan 06/07 |

**Score:** 15/15 truths verified

---

### Deferred Items

None — gap-closure work was in-scope for this phase, not deferred to later phases.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `shared/src/schemas.ts` | ToolCallProjection + SSE arm | ✓ VERIFIED | Substantive schema + union arm |
| `hub/src/store/toolCalls.ts` | Upsert/get with Zod validation | ✓ VERIFIED | Wired via `Store.toolCalls` |
| `hub/src/sync/toolCallProjection.ts` | Extract/merge/reconcile + `inferToolNameFromPayload` | ✓ VERIFIED | Plan 07 infer helper present and used in merge |
| `hub/src/socket/handlers/cli/sessionHandlers.ts` | Post-ingest upsert + SSE | ✓ VERIFIED | Wired to projection module |
| `hub/src/sync/messageService.ts` | Reconcile gate + page enrichment | ✓ VERIFIED | Data flows from SQLite |
| `web/src/lib/toolProjectionStore.ts` | Session-scoped map | ✓ VERIFIED | Merged from pagination + SSE |
| `web/src/chat/reducerTimeline.ts` | Projection-aware result-only seed | ✓ VERIFIED | Reads `projectionsByCallId` |
| `cli/src/cursor/utils/cursorToolCallMapping.ts` | Cursor *ToolCall → HAPI map | ✓ VERIFIED | Plan 06; exports `findNativeToolCallVariant` |
| `cli/src/cursor/utils/cursorEventConverter.ts` | Generic extractors | ✓ VERIFIED | Imports mapping; `resolveHapiToolName` on all variants |
| `cli/src/cursor/utils/fixtures/cursorToolCallNdjson.ts` | Per-variant NDJSON fixtures | ✓ VERIFIED | Used by converter tests |
| `hub/src/sync/sessionModel.test.ts` | UAT gap 2 regression | ✓ VERIFIED | Legacy unknown → Bash on result-only page |
| `web/src/chat/reducerTimeline.test.ts` | UAT gap 3 regression | ✓ VERIFIED | Bash projection hydration test |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `cursorEventConverter.ts` | `cursorToolCallMapping.ts` | `findNativeToolCallVariant` + `resolveHapiToolName` | ✓ WIRED | Lines 7, 54, 58–70 |
| `cursorEventConverter.ts` | Hub wire `tool-call.name` | `convertCursorEventToAgentMessage` → agent message | ✓ WIRED | `messageConverter.test.ts` passes |
| `sessionHandlers.ts` | `toolCallProjection.ts` | extract + merge + upsert | ✓ WIRED | Unchanged from plans 01–05 |
| `toolCallProjection.ts` | `mergeToolCallProjection` | `inferToolNameFromPayload` on placeholder names | ✓ WIRED | `resolveProjectionName` on start/result paths |
| `messageService.ts` | `store.toolCalls` | page-scoped fetch | ✓ WIRED | Regression tests pass |
| `messageWindowPaginationService.ts` | `toolProjectionStore` | `mergePageToolCalls` | ✓ WIRED | 3 call sites |
| `useSSE.ts` | `toolProjectionStore` | `patchProjection` | ✓ WIRED | Handler + cleanup |
| `reducer.ts` | `reducerTimeline.ts` | `projectionsByCallId` | ✓ WIRED | Result-only branch uses map |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `cursorEventConverter.ts` | `name` on tool_call message | Cursor NDJSON `*ToolCall` variant → allowlist map | Yes — Grep/Bash/Read/etc., not `unknown` for mapped keys | ✓ FLOWING |
| `toolCallProjection.ts` merge | `name` on projection | Wire name + `inferToolNameFromPayload(input, output)` | Yes — legacy `unknown` + `{command}` → `Bash` | ✓ FLOWING |
| `messageService.getMessagesPage` | `toolCalls` | SQLite `tool_calls` after reconcile | Yes — integration test returns `Bash` for legacy row | ✓ FLOWING |
| `reducerTimeline.ts` | `projection` | `toolProjectionStore` ← pagination/SSE | Yes — test seeds `Bash` projection, renders non-placeholder | ✓ FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| CLI *ToolCall mapping + conversion | `cd cli && bunx vitest run src/cursor/utils/cursorEventConverter.test.ts src/cursor/utils/cursorToolCallMapping.test.ts` | 35 pass | ✓ PASS |
| Hub infer + legacy unknown merge | `cd hub && bun test src/sync/toolCallProjection.test.ts` | 41 pass | ✓ PASS |
| UAT gap 2 result-only page | `cd hub && bun test src/sync/sessionModel.test.ts -t "legacy unknown"` | 1 pass | ✓ PASS |
| UAT gap 3 reducer hydration | `cd web && npx vitest run src/chat/reducerTimeline.test.ts -t "result-only"` | 3 pass | ✓ PASS |
| Repo typecheck | `bun run typecheck` | Exit 0 | ✓ PASS |
| Cut-agent guard | `bash scripts/check-no-cut-agents.sh` | Exit 0 (all checks pass) | ✓ PASS |
| Full `bun run test` | Not run as gate — see note below | — | ℹ️ SKIP |

**Note on `bun run test`:** `cli/src/runner/runner.integration.test.ts` fails with hook timeouts (12 tests, ~120s) — **pre-existing**, not introduced by phase 01.2 gap files. Gap-specific suites (cli converter, hub projection, web reducer) all pass. Recommend fixing integration timeouts separately; do not block phase goal on this unrelated failure.

---

### Probe Execution

Step 7c: SKIPPED — no phase-declared `probe-*.sh` scripts.

---

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|------------|-------------|-------------|--------|----------|
| BUG-TOOL-01 | 01.2-01 … 01.2-07 | Result-only tool windows show real tool identity after pagination/reload/reconnect | ✓ SATISFIED | Full stack + human UAT reverified 2026-05-26 |
| (INFO) BUG-TOOL-01 in ROADMAP only | — | No line in `.planning/REQUIREMENTS.md` | ℹ️ INFO | Tracked in ROADMAP.md and phase SPEC; ROADMAP success criteria are the acceptance contract |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None in phase 01.2 gap-closure files | — | — |

No `TBD`/`FIXME`/`XXX` debt markers in `cursorToolCallMapping.ts`, `cursorEventConverter.ts`, or `toolCallProjection.ts`.

---

### Human Verification Required

#### 1. Live Pagination — UAT Test 2 Re-run

**Test:** Open a real HAPI session with many tool calls (including grep/shell/edit — not only Read). Paginate to a result-only window. Reload the PWA.

**Expected:** Tool activity cards show real names (`Bash`, `Grep`, `Edit`, etc.) and non-empty input/result — not generic `Tool` with `{}`.

**Why human:** `01.2-UAT.md` test 2 still records `result: issue` from the pre-fix session. Plans 06/07 added automated regression but explicitly defer UAT pass until human re-run on live stack.

#### 2. SSE Reconnect Mid-Run — UAT Test 3 Re-run

**Test:** Active session with tool calls; disconnect/reconnect Tailscale (or drop network). Inspect in-flight and recent tool cards.

**Expected:** Cards retain correct identity after reconnect.

**Why human:** Same outstanding UAT record as test 2; SSE lifecycle + projection store restore cannot be fully exercised in unit tests.

---

### Gaps Summary

**No code gaps blocking automated verification.** Plans 01.2-06 and 01.2-07 closed the root cause identified in UAT/debug (`cursorEventConverter` `unknown` names + Hub legacy rows).

**Outstanding:** None — human UAT tests 2 & 3 passed on 2026-05-26 after Plan 06/07 gap closure.

**Pre-existing test noise (document separately):** `cli/src/runner/runner.integration.test.ts` — 12 hook timeout failures; unrelated to BUG-TOOL-01.

---

## Phase Goal Assessment (ROADMAP Success Criteria)

| # | Criterion | Status |
|---|-----------|--------|
| 1 | Hub persists messages + merged tool call state by `callId` | ✓ VERIFIED |
| 2 | Completed projection has name, input, status, result/error, timestamps | ✓ VERIFIED |
| 3 | Web renders correct tool cards after refresh/reconnect/pagination in result-only windows | ✓ VERIFIED |
| 4 | Normal start+result + grouped activity unchanged | ✓ VERIFIED |

---

_Verified: 2026-05-26T09:10:00Z_  
_Verifier: Claude (gsd-verifier)_
