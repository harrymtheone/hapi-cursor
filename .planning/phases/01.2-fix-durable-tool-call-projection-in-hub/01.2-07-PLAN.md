---
phase: 01.2-fix-durable-tool-call-projection-in-hub
plan: 07
type: execute
wave: 2
depends_on:
  - 01.2-06
gap_closure: true
files_modified:
  - hub/src/sync/toolCallProjection.ts
  - hub/src/sync/toolCallProjection.test.ts
  - hub/src/sync/sessionModel.test.ts
  - web/src/chat/reducerTimeline.test.ts
autonomous: true
requirements:
  - BUG-TOOL-01
must_haves:
  truths:
    - "Pre-existing Hub rows with name='unknown' upgrade to inferred tool identity when input/result payload shape is recognizable"
    - "Result-only pagination path renders non-placeholder tool name when projection was recovered from stored wire payload"
    - "UAT tests 2 and 3 pass after CLI fix plus optional Hub recovery for legacy sessions"
  artifacts:
    - path: "hub/src/sync/toolCallProjection.ts"
      provides: "inferToolNameFromPayload helper used during merge/reconcile when name is placeholder"
      contains: "inferToolNameFromPayload"
    - path: "hub/src/sync/toolCallProjection.test.ts"
      provides: "Legacy unknown recovery test cases"
  key_links:
    - from: "hub/src/sync/toolCallProjection.ts"
      to: "mergeToolCallProjection"
      via: "upgrade unknown name from input/result heuristics before persist"
      pattern: "inferToolNameFromPayload"
---

<objective>
Optional legacy recovery for sessions ingested before Plan 06, plus automated re-verification that UAT gaps 2 & 3 close end-to-end.

Purpose: Stored Hub messages retain wire name='unknown' for historical tool calls; reconcile alone cannot fix without payload inference. Small-scope heuristics upgrade placeholder projections without re-parsing Cursor NDJSON (UAT optional missing item).
Output: Hub infer helper, tests, integration assertion for result-only window with recovered name.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-UAT.md
@.planning/debug/tool-call-placeholder-unknown-name.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-06-SUMMARY.md
@hub/src/sync/toolCallProjection.ts
@hub/src/sync/toolCallProjection.test.ts
@hub/src/sync/sessionModel.test.ts
@web/src/chat/reducerTimeline.test.ts
@web/src/components/ToolCard/knownTools.tsx
</context>

<execution_preconditions>
- Plan 01.2-06 complete (CLI emits real names for new ingests).
- Run GitNexus `impact` on `mergeToolCallProjection` and `reconcileSessionToolCalls` before editing (`repo: "hapi-cursor"`, `direction: "upstream"`).
- Scope guard: infer helper must be &lt;80 lines, no new dependencies, no REST/SSE contract changes (D-06–D-08 unchanged).
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hub inferToolNameFromPayload for legacy unknown rows</name>
  <files>hub/src/sync/toolCallProjection.ts, hub/src/sync/toolCallProjection.test.ts</files>
  <read_first>
    - hub/src/sync/toolCallProjection.ts (mergeToolCallProjection placeholder rules lines 87-138)
    - web/src/components/ToolCard/knownTools.tsx (target name strings)
    - web/src/chat/reducerTools.ts (isPlaceholderToolName: '', 'tool', 'unknown')
  </read_first>
  <behavior>
    - inferToolNameFromPayload({ command: 'ls' }, null) returns 'Bash'
    - inferToolNameFromPayload({ pattern: 'foo', path: '/src' }, null) returns 'Grep' when path present with pattern (content search)
    - inferToolNameFromPayload({ pattern: '**/*.ts' }, null) returns 'Glob'
    - inferToolNameFromPayload({ path: '/foo' }, { success: { content: '...' } }) returns 'Read'
    - inferToolNameFromPayload({ path: '/foo', fileText: 'x' }, null) returns 'Write'
    - inferToolNameFromPayload({}, null) returns null (no guess)
    - mergeToolCallProjection with start event name='unknown' and input { command: 'echo hi' } yields name 'Bash'
    - mergeToolCallProjection does NOT downgrade existing non-placeholder name
  </behavior>
  <action>
    Add exported inferToolNameFromPayload(input: unknown, output: unknown): string | null in hub/src/sync/toolCallProjection.ts. Heuristics aligned with knownTools input keys (mirror getToolPresentation fallbacks, not full NLP):
    - command or cmd string → Bash
    - pattern + (path or glob-like **) → Grep vs Glob (glob when pattern contains * or **)
    - file_path/path + fileText or content → Write; file_path/path + read-shaped result.success.content → Read; file_path/path + old_string/new_string or edits → Edit
    - todos array or merge flag → TodoWrite
    - Return null when ambiguous (prefer no upgrade over wrong label).

    Integrate into mergeToolCallProjection: after computing candidate name for start events and for result-only bootstrap (prev null), if name is placeholder ('unknown' or empty) call inferToolNameFromPayload(incoming.input ?? prev?.input, incoming output for result kind). Apply same on reconcile path via existing merge calls only — do NOT add new reconcile triggers.

    Add toolCallProjection.test.ts cases for infer helper and merge upgrade from unknown→Bash/Read/Grep. Use bun:test only.
  </action>
  <verify>
    <automated>cd hub && bun test src/sync/toolCallProjection.test.ts -t "inferToolNameFromPayload\|legacy unknown"</automated>
  </verify>
  <done>Historical unknown projections upgrade when stored input/result shape is recognizable; new ingests already fixed by Plan 06.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end gap closure gate</name>
  <files>hub/src/sync/sessionModel.test.ts, web/src/chat/reducerTimeline.test.ts</files>
  <read_first>
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-UAT.md (tests 2 & 3 truths)
    - hub/src/sync/sessionModel.test.ts (existing toolCalls page enrichment tests)
    - web/src/chat/reducerTimeline.test.ts (projection hydration tests ~466+)
  </read_first>
  <action>
    Extend sessionModel.test.ts: ingest synthetic Cursor wire messages where tool-call name was 'unknown' but input contains { command: 'npm test' }; run reconcileSessionToolCalls; assert getMessagesPage toolCalls[callId].name === 'Bash' on result-only page window.

    Extend reducerTimeline.test.ts: projection map entry with name 'Bash' and input { command: 'npm test' } hydrates result-only tool card with non-placeholder presentation (tool.name !== 'unknown' and !== 'Tool').

    Run gap closure quality gate from repo root: bun run typecheck && bun run test && bash scripts/check-no-cut-agents.sh. Fix only failures in phase 01.2 gap files if any.

    Update 01.2-UAT.md gap statuses to resolved only if automated assertions cover the truths; otherwise leave for human UAT re-run (do not mark pass without evidence).
  </action>
  <verify>
    <automated>bun run typecheck && bun run test && cd hub && bun test src/sync/sessionModel.test.ts && cd ../web && bun run test -- src/chat/reducerTimeline.test.ts</automated>
  </verify>
  <done>UAT gaps 2 & 3 have automated regression coverage; legacy and new-ingest paths both produce non-placeholder tool cards in result-only windows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub merge heuristics | Infers identity from stored payload — must not overwrite explicit names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-10 | Tampering | inferToolNameFromPayload | mitigate | Return null when ambiguous; never overwrite non-placeholder names (existing merge guard) |
| T-01.2-11 | Information Disclosure | payload heuristics | accept | Operates on already-stored session data; no new exposure |
| T-01.2-SC | Tampering | npm/pip/cargo installs | accept | No new packages |

</threat_model>

<verification>
- cd hub && bun test src/sync/toolCallProjection.test.ts src/sync/sessionModel.test.ts
- cd web && bun run test -- src/chat/reducerTimeline.test.ts
- bun run typecheck && bun run test
</verification>

<success_criteria>
- UAT optional missing item "Hub reconcile from result payload for legacy unknown" delivered within small scope.
- Both UAT gap truths achievable: pagination/refresh and SSE reconnect show real tool names (new ingests via Plan 06; legacy via Plan 07 infer).
- BUG-TOOL-01 closed with regression tests.
</success_criteria>

<output>
Create `.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-07-SUMMARY.md` when done
</output>
