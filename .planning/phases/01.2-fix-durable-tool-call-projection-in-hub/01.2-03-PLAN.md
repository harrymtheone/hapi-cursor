---
phase: 01.2-fix-durable-tool-call-projection-in-hub
plan: 03
type: execute
wave: 1
depends_on:
  - 01.2-02
files_modified:
  - hub/src/socket/handlers/cli/sessionHandlers.ts
  - hub/src/sync/messageService.ts
  - hub/src/sync/messageService.test.ts
  - hub/src/sync/syncEngineMessage.ts
  - hub/src/sync/sessionModel.test.ts
autonomous: true
requirements:
  - BUG-TOOL-01
must_haves:
  truths:
    - "Every tool-call or tool-call-result ingest upserts tool_calls idempotently (D-02, D-03)."
    - "First getMessagesPage per session reconciles historical messages (D-04)."
    - "Paginated API returns toolCalls only for callIds in that page (D-06)."
    - "SSE emits tool-call-projection-updated before or with correct ordering vs message-received (D-08, Pitfall 3)."
  artifacts:
    - path: "hub/src/socket/handlers/cli/sessionHandlers.ts"
      provides: "Post-addMessage projection upsert and SSE emit"
    - path: "hub/src/sync/messageService.ts"
      provides: "reconciledSessions gate and toolCalls enrichment"
  key_links:
    - from: "hub/src/socket/handlers/cli/sessionHandlers.ts"
      to: "hub/src/sync/toolCallProjection.ts"
      via: "extract + merge + upsert after addMessage"
      pattern: "extractToolCallEventsFromMessageContent"
    - from: "hub/src/sync/messageService.ts"
      to: "store.toolCalls.getBySessionAndCallIds"
      via: "collectCallIdsFromDecryptedMessages"
      pattern: "toolCalls"
---

<objective>
Wire Hub ingest and message pagination to maintain and serve canonical tool call projections.

Purpose: Projections must update on live ingest and be available on page load without a separate REST resource (D-02, D-04, D-06, D-08).
Output: sessionHandlers upsert hook, MessageService page enrichment, reconcile gate, and tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-02-SUMMARY.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-SPEC.md
@hub/src/socket/handlers/cli/sessionHandlers.ts
@hub/src/sync/messageService.ts
@hub/src/sync/messageService.test.ts
@hub/src/sync/sessionModel.test.ts
@hub/src/web/routes/messages.ts
</context>

<execution_preconditions>
- Run GitNexus `impact` on sessionHandlers message handler and MessageService.getMessagesPage before edits.
- WARN user if HIGH/CRITICAL blast radius on ingest hot path.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ingest upsert and SSE projection events</name>
  <files>hub/src/socket/handlers/cli/sessionHandlers.ts, hub/src/sync/sessionModel.test.ts</files>
  <read_first>
    - hub/src/socket/handlers/cli/sessionHandlers.ts (todos block ~110-116; message emit after ~134)
    - hub/src/sync/toolCallProjection.ts (from plan 02)
    - hub/src/sync/eventPublisher.ts (SyncEventSchema dev validation)
    - hub/src/sync/sessionModel.test.ts (tool-call fixtures ~378-401)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md (D-02, D-03, D-08, D-13)
  </read_first>
  <behavior>
    - After tool-call ingest, tool_calls row exists with name CursorBash (or fixture name).
    - After tool-call-result, projection status completed with result payload.
    - Raw messages table row count unchanged vs pre-projection behavior (SPEC-3).
    - onWebappEvent receives tool-call-projection-updated before message-received for same ingest tick when both fire.
  </behavior>
  <action>
    In sessionHandlers after store.messages.addMessage and before existing message-received / session broadcast path: call extractToolCallEventsFromMessageContent(content, msg.createdAt). For each event, load existing projection via store.toolCalls, merge with mergeToolCallProjection, upsert, then onWebappEvent({ type: 'tool-call-projection-updated', sessionId: sid, callId, projection }) per D-08. Emit projection SSE before message-received for that message to avoid Web Tool flash (RESEARCH Pitfall 3). Do not mutate content or skip addMessage (D-03). Extend sessionModel.test.ts: after tool-call + tool-call-result sequence, assert store.toolCalls.getBySessionAndCallIds returns non-placeholder name; assert messages rows still match existing expectations (no snapshot change to message storage assertions). Do not change CLI wire (D-13).
  </action>
  <verify>
    <automated>cd hub && bun test src/sync/sessionModel.test.ts -t tool</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: sessionModel tool-call fixture leaves tool_calls row with merged name and input.
    - Test assertion: existing message storage assertions in sessionModel.test.ts still pass.
    - Source assertion: sessionHandlers imports toolCallProjection helpers only (no inline parse duplication).
    - Source assertion: tool-call-projection-updated emitted with validated projection shape.
  </acceptance_criteria>
  <done>Live ingest maintains durable projection and notifies Web via SSE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile gate and getMessagesPage toolCalls enrichment</name>
  <files>hub/src/sync/messageService.ts, hub/src/sync/messageService.test.ts, hub/src/sync/syncEngineMessage.ts</files>
  <read_first>
    - hub/src/sync/messageService.ts (getMessagesPage return ~123-131)
    - hub/src/sync/messageService.test.ts (pagination tests)
    - hub/src/sync/toolCallProjection.ts (reconcileSessionToolCalls, collectCallIdsFromDecryptedMessages)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md (D-04, D-06, D-07)
  </read_first>
  <behavior>
    - First getMessagesPage for sessionId triggers reconcile once per MessageService instance.
    - Second getMessagesPage same session does not full-scan again.
    - Returned toolCalls keys are subset of callIds referenced in returned messages only.
    - Result-only page still includes projection entry for result callId when reconcile ran.
  </behavior>
  <action>
    Add private reconciledSessions Set on MessageService per D-04. At start of getMessagesPage, if !reconciledSessions.has(sessionId), call reconcileSessionToolCalls(sessionId, this.store) then add sessionId. After building messages array, call collectCallIdsFromDecryptedMessages(messages), fetch store.toolCalls.getBySessionAndCallIds(sessionId, callIds), return { messages, page, toolCalls } (default {} when no ids). Update return type in messageService.ts and syncEngineMessage.ts passthrough. Extend messageService.test.ts: seed session with tool-call + result messages only in DB, first page fetch returns toolCalls[callId].name matching start message. Add test that second page call does not invoke reconcile again (spy or call counter on reconcile helper). hub/src/web/routes/messages.ts should need no logic change if SyncEngine returns enriched shape. Do not add GET /tool-calls route (D-07).
  </action>
  <verify>
    <automated>cd hub && bun test src/sync/messageService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: getMessagesPage response includes toolCalls object with projection for visible result-only callId.
    - Test assertion: reconcile runs once per session per MessageService lifetime in test.
    - Test assertion: toolCalls map excludes callIds not present in returned messages array.
    - Source assertion: getMessagesPage return type includes toolCalls in MessagesResponse shape.
    - Source assertion: pagination cursors (nextBeforeSeq, hasMore) behavior unchanged from baseline tests.
  </acceptance_criteria>
  <done>Web pagination API atomically returns messages plus per-page projection map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI socket → sessionHandlers | Untrusted agent message JSON |
| Hub → Web SSE/REST | Projection leaves Hub only after Zod validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-01 | Tampering | sessionHandlers ingest | mitigate | extract + merge + safeParse before upsert; raw message untouched |
| T-01.2-05 | Repudiation | reconcileSessionToolCalls | accept | Full scan is idempotent repair, not audit log |
| T-01.2-03 | Denial of Service | reconcile on first page | mitigate | Once per process per sessionId; not every pagination click |
| T-01.2-02 | Tampering | SSE payload | mitigate | ToolCallProjectionSchema + EventPublisher dev safeParse |

</threat_model>

<verification>
- cd hub && bun test src/sync/messageService.test.ts src/sync/sessionModel.test.ts
- bun run typecheck
</verification>

<success_criteria>
- Ingest upserts tool_calls and emits tool-call-projection-updated.
- getMessagesPage returns toolCalls scoped to page message callIds after one-time reconcile.
</success_criteria>

<output>
Create `.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-03-SUMMARY.md` when done
</output>
