---
phase: 01.2-fix-durable-tool-call-projection-in-hub
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - shared/src/schemas.ts
  - shared/src/responses.ts
  - shared/src/schemas.test.ts
  - shared/src/index.ts
autonomous: true
requirements:
  - BUG-TOOL-01
must_haves:
  truths:
    - "ToolCallProjection shape is validated at the protocol boundary (D-14)."
    - "Messages API contract includes per-page toolCalls map (D-06, D-07)."
    - "SSE clients can receive tool-call-projection-updated events (D-08)."
  artifacts:
    - path: "shared/src/schemas.ts"
      provides: "ToolCallProjectionSchema and SyncEvent arm"
      contains: "ToolCallProjectionSchema"
    - path: "shared/src/responses.ts"
      provides: "MessagesResponse.toolCalls field"
      contains: "toolCalls"
    - path: "shared/src/schemas.test.ts"
      provides: "SyncEvent schema acceptance for projection updates"
  key_links:
    - from: "shared/src/schemas.ts"
      to: "SyncEventSchema"
      via: "discriminated union arm tool-call-projection-updated"
      pattern: "tool-call-projection-updated"
    - from: "shared/src/responses.ts"
      to: "ToolCallProjectionSchema"
      via: "Record<string, ToolCallProjection>"
      pattern: "toolCalls"
---

<objective>
Define shared protocol contracts for durable tool call projections and live SSE patches.

Purpose: Hub store, ingest, API enrichment, and Web reducer must share one strict Zod schema before any persistence or UI wiring (D-14, SPEC-2).
Output: `ToolCallProjectionSchema`, extended `MessagesResponse`, and `tool-call-projection-updated` SyncEvent arm with tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-SPEC.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-RESEARCH.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-PATTERNS.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-VALIDATION.md
@shared/src/schemas.ts
@shared/src/responses.ts
@shared/src/schemas.test.ts
</context>

<execution_preconditions>
- Run GitNexus `impact` on `SyncEventSchema` and `MessagesResponse` exports before editing `shared/src/schemas.ts` or `shared/src/responses.ts` (`repo: "hapi-cursor"`, `direction: "upstream"`).
- Report blast radius; proceed unless CRITICAL (new union arm is additive).
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ToolCallProjectionSchema and type exports</name>
  <files>shared/src/schemas.ts, shared/src/index.ts</files>
  <read_first>
    - shared/src/schemas.ts (CursorModelSummarySchema.strict pattern ~280-285; SessionChangedSchema ~395-410)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-SPEC.md (req 2 field list)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md (D-14)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-RESEARCH.md (ToolCallProjection shape)
  </read_first>
  <behavior>
    - Valid projection: callId, name Bash, input object, status completed, startedAt number, optional completedAt and result.
    - Invalid: missing callId, empty name, unknown status enum, extra keys (.strict).
    - Optional permission object accepted when present; omitted when absent (A4).
  </behavior>
  <action>
    Add ToolCallProjectionSchema as z.object({ callId: z.string().min(1), name: z.string().min(1), input: z.unknown(), status: z.enum(['pending', 'in_progress', 'completed', 'failed']), result: z.unknown().optional(), error: z.unknown().optional(), startedAt: z.number(), completedAt: z.number().optional(), permission: z.object({ toolName: z.string().optional(), input: z.unknown().optional(), decision: z.string().optional() }).strict().optional() }).strict() per D-14 and SPEC req 2. Export ToolCallProjection type via z.infer. Re-export from shared/src/index.ts if other modules use barrel exports. Do not add createdAt alias field — use startedAt as canonical start timestamp per RESEARCH merge rules.
  </action>
  <verify>
    <automated>cd shared && bun test src/schemas.test.ts -t ToolCallProjection</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: ToolCallProjectionSchema exists in shared/src/schemas.ts with .strict().
    - Source assertion: status enum has exactly four values matching SPEC.
    - Test assertion: schemas.test.ts includes pass and fail fixtures for ToolCallProjectionSchema.
    - Source assertion: shared/src/index.ts exports ToolCallProjection type if barrel is used elsewhere.
  </acceptance_criteria>
  <done>Protocol projection schema validates complete and incomplete payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MessagesResponse.toolCalls and SyncEvent arm</name>
  <files>shared/src/schemas.ts, shared/src/responses.ts, shared/src/schemas.test.ts</files>
  <read_first>
    - shared/src/schemas.ts (SyncEventSchema discriminated union ~402-462)
    - shared/src/responses.ts (MessagesResponse ~8-16)
    - shared/src/schemas.test.ts (SyncEventSchema data union ~179-187)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md (D-06, D-07, D-08)
  </read_first>
  <behavior>
    - SyncEvent tool-call-projection-updated parses with sessionId, callId, projection.
    - Rejects wrong type literal or projection missing required fields.
    - MessagesResponse type includes toolCalls as Record keyed by callId.
  </behavior>
  <action>
    Extend MessagesResponse in shared/src/responses.ts with toolCalls: Record&lt;string, ToolCallProjection&gt; (import type from schemas). Default empty object is valid for callers. Add SyncEvent arm: SessionChangedSchema.extend({ type: z.literal('tool-call-projection-updated'), callId: z.string().min(1), projection: ToolCallProjectionSchema }) inside SyncEventSchema discriminated union per D-08. Do not add GET /sessions/:id/tool-calls route types (D-07). Add schemas.test.ts cases mirroring session-updated pattern: one valid tool-call-projection-updated event and one reject fixture (bad projection / missing callId).
  </action>
  <verify>
    <automated>cd shared && bun test src/schemas.test.ts -t "tool-call-projection"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: MessagesResponse includes toolCalls field typed as Record of ToolCallProjection.
    - Source assertion: SyncEventSchema includes type literal tool-call-projection-updated.
    - Test assertion: schemas.test.ts safeParse success for minimal valid projection event.
    - Test assertion: schemas.test.ts safeParse failure for malformed projection payload.
    - Source assertion: no new REST route types added (D-07).
  </acceptance_criteria>
  <done>Wire contract supports page enrichment and live SSE patches without parallel REST.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent payload → Hub projection | Untrusted tool-call content from CLI ingest |
| Hub → Web API/SSE | Projection served to PWA over Tailscale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-01 | Tampering | ToolCallProjectionSchema | mitigate | .strict() Zod at protocol boundary; Hub re-validates before persist (plan 02) |
| T-01.2-02 | Tampering | SyncEventSchema union | mitigate | Dev EventPublisher safeParse; reject malformed SSE in tests |
| T-01.2-SC | Tampering | npm workspace packages | accept | No new external packages this plan |

</threat_model>

<verification>
- cd shared && bun test src/schemas.test.ts
- bun run typecheck (shared package compiles)
</verification>

<success_criteria>
- ToolCallProjectionSchema and tool-call-projection-updated SyncEvent arm exported and tested.
- MessagesResponse documents toolCalls for paginated message loads.
</success_criteria>

<output>
Create `.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-01-SUMMARY.md` when done
</output>
