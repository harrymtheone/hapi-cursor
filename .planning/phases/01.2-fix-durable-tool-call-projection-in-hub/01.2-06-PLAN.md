---
phase: 01.2-fix-durable-tool-call-projection-in-hub
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - cli/src/cursor/utils/cursorToolCallMapping.ts
  - cli/src/cursor/utils/cursorEventConverter.ts
  - cli/src/cursor/utils/cursorEventConverter.test.ts
  - cli/src/cursor/utils/fixtures/cursorToolCallNdjson.ts
autonomous: true
requirements:
  - BUG-TOOL-01
must_haves:
  truths:
    - "Cursor grepToolCall, shellToolCall, editToolCall, globToolCall, lsToolCall, and todoToolCall NDJSON events convert to non-placeholder HAPI tool names"
    - "Converted tool-call wire messages carry args as input and completed variant.result as output — not empty {}"
    - "readToolCall and writeToolCall continue to convert correctly after generic parser refactor"
  artifacts:
    - path: "cli/src/cursor/utils/cursorToolCallMapping.ts"
      provides: "Cursor *ToolCall key → HAPI knownTools name map"
      exports: ["CURSOR_TOOL_KEY_TO_HAPI_NAME", "findNativeToolCallVariant"]
    - path: "cli/src/cursor/utils/fixtures/cursorToolCallNdjson.ts"
      provides: "Minimal started/completed NDJSON lines per Cursor native tool variant"
    - path: "cli/src/cursor/utils/cursorEventConverter.test.ts"
      provides: "Per-variant conversion assertions"
  key_links:
    - from: "cli/src/cursor/utils/cursorEventConverter.ts"
      to: "cli/src/cursor/utils/cursorToolCallMapping.ts"
      via: "findNativeToolCallVariant + map lookup in extractToolName/Input/Result"
      pattern: "findNativeToolCallVariant"
    - from: "cli/src/cursor/utils/cursorEventConverter.ts"
      to: "cli/src/agent/messageConverter.ts"
      via: "AgentMessage.tool_call.name flows to Hub wire tool-call.name"
      pattern: "name.*message\\.name"
---

<objective>
Close UAT gaps 2 & 3 at the root cause: generic Cursor native *ToolCall parsing and HAPI name mapping in the CLI converter.

Purpose: Hub projection faithfully persists wire `name`; when CLI emits `unknown`, Web shows placeholder "Tool". Fixing conversion upstream restores real tool identity for pagination, reload, and SSE reconnect (UAT root_cause, debug session).
Output: `cursorToolCallMapping.ts`, refactored `cursorEventConverter.ts`, NDJSON fixture tests per tool variant.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-UAT.md
@.planning/debug/tool-call-placeholder-unknown-name.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-05-SUMMARY.md
@cli/src/cursor/utils/cursorEventConverter.ts
@cli/src/cursor/utils/cursorEventConverter.test.ts
@cli/src/agent/messageConverter.ts
@web/src/components/ToolCard/knownTools.tsx
@web/src/chat/reducerTools.ts
</context>

<execution_preconditions>
- Run GitNexus `impact` on `convertCursorEventToAgentMessage` and `extractToolName` before editing (`repo: "hapi-cursor"`, `direction: "upstream"`). Report blast radius; proceed unless CRITICAL.
- Gap reason: cursorEventConverter only handles readToolCall/writeToolCall/function — all other *ToolCall variants return name='unknown' (01.2-UAT.md gaps, debug session).
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cursor *ToolCall mapping module and NDJSON fixtures</name>
  <files>cli/src/cursor/utils/cursorToolCallMapping.ts, cli/src/cursor/utils/fixtures/cursorToolCallNdjson.ts, cli/src/cursor/utils/cursorEventConverter.test.ts</files>
  <read_first>
    - cli/src/cursor/utils/cursorEventConverter.ts (extractToolName/Input/Result lines 52-88)
    - web/src/components/ToolCard/knownTools.tsx (registry keys: Bash, Grep, Glob, LS, Read, Write, Edit, TodoWrite, update_plan, WebFetch, WebSearch)
    - .planning/debug/tool-call-placeholder-unknown-name.md (variant list)
    - Cursor docs output-format: readToolCall/writeToolCall args.result shapes
  </read_first>
  <behavior>
    - findNativeToolCallVariant({ grepToolCall: { args: { pattern: 'foo' } } }) returns { key: 'grepToolCall', variant: { args: ... } }
    - CURSOR_TOOL_KEY_TO_HAPI_NAME.grepToolCall === 'Grep'
    - CURSOR_TOOL_KEY_TO_HAPI_NAME.shellToolCall and bashToolCall both === 'Bash'
    - readToolCall maps to 'Read', writeToolCall to 'Write' (knownTools keys; isReadFileToolCall still accepts read_file if legacy rows exist)
    - Fixture started line for grepToolCall converts to tool_call name 'Grep' with non-empty input
    - Fixture completed line for shellToolCall converts to tool_result with non-null output
    - function.name path still resolves MCP/custom names unchanged
    - Unmapped *ToolCall key falls back to PascalCase sans 'ToolCall' suffix only when key ends with ToolCall; otherwise 'unknown'
  </behavior>
  <action>
    Create cli/src/cursor/utils/cursorToolCallMapping.ts exporting:
    - CURSOR_TOOL_KEY_TO_HAPI_NAME: Record covering at minimum readToolCall→Read, writeToolCall→Write, grepToolCall→Grep, globToolCall→Glob, editToolCall→Edit, lsToolCall→LS, listDirToolCall→LS, shellToolCall→Bash, bashToolCall→Bash, todoToolCall→TodoWrite, updatePlanToolCall→update_plan, webFetchToolCall→WebFetch, webSearchToolCall→WebSearch, semSearchToolCall→Grep (document any additional keys found in Cursor docs with matching knownTools entry or leave unmapped).
    - findNativeToolCallVariant(toolCall: Record&lt;string, unknown&gt;): { key: string; variant: Record&lt;string, unknown&gt; } | null — scan own keys ending with 'ToolCall'; if multiple, prefer first key present in CURSOR_TOOL_KEY_TO_HAPI_NAME map order (read before write before grep before shell etc.).
    - resolveHapiToolName(toolCall): string — map lookup → function.name string → generated fallback → 'unknown'.

    Create cli/src/cursor/utils/fixtures/cursorToolCallNdjson.ts with minimal realistic started+completed NDJSON string pairs (one call_id per variant) for: readToolCall, writeToolCall, grepToolCall, globToolCall, editToolCall, lsToolCall, shellToolCall, bashToolCall, todoToolCall, and one function.name MCP-style object. Use Cursor doc field shapes (args.path, args.pattern, args.command, result.success where applicable). Export as Record&lt;variantKey, { started: string; completed: string; callId: string; expectedName: string }&gt;.

    Add failing tests in cursorEventConverter.test.ts: for each fixture variant, parseCursorEvent(started) → convertCursorEventToAgentMessage yields tool_call with expectedName and input !== {}; parseCursorEvent(completed) → tool_result with output defined. Add regression that grepToolCall/shellToolCall no longer produce name 'unknown'. Do NOT implement generic extractors yet — tests fail against current converter.
  </action>
  <verify>
    <automated>cd cli && vitest run src/cursor/utils/cursorEventConverter.test.ts -t "native ToolCall" 2>&1 | grep -q "FAIL\|failed" && echo "RED OK" || (echo "Tests should fail before Task 2" && exit 1)</automated>
  </verify>
  <done>Mapping module and per-variant NDJSON fixtures exist; converter tests fail on unmapped variants (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generic extractToolName/Input/Result via native variant parser</name>
  <files>cli/src/cursor/utils/cursorEventConverter.ts, cli/src/cursor/utils/cursorToolCallMapping.ts, cli/src/cursor/utils/cursorEventConverter.test.ts</files>
  <read_first>
    - cli/src/cursor/utils/cursorToolCallMapping.ts (from Task 1)
    - cli/src/agent/messageConverter.test.ts (wire shape expectations)
  </read_first>
  <behavior>
    - All fixture variants from Task 1 pass started/completed conversion tests
    - extractToolInput returns variant.args when present (not {})
    - extractToolResult returns variant.result when present on completed events
    - function branch unchanged: name from fn.name, input { arguments: fn.arguments }
    - Existing assistant/result parse tests still pass
  </behavior>
  <action>
    Refactor cursorEventConverter.ts extractToolName, extractToolInput, extractToolResult to use findNativeToolCallVariant + CURSOR_TOOL_KEY_TO_HAPI_NAME via resolveHapiToolName. Generic rules per gap missing item:
    - Name: resolveHapiToolName(toolCall) — replaces read/write-only branches; keep function.name branch after native variant scan fails.
    - Input: native variant.args ?? {}; for function keep { arguments: fn.arguments }.
    - Result: native variant.result ?? {} on completed tool_call events (preserve nested success/error objects verbatim for Web result views).

    Remove duplicated readToolCall/writeToolCall special cases once covered by map (behavior must remain equivalent: Read not read_file is intentional — aligns with knownTools and still satisfies isReadFileToolCall via 'Read' and path args).

    Run full cursorEventConverter.test.ts green. Run messageConverter.test.ts to confirm wire path unchanged.
  </action>
  <verify>
    <automated>cd cli && vitest run src/cursor/utils/cursorEventConverter.test.ts src/agent/messageConverter.test.ts</automated>
  </verify>
  <done>CLI emits real HAPI tool names for all Cursor native *ToolCall variants; UAT root cause closed for new ingests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cursor NDJSON → CLI converter | Untrusted stream lines from cursor-agent stdout |
| CLI wire → Hub projection | Tool name/input now authoritative for durability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-08 | Tampering | cursorToolCallMapping.ts | mitigate | Explicit allowlist map; unmapped keys stay 'unknown' not guessed from args |
| T-01.2-09 | Spoofing | resolveHapiToolName fallback | accept | PascalCase fallback only for *ToolCall suffix keys; MCP uses function.name |
| T-01.2-SC | Tampering | npm/pip/cargo installs | accept | No new external packages |

</threat_model>

<verification>
- cd cli && vitest run src/cursor/utils/cursorEventConverter.test.ts
- cd cli && vitest run src/agent/messageConverter.test.ts
- bun run typecheck (cli package compiles)
</verification>

<success_criteria>
- UAT gap missing items "generic *ToolCall parser" and "knownTools name mapping" and "NDJSON fixture tests" satisfied.
- grepToolCall, shellToolCall, editToolCall, globToolCall, lsToolCall, todoToolCall no longer emit name='unknown'.
</success_criteria>

<output>
Create `.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-06-SUMMARY.md` when done
</output>
