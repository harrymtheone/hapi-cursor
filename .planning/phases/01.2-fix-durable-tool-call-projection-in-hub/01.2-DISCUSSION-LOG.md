# Phase 01.2: Fix durable tool call projection in Hub — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-25
**Phase:** 01.2-fix-durable-tool-call-projection-in-hub
**Areas discussed:** Hub persistence, backfill, Web delivery, live SSE, reducer integration, CLI wire

---

## Hub persistence shape

| Option | Description | Selected |
|--------|-------------|----------|
| Dedicated `tool_calls` table | Normalized store keyed by `(session_id, call_id)` | ✓ |
| Session JSON blob | Projection map on `sessions` row | |
| Extend `agentState` only | Reuse permission request maps | |

**User's choice:** Follow agent recommendation (maintainability over cost)
**Notes:** Separates derived projection from permission-oriented `agent_state`; matches message-store durability patterns.

---

## Web delivery contract

| Option | Description | Selected |
|--------|-------------|----------|
| Enrich `getMessagesPage` | Per-page `toolCalls` for callIds in returned messages | ✓ |
| Parallel REST endpoint | Second fetch for tool calls | |
| SSE-only | No load-time projection | |

**User's choice:** Follow agent recommendation
**Notes:** Keeps pagination window and projection slice atomic; avoids client-side join races.

---

## Live updates while session is open

| Option | Description | Selected |
|--------|-------------|----------|
| Refetch-only on `message-received` | No new event type | |
| New SSE `tool-call-projection-updated` | Hub emits after upsert; Web merges map | ✓ |
| Projection only on full reload | Stale cards during stream | |

**User's choice:** Follow agent recommendation
**Notes:** `message-received` does not carry merged identity; projection updates can occur without a visible start message in-window.

---

## Backfill for existing messages

| Option | Description | Selected |
|--------|-------------|----------|
| Lazy forward-only | Broken until new events | |
| Eager idempotent reconcile | `reconcileSessionToolCalls` from stored messages | ✓ |
| On-demand per callId | Fragmented | |

**User's choice:** Follow agent recommendation
**Notes:** Trigger on first `getMessagesPage` per session per process (or schema bump); same merge rules as ingest.

---

## Reducer integration point

| Option | Description | Selected |
|--------|-------------|----------|
| `projectionsByCallId` in reducer context | Parallel to `permissionsById` | ✓ |
| Synthetic tool-call messages | Violates raw-message boundary | |
| Post-pass patch only | Harder to test / reason about | |

**User's choice:** Follow agent recommendation
**Notes:** Result-only path uses projection for `name`/`input`; permissions remain supplemental.

---

## Optional CLI wire enrichment

| Option | Description | Selected |
|--------|-------------|----------|
| Hub-only (authoritative) | SPEC deliverable | ✓ |
| Also enrich CLI `tool-call-result` | Out of scope | |

**User's choice:** Follow agent recommendation
**Notes:** CLI-only fix does not survive restart or pagination gaps; deferred as optional future work.

---

## Claude's Discretion

- Table DDL, SSE batching vs single-call events, reconcile trigger mechanics, query-key layout.

## Deferred Ideas

- CLI wire enrichment; separate tool-calls REST route; projection inside `agentState`.
