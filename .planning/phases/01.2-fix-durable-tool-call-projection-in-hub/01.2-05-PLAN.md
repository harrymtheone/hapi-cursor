---
phase: 01.2-fix-durable-tool-call-projection-in-hub
plan: 05
type: execute
wave: 3
depends_on:
  - 01.2-04
files_modified:
  - hub/src/sync/sessionModel.test.ts
  - hub/src/store/toolCalls.test.ts
  - web/src/chat/toolGroups.test.ts
  - web/src/components/ToolCard
  - scripts/check-no-cut-agents.sh
autonomous: true
requirements:
  - BUG-TOOL-01
must_haves:
  truths:
    - "Phase acceptance: durability across Hub reopen and result-only Web render (SPEC-5)."
    - "Happy-path tool grouping and ToolCard tests unchanged (SPEC-6)."
    - "Repo quality gates pass: typecheck, test, cut-agent guard."
  artifacts:
    - path: "hub/src/sync/sessionModel.test.ts"
      provides: "Integration coverage for ingest + page toolCalls"
    - path: "web/src/chat/toolGroups.test.ts"
      provides: "Regression gate for grouped tool activity"
  key_links:
    - from: "hub/src/sync/sessionModel.test.ts"
      to: "getMessagesPage"
      via: "result-only page toolCalls assertion"
      pattern: "toolCalls"
---

<objective>
Close phase 01.2 with integration assertions and milestone quality gates.

Purpose: Prove BUG-TOOL-01 and all six SPEC requirements hold end-to-end without regressing tool grouping or message storage (SPEC-3, SPEC-6).
Output: Extended integration tests and passing root typecheck/test/guard.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-04-SUMMARY.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-SPEC.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-VALIDATION.md
@hub/src/sync/sessionModel.test.ts
@hub/src/store/toolCalls.test.ts
@web/src/chat/toolGroups.test.ts
@web/src/chat/reducerTimeline.test.ts
</context>

<execution_preconditions>
- All plans 01.2-01 through 01.2-04 complete.
- Run GitNexus detect_changes before commit per project rules.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hub integration and durability scenarios</name>
  <files>hub/src/sync/sessionModel.test.ts, hub/src/store/toolCalls.test.ts</files>
  <read_first>
    - hub/src/sync/sessionModel.test.ts
    - hub/src/store/toolCalls.test.ts
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-SPEC.md (req 5 acceptance)
  </read_first>
  <behavior>
    - getMessagesPage on session with only result message in page window returns toolCalls with start-derived name.
    - Close and reopen Store on disk file: projection row still readable.
  </behavior>
  <action>
    Extend sessionModel.test.ts with scenario: ingest tool-call + tool-call-result, fetch getMessagesPage limited to result-side visibility (or filter messages to simulate result-only window), assert response.toolCalls[callId].name matches tool-call name. Extend toolCalls.test.ts with temp-file Store: upsert projection, close store, reopen same path, getBySessionAndCallIds returns identical projection (SPEC-5 durability). Ensure tests use bun:test only.
  </action>
  <verify>
    <automated>cd hub && bun test src/sync/sessionModel.test.ts src/store/toolCalls.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: page toolCalls enrichment returns non-empty name for result-only visibility case.
    - Test assertion: projection survives Store close/reopen on filesystem db path.
    - Test assertion: no test modifies expected raw message row counts from SPEC-3 baseline.
  </acceptance_criteria>
  <done>Hub side proves durable projection serves paginated API.</done>
</task>

<task type="auto">
  <name>Task 2: Web regression suite and phase gate</name>
  <files>web/src/chat/toolGroups.test.ts</files>
  <read_first>
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-SPEC.md (req 6, acceptance checklist)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-VALIDATION.md (01.2-GATE row)
    - AGENTS.md (commands)
  </read_first>
  <action>
    Run and fix only if failing: cd web && bun run test -- src/chat/toolGroups.test.ts src/chat/reducerTimeline.test.ts src/hooks/useSSE.test.tsx; run ToolCard test glob if present under web/src/components/ToolCard without snapshot churn. Then from repo root: bun run typecheck, bun run test, bash scripts/check-no-cut-agents.sh. If any failure, fix in smallest scope in files touched by phase 01.2. Do not broaden scope to unrelated packages.
  </action>
  <verify>
    <automated>bun run typecheck && bun run test && bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - Command assertion: bun run typecheck exits 0.
    - Command assertion: bun run test exits 0.
    - Command assertion: check-no-cut-agents.sh exits 0.
    - Test assertion: toolGroups.test.ts and reducerTimeline.test.ts pass without intentional behavior regressions (SPEC-6).
  </acceptance_criteria>
  <done>Phase 01.2 meets SPEC acceptance and repo quality gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/local test runner | Validates no secret leakage in fixtures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-07 | Tampering | test fixtures | accept | Use synthetic tool names only; no real credentials in projection JSON |
| T-01.2-SC | Tampering | dependency install | accept | No new packages in phase gate plan |

</threat_model>

<verification>
- bun run typecheck
- bun run test
- bash scripts/check-no-cut-agents.sh
- cd hub && bun test src/sync/sessionModel.test.ts
- cd web && bun run test -- src/chat/reducerTimeline.test.ts src/chat/toolGroups.test.ts src/hooks/useSSE.test.tsx
</verification>

<success_criteria>
- All SPEC acceptance criteria in 01.2-SPEC.md satisfied by automated tests.
- BUG-TOOL-01 closed: result-only windows show real tool identity after reload/pagination paths covered by tests.
</success_criteria>

<output>
Create `.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-05-SUMMARY.md` when done
</output>
