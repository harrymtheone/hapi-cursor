---
phase: 01.2-fix-durable-tool-call-projection-in-hub
plan: 02
type: execute
wave: 0
depends_on:
  - 01.2-01
files_modified:
  - hub/src/store/index.ts
  - hub/src/store/toolCalls.ts
  - hub/src/store/toolCallStore.ts
  - hub/src/store/toolCalls.test.ts
  - hub/src/store/namespace.test.ts
  - hub/src/store/index.test.ts
  - hub/src/sync/toolCallProjection.ts
  - hub/src/sync/toolCallProjection.test.ts
  - hub/README.md
autonomous: true
requirements:
  - BUG-TOOL-01
must_haves:
  truths:
    - "SQLite schema version 12 includes durable tool_calls table (D-01, D-05)."
    - "Hub can upsert and read projections by sessionId and callId."
    - "Merge and extract logic converges out-of-order start/result events (D-02, SPEC-1)."
  artifacts:
    - path: "hub/src/store/toolCalls.ts"
      provides: "SQL upsert and getBySessionAndCallIds"
    - path: "hub/src/sync/toolCallProjection.ts"
      provides: "extractToolCallEventsFromMessageContent, mergeToolCallProjection, reconcileSessionToolCalls"
    - path: "hub/src/store/index.ts"
      provides: "SCHEMA_VERSION 12 and tool_calls DDL"
      contains: "tool_calls"
  key_links:
    - from: "hub/src/store/toolCalls.ts"
      to: "ToolCallProjectionSchema"
      via: "safeParse on read and before write"
      pattern: "ToolCallProjectionSchema.safeParse"
    - from: "hub/src/store/index.ts"
      to: "tool_calls"
      via: "ToolCallStore on Store class"
      pattern: "readonly toolCalls"
---

<objective>
Add Hub SQLite persistence and pure merge/extract logic for canonical tool call projections.

Purpose: Durable storage and idempotent merge must exist before ingest hooks and API enrichment (D-01, D-02, D-04, D-05).
Output: schema v12, ToolCallStore, toolCallProjection module, and unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-01-SUMMARY.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-RESEARCH.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-PATTERNS.md
@hub/src/store/index.ts
@hub/src/store/pushSubscriptions.ts
@hub/src/store/messages.ts
@hub/src/store/messageStore.ts
@hub/src/store/json.ts
@hub/src/sync/todos.ts
@hub/src/sync/teams.ts
</context>

<execution_preconditions>
- Depends on 01.2-01 completing ToolCallProjectionSchema export.
- Run GitNexus `impact` on Store class and initSchema before editing hub/src/store/index.ts.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema v12 tool_calls table and ToolCallStore</name>
  <files>hub/src/store/index.ts, hub/src/store/toolCalls.ts, hub/src/store/toolCallStore.ts, hub/src/store/toolCalls.test.ts, hub/src/store/namespace.test.ts, hub/src/store/index.test.ts</files>
  <read_first>
    - hub/src/store/index.ts (SCHEMA_VERSION 11, createSchema, REQUIRED_TABLES, Store wiring)
    - hub/src/store/pushSubscriptions.ts (ON CONFLICT upsert pattern)
    - hub/src/store/messages.ts (toStoredMessage + safeJsonParse)
    - hub/src/store/json.ts
    - hub/src/store/namespace.test.ts (user_version 11 assertion)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md (D-01)
  </read_first>
  <behavior>
    - Fresh :memory: Store has PRAGMA user_version 12 and tool_calls table.
    - upsert then getBySessionAndCallIds returns merged projection.
    - Reopen DB file retains row (durability).
    - safeParse rejects corrupt projection JSON on read (returns skip or null, no throw).
    - Upsert rejects projection JSON over 65536 bytes (T-01.2-03).
  </behavior>
  <action>
    Bump SCHEMA_VERSION to 12 per D-01. Add tool_calls to REQUIRED_TABLES. Append CREATE TABLE tool_calls (session_id, call_id, projection TEXT NOT NULL, updated_at INTEGER, PK (session_id, call_id), FK session_id → sessions ON DELETE CASCADE) and idx_tool_calls_session index per RESEARCH. Wire readonly toolCalls: ToolCallStore on Store (mirror messages/push pattern). Implement hub/src/store/toolCalls.ts: upsertToolCall(db, sessionId, callId, projection) with JSON.stringify after ToolCallProjectionSchema.safeParse; reject oversize serialized payload before INSERT; getBySessionAndCallIds(sessionId, callIds[]) returns Record of valid projections only. Use safeJsonParse from hub/src/store/json.ts on read then safeParse schema. Implement toolCallStore.ts facade. Update namespace.test.ts: expect user_version 12 and table list includes tool_calls sorted with existing tables. Keep index.test.ts mismatch behavior unchanged (still throws on version drift). Do not add runtime ALTER migration in initSchema.
  </action>
  <verify>
    <automated>cd hub && bun test src/store/toolCalls.test.ts src/store/namespace.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: SCHEMA_VERSION constant equals 12 in hub/src/store/index.ts.
    - Source assertion: REQUIRED_TABLES includes tool_calls.
    - Test assertion: namespace.test.ts expects user_version 12.
    - Test assertion: toolCalls.test.ts proves upsert/get round-trip on :memory: and temp file reopen.
    - Test assertion: corrupt projection row is not returned to callers.
    - Test assertion: oversize JSON upsert is rejected without writing row.
  </acceptance_criteria>
  <done>Hub can durably store and retrieve validated tool call projections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract, merge, and reconcile pure functions</name>
  <files>hub/src/sync/toolCallProjection.ts, hub/src/sync/toolCallProjection.test.ts</files>
  <read_first>
    - hub/src/sync/todos.ts (unwrapRoleWrappedRecordEnvelope + role guard)
    - hub/src/sync/teams.ts (tool-call and tool_use parsing)
    - cli/src/agent/messageConverter.ts (wire shapes)
    - web/src/chat/normalizeAgent.ts (legacy tool_result blocks)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-RESEARCH.md (Pattern 1-2 merge table)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md (D-02, D-04)
  </read_first>
  <behavior>
    - tool-call then tool-call-result for same callId yields name Bash, status completed, result set.
    - Result before start still yields name after both applied (out-of-order).
    - Legacy tool_use + tool_result array blocks merge same as Cursor wire.
    - reconcileSessionToolCalls scans messages and matches ingest upsert outcome.
    - merge never downgrades non-placeholder name to empty.
  </behavior>
  <action>
    Create hub/src/sync/toolCallProjection.ts exporting: ToolCallEvent union (start | result), extractToolCallEventsFromMessageContent(content, messageAt) per RESEARCH Pattern 1 (Cursor AGENT_MESSAGE_PAYLOAD_TYPE tool-call/tool-call-result plus legacy tool_use/tool_result arrays), mergeToolCallProjection(prev | null, event) pure function per RESEARCH merge table (status mapping: failed when is_error else completed on result; startedAt min timestamp; completedAt on result), reconcileSessionToolCalls(sessionId, store) that loads all session messages via store.messages.getMessages or existing scan helper, applies extract+merge, upserts each callId via store.toolCalls. Export collectCallIdsFromDecryptedMessages(messages) for page enrichment (mirror web collectToolIdsFromMessages logic on DecryptedMessage content). Add toolCallProjection.test.ts with fixtures for Cursor wire and legacy array blocks. No ingest hook yet (plan 03).
  </action>
  <verify>
    <automated>cd hub && bun test src/sync/toolCallProjection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: out-of-order result then start converges to completed projection with correct name.
    - Test assertion: duplicate start events are idempotent.
    - Test assertion: legacy tool_use/tool_result fixture merges identically to Cursor wire fixture.
    - Test assertion: reconcileSessionToolCalls writes same row as sequential ingest merge for fixture session.
    - Source assertion: extract function does not mutate message content (D-03).
  </acceptance_criteria>
  <done>Hub merge rules are tested independent of socket ingest.</done>
</task>

<task type="auto">
  <name>[BLOCKING] Schema push: Hub restart and DB rebuild policy</name>
  <files>hub/README.md</files>
  <read_first>
    - hub/README.md (HAPI_HOME, DB_PATH)
    - hub/src/store/index.ts (buildSchemaMismatchError text)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-RESEARCH.md (Pitfall 1)
    - AGENTS.md (no backward compat)
  </read_first>
  <action>
    After schema v12 lands, update hub/README.md Database section: document that SCHEMA_VERSION 12 requires existing deployments to stop Hub, back up ${HAPI_HOME}/hapi.db if needed, remove or rebuild the database file, then restart Hub so createSchema runs fresh (no runtime migration). Reference fail-fast mismatch error users will see if they skip rebuild. Note reconcileSessionToolCalls backfills tool_calls from existing messages on first getMessagesPage after upgrade. Do not add silent ALTER in initSchema.
  </action>
  <verify>
    <automated>grep -E 'SCHEMA_VERSION|version 12|rebuild' hub/README.md | grep -v '^#' | wc -l | awk '{exit ($1 >= 2 ? 0 : 1)}' && cd hub && bun test src/store/namespace.test.ts src/store/index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: hub/README.md mentions schema version 12 and rebuild/restart steps for existing DB_PATH.
    - Source assertion: README states no runtime migration (matches initSchema throw behavior).
    - Test assertion: namespace.test.ts and index.test.ts pass with version 12 expectations.
  </acceptance_criteria>
  <done>Maintainer has explicit instructions before running Hub against an old on-disk DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite tool_calls.projection column | Persisted JSON from agent-derived merge |
| Hub store read path | Serves projection to API consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-01 | Tampering | extractToolCallEventsFromMessageContent | mitigate | Type guards + unwrapRoleWrappedRecordEnvelope; Zod before upsert |
| T-01.2-03 | Denial of Service | upsertToolCall | mitigate | Reject projection JSON over 65536 bytes at upsert |
| T-01.2-04 | Information Disclosure | safeParse on read | mitigate | Skip invalid rows instead of serving corrupt JSON |
| T-01.2-SC | Tampering | schema file deploy | accept | Single-user; rebuild documented in README |

</threat_model>

<verification>
- cd hub && bun test src/store/toolCalls.test.ts src/sync/toolCallProjection.test.ts src/store/namespace.test.ts
- bun run typecheck
</verification>

<success_criteria>
- SCHEMA_VERSION 12 with tool_calls table and ToolCallStore wired on Store.
- Merge/extract/reconcile unit tests green.
- README documents Hub restart and DB rebuild for existing installs.
</success_criteria>

<output>
Create `.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-02-SUMMARY.md` when done
</output>
