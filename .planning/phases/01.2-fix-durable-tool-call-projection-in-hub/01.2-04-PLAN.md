---
phase: 01.2-fix-durable-tool-call-projection-in-hub
plan: 04
type: execute
wave: 2
depends_on:
  - 01.2-03
files_modified:
  - web/src/lib/toolProjectionStore.ts
  - web/src/lib/toolProjectionStore.test.ts
  - web/src/lib/messageWindowPaginationService.ts
  - web/src/hooks/useSSE.ts
  - web/src/chat/reducer.ts
  - web/src/chat/reducerTimeline.ts
  - web/src/chat/reducerTimeline.test.ts
  - web/src/pages/SessionChat.tsx
autonomous: true
requirements:
  - BUG-TOOL-01
must_haves:
  truths:
    - "Web keeps session-scoped projection map outside trimmed message window (D-09, Pitfall 4)."
    - "Pagination merges per-page toolCalls into client map (D-06)."
    - "SSE tool-call-projection-updated patches map before timeline render (D-08, D-09)."
    - "Result-only timeline seeds real tool name from projection, not Tool placeholder (D-10, D-11, D-12, SPEC-4)."
  artifacts:
    - path: "web/src/lib/toolProjectionStore.ts"
      provides: "mergePageToolCalls, patchProjection, getProjectionsForSession"
    - path: "web/src/chat/reducerTimeline.ts"
      provides: "projection-aware ensureToolBlock seed"
  key_links:
    - from: "web/src/lib/messageWindowPaginationService.ts"
      to: "toolProjectionStore"
      via: "mergePageToolCalls after getMessages"
      pattern: "mergePageToolCalls"
    - from: "web/src/chat/reducer.ts"
      to: "reducerTimeline"
      via: "projectionsByCallId in context"
      pattern: "projectionsByCallId"
---

<objective>
Consume Hub tool call projections in the Web message window and timeline reducer.

Purpose: Fix generic Tool cards when the visible window is result-only (SPEC-4, SPEC-5) without changing ToolCard UI or happy-path pairing (SPEC-6, D-12).
Output: toolProjectionStore, pagination/SSE merge, reducer context wiring, and reducer tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-03-SUMMARY.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md
@.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-PATTERNS.md
@web/src/lib/messageWindowState.ts
@web/src/lib/messageWindowPaginationService.ts
@web/src/hooks/useSSE.ts
@web/src/chat/reducer.ts
@web/src/chat/reducerTimeline.ts
@web/src/chat/reducerTools.ts
@web/src/chat/reducerTimeline.test.ts
@web/src/pages/SessionChat.tsx
</context>

<execution_preconditions>
- Run GitNexus `impact` on reduceChatBlocks, reduceTimeline, and useSSE before edits.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session projection store and pagination merge</name>
  <files>web/src/lib/toolProjectionStore.ts, web/src/lib/toolProjectionStore.test.ts, web/src/lib/messageWindowPaginationService.ts</files>
  <read_first>
    - web/src/lib/messageWindowState.ts (module-private Maps pattern)
    - web/src/lib/messageWindowPaginationService.ts (fetchLatestMessages, backfillColdLoadMessages)
    - web/src/api/client.ts (getMessages returns MessagesResponse)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md (D-06, D-09)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-RESEARCH.md (A1 message-window not TanStack)
  </read_first>
  <behavior>
    - mergePageToolCalls unions callIds from multiple pages without dropping older entries.
    - clearProjectionsForSession removes map on session teardown.
    - Pagination service calls merge after each api.getMessages response including backfill loop.
  </behavior>
  <action>
    Create web/src/lib/toolProjectionStore.ts with module-private Map sessionId to Record of callId to ToolCallProjection per D-09 and RESEARCH A1 (not queryKeys.messages). Export mergePageToolCalls(sessionId, toolCalls), patchProjection(sessionId, callId, projection), getProjectionsForSession(sessionId), clearProjectionsForSession(sessionId). Import ToolCallProjection from @hapi/protocol. Add toolProjectionStore.test.ts. Update messageWindowPaginationService.ts: after each getMessages response (fetchLatestMessages, fetchOlderMessages, backfillColdLoadMessages combined path), call mergePageToolCalls(sessionId, response.toolCalls ?? {}). When merging combined pages in backfillColdLoadMessages, merge toolCalls maps from older and newer responses. Do not store projections inside trimmed messages array (Pitfall 4).
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/lib/toolProjectionStore.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: mergePageToolCalls retains prior callIds when new page adds different ids.
    - Test assertion: clearProjectionsForSession empties session entry.
    - Source assertion: messageWindowPaginationService invokes mergePageToolCalls on every getMessages success path.
    - Source assertion: no new TanStack queryKeys.messages usage introduced.
  </acceptance_criteria>
  <done>Client holds durable per-session projection map across pagination.</done>
</task>

<task type="auto">
  <name>Task 2: SSE handler and session cleanup</name>
  <files>web/src/hooks/useSSE.ts</files>
  <read_first>
    - web/src/hooks/useSSE.ts (message-received ~389; session-removed handlers)
    - web/src/lib/toolProjectionStore.ts
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
    In useSSE.ts add handler for event.type === 'tool-call-projection-updated' that calls patchProjection(event.sessionId, event.callId, event.projection). Register this branch so projection patch applies when processing events (before or in same tick as message-received for that session). On session-removed and any existing clearMessageWindow path for session, call clearProjectionsForSession(sessionId). Do not invalidate unused queryKeys.messages.
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/hooks/useSSE.test.ts -x 2>/dev/null || cd web && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: useSSE.ts contains tool-call-projection-updated branch calling patchProjection.
    - Source assertion: session removal clears projection store for that sessionId.
    - Source assertion: bun run typecheck passes for web package.
  </acceptance_criteria>
  <done>Live SSE keeps projection map current without full message refetch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reducer projectionsByCallId and result-only seed path</name>
  <files>web/src/chat/reducer.ts, web/src/chat/reducerTimeline.ts, web/src/chat/reducerTimeline.test.ts, web/src/pages/SessionChat.tsx</files>
  <read_first>
    - web/src/chat/reducer.ts (permissionsById assembly ~97-118)
    - web/src/chat/reducerTimeline.ts (result-only ensureToolBlock ~315-326)
    - web/src/chat/reducerTools.ts (ensureToolBlock placeholder rules, collectToolIdsFromMessages)
    - web/src/chat/reducerTimeline.test.ts (invokedAt preservation ~281-325)
    - .planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-CONTEXT.md (D-10, D-11, D-12)
  </read_first>
  <behavior>
    - Result-only normalized tool-result with projection map entry renders name Read not Tool.
    - When start+result both in window, behavior matches existing tests (SPEC-6).
    - invokedAt on result-only seed uses projection.startedAt for createdAt; does not pass result message invokedAt on first seed (existing test contract).
    - permissionsById still supplies approval metadata when projection lacks permission.
  </behavior>
  <action>
    Extend reduceTimeline context type with projectionsByCallId Map string to ToolCallProjection per D-10. In reducer.ts reduceChatBlocks (or equivalent entry), build Map from getProjectionsForSession(sessionId) and pass into reducerContext alongside permissionsById. In reducerTimeline.ts tool-result branch (~315): const projection = context.projectionsByCallId.get(c.tool_use_id); seed ensureToolBlock with name: projection?.name ?? permissionEntry?.toolName ?? 'Tool', input: projection?.input ?? permissionEntry?.input ?? null, createdAt: projection?.startedAt ?? msg.createdAt; omit invokedAt on result-only first seed per reducerTimeline.test.ts. Keep permission merge logic unchanged for approval UX (D-11). Wire SessionChat.tsx (or hook that calls reduceChatBlocks) to pass sessionId so projections resolve. Add reducerTimeline.test.ts case: single tool-result message + projectionsByCallId Map with Bash name → block.tool.name === 'Bash'. Do not change toolGroups.ts or ToolCard components.
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/chat/reducerTimeline.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: result-only + projection map yields non-Tool name.
    - Test assertion: existing invokedAt preservation test still passes without modification unless projection fixture added intentionally.
    - Test assertion: start+result in-window case in reducerTimeline.test.ts unchanged behavior.
    - Source assertion: permissionsById still built via getPermissions(agentState).
    - Source assertion: SessionChat passes projections into reduceChatBlocks path.
  </acceptance_criteria>
  <done>Timeline renders canonical tool identity from Hub projection on result-only windows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub API/SSE → Web toolProjectionStore | Projection JSON from network |
| Reducer → UI | Rendered tool name/input shown to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-01 | Tampering | toolProjectionStore | mitigate | Trust Hub-validated payloads; optional client-side schema parse if SSE raw |
| T-01.2-06 | Spoofing | SSE event sessionId | accept | Tailscale single-user; existing session SSE trust model |
| T-01.2-04 | Information Disclosure | projection map in memory | accept | Session-scoped; cleared on session removed |

</threat_model>

<verification>
- cd web && bunx vitest run src/lib/toolProjectionStore.test.ts src/chat/reducerTimeline.test.ts -x
- bun run typecheck
</verification>

<success_criteria>
- Pagination and SSE populate session projection map.
- Result-only reducer path shows real tool name and input from projection.
</success_criteria>

<output>
Create `.planning/phases/01.2-fix-durable-tool-call-projection-in-hub/01.2-04-SUMMARY.md` when done
</output>
