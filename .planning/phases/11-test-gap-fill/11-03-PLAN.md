---
phase: 11-test-gap-fill
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - hub/src/web/test-utils/assertNoSecretLeak.ts
  - hub/src/web/routes/auth.test.ts
  - hub/src/web/middleware/auth.test.ts
autonomous: true
requirements: [REFT-03]

must_haves:
  truths:
    - "Route `POST /api/auth` returns 400 `{ error: 'Invalid body' }` for non-JSON body, empty `{}`, and non-string `accessToken` (D-185 route layer, RESEARCH § M3 route table)"
    - "Route `POST /api/auth` returns 401 `{ error: 'Invalid access token' }` for `{ accessToken: '' }` and `{ accessToken: 'wrong' }` (RESEARCH ground-truth correction: `''` is 401 NOT 400 because `parseAccessToken('')` returns null after trim — adopted by orchestrator override 2026-05-23)"
    - "Route `POST /api/auth` returns 200 with a `jose.jwtVerify`-decodable HS256 JWT whose payload `uid === ownerId` for the correct token"
    - "Middleware short-circuits on path `/api/auth` (calls `next()` without checking Authorization) and the test pins this so future refactors cannot regress login (RESEARCH § M3 middleware row 1)"
    - "Middleware returns 401 `{ error: 'Missing authorization token' }` for missing header and for `Bearer ` with empty token (D-185 middleware bullets 1–2, RESEARCH § M3)"
    - "Middleware returns 401 `{ error: 'Invalid token' }` for tampered signature, expired JWT (replay-after-expiry per D-184), wrong alg (RS256 → rejected by HS256 allow-list), forged `alg:'none'` (jose rejects), and the wrong-secret JWT (D-185 middleware bullets 3–5)"
    - "Middleware returns 401 `{ error: 'Invalid token payload' }` for a valid HS256 JWT whose payload has no `uid` field — replaces the dropped `uid != ownerId` case per orchestrator override 2026-05-23 (middleware does not enforce uid equality, single-user Tailscale per D-184)"
    - "Every failure response body AND every captured `console.{log,warn,error}` line is free of `cliApiToken` and the base64 representation of `jwtSecret` (D-186, via `assertNoSecretLeak`)"
    - "JWT fixtures are produced by module-local `make*` factories `makeValidJwt / makeExpiredJwt / makeTamperedJwt / makeWrongAlgJwt / makeAlgNoneJwt` (D-187)"
    - "No JWT replay-detection (blacklist / nonce / single-use) is added; 'replayed JWT' = expired-JWT-resubmitted (D-184)"
    - "`assertNoSecretLeak.ts` lives at `hub/src/web/test-utils/assertNoSecretLeak.ts`, is NOT a `.test.ts` file, and imports from `'bun:test'` only (TESTING.md cross-runner anti-pattern)"
  artifacts:
    - path: "hub/src/web/test-utils/assertNoSecretLeak.ts"
      provides: "Shared no-leak invariant helper used by REFT-03 (D-186)"
      exports: ["assertNoSecretLeak"]
      contains: "export function assertNoSecretLeak"
    - path: "hub/src/web/routes/auth.test.ts"
      provides: "REFT-03 route-layer negative + positive cases via Hono `app.request()`"
      contains: "createAuthRoutes"
    - path: "hub/src/web/middleware/auth.test.ts"
      provides: "REFT-03 middleware-layer negative cases + path-skip pin"
      contains: "createAuthMiddleware"
  key_links:
    - from: "hub/src/web/routes/auth.test.ts"
      to: "hub/src/web/routes/auth.ts::createAuthRoutes"
      via: "Hono `app.request()` per RESEARCH § Pattern 3"
      pattern: "createAuthRoutes\\s*\\("
    - from: "hub/src/web/middleware/auth.test.ts"
      to: "hub/src/web/middleware/auth.ts::createAuthMiddleware"
      via: "mount on throwaway `Hono<WebAppEnv>` + `app.request()`"
      pattern: "createAuthMiddleware\\s*\\("
    - from: "hub/src/web/routes/auth.test.ts + middleware/auth.test.ts"
      to: "hub/src/web/test-utils/assertNoSecretLeak.ts"
      via: "relative import `../test-utils/assertNoSecretLeak`"
      pattern: "assertNoSecretLeak"
---

<objective>
Add REFT-03 negative-case coverage for `hub/src/web/routes/auth.ts` and
`hub/src/web/middleware/auth.ts`, plus the shared `assertNoSecretLeak`
helper they both consume. Every 4xx response and every captured log line
is asserted free of the access-token and JWT secret. No production code
changes; this is purely additive.

Purpose: Closes REFT-03 and ROADMAP §Phase 11 Success Criterion #3
("auth route negative-case tests cover bad token, expired JWT, replayed
JWT, and empty body — every case returns the expected 4xx status without
leaking secrets in the response body or logs").

Output: Three new files under `hub/src/web/` — one helper and two test
files. No edits to `auth.ts` (route or middleware) per D-190.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-test-gap-fill/11-CONTEXT.md
@.planning/phases/11-test-gap-fill/11-RESEARCH.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md

# SUTs (DO NOT modify in this plan — D-190)
@hub/src/web/routes/auth.ts
@hub/src/web/middleware/auth.ts
@hub/src/utils/accessToken.ts
@hub/src/utils/crypto.ts

# Pattern references
@hub/src/web/routes/cli.test.ts
@hub/src/web/routes/machines.test.ts

<interfaces>
<!-- Extracted from SUTs in planning phase -->
From `hub/src/web/middleware/auth.ts`:
- `export type WebAppEnv = { Variables: { userId: number; engine: SyncEngine; session: Session; machine: Machine; body: unknown } }`
- `export function createAuthMiddleware(jwtSecret: Uint8Array): MiddlewareHandler<WebAppEnv>`
- Path `/api/auth` short-circuits without checking Authorization.
- Token source order: `Authorization: Bearer <token>` → fallback to `?token=` only when path is `/api/events`.
- Verifies with `jwtVerify(token, jwtSecret, { algorithms: ['HS256'] })`, then validates payload via `z.object({ uid: z.number() })`.
- Error bodies: `{ error: 'Missing authorization token' }`, `{ error: 'Invalid token' }`, `{ error: 'Invalid token payload' }` — all 401.

From `hub/src/web/routes/auth.ts`:
- `export function createAuthRoutes(jwtSecret: Uint8Array, cliApiToken: string, ownerId: number): Hono<WebAppEnv>`
- `POST /auth` (mount with `app.route('/api', createAuthRoutes(...))` → effective path `/api/auth`).
- Body schema `z.object({ accessToken: z.string() })` — accepts empty string (NOT `.min(1)`).
- Token check: `parsedToken = parseAccessToken(accessToken)` then `constantTimeEquals(parsedToken, cliApiToken)`. `parseAccessToken('')` returns null after trim → 401 path.
- Success: signs HS256 JWT with `{ uid: ownerId }`, `setExpirationTime('4h')`. Response body: `{ token, user: { id: ownerId, username: undefined, firstName: 'Web User', lastName: undefined } }`.
- Error bodies: 400 `{ error: 'Invalid body' }`, 401 `{ error: 'Invalid access token' }`.

Test runner: `bun:test` (TESTING.md). NEVER import `vitest` or `vi`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External HTTP client → `/api/auth` | Untrusted JSON body, untrusted accessToken string crosses here. |
| External HTTP client → `/api/*` (any path except `/api/auth`) | Untrusted `Authorization: Bearer <jwt>` (and `?token=` for `/api/events`) crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing | `createAuthMiddleware` JWT verification | mitigate | Test asserts `jwtVerify(...,{ algorithms: ['HS256'] })` rejects tampered-signature, wrong-alg (RS256), and forged `alg:'none'` tokens with 401 `{ error: 'Invalid token' }`. (ASVS V2/V6) |
| T-11-02 | Spoofing / Repudiation | Replay of expired JWT | mitigate (rely on `jose` `exp`) | Test signs JWT with `setExpirationTime(<past>)` then submits — asserts 401. No replay-detection added (D-184: single-user Tailscale posture). (ASVS V2/V3) |
| T-11-03 | Tampering | Forged `alg:'none'` JWT | mitigate | Hand-construct `header.payload.` (jose refuses to sign `'none'`) and assert 401 `{ error: 'Invalid token' }`. (ASVS V6) |
| T-11-04 | Information Disclosure | Secret echoed in error response body | mitigate | `assertNoSecretLeak(responseBody, [], [cliApiToken, jwtSecretBase64])` on every 4xx path of route + middleware. (ASVS V7) |
| T-11-05 | Information Disclosure | Secret logged via `console.{log,warn,error}` during error handling | mitigate | Per-test `captureConsole()` → restore in cleanup; `assertNoSecretLeak(responseBody, capturedLogs, [...])` includes log lines. (ASVS V7) |
| T-11-06 | Spoofing | Input validation bypass (non-JSON body, missing/wrong-type `accessToken`) | mitigate | Route layer test asserts 400 `{ error: 'Invalid body' }` for non-JSON body, `{}`, and `{ accessToken: 123 }`. (ASVS V5) |
| T-11-07 | Spoofing | Empty-string accessToken accepted by `z.string()` but rejected at `parseAccessToken` | accept (current behavior) | Test pins behavior: `{ accessToken: '' }` returns 401 `{ error: 'Invalid access token' }` (NOT 400). Documented as-is; tightening to `z.string().min(1)` is scope creep forbidden by D-190. |
| T-11-08 | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH § Package Legitimacy Audit: skipped — zero installs). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create assertNoSecretLeak helper at hub/src/web/test-utils/assertNoSecretLeak.ts</name>
  <files>hub/src/web/test-utils/assertNoSecretLeak.ts</files>
  <read_first>
    - .planning/phases/11-test-gap-fill/11-RESEARCH.md § M3 → "Helper sketch" and recommended location rationale
    - .planning/codebase/TESTING.md § Bun test pattern (hub / shared) and § Anti-patterns ("Importing `vitest` in `hub/` ... is anti-pattern")
    - hub/src/web/routes/cli.test.ts (existing `bun:test` import header — copy this style)
  </read_first>
  <behavior>
    - For every secret in `secrets`, `expect(responseBody).not.toContain(secret)`.
    - For every secret, iterate `capturedLogs` and `expect(line).not.toContain(secret)`.
    - Falsy/empty secrets in the input array are skipped (defensive; prevents accidental `''.includes('')` true positives).
    - No state, no I/O, no side effects beyond `expect()` calls.
  </behavior>
  <action>
    Create new directory `hub/src/web/test-utils/` and new file
    `hub/src/web/test-utils/assertNoSecretLeak.ts`.

    File contents shape (per RESEARCH § M3 helper sketch — adapt formatting to
    CONVENTIONS.md but keep the logic identical):
    - Import `expect` from `'bun:test'`. NEVER `'vitest'`.
    - Export `function assertNoSecretLeak(responseBody: string, capturedLogs: string[], secrets: ReadonlyArray<string>): void`.
    - Body: outer `for (const secret of secrets) { if (!secret) continue; expect(responseBody).not.toContain(secret); for (const line of capturedLogs) expect(line).not.toContain(secret); }`.
    - JSDoc above the function: anchor `REFT-03` and `D-186`, document parameter intent (response body string, captured `console.*` lines, secrets to scan for).

    Naming: file MUST be `assertNoSecretLeak.ts` (no `.test` suffix — it is a
    helper, not a test, and `bun test` would otherwise try to discover it).
    Location MUST be `hub/src/web/test-utils/` (RESEARCH § M3 recommendation;
    `hub/src/test-utils/` is deferred per CONTEXT § Deferred).

    No production code touched. No new dependencies.
  </action>
  <verify>
    <automated>test -f hub/src/web/test-utils/assertNoSecretLeak.ts &amp;&amp; cd hub &amp;&amp; bun -e "import('./src/web/test-utils/assertNoSecretLeak.ts').then(m =&gt; { if (typeof m.assertNoSecretLeak !== 'function') throw new Error('export missing'); console.log('helper exports OK'); })"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exactly `hub/src/web/test-utils/assertNoSecretLeak.ts` (no `.test` in the name).
    - Exports a named function `assertNoSecretLeak` with signature `(responseBody: string, capturedLogs: string[], secrets: ReadonlyArray<string>) => void`.
    - `grep -E "from\s+'vitest'" hub/src/web/test-utils/assertNoSecretLeak.ts` returns no matches; `grep -E "from\s+'bun:test'" ...` returns exactly one match for `expect`.
    - `cd hub && bun typecheck` exits 0.
    - File contains JSDoc string `REFT-03` and `D-186` (anchoring for future readers).
    - `git diff --stat HEAD` shows only the new file added.
  </acceptance_criteria>
  <done>Helper exists, exports correctly, imports only from `bun:test`, hub typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create hub/src/web/routes/auth.test.ts (REFT-03 route layer)</name>
  <files>hub/src/web/routes/auth.test.ts</files>
  <read_first>
    - hub/src/web/routes/auth.ts (SUT — exact 400/401/200 paths and body shapes)
    - hub/src/web/routes/cli.test.ts (existing `app.request()` pattern reference)
    - hub/src/web/routes/machines.test.ts (additional bun:test conventions)
    - hub/src/utils/accessToken.ts and hub/src/utils/crypto.ts (confirm `parseAccessToken('')` trims to null and `constantTimeEquals` is the comparator)
    - hub/src/web/test-utils/assertNoSecretLeak.ts (the helper from Task 1 — relative import path `../test-utils/assertNoSecretLeak`)
    - .planning/phases/11-test-gap-fill/11-RESEARCH.md § M3 route table and § Pattern 3 (route example)
    - .planning/phases/11-test-gap-fill/11-CONTEXT.md D-185 + D-186 + D-187
  </read_first>
  <behavior>
    Route-layer cases via `app.request('/api/auth', …)` on a Hono app mounted with `app.route('/api', createAuthRoutes(jwtSecret, cliApiToken, ownerId))`:

    - `it('rejects non-JSON body with 400 Invalid body + no leak')` — `body: 'not json'` + `content-type: application/json` → 400 `{ error: 'Invalid body' }`.
    - `it('rejects empty {} body with 400 Invalid body + no leak')` — `body: '{}'` → 400 `{ error: 'Invalid body' }`.
    - `it('rejects non-string accessToken (number) with 400 Invalid body + no leak')` — `body: JSON.stringify({ accessToken: 123 })` → 400.
    - `it('rejects empty-string accessToken with 401 Invalid access token + no leak (parseAccessToken trims to null)')` — `body: JSON.stringify({ accessToken: '' })` → 401 `{ error: 'Invalid access token' }`. (RESEARCH § M3 correction adopted by orchestrator override 2026-05-23.)
    - `it('rejects wrong accessToken with 401 Invalid access token + no leak')` — `body: JSON.stringify({ accessToken: 'wrong-token-marker' })` → 401.
    - `it('accepts correct accessToken — returns 200 with HS256 JWT decodable to { uid: ownerId }')` — `body: JSON.stringify({ accessToken: cliApiToken })` → 200. Use `jose.jwtVerify(token, jwtSecret, { algorithms: ['HS256'] })` to confirm `protectedHeader.alg === 'HS256'` AND `payload.uid === ownerId`.
    - `it('does not leak secrets via console during any failure case')` — wraps each failing case above with `captureConsole()` and passes the captured array to `assertNoSecretLeak`. (Can be folded into each failure-case `it` rather than a separate one — implementer's choice as long as every failure case has its logs asserted.)
  </behavior>
  <action>
    Create new file `hub/src/web/routes/auth.test.ts`. Implementation requirements:

    - Imports (header MUST mirror cli.test.ts):
      - `import { describe, expect, it, spyOn } from 'bun:test'`
      - `import { Hono } from 'hono'`
      - `import { jwtVerify } from 'jose'`
      - `import { createAuthRoutes } from './auth'`
      - `import { assertNoSecretLeak } from '../test-utils/assertNoSecretLeak'`
    - Module-local fixtures (per D-187 `make*` pattern; declare BEFORE describe):
      - `const jwtSecret = new TextEncoder().encode('test-secret-with-enough-entropy-for-hs256-do-not-leak-marker')`
      - `const cliApiToken = 'cli-token-xyz-do-not-leak-marker'` — long, unique, non-base64-alphabet (Pitfall 2: avoid substring collision with the JWT in success-path assertions).
      - `const ownerId = 42`
      - `function makeApp(): Hono` — returns `new Hono().route('/api', createAuthRoutes(jwtSecret, cliApiToken, ownerId))`.
      - `function captureConsole(): { logs: string[]; restore: () => void }` per RESEARCH § M3 — `spyOn` `log`, `warn`, `error` and push joined args into `logs`; `restore()` calls `mockRestore()` on each spy.
    - One outer `describe('POST /api/auth (REFT-03 route)', …)` block with the 6 cases enumerated above.
    - Every test MUST set `headers: { 'content-type': 'application/json' }` on `app.request()` calls that send JSON bodies (RESEARCH § Pitfall 4).
    - The no-leak secrets array MUST be `[cliApiToken, new TextDecoder().decode(jwtSecret)]` so we scan for both the access token AND the JWT secret in raw form.
    - Failure-path tests MUST wrap the request call between `const cap = captureConsole(); try { … } finally { cap.restore() }` and pass `cap.logs` to `assertNoSecretLeak`.
    - Success-path test asserts `await jwtVerify(token, jwtSecret, { algorithms: ['HS256'] })` succeeds, `payload.uid === ownerId`, `protectedHeader.alg === 'HS256'`. No-leak check for the success path is OPTIONAL but recommended on the body (the JWT itself contains base64 payload, not the secret — Pitfall 2 mitigated by the marker token shape).
    - File MUST NOT touch any production file. NEVER import `vitest` or `vi`.
  </action>
  <verify>
    <automated>cd hub &amp;&amp; bun test src/web/routes/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd hub && bun test src/web/routes/auth.test.ts` exits 0 with ≥ 6 passing tests.
    - `cd hub && bun typecheck` exits 0.
    - `grep -E "from\s+'vitest'" hub/src/web/routes/auth.test.ts` returns no matches.
    - The success-path test successfully calls `jwtVerify` with `algorithms: ['HS256']` and asserts `payload.uid === ownerId` (use `bun test src/web/routes/auth.test.ts --reporter=text` to confirm test names in output).
    - Every failure-path test calls `assertNoSecretLeak` with secrets including BOTH `cliApiToken` AND the decoded `jwtSecret`. Confirm via `grep -c assertNoSecretLeak hub/src/web/routes/auth.test.ts` ≥ 5.
    - `git diff --stat HEAD` shows only `hub/src/web/routes/auth.test.ts` added (plus the Task 1 helper from the prior commit); `hub/src/web/routes/auth.ts` unchanged.
  </acceptance_criteria>
  <done>REFT-03 route layer green; every failure case asserts no secret leak in body + logs; success path returns a valid HS256 JWT.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create hub/src/web/middleware/auth.test.ts (REFT-03 middleware layer)</name>
  <files>hub/src/web/middleware/auth.test.ts</files>
  <read_first>
    - hub/src/web/middleware/auth.ts (SUT — exact 401 paths, payload schema, `/api/auth` short-circuit, `/api/events` query fallback)
    - hub/src/web/test-utils/assertNoSecretLeak.ts (Task 1 helper — `../test-utils/assertNoSecretLeak`)
    - .planning/phases/11-test-gap-fill/11-RESEARCH.md § M3 middleware table + § Code Examples middleware sketch + § Pitfall 1 (cross-runner import drift)
    - .planning/phases/11-test-gap-fill/11-CONTEXT.md D-184 / D-185 / D-187
  </read_first>
  <behavior>
    Middleware cases via mounting `createAuthMiddleware(jwtSecret)` on a throwaway `Hono<WebAppEnv>` app + `app.get('/api/ping', c => c.json({ ok: true }))` and calling `app.request(...)`:

    - `it('skips auth for path /api/auth (short-circuit; pins login endpoint behavior)')` — `app.post('/api/auth', c => c.json({ skipped: true }))` mounted; `app.request('/api/auth', { method: 'POST' })` with NO Authorization header MUST hit the handler (asserts middleware does not 401 the login path). Pins the early-return at lines 22–26 of auth.ts.
    - `it('returns 401 Missing authorization token when no header present on /api/ping')` — no Authorization → 401 `{ error: 'Missing authorization token' }`.
    - `it('returns 401 Missing authorization token for Bearer with empty token')` — `Authorization: 'Bearer '` (trailing space, empty slice) → 401 `{ error: 'Missing authorization token' }`.
    - `it('returns 401 Invalid token for tampered signature')` — `makeTamperedJwt()` → 401 `{ error: 'Invalid token' }`.
    - `it('returns 401 Invalid token for expired JWT (replay-after-expiry; D-184)')` — `makeExpiredJwt()` → 401 `{ error: 'Invalid token' }`.
    - `it('returns 401 Invalid token for wrong-alg JWT (RS256 vs HS256 allow-list)')` — `makeWrongAlgJwt()` (a JWT signed by some non-HS algorithm, OR equivalently a JWT whose alg claim is `RS256` but signed with HS key) → 401 `{ error: 'Invalid token' }`.
    - `it('returns 401 Invalid token for forged alg:none')` — `makeAlgNoneJwt()` (hand-constructed `header.payload.` per RESEARCH § Don't Hand-Roll — jose refuses to sign `'none'`) → 401 `{ error: 'Invalid token' }`.
    - `it('returns 401 Invalid token payload for valid HS256 JWT with no uid field (replaces uid != ownerId case per orchestrator override 2026-05-23)')` — sign a valid HS256 JWT with payload `{ foo: 'bar' }` → middleware accepts the signature but `jwtPayloadSchema.safeParse` rejects → 401 `{ error: 'Invalid token payload' }`. This replaces the dropped `uid != ownerId → 401` case from D-185 because the middleware does not enforce uid equality (per RESEARCH § M3 ⚠ Correction + orchestrator override).
    - `it('falls back to ?token= query param on /api/events and rejects bad token')` — `app.get('/api/events', c => c.json({ ok: true }))`; `app.request('/api/events?token=garbage')` → 401 `{ error: 'Invalid token' }` (pins the query-fallback at lines 30–31 of auth.ts; protects against future middleware refactors).
    - (Optional positive control) `it('passes valid HS256 JWT with { uid } payload through to next()')` — `makeValidJwt(1)` → 200 `{ ok: true }` from the dummy `/api/ping` handler (proves the rejection path isn't masking a green-on-all-failure bug).
  </behavior>
  <action>
    Create new file `hub/src/web/middleware/auth.test.ts`.

    Imports (mirror cli.test.ts header style):
    - `import { describe, expect, it, spyOn } from 'bun:test'`
    - `import { Hono } from 'hono'`
    - `import { SignJWT } from 'jose'`
    - `import { createAuthMiddleware, type WebAppEnv } from './auth'`
    - `import { assertNoSecretLeak } from '../test-utils/assertNoSecretLeak'`

    Module-local fixtures (D-187 `make*` pattern, declared before describe):
    - `const jwtSecret = new TextEncoder().encode('test-secret-with-enough-entropy-for-hs256-do-not-leak-marker')`
    - `async function makeValidJwt(uid: number = 1, ttl: string = '4h'): Promise<string>` — `new SignJWT({ uid }).setProtectedHeader({ alg: 'HS256' }).setIssuedAt().setExpirationTime(ttl).sign(jwtSecret)`.
    - `async function makeExpiredJwt(uid: number = 1): Promise<string>` — `setIssuedAt(Math.floor(Date.now()/1000) - 86400)` + `setExpirationTime(Math.floor(Date.now()/1000) - 60)` + `.sign(jwtSecret)`.
    - `async function makeTamperedJwt(uid: number = 1): Promise<string>` — sign valid, split on `.`, replace last 2 chars of signature segment with `AA`.
    - `async function makeWrongAlgJwt(uid: number = 1): Promise<string>` — sign with a different HS variant or build by signing with HS256 then rewriting the `alg` header to `RS256` (re-encode header base64url before re-joining). The intent is to land on the `jwtVerify` algorithm-mismatch rejection path; if HS256→HS512 re-sign is simpler, that also satisfies "wrong alg" — implementer chooses whichever lands on a deterministic 401. Document the chosen variant in a comment.
    - `function makeAlgNoneJwt(uid: number = 1): string` — `${b64url(JSON.stringify({alg:'none',typ:'JWT'}))}.${b64url(JSON.stringify({uid}))}.` (empty signature segment; jose refuses to sign `'none'` so we hand-construct).
    - `async function makePayloadWithoutUidJwt(): Promise<string>` — `new SignJWT({ foo: 'bar' }).setProtectedHeader({ alg: 'HS256' }).setExpirationTime('1h').sign(jwtSecret)`.
    - `function mount(): Hono<WebAppEnv>` — returns app with `createAuthMiddleware(jwtSecret)` mounted on `/api/*`, plus dummy handlers for `/api/auth`, `/api/ping`, `/api/events`.
    - `function captureConsole()` — same shape as Task 2.
    - Helper `function b64url(s: string): string` for the `alg:'none'` constructor (use `Buffer.from(s).toString('base64url')`).

    Test structure: one outer `describe('createAuthMiddleware (REFT-03)', …)` containing one `it` per case in `<behavior>`.

    Every failure-path test MUST wrap the request in `captureConsole()` and call `assertNoSecretLeak(await res.text(), cap.logs, [new TextDecoder().decode(jwtSecret)])`. (`cliApiToken` is not in scope at the middleware layer — only the JWT secret matters here.)

    NEVER import `vitest` or `vi`. NEVER touch production code.

    File-level JSDoc anchors REFT-03, D-184 (no replay-detection), D-185 (two-layer split), D-186 (no leak), D-187 (make* factories), and the orchestrator override 2026-05-23 (uid != ownerId case dropped).
  </action>
  <verify>
    <automated>cd hub &amp;&amp; bun test src/web/middleware/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd hub && bun test src/web/middleware/auth.test.ts` exits 0 with ≥ 9 passing tests (8 enumerated failure/skip cases + the positive control).
    - `cd hub && bun typecheck` exits 0.
    - `grep -E "from\s+'vitest'" hub/src/web/middleware/auth.test.ts` returns no matches.
    - `grep -c "uid\s*!=\s*ownerId\|ownerId\s*!=\s*uid" hub/src/web/middleware/auth.test.ts` returns 0 (the dropped case must not appear).
    - `grep -c "make\(Valid\|Expired\|Tampered\|WrongAlg\|AlgNone\|PayloadWithoutUid\)Jwt" hub/src/web/middleware/auth.test.ts` returns ≥ 6 (the make* factories from D-187 are present).
    - `grep -c "assertNoSecretLeak" hub/src/web/middleware/auth.test.ts` ≥ 7 (every failure case uses the helper).
    - Test output explicitly contains a test name matching `/skips auth for path \/api\/auth/` (pins the short-circuit).
    - Test output contains a test name matching `/Invalid token payload.*no uid/i` (replaces the dropped case).
    - `git diff --stat HEAD` shows only `hub/src/web/middleware/auth.test.ts` added; `hub/src/web/middleware/auth.ts` unchanged.
  </acceptance_criteria>
  <done>REFT-03 middleware green; no replay-detection added; orchestrator override implemented (uid != ownerId dropped, replaced by no-uid-in-payload case); short-circuit on /api/auth pinned.</done>
</task>

</tasks>

<verification>
- `cd hub && bun test src/web/routes/auth.test.ts src/web/middleware/auth.test.ts` green.
- `cd hub && bun test` green (full hub suite — no regression from new test-utils directory).
- `cd hub && bun typecheck` green.
- `git diff --name-only HEAD~3 HEAD` lists exactly: `hub/src/web/test-utils/assertNoSecretLeak.ts`, `hub/src/web/routes/auth.test.ts`, `hub/src/web/middleware/auth.test.ts`.
- `git diff hub/src/web/routes/auth.ts hub/src/web/middleware/auth.ts hub/src/utils/accessToken.ts hub/src/utils/crypto.ts` is empty (no SUT modifications — D-190).
</verification>

<success_criteria>
REFT-03 closed: route + middleware layers fully covered for negative paths; every failure response and every captured log line is asserted secret-free; no production code changed; no replay-detection added; orchestrator override (drop uid != ownerId case) applied.
</success_criteria>

<output>
Create `.planning/phases/11-test-gap-fill/11-03-SUMMARY.md` recording: test counts per file, helper location chosen (`hub/src/web/test-utils/`) with one-line rationale, the wrong-alg construction variant chosen (HS512-re-sign vs alg-header rewrite), and confirmation that the orchestrator override (drop uid != ownerId case, replace with no-uid-in-payload case) was applied.
</output>
