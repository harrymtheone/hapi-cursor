---
phase: 02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess
plan: 03
type: execute
wave: 2
depends_on:
  - 02.1-01
files_modified:
  - web/src/api/client.ts
  - web/src/hooks/mutations/useSessionActions.ts
  - web/src/hooks/mutations/useSessionActions.test.ts
  - web/src/hooks/queries/useSkills.ts
  - web/src/hooks/queries/useSkills.test.ts
  - web/src/router.tsx
  - web/src/components/AssistantChat/useHappyComposerHandlers.ts
  - web/src/lib/autocompleteMerge.test.ts
autonomous: true
requirements:
  - SKIL-01
  - SKIL-02
  - SKIL-03
must_haves:
  truths:
    - "D-03: Invalid skills excluded from `/` autocomplete and prompt filtering; valid-only suggestions."
    - "D-05: Web ApiClient and useSessionActions expose no skill-policy mutation APIs."
    - "D-07/D-14: useSkills suggests only valid skills with /name keys; no skillPolicy parameter."
    - "D-13: Composer / autocomplete returns merged valid skills (first) plus slash commands; key collision keeps skill."
  artifacts:
    - path: web/src/hooks/queries/useSkills.ts
      provides: getSuggestions without skillPolicy
    - path: web/src/router.tsx
      provides: merged getAutocompleteSuggestions for / prefix
  key_links:
    - from: web/src/router.tsx
      to: web/src/hooks/queries/useSkills.ts
      via: getSkillSuggestions + getSlashSuggestions merge
      pattern: getAutocompleteSuggestions
---

<objective>
Remove Web skill-policy mutations and implement valid-only `/` skill suggestions merged with slash commands.

Purpose: D-05 closes client attack surface; D-13/D-14 fix `$`→`/` and slash-command collision (RESEARCH open question resolved: skills first, commands second, duplicate key keeps skill; set suggestion description to Skill vs Command for scanability).
Output: Stripped ApiClient/useSessionActions, updated useSkills + router autocomplete + handler recency for `/`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-CONTEXT.md
@.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-RESEARCH.md
@.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-PATTERNS.md
@.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-VALIDATION.md
@web/src/hooks/queries/useSkills.ts
@web/src/router.tsx
@web/src/hooks/queries/useSlashCommands.ts
@web/src/api/client.ts
</context>

<execution_preconditions>
- Plan 02.1-01 complete (SkillPolicyState types removed from shared).
- GitNexus `impact` on `useSkills` and `getAutocompleteSuggestions` before edits.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete Web skill-policy API and mutations</name>
  <files>web/src/api/client.ts, web/src/hooks/mutations/useSessionActions.ts, web/src/hooks/mutations/useSessionActions.test.ts</files>
  <read_first>
    - web/src/api/client.ts (setSkillPolicy, applySkillPolicy, resetSkillPolicy)
    - web/src/hooks/mutations/useSessionActions.ts (skillPolicyMutation)
    - web/src/hooks/mutations/useSessionActions.test.ts
    - .planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-CONTEXT.md (D-05)
  </read_first>
  <action>
    Per D-05: Remove async setSkillPolicy, applySkillPolicy, resetSkillPolicy from ApiClient. Remove skillPolicyMutation and exported setSkillPolicy, applySkillPolicy, resetSkillPolicy from useSessionActions; remove isPending branches for policy. Update useSessionActions.test.ts to drop policy mutation cases. Grep web/src for setSkillPolicy|applySkillPolicy|resetSkillPolicy|skill-policy — fix imports (SessionChat handlers removed in plan 04).
  </action>
  <verify>
    <automated>cd /home/harry/projects/hapi-cursor/web && bun run test -- useSessionActions</automated>
    <automated>rg -v '^#' /home/harry/projects/hapi-cursor/web/src/api /home/harry/projects/hapi-cursor/web/src/hooks/mutations | rg 'setSkillPolicy|applySkillPolicy|resetSkillPolicy|skill-policy' | awk '{exit 1}' END{exit 0}</automated>
  </verify>
  <acceptance_criteria>
    - client.ts has no skill-policy HTTP methods.
    - useSessionActions.ts has no skillPolicyMutation.
    - useSessionActions tests pass without policy cases.
  </acceptance_criteria>
  <done>Web cannot mutate session skill policy over HTTP.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useSkills valid-only and / prefix suggestions</name>
  <files>web/src/hooks/queries/useSkills.ts, web/src/hooks/queries/useSkills.test.ts</files>
  <read_first>
    - web/src/hooks/queries/useSkills.ts (toSuggestion, filterSuggestibleSkills, skillPolicy arg)
    - web/src/hooks/queries/useSkills.test.ts
    - shared/src/skillPolicy.ts (isValidSkillForAutocomplete)
    - .planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-UI-SPEC.md (D-13, D-14)
  </read_first>
  <behavior>
    - useSkills(api, sessionId) — two args only.
    - Suggestions use key/text/label `/skillName` not `$skillName`.
    - Invalid skills (valid false) never appear in getSuggestions output.
    - Search term strips leading `/` when query starts with `/`.
  </behavior>
  <action>
    Per D-07 and D-14: Remove skillPolicy parameter from useSkills and getEffectiveSkillSuggestions. Replace filterSuggestibleSkills with filterValidSkills using skill.valid === true (or isValidSkillForAutocomplete from @hapi/protocol). Update toSuggestion to emit `/name` keys. Update searchTerm slice for `/` prefix. Remove imports of SkillPolicyState and isSkillSuggestible. Rewrite useSkills.test.ts: no policy map cases; assert `$` keys absent and `/` keys present; invalid skill excluded from suggestions.
  </action>
  <verify>
    <automated>cd /home/harry/projects/hapi-cursor/web && bun run test -- useSkills</automated>
  </verify>
  <acceptance_criteria>
    - useSkills.ts function signature has no skillPolicy parameter.
    - useSkills.test.ts asserts suggestion.key starts with `/` for valid skill.
    - useSkills.test.ts asserts invalid skill not in getSuggestions result.
  </acceptance_criteria>
  <done>Skill autocomplete data layer is valid-only with / prefix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Merged / autocomplete in router and handlers</name>
  <files>web/src/router.tsx, web/src/components/AssistantChat/useHappyComposerHandlers.ts, web/src/lib/autocompleteMerge.test.ts</files>
  <read_first>
    - web/src/router.tsx (getAutocompleteSuggestions ~356–361)
    - web/src/hooks/queries/useSlashCommands.ts (prefix /)
    - web/src/components/AssistantChat/useHappyComposerHandlers.ts (markSkillUsed $ check)
    - .planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-RESEARCH.md (Pattern 2, open question resolution)
  </read_first>
  <behavior>
    - Query not starting with `/` uses slash suggestions only (non-/ legacy path unchanged if any).
    - Query starting with `/` returns skills then commands; duplicate key keeps first (skill).
    - Skill suggestions have description containing Skill; slash commands description containing Command (or equivalent distinct strings).
    - markSkillUsed runs for skill suggestions only (e.g. suggestion.source === 'builtin' or key matches known skill list), not for slash commands.
  </behavior>
  <action>
    Per D-13: Change router useSkills call to useSkills(api, sessionId) without session.metadata.skillPolicy. Replace getAutocompleteSuggestions branch: when query.startsWith('/'), Promise.all getSkillSuggestions and getSlashSuggestions; concatenate skills first then commands; dedupe by suggestion.key keeping first occurrence. Remove query.startsWith('$') skill branch entirely. In useSkills toSuggestion, set description to include Skill label per UI-SPEC scanability. In useSlashCommands mapping, ensure command suggestions include Command in description (minimal change). Update useHappyComposerHandlers: markSkillUsed when suggestion.text.startsWith('/') AND suggestion is skill (use source === 'builtin' from toSuggestion). Add web/src/lib/autocompleteMerge.test.ts (or colocated router test) with mocked getSkillSuggestions/getSlashSuggestions: both return same key — skill wins; both lists present for `/foo` query. Wave 0 coverage per 02.1-VALIDATION.md.
  </action>
  <verify>
    <automated>cd /home/harry/projects/hapi-cursor/web && bun run test -- autocompleteMerge</automated>
    <automated>rg '\$' /home/harry/projects/hapi-cursor/web/src/hooks/queries/useSkills.ts /home/harry/projects/hapi-cursor/web/src/router.tsx | rg 'skill|Skill' | awk '{exit 1}' END{exit 0}</automated>
  </verify>
  <acceptance_criteria>
    - autocompleteMerge test passes with merged list length = skills + commands minus duplicates.
    - router.tsx has no startsWith('$') skill branch.
    - useHappyComposerHandlers does not call markSkillUsed for slash-only command suggestions.
  </acceptance_criteria>
  <done>Composer / autocomplete serves skills and slash commands without collision regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web → Hub API | Authenticated; policy POST removed (D-05) |
| Composer autocomplete | Untrusted user query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-06 | Tampering | Optimistic skillPolicy PATCH | mitigate | Delete client mutations (D-05) |
| T-02.1-07 | Spoofing | Malicious suggestion insert | accept | Suggestions from server discovery only; user still confirms send |
| T-02.1-08 | Information disclosure | GET /skills | accept | pathHint redaction unchanged in CLI |
| T-02.1-SC | Tampering | Package installs | accept | No new packages |
</threat_model>

<verification>
- `cd web && bun run test -- useSkills useSessionActions autocompleteMerge`
- `rg 'skillPolicy|startsWith\('\''\$'\''\)' web/src/router.tsx web/src/hooks/queries/useSkills.ts` → no policy param / $ skill gate
</verification>

<success_criteria>
- No Web API or mutation path for skill policy.
- Valid skills autocomplete as `/name`; merged with slash commands on `/` queries.
</success_criteria>

<output>
Create `.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-03-SUMMARY.md` when done
</output>
