# Phase 02.1: Skills auto-enable and header skills UI - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `02.1-CONTEXT.md`.

**Date:** 2026-05-26
**Phase:** 02.1 — skills auto-enable and header skills UI
**Areas discussed:** 自动启用时机, 策略/API 收缩, 顶栏入口, 列表与 Global/Local, Composer 补全, Preamble

---

## 1. 自动启用的触发时机

| Option | Description | Selected |
|--------|-------------|----------|
| A | 仅 CLI 启动时 listSkills 一次 | |
| B | 每条用户消息前刷新 listSkills，新 skill 自动加入允许集 | ✓ |
| C | 仅在 agent invoke Skill 工具时写入 | |

**User's choice:** B  
**Notes:** 允许集 = 最近一次 discovery 中 `valid === true` 的 skill；不依赖 skillPolicy 行。

---

## 2. 策略数据与 API 收缩

| Option | Description | Selected |
|--------|-------------|----------|
| A | 完全删除 skillPolicy 字段与 Hub 路由 | ✓ |
| B | 保留字段/路由，Web 只读，拒绝 disabled | |
| C | 保留 tri-state，仅隐藏 UI | |

**User's choice:** A  
**Notes:** 含 shared schema、Hub merge、Web mutations、CLI preamble 文件一并删除。

---

## 3. 顶栏入口与交互形态

| Option | Description | Selected |
|--------|-------------|----------|
| A | SessionHeader Files 旁图标 → 底部 Sheet | ✓ |
| B | 并入 Files 页 Tab | |
| C | Popover + Sheet 双形态 | |

**User's choice:** A  
**Notes:** Tooltip「Skills」；从 composer 移除入口。

---

## 4. 列表内容与 Global / Local

| Option | Description | Selected |
|--------|-------------|----------|
| A | 扁平：名称 + Global/Local 小标签 | ✓ |
| B | 按 Global/Local 分组 | |
| C | 仅名称无标签 | |

**User's choice:** A + invalid 显示红色 Invalid 标签（i18n），不展示 invalidReason 正文。

---

## 5. Composer 与 skill 补全

| Option | Description | Selected |
|--------|-------------|----------|
| A | 全部 valid discovered 可补全；去掉 composer skills 按钮 | ✓ |
| B | 保留 composer 按钮跳转顶栏 | |
| C | 关闭 skill 补全 | |

**User's choice:** A，且前缀改为 **`/`** 而非 **`$`**。

---

## 6. Prompt preamble 与 enforcement 文案

| Option | Description | Selected |
|--------|-------------|----------|
| A | 保留 preamble，列出全部 valid discovered | |
| B | 删除 preamble，完全交给 Cursor | ✓ |
| C | 仅存在显式 enabled 行才注入 | |

**User's choice:** B  
**Notes:** 删除 `skillPolicyPreamble` 及 runCursor 注入路径。

---

## Claude's Discretion

- Skills 图标造型、Sheet 是否保留搜索、settings 页 Global/Local 是否同步改名、组件重命名。

## Deferred Ideas

- 恢复 session 级 disable/enable — 需新 phase，本阶段明确删除。
- Invalid 详情在会话列表展开 — 用户选择仅 Invalid 标签。
