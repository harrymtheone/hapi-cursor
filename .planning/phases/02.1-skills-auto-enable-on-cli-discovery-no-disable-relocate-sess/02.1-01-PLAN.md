---
phase: 02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/schemas.ts
  - shared/src/types.ts
  - shared/src/index.ts
  - shared/src/schemas.test.ts
  - shared/src/skillPolicy.ts
  - shared/src/skillPolicy.test.ts
  - hub/src/store/index.ts
  - hub/src/store/index.test.ts
  - hub/src/sync/sessionMergeService.ts
  - hub/src/sync/sessionMergeService.test.ts
  - hub/src/sync/sessionConfigService.ts
  - hub/src/sync/sessionConfigService.test.ts
  - hub/src/sync/syncEngine.ts
  - hub/src/sync/syncEngineSession.ts
  - hub/src/sync/sessionCache.ts
  - hub/src/web/routes/sessions/skillPolicy.ts
  - hub/src/web/routes/sessions/index.ts
  - hub/src/web/routes/sessions/__tests__/skillPolicy.test.ts
autonomous: true
requirements:
  - SKIL-02
  - SKIL-03
must_haves:
  truths:
    - "D-04: Session metadata wire contract has no skillPolicy field; parsers and writers ignore removed policy shape."
    - "D-05: Hub exposes no POST /sessions/:id/skill-policy; policy mutation surface is gone."
    - "D-04: Hub SQLite SCHEMA_VERSION bumps with fail-fast mismatch (local dev recreates DB)."
  artifacts:
    - path: shared/src/schemas.ts
      provides: MetadataSchema without skillPolicy
    - path: shared/src/skillPolicy.ts
      provides: normalizeSkillSummaryForWire and isValidSkillForAutocomplete only
    - path: hub/src/web/routes/sessions/read.ts
      provides: GET /sessions/:id/skills unchanged
  key_links:
    - from: hub/src/web/routes/sessions/index.ts
      to: hub/src/web/routes/sessions/read.ts
      via: skills GET only (no skillPolicy mount)
      pattern: createSkillPolicyRoutes
---

<objective>
Remove session skill policy from shared protocol and Hub persistence/API; bump Hub schema version per fail-fast policy.

Purpose: D-04–D-05 delete the policy tier so CLI/Web can treat valid discovery as the only allow gate (SKIL-02/03 superseded on mobile).
Output: Stripped `@hapi/protocol` metadata, deleted Hub skill-policy route and merge/write paths, SCHEMA_VERSION 13.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-CONTEXT.md
@.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-RESEARCH.md
@.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-PATTERNS.md
@.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-VALIDATION.md
@shared/src/schemas.ts
@hub/src/store/index.ts
@hub/src/web/routes/sessions/skillPolicy.ts
@hub/src/sync/sessionMergeService.ts
</context>

<execution_preconditions>
- Run GitNexus `impact` with repo `hapi-cursor`, target `MetadataSchema`, direction `upstream` before editing shared schemas. Stop on HIGH/CRITICAL.
- Run GitNexus `impact` on `createSkillPolicyRoutes` / `applySkillPolicy` before Hub deletions.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip skillPolicy from @hapi/protocol</name>
  <files>shared/src/schemas.ts, shared/src/types.ts, shared/src/index.ts, shared/src/schemas.test.ts, shared/src/skillPolicy.ts, shared/src/skillPolicy.test.ts</files>
  <read_first>
    - shared/src/schemas.ts (MetadataSchema, SkillSummarySchema, ListSkillsResponseSchema)
    - shared/src/skillPolicy.ts
    - shared/src/skillPolicy.test.ts
    - shared/src/schemas.test.ts
    - .planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-CONTEXT.md (D-04, D-07)
    - .planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-PATTERNS.md
  </read_first>
  <behavior>
    - MetadataSchema parses sessions without skillPolicy key.
    - Metadata fixtures that include skillPolicy fail parse OR skillPolicy is stripped per strict schema (match existing strict metadata tests).
    - isValidSkillForAutocomplete returns true only when skill.valid === true.
    - normalizeSkillSummaryForWire coerces source to user|project and defaults valid true when undefined.
    - isEffectivelyAllowed and isSkillSuggestible are removed (no policy branches).
  </behavior>
  <action>
    Per D-04 and D-07: Remove skillPolicy from MetadataSchema. Remove SkillPolicyStateSchema and SkillPolicyMapSchema if no remaining consumers in shared (grep shared/ first). Delete isEffectivelyAllowed and isSkillSuggestible from shared/src/skillPolicy.ts; add isValidSkillForAutocomplete(skill) => skill.valid === true. Keep normalizeSkillSummaryForWire. Update shared/src/types.ts and index.ts exports. Rewrite skillPolicy.test.ts for valid-only helper and wire normalization only. Update schemas.test.ts: metadata without skillPolicy parses; remove tests that require skillPolicy on metadata. Do not add skillPolicy to SessionPatchSchema.
  </action>
  <verify>
    <automated>cd /home/harry/projects/hapi-cursor/shared && bun test schemas.test.ts skillPolicy.test.ts</automated>
    <automated>cd /home/harry/projects/hapi-cursor && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Grep shared/src for skillPolicy in schemas returns zero field definitions (comments in tests ok).
    - Test assertion: isValidSkillForAutocomplete false when valid false.
    - Test assertion: MetadataSchema safeParse succeeds without skillPolicy property.
  </acceptance_criteria>
  <done>Shared protocol no longer defines or exports session skill policy types/helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Delete Hub skill-policy route and merge/write paths</name>
  <files>hub/src/web/routes/sessions/skillPolicy.ts, hub/src/web/routes/sessions/index.ts, hub/src/web/routes/sessions/__tests__/skillPolicy.test.ts, hub/src/sync/sessionMergeService.ts, hub/src/sync/sessionMergeService.test.ts, hub/src/sync/sessionConfigService.ts, hub/src/sync/sessionConfigService.test.ts, hub/src/sync/syncEngine.ts, hub/src/sync/syncEngineSession.ts, hub/src/sync/sessionCache.ts</files>
  <read_first>
    - hub/src/web/routes/sessions/index.ts
    - hub/src/web/routes/sessions/skillPolicy.ts
    - hub/src/sync/sessionMergeService.ts (skillPolicy union block ~207–226)
    - hub/src/sync/sessionConfigService.ts (grep SkillPolicy)
    - hub/src/web/routes/sessions/read.ts (keep GET /skills)
    - .planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-PATTERNS.md
  </read_first>
  <action>
    Per D-05: Delete hub/src/web/routes/sessions/skillPolicy.ts and hub/src/web/routes/sessions/__tests__/skillPolicy.test.ts. Remove createSkillPolicyRoutes import and mount from hub/src/web/routes/sessions/index.ts. Keep GET /sessions/:id/skills in read.ts unchanged (normalizeSkillSummaryForWire on response). Per D-04: Remove skillPolicy union merge from sessionMergeService.ts; delete related tests in sessionMergeService.test.ts. Remove applySkillPolicy, applySkillPolicyBatch, resetSessionSkillPolicy, validateSkillPolicyMap, writeSkillPolicyMetadata from sessionConfigService.ts and tests. Remove policy facade methods from syncEngine.ts, syncEngineSession.ts, sessionCache.ts. Optional: add hub test that POST /sessions/:id/skill-policy returns 404 if a minimal route test file is recreated — otherwise rely on file deletion + index mount removal.
  </action>
  <verify>
    <automated>cd /home/harry/projects/hapi-cursor/hub && bun test src/sync/sessionMergeService.test.ts src/sync/sessionConfigService.test.ts</automated>
    <automated>rg -v '^#' hub/src | rg -c 'skillPolicy|createSkillPolicyRoutes' | awk '{exit ($1==0)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - hub/src/web/routes/sessions/skillPolicy.ts does not exist.
    - hub/src/web/routes/sessions/index.ts has no createSkillPolicyRoutes reference.
    - sessionMergeService.ts has no skillPolicy merge block.
    - GET /sessions/:id/skills handler still present in read.ts.
  </acceptance_criteria>
  <done>Hub no longer accepts or merges session skill policy; discovery GET path intact.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Bump Hub SCHEMA_VERSION to 13</name>
  <files>hub/src/store/index.ts, hub/src/store/index.test.ts</files>
  <read_first>
    - hub/src/store/index.ts (SCHEMA_VERSION constant, mismatch throw)
    - hub/src/store/index.test.ts
    - AGENTS.md (no backward compat / fail-fast)
    - .planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-RESEARCH.md (Pattern 4)
  </read_first>
  <action>
    Per D-04: Change SCHEMA_VERSION from 12 to 13 in hub/src/store/index.ts. Update hub/src/store/index.test.ts expected version strings if hardcoded. Add one-line comment at SCHEMA_VERSION: metadata JSON may contain orphan skillPolicy from pre-02.1 rows — ignored after code removal; fail-fast requires DB recreate for local dev (delete Hub SQLite per project docs). No SQL migration script. Document in plan SUMMARY: developers delete/recreate Hub DB when hub fails with schema mismatch.
  </action>
  <verify>
    <automated>cd /home/harry/projects/hapi-cursor/hub && bun test src/store/index.test.ts</automated>
    <automated>rg 'SCHEMA_VERSION.*=.*13' hub/src/store/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - SCHEMA_VERSION constant equals 13 in hub/src/store/index.ts.
    - store index tests pass with updated expectation.
  </acceptance_criteria>
  <done>Hub enforces schema v13; mismatch surfaces buildSchemaMismatchError with recreate guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web/CLI → Hub | Authenticated session routes; policy POST removed |
| Hub → SQLite | metadata JSON blob |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | Denial of Service | POST skill-policy oversized map | mitigate | Delete route and body schemas (D-05) |
| T-02.1-02 | Tampering | Client-written skillPolicy map | mitigate | Remove field from MetadataSchema and merge (D-04) |
| T-02.1-03 | Information disclosure | GET /skills response | accept | Keep existing pathHint redaction in CLI listSkills |
| T-02.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan |
</threat_model>

<verification>
- `cd shared && bun test schemas.test.ts skillPolicy.test.ts`
- `cd hub && bun test src/sync/ src/store/index.test.ts`
- `rg 'skillPolicy|SkillPolicyState|createSkillPolicyRoutes' hub/src shared/src --glob '*.ts' | rg -v test` → zero production hits
- `bun run typecheck`
</verification>

<success_criteria>
- Shared metadata has no skillPolicy; policy helpers deleted.
- Hub skill-policy route and policy merge/write code deleted; GET /skills retained.
- SCHEMA_VERSION is 13 with fail-fast behavior documented for executors.
</success_criteria>

<output>
Create `.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-01-SUMMARY.md` when done
</output>
