---
phase: 02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess
plan: 01
subsystem: api
tags: [skills, zod, hono, sqlite, schema-version]

requires: []
provides:
  - MetadataSchema without skillPolicy; valid-only skill wire helpers
  - Hub without POST skill-policy or policy merge/write paths
  - Hub SCHEMA_VERSION 13 fail-fast
affects:
  - 02.1-02 (CLI preamble removal)
  - 02.1-03 (Web policy API removal)

tech-stack:
  added: []
  patterns:
    - "Zod strips legacy metadata.skillPolicy on parse"
    - "Fail-fast SCHEMA_VERSION bump without SQL migration"

key-files:
  created: []
  modified:
    - shared/src/schemas.ts
    - shared/src/skillPolicy.ts
    - hub/src/sync/sessionConfigService.ts
    - hub/src/sync/sessionMergeService.ts
    - hub/src/store/index.ts
    - hub/src/web/routes/sessions/index.ts

key-decisions:
  - "Removed SkillPolicyState from protocol entirely; CLI/Web type fixes deferred to wave 2 plans 02-03"
  - "SCHEMA_VERSION 13 with recreate-DB guidance; no migration script per repo policy"

patterns-established:
  - "Skill allow gate is valid discovery only (isValidSkillForAutocomplete)"

requirements-completed: [SKIL-01, SKIL-02, SKIL-03]

duration: 15min
completed: 2026-05-26
---

# Phase 02.1 Plan 01: Protocol and Hub Policy Removal Summary

**Session skillPolicy removed from @hapi/protocol and Hub; SCHEMA_VERSION 13 enforces DB recreate; GET /sessions/:id/skills unchanged.**

## Performance

- **Duration:** 15 min
- **Tasks:** 3
- **Files modified:** 16

## Accomplishments

- Stripped `skillPolicy` from `MetadataSchema`; deleted policy Zod types and `isEffectivelyAllowed` / `isSkillSuggestible`
- Added `isValidSkillForAutocomplete` and kept `normalizeSkillSummaryForWire` for discovery wire coercion
- Deleted Hub `POST /sessions/:id/skill-policy`, merge union, and `SessionConfigService` policy writers
- Bumped `SCHEMA_VERSION` to 13 with fail-fast comment for local dev DB recreate

## Task Commits

1. **Task 1: Strip skillPolicy from @hapi/protocol** - `4693da4` (feat)
2. **Task 2: Delete Hub skill-policy route and merge/write paths** - `ac5e63b` (feat)
3. **Task 3: Bump Hub SCHEMA_VERSION to 13** - `f44f3b6` (feat)

## Files Created/Modified

- `shared/src/schemas.ts` — no `skillPolicy` on metadata
- `shared/src/skillPolicy.ts` — valid-only autocomplete helper
- `hub/src/web/routes/sessions/skillPolicy.ts` — deleted
- `hub/src/sync/sessionConfigService.ts` — policy methods removed
- `hub/src/sync/sessionMergeService.ts` — policy merge block removed
- `hub/src/store/index.ts` — `SCHEMA_VERSION = 13`

## Decisions Made

- Wave 1 scope is shared + Hub only; monorepo `bun run typecheck` breaks on CLI/Web until plans 02.1-02 and 02.1-03 (expected per ROADMAP wave 2)
- `bun run typecheck:hub` passes after this plan

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Full `bun run typecheck` fails on CLI (and will fail on Web) until wave 2 plans remove `SkillPolicyState` / `skillPolicy` usage — not in 02.1-01 file list
- `rg` not installed in shell; used workspace grep for hub zero-hit verification

## User Setup Required

When Hub fails with schema mismatch after deploy: **delete and recreate the Hub SQLite database** (local dev). Orphan `skillPolicy` keys in old metadata JSON rows are ignored by parsers but fail-fast version requires a fresh DB.

## Next Phase Readiness

- Ready for **02.1-02** (CLI) and **02.1-03** (Web) in parallel
- Hub `GET /sessions/:id/skills` intact for discovery consumers

## Self-Check: PASSED

- FOUND: `.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-01-SUMMARY.md`
- FOUND: commit `4693da4`
- FOUND: commit `ac5e63b`
- FOUND: commit `f44f3b6`
- FOUND: `hub/src/store/index.ts` SCHEMA_VERSION 13
- MISSING: full monorepo typecheck (blocked until 02.1-02/03)

---
*Phase: 02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess*
*Completed: 2026-05-26*
