---
phase: 02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess
plan: 02
subsystem: cli
tags: [skills, listSkills, runCursor, cursor-agent]

requires:
  - phase: 02.1-01
    provides: MetadataSchema without skillPolicy; Hub policy routes removed
provides:
  - Per-message coalesced listSkills refresh before message queue push
  - Plain user text queue without HAPI skill-policy preamble
  - Deleted skillPolicyPreamble module; resume no longer preserves skillPolicy
affects:
  - 02.1-03 (Web policy UI removal)
  - 02.1-04 (header skills UI)

tech-stack:
  added: []
  patterns:
    - "Single-flight refreshSkills in runCursor onUserMessage"
    - "Implicit allow set = all valid discovered skills (no metadata.skillPolicy)"

key-files:
  created: []
  modified:
    - cli/src/cursor/runCursor.ts
    - cli/src/cursor/runCursor.test.ts
    - cli/src/agent/sessionFactory.ts
  deleted:
    - cli/src/cursor/skillPolicyPreamble.ts
    - cli/src/cursor/skillPolicyPreamble.test.ts

key-decisions:
  - "Async onUserMessage awaits refreshSkills before push; apiSession does not await callback (existing fire-and-forget pattern)"
  - "listSkills failure logs debug and returns last cachedSkills (empty on first failure)"

patterns-established:
  - "CLI discovery refresh: coalesced listSkills(workingDirectory) per user message"

requirements-completed: [SKIL-01]

duration: 4min
completed: 2026-05-26
---

# Phase 02.1 Plan 02: CLI Per-Message Skill Refresh Summary

**runCursor coalesces listSkills before every queued user message and drops the HAPI skill-policy preamble path entirely.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-26T09:05:26Z
- **Completed:** 2026-05-26T09:09:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added single-flight `refreshSkills()` using `listSkills(workingDirectory)` before each `onUserMessage` queue push (D-01, D-02)
- Removed `applySkillPolicyToFormattedMessage` usage and re-export; queued text is plain formatted user content (D-06)
- Deleted `skillPolicyPreamble.ts` and tests; removed `metadata.skillPolicy` preserve on session resume

## Task Commits

1. **Task 1: Per-message listSkills refresh in runCursor** - `aa5bccc` (feat)
2. **Task 2: Delete preamble module and sessionFactory skillPolicy preserve** - `46f1213` (feat)

## Files Created/Modified

- `cli/src/cursor/runCursor.ts` — `refreshSkills` single-flight; async `onUserMessage` without preamble
- `cli/src/cursor/runCursor.test.ts` — mocks for listSkills per message, no marker, coalescing
- `cli/src/agent/sessionFactory.ts` — no `skillPolicy` in `pickExistingSessionMetadata`
- `cli/src/cursor/skillPolicyPreamble.ts` — deleted
- `cli/src/cursor/skillPolicyPreamble.test.ts` — deleted

## Decisions Made

- Used async `onUserMessage` handler per RESEARCH Pattern 1; `apiSession` still invokes callback without await (unchanged contract)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `rg` not available in executor shell; verified zero production hits via workspace grep (test file retains marker string only for assertion)

## User Setup Required

None.

## Next Phase Readiness

- Ready for **02.1-03** (Web policy removal) and **02.1-04** (header skills UI)
- `bun run typecheck:cli` passes; `vitest run src/cursor/runCursor.test.ts` passes

## Self-Check: PASSED

- FOUND: `.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-02-SUMMARY.md`
- FOUND: commit `aa5bccc`
- FOUND: commit `46f1213`
- MISSING: `cli/src/cursor/skillPolicyPreamble.ts` (intentionally deleted)

---
*Phase: 02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess*
*Completed: 2026-05-26*
