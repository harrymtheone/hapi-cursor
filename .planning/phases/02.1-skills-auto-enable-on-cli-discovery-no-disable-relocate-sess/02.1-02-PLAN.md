---
phase: 02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess
plan: 02
type: execute
wave: 2
depends_on:
  - 02.1-01
files_modified:
  - cli/src/cursor/runCursor.ts
  - cli/src/cursor/runCursor.test.ts
  - cli/src/cursor/skillPolicyPreamble.ts
  - cli/src/cursor/skillPolicyPreamble.test.ts
  - cli/src/agent/sessionFactory.ts
autonomous: true
requirements:
  - SKIL-01
must_haves:
  truths:
    - "D-01: Each queued user message awaits fresh listSkills (single-flight) before push."
    - "D-02: Allowed set is implicit — all valid discovered skills; no skillPolicy read."
    - "D-06: No HAPI skill-policy preamble injected into formatted user text."
  artifacts:
    - path: cli/src/cursor/runCursor.ts
      provides: per-message refreshSkills + plain formattedText queue
    - path: cli/src/cursor/skillPolicyPreamble.ts
      provides: deleted
  key_links:
    - from: cli/src/cursor/runCursor.ts
      to: cli/src/modules/common/skills.ts
      via: listSkills(workingDirectory) in refreshSkills
      pattern: listSkills
---

<objective>
CLI refreshes skill discovery before every user message and stops injecting session skill-policy preamble text.

Purpose: D-01/D-02 make discovery equal availability for the agent; D-06 removes HAPI-only enforcement (SKIL-03 superseded).
Output: runCursor single-flight refresh, deleted skillPolicyPreamble module, sessionFactory without skillPolicy preserve.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-CONTEXT.md
@.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-RESEARCH.md
@.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-PATTERNS.md
@cli/src/cursor/runCursor.ts
@cli/src/cursor/skillPolicyPreamble.ts
@cli/src/agent/sessionFactory.ts
</context>

<execution_preconditions>
- Plan 02.1-01 complete (MetadataSchema has no skillPolicy).
- GitNexus `impact` on `runCursor` and `applySkillPolicyToFormattedMessage` before edits.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-message listSkills refresh in runCursor</name>
  <files>cli/src/cursor/runCursor.ts, cli/src/cursor/runCursor.test.ts</files>
  <read_first>
    - cli/src/cursor/runCursor.ts (onUserMessage, startup listSkills cache, preamble usage)
    - cli/src/cursor/runCursor.test.ts
    - cli/src/modules/common/skills.ts (listSkills signature)
    - .planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-RESEARCH.md (Pattern 1)
  </read_first>
  <behavior>
    - onUserMessage awaits refreshSkills() before messageQueue.push.
    - refreshSkills coalesces concurrent calls (single in-flight Promise).
    - listSkills failure logs debug and returns last cachedSkills (may be empty on first failure).
    - Formatted message pushed has no HAPI_SESSION_SKILL_POLICY_MARKER or allow-list lines.
    - session.getMetadata().skillPolicy is not read.
  </behavior>
  <action>
    Per D-01 and D-02: Implement refreshSkills single-flight pattern in runCursor.ts using listSkills(workingDirectory). Call await refreshSkills() at start of session.onUserMessage before formatMessageWithAttachments and messageQueue.push. Per D-06: Remove import of applySkillPolicyToFormattedMessage; remove re-export of applySkillPolicyToFormattedMessage; push formattedText directly without policy wrapper. Remove startup-only skill cache if redundant with refreshSkills (keep cachedSkills updated by refresh). Update runCursor.test.ts: mock listSkills called on each user message; assert output text does not contain HAPI_SESSION_SKILL_POLICY_MARKER; remove preamble expectations.
  </action>
  <verify>
    <automated>cd /home/harry/projects/hapi-cursor/cli && bunx vitest run src/cursor/runCursor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - runCursor.test.ts asserts listSkills invoked when user message queued (at least once per message).
    - runCursor.test.ts asserts queued text has no HAPI_SESSION_SKILL_POLICY_MARKER substring.
    - runCursor.ts has no applySkillPolicyToFormattedMessage reference.
  </acceptance_criteria>
  <done>CLI discovery refreshes per user turn; messages queue without policy preamble.</done>
</task>

<task type="auto">
  <name>Task 2: Delete preamble module and sessionFactory skillPolicy preserve</name>
  <files>cli/src/cursor/skillPolicyPreamble.ts, cli/src/cursor/skillPolicyPreamble.test.ts, cli/src/agent/sessionFactory.ts</files>
  <read_first>
    - cli/src/cursor/skillPolicyPreamble.ts
    - cli/src/agent/sessionFactory.ts (metadata preserve block ~108)
    - rg skillPolicyPreamble cli/src
  </read_first>
  <action>
    Per D-06 and D-04: Delete cli/src/cursor/skillPolicyPreamble.ts and cli/src/cursor/skillPolicyPreamble.test.ts. Remove line preserving metadata.skillPolicy in sessionFactory.ts. Grep cli/src for skillPolicyPreamble, applySkillPolicyToFormattedMessage, HAPI_SESSION_SKILL_POLICY_MARKER — zero production hits after cleanup.
  </action>
  <verify>
    <automated>test ! -f /home/harry/projects/hapi-cursor/cli/src/cursor/skillPolicyPreamble.ts</automated>
    <automated>rg -v '^#' /home/harry/projects/hapi-cursor/cli/src | rg 'skillPolicyPreamble|applySkillPolicyToFormattedMessage|HAPI_SESSION_SKILL_POLICY_MARKER' | awk '{exit 1}' END{exit 0}</automated>
    <automated>cd /home/harry/projects/hapi-cursor/cli && bunx vitest run src/cursor/runCursor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - skillPolicyPreamble.ts and .test.ts files absent.
    - sessionFactory.ts does not reference metadata.skillPolicy.
  </acceptance_criteria>
  <done>Preamble path fully removed from CLI; resume/handoff no longer preserves skillPolicy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Cursor agent | User message text (no injected policy block) |
| CLI → local FS | listSkills reads skill directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-04 | Tampering | Preamble allow-list bypass | mitigate | Delete preamble module (D-06) |
| T-02.1-05 | Denial of Service | listSkills per message | accept | Single-flight coalescing; debug log on failure |
| T-02.1-SC | Tampering | Package installs | accept | No new packages |
</threat_model>

<verification>
- `cd cli && bunx vitest run src/cursor/runCursor.test.ts`
- `rg skillPolicyPreamble cli/src` → no matches
</verification>

<success_criteria>
- Every user message triggers coalesced listSkills refresh before queue.
- No skill policy preamble in queued text; preamble files deleted.
</success_criteria>

<output>
Create `.planning/phases/02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess/02.1-02-SUMMARY.md` when done
</output>
