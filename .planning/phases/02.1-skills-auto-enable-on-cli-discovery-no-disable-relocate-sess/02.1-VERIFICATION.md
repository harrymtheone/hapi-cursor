---
phase: 02.1-skills-auto-enable-on-cli-discovery-no-disable-relocate-sess
verified: 2026-05-26T18:10:00Z
status: passed
score: 14/14
overrides_applied: 0
human_verification:
  - test: "On a phone or narrow PWA viewport, open an active session. Tap the Skills icon in SessionHeader (between Files and Outline). Confirm the bottom sheet title is Skills, rows show skill names with Global/Local pills, invalid skills show a red Invalid pill, and there are no enable/disable toggles, tri-state controls, policy footer, or descriptions in the list."
    expected: "Read-only skills sheet from header only; Global/Local scope labels; Invalid tag on invalid rows; no policy controls."
    why_human: "Layout, touch targets, and visual pill styling cannot be verified by static grep."
  - test: "Mid-session, add a new valid skill folder on disk for the session workspace. Send a user message from mobile. Confirm the new skill appears in the header sheet and in `/` autocomplete on the next turn (may require session connected to CLI)."
    expected: "Per-message listSkills refresh surfaces newly discovered skills without a session policy toggle."
    why_human: "Requires live filesystem change, CLI agent timing, and end-to-end discovery path."
---

# Phase 02.1: Skills auto-enable and header skills UI Verification Report

**Phase Goal:** When the CLI agent discovers skills, they are enabled automatically for the session with no user disable path; the session skills UI moves from the chat composer to the header Files bar as a read-only name list (Global/Local).

**Verified:** 2026-05-26T17:22:00Z

**Status:** passed (UAT complete 2026-05-26 — 2/2 human tests passed)

**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CLI discovery auto-enables skills; users cannot disable via mobile or session policy UI | ✓ VERIFIED | `skillPolicy` removed from `MetadataSchema`; Hub has no `skill-policy` route; Web `useSessionActions` has no policy mutations; `SkillsPolicySheet` is read-only (no PATCH/tri-state). `runCursor` refreshes `listSkills` on each `onUserMessage` without policy preamble. |
| 2 | Session skills control lives in SessionHeader beside Files with tooltip/aria **Skills** | ✓ VERIFIED | `SessionHeader.tsx` renders Skills button after Files with `t('session.skills.title')` and `aria-expanded`; `SessionChat.tsx` wires `onOpenSkills` + sheet state. |
| 3 | Skills panel shows names only, Global/Local scope, no toggles/policy/descriptions | ✓ VERIFIED | `SessionSkillsRow.tsx` renders name + scope pill (`session.skills.scope.global/local`) + optional `invalidTag`; `SkillsPolicySheet.tsx` has no policy footer or tri-state. Tests pass in vitest. |
| 4 | Metadata wire contract has no `skillPolicy`; legacy keys stripped on parse | ✓ VERIFIED | `shared/src/schemas.ts` `MetadataSchema` has no `skillPolicy` field; `schemas.test.ts` asserts strip on parse. |
| 5 | Hub exposes no POST `/sessions/:id/skill-policy` | ✓ VERIFIED | `hub/src/web/routes/sessions/index.ts` mounts lifecycle/config/upload/read only; `skillPolicy.ts` route file deleted. |
| 6 | Hub SQLite SCHEMA_VERSION 13 with fail-fast mismatch | ✓ VERIFIED | `hub/src/store/index.ts` `SCHEMA_VERSION = 13`; `namespace.test.ts` expects user_version 13. |
| 7 | Each queued user message awaits fresh `listSkills` (single-flight) | ✓ VERIFIED | `runCursor.ts` `refreshSkills()` + `session.onUserMessage` awaits refresh; tests in `runCursor.test.ts` cover per-message and coalescing. |
| 8 | Allowed set is all valid discovered skills (no `skillPolicy` read) | ✓ VERIFIED | CLI has no policy preamble or metadata policy; `isValidSkillForAutocomplete` in `@hapi/protocol` gates Web suggestions only. |
| 9 | No HAPI skill-policy preamble in formatted user text | ✓ VERIFIED | `cli/src/cursor/skillPolicyPreamble.ts` deleted; `runCursor` queues plain `formatMessageWithAttachments` text. |
| 10 | Invalid skills excluded from `/` autocomplete; valid-only `/name` keys | ✓ VERIFIED | `useSkills.ts` uses `isValidSkillForAutocomplete` and `` `/${skill.name}` `` keys; `useSkills.test.ts` asserts no `$` keys. |
| 11 | Web has no skill-policy mutation APIs | ✓ VERIFIED | `rg` on `web/src/api` and mutations: no `setSkillPolicy` / `skill-policy`; `useSessionActions.test.ts` asserts properties absent. |
| 12 | `/` autocomplete merges valid skills (first) with slash commands | ✓ VERIFIED | `router.tsx` `query.startsWith('/')` → `mergeAutocompleteSuggestions`; `autocompleteMerge.test.ts` passes. |
| 13 | Composer has no skills policy button or sheet mount | ✓ VERIFIED | `HappyComposer.tsx` has no `skillPolicy` / `SkillsPolicySheet` props; `ComposerButtons.tsx` has no skill/policy UI; deleted `SkillPolicyRow`, `SkillTriStateControl`, `EnforcementBadge`. |
| 14 | Settings catalog remains read-only; policy i18n removed; Global/Local labels | ✓ VERIFIED | `settings/skills.tsx` shows description/invocationMode/invalidReason (catalog only); `footerNote` read-only; locales use Global/Local keys; no `composer.skillsPolicy` or tri-state keys in `en.ts`/`zh-CN.ts`. |

**Score:** 14/14 truths verified (automated)

### Requirements Coverage

| Requirement | Source Plan(s) | Phase intent | Status | Evidence |
|-------------|----------------|--------------|--------|----------|
| SKIL-01 | 01–04 | View discovered skills with metadata | ✓ SATISFIED | `GET /sessions/:id/skills` + `useSkills`; settings catalog shows source, invocation mode, description, invalid reason; session sheet shows name + scope + Invalid tag. |
| SKIL-02 | 01, 03–05 | **Superseded:** session policy toggles → auto-enable only | ✓ SATISFIED (supersession) | Policy map, Hub route, Web mutations, and tri-state UI removed; discovery validity is the only gate. |
| SKIL-03 | 01, 02, 03, 05 | **Superseded:** HAPI vs Cursor enforcement display | ✓ SATISFIED (supersession) | Preamble deleted; no `EnforcementBadge` or enforcement i18n in session UI. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `shared/src/schemas.ts` | Metadata without skillPolicy | ✓ VERIFIED | No field in schema; Zod strips unknown keys. |
| `shared/src/skillPolicy.ts` | normalize + valid-only autocomplete | ✓ VERIFIED | 19 lines; `isValidSkillForAutocomplete` only. |
| `hub/src/store/index.ts` | SCHEMA_VERSION 13 | ✓ VERIFIED | Comment + constant + fail-fast throw. |
| `cli/src/cursor/runCursor.ts` | Per-message refresh, plain queue | ✓ VERIFIED | Wired to `listSkills`; no preamble import. |
| `cli/src/cursor/skillPolicyPreamble.ts` | Deleted | ✓ VERIFIED | File absent. |
| `web/src/hooks/queries/useSkills.ts` | No skillPolicy; `/` suggestions | ✓ VERIFIED | `getEffectiveSkillSuggestions` filters valid only. |
| `web/src/router.tsx` | Merged `/` autocomplete | ✓ VERIFIED | `mergeAutocompleteSuggestions` on `/` prefix. |
| `web/src/components/SessionHeader.tsx` | Skills button | ✓ VERIFIED | Between Files and Outline. |
| `web/src/components/AssistantChat/SkillsPolicySheet.tsx` | Read-only session sheet | ✓ VERIFIED | Uses `SessionSkillsRow`; no mutations (rename to `SessionSkillsSheet` optional per CONTEXT). |
| `web/src/components/AssistantChat/HappyComposer.tsx` | No policy UI | ✓ VERIFIED | No skill policy props or sheet. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `hub/.../sessions/index.ts` | `read.ts` | GET skills only | ✓ WIRED | No `createSkillPolicyRoutes`. |
| `runCursor.ts` | `skills.ts` | `listSkills(workingDirectory)` | ✓ WIRED | `refreshSkills` in `onUserMessage`. |
| `router.tsx` | `useSkills.ts` | `getSkillSuggestions` + merge | ✓ WIRED | `/` branch calls both skill and slash getters. |
| `SessionChat.tsx` | `SkillsPolicySheet.tsx` | `skillsSheetOpen` + `api`/`sessionId` | ✓ WIRED | Header opens sheet; not mounted under composer. |
| `SessionChat.tsx` | `HappyComposer.tsx` | no `skillPolicy` prop | ✓ WIRED | Grep shows no policy props passed. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `SkillsPolicySheet` | `skills` | `useSkills` → `api.getSkills(sessionId)` → Hub RPC `listSkills` | Yes (normalized wire) | ✓ FLOWING |
| `useSkills` suggestions | `skills` + filter | Same query cache | Valid-only subset | ✓ FLOWING |
| `runCursor` refresh | `cachedSkills` | `listSkills(workingDirectory)` | Yes; used as refresh cache on failure | ✓ FLOWING (cache only; agent uses Cursor discovery) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Per-message listSkills | `cd cli && bunx vitest run src/cursor/runCursor.test.ts` | 68+ tests pass (runCursor suite) | ✓ PASS |
| Valid-only `/` suggestions | `cd web && bunx vitest run src/hooks/queries/useSkills.test.ts` | All pass | ✓ PASS |
| Header Skills button | `cd web && bunx vitest run src/components/SessionHeader.test.tsx` | 3/3 pass | ✓ PASS |
| Read-only sheet | `cd web && bunx vitest run src/components/AssistantChat/SkillsPolicySheet.test.tsx` | 4/4 pass | ✓ PASS |
| Merge autocomplete | `cd web && bunx vitest run src/lib/autocompleteMerge.test.ts` | All pass | ✓ PASS |
| Metadata strip | `cd shared && bun test schemas.test.ts skillPolicy.test.ts` | Pass (via bun test) | ✓ PASS |
| SCHEMA 13 | `cd hub && bun test src/store/namespace.test.ts` | user_version 13 | ✓ PASS |

### Probe Execution

Step 7c: SKIPPED — no phase-declared `probe-*.sh` scripts.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None in phase-touched implementation files | — | — |

Note: Component/file name `SkillsPolicySheet` and `skillPolicyUtils.ts` retain legacy naming but contain no policy behavior (planner discretion D-08).

### Human Verification Required

### 1. Mobile header Skills sheet

**Test:** On phone/narrow PWA, open a session → tap Skills beside Files.

**Expected:** Bottom sheet titled Skills; name-only rows; Global/Local pills; Invalid pill when `valid === false`; no toggles, policy labels, or descriptions in list.

**Why human:** Visual layout and touch UX.

### 2. CLI mid-session skill discovery

**Test:** Add a new skill folder while session is running; send a message.

**Expected:** New skill appears in header sheet and `/` autocomplete on next turn.

**Why human:** Filesystem + live CLI/hub timing.

### Gaps Summary

No automated gaps. All roadmap success criteria and plan must-haves are implemented in code with passing targeted tests. Phase status is **human_needed** because two end-to-end behaviors (mobile sheet UX and live discovery refresh) require manual confirmation per `02.1-VALIDATION.md`.

---

_Verified: 2026-05-26T17:22:00Z_
_Verifier: Claude (gsd-verifier)_
