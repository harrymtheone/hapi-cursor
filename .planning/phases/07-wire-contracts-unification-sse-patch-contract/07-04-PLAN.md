---
phase: 07-wire-contracts-unification-sse-patch-contract
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - scripts/check-no-cut-agents.sh
autonomous: true
requirements: [REFA-03, REFA-04]
tags: [guard, ripgrep, scripts, phase-gate]

must_haves:
  truths:
    - "scripts/check-no-cut-agents.sh no longer post-filters the 'codex' AGENT_MESSAGE_PAYLOAD_TYPE survivor — D-123 already flipped the constant to 'cursor', so the whitelist is dead code (D-124)"
    - "scripts/check-no-cut-agents.sh adds the Phase-7 D-126 sweep block covering 6 zero-tolerance keyword classes: hasUnknownSessionPatchKeys (all source trees), getSessionPatch (web/src/hooks/), interface Machine + export type Machine (cli/src + web/src + hub/src outside the machineCache.ts re-export line), RunnerStateSchema / MachineMetadataSchema (cli/src + web/src), 'codex' (all source trees zero-tolerance), metadata flavor: writes (cli/src + hub/src)"
    - "Header comments referencing 'Phase-5 single residue' are rewritten or removed (D-124 — residue is gone)"
    - "bash scripts/check-no-cut-agents.sh exits 0 against the post-Slice-3 source tree (phase gate)"
    - "Workspace-wide bun typecheck + bun run test still green (regression gate — no slipped edits between Slices 3 and 4)"
    - "shared/ is single source of wire schema (REFA-03 final)"
    - "strict patch schemas reject unknown keys; SSE handler has no refetch fallback (REFA-04 final)"
    - "no backward-compat shim anywhere (CONTEXT.md key constraint final)"
  artifacts:
    - path: "scripts/check-no-cut-agents.sh"
      provides: "Phase-7 D-126 guard block + Phase-5 whitelist removal"
      contains: "Phase-7"
  key_links:
    - from: "scripts/check-no-cut-agents.sh Phase-7 block"
      to: "ripgrep across cli/src hub/src web/src shared/src"
      via: "if rg -n PATTERN DIRS; then exit 1 idiom"
      pattern: "PHASE7"

slice_gate:
  command: "bash scripts/check-no-cut-agents.sh && bun typecheck && bun run test"
  must_pass: true
---

<objective>
Slice 4 of D-125 (the phase gate): remove the Phase-5 `AGENT_MESSAGE_PAYLOAD_TYPE` whitelist from `scripts/check-no-cut-agents.sh` (D-124), append a Phase-7 D-126 sweep block enforcing 6 zero-tolerance keyword classes that prevent re-introduction of the wire-contract / SSE-patch / flavor anti-patterns just collapsed in Slices 1–3, and run the final triple-gate: guard script + workspace typecheck + workspace tests.

Purpose: REFA-03 + REFA-04 acceptance criteria expressed as automated ripgrep gates that survive future commits. Codifies "shared is the only wire contract source" and "no refetch fallback in useSSE" as build-time invariants.

Output: `bash scripts/check-no-cut-agents.sh && bun typecheck && bun run test` green at workspace root. Phase 7 source-level cuts are now defended.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-CONTEXT.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-01-SUMMARY.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-02-SUMMARY.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-03-SUMMARY.md

<interfaces>
Current `scripts/check-no-cut-agents.sh` state (pre-Slice-4):
- Header comment block (lines ~4-17) references "Phase-5 single residue" — rewrite or delete (D-124).
- Line ~13: header comment about Phase-5 territory `'codex'` whitelist — delete or rewrite.
- Line ~84: `SURVIVORS_FILTERED=$(echo "$SURVIVORS" | grep -v "shared/src/modes.ts:.*AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const" || true)` — DELETE this post-filter line.
- Line ~91, ~96: error / success echoes mentioning "AGENT_MESSAGE_PAYLOAD_TYPE wire literal" exception language — rewrite to drop the exception.
- Phase-6 sweep block at lines ~162-217 is the structural analog for the new Phase-7 block.

Phase-7 D-126 sweep block to append after the Phase-6 block (~line 217). Six checks per D-126 / PATTERNS "AUGMENT scripts/check-no-cut-agents.sh":

(1) `hasUnknownSessionPatchKeys` — zero hits across `cli/src hub/src web/src shared/src`.
(2) `getSessionPatch` — zero hits inside `web/src/hooks/` (the original sin location; deletion of this identifier prevents re-introduction of ad-hoc patch narrows).
(3) `interface Machine\b` OR `^export type Machine =` (regex `^\s*export\s+(interface|type)\s+Machine\b`) — zero hits in `cli/src hub/src web/src` EXCEPT the legal `hub/src/sync/machineCache.ts:.*export type { Machine }` re-export line which must be filtered out.
(4) `RunnerStateSchema` / `MachineMetadataSchema` — zero hits in `cli/src web/src` except inside `from '@hapi/protocol` import lines (filter `grep -v 'from .@hapi/protocol'`).
(5) `'codex'` literal (single OR double quotes) — zero hits in `cli/src hub/src web/src shared/src`. Belt-and-suspenders check; the main PATTERN sweep should already cover this after the D-124 whitelist removal.
(6) `flavor:` writes — zero hits in `cli/src hub/src` for either `flavor:\s*['\"]` (key with string value) or `\.flavor\s*=` (property assignment), EXCEPT the legitimate `LocalResumeTarget` / `ResumableSession` / `resume.ts` resume-target flavor field shapes (filter `grep -v -E '(LocalResumeTarget|ResumableSession|resume\.ts)'`).

Each check follows the established `if "$RG_BIN" -n PATTERN DIRS; then echo "❌ ..."; exit 1; fi` idiom from the existing Phase-6 block. Use `$RG_BIN` (already set in the script) — do NOT hardcode `rg`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove D-124 whitelist + append D-126 Phase-7 sweep block to scripts/check-no-cut-agents.sh</name>
  <files>scripts/check-no-cut-agents.sh</files>
  <read_first>
    - scripts/check-no-cut-agents.sh (full file — verify $RG_BIN is set, locate the Phase-5 'codex' AGENT_MESSAGE_PAYLOAD_TYPE post-filter at ~line 84, the header comments at ~13, the error/success echo language at ~91/~96, and the Phase-6 block at ~162-217 as the structural analog)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md §8 (the AGENT_MESSAGE_PAYLOAD_TYPE survivor inventory — confirms `'codex'` literal zero post-D-123) + §1 (the 9-field SessionPatchSchema enumeration — context for why hasUnknownSessionPatchKeys deletion is permanent)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-CONTEXT.md D-124, D-126 (the binding spec for this task)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md "AUGMENT scripts/check-no-cut-agents.sh" (the exact append-block template)
  </read_first>
  <action>
Edit `scripts/check-no-cut-agents.sh`:

(A) Header / Phase-5 whitelist removal (D-124):
- In the header comment block (~lines 4-17), delete the line(s) describing Phase-5 single residue / AGENT_MESSAGE_PAYLOAD_TYPE territory. Replace with a one-line note: `# Phase-7 (D-124) removed the AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' whitelist — constant flipped to 'cursor', no whitelist needed.`
- Delete the `SURVIVORS_FILTERED=$(echo "$SURVIVORS" | grep -v "shared/src/modes.ts:.*AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const" || true)` post-filter line (~84).
- In the `if [ -n "$SURVIVORS_FILTERED" ]; then` check (~lines around 84-90), rename `$SURVIVORS_FILTERED` → `$SURVIVORS` and rewrite the error / success echo language (~91, ~96) to remove the "but wire literal exception" wording — now `'codex'` is zero-tolerance.

(B) Append Phase-7 D-126 sweep block immediately after the Phase-6 block (~line 217). Structure (follow PATTERNS "AUGMENT scripts/check-no-cut-agents.sh" verbatim with adaptation to the existing `$RG_BIN` variable):

```
# === Phase-7 wire-contract sweeps — D-126
PHASE7_SOURCE_DIRS=(cli/src hub/src web/src shared/src)

# (#1) useSSE-internal narrow heuristics — REFA-04
if "$RG_BIN" -n '\bhasUnknownSessionPatchKeys\b' "${PHASE7_SOURCE_DIRS[@]}"; then
  echo "❌ Phase-7 hasUnknownSessionPatchKeys residue (REFA-04: SSE patch contract strictified)."
  exit 1
fi

# (#2) getSessionPatch — zero hits in web/src/hooks/
if "$RG_BIN" -n '\bgetSessionPatch\b' web/src/hooks; then
  echo "❌ Phase-7 getSessionPatch residue in web/src/hooks/ (use SyncEventSchema discriminator)."
  exit 1
fi

# (#3) interface Machine + export type Machine — only allowed in shared/
DUP_MACHINE=$("$RG_BIN" -n '^\s*export\s+(interface|type)\s+Machine\b' \
  cli/src hub/src web/src \
  | grep -v 'hub/src/sync/machineCache\.ts:.*export type { Machine }' \
  || true)
if [ -n "$DUP_MACHINE" ]; then
  echo "$DUP_MACHINE"
  echo "❌ Phase-7 duplicate Machine declaration (REFA-03: shared single source)."
  exit 1
fi

# (#4) RunnerStateSchema / MachineMetadataSchema — only allowed in shared/src/schemas.ts (cli/web must import)
DUP_SCHEMA=$("$RG_BIN" -n '\b(RunnerStateSchema|MachineMetadataSchema)\b' cli/src web/src \
  | grep -v "from .@hapi/protocol" \
  || true)
if [ -n "$DUP_SCHEMA" ]; then
  echo "$DUP_SCHEMA"
  echo "❌ Phase-7 duplicate RunnerStateSchema/MachineMetadataSchema declaration (REFA-03)."
  exit 1
fi

# (#5) 'codex' literal — zero-tolerance across runtime source trees (D-123 rename complete)
if "$RG_BIN" -n "['\"]codex['\"]" "${PHASE7_SOURCE_DIRS[@]}"; then
  echo "❌ Phase-7 'codex' literal residue (D-123: AGENT_MESSAGE_PAYLOAD_TYPE = 'cursor')."
  exit 1
fi

# (#6) flavor field writes — zero hits in cli/src + hub/src (resume-target literals filtered)
FLAVOR_WRITES=$("$RG_BIN" -n "flavor:\s*['\"]|\.flavor\s*=" cli/src hub/src \
  | grep -v -E '(LocalResumeTarget|ResumableSession|resume\.ts)' \
  || true)
if [ -n "$FLAVOR_WRITES" ]; then
  echo "$FLAVOR_WRITES"
  echo "❌ Phase-7 metadata.flavor write residue (D-122: field deleted)."
  exit 1
fi

echo "✅ Phase-7 wire-contract sweeps clean (D-126)."
```

Note bash escaping: when this block lands in the actual `.sh` file, the single-quote / double-quote alternation in the `'codex'` pattern (#5) must escape correctly — use the exact form `"['\"]codex['\"]"` (double-quoted shell string containing escaped inner double-quote) so ripgrep receives the regex `['"]codex['"]`. Identical bracket-class pattern is used in the existing `$PATTERN` regex above; mirror that idiom if uncertain.

Identifier-level non-goals:
- Do NOT add a Phase-8 `madge --circular hub/src/` block (D-128 — that's Phase 8 REFH-02 territory).
- Do NOT remove the existing Phase-1/2/3/4/5/6 sweep blocks (they remain the foundation; Phase 7 only removes the Phase-5 whitelist line, not the broader Phase-5 sweep).
- Do NOT introduce a "Phase 12 docs prose" sweep (CUT-12 — Phase 12 owns README/AGENTS/docs/website).
- Do NOT widen check (#6) flavor-write filter to include arbitrary `flavor` paths — the filter is intentionally narrow (`LocalResumeTarget|ResumableSession|resume.ts`) to allow the resume-target field while catching everything else.
  </action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SURVIVORS_FILTERED" scripts/check-no-cut-agents.sh` returns 0 hits.
    - `rg -n "AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const" scripts/check-no-cut-agents.sh` returns 0 hits (whitelist line gone).
    - `rg -n "Phase-7" scripts/check-no-cut-agents.sh` returns at least 1 hit (new block landed).
    - `rg -n "hasUnknownSessionPatchKeys" scripts/check-no-cut-agents.sh` returns exactly 1 hit (the check, not a residue).
    - `rg -n "getSessionPatch" scripts/check-no-cut-agents.sh` returns exactly 1 hit.
    - `rg -n "RunnerStateSchema|MachineMetadataSchema" scripts/check-no-cut-agents.sh` returns at least 2 hits (the check pattern).
    - `bash scripts/check-no-cut-agents.sh` exits 0 against the post-Slice-3 tree. (Every check passes — confirms Slices 1-3 left no residue.)
  </acceptance_criteria>
  <done>D-124 whitelist removed and D-126 Phase-7 sweep block landed; the guard script exits 0 against the current source tree, codifying REFA-03 + REFA-04 as a permanent build-time invariant.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — bash scripts/check-no-cut-agents.sh && bun typecheck && bun run test</name>
  <files>(verification only — no edits)</files>
  <read_first>
    - scripts/check-no-cut-agents.sh (post-Task-1 — verify Phase-7 block syntactically correct)
    - All three prior phase SUMMARYs (07-01, 07-02, 07-03) for context on what each slice promised vs delivered.
  </read_first>
  <action>
Run the Phase 7 final triple-gate from the workspace root. The three checks together encode all four ROADMAP success criteria:
- SC#1 (single source of `Session/Machine/Message/RunnerState`): guard check #3 + #4 enforce it; `bun typecheck` rejects any restored local declaration.
- SC#2 (`hasUnknownSessionPatchKeys` removed; SSE emits full-or-strict-patch): guard check #1 enforces removal; tightened `SyncEventSchema` enforces emit shape; the hub contract test (Slice 2) + useSSE.test.tsx (Slice 3) verify at runtime.
- SC#3 (front-end SSE handler consumes canonical schema; no refetch fallback): guard check #2 + useSSE.test.tsx parse-failure-no-invalidate assertion enforce it.
- SC#4 (`bun typecheck && bun run test` green + strictly typed event stream test): the gate itself.

If any check fails, STOP and report which gate failed + which acceptance criterion of the failing slice's SUMMARY was over-promised. Do NOT add a whitelist or backward-compat shim to make the gate pass — fix the residue at the source.
  </action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh &amp;&amp; bun typecheck &amp;&amp; bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-no-cut-agents.sh` exits 0; final stdout line includes "✅ Phase-7 wire-contract sweeps clean (D-126)." (and the prior phase blocks' success messages).
    - `bun typecheck` exits 0 at workspace root.
    - `bun run test` exits 0 at workspace root; output includes PASSED for `shared/src/schemas.test.ts`, `hub/src/sync/sessionCache.test.ts`, `web/src/hooks/useSSE.test.tsx`.
    - `rg -n "hasUnknownSessionPatchKeys|getSessionPatch" cli/src hub/src web/src shared/src` returns 0 hits.
    - `rg -n "interface Machine\b" cli/src web/src` returns 0 hits.
    - `rg -n "RunnerStateSchema|MachineMetadataSchema" cli/src web/src` shows only `from '@hapi/protocol` import-line hits.
    - `rg -n "'codex'" cli/src hub/src web/src shared/src` returns 0 hits.
    - `rg -n "metadata: \{[^}]*flavor:" cli/src hub/src web/src` returns 0 hits.
  </acceptance_criteria>
  <done>Phase 7 D-125 Slice 4 (phase gate) green: all four ROADMAP SC achieved; guard script encodes the cuts as a permanent build-time invariant; Phase 8 (REFH-01/02/03/04) is free to start on a clean wire-contract foundation.</done>
</task>

</tasks>

<verification>
- `bash scripts/check-no-cut-agents.sh` covers all 6 D-126 keyword classes plus the broader Phase-1..5 sweeps.
- `bun typecheck && bun run test` (workspace root) covers SC#1..#4 of Phase 7.
- Each acceptance_criteria block uses concrete ripgrep patterns or scripted exits, so a future commit that re-introduces an anti-pattern will fail CI immediately.
</verification>

<success_criteria>
- D-125 Slice 4 (phase gate) green: `bash scripts/check-no-cut-agents.sh && bun typecheck && bun run test` all exit 0.
- REFA-03 final: shared/ is the only wire contract source; 0 duplicate declarations of Machine / RunnerState / Message wire schemas / response wrappers in cli/src or web/src or hub/src; enforced by guard script #3 + #4.
- REFA-04 final: hasUnknownSessionPatchKeys gone (#1); getSessionPatch gone in web/src/hooks (#2); SSE handler uses canonical schema with no refetch fallback (asserted at runtime by useSSE.test.tsx).
- D-124 executed: `'codex'` whitelist removed from check-no-cut-agents.sh; `'codex'` becomes zero-tolerance everywhere (#5).
- D-122 final: no metadata.flavor writes in cli/src or hub/src; presentation-tier flavor readers in web deleted.
- No backward-compat shim, no feature flag, no field alias anywhere (CONTEXT.md key constraint upheld through all 4 slices).
</success_criteria>

<output>
Create `.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-04-SUMMARY.md` documenting:
- Final exit codes of the triple-gate (guard script + typecheck + test).
- The 6 D-126 keyword classes' final hit counts (should all be 0 outside the script itself).
- Total `'codex'` and `flavor:` residue count across cli/src + hub/src + web/src + shared/src (expected: 0 codex; flavor only inside resume.ts and its derived types/tests).
- Pointer to the four phase SUMMARYs (07-01, 07-02, 07-03, 07-04) as the cumulative phase record.
- Open issue: SQLite legacy-data fallout from D-123 rename + D-122 field delete is documented as accepted Phase-10 schema-version reject path (REFC-01); no Phase 7 code change covers it.
</output>
