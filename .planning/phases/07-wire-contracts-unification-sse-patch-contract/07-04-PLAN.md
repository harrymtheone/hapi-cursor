---
phase: 07-wire-contracts-unification-sse-patch-contract
plan: 04
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - scripts/check-no-cut-agents.sh
autonomous: true
requirements: [REFA-03, REFA-04]
tags: [guard-script, ripgrep, phase-gate, verification]

must_haves:
  truths:
    - "scripts/check-no-cut-agents.sh no longer contains the AGENT_MESSAGE_PAYLOAD_TYPE line-anchored post-filter (the legacy literal it whitelisted is gone — D-123 renamed it to 'cursor' in 07-01); the Phase-5 'codex' territory comment block is removed (D-124)"
    - "scripts/check-no-cut-agents.sh carries a new Phase-7 sweep block (D-126) with 6 zero-tolerance checks: (1) hasUnknownSessionPatchKeys identifier residue; (2) getSessionPatch in web/src/hooks/; (3) duplicate Machine interface/type declarations outside shared/; (4) RunnerStateSchema / MachineMetadataSchema declarations outside shared/; (5) 'codex' literal across cli/src + hub/src + web/src + shared/src; (6) metadata.flavor write residue in cli/src + hub/src"
    - "The new Phase-7 block follows the established if rg ... then echo ... exit 1 fi idiom from the existing Phase-5 / Phase-6 sweeps (07-PATTERNS Pattern E)"
    - "Final phase gate: bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh all exit 0 (D-125 Slice 4 + Phase 7 SC#1-#4 closure)"
    - "REFA-03 + REFA-04 fully verified at the source-level via ripgrep zero-hit gates"
  artifacts:
    - path: "scripts/check-no-cut-agents.sh"
      provides: "Phase-7 D-126 sweep block + D-124 whitelist removal; existing Phase-1 through Phase-6 sweeps preserved unchanged"
      contains: "Phase-7"
  key_links:
    - from: "scripts/check-no-cut-agents.sh"
      to: "cli/src + hub/src + web/src + shared/src"
      via: "ripgrep sweep over all 4 source trees"
      pattern: "PHASE7_SOURCE_DIRS|hasUnknownSessionPatchKeys"

slice_gate:
  command: "bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh"
  must_pass: true
---

<objective>
Slice 4 of D-125 — the closing slice. Two responsibilities: (1) strike the D-124 retirements (the Phase-5 `AGENT_MESSAGE_PAYLOAD_TYPE = 'codex'` line-anchored post-filter + the "codex wire literal exception" rationale comments), now that 07-01 renamed the constant to `'cursor'`; (2) land the D-126 sweep block — 6 ripgrep zero-tolerance checks that guarantee the Phase 7 collapse stays collapsed (no new `hasUnknownSessionPatchKeys` re-introduction, no Machine/RunnerState type drift back into cli/web, no `'codex'` literal re-introduction, no metadata.flavor field re-introduction). The slice ends with the full phase gate: `bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh` all exit 0, closing Phase 7's SC#1-#4.

Purpose: convert the Phase 7 invariants from "true today" into "verified by guard on every CI run" — same template as P5 D-86 + P6 D-108#4.

Output: `scripts/check-no-cut-agents.sh` carries the new Phase-7 block; the legacy AGENT_MESSAGE_PAYLOAD_TYPE post-filter is gone; the full phase gate is green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-CONTEXT.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-01-SUMMARY.md

<interfaces>
Existing guard-script structure (verify by reading the file before editing):
- Lines ~1-17: header comment block — includes Phase-1 / Phase-5 rationale referencing the `'codex'` survivor and the AGENT_MESSAGE_PAYLOAD_TYPE post-filter
- Line ~13: PATTERN definition — currently `'\b(claude|codex|gemini|opencode|telegram|serverchan|elevenlabs|grammy)\b'` (preserved as-is; D-124 only removes the codex-related WHITELIST / post-filter, NOT the broad pattern that catches stray literal usages)
- Line ~84: the `SURVIVORS_FILTERED=$(echo "$SURVIVORS" | grep -v "shared/src/modes.ts:.*AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const" || true)` post-filter — this is the D-124 deletion target
- Lines ~91, 96: error / success echo messages mentioning "AGENT_MESSAGE_PAYLOAD_TYPE wire literal" — rewrite to remove the exception language
- Phase-5 block (~lines 82-160): D-86 keyword sweeps (PHASE5_IDENTIFIER_PATTERN, PHASE5_BRANCH_PATTERN, line-anchored AGENT_MESSAGE_PAYLOAD_TYPE post-filter) — the line-anchored post-filter that protected `shared/src/modes.ts:9: AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const` is now obsolete and must be removed; the rest of the Phase-5 block stays
- Phase-6 block (~lines 162-217): D-108 keyword + madge sweeps — preserved unchanged
- Append point for Phase-7 block: directly after the Phase-6 block

References:
- 07-PATTERNS.md "AUGMENT scripts/check-no-cut-agents.sh" — exact bash skeleton for the Phase-7 block (PHASE7_SOURCE_DIRS, six sweep cases, post-filter for legitimate resume-target flavor hits in case 6)
- 07-RESEARCH.md §8 (the D-124 deletion targets — lines 13, 84, 91, 96) + §1 (Phase-7 closure context)
- 07-CONTEXT.md D-124 + D-126 (sweep keyword authoritative list)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete D-124 retirements (Phase-5 AGENT_MESSAGE_PAYLOAD_TYPE post-filter + 'codex' whitelist comments) from scripts/check-no-cut-agents.sh</name>
  <files>scripts/check-no-cut-agents.sh</files>
  <read_first>
    - scripts/check-no-cut-agents.sh (full file — confirm current line ranges for the post-filter (~84) and the header comment block (~1-17) and the Phase-5 line-anchored AGENT_MESSAGE_PAYLOAD_TYPE protection elsewhere in the script)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-CONTEXT.md D-124 verbatim
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md §8 (action set for D-124)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md "Phase 1/5 whitelist removal pattern" section showing the exact before/after for the SURVIVORS_FILTERED block
  </read_first>
  <behavior>
    - The `SURVIVORS_FILTERED=$(echo "$SURVIVORS" | grep -v "shared/src/modes.ts:.*AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const" || true)` line is removed.
    - The subsequent `if [ -n "$SURVIVORS_FILTERED" ]; then` block is rewritten to use `$SURVIVORS` directly.
    - The error/success echo messages no longer reference "AGENT_MESSAGE_PAYLOAD_TYPE wire literal" exceptions.
    - Any line-anchored post-filter elsewhere in the script that specifically whitelists `shared/src/modes.ts:9` for the legacy codex constant is removed.
    - The header comment block at lines ~1-17 is updated: the Phase-5 single-residue rationale ("AGENT_MESSAGE_PAYLOAD_TYPE wire literal owned by Phase 7") becomes a historical artifact note OR is removed entirely. Keep the broader cut-agents rationale.
    - The main `PATTERN='\b(claude|codex|gemini|opencode|telegram|serverchan|elevenlabs|grammy)\b'` definition is PRESERVED — it now catches any stray `'codex'` literal as part of the broad sweep.
    - The Phase-5 PHASE5_IDENTIFIER_PATTERN + PHASE5_BRANCH_PATTERN sweep blocks elsewhere in the script are PRESERVED unchanged (they handle Cursor-only flavor invariants, not the codex literal).
    - The Phase-6 D-108 block is PRESERVED unchanged.
    - The script still exits 0 after this task (no Phase-7 sweep block added yet — that lands in Task 2).
  </behavior>
  <action>
Read the current `scripts/check-no-cut-agents.sh` end-to-end first to confirm exact line positions (the line numbers in 07-CONTEXT.md / 07-PATTERNS.md are approximate — locate by content, not by line number).

Delete the `SURVIVORS_FILTERED=$(...)` line that filters out the `AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' as const` exception. Replace the subsequent `if [ -n "$SURVIVORS_FILTERED" ]; then ... echo "$SURVIVORS_FILTERED" ...` block with the simpler `if [ -n "$SURVIVORS" ]; then ... echo "$SURVIVORS" ...` flow — the input variable is now the raw ripgrep result, and the error message says "Non-Cursor / external-channel literals found outside whitelist." (drop the "AGENT_MESSAGE_PAYLOAD_TYPE wire literal" language).

Sweep the rest of the script for any other line-anchored AGENT_MESSAGE_PAYLOAD_TYPE protection (e.g. inside the Phase-5 PHASE5_IDENTIFIER_PATTERN block there may be a `grep -v 'shared/src/modes.ts.*AGENT_MESSAGE_PAYLOAD_TYPE'` post-filter — if present, remove it). The constant is now `'cursor' as const`, so it never matches the cut-agents PATTERN; no whitelist is needed.

Update the header comment block (~lines 1-17): remove the "Phase-5 single residue" rationale paragraph and any mention of "wire-protocol legacy literal owned by Phase 7" — Phase 7 owns the rename, not a perpetual whitelist. A one-line note like `# Phase 7 (D-124) removed the AGENT_MESSAGE_PAYLOAD_TYPE='codex' whitelist after the constant was renamed to 'cursor'.` is acceptable but optional.

Verify the script's shebang, set -e/set -u flags, RG_BIN detection, WHITELIST array, and PATTERN definition are byte-identical to pre-edit (they are out of scope for D-124).

Run `bash scripts/check-no-cut-agents.sh` after the edit to confirm it still exits 0 against the post-07-03 source tree. If it fails because the broad PATTERN now catches a `'codex'` hit that was previously whitelisted, that is a real Phase-7 contract failure — STOP and investigate the offending file (it should be zero hits after 07-01 + 07-03 work).
  </action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SURVIVORS_FILTERED" scripts/check-no-cut-agents.sh` returns 0 hits.
    - `rg -n "AGENT_MESSAGE_PAYLOAD_TYPE = 'codex'" scripts/check-no-cut-agents.sh` returns 0 hits.
    - `rg -n "wire-protocol legacy literal|wire literal exception" scripts/check-no-cut-agents.sh` returns 0 hits.
    - `rg -n "PATTERN='\\\\b\\(claude\\|codex\\|gemini\\|opencode\\|telegram\\|serverchan\\|elevenlabs\\|grammy\\)\\\\b'" scripts/check-no-cut-agents.sh` returns 1 hit (the broad sweep is preserved).
    - `rg -n "Phase-5|PHASE5_" scripts/check-no-cut-agents.sh` still shows the Phase-5 block (identifier + branch patterns preserved — minus only the AGENT_MESSAGE_PAYLOAD_TYPE post-filter if it lived inside that block).
    - `rg -n "Phase-6|PHASE6_" scripts/check-no-cut-agents.sh` still shows the Phase-6 block unchanged.
    - `bash scripts/check-no-cut-agents.sh` exits 0.
  </acceptance_criteria>
  <done>D-124 retirements complete; 'codex' literal is now fully zero-tolerance via the existing broad PATTERN sweep; no exceptions remain for the renamed constant.</done>
</task>

<task type="auto">
  <name>Task 2: Append the Phase-7 D-126 sweep block to scripts/check-no-cut-agents.sh (6 zero-tolerance checks)</name>
  <files>scripts/check-no-cut-agents.sh</files>
  <read_first>
    - scripts/check-no-cut-agents.sh (post-Task-1 — locate the end of the Phase-6 D-108 sweep block, which is where the new Phase-7 block appends)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md "AUGMENT scripts/check-no-cut-agents.sh" — the verbatim Phase-7 sweep block skeleton with all 6 sweeps + the post-filter for resume-target legitimate `flavor` hits in sweep (#6)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-CONTEXT.md D-126 (authoritative keyword list — 6 sweeps)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md §1 + §6 + §7 (justifications for each sweep)
  </read_first>
  <behavior>
    The new Phase-7 block (appended directly after the Phase-6 block, before any final summary echo at the end of the script):
    - `PHASE7_SOURCE_DIRS=(cli/src hub/src web/src shared/src)` defined locally to the block.
    - Sweep (1): `hasUnknownSessionPatchKeys` identifier anywhere in PHASE7_SOURCE_DIRS → error + exit 1. Rationale message references REFA-04.
    - Sweep (2): `getSessionPatch` identifier in `web/src/hooks/` → error + exit 1. Rationale references SyncEventSchema discriminator usage.
    - Sweep (3): duplicate `Machine` interface / `export type Machine =` declarations in `cli/src`, `hub/src`, `web/src`, with a post-filter that allows `hub/src/sync/machineCache.ts:.*export type \{ Machine \}` (the legitimate re-export shim). Any other hit → error + exit 1. Rationale references REFA-03 single shared source.
    - Sweep (4): `RunnerStateSchema` or `MachineMetadataSchema` declarations in `cli/src` + `web/src`, with a post-filter that allows lines containing `from '@hapi/protocol` (re-export lines are legitimate). Any other hit → error + exit 1.
    - Sweep (5): `'codex'` literal (with single or double quotes) across all 4 PHASE7_SOURCE_DIRS → error + exit 1. This is a belt-and-suspenders check on top of the broad PATTERN sweep (D-124 already enforces the broader catch).
    - Sweep (6): metadata flavor writes — match `flavor:\s*['"]|\.flavor\s*=` in `cli/src` + `hub/src`, with a post-filter that allows hits in lines containing `LocalResumeTarget`, `ResumableSession`, or the `resume.ts` filename (these legitimately carry a top-level `flavor` field per RESEARCH §7 plan-checker note). Any other hit → error + exit 1.
    - Final success echo: `echo "✅ Phase-7 wire-contract sweeps clean (D-126)."` on completion.
    - `bash scripts/check-no-cut-agents.sh` exits 0 against the post-07-03 source tree (all 6 sweeps must already be satisfied because 07-01 + 07-02 + 07-03 work delivered the corresponding source-level invariants).
  </behavior>
  <action>
Read `scripts/check-no-cut-agents.sh` after Task 1 to confirm the file boundaries. Locate the `echo "✅ ... Phase-6 ..."` success line at the end of the Phase-6 block — that is the insertion point. Append the Phase-7 block immediately after it (before any closing `exit 0` at the script's end, if one exists).

Implement the block following the 07-PATTERNS.md "AUGMENT scripts/check-no-cut-agents.sh" skeleton — the structure is 6 sequential `if "$RG_BIN" -n PATTERN DIRS ...; then echo ... exit 1; fi` blocks (with sweep (3), (4), (6) using a capture into a temporary variable and a `grep -v` post-filter as documented in PATTERNS).

For sweep (3), the post-filter MUST allow the legitimate hub `export type { Machine }` re-export line in `hub/src/sync/machineCache.ts`. The recommended regex post-filter is `grep -v 'hub/src/sync/machineCache\.ts:.*export type \{ Machine \}'`.

For sweep (4), the post-filter MUST allow re-export lines: `grep -v "from '@hapi/protocol"`.

For sweep (6), the post-filter MUST allow LocalResumeTarget / ResumableSession / resume.ts hits: `grep -v -E '(LocalResumeTarget|ResumableSession|resume\.ts)'`.

Each error message must mention the phase + the requirement it enforces (REFA-03 or REFA-04) so a future maintainer hitting the gate failure has direct traceability to the constraint origin.

After the final sweep, emit a single success echo: `echo "✅ Phase-7 wire-contract sweeps clean (D-126)."`.

Run `bash scripts/check-no-cut-agents.sh` after the append. If any sweep fires, STOP and investigate — the failure points to a Phase-7 invariant violation that earlier slices missed:
- Sweep (1) / (2) fires → useSSE rewrite incomplete (07-03 Task 4).
- Sweep (3) / (4) fires → cli or web type-collapse incomplete (07-03 Task 1 / Task 3).
- Sweep (5) fires → a stray `'codex'` literal — investigate the offending file.
- Sweep (6) fires → cli writer-side flavor cleanup incomplete (07-03 Task 2) OR hub still writes `metadata.flavor` somewhere.
Do not paper over a real failure with a broader whitelist.
  </action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "PHASE7_SOURCE_DIRS=\(cli/src hub/src web/src shared/src\)" scripts/check-no-cut-agents.sh` returns 1 hit.
    - `rg -n "hasUnknownSessionPatchKeys" scripts/check-no-cut-agents.sh` returns ≥ 1 hit (the sweep definition).
    - `rg -n "getSessionPatch" scripts/check-no-cut-agents.sh` returns ≥ 1 hit.
    - `rg -n "interface Machine|export.*type.*Machine" scripts/check-no-cut-agents.sh` returns ≥ 1 hit (sweep (3) pattern).
    - `rg -n "RunnerStateSchema\|MachineMetadataSchema" scripts/check-no-cut-agents.sh` returns ≥ 1 hit (sweep (4) pattern).
    - `rg -n "LocalResumeTarget\|ResumableSession\|resume\\\\.ts" scripts/check-no-cut-agents.sh` returns ≥ 1 hit (sweep (6) post-filter preserving the legitimate top-level resume-target flavor field).
    - `rg -n "Phase-7 wire-contract sweeps clean" scripts/check-no-cut-agents.sh` returns 1 hit.
    - `rg -n "REFA-0[34]" scripts/check-no-cut-agents.sh` returns ≥ 2 hits across the new sweeps (traceability).
    - `bash scripts/check-no-cut-agents.sh` exits 0.
  </acceptance_criteria>
  <done>D-126 sweep block landed; every Phase-7 source-level invariant is now CI-enforced; the broad PATTERN sweep (D-124) covers `'codex'` and other agent literals while the targeted D-126 sweeps cover the wire-contract structure.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 7 final gate — bun typecheck + bun run test + bash scripts/check-no-cut-agents.sh all exit 0</name>
  <files>(verification only — no edits)</files>
  <read_first>
    - scripts/check-no-cut-agents.sh (post-Tasks 1 + 2)
    - .planning/ROADMAP.md (Phase 7 SC#1-#4)
    - .planning/REQUIREMENTS.md (REFA-03 + REFA-04)
  </read_first>
  <action>
Run the full phase gate from the workspace root. Expected: every Phase-7 success criterion is now empirically satisfied:
- SC#1: REFA-03 source-level invariants enforced by sweeps (3) + (4) + the no-mirror state of cli/web/hub.
- SC#2: REFA-04 — `hasUnknownSessionPatchKeys` deleted (sweep (1)) and useSSE consumes shared SyncEventSchema; the strict patch schema path is the only path.
- SC#3: front-end SSE handlers consume canonical schema directly; no fallback-to-refetch branch (verified by `web/src/hooks/useSSE.test.tsx` regression-guard test).
- SC#4: `bun typecheck && bun run test` green + new tests exercise the strictly typed event stream.

If any of the three commands fails, the failure points to a regression in the prior slices — investigate at the failing layer, do not add whitelists or weaken assertions.
  </action>
  <verify>
    <automated>bun typecheck &amp;&amp; bun run test &amp;&amp; bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bun typecheck` (workspace root) exits 0.
    - `bun run test` (workspace root) exits 0.
    - `bash scripts/check-no-cut-agents.sh` exits 0 with the `✅ Phase-7 wire-contract sweeps clean (D-126).` line present in stdout.
    - Test output includes the smoking-gun regression guard (`session-updated patch with only backgroundTaskCount mutates summary without invalidate (Phase 7 REFA-04 regression guard)`) in passing tests.
    - `rg -n "hasUnknownSessionPatchKeys" cli/src hub/src web/src shared/src` returns 0 hits.
    - `rg -n "^export type Machine = \{" cli/src web/src` returns 0 hits.
    - `rg -n "RunnerStateSchema = z\.object|MachineMetadataSchema = z\.object" cli/src web/src` returns 0 hits.
    - `rg -n "'codex'" cli/src hub/src web/src shared/src` returns 0 hits.
    - `rg -n "flavor: z\." shared/src/schemas.ts` returns 0 hits.
  </acceptance_criteria>
  <done>Phase 7 complete. SC#1-#4 met. REFA-03 + REFA-04 closed. Ready for `/gsd:verify-work` + phase archival.</done>
</task>

</tasks>

<verification>
- `bash scripts/check-no-cut-agents.sh` covers Tasks 1 + 2 individually (must exit 0 after each).
- `bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh` covers Task 3 — the Phase-7 final gate.
- The ripgrep acceptance assertions on Task 3 provide independent source-level evidence that the Phase-7 invariants hold (a belt-and-suspenders check against the guard script itself silently regressing).
</verification>

<success_criteria>
- D-124 retirements complete: legacy AGENT_MESSAGE_PAYLOAD_TYPE post-filter and Phase-5 codex whitelist commentary gone from `scripts/check-no-cut-agents.sh`.
- D-126 sweep block live: 6 ripgrep checks enforce hasUnknownSessionPatchKeys / getSessionPatch / interface Machine / RunnerStateSchema / 'codex' / metadata.flavor zero-tolerance.
- D-125 Slice 4 gate met: `bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh` exits 0.
- Phase 7 ROADMAP success criteria SC#1-#4 all empirically green via the combined gate.
- REFA-03 + REFA-04 marked complete in REQUIREMENTS.md (traceability column already reflects this; verification phase confirms it).
</success_criteria>

<output>
Create `.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-04-SUMMARY.md` documenting:
- The exact line-range surgery on `scripts/check-no-cut-agents.sh` (delete + append byte counts; the Phase-7 block's line count).
- Final test count at the workspace root (compare against 07-03's baseline; expect equal or higher).
- Stdout from `bash scripts/check-no-cut-agents.sh` final run (the `✅` summary lines for each phase block) — verbatim.
- Confirmation that every Phase-7 ROADMAP SC#1-#4 has an empirical green check, with the exact ripgrep / bun command that proved each.
</output>
