---
phase: 07-wire-contracts-unification-sse-patch-contract
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - hub/src/sync/machineCache.ts
  - hub/src/sync/sessionCache.ts
  - hub/src/sync/eventPublisher.ts
  - hub/src/sync/syncEngine.ts
  - hub/src/web/routes/sessions.ts
  - hub/src/web/routes/permissions.ts
  - hub/src/notifications/sessionInfo.ts
  - hub/src/push/pushNotificationChannel.ts
  - hub/src/sync/sessionModel.test.ts
  - hub/src/web/routes/cli.test.ts
  - hub/src/web/routes/sessions.test.ts
  - hub/src/sync/aliveEvents.test.ts
  - hub/src/push/pushNotificationChannel.test.ts
  - hub/src/sync/sessionCache.test.ts
autonomous: true
requirements: [REFA-04, REFA-03]
tags: [hub, sse-contract, broadcast, flavor-cleanup]

must_haves:
  truths:
    - "hub/src/sync/machineCache.ts no longer declares local `interface Machine` or local `machineMetadataSchema`; both come from @hapi/protocol/schemas (D-111)"
    - "Every publisher.emit({...}) site in hub conforms to the post-D-119 SyncEventSchema (compile-time enforced by tightened type) — 13 sessionCache emits + 4 machineCache emits accounted for (D-127#3, RESEARCH §1)"
    - "Hub no longer writes metadata.flavor anywhere; no `?? 'cursor'` fallback survives — call sites pass the literal 'cursor' directly (D-122, RESEARCH §7)"
    - "hub/src/sync/syncEngine.ts historical-flavor defense block (lines 507–521) is deleted because it is unreachable after MetadataSchema.flavor removal (Pitfall #3)"
    - "hub/src/notifications/sessionInfo.ts::getAgentName collapses to single-Cursor presentation (no flavor branching)"
    - "Hub test fixtures no longer carry `flavor: 'cursor'` inside `metadata: { ... }` literals (sessionModel/cli/sessions/aliveEvents/pushNotificationChannel tests); top-level `flavor` field on ResumableSession/LocalResumeTarget fixtures is PRESERVED (RESEARCH §7 plan-checker note)"
    - "New hub/src/sync/sessionCache.test.ts asserts SyncEventSchema.safeParse(emit).success === true for every captured publisher.emit across representative cache transitions (D-127#3)"
    - "No backward-compat shim or .passthrough() introduced in hub — slice gate passes only because hub emits already conform to the schema by construction"
  artifacts:
    - path: "hub/src/sync/machineCache.ts"
      provides: "MachineCache class with Machine type re-exported from shared; emits conform to SyncEventSchema"
      contains: "from '@hapi/protocol/types'"
    - path: "hub/src/sync/sessionCache.test.ts"
      provides: "Broadcast contract test — every publisher.emit payload SyncEventSchema.safeParse passes"
      min_lines: 60
  key_links:
    - from: "hub/src/sync/sessionCache.ts"
      to: "@hapi/protocol/schemas SyncEventSchema"
      via: "compile-time type narrowing on publisher.emit"
      pattern: "publisher\\.emit"
    - from: "hub/src/sync/eventPublisher.ts"
      to: "@hapi/protocol/schemas SyncEventSchema"
      via: "dev-mode self-check (Claude's Discretion — recommended)"
      pattern: "SyncEventSchema\\.parse"

slice_gate:
  command: "cd hub && bun typecheck && bun test"
  must_pass: true
---

<objective>
Slice 2 of D-125: align every hub broadcast site with the post-D-119 SyncEventSchema (compile-time narrowing surfaces drift; runtime contract test in `sessionCache.test.ts` catches data-shape drift the type checker cannot see — Pitfall #2). Replace `hub/src/sync/machineCache.ts` local `interface Machine` and local `machineMetadataSchema` with re-exports from `@hapi/protocol/schemas`. Delete every `metadata.flavor` reader/writer in hub source per RESEARCH §7 (collapse `?? 'cursor'` fallback to literal `'cursor'`; delete the syncEngine historical-flavor defense block; collapse `getAgentName` to Cursor-only). Strip `flavor: 'cursor'` keys from hub test fixtures that live inside `metadata: { ... }` literals — keep top-level `flavor` fields on `ResumableSession`/`LocalResumeTarget` fixtures untouched (plan-checker invariant). Add the D-127#3 broadcast contract test driving `SessionCache` + `MachineCache` through representative transitions.

Purpose: REFA-04 enforcement at the producer side (hub) + REFA-03 collapse on the hub-local `Machine`/metadata schema duplicates. Slice 1 supplied the strict patch + tightened SyncEvent types; this slice makes hub emit through them.

Output: `cd hub && bun typecheck && bun test` green; the new contract test exercises 13 sessionCache emit sites + 4 machineCache emit sites; `'flavor'` literal as a metadata key vanishes from hub source.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-CONTEXT.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-01-SUMMARY.md

<interfaces>
Post-Slice-1 shared surface (already shipped by 07-01):
- `MachineSchema, MachineMetadataSchema, RunnerStateSchema, SessionPatchSchema, MachinePatchSchema` from `@hapi/protocol/schemas`
- `Machine, MachineMetadata, RunnerState, SessionPatch, MachinePatch` types from `@hapi/protocol/types`
- `SyncEventSchema` with tightened `data` field on session-added (=SessionSchema), session-updated (=union(Session,SessionPatch)), machine-updated (=union(Machine,MachinePatch,null))
- `MetadataSchema` with `flavor` field DELETED (default-strip — legacy SQLite metadata.flavor silently strips per Pitfall #1)
- `AGENT_MESSAGE_PAYLOAD_TYPE = 'cursor' as const`

Hub-side state to mutate (current):
- `hub/src/sync/machineCache.ts:6-16` local `machineMetadataSchema` (all-optional shape) and `:18-38` local `interface Machine` — DELETE both; replace with imports from shared. Per RESEARCH §2 Open Question 1, the lifted `MachineMetadataSchema` already adopts the defensive all-optional shape from cli — hub semantics are preserved.
- `hub/src/sync/sessionCache.ts` 13 `publisher.emit` sites enumerated in RESEARCH §1 (lines 71, 139, 231-243, 263-270, 283-287, 316-320, 339, 353, 422, 472, 507/509, 603, 613). Type checker will surface any non-conformant payload at compile time once `SyncEventSchema.data` narrows (Pitfall #2). No source edits expected if the existing payload shape already matches RESEARCH §1 enumeration — but Task 1 must read each site and confirm.
- `hub/src/sync/syncEngine.ts:507-521` historical-flavor defense block — DELETE entirely (Pitfall #3, unreachable post-D-122).
- `hub/src/sync/syncEngine.ts:391,426,458,549` `resolveFlavor()` / `flavor: this.resolveFlavor(session)` — KEEP. These feed `LocalResumeTarget` / `ResumableSession` shapes whose `flavor: 'cursor'` literal field lives in `shared/src/resume.ts` and survives Phase 7 (plan-checker invariant from RESEARCH §7).
- `hub/src/web/routes/sessions.ts:145-147,293-302,331-332,414` and `hub/src/web/routes/permissions.ts:59-61`: collapse `const flavor = session.metadata?.flavor ?? 'cursor'` → pass literal `'cursor'` at the call site. Do NOT refactor the helper signatures (`isPermissionModeAllowedForFlavor`, `getPermissionModesForFlavor`, `supportsModelChange`) — those are Phase 9 work per RESEARCH Open Question 2.
- `hub/src/notifications/sessionInfo.ts:14-18 getAgentName` — collapse to `return 'Cursor'` (or fold into the caller `hub/src/push/pushNotificationChannel.ts:3`).
- `hub/src/sync/eventPublisher.ts` — recommended (Claude's Discretion) addition: a dev-mode `SyncEventSchema.parse(event)` self-check inside `emit`, gated by `if (process.env.NODE_ENV !== 'production')` or equivalent (RESEARCH §9). This catches "hub schema drift vs web schema upgrade" silently in dev/test. Production skips the parse cost.

Test fixture strip targets (RESEARCH §7, hub-side only — cli/web in Slice 3):
- `hub/src/sync/sessionModel.test.ts` (~30 occurrences of `flavor: 'cursor'` inside `metadata: { ... }` literals at lines ~27, 44, 60, 76, 82, 99, …, 1032)
- `hub/src/web/routes/cli.test.ts` (~4 occurrences at 68, 87, 105, 124)
- `hub/src/web/routes/sessions.test.ts` (~6 occurrences)
- `hub/src/sync/aliveEvents.test.ts` (~7 occurrences)
- `hub/src/push/pushNotificationChannel.test.ts` (~1 occurrence)
- ALL: only delete `flavor` keys nested inside `metadata: { ... }`; preserve top-level `flavor` fields on `ResumableSession` / `LocalResumeTarget` / `LocalResumeRecord` fixtures.

New contract-test analog (07-PATTERNS.md "NEW hub/src/sync/sessionCache.test.ts"):
- `hub/src/sync/aliveEvents.test.ts:1-16` shows the `from 'bun:test'` import idiom + `createPublisher(events)` mock-capture pattern + the in-memory `new Store(':memory:')` setup.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse machineCache local Machine/metadata schema to shared re-export + conform every publisher.emit + delete syncEngine historical-flavor block + collapse all `?? 'cursor'` readers</name>
  <files>hub/src/sync/machineCache.ts, hub/src/sync/sessionCache.ts, hub/src/sync/eventPublisher.ts, hub/src/sync/syncEngine.ts, hub/src/web/routes/sessions.ts, hub/src/web/routes/permissions.ts, hub/src/notifications/sessionInfo.ts, hub/src/push/pushNotificationChannel.ts</files>
  <read_first>
    - hub/src/sync/machineCache.ts (full file — current local `machineMetadataSchema` lines 6-16 + `interface Machine` lines 18-38 + 4 publisher.emit sites at lines 72/127/154/165)
    - hub/src/sync/sessionCache.ts (the 13 publisher.emit sites enumerated in RESEARCH §1 — lines 71, 139, 231-243, 263-270, 283-287, 316-320, 339, 353, 422, 472, 507, 509, 603, 613; verify each payload shape matches a SessionPatchSchema field subset or a full Session)
    - hub/src/sync/eventPublisher.ts (full file — confirms `emit(event: SyncEvent)` signature; add dev-mode parse self-check)
    - hub/src/sync/syncEngine.ts lines 380-560 (historical-flavor defense block at 507-521; resolveFlavor at 391; flavor passing at 426/458/549 — KEEP these last three)
    - hub/src/web/routes/sessions.ts lines 100-450 (the 4 `?? 'cursor'` collapse sites at 145-147, 293-302, 331-332, 414 + the `c.json({...})` shapes that consumers expect)
    - hub/src/web/routes/permissions.ts lines 50-80 (the single collapse site at 59-61)
    - hub/src/notifications/sessionInfo.ts (full file — getAgentName at 14-18)
    - hub/src/push/pushNotificationChannel.ts lines 1-20 (the caller of getAgentName)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md §1 (publisher.emit field-set inventory) + §7 (flavor reader site map) + Pitfall #2 + Pitfall #3
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md "MODIFY hub/src/sync/machineCache.ts" + "MODIFY (hub + web flavor readers)"
  </read_first>
  <action>
Edit `hub/src/sync/machineCache.ts`:
- Delete the local `machineMetadataSchema = z.object({...})` declaration.
- Delete the local `export interface Machine { ... }` declaration.
- Add `import type { Machine, MachineMetadata } from '@hapi/protocol/types'` and `import { MachineMetadataSchema } from '@hapi/protocol/schemas'`. Add `export type { Machine }` so downstream consumers that already import `Machine` from `machineCache.ts` keep working without churn.
- Replace internal uses of `machineMetadataSchema` (parse calls) with the imported `MachineMetadataSchema`. The lifted schema is semantically equivalent (RESEARCH §2 Open Question 1 — adopted hub's defensive all-optional shape upstream in Slice 1), so this is a name-only swap.
- Walk each of the 4 `publisher.emit({...})` sites (lines ~72, 127, 154, 165) and verify the `data` field matches the post-D-119 `SyncEventSchema` machine-updated variant union (`Machine | MachinePatch | null`). The compile-time type narrow will surface any mismatch. Do NOT add `as any` or `as SyncEvent` casts to silence errors — if the compiler complains, the payload shape is the bug; fix the payload (likely a missing field on the full Machine emit).

Walk `hub/src/sync/sessionCache.ts` 13 emit sites enumerated in RESEARCH §1 and confirm each compiles against the post-D-119 SyncEventSchema. Expected outcome from RESEARCH §1: zero source edits — every existing emit payload already lies inside the 9-field SessionPatchSchema or is a full Session. If the compiler surfaces a previously-undocumented field name not in the SessionPatchSchema field set, STOP and report (this is a RESEARCH §1 enumeration miss that needs to be either added to SessionPatchSchema in Slice 1 amendment OR removed from the hub emit). No backward-compat shim under any circumstance (key constraint).

Edit `hub/src/sync/eventPublisher.ts`:
- Add a dev-mode self-check inside `emit`: `if (process.env.NODE_ENV !== 'production') { SyncEventSchema.parse(event); }`. Place after any pre-existing validation, before listener dispatch. Import `SyncEventSchema` from `@hapi/protocol/schemas`. This is the Claude's Discretion "recommended" item from RESEARCH §9 — it converts schema-drift bugs from "silent broken web client" into "loud hub-side test failure".

Edit `hub/src/sync/syncEngine.ts`:
- Delete lines 507–521 (the historical-flavor defense block — the entire `const historicalFlavor = metadata.flavor; if (historicalFlavor != null && historicalFlavor !== 'cursor') { return { type: 'error', message: `Sessions of flavor "${historicalFlavor}" are no longer supported` }; }` construct). Unreachable post-D-122 (Pitfall #3).
- Confirm lines 391 (`private resolveFlavor`), 426, 458, 549 (callers passing `flavor: this.resolveFlavor(session)` or `flavor: target.flavor`) remain UNCHANGED — these feed `LocalResumeTarget` / `ResumableSession` shapes from `shared/src/resume.ts` which still carry a literal `flavor: 'cursor'` field (plan-checker invariant from RESEARCH §7).

Edit `hub/src/web/routes/sessions.ts`:
- At lines 145-147, 293-302, 331-332, 414: replace each `const flavor = session.metadata?.flavor ?? 'cursor'` (or inline `metadata?.flavor ?? 'cursor'`) with the literal `'cursor'` passed directly to the helper (`isPermissionModeAllowedForFlavor(mode, 'cursor')`, `getPermissionModesForFlavor('cursor')`, `supportsModelChange('cursor')`, `const agent = 'cursor'`). Do NOT inline the helpers themselves (Phase 9 work per RESEARCH §7 Open Question 2).

Edit `hub/src/web/routes/permissions.ts`:
- At lines ~59-61: same `?? 'cursor'` → literal collapse.

Edit `hub/src/notifications/sessionInfo.ts`:
- Replace `getAgentName(session: Session): string { ... flavor branching ... }` body with `return 'Cursor'`. Keep the export signature so the caller in `pushNotificationChannel.ts` compiles unchanged — OR delete the function entirely and inline `'Cursor'` at the caller (planner discretion; prefer the simpler delete-and-inline if the caller is the only consumer).

Edit `hub/src/push/pushNotificationChannel.ts`:
- If `getAgentName` was deleted in the previous step, replace the caller's `getAgentName(...)` invocation with the literal `'Cursor'`. Otherwise, leave unchanged.

Identifier-level non-goals:
- DO NOT touch `hub/src/sync/syncEngine.ts:391/426/458/549` `resolveFlavor` / `flavor: ...` lines (resume-target.flavor field is alive — RESEARCH §7 plan-checker note).
- DO NOT refactor `isPermissionModeAllowedForFlavor` / `getPermissionModesForFlavor` / `supportsModelChange` helper bodies (Phase 9).
- DO NOT modify the `c.json({...})` response shapes in routes — the response-wrapper lift is shared-side only this phase per RESEARCH §3 (web consumers structurally satisfy via TS).
- DO NOT add any `.passthrough()` or `as any` cast to silence a strict-schema rejection. If it rejects, the payload is the bug.
  </action>
  <verify>
    <automated>cd hub &amp;&amp; bun typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "^export interface Machine\b" hub/src` returns 0 hits.
    - `rg -n "^const machineMetadataSchema" hub/src/sync/machineCache.ts` returns 0 hits.
    - `rg -n "from '@hapi/protocol/types'" hub/src/sync/machineCache.ts` returns at least 1 hit.
    - `rg -n "historicalFlavor" hub/src` returns 0 hits.
    - `rg -n "metadata\?\.flavor\s*\?\?\s*'cursor'" hub/src` returns 0 hits.
    - `rg -n "metadata\?\.flavor" hub/src` returns 0 hits (all flavor-from-metadata reads collapsed).
    - `rg -n "SyncEventSchema\.parse" hub/src/sync/eventPublisher.ts` returns at least 1 hit (dev-mode self-check landed).
    - `rg -n "flavor:" hub/src` shows only hits inside `LocalResumeTarget` / `ResumableSession` literals (resume.ts-derived shapes) — verify by manual scan.
    - `cd hub && bun typecheck` exits 0.
  </acceptance_criteria>
  <done>Hub source (excluding test fixtures handled in Task 2) compiles against the post-Slice-1 shared schemas; every publisher.emit conforms by virtue of the tightened type; historical-flavor defense block deleted; getAgentName collapsed to Cursor-only; no `?? 'cursor'` fallback remains in hub routing source.</done>
</task>

<task type="auto">
  <name>Task 2: Strip `flavor: 'cursor'` from hub test fixtures (metadata-nested only — preserve resume-target top-level flavor) + add D-127#3 broadcast contract test in hub/src/sync/sessionCache.test.ts</name>
  <files>hub/src/sync/sessionModel.test.ts, hub/src/web/routes/cli.test.ts, hub/src/web/routes/sessions.test.ts, hub/src/sync/aliveEvents.test.ts, hub/src/push/pushNotificationChannel.test.ts, hub/src/sync/sessionCache.test.ts</files>
  <read_first>
    - hub/src/sync/aliveEvents.test.ts (full file — analog for the new contract test; the mock-publisher `createPublisher(events)` idiom at lines 10-16 + the in-memory Store + SessionCache + MachineCache wiring)
    - hub/src/sync/sessionModel.test.ts (skim — locate `flavor: 'cursor'` occurrences inside `metadata: { ... }` literals; ~30 sites at the line numbers listed in RESEARCH §7)
    - hub/src/web/routes/cli.test.ts (4 occurrences at 68/87/105/124)
    - hub/src/web/routes/sessions.test.ts (6 occurrences)
    - hub/src/sync/aliveEvents.test.ts (7 occurrences — distinguish metadata-nested from any LocalResumeTarget literals)
    - hub/src/push/pushNotificationChannel.test.ts (1 occurrence)
    - shared/src/resume.ts (verify the LocalResumeTarget / ResumableSession schema shape includes a top-level `flavor: AgentFlavorSchema` field — these literals must be PRESERVED in any fixture, per RESEARCH §7 plan-checker note)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md "NEW hub/src/sync/sessionCache.test.ts" section (the createPublisher + drive-and-assert pattern)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md §1 (the 13 sessionCache emit sites — the contract test should drive transitions that hit each one) + Pitfall #2 (scope of contract test: data-shape drift, not compile-time coverage)
  </read_first>
  <behavior>
    Contract test (`hub/src/sync/sessionCache.test.ts`) cases:
    - Create in-memory Store + SessionCache + MachineCache with a `createPublisher(events: SyncEvent[])` mock.
    - Drive SessionCache through representative transitions covering each RESEARCH §1 emit site: `getOrCreateSession` (→ session-added), `handleSessionAlive` (→ session-updated with mode/active/thinking fields), `markMessageQueued` (→ session-updated with thinking+updatedAt), `applyBackgroundTaskDelta(+1)` (→ session-updated single-field backgroundTaskCount — the smoking-gun fixture), `recordSessionActivity` (→ session-updated updatedAt), `handleSessionEnd` (→ session-updated active:false+thinking:false+backgroundTaskCount:0), `expireInactive` (→ session-updated active:false), `applySessionConfig` (→ session-updated full Session), `deleteSession` (→ session-removed), `mergeSessionData` (→ messages-invalidated + session-removed + session-updated full).
    - Drive MachineCache: `refreshMachine` (→ machine-updated full Machine), `handleMachineAlive` (→ machine-updated full Machine), `expireInactive` (→ machine-updated `{ active: false }`), deletion (→ machine-updated data:null).
    - For every captured event in the `events` array: `expect(SyncEventSchema.safeParse(event).success).toBe(true)`. On failure, the assertion message must include the event JSON for debuggability (`if (!result.success) throw new Error(...)` pattern from PATTERNS).
    - Fixture-strip behavior: existing tests that previously declared `metadata: { ..., flavor: 'cursor', ... }` must continue to pass after `flavor` is deleted from those literal blocks (default-strip behavior on legacy data is verified separately in Slice 1's schemas.test.ts; here we just confirm the test infra no longer carries the key).
  </behavior>
  <action>
For each hub test file (`sessionModel.test.ts`, `cli.test.ts`, `sessions.test.ts`, `aliveEvents.test.ts`, `pushNotificationChannel.test.ts`): walk every occurrence of `flavor:` and apply the plan-checker discriminator from RESEARCH §7:
- If `flavor:` appears as a key inside a `metadata: { ... }` literal (or inside a builder that produces metadata, e.g. `buildSessionMetadata({ flavor: 'cursor', ... })`), DELETE the `flavor` key (and clean up the trailing comma).
- If `flavor:` appears as a top-level field on a `LocalResumeTarget` / `ResumableSession` / `LocalResumeRecord` literal (typically alongside `sessionId`, `path`, `host`, `agentSessionId`, `permissionMode`), PRESERVE it — those shapes' Zod schemas still require `flavor: z.literal('cursor')` (verified via reading `shared/src/resume.ts`).

Create `hub/src/sync/sessionCache.test.ts` (NEW file — does not exist today). Follow the `bun:test` + `createPublisher(events: SyncEvent[])` mock pattern from `hub/src/sync/aliveEvents.test.ts:1-16`. Import `{ describe, expect, it } from 'bun:test'`, `import type { SyncEvent } from '@hapi/protocol/types'`, `import { SyncEventSchema } from '@hapi/protocol/schemas'`, `import { Store } from '../store'`, `import type { EventPublisher } from './eventPublisher'`, `import { MachineCache } from './machineCache'`, `import { SessionCache } from './sessionCache'`. Implement a single `describe('SessionCache + MachineCache emit contract', () => { ... })` that drives the transitions enumerated in `<behavior>` and asserts `SyncEventSchema.safeParse(event).success === true` for each captured event. Group transitions into 2–4 `it()` cases (e.g. one per cache class + one per high-traffic transition cluster like background-task / mode-change). Build the minimum-required initial Session/Machine fixtures (no `flavor` key in metadata).

Identifier-level non-goals:
- Do NOT delete top-level `flavor: 'cursor'` keys on `LocalResumeTarget` / `ResumableSession` / `LocalResumeRecord` fixtures.
- Do NOT change non-flavor fields in any fixture (e.g. `path`, `host`, `version`, `summary`, `worktree`, `agentSessionId`, `capabilities` stay).
- Do NOT extend `hub/src/sse/sseManager.test.ts` with the contract block — PATTERNS recommends the new `sessionCache.test.ts` file specifically (sseManager is visibility/relevance, not broadcast).
  </action>
  <verify>
    <automated>cd hub &amp;&amp; bun test src/sync/sessionCache.test.ts src/sync/aliveEvents.test.ts src/sync/sessionModel.test.ts src/web/routes/sessions.test.ts src/web/routes/cli.test.ts src/push/pushNotificationChannel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `hub/src/sync/sessionCache.test.ts` exists; `rg -n "SyncEventSchema\.safeParse" hub/src/sync/sessionCache.test.ts` returns at least 1 hit; `rg -n "applyBackgroundTaskDelta" hub/src/sync/sessionCache.test.ts` returns at least 1 hit (smoking-gun transition covered).
    - `rg -n "flavor: 'cursor'" hub/src/sync/sessionModel.test.ts` shows only hits inside `LocalResumeTarget`/`ResumableSession`/`LocalResumeRecord` literals (manual scan: each remaining hit's parent literal contains `sessionId:`/`path:`/`host:` keys typical of resume-target shapes, NOT inside a `metadata: { ... }` block).
    - `rg -n "flavor: 'cursor'" hub/src/web/routes/cli.test.ts hub/src/web/routes/sessions.test.ts hub/src/sync/aliveEvents.test.ts hub/src/push/pushNotificationChannel.test.ts` — every remaining hit (if any) lies inside a resume-target literal (verify per RESEARCH §7).
    - `rg -n "metadata: {[^}]*flavor:" hub/src` returns 0 hits (no metadata literal still carries flavor).
    - Each listed test file passes individually; the contract test asserts SyncEventSchema.safeParse success across all driven transitions.
  </acceptance_criteria>
  <done>Hub test fixtures purged of `metadata.flavor` keys (resume-target top-level `flavor` preserved); new `sessionCache.test.ts` exercises every representative cache transition and asserts every emit passes `SyncEventSchema.safeParse`.</done>
</task>

<task type="auto">
  <name>Task 3: Slice 2 gate — `cd hub && bun typecheck && bun test`</name>
  <files>(verification only — no edits)</files>
  <read_first>
    - hub/src/sync/sessionCache.ts (post-Task-1)
    - hub/src/sync/machineCache.ts (post-Task-1)
    - hub/src/sync/sessionCache.test.ts (post-Task-2)
  </read_first>
  <action>
Run the hub-workspace slice gate. The dev-mode `SyncEventSchema.parse` in `eventPublisher.emit` will exercise every captured emit during the test run (since `NODE_ENV !== 'production'` in the test environment) — any hub-side payload drift surfaces as a test failure here. The new `sessionCache.test.ts` adds a layer of explicit-assertion contract testing on top. Workspace-wide `bun typecheck && bun run test` is held until Slice 4 (the final phase gate), since cli + web still carry their pre-Slice-3 duplicate Machine / RunnerState declarations that would clash with the new shared exports at this point. (Reason: the existing local declarations in cli and web do not import from `@hapi/protocol/types`, so they coexist with the shared additions without a typecheck failure at the WORKSPACE level. If `bun typecheck` at workspace root fails for an unexpected reason, STOP and report.)
  </action>
  <verify>
    <automated>cd hub &amp;&amp; bun typecheck &amp;&amp; bun test</automated>
  </verify>
  <acceptance_criteria>
    - `cd hub && bun typecheck` exits 0.
    - `cd hub && bun test` exits 0; `sessionCache.test.ts` PASSED; `aliveEvents.test.ts` still green; the dev-mode `SyncEventSchema.parse` in `eventPublisher.emit` did NOT throw during any test run.
    - `bun typecheck` (workspace root) exits 0 — cli/web local Machine/RunnerState duplicates are still present but do not collide with shared additions yet (collapse happens in Slice 3).
  </acceptance_criteria>
  <done>Slice 2 of D-125 green — hub produces wire-conformant events by construction; data-shape contract verified by the new sessionCache.test.ts; flavor field gone from hub source + metadata-nested test fixtures.</done>
</task>

</tasks>

<verification>
- `cd hub && bun typecheck && bun test` is the slice gate.
- `cd hub && bun test src/sync/sessionCache.test.ts` covers the new contract test in isolation.
- The dev-mode `SyncEventSchema.parse` inside `eventPublisher.emit` (Task 1) acts as a continuous in-test sentinel — if any pre-existing hub test drives a transition that produces a non-conformant payload, that test fails during Slice 2 even without explicit contract assertions.
</verification>

<success_criteria>
- D-125 Slice 2 gate met: `cd hub && bun typecheck && bun test` green.
- REFA-04: Hub broadcast contract enforced — every publisher.emit conforms to SyncEventSchema by virtue of the tightened type (Slice 1) and verified at runtime by the new contract test (D-127#3) and the dev-mode eventPublisher self-check.
- REFA-03: machineCache.ts no longer declares local `Machine` interface or local `machineMetadataSchema`.
- D-122 (hub side): All metadata.flavor reads collapsed to literal 'cursor' or deleted entirely; syncEngine historical-flavor defense block removed; getAgentName collapsed; hub test fixtures purged of metadata-nested flavor keys.
- Plan-checker invariant respected: resume-target top-level `flavor` field preserved everywhere.
</success_criteria>

<output>
Create `.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-02-SUMMARY.md` documenting:
- Whether any RESEARCH §1 emit-site enumeration miss surfaced (e.g. a hub emit field not in the 9-field SessionPatchSchema set) and how it was resolved.
- The chosen disposition for `getAgentName` (collapsed to `return 'Cursor'` vs deleted-and-inlined).
- The final number of test-fixture `flavor: 'cursor'` deletions per file vs preserved resume-target hits.
- The dev-mode `SyncEventSchema.parse` placement inside `eventPublisher.emit` and any environment-guard logic.
</output>
