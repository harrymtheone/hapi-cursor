---
phase: 07-wire-contracts-unification-sse-patch-contract
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - hub/src/sync/machineCache.ts
  - hub/src/sync/eventPublisher.ts
  - hub/src/sync/sessionCache.test.ts
autonomous: true
requirements: [REFA-03, REFA-04]
tags: [hub, sse, broadcast-contract, machine-cache, sync-engine]

must_haves:
  truths:
    - "Every publisher.emit payload produced by SessionCache + MachineCache representative state transitions passes SyncEventSchema.safeParse at runtime (D-127#3 contract test)"
    - "hub/src/sync/machineCache.ts no longer declares its own local machineMetadataSchema — defensive optional-tolerant decode is rebuilt on shared MachineMetadataSchema (REFA-03 single-source invariant, D-111)"
    - "eventPublisher.emit guards against schema drift via dev-mode SyncEventSchema.parse() self-check (Claude's Discretion item per RESEARCH §9; production skips parse cost)"
    - "hub historical-flavor defense block, getAgentName flavor read, web-route ?? 'cursor' fallbacks, and local interface Machine were already removed in 07-01 (deviations doc) — slice 02 does NOT redo that work"
    - "Slice 2 gate: bun typecheck && bun run test green at workspace root; new hub contract test green (D-125 gate #2)"
  artifacts:
    - path: "hub/src/sync/sessionCache.test.ts"
      provides: "Broadcast contract test driving SessionCache + MachineCache through alive / mode-change / background-task-delta / session-end / inactive / merge / activate-deactivate transitions and asserting SyncEventSchema.safeParse(emit).success === true on every captured event (D-127#3, RESEARCH §1 13-emit-site coverage)"
      contains: "SyncEventSchema.safeParse"
      min_lines: 100
    - path: "hub/src/sync/machineCache.ts"
      provides: "Local machineMetadataSchema removed; uses shared MachineMetadataSchema for defensive decode while preserving 'unknown' fallback for missing required fields"
    - path: "hub/src/sync/eventPublisher.ts"
      provides: "Optional dev-mode SyncEventSchema.parse self-check (NODE_ENV !== 'production' gate)"
  key_links:
    - from: "hub/src/sync/sessionCache.test.ts"
      to: "@hapi/protocol/schemas"
      via: "SyncEventSchema import + safeParse contract assertion"
      pattern: "SyncEventSchema.*safeParse"
    - from: "hub/src/sync/machineCache.ts"
      to: "@hapi/protocol/schemas"
      via: "MachineMetadataSchema import (replaces local schema)"
      pattern: "MachineMetadataSchema.*from '@hapi/protocol/schemas'"

slice_gate:
  command: "bun typecheck && bun run test && cd hub && bun test src/sync/sessionCache.test.ts"
  must_pass: true
---

<objective>
Slice 2 of D-125: prove (at runtime, beyond TS type-checking) that every `publisher.emit` payload from `SessionCache` + `MachineCache` conforms to the tightened `SyncEventSchema` shipped in 07-01. Most of the slice's original scope (hub flavor reads, historical-flavor defense block, hub-local `interface Machine` collision, web-route `?? 'cursor'` fallbacks) was already absorbed into 07-01 as Rule-3 deviations — see 07-01-SUMMARY.md "Deviations from Plan". What remains: (1) the D-127#3 broadcast contract test (Pitfall #2 in RESEARCH calls out that the runtime contract catches data drift the type checker cannot — nested object mutation, runtime field synthesis); (2) collapse `hub/src/sync/machineCache.ts`'s local `machineMetadataSchema` onto shared `MachineMetadataSchema` so `RunnerStateSchema|MachineMetadataSchema` truly live only in shared (REFA-03); (3) the dev-mode `eventPublisher.emit` self-check recommended by RESEARCH §9 + Claude's Discretion in CONTEXT.md.

Purpose: lock in REFA-04's strict-patch contract at the hub side with a runtime-evidence gate, and complete REFA-03's "shared single source" invariant by removing the last duplicate `MachineMetadataSchema` declaration in `hub/`.

Output: Hub broadcast contract test green; local `machineMetadataSchema` deleted from `machineCache.ts`; optional dev-mode parse self-check landed in `eventPublisher.emit`; slice-2 gate green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-CONTEXT.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md
@.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-01-SUMMARY.md

<interfaces>
Already-shipped in 07-01 (DO NOT re-do):
- `SyncEventSchema` discriminator with `data: SessionSchema` (session-added), `data: z.union([SessionSchema, SessionPatchSchema])` (session-updated), `data: z.union([MachineSchema, MachinePatchSchema, z.null()])` (machine-updated)
- `SessionPatchSchema` (strict, 9 optional fields per RESEARCH §1)
- `MachinePatchSchema` (`{ active: z.literal(false), activeAt: z.number().optional() }`, strict)
- `MachineSchema`, `MachineMetadataSchema`, `RunnerStateSchema` in `shared/src/schemas.ts`
- `hub/src/sync/machineCache.ts` already deleted its local `interface Machine` and uses `import type { Machine } from '@hapi/protocol/types'`
- `hub/src/sync/syncEngine.ts` historical-flavor defense block already deleted
- `hub/src/notifications/sessionInfo.ts::getAgentName` already collapsed to `'Cursor'`
- `hub/src/web/routes/{sessions,permissions}.ts` `?? 'cursor'` fallbacks already collapsed to literal
- `hub/src/socket/handlers/cli/{session,machine}Handlers.ts` emit shapes already adjusted

Still alive in 07-02 scope (verify before editing):
- `hub/src/sync/machineCache.ts:10-20` — local `machineMetadataSchema` (all-optional) still present; lines 61-89 rebuild a defensive object with `'unknown'` fallbacks
- `hub/src/sync/eventPublisher.ts` — no schema self-check; `emit(event: SyncEvent)` only relies on TS narrowing
- No `hub/src/sync/sessionCache.test.ts` exists yet (the closest analog is `hub/src/sync/aliveEvents.test.ts` per PATTERNS)

`hub/src/sync/sessionCache.ts` 13 emit sites enumerated in RESEARCH §1 (lines 71, 139, 231-243, 263-270, 283-287, 316-320, 339, 353, 422, 472, 507-509, 603, 613) — the contract test drives the cache through transitions that hit each site at least once.

`hub/src/sync/machineCache.ts` emit sites enumerated in RESEARCH §1 (lines 54 [null], 113, 140, 151) — already wired in 07-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create hub/src/sync/sessionCache.test.ts contract test driving SessionCache + MachineCache through representative transitions and asserting SyncEventSchema.safeParse success on every emit</name>
  <files>hub/src/sync/sessionCache.test.ts</files>
  <read_first>
    - hub/src/sync/aliveEvents.test.ts (full file — analog for `bun:test` createPublisher capture pattern per 07-PATTERNS "NEW (or extend) hub/src/sync/sessionCache.test.ts" section)
    - hub/src/sync/sessionCache.ts (full file — 13 publisher.emit call sites; constructor signature accepting Store + EventPublisher; the public API: getOrCreateSession, handleSessionAlive, markMessageQueued, applyBackgroundTaskDelta, recordSessionActivity, handleSessionEnd, expireInactive, applySessionConfig, deleteSession, mergeSessionData)
    - hub/src/sync/machineCache.ts (full file — MachineCache constructor + getOrCreateMachine, refreshMachine, handleMachineAlive, expireInactive)
    - hub/src/sync/eventPublisher.ts (full file — EventPublisher.emit signature, SyncEvent type import path)
    - shared/src/schemas.ts (post-07-01 — verify SyncEventSchema, SessionPatchSchema, MachinePatchSchema all exported)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md §1 (the 13 sessionCache emit sites + 4 machineCache emit sites enumeration — every transition the test must trigger)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-PATTERNS.md "Pattern C — Mock-publisher capture for emit-contract tests" + "NEW (or extend) hub/src/sync/sessionCache.test.ts" (uses `bun:test`, NOT vitest)
  </read_first>
  <behavior>
    Test cases (one describe block grouping the contract assertions; `bun:test` framework):
    - SessionCache transitions: drive getOrCreateSession → handleSessionAlive (with permissionMode + model change variations) → markMessageQueued → applyBackgroundTaskDelta(+1 then -1) → recordSessionActivity → handleSessionEnd → expireInactive (after activeAt advance) → applySessionConfig → deleteSession → mergeSessionData (with both delete-old and refresh-new paths). Every captured emit asserted via `SyncEventSchema.safeParse(event).success === true`; on failure include the offending event JSON in the assertion message for debugging.
    - The `applyBackgroundTaskDelta` step MUST be present and MUST result in a captured `session-updated` event whose `data` is exactly `{ backgroundTaskCount: <number> }` — this is the smoking-gun fixture (RESEARCH §1 line 283-287; the field that historically caused `hasUnknownSessionPatchKeys = true` and integer-list refetch in web).
    - MachineCache transitions: drive getOrCreateMachine → refreshMachine → handleMachineAlive (with broadcast-throttle scenario) → expireInactive (triggers `data: { active: false }` patch) → refreshMachine on a non-existent machine (triggers `data: null`). Every captured emit asserted via `SyncEventSchema.safeParse(event).success === true`.
    - Smoke assertion: after all transitions, total captured event count ≥ 8 (sanity check that the suite actually exercised the cache, not just constructed it).
  </behavior>
  <action>
Create the new test file at `hub/src/sync/sessionCache.test.ts` following the `aliveEvents.test.ts` analog idiom. Imports: `{ describe, expect, it } from 'bun:test'`; `type { SyncEvent } from '@hapi/protocol/types'`; `{ SyncEventSchema } from '@hapi/protocol/schemas'`; `{ Store } from '../store'`; `type { EventPublisher } from './eventPublisher'`; `{ MachineCache } from './machineCache'`; `{ SessionCache } from './sessionCache'`. Define the `createPublisher(events: SyncEvent[]): EventPublisher` helper that pushes every emitted event into an array (verbatim shape from aliveEvents.test.ts:10-16).

Implement one top-level `describe('publisher.emit contract — SyncEventSchema conformance', () => { ... })` containing two nested `describe('SessionCache')` and `describe('MachineCache')` blocks. Use `Store(':memory:')` for in-memory SQLite. Drive each cache through the representative transition matrix enumerated under <behavior>. Use minimal valid metadata literals (path, host, machineId) — do NOT include a `flavor` key (the field is gone from MetadataSchema post-07-01).

After driving each transition group, iterate the captured `events` array and assert via:
- `for (const event of events) { const result = SyncEventSchema.safeParse(event); if (!result.success) { throw new Error(`emit violates SyncEventSchema: ${JSON.stringify(event)} — ${result.error.message}`); } expect(result.success).toBe(true); }`
- After the loop, `expect(events.length).toBeGreaterThanOrEqual(8)`.

Add an explicit `it('applyBackgroundTaskDelta emits a session-updated patch with exactly { backgroundTaskCount } data field', ...)` that captures emits during a single `applyBackgroundTaskDelta` call, finds the resulting `session-updated` event, asserts `result.data` keys equals `['backgroundTaskCount']`, and asserts the value is a finite number. This is the test that proves the schema accepts the historically-rejected (by web heuristic) single-field patch.

Add an explicit `it('refreshMachine on non-existent machineId emits machine-updated with data:null', ...)` and `it('expireInactive emits machine-updated with data:{ active: false }', ...)`.

Add a final `it('SessionCache.applySessionConfig emits session-updated with full Session payload', ...)` so the union-arm with full-Session-shape is also covered (sessionCache.ts:422 emits `data: session`).

Do NOT mix Vitest idioms — this is `bun:test` only. Do NOT add `flavor` keys to any metadata literal. Do NOT add `.passthrough()` workarounds — if a test fixture fails `safeParse`, the cache emit is the bug, not the schema.
  </action>
  <verify>
    <automated>cd hub &amp;&amp; bun test src/sync/sessionCache.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `hub/src/sync/sessionCache.test.ts` exists.
    - `rg -n "from 'bun:test'" hub/src/sync/sessionCache.test.ts` returns exactly 1 hit; `rg -n "from 'vitest'" hub/src/sync/sessionCache.test.ts` returns 0 hits.
    - `rg -n "SyncEventSchema\.safeParse" hub/src/sync/sessionCache.test.ts` returns at least 1 hit (the per-event contract assertion).
    - `rg -n "backgroundTaskCount" hub/src/sync/sessionCache.test.ts` returns at least 1 hit (the smoking-gun fixture).
    - `rg -n "applyBackgroundTaskDelta|handleSessionAlive|expireInactive|applySessionConfig|deleteSession|mergeSessionData" hub/src/sync/sessionCache.test.ts` returns at least 6 distinct method names (transition coverage).
    - `rg -n "machine-updated.*data: null|data: null.*machine-updated|refreshMachine" hub/src/sync/sessionCache.test.ts` confirms machine-null + machine-patch transitions are covered.
    - `rg -n "flavor:" hub/src/sync/sessionCache.test.ts` returns 0 hits (no flavor in any test metadata literal).
    - `cd hub && bun test src/sync/sessionCache.test.ts` exits 0; reported test count ≥ 4 distinct `it()` cases.
  </acceptance_criteria>
  <done>Runtime contract test green; every captured publisher.emit payload SyncEventSchema-validates; smoking-gun backgroundTaskCount single-field patch documented as a passing fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse hub/src/sync/machineCache.ts local machineMetadataSchema onto shared MachineMetadataSchema (close REFA-03 single-source invariant in hub)</name>
  <files>hub/src/sync/machineCache.ts</files>
  <read_first>
    - hub/src/sync/machineCache.ts (full file — locate local `machineMetadataSchema` lines 10-20 and the defensive decode block lines 61-89 that builds the metadata object with 'unknown' fallbacks for missing required fields)
    - shared/src/schemas.ts (post-07-01 — read MachineMetadataSchema definition; note it is strict-required for host/platform/happyCliVersion/homeDir/happyHomeDir/happyLibDir per 07-01-SUMMARY.md "MachineMetadataSchema shape decision" — verbatim from cli, NOT the looser all-optional shape)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-01-SUMMARY.md "MachineMetadataSchema shape decision" section (explains why the strict cli verbatim shape was chosen and that the defensive 'unknown' fallback now lives at the hub read site, not in the schema)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md §2 Open Question 1 (the strict-required-vs-defensive-optional tradeoff)
  </read_first>
  <behavior>
    - Local `const machineMetadataSchema` declaration (lines 10-20) is deleted.
    - The defensive read block at `refreshMachine` (lines 61-89) is rewritten to use shared `MachineMetadataSchema.safeParse(stored.metadata)` directly. Because the shared schema is strict-required, the safeParse may fail for legacy / stale machine rows that lack `homeDir/happyHomeDir/happyLibDir` — in that case the existing defensive behavior (fill `'unknown'` for missing string fields) must be preserved by pre-massaging the raw `stored.metadata` (object-spread + supply default `'unknown'` strings for the 6 required string keys when they are missing or non-string) BEFORE passing to `MachineMetadataSchema.safeParse`. If safeParse still fails after the defensive massage, fall back to `metadata: null` (existing behavior on parse failure).
    - All 4 publisher.emit sites in this file (lines 54, 113, 140, 151) remain unchanged in shape — they already satisfy `MachineSchema | MachinePatchSchema | null` (verified by 07-01 typecheck pass).
    - `cd hub && bun typecheck` + `cd hub && bun test` stay green.
    - Hub contract test from Task 1 stays green (machine transitions still emit conformant events).
  </behavior>
  <action>
Delete the local `machineMetadataSchema` constant (lines 10-20). Import `MachineMetadataSchema` from `@hapi/protocol/schemas` (extend the existing `import { RunnerStateSchema } from '@hapi/protocol/schemas'` line into `import { MachineMetadataSchema, RunnerStateSchema } from '@hapi/protocol/schemas'`).

Rewrite the `metadata` IIFE inside `refreshMachine` (currently lines 61-89). The new flow:
1. If `stored.metadata` is not a plain object, return `null` (existing behavior).
2. Build a `defensive` shallow-clone of `stored.metadata` where each of the 6 required string keys (`host`, `platform`, `happyCliVersion`, `homeDir`, `happyHomeDir`, `happyLibDir`) is replaced by `'unknown'` if the original value is missing or not a string. Leave `displayName`, `workspaceRoot`, `workspaceRoots` untouched (the shared schema treats those as `.optional()`).
3. Call `MachineMetadataSchema.safeParse(defensive)`. The shared schema's `.transform` will normalize `workspaceRoot ↔ workspaceRoots` per the existing post-07-01 transform body.
4. If `parsed.success === false`, return `null` (matches current parse-failure behavior).
5. If success, return `parsed.data` directly — the manual workspaceRoots normalization at lines 72-78 is dropped because the shared schema's `.transform` already does the equivalent work.

Verify post-edit: `rg -n "machineMetadataSchema" hub/src/sync/machineCache.ts` returns 0 hits (local schema gone); the import line now references the shared `MachineMetadataSchema`. The 4 `publisher.emit` sites are byte-identical to pre-edit.
  </action>
  <verify>
    <automated>cd hub &amp;&amp; bun typecheck &amp;&amp; bun test src/sync/sessionCache.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "const machineMetadataSchema" hub/src/sync/machineCache.ts` returns 0 hits.
    - `rg -n "MachineMetadataSchema" hub/src/sync/machineCache.ts` returns at least 1 hit (the import + the safeParse call).
    - `rg -n "import .* MachineMetadataSchema.*from '@hapi/protocol/schemas'" hub/src/sync/machineCache.ts` returns 1 hit (single canonical import).
    - `rg -n "RunnerStateSchema.safeParse" hub/src/sync/machineCache.ts` still returns at least 1 hit (the pre-existing runnerState defensive parse is preserved).
    - `cd hub && bun typecheck` exits 0.
    - Task 1's `hub/src/sync/sessionCache.test.ts` machine transitions still pass (MachineCache emits remain conformant).
  </acceptance_criteria>
  <done>Hub no longer carries any local Machine wire schema declaration; REFA-03 single-source invariant complete on the hub side; existing defensive 'unknown' fallback behavior preserved at the read site, not in the schema.</done>
</task>

<task type="auto">
  <name>Task 3: Add dev-mode SyncEventSchema.parse self-check to eventPublisher.emit (Claude's Discretion per RESEARCH §9 + CONTEXT.md)</name>
  <files>hub/src/sync/eventPublisher.ts</files>
  <read_first>
    - hub/src/sync/eventPublisher.ts (full file — locate the `emit(event: SyncEvent)` method body and the existing imports)
    - shared/src/schemas.ts (verify SyncEventSchema is exported)
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-CONTEXT.md `<decisions>` Claude's Discretion section ("hub eventPublisher.emit ... dev 模式 SyncEventSchema.parse() 自检")
    - .planning/phases/07-wire-contracts-unification-sse-patch-contract/07-RESEARCH.md §9 (`hub/src/sync/eventPublisher.ts:1,4,17` — usage map + recommended self-check placement)
  </read_first>
  <behavior>
    - In non-production environments (`process.env.NODE_ENV !== 'production'`), every `emit(event)` call runs `SyncEventSchema.safeParse(event)` first; on failure, logs the offending event JSON + error message via `console.error` and **still emits** (do NOT block the event — the self-check is observability, not enforcement; the contract test in Task 1 is enforcement).
    - In production (`process.env.NODE_ENV === 'production'`), the self-check is skipped entirely (zero parse cost).
    - The existing `emit` behavior (notifying listeners, SSEManager broadcast wiring) is otherwise unchanged.
    - All existing hub tests stay green; the Task 1 contract test stays green (the self-check produces no errors when the contract is honored).
  </behavior>
  <action>
Import `SyncEventSchema` from `@hapi/protocol/schemas` at the top of the file (if not already imported — check the existing import block first; the type-only `SyncEvent` import does not bring the runtime schema).

Wrap the start of the `emit(event: SyncEvent)` method body with the dev-only guard. The shape is: a single `if` condition checking `process.env.NODE_ENV !== 'production'`, inside which a `safeParse` runs and on `result.success === false` calls `console.error('[eventPublisher] emit violates SyncEventSchema:', JSON.stringify(event), result.error.message)`. Place this BEFORE the existing listener-notification logic. Do NOT throw; do NOT return early; do NOT swallow the event. This is observability-only.

Verify by reading the existing `emit` implementation — preserve every existing line of behavior; only prepend the dev-guard block.
  </action>
  <verify>
    <automated>cd hub &amp;&amp; bun typecheck &amp;&amp; bun test src/sync</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SyncEventSchema" hub/src/sync/eventPublisher.ts` returns at least 1 hit (the import + the safeParse call).
    - `rg -n "process\.env\.NODE_ENV" hub/src/sync/eventPublisher.ts` returns at least 1 hit (the dev-mode guard).
    - `rg -n "console\.error.*eventPublisher.*SyncEventSchema" hub/src/sync/eventPublisher.ts` returns 1 hit (the observability log).
    - The dev-guard block does NOT contain `return`, `throw`, or any early-exit keyword between the `safeParse` and the existing listener-notification call.
    - `cd hub && bun typecheck` exits 0; `cd hub && bun test src/sync` exits 0.
  </acceptance_criteria>
  <done>eventPublisher.emit logs schema violations in dev/test, stays silent + zero-cost in production, never blocks an event from reaching SSE listeners.</done>
</task>

<task type="auto">
  <name>Task 4: Slice 2 gate — workspace typecheck + full test suite + new hub contract test all green</name>
  <files>(verification only — no edits)</files>
  <read_first>
    - hub/src/sync/sessionCache.test.ts (post-Task-1)
    - hub/src/sync/machineCache.ts (post-Task-2)
    - hub/src/sync/eventPublisher.ts (post-Task-3)
  </read_first>
  <action>
Run the slice-2 gate from the workspace root. Expected baseline: 532 tests from 07-01 plus the new contract test cases in `hub/src/sync/sessionCache.test.ts`. If the count diverges by more than the new cases, investigate before declaring slice 2 complete. The hub `bun typecheck` was already green at the end of 07-01; this slice's changes are additive (new test file, internal refactor of machineCache decode block, observability hook in eventPublisher) and must not break any existing test.
  </action>
  <verify>
    <automated>bun typecheck &amp;&amp; bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `bun typecheck` (workspace root) exits 0.
    - `bun run test` (workspace root) exits 0.
    - Test count ≥ 532 + (new it() cases from Task 1, ≥ 4) at the workspace root.
    - `rg -n "const machineMetadataSchema" hub/src/sync/machineCache.ts` returns 0 hits.
    - `rg -n "SyncEventSchema" hub/src/sync/eventPublisher.ts` returns ≥ 1 hit.
    - The new test file's smoking-gun assertion (the `backgroundTaskCount`-single-field-patch case) appears in passing output.
  </acceptance_criteria>
  <done>Slice 2 of D-125 green; hub broadcast contract empirically validated; machineCache stripped of its last local wire schema; eventPublisher carries dev-mode self-check observability.</done>
</task>

</tasks>

<verification>
- `cd hub && bun test src/sync/sessionCache.test.ts` covers Task 1 (D-127#3 contract test).
- `cd hub && bun typecheck` + Task 1's test re-run cover Task 2 (machineCache schema collapse).
- `cd hub && bun test src/sync` covers Task 3 (eventPublisher self-check does not regress existing hub tests).
- `bun typecheck && bun run test` (workspace root) covers Task 4 slice gate.
</verification>

<success_criteria>
- D-125 Slice 2 gate met: workspace `bun typecheck && bun run test` green; new `hub/src/sync/sessionCache.test.ts` green.
- D-127#3 satisfied: every representative `publisher.emit` payload conforms to `SyncEventSchema` at runtime (the contract test is the evidence).
- REFA-03 hub-side completion: `MachineMetadataSchema` / `RunnerStateSchema` declarations live ONLY in `shared/src/schemas.ts`; hub re-exports `Machine` type via `import type` only.
- REFA-04 runtime evidence: the `backgroundTaskCount` single-field patch — the smoking-gun fixture for the deleted `hasUnknownSessionPatchKeys` heuristic — flows through the hub emit pipeline as a schema-valid event.
- Observability: `eventPublisher.emit` carries a non-blocking dev-mode self-check so future schema drift surfaces in dev/test rather than silently breaking the web.
</success_criteria>

<output>
Create `.planning/phases/07-wire-contracts-unification-sse-patch-contract/07-02-SUMMARY.md` documenting:
- Test count delta vs 07-01's 532-test baseline.
- Confirmation that `hub/src/sync/machineCache.ts` carries zero local Zod schema declarations after Task 2.
- Any new emit shape needed in `sessionCache.ts` that surfaced during Task 1 (should be none if 07-01 cascade-fixes were correct; if any, list line + emit + new shape).
- Whether the eventPublisher dev-mode self-check fired any error during the full test run (it should not — if it did, the offending emit and resolution).
</output>
