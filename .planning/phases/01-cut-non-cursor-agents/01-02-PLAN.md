---
phase: 01-cut-non-cursor-agents
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - cli/src/codex/
  - cli/src/commands/codex.ts
  - cli/src/commands/codex.test.ts
  - cli/src/commands/registry.ts
  - cli/src/commands/resume.ts
  - cli/src/commands/resume.test.ts
  - cli/src/agent/messageConverter.ts
  - cli/src/agent/messageConverter.test.ts
  - cli/src/cursor/cursorRemoteLauncher.ts
  - cli/src/api/apiMachine.ts
  - cli/src/api/apiMachine.test.ts
  - cli/src/api/types.ts
  - cli/src/runner/run.ts
  - cli/src/runner/buildCliArgs.test.ts
  - hub/src/sync/todos.ts
  - hub/src/sync/syncEngine.ts
  - hub/src/sync/rpcGateway.ts
  - hub/src/sync/rpcGateway.test.ts
  - hub/src/sync/sessionCache.ts
  - hub/src/sync/sessionModel.test.ts
  - hub/src/sync/aliveEvents.test.ts
  - hub/src/sync/teams.ts
  - hub/src/socket/handlers/cli/sessionHandlers.ts
  - hub/src/socket/handlers/cli/index.ts
  - hub/src/web/routes/sessions.ts
  - hub/src/web/routes/sessions.test.ts
  - hub/src/web/routes/machines.ts
  - hub/src/web/routes/machines.test.ts
  - hub/src/web/routes/cli.test.ts
  - hub/src/push/pushNotificationChannel.test.ts
  - shared/src/schemas.ts
  - shared/src/sessionSummary.ts
  - shared/src/socket.ts
  - shared/src/types.ts
  - web/src/lib/message-window-store.ts
  - web/src/lib/message-window-store.test.ts
  - web/src/components/AssistantChat/StatusBar.tsx
  - .github/workflows/codex-pr-review.yml
  - .github/workflows/codex-mention-response.yml
  - scripts/dev/self-test-codex-web-env.sh
  - scripts/dev/seed-codex-web-fixture.ts
autonomous: true
requirements: [CUT-02]
user_setup: []

must_haves:
  truths:
    - "Phase 1 SC#1: bun typecheck + bun run test green after CUT-02 commit"
    - "Phase 1 SC#4: codex-pr-review.yml + codex-mention-response.yml + cli/src/codex/happyMcpStdioBridge.ts removed"
    - "Wire literal AGENT_MESSAGE_PAYLOAD_TYPE = 'codex' preserved (whitelist via shared/src/modes.ts per RESEARCH §Pitfall 2)"
  artifacts:
    - path: "cli/src/codex/"
      provides: "Must NOT exist"
      absent: true
    - path: "cli/src/commands/codex.ts"
      provides: "Must NOT exist"
      absent: true
    - path: ".github/workflows/codex-pr-review.yml"
      provides: "Must NOT exist (SC#4)"
      absent: true
    - path: ".github/workflows/codex-mention-response.yml"
      provides: "Must NOT exist (SC#4)"
      absent: true
  key_links:
    - from: "hub/src/sync/todos.ts"
      to: "shared/src/modes.ts"
      via: "imports AGENT_MESSAGE_PAYLOAD_TYPE constant (no bare 'codex' literals in business code)"
      pattern: "AGENT_MESSAGE_PAYLOAD_TYPE"
---

<objective>
Execute CUT-02: physically remove the Codex agent (`cli/src/codex/` whole-tree, `happyMcpStdioBridge`, GitHub workflows, all consumer branches). Preserve the wire-level constant `AGENT_MESSAGE_PAYLOAD_TYPE = 'codex'` in `shared/src/modes.ts` (D-12 whitelist + Pitfall 2). Rename internal symbols that misleadingly carry "codex" (e.g. `extractTodosFromCodexMessage` → `extractTodosFromAgentMessage`, `CodexMessage` type → `AgentWireMessage`).

Purpose: Second atomic commit in D-14 sequence; each commit individually green per D-15.
Output: `feat(phase-01): CUT-02 remove Codex agent` commit.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md
@.planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md
@.planning/phases/01-cut-non-cursor-agents/01-VALIDATION.md
@.planning/phases/01-cut-non-cursor-agents/01-01-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Codex source tree + commands + workflows + dev scripts</name>
  <files>cli/src/codex/, cli/src/commands/codex.ts, cli/src/commands/codex.test.ts, .github/workflows/codex-pr-review.yml, .github/workflows/codex-mention-response.yml, scripts/dev/self-test-codex-web-env.sh, scripts/dev/seed-codex-web-fixture.ts</files>
  <read_first>
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"CUT-02 deletion map" → "Source dirs / files to delete outright"
    - .planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md D-05, D-14 commit #2
    - .planning/ROADMAP.md Phase 1 SC#4
    - cli/src/codex/ (top-level — confirm tree before recursive delete)
  </read_first>
  <action>
    1. `rm -rf cli/src/codex/` — entire tree including subdirs `__tests__/`, `utils/`, files codexLocal.ts, codexLocalLauncher.ts, codexRemoteLauncher.ts, codexAppServerClient.ts, codexSpecialCommands.ts, loop.ts, session.ts, runCodex.ts, appServerTypes.ts, **happyMcpStdioBridge.ts** (SC#4 explicit), and all `*.test.ts`.
    2. `rm cli/src/commands/codex.ts` (`codexCommand` definition).
    3. `rm cli/src/commands/codex.test.ts`.
    4. `rm .github/workflows/codex-pr-review.yml`.
    5. `rm .github/workflows/codex-mention-response.yml`.
    6. `rm scripts/dev/self-test-codex-web-env.sh`.
    7. `rm scripts/dev/seed-codex-web-fixture.ts`.
  </action>
  <verify>
    <automated>! test -d cli/src/codex; ! test -f cli/src/commands/codex.ts; ! test -f .github/workflows/codex-pr-review.yml; ! test -f .github/workflows/codex-mention-response.yml; ! test -f cli/src/codex/happyMcpStdioBridge.ts; ! test -f scripts/dev/self-test-codex-web-env.sh; ! test -f scripts/dev/seed-codex-web-fixture.ts</automated>
  </verify>
  <acceptance_criteria>
    - `! test -d cli/src/codex`
    - `! test -f cli/src/commands/codex.ts`
    - `! test -f cli/src/commands/codex.test.ts`
    - `! test -f .github/workflows/codex-pr-review.yml` (SC#4)
    - `! test -f .github/workflows/codex-mention-response.yml` (SC#4)
    - `! test -f scripts/dev/self-test-codex-web-env.sh`
    - `! test -f scripts/dev/seed-codex-web-fixture.ts`
  </acceptance_criteria>
  <done>Codex source + tests + workflows + dev scripts deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Codex consumers — rename wire-symbols + strip business branches</name>
  <files>cli/src/commands/registry.ts, cli/src/commands/resume.ts, cli/src/commands/resume.test.ts, cli/src/agent/messageConverter.ts, cli/src/agent/messageConverter.test.ts, cli/src/cursor/cursorRemoteLauncher.ts, cli/src/api/apiMachine.ts, cli/src/api/apiMachine.test.ts, cli/src/api/types.ts, cli/src/runner/run.ts, cli/src/runner/buildCliArgs.test.ts, hub/src/sync/todos.ts, hub/src/sync/syncEngine.ts, hub/src/sync/rpcGateway.ts, hub/src/sync/rpcGateway.test.ts, hub/src/sync/sessionCache.ts, hub/src/sync/sessionModel.test.ts, hub/src/sync/aliveEvents.test.ts, hub/src/sync/teams.ts, hub/src/socket/handlers/cli/sessionHandlers.ts, hub/src/socket/handlers/cli/index.ts, hub/src/web/routes/sessions.ts, hub/src/web/routes/sessions.test.ts, hub/src/web/routes/machines.ts, hub/src/web/routes/machines.test.ts, hub/src/web/routes/cli.test.ts, hub/src/push/pushNotificationChannel.test.ts, shared/src/schemas.ts, shared/src/sessionSummary.ts, shared/src/socket.ts, shared/src/types.ts, web/src/lib/message-window-store.ts, web/src/lib/message-window-store.test.ts, web/src/components/AssistantChat/StatusBar.tsx</files>
  <read_first>
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"CUT-02 deletion map" → "Business-consumer Codex rewrites (D-03)"
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"Summary" point 4 (AGENT_MESSAGE_PAYLOAD_TYPE is wire-level)
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"Pitfall 2" (do NOT remove 'codex' string value from modes.ts; import as constant in business code instead)
    - .planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md D-02, D-03, D-12
    - cli/src/agent/messageConverter.ts (current `CodexMessage` type definition + line 29-30 comment)
    - cli/src/cursor/cursorRemoteLauncher.ts (line 126 — `codexMsg` local var)
    - hub/src/sync/todos.ts (current `extractTodosFromCodexMessage` + literal `'codex'` checks)
    - shared/src/modes.ts (confirm `AGENT_MESSAGE_PAYLOAD_TYPE` is exported)
    - shared/src/schemas.ts (codexSessionId field, CodexCollaborationModeSchema)
    - shared/src/socket.ts (line 161 — collaborationMode field)
    - web/src/components/AssistantChat/StatusBar.tsx (Codex-specific bits lines 2,9,126-214,249-254)
  </read_first>
  <action>
    Rewrites in bottom-up dependency order:

    1. **`cli/src/commands/registry.ts`**: remove the `import { codexCommand } from './codex'` line and any `codexCommand` map entry. Leave gemini/opencode entries (die in 01-03 / 01-04).

    2. **`cli/src/commands/resume.ts`**: drop `import { CodexPermissionMode } from '@/codex/...'`, drop `import { ReasoningEffort } from '@/codex/appServerTypes'`, drop `import { assertCodexLocalSupported } from '@/codex/...'`. Strip `flavor === 'codex'` arms.

    3. **`cli/src/commands/resume.test.ts`**: strip codex test cases (D-06).

    4. **`cli/src/runner/run.ts`**: strip Codex spawn-path branches (`case 'codex'` etc.) + `import.*@/codex/` lines.

    5. **`cli/src/runner/buildCliArgs.test.ts`**: strip codex cases (D-06).

    6. **`cli/src/agent/messageConverter.ts`**:
        - Rename exported `type CodexMessage` → `type AgentWireMessage`. Update internal usages.
        - Rewrite the comment at line ~29-30 from "Codex wire format..." to "Agent wire-payload format (the wire `type: 'codex'` tag is a legacy literal preserved via AGENT_MESSAGE_PAYLOAD_TYPE)". No literal-string change here; only the doc comment.

    7. **`cli/src/agent/messageConverter.test.ts`**: rename type references; tests still pass.

    8. **`cli/src/cursor/cursorRemoteLauncher.ts`** line 126: rename local var `codexMsg` → `agentMsg` (the var holds an `AgentWireMessage`).

    9. **`cli/src/api/apiMachine.ts` + `apiMachine.test.ts`**: strip `flavor === 'codex'` branches + `codexModelPreset` payload refs + test fixtures.

    10. **`cli/src/api/types.ts`**: drop `CodexPermissionMode` / `CodexModelPreset` re-exports.

    11. **`hub/src/sync/todos.ts`** (per RESEARCH rewrite map row "hub/src/sync/todos.ts | 6 hits"):
        - Rename exported function `extractTodosFromCodexMessage` → `extractTodosFromAgentMessage`. Update all import callers.
        - Replace bare string literal `'codex'` in any `content.type === 'codex'` / `content.type !== 'codex'` checks with `content.type === AGENT_MESSAGE_PAYLOAD_TYPE`. Add `import { AGENT_MESSAGE_PAYLOAD_TYPE } from '@hapi/protocol/modes'` (or whatever the local path alias is — verify by reading `shared/src/modes.ts` export and existing imports in the file). The constant value stays `'codex'` (per D-02 + Pitfall 2); only the source of the literal changes.

    12. **`hub/src/sync/syncEngine.ts`**:
        - Delete `listCodexModelsForSession`, `listCodexModelsForMachine`, types `RpcCodexModel` / `RpcListCodexModelsResponse`.
        - Delete `flavor === 'codex'` branches.
        - Drop `'codex'` from any agent-union string literal sets that aren't structurally tied to AgentFlavor (e.g. `['cursor','codex','gemini','opencode']` → `['cursor','gemini','opencode']`). Keep gemini/opencode (die later).

    13. **`hub/src/sync/rpcGateway.ts`**: delete `listCodexModelsForSession`/`listCodexModelsForMachine` methods + `RpcCodexModel`/`RpcListCodexModelsResponse` types. Drop `'codex'` from internal agent-union literals.

    14. **`hub/src/sync/rpcGateway.test.ts`**: delete the "codex RPC timeout" test.

    15. **`hub/src/sync/sessionCache.ts`** (lines 173, 382, 702-706):
        - Drop the `import { CodexCollaborationMode } from ...` line.
        - Remove `collaborationMode` field references in cache row shape.
        - Drop `codexSessionId` from the per-flavor session-id picker (line ~702-706 chain).

    16. **`hub/src/sync/sessionModel.test.ts`**: strip Codex fixtures from the matrix (D-06).

    17. **`hub/src/sync/aliveEvents.test.ts`**: rewrite `flavor: 'codex'` fixtures → `flavor: 'cursor'`.

    18. **`hub/src/sync/teams.ts`** line ~32: replace comment "Agent payload format: { type: 'codex' }" with "Agent payload format: { type: AGENT_MESSAGE_PAYLOAD_TYPE }" (avoid the bare string in a comment so ripgrep is happy).

    19. **`hub/src/socket/handlers/cli/sessionHandlers.ts`** + **`index.ts`**: drop the `CodexCollaborationMode` import + the `collaborationMode` field from event types/payload destructuring.

    20. **`hub/src/web/routes/sessions.ts`**:
        - DELETE route `/sessions/:id/codex-models`
        - DELETE route `/sessions/:id/collaboration-mode`
        - DELETE route `/sessions/:id/model-reasoning-effort`
        - DELETE all `flavor === 'codex'` guards (lines ~305-410)

    21. **`hub/src/web/routes/sessions.test.ts`**: delete codex-route test cases.

    22. **`hub/src/web/routes/machines.ts`**:
        - DELETE route `/machines/:id/codex-models`
        - Drop `'codex'` from the `z.enum([...])` literal for flavor (keep `'cursor', 'gemini', 'opencode'`).

    23. **`hub/src/web/routes/machines.test.ts`**: delete codex-route tests.

    24. **`hub/src/web/routes/cli.test.ts`**: strip codex fixtures.

    25. **`hub/src/push/pushNotificationChannel.test.ts`** line ~12: rewrite `flavor: 'codex'` → `flavor: 'cursor'`.

    26. **`shared/src/schemas.ts`**:
        - DELETE `codexSessionId: z.string().optional()` field
        - WHITELIST (do NOT touch) `CodexCollaborationModeSchema` if present — it's structurally tied to `CODEX_COLLABORATION_MODES` in `shared/src/modes.ts` (Phase 5 territory). However it gets removed from consumers (sessionCache, socket.ts) — verify it's only referenced inside `shared/src/{modes,schemas,resume}.ts` after this task; if the only references are within the whitelist, fine.

    27. **`shared/src/sessionSummary.ts`**: drop `codexSessionId` from the `??` chain.

    28. **`shared/src/socket.ts`** line ~161: drop the `collaborationMode?: CodexCollaborationMode` field (the hub wire-side already lost it).

    29. **`shared/src/types.ts`**: drop `CodexPermissionMode` / `CodexModelPreset` re-exports.

    30. **`web/src/lib/message-window-store.ts` + `.test.ts`**: strip codex flavor branches + rewrite `flavor: 'codex'` fixtures to `'cursor'`.

    31. **`web/src/components/AssistantChat/StatusBar.tsx`** (per RESEARCH rewrite map):
        - Drop `getCodexCollaborationModeLabel` import + usage (line 2)
        - Drop `formatCodexReasoningLabel` (line 9)
        - Drop `isCodexFastMode`
        - Delete all `agentFlavor === 'codex'` branches + their JSX blocks (lines ~126-214, 249-254)

    NOTE for `web/src/components/ToolCard/PermissionFooter.tsx`: per RESEARCH §"CUT-02 deletion map" + cross-CUT note, the `isCodexFamilyFlavor` usage may need cross-CUT cleanup; for THIS commit only remove the codex-specific arm — Gemini/OpenCode arms die in 01-03/01-04 + final sweep in 01-05.

    Run `bun typecheck` from each touched workspace after every cluster of edits; then root `bun typecheck && bun run test` once at the end.
  </action>
  <verify>
    <automated>bun typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `! rg 'CodexMessage' cli/src/agent/messageConverter.ts` (renamed to AgentWireMessage)
    - `! rg 'extractTodosFromCodexMessage' hub/src/ cli/src/ web/src/` (renamed)
    - `! rg '\\.codexModelPreset|CodexPermissionMode|CodexModelPreset' cli/src/ hub/src/ shared/src/ web/src/ --glob '!shared/src/{flavors,modes,resume,voice}.ts'`
    - `! rg 'codexSessionId' shared/src/schemas.ts shared/src/sessionSummary.ts hub/src/sync/sessionCache.ts`
    - `! rg 'listCodexModels' hub/src/`
    - `! rg 'codexCommand' cli/src/commands/registry.ts`
    - `! rg "flavor === 'codex'|flavor ?? 'codex'" cli/ hub/ shared/ web/`
    - `! rg "collaborationMode" hub/src/socket/ hub/src/sync/sessionCache.ts shared/src/socket.ts`
    - `! rg "'/codex-models'|'/collaboration-mode'|'/model-reasoning-effort'" hub/src/`
    - In `hub/src/sync/todos.ts`: `rg -q 'AGENT_MESSAGE_PAYLOAD_TYPE' hub/src/sync/todos.ts` (literal replaced by constant)
    - `bun typecheck` exits 0
  </acceptance_criteria>
  <done>All Codex consumer rewrites complete; wire constant preserved via import; `bun typecheck` green.</done>
</task>

<task type="auto">
  <name>Task 3: Test + guard + commit CUT-02</name>
  <files>(none modified; runs scripts + commits)</files>
  <read_first>
    - .planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md D-14 commit #2 + D-15
  </read_first>
  <action>
    1. `bun typecheck` — exit 0 required.
    2. `bun run test` — exit 0 required (includes test:guard).
    3. `bash scripts/check-no-cut-agents.sh` — exit 0 required.
    4. Commit:
       `git add -A && git commit -m 'feat(phase-01): CUT-02 remove Codex agent

       - Delete cli/src/codex/ tree (including happyMcpStdioBridge.ts per SC#4)
       - Delete cli/src/commands/codex.{ts,test.ts}
       - Delete .github/workflows/{codex-pr-review,codex-mention-response}.yml (SC#4)
       - Delete scripts/dev/{self-test-codex-web-env.sh,seed-codex-web-fixture.ts}
       - Rename CodexMessage type → AgentWireMessage; extractTodosFromCodexMessage → extractTodosFromAgentMessage
       - Replace bare \"codex\" literals in business code with AGENT_MESSAGE_PAYLOAD_TYPE constant (wire literal value preserved in shared/src/modes.ts per D-12)
       - Strip Codex branches from hub routes (sessions, machines), syncEngine RPC, sessionCache, socket handlers, web StatusBar
       - Delete codexSessionId schema field; drop CodexCollaborationMode from wire surfaces (collaborationMode field)

       Closes: CUT-02 (Phase 1)
       Refs: D-02, D-03, D-12, D-14 commit #2'`
  </action>
  <verify>
    <automated>bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh && git log -1 --format='%s' | grep -q 'CUT-02'</automated>
  </verify>
  <acceptance_criteria>
    - `bun typecheck` exits 0
    - `bun run test` exits 0
    - `bash scripts/check-no-cut-agents.sh` exits 0
    - `git log -1 --format='%s'` matches `feat(phase-01): CUT-02 remove Codex agent`
    - `git status --porcelain` empty
  </acceptance_criteria>
  <done>CUT-02 atomic commit landed; D-15 gate passed.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-N1 | — | — | n/a | **No new threats — pure deletion.** Phase-12 (VRFY-02/03) is the ASVS verification gate. Net negative attack surface: `cli/src/codex/happyMcpStdioBridge.ts` (a stdio bridge) is removed. |
</threat_model>

<verification>
- `bun typecheck` green
- `bun run test` green (chains cli/hub/web Vitest + test:guard)
- `bash scripts/check-no-cut-agents.sh` exits 0
- File-existence: codex source, commands, workflows, dev scripts all absent
- `AGENT_MESSAGE_PAYLOAD_TYPE` value remains `'codex'` in `shared/src/modes.ts` (whitelist OK)
</verification>

<success_criteria>
- Commit `feat(phase-01): CUT-02 remove Codex agent` on HEAD with `bun typecheck && bun run test && bun run test:guard` green (D-15)
- `cli/src/codex/`, `cli/src/commands/codex.ts`, both `codex-*.yml` workflows, `cli/src/codex/happyMcpStdioBridge.ts` all absent (SC#4)
- No bare `'codex'` string literals in business code; only the constant in `shared/src/modes.ts` carries the wire value
</success_criteria>

<output>
Create `.planning/phases/01-cut-non-cursor-agents/01-02-SUMMARY.md` listing deleted files, renamed symbols, and any deviations.
</output>
