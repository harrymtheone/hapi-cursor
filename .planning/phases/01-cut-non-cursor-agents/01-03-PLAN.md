---
phase: 01-cut-non-cursor-agents
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - cli/src/gemini/
  - cli/src/agent/backends/
  - cli/src/agent/AgentRegistry.ts
  - cli/src/agent/types.ts
  - cli/src/agent/rateLimitParser.ts
  - cli/src/agent/rateLimitParser.test.ts
  - cli/src/agent/internalEventFilter.ts
  - cli/src/agent/internalEventFilter.test.ts
  - cli/src/agent/permissionAdapter.ts
  - cli/src/agent/permissionAdapter.test.ts
  - cli/src/commands/gemini.ts
  - cli/src/commands/registry.ts
  - cli/src/commands/resume.ts
  - cli/src/commands/resume.test.ts
  - cli/src/api/apiMachine.ts
  - cli/src/api/apiMachine.test.ts
  - cli/src/api/types.ts
  - cli/src/runner/run.ts
  - cli/src/runner/buildCliArgs.test.ts
  - hub/src/sync/syncEngine.ts
  - hub/src/sync/rpcGateway.ts
  - hub/src/sync/sessionCache.ts
  - hub/src/sync/sessionModel.test.ts
  - hub/src/web/routes/sessions.ts
  - hub/src/web/routes/sessions.test.ts
  - hub/src/web/routes/machines.ts
  - shared/src/schemas.ts
  - shared/src/sessionSummary.ts
  - shared/src/models.ts
  - shared/src/models.test.ts
  - shared/src/types.ts
  - web/src/components/ToolCard/PermissionFooter.tsx
autonomous: true
requirements: [CUT-03]
user_setup: []

must_haves:
  truths:
    - "Phase 1 SC#1: bun typecheck + bun run test green after CUT-03 commit"
    - "cli/src/agent/backends/ ACP backend tree removed"
    - "Dead shared abstractions (AgentRegistry, types.ts, rateLimitParser, internalEventFilter) removed once their only consumer (the ACP backend) is gone"
  artifacts:
    - path: "cli/src/gemini/"
      provides: "Must NOT exist"
      absent: true
    - path: "cli/src/agent/backends/"
      provides: "Must NOT exist"
      absent: true
    - path: "cli/src/agent/AgentRegistry.ts"
      provides: "Must NOT exist (dead after ACP gone)"
      absent: true
    - path: "cli/src/commands/gemini.ts"
      provides: "Must NOT exist"
      absent: true
  key_links:
    - from: "cli/src/agent/messageConverter.ts"
      to: "cli/src/cursor/cursorRemoteLauncher.ts"
      via: "Cursor still uses messageConverter — must remain functional after Gemini cut"
      pattern: "messageConverter"
---

<objective>
Execute CUT-03: physically remove Gemini agent + ACP backend + the now-dead shared abstractions (`AgentRegistry`, `types.ts`, `rateLimitParser`, `internalEventFilter`). Decision on `permissionAdapter.{ts,test.ts}` deferred to Wave 0 finding in 01-01 task 0 (A2): if Cursor-coupled, keep + rewrite consumer branches; if dead, delete with this CUT.

Purpose: Third atomic commit in D-14 sequence.
Output: `feat(phase-01): CUT-03 remove Gemini agent + ACP backend` commit.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md
@.planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md
@.planning/phases/01-cut-non-cursor-agents/01-VALIDATION.md
@.planning/phases/01-cut-non-cursor-agents/01-01-SUMMARY.md
@.planning/phases/01-cut-non-cursor-agents/01-02-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Gemini + ACP backend trees</name>
  <files>cli/src/gemini/, cli/src/agent/backends/, cli/src/commands/gemini.ts</files>
  <read_first>
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"CUT-03 deletion map" → "Source dirs / files to delete outright"
    - .planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md D-05, D-14 commit #3
    - cli/src/gemini/ (top-level listing)
    - cli/src/agent/backends/ (top-level listing)
  </read_first>
  <action>
    1. `rm -rf cli/src/gemini/` — entire tree.
    2. `rm -rf cli/src/agent/backends/` — entire tree (acp/AcpSdkBackend, AcpMessageHandler, AcpStdioTransport, constants, index, __fixtures__/).
    3. `rm cli/src/commands/gemini.ts`.
  </action>
  <verify>
    <automated>! test -d cli/src/gemini; ! test -d cli/src/agent/backends; ! test -f cli/src/commands/gemini.ts</automated>
  </verify>
  <acceptance_criteria>
    - `! test -d cli/src/gemini`
    - `! test -d cli/src/agent/backends`
    - `! test -f cli/src/commands/gemini.ts`
  </acceptance_criteria>
  <done>Gemini + ACP backend source dirs and command file deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Delete now-dead shared abstractions (AgentRegistry, types, rateLimitParser, internalEventFilter, permissionAdapter if dead)</name>
  <files>cli/src/agent/AgentRegistry.ts, cli/src/agent/types.ts, cli/src/agent/rateLimitParser.ts, cli/src/agent/rateLimitParser.test.ts, cli/src/agent/internalEventFilter.ts, cli/src/agent/internalEventFilter.test.ts, cli/src/agent/permissionAdapter.ts, cli/src/agent/permissionAdapter.test.ts</files>
  <read_first>
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"cli/src/agent/ shared-abstraction map (post-CUT-01..04)"
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"Hidden Landmines" #5 (types.ts may be re-exported)
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"Assumptions Log" A1, A2
    - .planning/phases/01-cut-non-cursor-agents/01-01-SUMMARY.md (A1/A2 findings from Wave 0)
    - cli/src/agent/AgentRegistry.ts
    - cli/src/agent/types.ts
    - cli/src/agent/rateLimitParser.ts
    - cli/src/agent/internalEventFilter.ts
    - cli/src/agent/permissionAdapter.ts
    - cli/src/lib.ts (verify no re-exports of these)
  </read_first>
  <action>
    Run sanity grep BEFORE deleting each file:

    1. `rg -n "from '@/agent/AgentRegistry'|from '\\.\\./AgentRegistry'|from '\\./AgentRegistry'" cli/src/` — should show ZERO hits (only `runAgentSession.ts` consumed it, deleted in 01-01). If hits → stop and ask user.
       Then `rm cli/src/agent/AgentRegistry.ts`.

    2. `rg -n "from '@/agent/types'|from '\\./types'" cli/src/agent/ cli/src/lib.ts cli/src/cursor/` — should show ZERO hits after step 1. If hits → stop. Then `rm cli/src/agent/types.ts`.

    3. `rg -n "rateLimitParser|parseRateLimit" cli/src/ hub/src/ web/src/` — should show ZERO hits (only ACP backend consumed it, just deleted). Then `rm cli/src/agent/rateLimitParser.ts cli/src/agent/rateLimitParser.test.ts`.

    4. `rg -n "internalEventFilter" cli/src/ hub/src/ web/src/` — should show ZERO hits. Then `rm cli/src/agent/internalEventFilter.ts cli/src/agent/internalEventFilter.test.ts`.

    5. **PermissionAdapter conditional delete** (A2):
       - Re-run A2 grep: `rg -n "permissionAdapter|PermissionAdapter" cli/src/cursor/ cli/src/agent/loopBase.ts cli/src/agent/sessionBase.ts`
       - If ZERO hits in `cli/src/cursor/` → safe to delete: `rm cli/src/agent/permissionAdapter.ts cli/src/agent/permissionAdapter.test.ts`.
       - If Cursor hits exist → KEEP both files. Inspect for any flavor-literal arms in `permissionAdapter.ts`; remove Claude/Codex/Gemini/OpenCode branches if present (Cursor-only). Update the test to drop non-Cursor cases. Document the keep-decision in commit body.

    6. After deletes, check `cli/src/lib.ts` for any re-exports of the deleted symbols (`export * from './agent/AgentRegistry'` etc.) and remove the lines.
  </action>
  <verify>
    <automated>! test -f cli/src/agent/AgentRegistry.ts && ! test -f cli/src/agent/types.ts && ! test -f cli/src/agent/rateLimitParser.ts && ! test -f cli/src/agent/internalEventFilter.ts</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f cli/src/agent/AgentRegistry.ts`
    - `! test -f cli/src/agent/types.ts`
    - `! test -f cli/src/agent/rateLimitParser.ts`
    - `! test -f cli/src/agent/rateLimitParser.test.ts`
    - `! test -f cli/src/agent/internalEventFilter.ts`
    - `! test -f cli/src/agent/internalEventFilter.test.ts`
    - `rg -c "AgentRegistry|rateLimitParser|internalEventFilter" cli/src/ hub/src/ web/src/` returns 0
    - PermissionAdapter status: either both files absent OR both present with all non-Cursor branches stripped (decision recorded in commit body)
  </acceptance_criteria>
  <done>Dead shared abstractions deleted; permissionAdapter handled per A2 finding.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite Gemini consumers — strip business branches + drop wire fields</name>
  <files>cli/src/commands/registry.ts, cli/src/commands/resume.ts, cli/src/commands/resume.test.ts, cli/src/api/apiMachine.ts, cli/src/api/apiMachine.test.ts, cli/src/api/types.ts, cli/src/runner/run.ts, cli/src/runner/buildCliArgs.test.ts, hub/src/sync/syncEngine.ts, hub/src/sync/rpcGateway.ts, hub/src/sync/sessionCache.ts, hub/src/sync/sessionModel.test.ts, hub/src/web/routes/sessions.ts, hub/src/web/routes/sessions.test.ts, hub/src/web/routes/machines.ts, shared/src/schemas.ts, shared/src/sessionSummary.ts, shared/src/models.ts, shared/src/models.test.ts, shared/src/types.ts, web/src/components/ToolCard/PermissionFooter.tsx</files>
  <read_first>
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"CUT-03 deletion map" → "Business-consumer Gemini rewrites (D-03)"
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"Business-consumer rewrite map" rows tagged Gemini
    - hub/src/sync/syncEngine.ts (lines 412, 439, 452 — agent union + flavor-id branch)
    - hub/src/sync/rpcGateway.ts (line 153 — agent union)
    - hub/src/web/routes/sessions.test.ts (line 285-303 — Gemini regression test)
  </read_first>
  <action>
    1. **`cli/src/commands/registry.ts`**: remove `geminiCommand` import + entry.

    2. **`cli/src/commands/resume.ts`**: drop `GeminiPermissionMode` import + `flavor === 'gemini'` arm.

    3. **`cli/src/commands/resume.test.ts`**: strip Gemini cases.

    4. **`cli/src/api/apiMachine.ts` + `apiMachine.test.ts`**: strip Gemini branches/fixtures.

    5. **`cli/src/api/types.ts`**: drop `GeminiPermissionMode`/`GeminiModelPreset` re-exports.

    6. **`cli/src/runner/run.ts`**: strip Gemini spawn branches + `@/gemini/` imports.

    7. **`cli/src/runner/buildCliArgs.test.ts`**: strip Gemini cases.

    8. **`hub/src/web/routes/sessions.ts`**: drop `'gemini'` from `z.enum` literals; strip `flavor === 'gemini'` branches if any.

    9. **`hub/src/web/routes/sessions.test.ts`**: delete the "Gemini regression test" case (lines ~285-303).

    10. **`hub/src/web/routes/machines.ts`**: drop `'gemini'` from `z.enum`. (After this CUT only `'cursor', 'opencode'` remain — opencode dies in 01-04.)

    11. **`hub/src/sync/syncEngine.ts`** (lines 412, 439, 452): drop `'gemini'` from agent union literal lists; remove the flavor-id branch for gemini.

    12. **`hub/src/sync/rpcGateway.ts`** (line 153): drop `'gemini'` from agent union literal.

    13. **`hub/src/sync/sessionCache.ts`**: drop `geminiSessionId` from picker chain.

    14. **`hub/src/sync/sessionModel.test.ts`**: strip Gemini fixtures (D-06).

    15. **`shared/src/schemas.ts`**: delete `geminiSessionId` field.

    16. **`shared/src/sessionSummary.ts`**: drop `geminiSessionId` from `??` chain.

    17. **`shared/src/models.ts`**: delete `GEMINI_MODEL_LABELS`, `GEMINI_MODEL_PRESETS`, `GeminiModelPreset`, `DEFAULT_GEMINI_MODEL`.

    18. **`shared/src/models.test.ts`**: drop Gemini cases.

    19. **`shared/src/types.ts`**: drop `GeminiPermissionMode`, `GeminiModelPreset` re-exports.

    20. **`web/src/components/ToolCard/PermissionFooter.tsx`**: drop the `toolName.startsWith('Gemini')` branch.

    Run `bun typecheck` after each cluster; root `bun typecheck && bun run test` at end.

    21. **Shrink guard whitelist — drop CUT-03-owned TEMP entries.** Edit `scripts/check-no-cut-agents.sh`:
        - Remove every `--glob` line tagged `# TEMP-CUT-03:` (runtime-dir blanket + `cli/src/agent/backends/**`).
        - Remove every `--glob` line tagged `# TEMP-WIDE: owner=CUT-03` (if W0.0 created any).
        - For broad `01-05-cleanup` TEMP-WIDE entries: KEEP — they still carry OpenCode (CUT-04) hits.
        - Cross-check: `rg -c '\bgemini\b' <file>` returns 0 for every CUT-03-owned file just edited. Run `bash scripts/check-no-cut-agents.sh` — MUST exit 0.
  </action>
  <checkpoint_strategy>
    Touches ~21 files. Cluster order:

    1. **cli (commands, api, runner)** (steps 1-7) → `cd cli && bun run typecheck`
    2. **shared/src** (steps 15-19) → `cd shared && bun run typecheck && bun run test`
    3. **hub/src** (steps 8-14) → `cd hub && bun run typecheck && bun run test`
    4. **web/src** (step 20) → `cd web && bun run typecheck`
    5. **Whitelist shrink** (step 21) → `bash scripts/check-no-cut-agents.sh`
    6. **Root** → `bun typecheck && bun run test`
  </checkpoint_strategy>
  <verify>
    <automated>bun typecheck && bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - `! rg 'geminiCommand|GeminiPermissionMode|GeminiModelPreset|GEMINI_MODEL_LABELS|GEMINI_MODEL_PRESETS|DEFAULT_GEMINI_MODEL' cli/src/ hub/src/ shared/src/ web/src/ --glob '!shared/src/{flavors,modes,resume,voice}.ts'`
    - `! rg 'geminiSessionId' shared/src/schemas.ts shared/src/sessionSummary.ts hub/src/sync/sessionCache.ts`
    - `! rg "'gemini'" hub/src/sync/syncEngine.ts hub/src/sync/rpcGateway.ts hub/src/web/routes/sessions.ts hub/src/web/routes/machines.ts`
    - `! rg "startsWith\\('Gemini'\\)" web/src/`
    - `bun typecheck` exits 0
  </acceptance_criteria>
  <done>All Gemini consumer rewrites complete; bun typecheck green.</done>
</task>

<task type="auto">
  <name>Task 3.5: Rewrite W0.0-inventory consumers assigned to CUT-03</name>
  <files>(determined by 01-WAVE0-FINDINGS.md §"HEAD inventory" filtered to owner-commit=CUT-03)</files>
  <read_first>
    - .planning/phases/01-cut-non-cursor-agents/01-WAVE0-FINDINGS.md §"HEAD inventory"
    - .planning/phases/01-cut-non-cursor-agents/01-RESEARCH.md §"Business-consumer rewrite map" categories
  </read_first>
  <action>
    Filter `01-WAVE0-FINDINGS.md` §"HEAD inventory" to rows with `proposed owner-commit = CUT-03` not already handled by Task 3 (e.g., `cli/src/ui/ink/GeminiDisplay.tsx` — Gemini-specific UI; any other Gemini-dominant file W0.0 surfaces).

    For each row: apply RESEARCH category (a/b/d). Gemini-only files (`cli/src/ui/ink/GeminiDisplay.tsx`) → `git rm`. Mark row `done-CUT-03` in `01-WAVE0-FINDINGS.md`. Multi-flavor reassignment to `01-05-cleanup` if non-Gemini arms remain.
  </action>
  <verify>
    <automated>bun typecheck && bash scripts/check-no-cut-agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - Every `01-WAVE0-FINDINGS.md` row with `owner-commit = CUT-03` is marked `done-CUT-03` OR reassigned
    - `bun typecheck` and `bash scripts/check-no-cut-agents.sh` both exit 0
  </acceptance_criteria>
  <done>W0.0 CUT-03 rows processed; ledger updated.</done>
</task>

<task type="auto">
  <name>Task 4: Test + guard + commit CUT-03</name>
  <files>(commit only)</files>
  <read_first>
    - .planning/phases/01-cut-non-cursor-agents/01-CONTEXT.md D-14 commit #3 + D-15
  </read_first>
  <action>
    1. `bun typecheck` — exit 0.
    2. `bun run test` — exit 0.
    3. `bash scripts/check-no-cut-agents.sh` — exit 0.
    4. Commit:
       `git add -A && git commit -m 'feat(phase-01): CUT-03 remove Gemini agent + ACP backend

       - Delete cli/src/gemini/ tree
       - Delete cli/src/agent/backends/ tree (ACP backend)
       - Delete cli/src/commands/gemini.ts
       - Delete now-dead shared abstractions: AgentRegistry.ts, types.ts, rateLimitParser.{ts,test.ts}, internalEventFilter.{ts,test.ts}
       - permissionAdapter: <kept|deleted> per Wave 0 A2 finding
       - Strip Gemini branches: hub routes (sessions, machines), syncEngine, rpcGateway, sessionCache picker, schemas.geminiSessionId, shared models, web PermissionFooter

       Closes: CUT-03 (Phase 1)
       Refs: D-01, D-03, D-14 commit #3'`
  </action>
  <verify>
    <automated>bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh && git log -1 --format='%s' | grep -q 'CUT-03'</automated>
  </verify>
  <acceptance_criteria>
    - `bun typecheck` exits 0
    - `bun run test` exits 0
    - `bash scripts/check-no-cut-agents.sh` exits 0
    - `git log -1 --format='%s'` matches `feat(phase-01): CUT-03 remove Gemini agent + ACP backend`
    - `git status --porcelain` empty
  </acceptance_criteria>
  <done>CUT-03 atomic commit landed; D-15 gate passed.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-N1 | — | — | n/a | **No new threats — pure deletion.** ACP protocol attack surface (stdio transport in `cli/src/agent/backends/acp/`) is REMOVED. ASVS verification deferred to Phase 12 (VRFY-02/03). |
</threat_model>

<verification>
- `bun typecheck` green
- `bun run test` green
- `bash scripts/check-no-cut-agents.sh` exits 0
- File-existence: gemini source, ACP backend, AgentRegistry, types.ts, rateLimitParser, internalEventFilter all absent
- permissionAdapter status documented in commit body
</verification>

<success_criteria>
- Commit `feat(phase-01): CUT-03 remove Gemini agent + ACP backend` on HEAD with all three gates green (D-15)
- `cli/src/gemini/`, `cli/src/agent/backends/`, `cli/src/commands/gemini.ts` absent
- Dead shared abstractions cleared
</success_criteria>

<output>
Create `.planning/phases/01-cut-non-cursor-agents/01-03-SUMMARY.md` listing deletions, A2 decision outcome, any deviations.
</output>
