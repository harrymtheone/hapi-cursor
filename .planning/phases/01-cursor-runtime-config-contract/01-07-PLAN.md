---
phase: 01-cursor-runtime-config-contract
plan: 07
type: execute
wave: 6
depends_on:
  - 01-06
files_modified:
  - web/src/components/AssistantChat/StatusBar.tsx
  - web/src/components/AssistantChat/StatusBar.test.tsx
  - web/src/components/AssistantChat/useHappyComposerState.ts
  - web/src/components/AssistantChat/useHappyComposerState.test.ts
  - web/src/components/AssistantChat/HappyComposerOverlays.tsx
  - web/src/components/AssistantChat/useHappyComposerHandlers.ts
  - web/src/components/AssistantChat/HappyComposer.tsx
  - web/src/components/AssistantChat/HappyComposer.test.tsx
autonomous: true
requirements:
  - CURS-03
  - CURS-04
must_haves:
  truths:
    - "D-07: Model/effort is visible near the chat composer, not in the session list."
    - "D-09: Model selector opens only when true runtime switching is available."
    - "D-11: Model selector opens only when the session is idle and unavailable while busy, thinking, or running tools."
    - "D-10: Applying, applied, failed, and applies-next-run feedback appears inside the model information box."
    - "D-12: Model-switch feedback remains inside composer/status controls and is not inserted into the chat timeline."
    - "Existing model keyboard shortcuts are unavailable when runtime switching is unsupported."
  artifacts:
    - path: "web/src/components/AssistantChat/StatusBar.tsx"
      provides: "Composer-adjacent model information box"
    - path: "web/src/components/AssistantChat/StatusBar.test.tsx"
      provides: "Model info box display and status copy coverage"
    - path: "web/src/components/AssistantChat/useHappyComposerState.ts"
      provides: "Runtime capability and busy-state gates"
    - path: "web/src/components/AssistantChat/HappyComposerOverlays.tsx"
      provides: "Floating selector only when switching is allowed"
    - path: "web/src/components/AssistantChat/useHappyComposerState.test.ts"
      provides: "Gate and state coverage"
    - path: "web/src/components/AssistantChat/HappyComposer.test.tsx"
      provides: "Composer model display, effort display, and selector gating coverage"
  key_links:
    - from: "web/src/components/SessionChat.tsx"
      to: "web/src/components/AssistantChat/HappyComposer.tsx"
      via: "modelSwitchState prop"
      pattern: "modelSwitch"
    - from: "web/src/components/AssistantChat/useHappyComposerState.ts"
      to: "web/src/components/AssistantChat/HappyComposerOverlays.tsx"
      via: "showModelSettings"
      pattern: "showModelSettings"
---

<objective>
Render truthful model state and switch controls in the composer area.

Purpose: Runtime config visibility belongs near the chat input/model control, with switching affordances gated by actual runtime support and session idle state.
Output: Composer model info box, gated selector behavior, and focused tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
@.planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
@.planning/phases/01-cursor-runtime-config-contract/01-VALIDATION.md
@.planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
@.planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
@web/src/components/AssistantChat/StatusBar.tsx
@web/src/components/AssistantChat/StatusBar.test.tsx
@web/src/components/AssistantChat/useHappyComposerState.ts
@web/src/components/AssistantChat/useHappyComposerState.test.ts
@web/src/components/AssistantChat/HappyComposerOverlays.tsx
@web/src/components/AssistantChat/useHappyComposerHandlers.ts
@web/src/components/AssistantChat/HappyComposer.tsx
@web/src/components/AssistantChat/HappyComposer.test.tsx
@web/src/components/SessionChat.tsx

<interfaces>
From Plan 06:
- `SessionChat` passes model switch state to composer/status props.
- Locale keys exist for applying, applied, failed, applies-next-run, read-only, unavailable, and idle messages.

UI contract:
- Model/effort visibility is composer-adjacent.
- If hot switch is unsupported or unproven, the model box is read-only and cannot open a selector.
- Disable switching while `thinking`, active tool/background work, pending approval, or controls disabled.
</interfaces>
</context>

<execution_preconditions>
- Before editing any existing function, class, or method named in this plan, run GitNexus `impact` with `repo: "hapi-cursor"`, `direction: "upstream"`, and the target symbol name. Record direct callers, affected processes, and risk level in the plan summary. If impact is HIGH or CRITICAL, stop and warn before editing.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate model selector by runtime support and idle state</name>
  <files>web/src/components/AssistantChat/useHappyComposerState.ts, web/src/components/AssistantChat/useHappyComposerState.test.ts, web/src/components/AssistantChat/useHappyComposerHandlers.ts</files>
  <read_first>
    - web/src/components/AssistantChat/useHappyComposerState.ts
    - web/src/components/AssistantChat/useHappyComposerState.test.ts
    - web/src/components/AssistantChat/useHappyComposerHandlers.ts
    - web/src/components/AssistantChat/modelOptions.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
    - .planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
    - .planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
    - .planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
  </read_first>
  <behavior>
    - `showModelSettings` is false when runtime hot switch support is false or unproven.
    - `showModelSettings` is false while the session is thinking, controls are disabled, background tools are running, or pending requests exist.
    - Existing model keyboard cycling is disabled unless `showModelSettings` would be true.
    - Available model options come from discovered/runtime-approved options only.
  </behavior>
  <action>Extend `UseHappyComposerStateProps` with runtime switch support state from SessionChat, pending request count if needed, and model switch state. Compute a boolean such as `canOpenModelSelector` from: `onModelChange` exists, Cursor runtime reports switch support, available model options length is greater than zero, `thinking` is false, `backgroundTaskCount` is zero, and controls are not disabled. Use this boolean for `showModelSettings` and for the keyboard shortcut in `useHappyComposerHandlers.ts`. Add tests in `useHappyComposerState.test.ts` for supported idle, unsupported read-only, thinking disabled, background work disabled, and no options disabled. This implements D-09 and D-11.</action>
  <verify>
    <automated>cd web && bun run test -- useHappyComposerState</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: selector visibility is controlled by a runtime-support boolean plus idle-state checks.
    - Source assertion: keyboard model cycling checks the same runtime support and idle gate.
    - Test assertion: `cd web && bun run test -- useHappyComposerState` covers unsupported, thinking, background work, no options, and supported-idle cases.
    - Source assertion: no hardcoded model option list is added to composer code.
  </acceptance_criteria>
  <done>Composer model selector cannot open unless real runtime switching is available and safe to attempt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render model information box and switch feedback</name>
  <files>web/src/components/AssistantChat/StatusBar.tsx, web/src/components/AssistantChat/StatusBar.test.tsx, web/src/components/AssistantChat/HappyComposerOverlays.tsx, web/src/components/AssistantChat/HappyComposer.tsx, web/src/components/AssistantChat/HappyComposer.test.tsx, web/src/components/AssistantChat/useHappyComposerState.test.ts</files>
  <read_first>
    - web/src/components/AssistantChat/StatusBar.tsx
    - web/src/components/AssistantChat/StatusBar.test.tsx
    - web/src/components/AssistantChat/HappyComposerOverlays.tsx
    - web/src/components/AssistantChat/HappyComposer.tsx
    - web/src/components/AssistantChat/HappyComposer.test.tsx
    - web/src/components/AssistantChat/useHappyComposerState.ts
    - web/src/components/SessionChat.tsx
    - web/src/lib/locales/en.ts
    - web/src/lib/locales/zh-CN.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
    - .planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
  </read_first>
  <behavior>
    - Composer status area shows raw current model id or localized `Auto (unspecified)`.
    - Separate effort text appears only when a non-null verified effort/modelReasoningEffort exists.
    - Applying/applied/failed/applies-next-run state appears inside the model information box.
    - Failed state exposes retry affordance by reusing the same model change action with the current target model.
    - No model/effort text is added to session list rows.
  </behavior>
  <action>After GitNexus impact prechecks for `StatusBar`, `HappyComposer`, and overlay/handler symbols, render a compact model information box in `StatusBar.tsx` or the existing composer status area using UI-SPEC spacing: `gap-2`, text size `text-xs` or `text-sm`, and existing CSS variables. Display the raw `model` value as the primary value; display `Auto (unspecified)` when null. Display `effort` or `modelReasoningEffort` only when present from session fields. Show switch state copy from locale keys for applying, applied, failed, and applies-next-run. Keep the floating selector in `HappyComposerOverlays.tsx` behind the gate from Task 1 and use the existing row pattern with raw model ids. Wire `HappyComposer.tsx` props through without creating timeline events. Add `StatusBar.test.tsx` and `HappyComposer.test.tsx` coverage for raw model display, auto display, effort/modelReasoningEffort display, applying/applied/failed/applies-next-run copy, read-only unsupported state, and selector gating. This implements D-07, D-09, D-10, D-11, D-12, and UI-SPEC composer contracts.</action>
  <verify>
    <automated>cd web && bun run test -- useHappyComposerState HappyComposer StatusBar</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: composer/status code renders the raw `model` string and does not call a relabeling helper that hides the id.
    - Source assertion: separate effort UI is conditional on non-null session effort/modelReasoningEffort fields.
    - Source assertion: failed and applies-next-run states render inside composer/status components, not chat message components.
    - Test assertion: targeted tests prove read-only unsupported state cannot open selector and supported idle state can.
    - Test assertion: `StatusBar.test.tsx` and `HappyComposer.test.tsx` cover model display, effort display, status copy, and gating.
    - Typecheck assertion: `bun run typecheck` passes after prop threading.
  </acceptance_criteria>
  <done>Users can see current model/effort and truthful switch feedback next to the chat input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session metadata to composer UI | Persisted model/effort values become visible mobile state. |
| User composer action to mutation callback | Selector clicks can trigger runtime config changes. |
| Runtime status to visible feedback | Applying/failed/applies-next-run states guide user decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20 | Integrity | composer selector gate | mitigate | Hide selector unless runtime support and idle state are true; disable keyboard shortcut under the same condition. |
| T-01-21 | Spoofing | model information box | mitigate | Show raw Cursor model id as primary text and avoid HAPI-renamed labels. |
| T-01-22 | Repudiation | switch status copy | mitigate | Display explicit applied/failed/applies-next-run state from Hub result inside the model box. |
| T-01-SC | Tampering | package installs | accept | No package-manager installs in this plan; package legitimacy audit says no install. |
</threat_model>

<verification>
Run `cd web && bun run test -- useHappyComposerState HappyComposer StatusBar`, then `bun run typecheck`.
</verification>

<success_criteria>
The composer becomes the canonical mobile runtime config surface, and it never implies hot switching unless the runtime path is actually available.
</success_criteria>

<output>
Create `.planning/phases/01-cursor-runtime-config-contract/01-07-SUMMARY.md` when done.
</output>
