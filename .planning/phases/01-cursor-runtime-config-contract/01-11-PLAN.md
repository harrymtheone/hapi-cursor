---
phase: 01-cursor-runtime-config-contract
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - hub/src/web/routes/machines.ts
  - hub/src/web/routes/machines.test.ts
  - hub/src/sync/rpcGateway.ts
  - hub/src/sync/syncEngineRpc.ts
  - hub/src/sync/syncEngine.ts
  - hub/src/sync/sessionModel.test.ts
autonomous: true
gap_closure: true
requirements: [CURS-02]
user_setup: []
must_haves:
  truths:
    - "D-06: Hub spawn rejects unsupported effort/modelReasoningEffort before session creation can persist divergent runtime config."
    - "D-05: Auto/unspecified launches do not send or persist effort fields."
    - "D-08: Selected model spawn behavior remains unchanged while unsupported effort input fails at the Hub boundary."
  artifacts:
    - path: "hub/src/web/routes/machines.ts"
      provides: "Spawn request boundary that rejects unsupported effort/modelReasoningEffort fields"
      contains: "spawnBodySchema"
    - path: "hub/src/sync/rpcGateway.ts"
      provides: "Machine spawn RPC payload without unsupported effort fields"
      contains: "spawnSession"
  key_links:
    - from: "hub/src/web/routes/machines.ts"
      to: "hub/src/sync/rpcGateway.ts"
      via: "spawnBodySchema -> SyncEngine.spawnSession -> RpcGateway.spawnSession"
      pattern: "spawnSession"
---

<objective>
Close the Hub spawn-boundary slice of the CURS-02 effort gap by rejecting unsupported effort config before session creation can persist or forward it.

Purpose: Verification found that HAPI accepts effort/modelReasoningEffort but Cursor launch only receives `--model`. Research and D-06 say separate effort is unsupported unless runtime behavior proves it, so the truthful closure is to reject/remove unsupported effort inputs instead of inventing flags.
Output: Hub spawn route rejects effort fields, Hub spawn facades drop unsupported effort parameters, and tests prove model-only spawn remains intact.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
@.planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
@.planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
@.planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
@.planning/phases/01-cursor-runtime-config-contract/01-02-SUMMARY.md
@.planning/phases/01-cursor-runtime-config-contract/01-03-SUMMARY.md
@.planning/phases/01-cursor-runtime-config-contract/01-04-SUMMARY.md
@.planning/phases/01-cursor-runtime-config-contract/01-05-SUMMARY.md
@hub/src/web/routes/machines.ts
@hub/src/sync/rpcGateway.ts
@hub/src/sync/syncEngineRpc.ts
@hub/src/sync/syncEngine.ts

<interfaces>
Existing runtime facts:
- `spawnBodySchema` currently accepts `effort` and `modelReasoningEffort`; this is the root of the backend divergence.
- `SyncEngine.spawnSession`, `SyncEngineRpc.spawnSession`, and `RpcGateway.spawnSession` currently mirror the route payload into the machine spawn RPC.
- Existing selected model launch failures are labeled with `code: 'selected-runtime-config-rejected'`.
</interfaces>

<preconditions>
Completed plans 01-02, 01-04, and 01-05 are historical prerequisites for the existing spawn/model plumbing and rejection vocabulary. They are read-first context, not executable dependencies for this gap-closure wave.
</preconditions>

<gap_source>
01-VERIFICATION gap #1 plus checker blocker: Effort/modelReasoningEffort are accepted through API/session metadata paths but are not passed to the spawned Cursor process. This split segment covers the Hub spawn boundary only; 01-14 covers session-config persistence/RPC and 01-15 covers the CLI runner spawn contract.
</gap_source>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject unsupported effort at Hub spawn boundary</name>
  <files>hub/src/web/routes/machines.ts, hub/src/web/routes/machines.test.ts, hub/src/sync/rpcGateway.ts, hub/src/sync/syncEngineRpc.ts, hub/src/sync/syncEngine.ts, hub/src/sync/sessionModel.test.ts</files>
  <read_first>
    - hub/src/web/routes/machines.ts
    - hub/src/web/routes/machines.test.ts
    - hub/src/sync/rpcGateway.ts
    - hub/src/sync/syncEngineRpc.ts
    - hub/src/sync/syncEngine.ts
    - hub/src/sync/sessionModel.test.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
    - .planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
  </read_first>
  <behavior>
    - `POST /api/machines/:id/spawn` accepts `model` but rejects request bodies containing `effort` or `modelReasoningEffort`.
    - Hub `spawnSession` facade/RPC signatures no longer accept or forward `effort` or `modelReasoningEffort`.
    - Selected model launch still flows through `model` unchanged.
    - Rejected unsupported effort input returns a safe 400 boundary error and does not call `engine.spawnSession`.
  </behavior>
  <action>After GitNexus impact prechecks for `createMachinesRoutes`, `RpcGateway.spawnSession`, `SyncEngineRpc.spawnSession`, and `SyncEngine.spawnSession`, remove `effort` and `modelReasoningEffort` from the Hub spawn request schema and spawn method signatures. Use a strict Zod object or explicit unknown-key handling so request bodies containing `effort` or `modelReasoningEffort` fail at the route boundary with HTTP 400 and a safe error message/code such as `unsupported-runtime-effort`. Do not add a Cursor effort flag. Preserve `model` and `permissionMode` launch behavior. Update Hub tests to prove effort-only and modelReasoningEffort-only spawn requests are rejected before engine/RPC invocation, model-only requests still call spawn with the raw model id, and selected-runtime-config-rejected model failures still pass through as safe spawn errors. This implements D-06 and closes the first missing item from 01-VERIFICATION conservatively.</action>
  <verify>
    <automated>cd hub && bun test src/web/routes/machines.test.ts src/sync/sessionModel.test.ts</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `hub/src/web/routes/machines.ts` no longer lists `effort` or `modelReasoningEffort` in `spawnBodySchema`.
    - Source assertion: `hub/src/sync/rpcGateway.ts`, `hub/src/sync/syncEngineRpc.ts`, and `hub/src/sync/syncEngine.ts` no longer include spawn parameters named `effort` or `modelReasoningEffort`.
    - Test assertion: Hub route tests prove effort/modelReasoningEffort spawn bodies return 400 and do not call the engine.
    - Test assertion: Hub route tests prove model-only spawn still passes the selected model.
    - Typecheck assertion: `bun run typecheck` catches no stale spawn effort signature references.
  </acceptance_criteria>
  <done>Unsupported separate effort launch config is rejected at the authenticated Hub boundary instead of reaching persistence or runner launch code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web to Hub spawn route | User-provided runtime launch fields enter the authenticated server boundary. |
| Hub to CLI machine RPC | Validated spawn options cross Socket.IO to the runner process. |
| CLI runner to Cursor process | Selected model becomes a process argument array. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11-01 | Tampering | `spawnBodySchema` | mitigate | Reject unsupported effort/modelReasoningEffort fields at the Hub route boundary with Zod/strict validation before RPC or persistence. |
| T-01-11-02 | Tampering | Hub spawn RPC facade | mitigate | Remove unsupported effort fields from Hub spawn method signatures so validated route payloads cannot be reintroduced downstream. |
| T-01-11-03 | Information Disclosure | spawn failure response | mitigate | Continue returning safe spawn error codes/messages; do not expose raw runner stderr in route or Web contracts. |
| T-01-11-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. If an executor adds one, stop for slopcheck and a blocking human checkpoint. |
</threat_model>

<verification>
Run targeted Hub spawn tests and `bun run typecheck`. If targeted checks pass, run `bash scripts/check-no-cut-agents.sh`; broader runner/CLI validation belongs to 01-15.
</verification>

<success_criteria>
- Hub spawn rejects separate effort/modelReasoningEffort input before it can be persisted or sent to the runner.
- Hub spawn facades no longer accept unsupported effort fields.
- Selected model spawn remains valid and safe model rejection behavior is preserved.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Start sessions with selected model/effort truthfully | 01-11 | COVERED | Chooses conservative D-06 closure: reject unsupported effort until Cursor support is verified.
REQ | CURS-02 | Selected model/effort launch and session metadata does not diverge from runtime | 01-11, 01-14, 01-15 | COVERED | This plan covers Hub spawn boundary; 01-14 covers session-config persistence/RPC; 01-15 covers CLI runner launch.
RESEARCH | Pitfall 2 | Do not enable effort controls without runtime proof | 01-11 | COVERED | No effort flags or spawn fields are invented.
CONTEXT | D-05 | Default runtime config is auto/unspecified | 01-11 | COVERED | Auto launches still omit runtime fields except explicit model.
CONTEXT | D-06 | Effort is runtime capability, not HAPI promise | 01-11, 01-14, 01-15 | COVERED | Split across boundary, persistence/RPC, and runner contract.
CONTEXT | D-08 | Selected runtime rejection is clear, no fallback | 01-11, 01-15 | COVERED | Hub model failure behavior is preserved here; runner classifier is finalized in 01-15.
</source_audit>

<output>
Create `.planning/phases/01-cursor-runtime-config-contract/01-11-SUMMARY.md` when done.
</output>
