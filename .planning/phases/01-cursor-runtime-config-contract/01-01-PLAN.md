---
phase: 01-cursor-runtime-config-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/schemas.ts
  - shared/src/types.ts
  - shared/src/schemas.test.ts
autonomous: true
requirements:
  - CURS-01
  - CURS-03
  - CURS-04
must_haves:
  truths:
    - "D-01: Shared protocol exposes Cursor model discovery results with no static model catalog."
    - "D-03: Shared protocol exposes safe discovery failure reasons without raw stderr in normal UI contracts."
    - "D-04: Shared protocol preserves raw Cursor model ids as primary values."
    - "D-10: Shared protocol exposes status-bearing runtime config apply results for applied, pending, failed, and applies-next-run states."
    - "D-12: Strict session patches continue accepting only declared live session fields and keep model-switch results out of chat timeline payloads."
  artifacts:
    - path: "shared/src/schemas.ts"
      provides: "Cursor runtime discovery and apply-result Zod schemas"
      exports:
        - "CursorModelDiscoveryResultSchema"
        - "CursorRuntimeConfigApplyResultSchema"
    - path: "shared/src/types.ts"
      provides: "Runtime config contract type exports"
      exports:
        - "CursorModelDiscoveryResult"
        - "CursorRuntimeConfigApplyResult"
    - path: "shared/src/schemas.test.ts"
      provides: "Protocol contract regression coverage"
  key_links:
    - from: "shared/src/schemas.ts"
      to: "cli/hub/web runtime config code"
      via: "schemas imported from @hapi/protocol/schemas"
      pattern: "Cursor(ModelDiscovery|RuntimeConfigApply)ResultSchema"
---

<objective>
Define the shared Cursor runtime config contract before CLI, Hub, and Web consume it.

Purpose: Downstream plans need one typed source of truth for model discovery, safe discovery failures, and truthful model-switch outcomes.
Output: Shared Zod schemas, exported inferred types, and contract tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
@.planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
@.planning/phases/01-cursor-runtime-config-contract/01-VALIDATION.md
@.planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
@shared/src/schemas.ts
@shared/src/types.ts
@shared/src/schemas.test.ts

<interfaces>
Existing contract patterns:
- `SessionSchema` already has `model`, `modelReasoningEffort`, and `effort`.
- `SessionPatchSchema` is strict and already includes `model`, `modelReasoningEffort`, `effort`, `active`, `thinking`, and `backgroundTaskCount`.
- `SyncEventSchema` uses `SessionPatchSchema` for `session-updated` patch payloads.
- `shared/src/types.ts` re-exports inferred types from `./schemas`.
</interfaces>
</context>

<execution_preconditions>
- Before editing any existing function, class, or method named in this plan, run GitNexus `impact` with `repo: "hapi-cursor"`, `direction: "upstream"`, and the target symbol name. Record direct callers, affected processes, and risk level in the plan summary. If impact is HIGH or CRITICAL, stop and warn before editing.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Cursor runtime contract schemas</name>
  <files>shared/src/schemas.ts, shared/src/types.ts, shared/src/schemas.test.ts</files>
  <read_first>
    - shared/src/schemas.ts
    - shared/src/types.ts
    - shared/src/schemas.test.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
    - .planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
    - .planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
  </read_first>
  <behavior>
    - `CursorModelDiscoveryResultSchema` accepts `{ status: 'ok', models, discoveredAt }` where each model has raw `id`, optional `label`, optional `isDefault`, and optional `isCurrent`.
    - `CursorModelDiscoveryResultSchema` accepts `{ status: 'error', reason, discoveredAt }` with safe reason values only: `cursor-cli-unavailable`, `not-authenticated`, `timed-out`, `empty-model-list`, `unknown`.
    - `CursorRuntimeConfigApplyResultSchema` accepts only `applied`, `pending`, `failed`, and `applies-next-run` statuses with nullable `model`, nullable `modelReasoningEffort`, nullable `effort`, and optional safe `reason` where applicable.
    - `SessionPatchSchema` remains strict and rejects unknown fields.
  </behavior>
  <action>After GitNexus impact prechecks for existing shared schema/type exports, add named Zod schemas in `shared/src/schemas.ts` for `CursorModelSummarySchema`, `CursorModelDiscoveryResultSchema`, and `CursorRuntimeConfigApplyResultSchema`. Use discriminated unions on `status`. Do not add any hardcoded model ids. Export inferred `CursorModelSummary`, `CursorModelDiscoveryResult`, and `CursorRuntimeConfigApplyResult` through `shared/src/types.ts`. Add `shared/src/schemas.test.ts` cases that parse success/error discovery results, parse all four apply statuses, reject raw stderr-like arbitrary reason values, and confirm malformed `session-updated` patch fields still fail because `SessionPatchSchema` is strict. This implements D-01, D-03, D-04, D-10, and D-12 at the protocol layer.</action>
  <verify>
    <automated>cd shared && bun test schemas.test.ts</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `shared/src/schemas.ts` exports `CursorModelDiscoveryResultSchema` and `CursorRuntimeConfigApplyResultSchema`.
    - Source assertion: `shared/src/types.ts` exports `CursorModelDiscoveryResult` and `CursorRuntimeConfigApplyResult`.
    - Test assertion: `cd shared && bun test schemas.test.ts` includes passing cases for discovery `ok`, discovery `error`, `applied`, `pending`, `failed`, and `applies-next-run`.
    - Test assertion: an unknown `SessionPatchSchema` field still fails validation.
    - Source assertion: no model id literals such as `sonnet`, `opus`, `gpt`, or `composer` are introduced in `shared/src/schemas.ts` outside tests that explicitly assert raw ids pass through unchanged.
  </acceptance_criteria>
  <done>Shared runtime config contract exists, exports through `@hapi/protocol`, and is covered by Bun tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI runtime to shared protocol | Untrusted local CLI output is normalized before entering typed app contracts. |
| Hub/Web to shared protocol | Route and UI payloads must use strict schemas instead of ad hoc shape checks. |
| SSE patch producer to Web cache | Strict patch schema prevents undeclared live fields from mutating client state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `CursorModelDiscoveryResultSchema` | mitigate | Discovery results require Zod validation and preserve raw ids without trusting extra fields. |
| T-01-02 | Information Disclosure | discovery error contract | mitigate | Normal UI reason values are enumerated safe categories; raw stderr is not representable in the normal result reason field. |
| T-01-03 | Integrity | `SessionPatchSchema` | mitigate | Keep strict patch parsing and add regression tests for unknown field rejection. |
| T-01-SC | Tampering | package installs | accept | No package-manager installs in this plan; package legitimacy audit says no install. |
</threat_model>

<verification>
Run `cd shared && bun test schemas.test.ts`, then `bun run typecheck`.
</verification>

<success_criteria>
The protocol contract can be imported by CLI, Hub, and Web without each tier inventing its own model discovery or apply-result shape.
</success_criteria>

<output>
Create `.planning/phases/01-cursor-runtime-config-contract/01-01-SUMMARY.md` when done.
</output>
