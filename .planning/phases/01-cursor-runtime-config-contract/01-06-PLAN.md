---
phase: 01-cursor-runtime-config-contract
plan: 06
type: execute
wave: 5
depends_on:
  - 01-04
  - 01-05
files_modified:
  - web/src/api/client.ts
  - web/src/hooks/mutations/useSessionActions.ts
  - web/src/hooks/mutations/useSessionActions.test.ts
  - web/src/components/SessionChat.tsx
  - web/src/components/SessionChat.test.tsx
  - web/src/lib/locales/en.ts
  - web/src/lib/locales/zh-CN.ts
autonomous: true
requirements:
  - CURS-03
must_haves:
  truths:
    - "D-10: Web model mutation receives applied, pending, failed, or applies-next-run results from Hub."
    - "D-12: Switch feedback is local to session controls and is not inserted into the chat timeline."
    - "D-10: Localized copy exists for applying, applied, failed, retry, read-only, busy, and applies-next-run states."
  artifacts:
    - path: "web/src/api/client.ts"
      provides: "typed setModel response"
    - path: "web/src/hooks/mutations/useSessionActions.ts"
      provides: "model mutation status result"
    - path: "web/src/hooks/mutations/useSessionActions.test.ts"
      provides: "Mutation return and error coverage"
    - path: "web/src/components/SessionChat.tsx"
      provides: "session-level model switch status state"
    - path: "web/src/components/SessionChat.test.tsx"
      provides: "Switch status propagation and no timeline pollution coverage"
    - path: "web/src/lib/locales/en.ts"
      provides: "English runtime state copy"
    - path: "web/src/lib/locales/zh-CN.ts"
      provides: "Simplified Chinese runtime state copy"
  key_links:
    - from: "web/src/components/SessionChat.tsx"
      to: "web/src/hooks/mutations/useSessionActions.ts"
      via: "handleModelChange awaits status-bearing setModel"
      pattern: "setModel"
---

<objective>
Plumb status-bearing model switch results into Web session state.

Purpose: Composer UI plans need a concrete mutation result and localized state vocabulary before rendering switch feedback.
Output: Typed Web API response, mutation return type, SessionChat switch-status state, and locale keys.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
@.planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
@.planning/phases/01-cursor-runtime-config-contract/01-VALIDATION.md
@.planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
@.planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
@web/src/api/client.ts
@web/src/hooks/mutations/useSessionActions.ts
@web/src/hooks/mutations/useSessionActions.test.ts
@web/src/components/SessionChat.tsx
@web/src/components/SessionChat.test.tsx
@web/src/lib/locales/en.ts
@web/src/lib/locales/zh-CN.ts
@shared/src/types.ts

<interfaces>
From Plan 05:
- `POST /api/sessions/:id/model` returns `CursorRuntimeConfigApplyResult`.

Required copy keys:
- `composer.model.applying`: `Applying...`
- `composer.model.applied`: `Applied`
- `composer.model.appliesNextRun`: `Applies next run`
- `composer.model.switchFailed`: `Switch failed. Retry`
- `composer.model.switchingUnavailable`: `Switching unavailable for this runtime`
- `composer.model.changesApplyBeforeLaunch`: `Model changes apply before launch only`
- `composer.model.availableWhenIdle`: `Switching available when the session is idle`
- `session.status.running`, `session.status.waiting`, `session.status.error`, `session.status.unreadComplete`, `session.status.viewed`
</interfaces>
</context>

<execution_preconditions>
- Before editing any existing function, class, or method named in this plan, run GitNexus `impact` with `repo: "hapi-cursor"`, `direction: "upstream"`, and the target symbol name. Record direct callers, affected processes, and risk level in the plan summary. If impact is HIGH or CRITICAL, stop and warn before editing.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type Web model mutation result</name>
  <files>web/src/api/client.ts, web/src/hooks/mutations/useSessionActions.ts, web/src/hooks/mutations/useSessionActions.test.ts</files>
  <read_first>
    - web/src/api/client.ts
    - web/src/hooks/mutations/useSessionActions.ts
    - web/src/hooks/mutations/useSessionActions.test.ts
    - shared/src/types.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
    - .planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
    - .planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
  </read_first>
  <behavior>
    - `ApiClient.setModel` returns `Promise<CursorRuntimeConfigApplyResult>`.
    - `useSessionActions().setModel` resolves to the same apply result.
    - Mutation errors still reject for HTTP errors.
  </behavior>
  <action>After GitNexus impact prechecks for `ApiClient.setModel` and `useSessionActions`, change `ApiClient.setModel(sessionId, model)` from `Promise<void>` to `Promise<CursorRuntimeConfigApplyResult>` and return the parsed JSON response from `/api/sessions/:id/model`. Update `useSessionActions.ts` so its `setModel` action resolves to `CursorRuntimeConfigApplyResult`. Keep existing mutation invalidation/refresh behavior intact. Do not add optimistic `applied` status in Web; rely on the Hub response. Add `web/src/hooks/mutations/useSessionActions.test.ts` or extend it if it exists to cover applied, applies-next-run, failed result propagation, and HTTP error rejection. This implements D-10 and D-12 at the Web API boundary.</action>
  <verify>
    <automated>cd web && bun run test -- useSessionActions</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `web/src/api/client.ts` imports `CursorRuntimeConfigApplyResult` from `@hapi/protocol` or `@hapi/protocol/types`.
    - Source assertion: `ApiClient.setModel` returns the JSON body instead of discarding it.
    - Source assertion: `useSessionActions` exposes `setModel: (model: string | null) => Promise<CursorRuntimeConfigApplyResult>`.
    - Test assertion: `useSessionActions.test.ts` proves returned statuses propagate unchanged and HTTP failures still reject.
    - Typecheck assertion: `bun run typecheck` passes with callers updated.
  </acceptance_criteria>
  <done>Web model mutation callers can distinguish applied, failed, pending, and applies-next-run results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store session-local switch feedback and locale keys</name>
  <files>web/src/components/SessionChat.tsx, web/src/components/SessionChat.test.tsx, web/src/lib/locales/en.ts, web/src/lib/locales/zh-CN.ts</files>
  <read_first>
    - web/src/components/SessionChat.tsx
    - web/src/components/SessionChat.test.tsx
    - web/src/hooks/mutations/useSessionActions.ts
    - web/src/lib/locales/en.ts
    - web/src/lib/locales/zh-CN.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
    - .planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
    - .planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
  </read_first>
  <behavior>
    - `SessionChat` sets local switch state to `applying` before awaiting `setModel`.
    - `SessionChat` stores the returned status and safe reason for composer/status display.
    - Failure haptic remains error; applied/applies-next-run haptic remains non-error.
    - No message is appended to chat timeline for any switch status.
  </behavior>
  <action>After the GitNexus impact precheck for `SessionChat`, add a small local state in `SessionChat.tsx` named `modelSwitchState` or equivalent with status values `idle`, `applying`, `applied`, `failed`, and `applies-next-run`, plus optional safe `reason`. In `handleModelChange`, set applying before calling `setModel`, then store the returned status. Keep `props.onRefresh()` after the mutation so strict SSE/session detail can refresh. Pass the switch state down to `HappyComposer` or `StatusBar` props for Plan 07 to render. Add or update `web/src/components/SessionChat.test.tsx` to cover applying state propagation, applied/applies-next-run/failed status propagation to composer props, error haptic on failed/rejected mutation, and no chat timeline/message append when switching. Add the locale keys listed in this plan's interfaces to both English and Simplified Chinese locale files. Do not create chat messages or timeline events for switch status. This implements D-10 and D-12.</action>
  <verify>
    <automated>cd web && bun run test -- SessionChat useSessionActions</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `SessionChat.tsx` has a session-local switch status state and passes it to composer/status props.
    - Source assertion: `SessionChat.tsx` does not call message-send, timeline, or chat reducer APIs from `handleModelChange`.
    - Test assertion: `SessionChat.test.tsx` proves switch status reaches composer/status props and no timeline/message append occurs.
    - Source assertion: `web/src/lib/locales/en.ts` contains exact English strings for `Applying...`, `Applied`, `Applies next run`, and `Switch failed. Retry`.
    - Source assertion: `web/src/lib/locales/zh-CN.ts` contains matching keys for every English key added.
    - Typecheck assertion: `bun run typecheck` passes.
  </acceptance_criteria>
  <done>Web has a truthful, localized model switch state ready for composer rendering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub config response to Web mutation | Server truth enters client state. |
| SessionChat state to composer props | Local state controls visible switch feedback. |
| Runtime failure reason to mobile UI | Safe reason may be displayed near composer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Integrity | `ApiClient.setModel` | mitigate | Return Hub's status-bearing response; do not synthesize optimistic success. |
| T-01-18 | Repudiation | `SessionChat` switch feedback | mitigate | Store explicit result state so user can see whether the switch applied or applies next run. |
| T-01-19 | Information Disclosure | switch failure copy | mitigate | Display only localized safe reason values, not raw runtime output. |
| T-01-SC | Tampering | package installs | accept | No package-manager installs in this plan; package legitimacy audit says no install. |
</threat_model>

<verification>
Run `cd web && bun run test -- useSessionActions SessionChat`, then `bun run typecheck`.
</verification>

<success_criteria>
Web receives and preserves truthful model-switch results without polluting the chat timeline.
</success_criteria>

<output>
Create `.planning/phases/01-cursor-runtime-config-contract/01-06-SUMMARY.md` when done.
</output>
