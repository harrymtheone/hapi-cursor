---
phase: 01-cursor-runtime-config-contract
plan: 15
type: execute
wave: 3
depends_on: ["01-14"]
files_modified:
  - cli/src/modules/common/rpcTypes.ts
  - cli/src/api/apiMachine.ts
  - cli/src/api/apiMachine.test.ts
  - cli/src/runner/run.ts
  - cli/src/runner/run.test.ts
autonomous: true
gap_closure: true
requirements: [CURS-02]
user_setup: []
must_haves:
  truths:
    - "D-06: CLI runner spawn contract exposes only verified Cursor launch fields."
    - "D-05: Auto/unspecified launches and resumes do not send unsupported effort flags."
    - "D-08: Selected-runtime-config-rejected is reserved for explicit selected model launch rejection."
    - "Resume launch argument coverage proves model is passed and unsupported effort fields cannot reach Cursor launch."
  artifacts:
    - path: "cli/src/modules/common/rpcTypes.ts"
      provides: "Runner spawn contract without unsupported effort fields"
      contains: "SpawnSessionOptions"
    - path: "cli/src/api/apiMachine.ts"
      provides: "Machine RPC spawn handler that forwards selected model only"
      contains: "spawn-happy-session"
    - path: "cli/src/runner/run.ts"
      provides: "Cursor launch arg builder and selected-runtime rejection classifier"
      contains: "buildCliArgs"
  key_links:
    - from: "cli/src/api/apiMachine.ts"
      to: "cli/src/runner/run.ts"
      via: "spawn-happy-session -> spawnSession -> buildCliArgs"
      pattern: "spawnSession"
    - from: "cli/src/runner/run.ts"
      to: "Cursor Agent process"
      via: "argument array from buildCliArgs"
      pattern: "--model"
---

<objective>
Close the CLI runner spawn-contract slice of the CURS-02 effort gap by proving only supported runtime arguments reach Cursor launch and resume.

Purpose: 01-11 rejects unsupported effort before Hub spawn persistence, and 01-14 blocks session-config persistence. This final split segment removes the stale CLI spawn option surface and proves model launch/resume behavior remains supported without inventing effort flags.
Output: CLI spawn RPC types and machine handler are model-only for selected runtime launch, `buildCliArgs()` coverage proves model/resume behavior, and selected-runtime rejection classification remains model-scoped.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
@.planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
@.planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
@.planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
@.planning/phases/01-cursor-runtime-config-contract/01-11-PLAN.md
@.planning/phases/01-cursor-runtime-config-contract/01-14-PLAN.md
@cli/src/modules/common/rpcTypes.ts
@cli/src/api/apiMachine.ts
@cli/src/runner/run.ts

<interfaces>
Existing runtime facts:
- `SpawnSessionOptions` currently carries `effort?: string` and `modelReasoningEffort?: string`; removing these from the spawn contract prevents downstream forwarding.
- `buildCliArgs()` currently emits `--model` and `--permission-mode`, and has no verified Cursor effort flag to emit.
- `toSafeSpawnFailure()` currently labels selected runtime launch failures; after this plan, the label must be tied to explicit selected model input only.
- `ApiMachine` handles `spawn-happy-session` RPC and forwards spawn options to the runner.
</interfaces>

<preconditions>
01-14 must be complete first so the backend has no remaining session-config persistence path for unsupported effort. This plan intentionally owns only CLI spawn type/handler/launch files and remains below the 15-file scope threshold.
</preconditions>

<gap_source>
01-VERIFICATION gap #1: `buildCliArgs()` emits `--model` but drops effort/modelReasoningEffort, while the spawn contract exposes those fields. This segment finalizes the CLI launch contract and test evidence.
</gap_source>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove unsupported effort from CLI spawn types</name>
  <files>cli/src/modules/common/rpcTypes.ts, cli/src/api/apiMachine.ts, cli/src/api/apiMachine.test.ts</files>
  <read_first>
    - cli/src/modules/common/rpcTypes.ts
    - cli/src/api/apiMachine.ts
    - cli/src/api/apiMachine.test.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
    - .planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
  </read_first>
  <behavior>
    - `SpawnSessionOptions` no longer exposes `effort` or `modelReasoningEffort`.
    - `ApiMachine` `spawn-happy-session` handler no longer destructures or passes effort fields.
    - `apiMachine` tests prove the handler forwards model but no effort fields in spawn options.
  </behavior>
  <action>After GitNexus impact prechecks for `SpawnSessionOptions` and `ApiMachineClient.setRPCHandlers`, delete `effort` and `modelReasoningEffort` from the CLI runner spawn option type and the `spawn-happy-session` payload destructuring/forwarding path. Keep explicit `model` forwarding unchanged. Update `apiMachine` tests so the handler forwards model while the spawn options it passes to `spawnSession` have no effort fields. Do not add compatibility aliases or fallback mapping for unsupported effort because this branch has no install base and D-06 requires truthful runtime support.</action>
  <verify>
    <automated>cd cli && bun run test -- apiMachine</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `cli/src/modules/common/rpcTypes.ts` `SpawnSessionOptions` has `model?: string` but no `effort` or `modelReasoningEffort` properties.
    - Source assertion: `cli/src/api/apiMachine.ts` does not destructure or forward `effort` or `modelReasoningEffort` in `spawn-happy-session`.
    - Test assertion: `apiMachine` tests prove selected model still reaches `spawnSession`.
    - Typecheck assertion: stale spawn effort references fail compilation if any remain.
  </acceptance_criteria>
  <done>The CLI machine spawn RPC contract cannot carry unsupported effort fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove Cursor launch args stay supported-only</name>
  <files>cli/src/runner/run.ts, cli/src/runner/run.test.ts</files>
  <read_first>
    - cli/src/runner/run.ts
    - cli/src/runner/run.test.ts
    - cli/src/modules/common/rpcTypes.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
    - .planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
  </read_first>
  <behavior>
    - `buildCliArgs()` tests cover selected model launch.
    - `buildCliArgs()` tests cover resume plus selected model launch.
    - No test or source path emits invented effort/modelReasoningEffort flags.
    - `toSafeSpawnFailure()` labels selected-runtime-config-rejected only when explicit `model` was present.
  </behavior>
  <action>After GitNexus impact prechecks for `buildCliArgs` and `toSafeSpawnFailure`, keep `buildCliArgs()` limited to verified Cursor flags and update tests to prove `buildCliArgs('cursor', { model })` includes `--model`, `buildCliArgs('cursor', { resumeSessionId, model })` includes both `--resume` and `--model`, and no unsupported effort flag appears. Change `toSafeSpawnFailure()` so only `options.model` triggers `selected-runtime-config-rejected`; effort-only requests cannot reach this function after Tasks 01-11 and 01-15, and must not broaden the selected-runtime label. This implements D-05, D-06, and D-08.</action>
  <verify>
    <automated>cd cli && bun run test -- run</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `buildCliArgs()` emits no effort or modelReasoningEffort flag.
    - Test assertion: selected model launch args include `--model`.
    - Test assertion: resumed selected model launch args include `--resume` and `--model`.
    - Source assertion: `toSafeSpawnFailure()` checks `options.model` only for selected-runtime-config rejection.
    - Typecheck assertion: `bun run typecheck` passes after removing stale effort spawn references.
  </acceptance_criteria>
  <done>The runner launch contract is model-only for selected runtime launch, matching the verified Cursor launch surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub to CLI machine RPC | Spawn payload enters the runner process. |
| CLI runner to Cursor process | Selected runtime config becomes process argument array. |
| Runner failure to Hub/Web | Launch failure classification becomes user-visible status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15-01 | Tampering | `SpawnSessionOptions` | mitigate | Remove unsupported effort fields from the spawn contract so callers cannot smuggle unsupported runtime config. |
| T-01-15-02 | Integrity | `buildCliArgs` | mitigate | Keep launch args limited to verified Cursor flags; tests prove no invented effort flag is emitted. |
| T-01-15-03 | Repudiation | `toSafeSpawnFailure` | mitigate | Label selected-runtime-config-rejected only for explicit selected model rejection, avoiding false effort rejection classification. |
| T-01-15-04 | Information Disclosure | runner launch failure | mitigate | Preserve safe failure mapping without exposing raw process stderr beyond existing safe error contracts. |
| T-01-15-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. If an executor adds one, stop for slopcheck and a blocking human checkpoint. |
</threat_model>

<verification>
Run focused CLI apiMachine and runner tests plus root typecheck. If targeted checks pass and no known runner integration hang blocks locally, run `bash scripts/check-no-cut-agents.sh`; broader `bun run test` can follow after 01-12 and 01-13 execute.
</verification>

<success_criteria>
- CLI spawn RPC types and handlers cannot carry unsupported effort/modelReasoningEffort.
- New and resumed Cursor launches pass selected model and no unsupported effort flags.
- Selected model rejection remains a safe selected-runtime-config-rejected failure; unsupported effort no longer creates a divergent stored/runtime state.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Start sessions with selected model/effort truthfully | 01-15 | COVERED | Finalizes runner launch truth by removing unsupported effort from CLI contract.
REQ | CURS-02 | Selected model/effort launch and session metadata does not diverge from runtime | 01-15 | COVERED | Runner cannot receive or emit unsupported effort launch args.
RESEARCH | Pitfall 2 | Do not enable effort controls without runtime proof | 01-15 | COVERED | No effort flags are invented.
CONTEXT | D-05 | Default runtime config is auto/unspecified | 01-15 | COVERED | Auto/resume launches omit unsupported effort fields.
CONTEXT | D-06 | Effort is runtime capability, not HAPI-owned promise | 01-15 | COVERED | CLI spawn contract exposes only verified Cursor runtime fields.
CONTEXT | D-08 | Selected runtime rejection is clear, no fallback | 01-15 | COVERED | Rejection classifier remains selected-model scoped.
</source_audit>

<output>
Create `.planning/phases/01-cursor-runtime-config-contract/01-15-SUMMARY.md` when done.
</output>
