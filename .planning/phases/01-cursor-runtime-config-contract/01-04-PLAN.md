---
phase: 01-cursor-runtime-config-contract
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - web/src/components/NewSession/ModelSelector.tsx
  - web/src/components/NewSession/ModelSelector.test.tsx
  - web/src/components/NewSession/index.tsx
  - web/src/hooks/mutations/useSpawnSession.ts
  - web/src/lib/locales/en.ts
  - web/src/lib/locales/zh-CN.ts
autonomous: true
requirements:
  - CURS-01
  - CURS-02
must_haves:
  truths:
    - "D-02: Opening the new-session panel triggers model discovery for the selected machine."
    - "D-03: The selector always offers Auto (unspecified), including loading, empty, and error states with safe retry copy."
    - "D-04: Successful discovery renders raw Cursor model ids as primary selector values."
    - "D-05: An explicit model is passed to spawn only when the user selects a discovered raw model id."
    - "D-06: Effort remains auto/unspecified unless Cursor discovery or verified runtime behavior confirms support."
    - "D-08: Launch rejection for selected runtime config is surfaced clearly and does not silently continue with auto."
  artifacts:
    - path: "web/src/components/NewSession/ModelSelector.tsx"
      provides: "Discovery-aware runtime model selector"
    - path: "web/src/components/NewSession/index.tsx"
      provides: "Panel-open discovery trigger and explicit-only spawn payload"
    - path: "web/src/components/NewSession/ModelSelector.test.tsx"
      provides: "Selector state coverage"
    - path: "web/src/lib/locales/en.ts"
      provides: "English runtime config strings"
    - path: "web/src/lib/locales/zh-CN.ts"
      provides: "Simplified Chinese runtime config strings"
  key_links:
    - from: "web/src/components/NewSession/index.tsx"
      to: "web/src/hooks/useCursorModels.ts"
      via: "panel-open enabled flag"
      pattern: "useCursorModels"
    - from: "web/src/components/NewSession/index.tsx"
      to: "web/src/hooks/mutations/useSpawnSession.ts"
      via: "model !== 'auto' ? model : undefined"
      pattern: "resolvedModel"
---

<objective>
Wire discovered Cursor models into the mobile new-session flow.

Purpose: Users must be able to discover models before launch, choose one explicitly, or continue with no explicit model when discovery fails.
Output: Discovery-aware selector, launch wiring, localized copy, and focused component tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
@.planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
@.planning/phases/01-cursor-runtime-config-contract/01-VALIDATION.md
@.planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
@.planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
@web/src/components/NewSession/ModelSelector.tsx
@web/src/components/NewSession/index.tsx
@web/src/hooks/useCursorModels.ts
@web/src/hooks/mutations/useSpawnSession.ts
@web/src/lib/locales/en.ts
@web/src/lib/locales/zh-CN.ts

<interfaces>
From prior plans:
- `useCursorModels(api, machineId, enabled)` returns discovery result state and `retry()`.
- `ApiClient.spawnSession` accepts optional `model`.

UI contract:
- Auto option copy: `Auto (unspecified)`.
- Loading copy: `Loading Cursor models...`.
- Error copy: `Could not load Cursor models. Retry discovery, or continue with auto.`
- Empty heading: `No Cursor models found`.
- Launch rejection copy: `Cursor rejected the selected runtime config. Choose another model or continue with auto.`
</interfaces>
</context>

<execution_preconditions>
- Before editing any existing function, class, or method named in this plan, run GitNexus `impact` with `repo: "hapi-cursor"`, `direction: "upstream"`, and the target symbol name. Record direct callers, affected processes, and risk level in the plan summary. If impact is HIGH or CRITICAL, stop and warn before editing.
- Confirm Plan 02 and Plan 03 produced a backend launch rejection contract for D-08: CLI returns `code: 'selected-runtime-config-rejected'` for explicit selected model/effort rejection, and Hub preserves that safe spawn failure without fallback. Web copy in this plan maps only that safe backend signal; it must not infer rejection from arbitrary process output.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render discovery-aware model selector states</name>
  <files>web/src/components/NewSession/ModelSelector.tsx, web/src/components/NewSession/ModelSelector.test.tsx, web/src/lib/locales/en.ts, web/src/lib/locales/zh-CN.ts</files>
  <read_first>
    - web/src/components/NewSession/ModelSelector.tsx
    - web/src/components/NewSession/ModelSelector.test.tsx
    - web/src/lib/locales/en.ts
    - web/src/lib/locales/zh-CN.ts
    - web/src/lib/use-translation.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
    - .planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
    - .planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
  </read_first>
  <behavior>
    - Loading state keeps the select visible, disabled, and shows localized `Loading Cursor models...`.
    - Error state shows safe localized reason, `Retry discovery`, and keeps `Auto (unspecified)` selectable.
    - Empty state shows `No Cursor models found` and the empty-state body from UI-SPEC.
    - Success state lists raw Cursor model ids as primary option text; labels are secondary and must not hide ids.
  </behavior>
  <action>Extend `ModelSelector` props to accept discovery result data, `onRetryDiscovery`, and safe discovery reason. Always include the auto sentinel option with localized text `newSession.model.autoUnspecified`. Render discovered raw model ids as option labels that begin with the raw `id`; if a secondary label exists, append it without replacing the id. Add locale keys under existing `newSession.*` or nearest local grouping for loading, retry, empty, safe reasons (`cursor-cli-unavailable`, `not-authenticated`, `timed-out`, `empty-model-list`, `unknown`), and launch rejection. Add `ModelSelector.test.tsx` with `I18nProvider` coverage for loading, success, empty, error+retry, and raw id preservation. This implements D-03, D-04, D-05, and UI-SPEC copy/spacing constraints.</action>
  <verify>
    <automated>cd web && bun run test -- ModelSelector</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `ModelSelector.tsx` calls `useTranslation()` for all new user-facing strings.
    - Source assertion: the auto option value remains `auto` and its English text is `Auto (unspecified)`.
    - Test assertion: `cd web && bun run test -- ModelSelector` verifies retry callback firing and raw id text visibility.
    - Source assertion: no hardcoded English discovery strings appear in `ModelSelector.tsx`.
  </acceptance_criteria>
  <done>New-session model selector shows runtime discovery states safely and accessibly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trigger discovery on panel open and preserve explicit-only launch config</name>
  <files>web/src/components/NewSession/index.tsx, web/src/hooks/mutations/useSpawnSession.ts, web/src/components/NewSession/ModelSelector.test.tsx</files>
  <read_first>
    - web/src/components/NewSession/index.tsx
    - web/src/hooks/useCursorModels.ts
    - web/src/hooks/mutations/useSpawnSession.ts
    - web/src/components/NewSession/ModelSelector.tsx
    - web/src/components/NewSession/ModelSelector.test.tsx
    - .planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
    - .planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
    - .planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
  </read_first>
  <behavior>
    - Discovery is enabled only while the new-session panel is open and `machineId` is set.
    - Leaving the selector on auto passes `model: undefined` to spawn.
    - Selecting a discovered id passes exactly that raw id to spawn.
    - Separate effort controls are not rendered unless a verified separate effort capability exists; this phase should keep effort auto/unspecified.
    - A spawn error caused by selected runtime config shows the localized launch rejection copy.
  </behavior>
  <action>After GitNexus impact prechecks for `NewSession`, `useSpawnSession`, and selector components, verify the backend D-08 contract from Plan 02/03 exists before mapping copy: CLI detects explicit selected `model`, `effort`, or `modelReasoningEffort` rejection and Hub returns `code: 'selected-runtime-config-rejected'` as a safe spawn failure without retrying auto. In `NewSession/index.tsx`, call `useCursorModels(api, machineId, isPanelOpenOrMounted)` using the component's existing open/mounted state and selected machine id. Feed the result into `ModelSelector`, including retry. Keep `const resolvedModel = model !== 'auto' ? model : undefined` as the only model spawn mapping. Do not persist or send `'auto'`. Do not add a standalone effort dropdown or hardcoded effort list. In `useSpawnSession.ts`, keep `model` optional and map only the safe `selected-runtime-config-rejected` spawn code to the launch rejection locale key; keep generic spawn errors generic and never parse raw stderr in Web. This implements D-02, D-05, D-06, and D-08.</action>
  <verify>
    <automated>cd web && bun run test -- ModelSelector NewSession useSpawnSession</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `NewSession/index.tsx` calls `useCursorModels` with an enabled/open flag and `machineId`.
    - Source assertion: `resolvedModel` maps only non-`auto` values to the spawn payload.
    - Source assertion: no hardcoded effort option array is added in `NewSession` or `ModelSelector`.
    - Test assertion: auto launch produces `model: undefined`; explicit model launch produces the selected raw id.
    - Test assertion: selected runtime config rejection code from Hub renders the localized launch rejection text, while generic spawn errors do not use that copy.
    - Source assertion: Web does not parse raw stderr/process output to decide selected runtime rejection.
  </acceptance_criteria>
  <done>Users can discover and choose a model at launch, while default launches remain genuinely unspecified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub discovery result to Web UI | Runtime metadata and safe errors become mobile-visible selector state. |
| Web form to Hub spawn route | User-selected model id becomes a runtime launch option. |
| Cursor launch failure to Web copy | Runtime rejection must be clear without leaking noisy process output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Spoofing / Integrity | `ModelSelector` model labels | mitigate | Show raw Cursor model id as primary value and never rename it into a HAPI-owned alias. |
| T-01-11 | Tampering | `NewSession` spawn payload | mitigate | Send only `undefined` for auto or an explicitly selected raw discovered id; never send `'auto'` as a model id. |
| T-01-12 | Information Disclosure | launch rejection UI | mitigate | Show localized safe rejection copy; do not render raw stderr or process output. |
| T-01-SC | Tampering | package installs | accept | No package-manager installs in this plan; package legitimacy audit says no install. |
</threat_model>

<verification>
Run `cd web && bun run test -- ModelSelector NewSession useSpawnSession`, then `bun run typecheck`.
</verification>

<success_criteria>
The new-session flow lets users discover runtime-owned Cursor model ids, start with a selected model only when explicit, and continue with auto when discovery fails or is empty.
</success_criteria>

<output>
Create `.planning/phases/01-cursor-runtime-config-contract/01-04-SUMMARY.md` when done.
</output>
