---
phase: 01-cursor-runtime-config-contract
plan: 08
type: execute
wave: 6
depends_on:
  - 01-06
files_modified:
  - web/src/components/SessionList.tsx
  - web/src/components/SessionList/SessionListItem.tsx
  - web/src/components/SessionList/SessionListItem.test.tsx
  - web/src/hooks/useSSE.ts
  - web/src/hooks/useSSE.test.tsx
  - shared/src/sessionSummary.ts
  - shared/src/schemas.ts
autonomous: true
requirements:
  - CURS-04
must_haves:
  truths:
    - "D-13: Session list rows show compact status indicators, not model/effort text."
    - "D-13: Running and thinking show spinner-style status."
    - "D-16: Waiting for input and waiting for approval collapse to one yellow dot."
    - "D-14: Status labels are accessible through hidden, hover, title, or aria labels without normal visible row text."
    - "D-15: Error shows red, unread completed result shows green, and viewed/inactive state shows gray."
    - "Strict SSE patches keep session summary/detail runtime fields live without malformed-event fallback."
  artifacts:
    - path: "web/src/components/SessionList/SessionListItem.tsx"
      provides: "Compact accessible status indicator"
    - path: "web/src/components/SessionList.tsx"
      provides: "Viewed-completed session tracking"
    - path: "web/src/components/SessionList/SessionListItem.test.tsx"
      provides: "Status indicator coverage"
    - path: "web/src/hooks/useSSE.ts"
      provides: "Strict patch cache handling for model/effort/status fields"
    - path: "web/src/hooks/useSSE.test.tsx"
      provides: "Patch convergence and strict rejection coverage"
    - path: "shared/src/sessionSummary.ts"
      provides: "Explicit summary status, completion, and error marker fields"
    - path: "shared/src/schemas.ts"
      provides: "Strict patch fields for status/completion markers when emitted live"
  key_links:
    - from: "web/src/hooks/useSSE.ts"
      to: "web/src/components/SessionList/SessionListItem.tsx"
      via: "TanStack sessions query cache"
      pattern: "patchSessionSummary"
---

<objective>
Make the mobile session list a compact live status surface.

Purpose: Users should scan attention state quickly from the list while model/effort remain in the composer area.
Output: Compact indicators, accessibility labels, and SSE patch tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
@.planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
@.planning/phases/01-cursor-runtime-config-contract/01-VALIDATION.md
@.planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
@.planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
@web/src/components/SessionList.tsx
@web/src/components/SessionList/SessionListItem.tsx
@web/src/components/SessionList/SessionListItem.test.tsx
@web/src/hooks/useSSE.ts
@web/src/hooks/useSSE.test.tsx
@shared/src/sessionSummary.ts
@shared/src/schemas.ts

<interfaces>
From Plan 06:
- Locale keys exist for session status labels: running, waiting, error, unreadComplete, viewed.

Existing live fields:
- `SessionSummary.active`
- `SessionSummary.thinking`
- `SessionSummary.pendingRequestsCount`
- `SessionSummary.backgroundTaskCount`
- `SessionSummary.model`
- `SessionSummary.effort`
- `SessionPatchSchema` strict patch fields for model/effort/thinking/active/backgroundTaskCount.

Required explicit status fields:
- Add or prove existing `SessionSummary.statusKind: 'running' | 'thinking' | 'waiting' | 'error' | 'completed' | 'idle'`.
- Add or prove existing `SessionSummary.completionMarker: number | null`, using a completion-specific timestamp if available or `updatedAt` when `endReason === 'completed'`.
- Add or prove existing `SessionSummary.errorMarker: number | null`, using an error-specific timestamp if available or `updatedAt` when `endReason === 'error'`.
- If status/completion/error can change via patch without a full session object, add matching strict `SessionPatchSchema` optional fields before emitting them through SSE.

Implementation rule for D-15:
- Treat `SessionSummary.statusKind === 'completed'` with non-null `completionMarker` as unread until the user selects/views that marker.
- Track viewed completed sessions in Web state keyed by `session.id` and `completionMarker`, so a later completed result can become green again.
</interfaces>
</context>

<execution_preconditions>
- Before editing any existing function, class, or method named in this plan, run GitNexus `impact` with `repo: "hapi-cursor"`, `direction: "upstream"`, and the target symbol name. Record direct callers, affected processes, and risk level in the plan summary. If impact is HIGH or CRITICAL, stop and warn before editing.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render compact session status indicators</name>
  <files>shared/src/sessionSummary.ts, shared/src/schemas.ts, web/src/components/SessionList.tsx, web/src/components/SessionList/SessionListItem.tsx, web/src/components/SessionList/SessionListItem.test.tsx</files>
  <read_first>
    - web/src/components/SessionList.tsx
    - web/src/components/SessionList/SessionListItem.tsx
    - web/src/components/SessionList/SessionListItem.test.tsx
    - shared/src/sessionSummary.ts
    - shared/src/schemas.ts
    - web/src/lib/locales/en.ts
    - web/src/lib/locales/zh-CN.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
    - .planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
    - .planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
  </read_first>
  <behavior>
    - Active running/thinking row shows a spinner-style icon.
    - Any pending request count greater than zero shows one yellow dot.
    - Error state shows one red dot when `SessionSummary.statusKind === 'error'` or a proven existing equivalent error summary field is present.
    - Completed unread result shows one green dot when `SessionSummary.statusKind === 'completed'` and `completionMarker` has not been viewed.
    - After the user selects/views a completed session, the same completion marker shows gray.
    - Row visible text does not include `s.model` or `s.effort`.
  </behavior>
  <action>After GitNexus impact prechecks for `toSessionSummary`, `SessionList`, and `SessionListItem`, define explicit status/completion/error source fields before rendering indicators. Prefer adding `statusKind`, `completionMarker`, and `errorMarker` to `shared/src/sessionSummary.ts`; if equivalent fields already exist by execution time, use those exact fields and document the source in the summary. Derive `statusKind` from existing session data in this order: active/thinking/background work => running/thinking, pending request count => waiting, `endReason === 'error'` or equivalent error field => error, `endReason === 'completed'` or equivalent completed field => completed, otherwise idle. Add matching optional strict `SessionPatchSchema` fields in `shared/src/schemas.ts` only if live patch producers emit them; otherwise keep summary derivation from full session updates and existing patch fields. Update `SessionList.tsx` to track viewed completed sessions keyed by session id and `completionMarker`. Mark a completed session viewed when the user selects it. Update `SessionListItem.tsx` to render one compact status indicator inside the existing row density, accepting concrete props derived from `statusKind`, `completionMarker`, `errorMarker`, and viewed state. Use the existing `LoaderIcon` for running/thinking. Render an 8px dot for waiting, error, unread completed, and viewed/inactive states using UI-SPEC colors. Provide `aria-label` or `title` from locale keys for every indicator. Keep normal visible row text to title, path/todo/relative-time surfaces that already exist; do not add model or effort text. Extend `SessionListItem.test.tsx` for spinner, yellow dot, single yellow dot when multiple pending states collapse, red error, green unread completed, gray viewed completed, no model/effort text, and accessible labels. This implements D-13, D-14, D-15, D-16, and the corrected CURS-04 interpretation.</action>
  <verify>
    <automated>cd shared && bun test sessionSummary</automated>
    <automated>cd web && bun run test -- SessionListItem</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `SessionListItem.tsx` does not render `s.model` or `s.effort` as visible row text.
    - Source assertion: `SessionSummary` exposes concrete status/completion/error marker fields or the summary documents the proven existing equivalents used.
    - Test assertion: shared summary tests cover completed and error marker derivation.
    - Test assertion: pending requests render one yellow indicator, not visible `Pending N` text unless the existing design intentionally retains pending count outside the indicator.
    - Test assertion: indicator has an accessible label for running/thinking and waiting states.
    - Test assertion: active thinking state uses a spinner-style icon.
    - Test assertion: unread completed renders green before selection and gray after the viewed marker is recorded.
    - UI assertion: dot size is 8px or Tailwind equivalent `h-2 w-2`.
  </acceptance_criteria>
  <done>Mobile session rows show compact accessible attention state without model/effort clutter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify strict SSE patch convergence for runtime fields</name>
  <files>web/src/hooks/useSSE.ts, web/src/hooks/useSSE.test.tsx</files>
  <read_first>
    - web/src/hooks/useSSE.ts
    - web/src/hooks/useSSE.test.tsx
    - shared/src/schemas.ts
    - shared/src/sessionSummary.ts
    - web/src/lib/query-keys.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
    - .planning/phases/01-cursor-runtime-config-contract/01-VALIDATION.md
    - .planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
  </read_first>
  <behavior>
    - `session-updated` patch for `model` updates session detail and summary caches without full refetch.
    - `session-updated` patch for `effort` updates session detail and summary caches without full refetch.
    - `session-updated` patch for `modelReasoningEffort` updates session detail cache.
    - Unknown patch field still logs and does not mutate caches.
    - Runtime patches keep status list and composer metadata live across SSE.
  </behavior>
  <action>After GitNexus impact prechecks for `useSSE` patch helpers, review `useSSE.ts` patch handling and update only if needed so model, effort, modelReasoningEffort, active, thinking, pending/background fields and any newly emitted status/completion/error patch fields converge through strict `SessionPatchSchema` updates. Extend `useSSE.test.tsx` with cases from `01-VALIDATION.md`: model, effort, modelReasoningEffort, thinking/status changes, completion marker/status update when emitted as a patch, error marker/status update when emitted as a patch, and malformed unknown-field rejection. Do not reintroduce a malformed-event refetch fallback. This implements strict live patches for CURS-04 and protects the contract from research Pitfall 4.</action>
  <verify>
    <automated>cd web && bun run test -- useSSE SessionListItem</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: `cd web && bun run test -- useSSE` includes model, effort, modelReasoningEffort, thinking/status, completion marker, and error marker patch cases when those fields are emitted live.
    - Test assertion: malformed patch with an unknown field logs and does not mutate query caches.
    - Source assertion: `useSSE.ts` does not add a full-list refetch fallback for unknown patch fields.
    - Typecheck assertion: `bun run typecheck` passes.
  </acceptance_criteria>
  <done>Strict SSE patches keep runtime and status UI live without weakening malformed-event handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream to Web cache | Live patches mutate session list and detail state. |
| Session summary to mobile list | Persisted/runtime state becomes compact user-visible status. |
| Accessibility label generation | Hidden labels communicate state to assistive tech. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-23 | Integrity | `useSSE` patch handling | mitigate | Keep strict schema parsing and tests for unknown-field rejection. |
| T-01-24 | Spoofing | session list status indicator | mitigate | Derive indicators only from existing session summary fields; do not invent unread/error state without source data. |
| T-01-25 | Information Disclosure | mobile session row | mitigate | Do not render model/effort text in the session list per context correction. |
| T-01-SC | Tampering | package installs | accept | No package-manager installs in this plan; package legitimacy audit says no install. |
</threat_model>

<verification>
Run `cd web && bun run test -- SessionListItem useSSE`, then `bun run typecheck`.
</verification>

<success_criteria>
Users can scan live session attention state from mobile rows, and runtime metadata stays live through strict SSE patches while model/effort visibility remains composer-adjacent.
</success_criteria>

<output>
Create `.planning/phases/01-cursor-runtime-config-contract/01-08-SUMMARY.md` when done.
</output>
