---
phase: 01-cursor-runtime-config-contract
plan: 12
type: execute
wave: 4
depends_on: ["01-15"]
files_modified:
  - web/src/api/client.ts
  - web/src/hooks/mutations/useSpawnSession.ts
  - web/src/hooks/mutations/useSessionActions.ts
  - web/src/hooks/mutations/useSessionActions.test.ts
  - web/src/components/NewSession/NewSession.test.tsx
  - web/src/components/SessionChat.tsx
  - web/src/components/SessionChat.test.tsx
  - web/src/components/AssistantChat/HappyComposer.tsx
  - web/src/components/AssistantChat/HappyComposer.test.tsx
autonomous: true
gap_closure: true
requirements: [CURS-02]
user_setup: []
must_haves:
  truths:
    - "D-06: Web exposes no separate effort mutation while Cursor effort support is unverified."
    - "D-07: Existing effort metadata can still display near the composer when runtime/session data contains it."
    - "D-05: New-session Web spawn payload sends selected model only, never auto or unsupported effort."
    - "CURS-02 selected runtime UI cannot claim effort persistence that the runtime did not receive."
  artifacts:
    - path: "web/src/api/client.ts"
      provides: "Web API client without unsupported setEffort or spawn effort payload"
      contains: "spawnSession"
    - path: "web/src/hooks/mutations/useSessionActions.ts"
      provides: "Session actions without unsupported effort mutation"
      contains: "useSessionActions"
    - path: "web/src/components/SessionChat.tsx"
      provides: "Composer wiring that displays effort metadata but cannot mutate unsupported effort"
      contains: "HappyComposer"
    - path: "web/src/components/AssistantChat/HappyComposer.tsx"
      provides: "Composer props without unsupported effort mutation callback"
      contains: "HappyComposerProps"
  key_links:
    - from: "web/src/components/NewSession/index.tsx"
      to: "web/src/api/client.ts"
      via: "useSpawnSession -> ApiClient.spawnSession"
      pattern: "spawnSession"
    - from: "web/src/components/SessionChat.tsx"
      to: "web/src/hooks/mutations/useSessionActions.ts"
      via: "session actions used for composer runtime config"
      pattern: "setModel"
---

<objective>
Close the Web half of the CURS-02 effort gap by removing the unsupported effort mutation surface while preserving read-only effort display from real session metadata.

Purpose: Verification found `ApiClient.setEffort()` posts to a route that does not exist. Since Phase 01 has no verified separate Cursor effort support, Web must not expose a mutation path that can drift from runtime truth.
Output: Web spawn and session-action APIs are model-only for runtime mutation, SessionChat no longer passes an effort-change callback, and tests prove effort metadata remains display-only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
@.planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
@.planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
@.planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
@.planning/phases/01-cursor-runtime-config-contract/01-04-SUMMARY.md
@.planning/phases/01-cursor-runtime-config-contract/01-06-SUMMARY.md
@.planning/phases/01-cursor-runtime-config-contract/01-07-SUMMARY.md
@.planning/phases/01-cursor-runtime-config-contract/01-15-PLAN.md
@web/src/api/client.ts
@web/src/hooks/mutations/useSpawnSession.ts
@web/src/hooks/mutations/useSessionActions.ts
@web/src/components/SessionChat.tsx
@web/src/components/AssistantChat/HappyComposer.tsx
@web/src/components/AssistantChat/StatusBar.tsx

<interfaces>
Existing Web contracts:
- `ApiClient.setModel(sessionId, model)` returns `CursorRuntimeConfigApplyResult` and remains the only session runtime mutation endpoint.
- `ApiClient.setEffort(sessionId, effort)` currently posts to missing `/api/sessions/:id/effort`; remove it.
- `useSessionActions()` currently exposes `setEffort`; remove it from the returned action object and pending-state calculation.
- `StatusBar` can display `modelReasoningEffort` or `effort` props when present; preserve display-only metadata rendering.
</interfaces>

<gap_source>
01-VERIFICATION gap #1: `web/src/api/client.ts` exposes `setEffort()` while no Hub effort route exists, creating a public mutation contract that cannot truthfully apply to Cursor runtime.
</gap_source>
<preconditions>
01-15 must be complete first so the backend and CLI runner have fully removed unsupported effort from spawn and session-config paths before Web removes the unsupported effort mutation surface.
</preconditions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove unsupported Web effort API and spawn payload</name>
  <files>web/src/api/client.ts, web/src/hooks/mutations/useSpawnSession.ts, web/src/hooks/mutations/useSessionActions.ts, web/src/hooks/mutations/useSessionActions.test.ts, web/src/components/NewSession/NewSession.test.tsx</files>
  <read_first>
    - web/src/api/client.ts
    - web/src/hooks/mutations/useSpawnSession.ts
    - web/src/hooks/mutations/useSessionActions.ts
    - web/src/hooks/mutations/useSessionActions.test.ts
    - web/src/components/NewSession/NewSession.test.tsx
    - .planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
    - .planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
    - .planning/phases/01-cursor-runtime-config-contract/01-15-PLAN.md
  </read_first>
  <behavior>
    - `ApiClient` has no `setEffort()` method.
    - `ApiClient.spawnSession()` no longer accepts or serializes an `effort` argument.
    - `useSpawnSession` no longer includes `effort` in its mutation input type or request body.
    - `useSessionActions()` exposes `setModel` but no `setEffort`.
    - New-session tests prove auto sends no model and no effort, explicit model sends only model.
  </behavior>
  <action>After GitNexus impact prechecks for `ApiClient`, `useSpawnSession`, and `useSessionActions`, delete the `setEffort()` method from `web/src/api/client.ts`. Remove `effort` from `ApiClient.spawnSession()` parameters and JSON body. Remove `effort` from `useSpawnSession` input typing and call sites. Remove the `effortMutation`, `setEffort` return field, and pending-state contribution from `useSessionActions.ts`. Keep `setModel` unchanged and status-bearing. Update `useSessionActions.test.ts` to assert the returned actions do not include `setEffort` and that model result propagation still works. Update new-session launch tests so auto launches send no model/effort and explicit launches send only the raw selected model id. Do not add a replacement unsupported route call or optimistic effort state. This implements D-05 and D-06.</action>
  <verify>
    <automated>cd web && bun run test -- useSessionActions NewSession useSpawnSession</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `web/src/api/client.ts` contains no method named `setEffort` and no `/effort` session route string.
    - Source assertion: `ApiClient.spawnSession` and `useSpawnSession` do not accept or serialize an `effort` field.
    - Source assertion: `useSessionActions` return type has no `setEffort`.
    - Test assertion: explicit model launch sends `model` only; default auto launch omits both model and effort.
    - Test assertion: model mutation result propagation remains covered after removing effort mutation.
    - Typecheck assertion: `bun run typecheck` catches no stale Web `setEffort` references.
  </acceptance_criteria>
  <done>Web no longer exposes an unsupported effort API or spawn payload field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make composer effort display read-only</name>
  <files>web/src/components/SessionChat.tsx, web/src/components/SessionChat.test.tsx, web/src/components/AssistantChat/HappyComposer.tsx, web/src/components/AssistantChat/HappyComposer.test.tsx</files>
  <read_first>
    - web/src/components/SessionChat.tsx
    - web/src/components/SessionChat.test.tsx
    - web/src/components/AssistantChat/HappyComposer.tsx
    - web/src/components/AssistantChat/HappyComposer.test.tsx
    - web/src/components/AssistantChat/StatusBar.tsx
    - .planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
    - .planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
    - .planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
  </read_first>
  <behavior>
    - `SessionChat` no longer creates `handleEffortChange` or passes `onEffortChange` to `HappyComposer`.
    - `HappyComposerProps` no longer exposes effort mutation callbacks.
    - `modelReasoningEffort` and `effort` props still flow to `StatusBar` for read-only display when session metadata contains them.
    - No hardcoded user-facing copy is added; any existing display copy remains localized through existing paths.
  </behavior>
  <action>After GitNexus impact prechecks for `SessionChat` and `HappyComposer`, remove the effort mutation callback path from the composer layer. Delete `handleEffortChange` from `SessionChat.tsx`, remove `onEffortChange` and any `onModelReasoningEffortChange` mutation props from `HappyComposerProps`, and stop passing those callbacks. Preserve read-only `modelReasoningEffort={props.session.modelReasoningEffort}` and `effort={props.session.effort}` prop threading into `StatusBar` so runtime-provided metadata remains visible per D-07. Update `SessionChat.test.tsx` and `HappyComposer.test.tsx` to prove effort text can render from props but no effort mutation callback is passed or invoked. Do not add a standalone effort selector, hardcoded effort options, or chat timeline event. This implements D-06, D-07, and D-12.</action>
  <verify>
    <automated>cd web && bun run test -- SessionChat HappyComposer StatusBar</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `SessionChat.tsx` contains no `handleEffortChange` and does not pass `onEffortChange`.
    - Source assertion: `HappyComposerProps` contains `modelReasoningEffort` and `effort` display props but no effort mutation callback props.
    - Test assertion: `HappyComposer` or `StatusBar` still renders an existing non-null effort/modelReasoningEffort value.
    - Test assertion: there is no selectable effort option or effort mutation callback in composer tests.
    - Source assertion: no hardcoded effort option list is added in Web components.
  </acceptance_criteria>
  <done>Effort remains an honest display-only runtime metadata field unless future verified Cursor support creates a real mutation path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web runtime controls to Hub API | Composer and new-session controls can initiate runtime config mutations. |
| Session metadata to composer UI | Stored/runtime-provided effort metadata becomes visible to the user. |
| Web error handling to user-visible status | Failed runtime mutations must not leak raw transport/runtime output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12-01 | Integrity | `ApiClient.setEffort` | mitigate | Remove the unsupported method so Web cannot call a missing Hub route or imply runtime support. |
| T-01-12-02 | Tampering | `spawnSession` payload | mitigate | Serialize only supported launch fields; unsupported effort cannot be injected from Web form state. |
| T-01-12-03 | Spoofing | composer effort UI | mitigate | Preserve effort as read-only metadata; do not render controls that imply HAPI can change Cursor effort. |
| T-01-12-04 | Information Disclosure | runtime error UI | mitigate | No new error surface is added; existing model status uses localized safe reasons only. |
| T-01-12-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. If an executor adds one, stop for slopcheck and a blocking human checkpoint. |
</threat_model>

<verification>
Run focused Web tests for session actions, new-session spawn, composer display, and typecheck. Then run `bash scripts/check-no-cut-agents.sh`; run broader Web/root tests if local runner integration state allows.
</verification>

<success_criteria>
- Web cannot send unsupported effort at launch or in-session.
- Existing effort/modelReasoningEffort metadata remains visible near the composer only when session data contains it.
- CURS-02 no longer has a Web-facing effort mutation/persistence contract that diverges from Cursor runtime behavior.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Start sessions with selected model/effort truthfully | 01-12 | COVERED | Removes unsupported Web effort mutation so runtime metadata cannot lie.
REQ | CURS-02 | Selected model/effort is persisted only when real | 01-12 | COVERED | Web sends selected model only; effort display remains read-only.
RESEARCH | Pitfall 2 | Do not enable effort controls without runtime proof | 01-12 | COVERED | No effort controls or mutation callbacks remain.
CONTEXT | D-06 | Effort is runtime capability, not HAPI-owned promise | 01-12 | COVERED | Web no longer promises effort mutation.
CONTEXT | D-07 | Model/effort visible near composer | 01-12 | COVERED | Display-only effort metadata remains in composer/status path.
CONTEXT | D-12 | Runtime config feedback stays out of timeline | 01-12 | COVERED | No effort timeline events or mutation messages are added.
</source_audit>

<output>
Create `.planning/phases/01-cursor-runtime-config-contract/01-12-SUMMARY.md` when done.
</output>
