---
phase: 01-cursor-runtime-config-contract
plan: 09
type: execute
wave: 1
depends_on: ["01-06", "01-07"]
files_modified:
  - web/src/router.tsx
  - web/src/components/SessionChat.tsx
  - web/src/components/SessionChat.test.tsx
  - web/src/components/AssistantChat/HappyComposer.test.tsx
autonomous: true
gap_closure: true
requirements: [CURS-03]
user_setup: []
must_haves:
  truths:
    - "Live session composer receives discovered Cursor model options for the session machine."
    - "Live session composer opens the model selector when the runtime request path is available and the session is idle."
    - "Requesting a model change in-session reaches ApiClient.setModel and surfaces applied, pending, failed, or applies-next-run in composer status."
    - "Model switch feedback remains composer-local and does not create chat timeline messages."
  artifacts:
    - path: "web/src/router.tsx"
      provides: "SessionPage-owned Cursor model discovery for the selected session machine"
      contains: "useCursorModels"
    - path: "web/src/components/SessionChat.tsx"
      provides: "SessionChat-to-HappyComposer runtimeModelSwitchSupported and availableModelOptions props"
      contains: "runtimeModelSwitchSupported"
    - path: "web/src/components/SessionChat.test.tsx"
      provides: "Regression coverage for production SessionChat runtime switch props and applies-next-run path"
      contains: "runtimeModelSwitchSupported"
    - path: "web/src/components/AssistantChat/HappyComposer.test.tsx"
      provides: "Component coverage that discovered live options open the selector and unavailable copy is not shown"
      contains: "runtimeModelSwitchSupported"
  key_links:
    - from: "web/src/router.tsx"
      to: "web/src/hooks/useCursorModels.ts"
      via: "SessionPage uses session.metadata.machineId to discover Cursor models"
      pattern: "useCursorModels\\(api, .*machineId"
    - from: "web/src/router.tsx"
      to: "web/src/components/SessionChat.tsx"
      via: "SessionPage passes runtime switch support and model options"
      pattern: "runtimeModelSwitchSupported"
    - from: "web/src/components/SessionChat.tsx"
      to: "web/src/components/AssistantChat/HappyComposer.tsx"
      via: "HappyComposer receives support and options props"
      pattern: "availableModelOptions"
---

<objective>
Close UAT Test 3: the live session composer must use real Cursor model discovery and runtime request support so model switch requests reach the existing Hub/CLI config path and can show the truthful applies-next-run result.

Purpose: The backend already returns status-bearing CursorRuntimeConfigApplyResult values, including applies-next-run for Cursor model requests. The remaining gap is production Web wiring: SessionChat never supplies runtimeModelSwitchSupported or discovered availableModelOptions, so the composer gate closes before the request can run.

Output: Production SessionPage/SessionChat wiring plus focused Web tests proving live model options reach HappyComposer and model changes surface applies-next-run without timeline messages.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cursor-runtime-config-contract/01-UAT.md
@.planning/debug/test-3-runtime-switch.md
@.planning/phases/01-cursor-runtime-config-contract/01-06-SUMMARY.md
@.planning/phases/01-cursor-runtime-config-contract/01-07-SUMMARY.md
@web/src/router.tsx
@web/src/components/SessionChat.tsx
@web/src/components/AssistantChat/HappyComposer.tsx
@web/src/components/AssistantChat/useHappyComposerState.ts
@web/src/components/AssistantChat/modelOptions.ts
@web/src/hooks/useCursorModels.ts
@web/src/components/NewSession/index.tsx
@web/src/components/NewSession/ModelSelector.tsx

<interfaces>
Existing contracts to use directly:

- `useCursorModels(api, machineId, enabled)` returns `{ result, isLoading, error, retry, lastFetchedAt }`, where an ok result is `{ status: 'ok', models: CursorModelSummary[], discoveredAt: number }` and an error result is safe categorized data.
- `HappyComposerProps` already accepts `runtimeModelSwitchSupported?: boolean` and `availableModelOptions?: Array<{ value: string | null; label: string }>`.
- `useHappyComposerState` opens `canOpenModelSelector` only when `onModelChange`, `runtimeModelSwitchSupported`, non-empty `availableModelOptions`, idle state, and enabled controls are all true.
- `SessionChat` already owns `modelSwitchState`, calls `setModel(model)`, stores the returned status, and keeps switch feedback out of HappyThread/timeline state.
</interfaces>

<gap_source>
UAT Test 3 failed with "Switching unavailable for this runtime". Debug root cause: production SessionChat/router omit runtimeModelSwitchSupported and availableModelOptions, so useHappyComposerState defaults support to false and StatusBar renders the unavailable hint locally before ApiClient.setModel can reach the Hub/CLI applies-next-run path.
</gap_source>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing coverage for live composer runtime options</name>
  <files>web/src/components/SessionChat.test.tsx, web/src/components/AssistantChat/HappyComposer.test.tsx</files>
  <behavior>
    - SessionChat forwards runtimeModelSwitchSupported=true and discovered availableModelOptions into HappyComposer for a Cursor session with an ok discovery result.
    - SessionChat forwards runtimeModelSwitchSupported=false and an empty options list when discovery is unavailable or has no model ids.
    - HappyComposer opens the model selector for discovered raw Cursor model ids when runtimeModelSwitchSupported is true and the session is idle.
    - Clicking a discovered model option calls onModelChange and lets the existing SessionChat switch-state tests observe applies-next-run without adding chat timeline messages.
  </behavior>
  <action>Add RED tests before production edits. Extend the existing SessionChat HappyComposer mock so it captures new runtime props, then add expectations around supported ok discovery and unavailable discovery. Extend HappyComposer coverage to prove a discovered option passed through availableModelOptions opens the selector and no "Switching unavailable for this runtime" text appears in the supported idle path. Keep tests focused on props and observable UI behavior; do not rewrite useHappyComposerState gate semantics.</action>
  <verify>
    <automated>cd web && bun run test -- SessionChat HappyComposer</automated>
  </verify>
  <done>The targeted tests fail before implementation because production SessionChat does not provide runtime support/options and pass after Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire session-scoped Cursor discovery into SessionChat</name>
  <files>web/src/router.tsx, web/src/components/SessionChat.tsx, web/src/components/SessionChat.test.tsx</files>
  <behavior>
    - For a selected session with `session.metadata.machineId`, SessionPage discovers Cursor models through `useCursorModels(api, machineId, Boolean(machineId))`.
    - For `result.status === 'ok'` with one or more models, SessionPage builds live model options containing Auto as `{ value: null, label: t('newSession.model.autoUnspecified') }` plus each raw model id as `{ value: model.id, label: model.label && model.label !== model.id ? model.id + ' - ' + model.label : model.id }`.
    - SessionPage passes `runtimeModelSwitchSupported=true` only for the ok, non-empty discovery state; error, loading, no machine id, and empty model list pass false/empty options.
    - SessionChat forwards those props unchanged to HappyComposer, where the existing model switch handler surfaces the Hub result including applies-next-run.
  </behavior>
  <action>Implement the missing production wiring per Phase 01 decisions: keep raw Cursor model ids unconstrained, keep Auto as a selector sentinel that sends null/undefined rather than the literal "auto", and use the existing Hub apply-result contract as the source of truth. Add optional SessionChat props for `runtimeModelSwitchSupported` and `availableModelOptions`; thread them into HappyComposer. In router SessionPage, import and call `useCursorModels`, derive a memoized options array from the selected session machine id, and pass the support boolean/options to SessionChat. Do not infer support from static flavor helpers, do not add chat timeline messages, and do not parse raw CLI stderr in Web.</action>
  <verify>
    <automated>cd web && bun run test -- SessionChat HappyComposer useCursorModels NewSession</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <done>With a discovered model list, the live session composer opens the model selector, selecting a model calls the existing setModel path, and applies-next-run/failed/applied feedback renders in composer status instead of the timeline. With discovery unavailable, the selector remains closed and safe unavailable/error copy remains truthful.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub model discovery -> Web composer | Runtime-discovered model ids enter client UI and selector state. |
| Web composer -> Hub session config route | User-selected model id crosses from browser into authenticated Hub model mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09-01 | Tampering | `web/src/router.tsx` model option mapping | mitigate | Only use `CursorModelDiscoveryResult.status === 'ok'` model ids from the authenticated `useCursorModels` path; preserve Auto as `null` and do not synthesize arbitrary catalog values. |
| T-01-09-02 | Information Disclosure | Composer failure/status copy | mitigate | Continue rendering only localized safe failure reason copy from existing StatusBar paths; do not expose raw CLI stderr or HTTP response text. |
| T-01-09-03 | Repudiation | Composer switch feedback | accept | Switch state is transient UI feedback for a single-user local PWA; Hub mutation remains the authoritative state update path. |
| T-01-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. If an executor adds one, stop for slopcheck and a blocking human checkpoint. |
</threat_model>

<verification>
Run targeted Web tests first, then repo-level typecheck. If touched files remain within the planned set, run `bun run test` before summary.
</verification>

<success_criteria>
- UAT Test 3 can reach the backend model config mutation from the live session composer when Cursor model discovery succeeds.
- The composer model box displays one of the status-bearing states from CursorRuntimeConfigApplyResult, including applies-next-run.
- The reported "Switching unavailable for this runtime" message appears only when the runtime support/options gate is actually false.
- No model switch result is appended to the chat timeline.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Users can switch models in-session with truthful state | 01-09 | COVERED | Closes UAT Test 3 frontend gate.
REQ | CURS-03 | Request an in-session model switch and see applied/pending/failed/applies-next-run | 01-09 | COVERED | Uses existing Hub/CLI apply-result contract from 01-06.
RESEARCH | - | Runtime discovery is machine-scoped and safe-categorized | 01-09 | COVERED | Reuses `useCursorModels` and does not add a static catalog.
CONTEXT | Phase 01 decision | Use runtimeModelSwitchSupported as authoritative composer selector gate | 01-09 | COVERED | Supplies the missing production gate input.
CONTEXT | Phase 01 decision | Keep switch feedback in composer/status, not chat timeline | 01-09 | COVERED | Tests preserve no timeline additions.
</source_audit>

<output>
Create `.planning/phases/01-cursor-runtime-config-contract/01-09-SUMMARY.md` when done.
</output>
