---
phase: 01-cursor-runtime-config-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - hub/src/sync/rpcGateway.ts
  - hub/src/sync/syncEngineMachine.ts
  - hub/src/web/routes/machines.ts
  - hub/src/web/routes/machines.test.ts
  - web/src/api/client.ts
  - web/src/hooks/useCursorModels.ts
  - web/src/hooks/useCursorModels.test.ts
autonomous: true
requirements:
  - CURS-01
must_haves:
  truths:
    - "D-01: Authenticated Web clients can request machine-scoped Cursor model discovery from the local runtime path."
    - "D-02: Web has a short-cache hook that fetches discovery only when the new-session panel has an online machine."
    - "D-03: Hub and Web preserve safe discovery error results instead of leaking raw runtime output."
    - "D-08: Hub preserves selected runtime config rejection as a safe failure without retrying with auto."
    - "Hub routes discovery through SyncEngine and RpcGateway rather than shelling out directly."
  artifacts:
    - path: "hub/src/web/routes/machines.ts"
      provides: "GET /api/machines/:id/cursor/models"
    - path: "hub/src/sync/rpcGateway.ts"
      provides: "machine RPC bridge for `discover-cursor-models`"
    - path: "web/src/hooks/useCursorModels.ts"
      provides: "short-lived discovery cache hook"
    - path: "web/src/hooks/useCursorModels.test.ts"
      provides: "Discovery hook cache, retry, disabled, and safe-error coverage"
    - path: "web/src/api/client.ts"
      provides: "typed discovery client method"
  key_links:
    - from: "web/src/hooks/useCursorModels.ts"
      to: "GET /api/machines/:id/cursor/models"
      via: "ApiClient.getCursorModels(machineId)"
      pattern: "getCursorModels"
    - from: "hub/src/web/routes/machines.ts"
      to: "hub/src/sync/rpcGateway.ts"
      via: "SyncEngine machine discovery method"
      pattern: "discoverCursorModels"
---

<objective>
Expose Cursor model discovery through authenticated Hub and Web surfaces.

Purpose: The new-session UI needs a safe, machine-scoped request path that follows existing Web -> Hub -> CLI architecture.
Output: Hub discovery route, RPC bridge, Web API method, and short-cache hook.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
@.planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
@.planning/phases/01-cursor-runtime-config-contract/01-VALIDATION.md
@.planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
@hub/src/sync/rpcGateway.ts
@hub/src/sync/syncEngineMachine.ts
@hub/src/web/routes/machines.ts
@hub/src/web/routes/machines.test.ts
@web/src/api/client.ts
@web/src/hooks/useMachinePathsExists.ts
@web/src/hooks/useCursorModels.test.ts
@shared/src/schemas.ts

<interfaces>
From prior plans:
- CLI machine RPC method: `discover-cursor-models`
- Shared result: `CursorModelDiscoveryResult`

Existing route/API patterns:
- Machine routes use `requireMachine` and route through `SyncEngine`.
- API client methods call `this.request<T>(path, init?)`.
- Hook cancellation follows `useMachinePathsExists` with a local `cancelled` flag.
</interfaces>
</context>

<execution_preconditions>
- Before editing any existing function, class, or method named in this plan, run GitNexus `impact` with `repo: "hapi-cursor"`, `direction: "upstream"`, and the target symbol name. Record direct callers, affected processes, and risk level in the plan summary. If impact is HIGH or CRITICAL, stop and warn before editing.
</execution_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Hub discovery bridge and route</name>
  <files>hub/src/sync/rpcGateway.ts, hub/src/sync/syncEngineMachine.ts, hub/src/web/routes/machines.ts, hub/src/web/routes/machines.test.ts</files>
  <read_first>
    - hub/src/sync/rpcGateway.ts
    - hub/src/sync/syncEngineMachine.ts
    - hub/src/web/routes/machines.ts
    - hub/src/web/routes/machines.test.ts
    - hub/src/web/routes/guards.ts
    - hub/src/web/middleware/apiRouteError.ts
    - shared/src/schemas.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
    - .planning/phases/01-cursor-runtime-config-contract/01-VALIDATION.md
  </read_first>
  <behavior>
    - `GET /api/machines/:id/cursor/models` requires normal web auth and an online machine.
    - The route calls a SyncEngine method, which calls `RpcGateway.machineRpc(machineId, 'discover-cursor-models', {})`.
    - The response is parsed by `CursorModelDiscoveryResultSchema`.
    - Missing machine returns the existing machine guard error shape rather than attempting discovery.
    - Spawn responses with CLI code `selected-runtime-config-rejected` pass through Hub as a safe spawn failure for Web to localize.
  </behavior>
  <action>After GitNexus impact prechecks for `RpcGateway`, `SyncEngineMachine`, and the machine routes, add `discoverCursorModels(machineId: string)` to the Hub machine/sync layer and implement it through `RpcGateway` using the existing machine RPC timeout path. In `hub/src/web/routes/machines.ts`, add `GET /api/machines/:id/cursor/models` after machine lookup guards; return the parsed `CursorModelDiscoveryResult` directly. Use `ApiRouteError` with code `model-discovery-failed` only for transport/validation failures, not for normal `{ status: 'error' }` discovery results. In the existing spawn route/RPC response validation path, preserve CLI spawn errors with `code: 'selected-runtime-config-rejected'` and a safe message so Web can map launch rejection copy without seeing stderr. Do not convert this code to a generic 500, and do not retry with `model` omitted. Extend `hub/src/web/routes/machines.test.ts` with authenticated route tests for discovery success, safe error result, missing/offline machine, invalid RPC result, and selected runtime config rejection passing through as a safe spawn failure. This implements D-01, D-02, D-03, and D-08 without adding direct shell execution to Hub.</action>
  <verify>
    <automated>cd hub && bun test src/web/routes/machines.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `hub/src/web/routes/machines.ts` contains route path `/api/machines/:id/cursor/models` or route-local equivalent mounted under `/api/machines`.
    - Source assertion: Hub code does not import `node:child_process` for model discovery.
    - Test assertion: `cd hub && bun test src/web/routes/machines.test.ts` covers success and `{ status: 'error', reason: ... }` without returning HTTP 500.
    - Test assertion: invalid RPC result is rejected through the existing API error format.
    - Test assertion: spawn response with `code: 'selected-runtime-config-rejected'` reaches Web as a safe error and does not trigger an auto fallback.
  </acceptance_criteria>
  <done>Hub exposes authenticated machine-scoped Cursor model discovery via RPC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Web discovery client and short cache hook</name>
  <files>web/src/api/client.ts, web/src/hooks/useCursorModels.ts, web/src/hooks/useCursorModels.test.ts</files>
  <read_first>
    - web/src/api/client.ts
    - web/src/hooks/useMachinePathsExists.ts
    - web/src/hooks/useCursorModels.test.ts
    - shared/src/types.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
    - .planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
    - .planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
  </read_first>
  <behavior>
    - `ApiClient.getCursorModels(machineId)` fetches `/api/machines/{machineId}/cursor/models`.
    - `useCursorModels(api, machineId, enabled)` fetches only when `enabled === true` and `machineId` is non-null.
    - The hook exposes `result`, `isLoading`, `error`, `retry`, and `lastFetchedAt`.
    - The hook uses a short in-memory cache of 30000ms keyed by machine id.
  </behavior>
  <action>After the GitNexus impact precheck for `ApiClient`/`getCursorModels` and `useCursorModels`, add `getCursorModels(machineId: string): Promise<CursorModelDiscoveryResult>` to `web/src/api/client.ts`. Create `web/src/hooks/useCursorModels.ts` using `useState`, `useEffect`, and `useCallback`, following the cancellation pattern from `useMachinePathsExists`. Cache successful and error discovery results for 30000ms per machine id inside module-local state. Expose a `retry()` callback that bypasses the cache for the current machine. Do not fetch when the new-session panel is closed; represent that through the `enabled` argument. Add `web/src/hooks/useCursorModels.test.ts` with Vitest coverage for cache hit without a second API call, retry bypassing cache, disabled state making no API call and clearing transient loading, safe `{ status: 'error', reason }` result handling, and rejected API promise mapping to the hook `error` state without exposing raw response text. This implements D-02 and prepares the UI for safe selector states.</action>
  <verify>
    <automated>cd web && bun run test -- useCursorModels</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `web/src/api/client.ts` exports an instance method named `getCursorModels`.
    - Source assertion: `web/src/hooks/useCursorModels.ts` has a cache TTL constant set to `30000`.
    - Source assertion: the hook has an `enabled` parameter and does not call the API when `enabled` is false.
    - Test assertion: `web/src/hooks/useCursorModels.test.ts` covers cache hit, retry bypass, disabled state, safe discovery error result handling, and rejected request error handling.
  </acceptance_criteria>
  <done>Web can request Cursor model discovery only when the new-session panel is ready to show it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web to Hub REST | Authenticated mobile client requests machine runtime metadata. |
| Hub to CLI machine RPC | Hub forwards discovery to an online runner over Socket.IO. |
| Hub route to Web cache | Discovery errors are user-visible but must remain safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | `GET /api/machines/:id/cursor/models` | mitigate | Keep route behind existing web auth and machine guard; return only schema-safe result fields. |
| T-01-08 | Elevation | Hub discovery route | mitigate | Route through `SyncEngine` and machine RPC; do not add direct shell execution to Hub. |
| T-01-09 | Denial of Service | Web discovery hook | mitigate | Fetch only when enabled and cache results for 30000ms per machine id. |
| T-01-SC | Tampering | package installs | accept | No package-manager installs in this plan; package legitimacy audit says no install. |
</threat_model>

<verification>
Run `cd hub && bun test src/web/routes/machines.test.ts`, `cd web && bun run test -- useCursorModels`, and `bun run typecheck`.
</verification>

<success_criteria>
The new-session UI has a safe authenticated path to discover Cursor models from the selected online runner machine.
</success_criteria>

<output>
Create `.planning/phases/01-cursor-runtime-config-contract/01-03-SUMMARY.md` when done.
</output>
