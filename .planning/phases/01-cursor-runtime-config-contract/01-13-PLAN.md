---
phase: 01-cursor-runtime-config-contract
plan: 13
type: execute
wave: 4
depends_on: ["01-15"]
files_modified:
  - hub/src/store/index.ts
  - hub/src/store/types.ts
  - hub/src/store/sessions.ts
  - hub/src/store/sessionStore.ts
  - hub/src/sync/sessionRepository.ts
  - hub/src/sync/sessionLivenessService.ts
  - hub/src/sync/sessionLivenessService.test.ts
  - hub/src/sync/sessionModel.test.ts
  - shared/src/schemas.ts
  - shared/src/sessionSummary.ts
  - shared/src/sessionSummary.test.ts
  - web/src/hooks/useSSE.test.tsx
autonomous: true
gap_closure: true
requirements: [CURS-04]
user_setup: []
must_haves:
  truths:
    - "D-13: A completed unread turn remains a completed session-list status after /api/sessions refetch, reload, or reconnect."
    - "D-15: Completion read state remains local to Web, while Hub persists only the runtime completion marker."
    - "New work clears the durable completion marker so the row returns to thinking/running instead of stale green."
    - "Idle keepalive patches and authoritative summary rebuilds do not erase the latest unread completion marker."
  artifacts:
    - path: "hub/src/store/index.ts"
      provides: "SQLite schema version and durable session completion marker column"
      contains: "SCHEMA_VERSION"
    - path: "hub/src/store/sessions.ts"
      provides: "Store read/write helpers for durable turn-completion marker"
      contains: "turn_completion_marker"
    - path: "hub/src/sync/sessionLivenessService.ts"
      provides: "Persistence of ready-turn completion marker and clearing on new work"
      contains: "recordSessionActivity"
    - path: "shared/src/sessionSummary.ts"
      provides: "Authoritative summary derivation from durable completion marker"
      contains: "toSessionSummary"
  key_links:
    - from: "hub/src/sync/sessionLivenessService.ts"
      to: "hub/src/store/sessions.ts"
      via: "ready turn completion persists marker before emitting patch"
      pattern: "setSessionTurnCompletionMarker"
    - from: "hub/src/store/sessions.ts"
      to: "shared/src/sessionSummary.ts"
      via: "SessionRepository.refreshSession -> Session -> toSessionSummary"
      pattern: "turnCompletionMarker"
---

<objective>
Close the CURS-04 durability gap by persisting ready-turn completion markers and deriving session summaries from that durable state after refetch or reload.

Purpose: Plan 10 made live completion patches work, but verification found `/api/sessions` refetch rebuilds summaries from persisted sessions and loses completed/unread state for still-open Cursor sessions. Hub must store the runtime completion marker, while Web keeps viewed/read state local.
Output: SQLite/store marker field, session schema and repository plumbing, liveness persistence/clearing, summary derivation, and targeted shared/hub/web regression tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
@.planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
@.planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
@.planning/phases/01-cursor-runtime-config-contract/01-UI-SPEC.md
@.planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
@.planning/phases/01-cursor-runtime-config-contract/01-UAT.md
@.planning/phases/01-cursor-runtime-config-contract/01-08-SUMMARY.md
@.planning/phases/01-cursor-runtime-config-contract/01-10-SUMMARY.md
@hub/src/store/index.ts
@hub/src/store/types.ts
@hub/src/store/sessions.ts
@hub/src/store/sessionStore.ts
@hub/src/sync/sessionRepository.ts
@hub/src/sync/sessionLivenessService.ts
@shared/src/schemas.ts
@shared/src/sessionSummary.ts
@web/src/hooks/useSSE.ts

<interfaces>
Existing contracts to preserve:
- `SessionPatchSchema` already accepts strict optional `statusKind`, `completionMarker`, and `errorMarker`.
- `SessionSummary` already includes `statusKind`, `completionMarker`, and `errorMarker`.
- `SessionList` already keeps viewed/read completion state local by session id plus `completionMarker`.
- Store schema currently has `SCHEMA_VERSION = 10` and no migration path; per project rules, bump schema and let existing non-matching databases fail fast rather than adding runtime migrations.
</interfaces>

<preconditions>
Completed plan 01-10 established the live idle/completion status marker behavior this gap plan makes durable. 01-15 must be complete first per the targeted CURS-02 split dependency update; this CURS-04 plan has no file overlap with 01-15 but waits until the backend/CLI effort split is finalized.
</preconditions>

<gap_source>
01-VERIFICATION gap #2: `recordSessionActivity()` emits completionMarker/statusKind patches but persists only updatedAt; `toSessionSummary()` can reconstruct completed markers only from `endReason === 'completed'`, so authoritative `/api/sessions` refetch drops ready-turn completion state for still-active sessions.
</gap_source>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable turn-completion marker to sessions</name>
  <files>hub/src/store/index.ts, hub/src/store/types.ts, hub/src/store/sessions.ts, hub/src/store/sessionStore.ts, shared/src/schemas.ts, shared/src/sessionSummary.ts, shared/src/sessionSummary.test.ts</files>
  <read_first>
    - hub/src/store/index.ts
    - hub/src/store/types.ts
    - hub/src/store/sessions.ts
    - hub/src/store/sessionStore.ts
    - shared/src/schemas.ts
    - shared/src/sessionSummary.ts
    - shared/src/sessionSummary.test.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
    - .planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
  </read_first>
  <behavior>
    - New sessions have `turn_completion_marker` persisted as null by default.
    - Stored sessions expose a nullable marker such as `turnCompletionMarker`.
    - Shared `SessionSchema` exposes the same nullable marker, defaulting to null for absent data.
    - `toSessionSummary()` returns `statusKind: 'completed'` and `completionMarker` from the durable marker when no active thinking/waiting/background work supersedes it.
    - Completed/error end reasons still work as before.
  </behavior>
  <action>After GitNexus impact prechecks for store/session summary symbols, add a durable nullable per-session completion marker column to the sessions table, using a concrete name such as `turn_completion_marker INTEGER`. Bump `SCHEMA_VERSION` from 10 to 11 in `hub/src/store/index.ts`; do not add runtime migration logic because this repo intentionally rejects schema mismatches. Add `turnCompletionMarker: number | null` to `StoredSession`, `DbSessionRow`, `toStoredSession()`, insert defaults, and `SessionStore` wrapper methods for setting and clearing the marker. Add an optional/defaulted `turnCompletionMarker` field to `SessionSchema` so refreshed sessions carry durable state into shared summary derivation. Update `toSessionSummary()` precedence so thinking, waiting, and background work win first; then `endReason === 'error'`; then `session.turnCompletionMarker` or `endReason === 'completed'` yields `statusKind: 'completed'` and `completionMarker`. Add shared tests for durable marker summary derivation, active idle with marker returning completed, active thinking with marker returning thinking and preserving no completion marker in summary until work completes again, and existing ended completed/error behavior. This implements D-13 and D-15 without moving read/viewed state into Hub.</action>
  <verify>
    <automated>cd shared && bun test sessionSummary.test.ts schemas.test.ts</automated>
    <automated>cd hub && bun test src/store src/sync/sessionModel.test.ts</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `hub/src/store/index.ts` increments `SCHEMA_VERSION` and creates a nullable completion marker column in `sessions`.
    - Source assertion: `hub/src/store/sessions.ts` maps the new column into `StoredSession` and provides a setter/clearer.
    - Source assertion: `shared/src/schemas.ts` `SessionSchema` includes the durable marker as nullable/defaulted.
    - Test assertion: `shared/src/sessionSummary.test.ts` proves `/api/sessions` style summary derivation uses the durable marker.
    - Test assertion: active thinking/waiting/background work takes precedence over stale completion marker.
    - Typecheck assertion: `bun run typecheck` passes after schema and store type plumbing.
  </acceptance_criteria>
  <done>Persisted session rows can carry the latest unread turn-completion marker into authoritative summary rebuilds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and clear completion marker from liveness events</name>
  <files>hub/src/sync/sessionRepository.ts, hub/src/sync/sessionLivenessService.ts, hub/src/sync/sessionLivenessService.test.ts, hub/src/sync/sessionModel.test.ts, web/src/hooks/useSSE.test.tsx</files>
  <read_first>
    - hub/src/sync/sessionRepository.ts
    - hub/src/sync/sessionLivenessService.ts
    - hub/src/sync/sessionLivenessService.test.ts
    - hub/src/sync/sessionModel.test.ts
    - web/src/hooks/useSSE.ts
    - web/src/hooks/useSSE.test.tsx
    - shared/src/sessionSummary.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
    - .planning/phases/01-cursor-runtime-config-contract/01-UAT.md
  </read_first>
  <behavior>
    - `recordSessionActivity(..., { kind: 'turn-completed' })` writes the durable marker before emitting the completed patch.
    - `markMessageQueued()` clears the durable marker when new work starts and emits `completionMarker: null`.
    - `SessionRepository.refreshSession()` copies the durable marker from `StoredSession` into the in-memory `Session`.
    - A refetch-style path that refreshes a session after turn completion produces a summary with `statusKind: 'completed'` and the same marker.
    - Idle keepalives do not clear the marker.
  </behavior>
  <action>After GitNexus impact prechecks for `SessionRepository.refreshSession`, `SessionLivenessService.recordSessionActivity`, and `SessionLivenessService.markMessageQueued`, plumb the durable marker through the Hub session cache. In `refreshSession()`, set `session.turnCompletionMarker` from stored data. In `recordSessionActivity()` for `activity.kind === 'turn-completed'`, call the new store setter with `nextUpdatedAt`, update the in-memory session marker, then emit the existing strict patch with `statusKind: 'completed'` and `completionMarker`. In `markMessageQueued()`, clear the stored marker and in-memory marker before emitting the thinking patch with `completionMarker: null` and `errorMarker: null`. Do not clear the marker from `handleSessionAlive()` keepalive-only updates. Add Hub tests proving persistence on ready completion, clearing on queued message, refresh/refetch summary survival, and keepalive preservation. Add Web SSE regression coverage only as needed to prove patch behavior still preserves completion across keepalive and clears on new work; do not add malformed-event refetch fallback. This closes the second 01-VERIFICATION blocker.</action>
  <verify>
    <automated>cd hub && bun test src/sync/sessionLivenessService.test.ts src/sync/sessionModel.test.ts</automated>
    <automated>cd web && bun run test -- useSSE SessionListItem</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `recordSessionActivity` writes durable completion marker for `turn-completed`.
    - Source assertion: `markMessageQueued` clears durable completion marker when new work starts.
    - Source assertion: `handleSessionAlive` does not clear durable completion marker on idle keepalives.
    - Test assertion: Hub tests prove a refreshed session after ready completion derives completed summary from persisted marker.
    - Test assertion: Hub tests prove queued new work clears the persisted marker and emits `completionMarker: null`.
    - Test assertion: Web SSE tests still pass for completion marker preservation across keepalive and clearing on thinking patch.
  </acceptance_criteria>
  <done>Ready-turn completion status survives authoritative refetch/reload and clears when the next turn starts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI ready event to Hub liveness | Runtime ready activity becomes durable session attention state. |
| Hub store to shared summary | Persisted marker controls authoritative `/api/sessions` status after refetch. |
| Hub SSE to Web cache | Live completion marker patches and durable refetch state must converge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13-01 | Integrity | `turn_completion_marker` store field | mitigate | Persist only from typed `turn-completed` activity and clear on queued work; tests cover both transitions. |
| T-01-13-02 | Spoofing | ready activity classification | mitigate | Reuse the existing role-wrapped ready-event classifier from Plan 10; do not trust arbitrary text as a completion marker. |
| T-01-13-03 | Tampering | schema version bump | mitigate | Bump schema version and rely on the existing fail-fast mismatch policy; no unsafe runtime migration is introduced. |
| T-01-13-04 | Denial of Service | Web SSE/cache convergence | mitigate | Preserve strict patch parsing and avoid malformed-event refetch loops. |
| T-01-13-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. If an executor adds one, stop for slopcheck and a blocking human checkpoint. |
</threat_model>

<verification>
Run shared summary/schema tests, Hub liveness/session model tests, Web SSE/session-list tests, and root typecheck. If targeted checks pass, run `bun run madge:check` and `bash scripts/check-no-cut-agents.sh`; run `bun run test` if the known runner integration environment is healthy.
</verification>

<success_criteria>
- A normal completed Cursor turn persists a durable marker and `/api/sessions` summaries show completed/unread after refetch or reload.
- Opening/viewing remains local Web read state keyed by session id plus marker.
- New queued work clears the marker durably and live patches switch the row back to thinking/running.
- Idle keepalive and reconnect paths do not erase completion state.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Scan live status from the mobile session list | 01-13 | COVERED | Makes completion status survive authoritative refetch/reload.
REQ | CURS-04 | Session status updated live through strict patches and durable after refetch | 01-13 | COVERED | Adds durable completion marker plus strict live patch preservation.
RESEARCH | Pitfall 4 | Strict SSE patches must remain declared and convergent | 01-13 | COVERED | Reuses existing strict fields; no unknown-field fallback.
CONTEXT | D-13 | Completed/unread result shows green compact indicator | 01-13 | COVERED | Summary retains completed marker after refetch.
CONTEXT | D-15 | Completed state clears after viewed, later completed results unread again | 01-13 | COVERED | Hub persists runtime marker only; Web read state remains keyed by marker.
CONTEXT | D-16 | Pending states still collapse to one yellow dot | 01-13 | COVERED | Summary precedence keeps waiting/thinking over stale completion marker.
</source_audit>

<output>
Create `.planning/phases/01-cursor-runtime-config-contract/01-13-SUMMARY.md` when done.
</output>
