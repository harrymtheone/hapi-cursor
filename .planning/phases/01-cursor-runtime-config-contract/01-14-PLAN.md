---
phase: 01-cursor-runtime-config-contract
plan: 14
type: execute
wave: 2
depends_on: ["01-11"]
files_modified:
  - hub/src/sync/rpcGateway.ts
  - hub/src/sync/syncEngine.ts
  - hub/src/sync/syncEngineSession.ts
  - hub/src/sync/syncEngineSession.test.ts
  - hub/src/sync/sessionConfigService.ts
  - hub/src/sync/sessionConfigService.test.ts
  - hub/src/sync/sessionModel.test.ts
  - cli/src/cursor/runCursor.ts
  - cli/src/cursor/runCursor.test.ts
autonomous: true
gap_closure: true
requirements: [CURS-02]
user_setup: []
must_haves:
  truths:
    - "D-06: Active session-config RPC cannot apply or persist unsupported effort/modelReasoningEffort unless verified Cursor support exists."
    - "D-06: Inactive session-config persistence cannot store unsupported effort/modelReasoningEffort unless verified Cursor support exists."
    - "D-05: Model and permissionMode session-config behavior remains truthful and status-bearing."
  artifacts:
    - path: "cli/src/cursor/runCursor.ts"
      provides: "Runner set-session-config RPC handler that rejects or strips unsupported effort fields"
      contains: "set-session-config"
    - path: "hub/src/sync/syncEngineSession.ts"
      provides: "Active session-config apply gate that persists only acknowledged supported fields"
      contains: "applySessionConfig"
    - path: "hub/src/sync/sessionConfigService.ts"
      provides: "Inactive/cache session-config persistence gate without unsupported effort writes"
      contains: "SessionConfigService"
  key_links:
    - from: "hub/src/web/routes/sessions/config.ts"
      to: "hub/src/sync/sessionConfigService.ts"
      via: "SyncEngine.applySessionConfig -> SyncEngineSession.applySessionConfig -> SessionConfigService.applySessionConfig"
      pattern: "applySessionConfig"
    - from: "hub/src/sync/syncEngineSession.ts"
      to: "cli/src/cursor/runCursor.ts"
      via: "RpcGateway.sessionRpc('set-session-config')"
      pattern: "set-session-config"
---

<objective>
Close the session-config RPC/apply/persistence slice of the CURS-02 effort gap by preventing unsupported effort fields from being applied or stored on active and inactive sessions.

Purpose: 01-11 blocks unsupported effort at spawn creation. This plan closes the remaining session-config path so later Web work can remove the unsupported effort UI without leaving a backend persistence path that still claims unsupported runtime state.
Output: Runner session-config RPC rejects or strips unsupported effort fields, Hub active apply persists only supported acknowledged fields, inactive persistence ignores or rejects unsupported effort, and focused tests cover both paths.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cursor-runtime-config-contract/01-CONTEXT.md
@.planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
@.planning/phases/01-cursor-runtime-config-contract/01-PATTERNS.md
@.planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
@.planning/phases/01-cursor-runtime-config-contract/01-11-PLAN.md
@hub/src/sync/rpcGateway.ts
@hub/src/sync/syncEngine.ts
@hub/src/sync/syncEngineSession.ts
@hub/src/sync/sessionConfigService.ts
@cli/src/cursor/runCursor.ts

<interfaces>
Existing runtime facts:
- `runCursor` registers a `set-session-config` RPC handler; unsupported effort/modelReasoningEffort must be rejected or stripped there unless Cursor support is verified.
- `SyncEngineSession.applySessionConfig()` handles active sessions through session RPC and inactive sessions through cached persistence.
- `SessionConfigService.applySessionConfig()` currently persists model/effort fields when provided; after this plan it must persist model and permissionMode only for current verified support.
- Model and permissionMode apply results are status-bearing and must remain unchanged.
</interfaces>

<preconditions>
01-11 must be complete first so the spawn boundary has already stopped creating new divergent runtime config. This plan intentionally owns only session-config RPC/apply/persistence files and remains below the 15-file scope threshold.
</preconditions>

<gap_source>
01-VERIFICATION gap #1: Effort/modelReasoningEffort are accepted and persisted through API/session metadata paths but are not passed to the spawned Cursor process. This segment covers active and inactive session-config paths after session creation.
</gap_source>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject unsupported effort in active session-config RPC</name>
  <files>hub/src/sync/rpcGateway.ts, hub/src/sync/syncEngine.ts, hub/src/sync/syncEngineSession.ts, hub/src/sync/syncEngineSession.test.ts, cli/src/cursor/runCursor.ts, cli/src/cursor/runCursor.test.ts</files>
  <read_first>
    - hub/src/sync/rpcGateway.ts
    - hub/src/sync/syncEngine.ts
    - hub/src/sync/syncEngineSession.ts
    - hub/src/sync/syncEngineSession.test.ts
    - cli/src/cursor/runCursor.ts
    - cli/src/cursor/runCursor.test.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
    - .planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
  </read_first>
  <behavior>
    - `runCursor` `set-session-config` rejects or strips payload fields named `effort` and `modelReasoningEffort` unless verified Cursor runtime support exists.
    - `SyncEngineSession.applySessionConfig()` does not persist unsupported effort/modelReasoningEffort from active-session apply results.
    - Active model and permissionMode apply behavior remains status-bearing and unchanged.
  </behavior>
  <action>After GitNexus impact prechecks for `runCursor` `set-session-config`, `RpcGateway.sessionRpc`, `SyncEngine.applySessionConfig`, and `SyncEngineSession.applySessionConfig`, close the active session-config path called out by the checker. With current Cursor support, unsupported `effort` and `modelReasoningEffort` must either produce a safe unsupported result at the runner RPC boundary or be stripped before acknowledgement; they must not be included in any applied/applies-next-run persistence payload. Ensure `SyncEngineSession.applySessionConfig()` only calls session cache/store apply with acknowledged supported fields such as `model` and `permissionMode`. Update active-session tests that currently expect effort persistence so they instead prove unsupported effort/modelReasoningEffort are not written while model set/clear and permissionMode behavior remain covered. This implements D-06 for active sessions.</action>
  <verify>
    <automated>cd hub && bun test src/sync/syncEngineSession.test.ts src/sync/sessionModel.test.ts</automated>
    <automated>cd cli && bun run test -- runCursor</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `cli/src/cursor/runCursor.ts` `set-session-config` does not acknowledge unsupported effort/modelReasoningEffort as applied runtime config.
    - Source assertion: `hub/src/sync/syncEngineSession.ts` does not call session persistence with unsupported effort/modelReasoningEffort.
    - Test assertion: active session-config apply with effort/modelReasoningEffort does not update cache or store.
    - Test assertion: model set/clear and permissionMode apply paths still pass.
  </acceptance_criteria>
  <done>Unsupported effort/modelReasoningEffort cannot persist through the active session-config RPC/apply path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strip unsupported effort from inactive persistence</name>
  <files>hub/src/sync/syncEngine.ts, hub/src/sync/syncEngineSession.ts, hub/src/sync/syncEngineSession.test.ts, hub/src/sync/sessionConfigService.ts, hub/src/sync/sessionConfigService.test.ts, hub/src/sync/sessionModel.test.ts</files>
  <read_first>
    - hub/src/sync/syncEngine.ts
    - hub/src/sync/syncEngineSession.ts
    - hub/src/sync/syncEngineSession.test.ts
    - hub/src/sync/sessionConfigService.ts
    - hub/src/sync/sessionConfigService.test.ts
    - hub/src/sync/sessionModel.test.ts
    - .planning/phases/01-cursor-runtime-config-contract/01-VERIFICATION.md
    - .planning/phases/01-cursor-runtime-config-contract/01-RESEARCH.md
  </read_first>
  <behavior>
    - `SessionConfigService.applySessionConfig()` ignores or rejects unsupported effort/modelReasoningEffort before store writes and event publishing.
    - Inactive/cache-only session-config apply cannot create stored effort divergence.
    - Model and permissionMode persistence for inactive sessions remains intact.
  </behavior>
  <action>After GitNexus impact prechecks for `SessionConfigService.applySessionConfig`, `SyncEngineSession.applySessionConfig`, and `SyncEngine.applySessionConfig`, close the inactive/session-cache branch. In `SessionConfigService.applySessionConfig()`, reject or strip `effort` and `modelReasoningEffort` before store writes and event publishing under current unverified Cursor support. Keep existing supported fields, especially `model` and `permissionMode`, using the same status-bearing semantics already established by prior phase plans. Update service and session-model tests so inactive/cache-only apply with unsupported effort/modelReasoningEffort leaves stored session config unchanged for those fields, while model clearing/setting and permissionMode still persist as before. This implements D-06 for inactive sessions and preserves D-05 auto/unspecified truth.</action>
  <verify>
    <automated>cd hub && bun test src/sync/sessionConfigService.test.ts src/sync/syncEngineSession.test.ts src/sync/sessionModel.test.ts</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `hub/src/sync/sessionConfigService.ts` does not persist unsupported effort/modelReasoningEffort to the store.
    - Test assertion: inactive/cache-only session-config apply with effort/modelReasoningEffort does not update cache or store.
    - Test assertion: model set/clear and permissionMode persistence still pass.
    - Typecheck assertion: `bun run typecheck` catches no stale unsupported effort persistence references.
  </acceptance_criteria>
  <done>Unsupported effort/modelReasoningEffort cannot be stored through inactive session-config persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub to CLI session RPC | Runtime config payload crosses from Hub into the runner process for active sessions. |
| Hub session-config service to SQLite store | User-requested runtime metadata becomes durable session state. |
| Hub event publisher to Web SSE | Accepted config state becomes user-visible metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14-01 | Tampering | `runCursor` `set-session-config` | mitigate | Reject or strip unsupported effort fields before they can be acknowledged as runtime-applied config. |
| T-01-14-02 | Integrity | `SyncEngineSession.applySessionConfig` | mitigate | Persist only acknowledged supported fields from active session-config results. |
| T-01-14-03 | Integrity | `SessionConfigService.applySessionConfig` | mitigate | Reject or strip unsupported effort/modelReasoningEffort before store writes and event publishing. |
| T-01-14-04 | Repudiation | runtime config apply result | mitigate | Preserve status-bearing model/permissionMode results so unsupported effort is not disguised as a successful runtime change. |
| T-01-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. If an executor adds one, stop for slopcheck and a blocking human checkpoint. |
</threat_model>

<verification>
Run focused Hub session-config tests, CLI runCursor tests, and root typecheck. If targeted checks pass, run `bash scripts/check-no-cut-agents.sh`; CLI launch argument coverage belongs to 01-15.
</verification>

<success_criteria>
- Active session-config requests cannot persist unsupported effort/modelReasoningEffort.
- Inactive session-config requests cannot persist unsupported effort/modelReasoningEffort.
- Supported model and permissionMode session-config behavior remains unchanged and tested.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Start sessions with selected model/effort truthfully | 01-14 | COVERED | Prevents post-spawn session-config persistence from claiming unsupported effort runtime state.
REQ | CURS-02 | Selected model/effort launch and session metadata does not diverge from runtime | 01-14 | COVERED | Active and inactive session-config paths cannot store unsupported effort.
RESEARCH | Pitfall 2 | Do not enable effort controls without runtime proof | 01-14 | COVERED | No session-config effort support is invented.
CONTEXT | D-05 | Default runtime config is auto/unspecified | 01-14 | COVERED | Unsupported effort remains omitted unless explicit verified support exists.
CONTEXT | D-06 | Effort is runtime capability, not HAPI-owned promise | 01-14 | COVERED | Unsupported effort rejected or stripped before session-config persistence.
</source_audit>

<output>
Create `.planning/phases/01-cursor-runtime-config-contract/01-14-SUMMARY.md` when done.
</output>
