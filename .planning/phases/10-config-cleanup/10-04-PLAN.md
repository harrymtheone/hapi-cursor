---
phase: 10-config-cleanup
plan: 04
type: execute
wave: 4
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - cli/src/lib.ts
  - hub/src/store/index.ts
  - hub/src/store/index.test.ts
  - scripts/check-no-cut-agents.sh
autonomous: true
requirements: [REFC-01, REFC-02]

must_haves:
  truths:
    - "`cli/src/lib.ts` exports `loadConfig` + `type Config` instead of `configuration`; downstream lib consumers must call `await loadConfig()` (D-166 — public-library breaking change documented in the file's JSDoc)"
    - "`hub/src/store/index.ts` schema-mismatch error message contains: the DB path, the expected `SCHEMA_VERSION`, the found version, and explicit instructions to back up the DB and run the relevant offline migration tool or rebuild (D-173)"
    - "Phase 10 explicitly evaluates D-174: a written decision in `10-04-SUMMARY.md` records whether `SCHEMA_VERSION` was bumped this phase (and why) — Phase 10's CLI/Hub config refactor by itself does not change persisted JSON shape, so the default expected outcome is `no bump`; if any stricter persisted-data decoding slipped in via Plan 02/03 the executor must bump and document it"
    - "Runtime `migration-v*.ts` files: zero present in `hub/src` (already true; guard #4 confirms)"
    - "`scripts/check-no-cut-agents.sh` Phase-10 block has ALL five sub-checks active: (#1) legacy field reads `serverUrl|webapp(Host|Port|Url|Origin)`, (#2) mutable setters `_setApiUrl|_setCliApiToken|_setExtraHeaders`, (#3) `name: 'server'` alias, (#4) runtime `migration-v*.ts` files, (#5) singleton imports `getConfiguration\\(\\)` / `from '@/configuration'` / `from '../configuration'`. The block terminates with `echo \"✅ Phase 10 guard PASS.\"`"
    - "`bash scripts/check-no-cut-agents.sh` exits 0 with the Phase-10 PASS line"
    - "`bun typecheck && bun run test` both green; ROADMAP Phase 10 success criteria 1-4 all satisfied"
  artifacts:
    - path: "cli/src/lib.ts"
      provides: "Public-library surface: exports loadConfig + Config type; no `configuration` re-export"
      contains: "loadConfig"
    - path: "hub/src/store/index.ts"
      provides: "Schema-mismatch error matching D-173 wording (DB path, expected version, found version, repair guidance)"
      contains: "SCHEMA_VERSION"
    - path: "hub/src/store/index.test.ts"
      provides: "Tests asserting D-173 error wording (DB path + version numbers + offline-tool/rebuild guidance substring)"
    - path: "scripts/check-no-cut-agents.sh"
      provides: "Phase-10 guard block with all 5 sub-checks active and Phase-10 PASS line"
      contains: "Phase 10 guard PASS"
  key_links:
    - from: "cli/src/lib.ts"
      to: "cli/src/configuration.ts::loadConfig"
      via: "named re-export"
      pattern: "export \\{ loadConfig"
    - from: "hub/src/store/index.ts (mismatch error)"
      to: "hub/src/store/index.test.ts (assertion on substrings)"
      via: "error message contains DB path + expected + found + 'offline migration' / 'rebuild'"
      pattern: "expected.*found|offline migration"
    - from: "scripts/check-no-cut-agents.sh Phase-10 block"
      to: "all REFC-01 + REFC-02 success criteria"
      via: "ripgrep sweeps over cli/src + hub/src for legacy fields, setters, alias, migration-v*, singleton imports"
      pattern: "Phase 10"
---

<objective>
Slice 4 of Phase 10: close the residual loose ends after the CLI + Hub cutovers (Plans 02 + 03). Swap the `cli/src/lib.ts` public-library export from the singleton to `loadConfig` + `Config` (D-166, Pitfall 3 — breaking change). Verify and harden the Hub `Store` schema-mismatch error message per D-173 (DB path + expected + found + repair guidance). Make a written D-174 schema-version decision. Flip the staged Phase-10 guard sub-checks from comments to active and run the final guard sweep. The phase ends green per the 4 ROADMAP success criteria.

Purpose: REFC-01 (final guard enforcement of legacy-residue zero-tolerance + SQLite mismatch behavior verification) + REFC-02 (public-library surface migration). This is the cleanup-and-lockdown slice.

Output: `lib.ts` rewritten; `Store` mismatch error message guaranteed by tests; `SCHEMA_VERSION` decision recorded; all five Phase-10 guard sub-checks live; full `bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh` green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-config-cleanup/10-CONTEXT.md
@.planning/phases/10-config-cleanup/10-RESEARCH.md
@.planning/phases/10-config-cleanup/10-PATTERNS.md
@.planning/phases/10-config-cleanup/10-VALIDATION.md
@.planning/phases/10-config-cleanup/10-01-SUMMARY.md
@.planning/phases/10-config-cleanup/10-02-SUMMARY.md
@.planning/phases/10-config-cleanup/10-03-SUMMARY.md
@cli/src/lib.ts
@cli/src/configuration.ts
@hub/src/store/index.ts
@hub/src/store/index.test.ts
@scripts/check-no-cut-agents.sh

<interfaces>
<!-- Contracts the executor needs. -->

`cli/src/lib.ts` current shape (file is the public library surface — comment says "dev-environment cli helper programs"):

```typescript
export { ApiClient } from '@/api/api'
export { ApiSessionClient } from '@/api/apiSession'
export { logger } from '@/ui/logger'
export { configuration } from '@/configuration'   // ← REMOVE this line
export { RawJSONLinesSchema, type RawJSONLines } from '@/agent/agentLogSchema'
```

Target shape per PATTERNS §"cli/src/lib.ts":

```typescript
export { ApiClient } from '@/api/api'
export { ApiSessionClient } from '@/api/apiSession'
export { logger } from '@/ui/logger'
export { loadConfig, type Config } from '@/configuration'   // ← REPLACE
export { RawJSONLinesSchema, type RawJSONLines } from '@/agent/agentLogSchema'
```

`hub/src/store/index.ts::Store` constructor (`initSchema`) already throws on schema-version mismatch. D-173 spells out exactly what the message MUST contain: DB path, expected `SCHEMA_VERSION`, found version, and a concise instruction to "back up, then run the relevant offline migration tool or rebuild the database". The Plan 01 test (`hub/src/store/index.test.ts`) asserts the path + expected + found; Plan 04 must extend it to also assert the repair-guidance substring (e.g. `expect(err.message).toMatch(/offline migration|rebuild/i)`).

`scripts/check-no-cut-agents.sh` Phase-10 block was seeded by Plan 01 with sub-checks #3 (`name: 'server'`) and #4 (`migration-v*.ts`) ACTIVE, and #1 / #2 / #5 staged as block comments labelled `# Plan 04: enable after CLI/Hub DI cutover`. Plan 04 simply un-comments those three sub-checks and verifies the sweep passes against the now-cutover codebase.

Reference patterns for the three staged sub-checks (from RESEARCH §"Code Examples"):

```bash
# (#1) Legacy field reads — serverUrl + webapp*
if "$RG_BIN" -n --glob '!**/*.test.ts' --glob '!**/serverSettings.ts' \
    '\bserverUrl\b|\bwebapp(Host|Port|Url|Origin)\b' "${PHASE10_SOURCE_DIRS[@]}"; then
  echo "❌ Phase 10 D-160: legacy config field read in production source."
  exit 1
fi

# (#2) Mutable setters
if "$RG_BIN" -n --glob '!**/*.test.ts' '_setApiUrl|_setCliApiToken|_setExtraHeaders' "${PHASE10_SOURCE_DIRS[@]}"; then
  echo "❌ Phase 10 D-164/D-168: mutable config setter found in production source."
  exit 1
fi

# (#5) Singleton imports — getConfiguration() / configuration module imports outside configuration.ts itself
PHASE10_SINGLETON=$("$RG_BIN" -n "getConfiguration\\(\\)|from ['\"]@/configuration['\"]|from ['\"]\\.\\./configuration['\"]" \
  "${PHASE10_SOURCE_DIRS[@]}" --glob '!**/*.test.ts' --glob '!cli/src/configuration.ts' --glob '!hub/src/configuration.ts' \
  --glob '!cli/src/lib.ts' --glob '!cli/src/commands/types.ts' || true)
if [ -n "$PHASE10_SINGLETON" ]; then
  echo "$PHASE10_SINGLETON"
  echo "❌ Phase 10 D-166/D-171: production code still imports the config singleton."
  exit 1
fi
```

Note for sub-check #5: `cli/src/lib.ts` and `cli/src/commands/types.ts` are the legitimate `from '@/configuration'` re-exporters/importers (lib re-exports `loadConfig` + type; `commands/types.ts` imports `type Config`). They are whitelisted via `--glob` so the sweep does NOT false-positive after this slice.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap `cli/src/lib.ts` public-library export</name>
  <files>cli/src/lib.ts</files>
  <read_first>
    - cli/src/lib.ts (current 5-line export list)
    - cli/src/configuration.ts (verify `loadConfig` and `Config` are exported after Plan 03)
    - .planning/phases/10-config-cleanup/10-RESEARCH.md (§Pitfall 3 — public-library breaking change rationale)
    - .planning/phases/10-config-cleanup/10-PATTERNS.md (§"cli/src/lib.ts — library public surface")
  </read_first>
  <behavior>
    - `cli/src/lib.ts` no longer exports `configuration`. It exports `loadConfig` and `type Config` from `@/configuration`. The file's existing JSDoc comment gains one line documenting the breaking change: "Note: the previous `configuration` singleton was removed in Phase 10; consumers must now call `await loadConfig()` and pass the resulting `Config` explicitly."
    - All other existing exports (`ApiClient`, `ApiSessionClient`, `logger`, `RawJSONLinesSchema`, `RawJSONLines`) are unchanged.
    - `cd cli && bun typecheck` and `bun run test` remain green (no production source imports `configuration` from `@/lib` — the production code path was already cutover in Plan 03; this slice only addresses the library re-export surface).
  </behavior>
  <action>Edit `cli/src/lib.ts`: replace the line `export { configuration } from '@/configuration'` with `export { loadConfig, type Config } from '@/configuration'`. Add a single-line JSDoc note above the imports describing the breaking change (D-166, RESEARCH Pitfall 3). Do not change any other export. Run `cd cli && bun typecheck` and `bun run test:cli` as a sanity check.</action>
  <verify>
    <automated>cd cli &amp;&amp; bun typecheck &amp;&amp; bun run test:cli</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "export \\{ configuration \\}" cli/src/lib.ts` returns zero hits.
    - `rg -n "export \\{ loadConfig" cli/src/lib.ts` returns exactly one hit and that line also exports `type Config`.
    - `rg -n "type Config" cli/src/lib.ts` returns at least one hit.
    - `cd cli && bun typecheck` is green.
    - `cd cli && bun run test:cli` is green.
  </acceptance_criteria>
  <done>Public-library surface migrated; consumers see `loadConfig` + `Config` only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden Store schema-mismatch error message per D-173 + evaluate D-174 schema-version bump</name>
  <files>hub/src/store/index.ts, hub/src/store/index.test.ts</files>
  <read_first>
    - hub/src/store/index.ts (current `initSchema` body + the `throw` paths + the `SCHEMA_VERSION` constant)
    - hub/src/store/index.test.ts (the schema-mismatch + missing-table tests added in Plan 01)
    - hub/scripts/migrate-namespace-isolation.ts (the surviving "explicitly named offline migration tool" per D-172 — its filename / path is what the error message should reference)
    - .planning/phases/10-config-cleanup/10-CONTEXT.md (D-173, D-174)
    - .planning/phases/10-config-cleanup/10-RESEARCH.md (§"Runtime State Inventory" — `SCHEMA_VERSION` currently = 10)
    - The full diff of Plans 02 and 03 (especially `hub/src/store/messages.ts`, any zod schemas under `hub/src/store/` or `shared/src/wire/`) — search for any change that tightens persisted-JSON decoding versus the prior state, because D-174 says any such change requires a `SCHEMA_VERSION` bump
  </read_first>
  <behavior>
    - When `initSchema` detects `PRAGMA user_version !== SCHEMA_VERSION`, the thrown Error message contains: the DB path, the expected version (the current `SCHEMA_VERSION` numeric value), the found version (the numeric value read from PRAGMA), AND a substring matching `/offline migration|rebuild/i` that points users at the explicit offline-migration tool path (`hub/scripts/migrate-namespace-isolation.ts` or "rebuild the database"). The message is concise (single error, not a multi-paragraph manual).
    - When a required table is missing, the existing message naming the table is preserved (no D-173 wording change required for that path).
    - `hub/src/store/index.test.ts` is extended to assert the new D-173 substring in the schema-mismatch test (`expect(err.message).toMatch(/offline migration|rebuild/i)`). The DB-path + expected + found assertions from Plan 01 are kept.
    - D-174 evaluation: the executor reviews the diffs introduced by Plans 02 + 03 for any change that tightens persisted-JSON decoding (e.g. stricter zod schemas that would reject previously-accepted persisted message metadata). If none, NO `SCHEMA_VERSION` bump is performed and `10-04-SUMMARY.md` records the explicit `no bump — Phase 10 refactor is config-only; persisted message/wire decoders unchanged` decision. If a stricter decoder was introduced, the executor bumps `SCHEMA_VERSION` (e.g. 10 → 11) and `10-04-SUMMARY.md` records the rationale + which decoder/schema motivated the bump. Either way, the decision is documented.
  </behavior>
  <action>If `hub/src/store/index.ts::initSchema` schema-mismatch throw does not yet include a `/offline migration|rebuild/i` substring, modify the `throw new Error(...)` to read along the lines of `\`SQLite schema version mismatch at ${dbPath}: expected ${SCHEMA_VERSION}, found ${userVersion}. Back up the file, then run the offline migration tool at hub/scripts/migrate-namespace-isolation.ts or rebuild the database.\`` per D-173 wording in PATTERNS / RESEARCH. Extend `hub/src/store/index.test.ts` schema-mismatch case to add `expect(err.message).toMatch(/offline migration|rebuild/i)` AFTER the existing path/version assertions. D-174 step: search the diffs of Plans 02 + 03 for any tightening of persisted-data decoders — practical search: `git diff <plan-02-commit>..HEAD -- hub/src/store/ shared/src/wire/` and `rg -n "z\\.object|z\\.discriminatedUnion|safeParse|\\.parse\\(" hub/src/store/ shared/src/wire/` against pre-Phase-10 state. If a stricter decoder was introduced, bump `SCHEMA_VERSION` from 10 to 11 in `hub/src/store/index.ts` and document the rationale in `10-04-SUMMARY.md`. If no such change, leave `SCHEMA_VERSION` at 10 and document the `no bump` decision in `10-04-SUMMARY.md`. In either case, `bun run test:hub` must remain green.</action>
  <verify>
    <automated>cd hub &amp;&amp; bun test src/store/index.test.ts -t "schema mismatch" &amp;&amp; bun typecheck &amp;&amp; bun run test:hub</automated>
  </verify>
  <acceptance_criteria>
    - `cd hub && bun test src/store/index.test.ts -t "schema mismatch"` exits 0 and asserts the D-173 substring.
    - `rg -n "offline migration|rebuild" hub/src/store/index.ts` returns at least one hit (proof the error message now references repair guidance).
    - `rg -n "match\\(/offline migration\\|rebuild/i\\)|toMatch\\(/offline migration\\|rebuild/i\\)" hub/src/store/index.test.ts` returns at least one hit (proof the test asserts on the repair substring).
    - `cd hub && bun typecheck && bun run test:hub` is green.
    - The D-174 schema-version decision (bump or no-bump + rationale) is captured in the eventual `10-04-SUMMARY.md` (this acceptance criterion is the SUMMARY itself; the planner records the requirement here and the executor produces it at task close).
  </acceptance_criteria>
  <done>Store schema-mismatch error message satisfies D-173 wording requirements with test coverage; D-174 schema-version decision is explicitly evaluated and recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Enable staged Phase-10 guard sub-checks + final phase-gate run</name>
  <files>scripts/check-no-cut-agents.sh</files>
  <read_first>
    - scripts/check-no-cut-agents.sh (the Phase-10 block — Plan 01 seeded sub-checks #3 + #4 active and #1 + #2 + #5 as commented stubs labelled `# Plan 04: enable after CLI/Hub DI cutover`)
    - .planning/phases/10-config-cleanup/10-RESEARCH.md (§"Code Examples" — full Phase-10 guard block reference)
    - .planning/phases/10-config-cleanup/10-PATTERNS.md (§"scripts/check-no-cut-agents.sh — Phase-10 guard block")
    - cli/src/lib.ts and cli/src/commands/types.ts (the two legitimate `from '@/configuration'` import sites that sub-check #5 must whitelist via `--glob`)
  </read_first>
  <behavior>
    - The Phase-10 block in `scripts/check-no-cut-agents.sh` has ALL five sub-checks active:
      - #1 rejects `\bserverUrl\b|\bwebapp(Host|Port|Url|Origin)\b` reads under `cli/src hub/src` excluding `**/*.test.ts` and the legitimate `hub/src/config/serverSettings.ts` (which intentionally lists the field names in its `OLD_SETTINGS_FIELDS` array).
      - #2 rejects `_setApiUrl|_setCliApiToken|_setExtraHeaders` under `cli/src hub/src` excluding `**/*.test.ts`.
      - #3 (already active from Plan 01) rejects `name:\s*['\"]server['\"]` in `cli/src/commands/registry.ts`.
      - #4 (already active from Plan 01) rejects any `hub/src/**/migration-v*.ts` file.
      - #5 rejects `getConfiguration\\(\\)` / `from '@/configuration'` / `from '../configuration'` outside the legitimate sites (`cli/src/configuration.ts`, `hub/src/configuration.ts`, `cli/src/lib.ts`, `cli/src/commands/types.ts`, and any `**/*.test.ts`).
    - The block terminates with `echo "✅ Phase 10 guard PASS."` and `bash scripts/check-no-cut-agents.sh` exits 0 against the now-cutover repo state.
    - `bun typecheck && bun run test` (full repo) is green; ROADMAP Phase 10 success criteria 1-4 are all satisfied by the new ripgrep / test state.
  </behavior>
  <action>Open `scripts/check-no-cut-agents.sh`, locate the Phase-10 block, and un-comment the three staged sub-checks (#1 legacy fields, #2 mutable setters, #5 singleton imports) using the patterns shown in the interfaces block above. Make sure sub-check #5 whitelists `cli/src/lib.ts` AND `cli/src/commands/types.ts` via additional `--glob '!cli/src/lib.ts' --glob '!cli/src/commands/types.ts'` arguments so the legitimate `loadConfig`/`type Config` re-export/import is not flagged. Remove the `# Plan 04: enable after CLI/Hub DI cutover` TODO comments. Run `bash scripts/check-no-cut-agents.sh` and address any false positive by tightening the glob (NOT by adding production exclusions — any genuine production hit means Plan 02 or Plan 03 missed a callsite and must be fixed in this slice, not whitelisted away). Run `bun typecheck && bun run test` as the final phase gate.</action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh &amp;&amp; bun typecheck &amp;&amp; bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-no-cut-agents.sh` exits 0 and stdout contains the literal line `✅ Phase 10 guard PASS.`.
    - `rg -n "Plan 04: enable" scripts/check-no-cut-agents.sh` returns zero hits (TODO comments removed; checks now active).
    - All five Phase-10 sub-check error messages appear in the script's source: `rg -nc 'Phase 10 D-' scripts/check-no-cut-agents.sh` returns a count of at least 5 (one per sub-check) — proof every sub-check is present with its D-number reference.
    - `bun typecheck` is green across the repo.
    - `bun run test` (full repo) is green.
    - The four ROADMAP Phase 10 success criteria are now demonstrably satisfied: SC1 by ripgrep sweeps in #1 / #2 / #3 / #5, SC2 by Store mismatch behavior + guard #4 (already true), SC3 by `loadConfig` + DI cutover from Plans 02 + 03 + the singleton-import guard #5, SC4 by `bun typecheck && bun run test` green + the frozen-mutation tests landed in Plans 02 + 03.
  </acceptance_criteria>
  <done>Phase-10 guard block fully active; `bash scripts/check-no-cut-agents.sh` and `bun run test` both green; the ROADMAP Phase 10 success criteria 1-4 are satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External library consumers → `cli/src/lib.ts` exports | Breaking-change boundary: the previous singleton export is replaced; consumers see `loadConfig` + `Config`. |
| operator → SQLite file on disk | Hub trusts the schema-version PRAGMA; mismatch error tells the operator how to migrate or rebuild. |
| repo source → ripgrep guard | Guard sweep is the lockdown gate that prevents regressions to the singleton or legacy aliases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Information Disclosure | `cli/src/lib.ts` exports | accept | No secrets exposed; `loadConfig` is a function (no captured state); `Config` is a type. Old `configuration` value is removed, so even if a library consumer reached for it, the symbol is gone (compile-time error, not silent fallback). |
| T-10-04-02 | Tampering | `hub/src/store/index.ts` schema-mismatch error wording | mitigate | Error message contains DB path + version numbers + repair guidance only. No secrets. D-173 explicitly requires this telemetry; the test asserts the substrings so the wording cannot drift silently. |
| T-10-04-03 | Spoofing / Regression | Future re-introduction of singleton or legacy alias | mitigate | All five Phase-10 guard sub-checks active. Any future PR that re-introduces `configuration` import, `_set*` setter, `serverUrl` field, `webapp*` field, `name: 'server'`, or `migration-v*.ts` file fails the guard at PR-time. |
| T-10-04-04 | Tampering | Guard whitelist over-broad (false-negative risk) | mitigate | Sub-check #5 whitelist is explicit: `cli/src/configuration.ts`, `hub/src/configuration.ts`, `cli/src/lib.ts`, `cli/src/commands/types.ts`. Any new whitelist entry requires a documented justification (planner must explain in this PLAN before adding). |
| T-10-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan; package legitimacy gate not applicable. |
</threat_model>

<verification>
- `bash scripts/check-no-cut-agents.sh` exits 0 with `✅ Phase 10 guard PASS.` line in output.
- `bun typecheck` is green across the repo.
- `bun run test` is green across the repo.
- Spot-checks for ROADMAP Phase 10 SC1: `rg -nc '\bserverUrl\b|\bwebapp(Url|Host|Origin)\b' cli/src hub/src --glob '!**/*.test.ts' --glob '!hub/src/config/serverSettings.ts'` returns 0; `rg -nc "name:\\s*['\"]server['\"]" cli/src hub/src --glob '!**/*.test.ts'` returns 0; `rg -nc '_setApiUrl|_setCliApiToken|_setExtraHeaders' cli/src hub/src --glob '!**/*.test.ts'` returns 0.
- Spot-check for ROADMAP Phase 10 SC2: `find hub/src -name 'migration-v*.ts' | wc -l` returns 0; `cd hub && bun test src/store/index.test.ts -t "schema mismatch"` is green and asserts the D-173 repair substring.
- Spot-check for ROADMAP Phase 10 SC3: `rg -n 'export async function loadConfig' cli/src/configuration.ts hub/src/configuration.ts` returns exactly two hits (one per package); `rg -n "from ['\"]@/configuration['\"]" cli/src --glob '!**/*.test.ts' --glob '!cli/src/configuration.ts' --glob '!cli/src/lib.ts' --glob '!cli/src/commands/types.ts'` returns 0.
- Spot-check for ROADMAP Phase 10 SC4: `bun typecheck && bun run test` is green; `cd cli && bun test src/configuration.test.ts -t "frozen"` and `cd hub && bun test src/configuration.test.ts -t "frozen"` are both green.
</verification>

<success_criteria>
- `cli/src/lib.ts` exports `loadConfig` + `type Config` (no `configuration` singleton).
- `hub/src/store/index.ts` schema-mismatch error message satisfies D-173 (DB path + expected + found + offline-migration/rebuild guidance) with test coverage.
- D-174 schema-version bump decision is explicitly evaluated and recorded in `10-04-SUMMARY.md`.
- `scripts/check-no-cut-agents.sh` Phase-10 block has all five sub-checks active; `bash scripts/check-no-cut-agents.sh` exits 0 with the Phase-10 PASS line.
- `bun typecheck && bun run test` are green across the repo.
- ROADMAP Phase 10 success criteria 1, 2, 3, 4 are all demonstrably satisfied.
</success_criteria>

<output>
Create `.planning/phases/10-config-cleanup/10-04-SUMMARY.md` when done documenting: lib.ts export swap, D-173 message hardening (final wording + test assertion), the explicit D-174 schema-version decision (bump or no-bump + rationale), and the final guard-block enablement. Conclude with a one-line confirmation that ROADMAP Phase 10 SC1-SC4 are satisfied.
</output>
