---
phase: 10-config-cleanup
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - hub/src/store/index.ts
  - hub/src/store/index.test.ts
  - scripts/check-no-cut-agents.sh
autonomous: true
requirements: [REFC-01]

must_haves:
  truths:
    - "SCHEMA_VERSION bump audit is documented in the plan SUMMARY: either bump 10→11 with rationale, or note explicitly that no Phase-10 change tightened persisted-JSON decoding (D-174)"
    - "`hub/src/store/index.ts` schema-mismatch error includes DB path, expected version, found version, and a concise instruction pointing to the namespace-isolation offline migration tool or rebuild (D-173)"
    - "`hub/src/store/index.test.ts` asserts the D-173 message wording — the error string contains the substring `migrate` (or the canonical offline tool name) and either `back up` or `rebuild`"
    - "Phase-10 guard block in `scripts/check-no-cut-agents.sh` has all five sub-checks ACTIVE: (#1) `serverUrl` / `webapp(Host|Port|Url|Origin)` legacy field reads zero hits in production source; (#2) `_setApiUrl` / `_setCliApiToken` / `_setExtraHeaders` zero hits in production source; (#3) `name: 'server'` zero hits in `cli/src/commands/registry.ts`; (#4) zero `migration-v*.ts` files under `hub/src`; (#5) zero `getConfiguration()` / production-source singleton imports of `@/configuration` outside `cli/src/configuration.ts` and `hub/src/configuration.ts`"
    - "`bash scripts/check-no-cut-agents.sh` exits 0 with `✅ Phase 10 guard PASS.` after Plans 01-03 land"
    - "Phase gate green: `bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh` all exit 0"
  artifacts:
    - path: "hub/src/store/index.ts"
      provides: "SCHEMA_VERSION (possibly bumped per audit) + schema-mismatch error wording per D-173"
      contains: "SCHEMA_VERSION"
    - path: "hub/src/store/index.test.ts"
      provides: "D-173 message-content assertion (path + versions + offline-tool/rebuild instruction)"
    - path: "scripts/check-no-cut-agents.sh"
      provides: "Fully active Phase-10 guard block (5 sub-checks)"
      contains: "Phase 10"
  key_links:
    - from: "hub/src/store/index.ts::initSchema"
      to: "hub/scripts/migrate-namespace-isolation.ts"
      via: "schema-mismatch error message references the canonical offline migration tool path"
      pattern: "migrate-namespace-isolation"
    - from: "scripts/check-no-cut-agents.sh"
      to: "cli/src + hub/src production source"
      via: "five active rg/find sub-checks gating the phase"
      pattern: "Phase 10"
---

<objective>
Slice 4 of Phase 10: finalize the SQLite cleanup (schema-mismatch wording per D-173 + SCHEMA_VERSION bump audit per D-174), flip every staged Phase-10 guard sub-check to active in `scripts/check-no-cut-agents.sh`, and close the phase gate.

Purpose: REFC-01's SQLite half + the final guard sweep. Per RESEARCH §"Runtime State Inventory", the actual `migration-v*.ts` deletion is already done by prior phases; Phase 10's SQLite work is therefore tests + guard + schema-version audit + (possibly) a `SCHEMA_VERSION` bump. The guard sweep can only flip the legacy-field / setter / singleton-import checks to active AFTER Plans 02 and 03 have removed those patterns from production source (this is why these checks were staged with TODO markers in Plan 01).

Output: A green phase gate. `bash scripts/check-no-cut-agents.sh` prints the full Phase-10 PASS message; full `bun typecheck && bun run test` are green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-config-cleanup/10-CONTEXT.md
@.planning/phases/10-config-cleanup/10-RESEARCH.md
@.planning/phases/10-config-cleanup/10-PATTERNS.md
@.planning/phases/10-config-cleanup/10-01-SUMMARY.md
@.planning/phases/10-config-cleanup/10-02-SUMMARY.md
@.planning/phases/10-config-cleanup/10-03-SUMMARY.md
@hub/src/store/index.ts
@hub/src/store/index.test.ts
@scripts/check-no-cut-agents.sh

<interfaces>
<!-- Contracts the executor needs. -->

`hub/src/store/index.ts::SCHEMA_VERSION` — currently 10 per Phase 03 (`Plan 03-06 cuts runtime SQLite store to schema v10`). Phase 10's bump decision per D-174: bump only if Phase-10 changes have tightened persisted-JSON decoding such that old persisted data fails under strict schemas. Read Plan 03 SUMMARY for any zod schema tightening; the bump audit goes in this plan's SUMMARY.

Existing offline migration tool: `hub/scripts/migrate-namespace-isolation.ts` (per RESEARCH "Legacy Residue Map — Runtime SQLite migrations"). The schema-mismatch error message should point users at this script when applicable (or to "rebuild the database" when no migration tool exists for the version delta).

Phase-10 guard block from Plan 01 has these comment markers staged: `# Plan 04: enable after Plan 02 + Plan 03 land DI cutover`. This plan replaces those staged comment blocks with active sub-checks.

Guard whitelist precedent (from Phase 9 block + RESEARCH §"Code Examples"):
- (#1) `serverUrl` / `webapp(Host|Port|Url|Origin)` rejection: scope `cli/src hub/src`, exclude `**/*.test.ts` and `hub/src/config/serverSettings.ts` (the rejection helper itself MUST contain the literal `'serverUrl'` and `'webappHost'` strings; whitelist this file).
- (#2) `_setApiUrl|_setCliApiToken|_setExtraHeaders` rejection: scope `cli/src hub/src`, exclude `**/*.test.ts` (production source only).
- (#5) `getConfiguration\(\)` / singleton imports rejection: scope `cli/src hub/src`, exclude `**/*.test.ts`, `cli/src/configuration.ts`, `hub/src/configuration.ts`, `cli/src/__fixtures__/**`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SCHEMA_VERSION bump audit + D-173 message wording</name>
  <files>hub/src/store/index.ts, hub/src/store/index.test.ts</files>
  <read_first>
    - hub/src/store/index.ts (current SCHEMA_VERSION + initSchema mismatch error wording)
    - hub/src/store/index.test.ts (the schema-mismatch + missing-table tests added in Plan 01)
    - .planning/phases/10-config-cleanup/10-03-SUMMARY.md (so the executor can audit any zod schema tightening Plan 03 introduced)
    - .planning/phases/10-config-cleanup/10-CONTEXT.md (D-173, D-174)
    - hub/scripts/migrate-namespace-isolation.ts (or just `ls hub/scripts/` to confirm the offline tool path used in error wording)
  </read_first>
  <behavior>
    - The executor performs an audit: read 10-03-SUMMARY.md (and any Plan 02/03 source diffs accessible via git) for any zod schema or strict JSON parsing change that would make existing persisted JSON in `~/.hapi/hapi.db` fail to decode under the new code. Examples that would warrant a bump: stricter `MetadataSchema` parse (e.g. removing `.passthrough()` or rejecting unknown fields) on a column that holds free-form metadata. Examples that do NOT warrant a bump: changing config field names that never persist to the DB.
    - If a bump is needed: increment `SCHEMA_VERSION` from 10 to 11 in `hub/src/store/index.ts`. Update the schema-mismatch error message to include: (a) the DB path, (b) the expected version (the new constant), (c) the found version, (d) the instruction `Back up the database, then run \`bun run hub/scripts/migrate-namespace-isolation.ts\` if applicable, or rebuild the database from scratch.` (or equivalent wording per D-173). The plan SUMMARY documents WHAT changed in Phase 10 that triggered the bump.
    - If a bump is NOT needed: leave `SCHEMA_VERSION = 10` and only harden the error wording per D-173 (path + expected + found + back-up + offline-tool/rebuild instruction). The plan SUMMARY documents the audit conclusion: "Phase 10 made no change that tightens persisted-JSON decoding; SCHEMA_VERSION stays at 10."
    - `hub/src/store/index.test.ts` is augmented (or its mismatch test updated) to assert the new D-173 message wording: the error message contains the substring `migrate` OR the literal offline-tool path, AND either `back up` OR `rebuild` (case-insensitive). Existing path / expected / found assertions from Plan 01 are preserved.
  </behavior>
  <action>Read Plan 03 SUMMARY + grep `cli/src hub/src` for any new `z.object({...}).strict()` / `.parse(...)` introduced this phase that would touch persisted-DB data. Decide bump or no-bump. If bump: update `SCHEMA_VERSION` in `hub/src/store/index.ts`; rewrite the schema-mismatch throw to include DB path + expected + found + back-up + tool-or-rebuild instruction. If no bump: only update the throw wording (skip the constant change). In both cases, update `hub/src/store/index.test.ts` to assert the wording — match the substring `migrate` (or the offline-tool path) AND `back up` / `rebuild`. Document the audit decision in the executor's commit message + the eventual SUMMARY.md.</action>
  <verify>
    <automated>cd hub &amp;&amp; bun test src/store/index.test.ts &amp;&amp; bun typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd hub && bun test src/store/index.test.ts` exits 0 with the augmented assertion passing.
    - `rg -n 'SCHEMA_VERSION' hub/src/store/index.ts` returns at least one hit.
    - The schema-mismatch test asserts the message contains the substring `migrate` OR the offline-tool name (verifiable by reading the test source and confirming the `expect(err.message).toMatch(...)` or `.toContain(...)` call).
    - The schema-mismatch test asserts the message contains either `back up` or `rebuild` (case-insensitive ok).
    - `cd hub && bun typecheck` is green.
  </acceptance_criteria>
  <done>SCHEMA_VERSION audit decision recorded; D-173 wording landed; tests assert the wording.</done>
</task>

<task type="auto">
  <name>Task 2: Activate all Phase-10 guard sub-checks + close phase gate</name>
  <files>scripts/check-no-cut-agents.sh</files>
  <read_first>
    - scripts/check-no-cut-agents.sh (the Phase-10 block landed in Plan 01 — find the `# Plan 04: enable after ...` markers)
    - .planning/phases/10-config-cleanup/10-RESEARCH.md (§"Code Examples" — the proposed Phase-10 guard block source)
    - .planning/phases/10-config-cleanup/10-PATTERNS.md (§"scripts/check-no-cut-agents.sh — Phase-10 guard block")
    - hub/src/config/serverSettings.ts (whitelist for sub-check #1 — this file legitimately contains `'serverUrl'` and `'webappHost'` literals as part of OLD_SETTINGS_FIELDS)
    - cli/src/configuration.ts (whitelist for sub-check #5 — the file defining `loadConfig` is allowed to import its own helpers)
    - cli/src/__fixtures__/config.ts (whitelist for sub-check #5 — test factory)
  </read_first>
  <behavior>
    - The Phase-10 guard block in `scripts/check-no-cut-agents.sh` has FIVE active sub-checks (no `Plan 04: enable after...` TODO markers remain):
      * (#1) Legacy field reads: `rg -n '\bserverUrl\b|\bwebapp(Host|Port|Url|Origin)\b' cli/src hub/src --glob '!**/*.test.ts' --glob '!hub/src/config/serverSettings.ts'` returns zero hits → otherwise `echo "❌ Phase 10 D-160: legacy config field read in production source." ; exit 1`.
      * (#2) Mutable setters: `rg -n '_setApiUrl|_setCliApiToken|_setExtraHeaders' cli/src hub/src --glob '!**/*.test.ts'` returns zero hits.
      * (#3) `name: 'server'` alias rejection (already active from Plan 01 — preserve verbatim).
      * (#4) Runtime migration files: `find hub/src -name 'migration-v*.ts' | grep -q .` returns false (already active from Plan 01 — preserve verbatim).
      * (#5) Singleton imports: `rg -n 'getConfiguration\\(\\)|from .@/configuration.|from ..*configuration.\\)' cli/src hub/src --glob '!**/*.test.ts' --glob '!cli/src/configuration.ts' --glob '!hub/src/configuration.ts' --glob '!cli/src/__fixtures__/**'` returns zero hits, allowing only `import type { Config }` patterns. The exact regex MAY treat `import type { Config } from '@/configuration'` as a non-violation — easiest pattern: a `rg` invocation that filters out the `import type` form via a second `rg --invert-match` pipe, or a per-line check that excludes lines containing the literal `import type`.
    - Each active sub-check ends with a clear `❌ Phase 10 D-XXX: <message>` on failure and an `✅ Phase 10 D-XXX: <success>` on pass (matching the cadence of Phase-9 sub-checks at L400-412).
    - The block ends with `echo "✅ Phase 10 guard PASS."` and `bash scripts/check-no-cut-agents.sh` exits 0 against the post-Plan-03 codebase.
    - The block whitelists are minimal: production source is the target, tests + the two `configuration.ts` files + the `__fixtures__/` directory + (for #1 only) `hub/src/config/serverSettings.ts` are the only allowed exclusions.
  </behavior>
  <action>Replace each `# Plan 04: enable after ...` marker block in `scripts/check-no-cut-agents.sh` with a fully active sub-check per the behavior block. Mirror the Phase-9 cadence (read Phase-9 #1 at ~L400-412 — it captures `rg` output to a variable, counts hits, prints the offending lines on failure, prints a success line on pass). Use the exact whitelists in the interfaces block; do NOT add broad whitelists. For (#5), use the simplest correct approach to allow `import type { Config }` while rejecting value imports — e.g. an `rg` pattern that matches `import \{` (not `import type \{`) followed by a value import from `@/configuration`. Run the full guard sweep + phase gate; if any sub-check trips on legitimate code that should be whitelisted, narrow the whitelist with a comment explaining why (e.g. "test factory whitelisted — D-170 fixture pattern"). Do NOT broaden whitelists to swallow unintended hits — those indicate Plans 02/03 left residue and must be fixed at the source.</action>
  <verify>
    <automated>bash scripts/check-no-cut-agents.sh &amp;&amp; bun typecheck &amp;&amp; bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-no-cut-agents.sh` exits 0 and the output contains `✅ Phase 10 guard PASS.`.
    - `rg -n 'Plan 04: enable' scripts/check-no-cut-agents.sh` returns zero hits (no staged TODOs remain).
    - `rg -n 'Phase 10 D-160|Phase 10 D-164|Phase 10 D-166|Phase 10 D-172' scripts/check-no-cut-agents.sh` returns at least four hits (one per active sub-check that references a decision ID).
    - `bun typecheck` is green at the repo root.
    - `bun run test` is green at the repo root (full suite).
    - Final phase gate: `bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh` exits 0.
  </acceptance_criteria>
  <done>Phase-10 guard block fully active; phase gate green; Phase 10 ready for `/gsd-verify-work`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → SQLite file on disk | Hub trusts `PRAGMA user_version`; mismatch → fail-closed startup with operationally useful error. |
| repo → CI / phase gate | Guard script enforces the absence-of-residue contract for Phase 10. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | SQLite database with mismatched schema version | mitigate | `Store.initSchema` throws fail-closed with DB path + expected/found versions + offline-tool/rebuild instruction (D-173). Asserted by augmented test. |
| T-10-04-02 | Information Disclosure | Schema-mismatch error wording | accept | Error includes DB path; no secrets are stored in path names (single-user `~/.hapi/hapi.db` per PROJECT.md). Risk accepted. |
| T-10-04-03 | Tampering | Phase-10 regressions sneaking in via future commits | mitigate | Five active guard sub-checks reject legacy-field reads, mutable setters, the `server` alias, runtime `migration-v*.ts` files, and singleton imports. Whitelists are minimal and decision-IDed. |
| T-10-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan; package legitimacy gate not applicable. |
</threat_model>

<verification>
- `bun typecheck` (repo root) green.
- `bun run test` (repo root) green — all CLI + Hub + Web suites + test:guard.
- `bash scripts/check-no-cut-agents.sh` exits 0 with `✅ Phase 10 guard PASS.`.
- `rg -n 'Plan 04: enable' scripts/check-no-cut-agents.sh` returns zero hits.
- ROADMAP Phase 10 success criteria all met:
  1. Zero `serverUrl` / `webapp*` legacy reads, zero `hapi server` alias, zero `_set*` callsites — enforced by guard.
  2. SQLite schema-version mismatch fails startup with offline-tool/rebuild instruction — D-173.
  3. CLI + Hub `loadConfig()` return frozen objects; consumers receive config via DI; no module-level mutable singleton — Plans 02/03.
  4. `bun typecheck` + `bun run test` pass; frozen-mutation tests assert mutation throws — Plans 02/03.
</verification>

<success_criteria>
- SCHEMA_VERSION audit decision recorded in SUMMARY (bump or no-bump with rationale).
- D-173 message wording landed in `Store` constructor + asserted by test.
- All five Phase-10 guard sub-checks active with minimal, decision-IDed whitelists.
- Repo-wide `bun typecheck && bun run test && bash scripts/check-no-cut-agents.sh` exit 0.
- Phase 10 ready for `/gsd-verify-work` (all 4 ROADMAP success criteria met).
</success_criteria>

<output>
Create `.planning/phases/10-config-cleanup/10-04-SUMMARY.md` documenting: SCHEMA_VERSION audit decision (bump or no-bump + rationale per D-174), D-173 wording landed, 5 active guard sub-checks with whitelist rationale, full phase gate green, and the four ROADMAP Phase-10 success criteria mapped to the artifacts that satisfy them.
</output>
