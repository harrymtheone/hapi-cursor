import { describe } from 'bun:test'

describe('machines routes', () => {
    // OpenCode model listing endpoints removed in CUT-04; remaining
    // machine routes are exercised via integration tests.
})
